[package]
name = "zero-ui-adapter-android"
description = "Android 平台适配器 — Android 触摸/键盘/IME/back→UiEvent 转换 + PlatformRuntime 实现（不依赖 winit）"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
zero-ui-core = { workspace = true }
zero-ui-runtime = { workspace = true }
zero-ui-gestures = { workspace = true }
