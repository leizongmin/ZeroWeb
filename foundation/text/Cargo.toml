[package]
name = "zero-text-foundation"
description = "共享文本/字体基础层 — 字体发现/fallback/shaping/bidi/line break/grapheme/glyph cache（UI SDK 与 WebView 共用，浏览器无关）"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
serde = { workspace = true, features = ["derive"] }
thiserror = { workspace = true }
hashbrown = { workspace = true }
# DC-11 / TBD-8：真实文本后端（fontdue 度量/光栅 + rustybuzz OpenType shaping）。
# 均为纯字体 crate（无 GPU 后端），workspace 已声明，license MIT/Apache-2.0。
fontdue = { workspace = true }
rustybuzz = { workspace = true }
