# M3 切片 8 第二增量（第六小步，进行中）— slotchange signalSet 架构迁移（web-components）

- 日期：2026-09-11
- 对照：`2026-09-11-wc-m3s8f.md`（3532/4763）
- 成绩：**3532/4763（74.2%）净 0 零回归**（架构迁移轮——全文件级 net-neutral，孤立
  filtered run 的 MO 交错 4F 改善在文件内上下文中被其余簇的时序位移抵消）
- 全量 JSON：`2026-09-11-wc-m3s8g.json`

## 架构变更（纯 JS shim：part01/part03/part05）

### slotchange 派发迁移至 spec signalSet 模型

- **spec 依据**（notify mutation observers）：①slotchange 的 `bubbles: true`（旧实现
  false——嵌套 slot 案的上游监听经 assigned-slot 父边可达的前置）；②派发时机 = MO
  复合微任务内、每个 observer 回调投递之后（signalSet 逐轮排空 + 无 observer 兜底）；
  ③MO 回调内断言「slotchange 尚未 fire」的分界成立。
- 实现：`__zwSlotSignalRoots` 全局 signalSet（plain/handle 双形态条目）+ `_mo_scheduleFlush`
  逐 observer 投递后排空（`__zwFlushSlotSignals` → `__zwFlushPlainSlotSignals` /
  `__zwFlushHandleSlotSignals`）+ 队列函数改 push + `__zw_mo_flush_lite()`。
- 教训：派发体从队列函数闭包提出为全局函数时，局部 helper（flattenMap/diffInto）必须
  随迁——ReferenceError 被 try/catch 吞成全量不 fire（filtered run 曾全挂，探针定位）。

## 已知未决（本轮实测）

- filtered run 改善的 MO 交错 4F 在全文件上下文中回退（net 0）——**文件内测试间状态
  污染 + 嵌套 slots 的 signal 级联顺序**（外 slot 事件先于内 slot 自身事件到达——
  1st fire target 断言反序）。下一轮：①signal 级联顺序（内树信号先于外树）；②
  removed-immediately 4F（R52 身份二相）；③innerHTML 尾计数。
- 门禁：fmt/clippy 全过；make test 结果见提交说明。
