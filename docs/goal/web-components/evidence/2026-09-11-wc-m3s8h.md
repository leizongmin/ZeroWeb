# M3 切片 8 第二增量（第七小步）— nested slots 4F 全清（signal 深度序派发）（web-components）

- 日期：2026-09-11
- 对照：`2026-09-11-wc-m3s8g.md`（3532/4763）
- 成绩：**3536/4763（74.2%）净 +4，零回归**
- 全量 JSON：`2026-09-11-wc-m3s8h.json`
- 门禁：fmt/clippy 全过；`make test` 全绿

## 修复内容（纯 JS shim：part03）

### nested slots 4F 全清（signal 派发深度序）

- **根因**（flush 序探针）：signalSet 按 roots 入队序派发（[outerShadow, innerShadow]）
  ——外层 slot 的变异信号先于内层 slot 的 setup 信号 fire，inner listener 的 1st fire
  收到的是 outer 事件经 assigned-slot 父边的冒泡（期望 1st = inner 自身、2nd = outer）。
- **修**：`__zwFlushSlotSignals` 派发前按 shadow 树深度**降序**排序（最深的 shadow 树
  最先 flush——其 slot 信号是级联源头；外层 slot 的信号随后经 assigned-slot 父边冒泡
  到内层监听）。深度计算：root → host → 宿主所在树根 → 若为 shadow root 则 +1 递归
  （`__zwSlotRootDepth`，探针实证 outer=1 / inner=2）。
- 教训：首版排序写成**升序**（浅先）——方向反了致 16F 原样；二分定位后改降序即清。

## 成绩分布

| 文件 | 前 | 后 |
|---|---:|---:|
| slotchange-event（nested slots 4 变体） | ✗×4 | ✓×4 |

## 余项

- slotchange 尾 8 案（两簇）：①removed-immediately 4F——被移除 slot 的 proxy 身份
  二相（R52 消零重建 vs 页面持有引用，`__n111 vs __n111` 同句柄不同代理）；②
  end-of-microtask 4F——signal 与 MO records 的投递分界（per-observer 排空 vs 尾部
  排空的轮次语义）；③innerHTML 尾计数 4F——async host apply 过度标记。
- form-associated 簇（attachInternals 等）：goal Support Envelope 排除，等用户点名。
- DC-5 终判。

## 累计

| 阶段 | Pass/Total | 通过率 |
|---|---:|---:|
| M1 基线 | 437/4730 | 9% |
| M3 切片 8 第二增量第六小步 | 3532/4763 | 74.2% |
| **M3 切片 8 第二增量第七小步** | **3536/4763** | **74.2%** |
