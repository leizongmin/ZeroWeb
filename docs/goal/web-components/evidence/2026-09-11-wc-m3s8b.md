# M3 切片 8 第二增量（第一小步）— 轻量 shadow EventTarget 面 + post-dispatch target 恢复（web-components）

- 日期：2026-09-11
- 对照：`2026-09-11-wc-m3s8.md`（3425/4762）
- 成绩：**3427/4762（72%）零回归，净 +2**（event-composed-path-with-related-target +2）
- 全量 JSON：`2026-09-11-wc-m3s8b.json`
- 门禁：fmt/clippy + `make test` 全绿

## 修复内容

### ① 轻量 shadow 的 EventTarget 面（addEventListener/removeEventListener）

- plain 宿主 attachShadow 的轻量 shadow 对象缺 `addEventListener`——WPT helper
  （dispatchEventWithEventLog / dispatchEventWithLog）沿 parentNode 链挂站 listener 时
  于 shadowRoot 站硬抛 'node.addEventListener is not a function'（event-post-dispatch
  的 ShadowRoot 变体 4 案 + event-inside-slotted-node 全簇前置断言）。
- 存储走 `this._zwEvLs[type]`——R167 工厂派发 fireStation 的 view 站读取域（零新增
  读取面，shadow root 站入链即 fire——第一增量的链路直接受益）。

### ② R167 dispatch 结束恢复 ev.target = 原始 target

- per-station retarget 的逐站改写此前外溢到 post-dispatch（spec：dispatch 结束
  target = 原始 target）。与 _dispatchWithBubble finally 的 target 恢复对齐。

## 余项（挂账更新）

- R114（handle 世界）slot detour + shadow root 站 + retarget：首版回退的两坑
  （`_zwNodeParent` 上行环、`_dispatchToListeners` thisObj 契约）——三步重设计待实施
  （本步已单独消解轻量 shadow 的 EventTarget 面，属三步外的独立前置）
- event-post-dispatch 尾案（wrapper 身份二相——target/currentTarget identity 断言）
- relatedTarget retarget 过滤（event-with-related-target 族）
- slotchange 尾 12 案、disabledFeatures×attachShadow（尾 2 案）、
  Rust `resolve_slots` 接线（渲染级等点名）、DC-5 终判

## 累计

| 阶段 | Pass/Total | 通过率 |
|---|---:|---:|
| M1 基线 | 437/4730 | 9% |
| M3 切片 7 | 3413/4762 | 72% |
| M3 切片 8 第一增量 | 3425/4762 | 72% |
| **M3 切片 8 第二增量（第一小步）** | **3427/4762** | **72%** |
