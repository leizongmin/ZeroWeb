# M3 切片 8（第一增量）— plain 世界 composed path / shadow root 站 / retarget（web-components）

- 日期：2026-09-11
- 对照：`2026-09-11-wc-m3s7.md`（3413/4762）
- 成绩：**3425/4762（72%）零回归，净 +12**（event-composed 2/9 → 9/9、
  event-composed-path-with-related-target +2、capturing-and-bubbling-across-shadow-trees
  +1、event-composed-path +1、event-dispatch-order +1）
- 全量 JSON：`2026-09-11-wc-m3s8.json`
- 门禁：fmt/clippy + `make test` 全绿

## 修复内容（探针定位 → 三线实施）

### ① 展平子 parentNode 反链指 canonical 容器 wrapper（R97/handle-flatten 双点）

- `shadowRoot.appendChild(importNode(...))` 的展平子 parentNode 残留 importNode 空壳
  （'#document-fragment' 散 wrapper，同 handle 不同对象）——R167 派发链上行时 shadow
  root 站 listener key（'@'+handle）失配。两处展平点（R97 无 handle fragment、
  `__zw_append_fragment_children_handle` 后）补 `_zwForceParentLink(child,
  _makeProxy(null, containerHandle))`（per-key 缓存 = 页面持有的 ShadowRoot wrapper）。

### ② R167 派发链：shadow root 站 + composed 跨边界续推 host + light 祖先

- shadow root 容器站（nodeType 11 + `_shadowHandles` 印记）入链：非 composed 止于它
  （path = [target, shadowRoot]）；composed 续推 host（`_shadowHandleMeta` 取）并沿
  host 的 parentNode 收 light 祖先（detached 树 path = [target, shadowRoot, host]）。

### ③ per-station shadow-adjusted target retarget + composedPath 生命周期

- fireStation 逐站设 ev.target：边界内站 = target 本身、host 站及以上 = host（capture
  逆序逐站复位——WPT 'target at 0 should be same' 断言）。
- dispatch 期 `ev._composedPath = chain167`（composedPath() 读同源）、dispatch 结束清
  null（spec 非 dispatch 返 []——event-post-dispatch 断言，与 R3244 finally 同语义）。

### ④ native MouseEvent composed init 修补（R109 推广）

- native V8 MouseEvent 模板恒设 composed=false、丢弃 init dict composed（KeyboardEvent
  R109 注记同源）——包一层以 init dict 为事实源（'A synthetic MouseEvent with
  composed=true' 簇）。

### ⑤ UA click 合成为 composed

- 工厂 `click()` 的合成 click（spec click 事件定义 composed: true）——WPT
  'A UA click event should not be scoped'。

## 余项（挂账更新）

- handle 世界 `_dispatchWithBubble` 的 shadow root 站/retarget 同构化（其 R114 链已跨
  边界但 shadow root 本体不入站、target 单点设置）——event-inside-slotted-node（20）、
  Extensions-to-Event-Interface（12）等
- relatedTarget retarget 过滤（event-with-related-target 18 案——relatedTarget 与 target
  同 shadow-including tree 站的 skip 规则）
- slotchange 尾 12 案、disabledFeatures×attachShadow（尾 2 案）、
  Rust `resolve_slots` 接线（渲染级等点名）、DC-5 终判

## 累计

| 阶段 | Pass/Total | 通过率 |
|---|---:|---:|
| M1 基线 | 437/4730 | 9% |
| M3 切片 5 | 3142/4762 | 66% |
| M3 切片 6 | 3145/4762 | 66% |
| M3 切片 7 | 3413/4762 | 72% |
| **M3 切片 8（第一增量）** | **3425/4762** | **72%** |
