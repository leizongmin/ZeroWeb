# M3 切片 8 第二增量（第八小步）— MO flush 预算外调度（web-components，基础设施硬化）

- 日期：2026-09-11
- 对照：`2026-09-11-wc-m3s8h.md`（3536/4763）
- 成绩：**3536/4763（74.2%）净 0，零回归**（基础设施硬化轮）
- 全量 JSON：`2026-09-11-wc-m3s8i.json`

## 变更（纯 JS shim：part01）

### MO flush 调度绕过 _deferBudget（spec 正确性硬化）

- `_mo_scheduleFlush` 的 flush 体从 `_defer`（预算计数、耗尽静默丢弃）改为直接
  `queueMicrotask`（无 queueMicrotask 环境回落 `_defer`）。spec：notify mutation
  observers 是 microtask——不可被页面级脚本预算丢弃。长测试文件（slotchange-event
  61 subtest 连续变异）中途耗尽 256 预算使后续 flush 调度丢失的隐患消除。

## 实测结论（end-of-microtask 簇根因再收窄）

- 预算外调度落地后 end-of-microtask 4F 原样（net 0）——**预算耗尽非该簇根因**。
- 簇根因收窄至 **MO 投递机制的分轮语义**：attribute 记录（MO1，attributeFilter id）
  与 childList signal 的投递门控不同轮——signal 在 MO1 投递前已排空。下一轮修法：
  MO 投递分轮对齐（attribute/childList 记录同轮投递后再排空 signalSet），或 signal
  排空挪至 MO 投递轮完成后（现挂 observer 回调后 + 尾部，均为同轮——需查 attribute
  记录是否走了延迟投递分支 `_mo_deliverToId` 的 subtree 门控）。

## 余项

- slotchange 尾 8 案（end-of-microtask 4F——上记 MO 投递分轮；removed-immediately
  4F——R52 身份二相）
- slotchange innerHTML 尾计数 4F——async host apply 过度标记
- DC-5 终判（make test + clippy + reftest 持续全绿）

## 累计

| 阶段 | Pass/Total | 通过率 |
|---|---:|---:|
| M1 基线 | 437/4730 | 9% |
| M3 切片 8 第二增量第七小步 | 3536/4763 | 74.2% |
| **M3 切片 8 第二增量第八小步（硬化）** | **3536/4763** | **74.2%** |
