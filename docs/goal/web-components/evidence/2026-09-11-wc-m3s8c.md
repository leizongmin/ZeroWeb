# M3 切片 8 第二增量（第二小步）— R114/R167 事件路径同构化 + relatedTarget 全语义面（web-components）

- 日期：2026-09-11
- 对照：`2026-09-11-wc-m3s8b.md`（3427/4762）
- 成绩：**3494/4763（73.4%）净 +67，零回归**
- 全量 JSON：`2026-09-11-wc-m3s8c.json`（分母 +1：某 case 走得更深多出 1 subtest）
- 门禁：fmt/clippy 全过；`make test` 见提交说明（send_keys 3 案单跑全绿，全量跑疑并行 flake）

## 修复内容（纯 JS shim：part03/part05）

### ① handle 世界 `_dispatchWithBubble` 上行建链三规则重写（R114 ② 三试，spec 直译落地）

- **slot detour**：直接父是 shadow host（`_shadowRoots['@'+P]` 命中）时按 spec
  find-a-slot（named 树序 DFS 复用 `__zwFindSlotInHandleShadow` / manual 列表匹配）
  步进到 assigned slot（light→shadow 穿越；分配与 open/closed 无关——closed 变体
  此前 1 站的根因就是旧链在 closed 时直接断）。
- **shadow root 站**：root 本体站入链（非 composed 止于 root；旧「root 无 listener
  站直接断链」丢站）。
- **per-station shadow-adjusted target**：`adj` 逐站记录 + `_r114ApplyAdj` 无条件
  设置（capture 逆序由外向内收敛回 null 须逐站复位）；retarget 只在「跨出 adjusted
  所在 shadow 树」（root→host 且 adjusted 树根 === root）时发生——light target 穿入
  穿出不 retarget（WPT event-inside-slotted-node 全程 target 断言）。
- `_r114Seen` 身份查重（首版 86 站失控的最后防线）+ `_r114CurDepth` 随 detour +1/
  crossing -1（window.event 抑制段判定）；`_r114Entry` 透传 `shadow`/`adj`（旧实现
  丢字段使 capture/bubble 抑制恒关）。

### ② plain 世界 R167 同构化（createTestTree 克隆树世界）

- plain 轻量 shadow root 站（nodeType 11 + `.host` + 无印记）识别 + composed 跨界
  `cur167 = cur167.host; continue`（host 站由主循环顶入链，嵌套 shadow 双 crossing
  自然续处理——test4-7 的 open>closed 等 4 形态）。
- plain slot detour（`.shadowRoot` 持轻量 shadow 的宿主 + `__zwFindSlotInShadow`/
  `__zwFindManualSlotInShadow`）。
- per-station adjusted target 用 `_s8Adj167` 与 chain167 对位；proxy 世界粗粒度
  `_s8RootIdx`/`_s8HostStation` 机制保留兜底（两世界一次派发互斥）。

### ③ relatedTarget 全语义面（spec DOM §dispatch 直译）

- 世界无关 helpers：`_zwIsSRNode`/`_zwNodeRootOf`/`_zwSiiDesc`（shadow-including
  inclusive descendant）/`_zwRetargetRel`（spec retargeting 算法）。
- 每站 retargeted relatedTarget（R167 `_s8Rel167` 对位；fireStation 设置
  `ev.relatedTarget`）——WPT 第三列断言。
- **停止规则**：step 的 first-branch（adjusted 树根是 parent 的 sii 祖先）失败且
  parent === retargeted relatedTarget → path 止（WPT 'should stop at the shadow
  root' 两案从「意外过（旧链短）→ 回归 → 按规范恢复」）。
- **空路径 guard**：target === retargeted relatedTarget 且 ≠ original → 零站派发
  （capture/target/bubble 全跳——test4 case3 零断言）。
- **clearTargets**（最后 adjusted 站的 adjusted/relatedTarget 任一是 root 为
  shadow root 的节点）→ post target/relatedTarget = null；否则 post target = 最外
  站 walk-back 值（open/composed → host；无跨界 → 原始 target）。

### ④ composedPath() closed shadow tree 隐藏（spec dom-event-composedpath）

- `__zwCpClosedFilter`：条目 E 隐藏 ⟺ E 的 shadow-including 祖先链上存在「树根为
  closed shadow root 且 currentTarget 不在该树内」的节点；两个 getter（part03 工厂
  + part05 R138 原型面）同源接入；非 dispatch（currentTarget null）不过滤。
- 验证全型：closed 单树、open>closed、closed>open、closed>closed、closed slot、
  双 closed 嵌套的 per-station 截断面（test3/5/6/7/9/11）。

## 成绩分布

| 文件 | 前 | 后 |
|---|---:|---:|
| event-inside-slotted-node | 0/20 | 20/20 |
| event-inside-shadow-tree | 0/12 | 12/12 |
| event-post-dispatch | 3/16 | 15/16 |
| event-composed-path | 1/11 | 11/11 |
| event-composed-path-with-related-target | 4/13 | 11/13 |
| event-post-dispatch-no-listeners | 0/5 | 5/5 |
| capturing-and-bubbling | 1/5 | 2/5 |

## 余项（根因已定位）

- **event-composed-path-with-related-target every-slots 2F**：plain 克隆宿主
  `.shadowRoot` 读值 ≠ attachShadow 注册对象（own data property 存在但值是被
  二次克隆的 B-shadow——`foundViaHostIsSlot4=false`/`foundViaSr4IsSlot4=true`），
  detour 落 B 宇宙站上无页面 listener。需 identity 稳定的 shadow 查找（顺 A 宇宙
  `_zwForceParentLink` 链或修正 getter 写回序）。
- **event-with-related-target 18F**：自有 log 形态（label 数组），labels 全
  undefined——其专用 helper/树构建的世界归属未查，独立小切片。
- capturing-and-bubbling 余 3F、event-composed-path-after-dom-mutation 1F、
  slotchange 尾 12 案、disabledFeatures×attachShadow 尾 2 案照旧。

## 累计

| 阶段 | Pass/Total | 通过率 |
|---|---:|---:|
| M1 基线 | 437/4730 | 9% |
| M3 切片 8 第一增量 | 3425/4762 | 72% |
| M3 切片 8 第二增量第一小步 | 3427/4762 | 72% |
| **M3 切片 8 第二增量第二小步** | **3494/4763** | **73.4%** |
