# M3 切片 2 — slot plain 面 IDL + fallback flatten + text handle 序列化（web-components）

- 日期：2026-09-10
- 对照：`2026-09-10-wc-m3s1.md`（3046/4752）
- 成绩：**3050/4752（64%）零回归，净 +4**（slots.html +4——克隆树 assignedSlot/slot IDL）
- 全量 JSON：`2026-09-10-wc-m3s2.json`
- 门禁：make test 全绿（v8 16839P）+ fmt/clippy

## 修复内容

### ① plain wrapper 的 slot IDL 面

- cloneNode/解析产物的 `_zwMEl` wrapper 无 part04 trap——`c1.slot`/`c1.assignedSlot` 恒
  undefined（WPT shadow-dom/slots.html 的 createTestTree 克隆树断言面）。补 `slot`
  get/set（attrs 反射）与 `assignedSlot`（plain 世界 shadow 上溯）。

### ② plain 世界的 shadow 上溯

- `__zwSlotForSlottable` 补 plain 分支：沿 parentNode 上溯找轻量 shadow（`host.shadowRoot`
  own 槽——part03:466 attachShadow 的产物），shadow.childNodes 内 name 匹配 slot。

### ③ fallback flatten 语义

- `assignedNodes({flatten})` 在 matched 空时 fallback 直接成为输出（spec：空 slot 的
  flatten 树含 fallback 内容——WPT slots-fallback Basic）；handle/plain 双实现同步。

## 余项（M3 切片 3）

- slots-fallback 簇（fallback 递归在 sel-clone 混合树的身份贯通——probe 显示 plain flatten
  逻辑正确但 createTestTree 混合 handle/plain 树的 assignedNodes 分派域仍分裂）
- imperative-slot-api（手动分配）；Rust `resolve_slots` 接线（shadow.rs → engine 消费）

## 累计

| 阶段 | Pass/Total | 通过率 |
|---|---:|---:|
| M1 基线 | 437/4730 | 9% |
| M3 切片 1 | 3046/4752 | 64% |
| **M3 切片 2** | **3050/4752** | **64%** |
