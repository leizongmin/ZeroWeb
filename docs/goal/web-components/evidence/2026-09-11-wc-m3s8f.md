# M3 切片 8 第二增量（第五小步）— disabledFeatures×attachShadow 全清 + clearTargets rel 维度（web-components）

- 日期：2026-09-11
- 对照：`2026-09-11-wc-m3s8e.md`（3529/4763）
- 成绩：**3532/4763（74.2%）净 +3，零回归**
- 全量 JSON：`2026-09-11-wc-m3s8f.json`
- 门禁：fmt/clippy 全过；`make test` 全绿

## 修复内容（纯 JS shim：part03/part05）

### ① disabledFeatures=['shadow'] × attachShadow（尾 2 案全清）

- **define 侧**（`_ceDefine`）：best-effort 读取 `ctor.disabledFeatures` 序列（iterable/
  array-like 双形态，String 归一）存 registry entry.disabledFeatures（Get 顺序与异常
  rethrow 的完整 spec 面属 CustomElementRegistry 簇，另行挂账）。
- **attachShadow 侧**（part05 `_attachShadow`）：safelist 校验后查 `_ceEntryFor` 的
  entry.disabledFeatures 含 'shadow' → NotSupportedError。**教训**：判定抛出初版写在
  查找兜底 try 内被自身 catch 吞（探针 probeHas/probeDis 全对但 attachErr none）——
  抛出必须在兜底 try 外。
- 覆盖：autonomous + customized built-in（`{is}` 经 `_ceEntryFor` 的 is 反查）双形态；
  `['SHADOW']` 大小写敏感不触发 ✓；无定义元素不触发 ✓；二次 attachShadow 的既有
  NotSupportedError 优先序不变 ✓。

### ② clearTargets 的 relatedTarget 维度（handle 世界，尾 1F 全清）

- spec dispatch 建链末段 clearTargets：最后一个 adjusted 非空站的 adjusted target 或
  其 retargeted relatedTarget 任一是「root 为 shadow root 的节点」→ post target/
  relatedTarget 置 null。R167 侧已有（`_s8ClearTargets`），handle 世界缺 rel 维度
  （WPT event-post-dispatch 'moved out of the shadow tree' 的 relatedTarget 清空断言）。
- 实现：`_r114LastAdjRel`（站 0 = target 站 retargeted rel，跨界 retarget 更新为 host
  站值）+ `_r114ClearTargets`（`_zwNodeRootOf` 判 shadow 树内）+ finally 联动清
  target/relatedTarget（与 `_r114PostRootEnd` 并集）。

## 成绩分布

| 文件 | 前 | 后 |
|---|---:|---:|
| Element-interface-attachShadow-custom-element | 4/6 | 6/6 |
| event-post-dispatch | 15/16 | 16/16 |

## 余项

- **slotchange 尾 12 案**（四簇，独立切片）：①removed-immediately 4F——被移除 slot 的
  proxy 身份二相（R52 消零重建 vs 页面持有引用，`__n111 vs __n111` 同句柄不同代理）；
  ②MO 交错 4F——slotchange 微任务须在 mutation records 投递之后同一复合微任务内；
  ③嵌套 slots 4F——内 slot 分配变化须连带标记外 slot（diff 链嵌套面）；④innerHTML
  尾计数 2F——async host apply 下的过度标记。
- form-associated 簇（attachInternals/formDisabledCallback）：goal Support Envelope
  明确排除（只做评估记录，等用户点名）。
- DC-5 终判（make test + clippy + reftest 持续全绿）。

## 累计

| 阶段 | Pass/Total | 通过率 |
|---|---:|---:|
| M1 基线 | 437/4730 | 9% |
| M3 切片 8 第二增量第四小步 | 3529/4763 | 74.1% |
| **M3 切片 8 第二增量第五小步** | **3532/4763** | **74.2%** |
