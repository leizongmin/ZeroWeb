# M3 切片 5 — name-mode slotchange diff 化（flatten 口径 + marked-transition 链）（web-components）

- 日期：2026-09-10
- 对照：`2026-09-10-wc-m3s4.md`（3137/4763）
- 成绩：**3142/4762，净 +5**（+6/−1：slotchange-event must-not-fire +4、
  slotchange-customelements +1、inserting-fragment-under-shadow-host +1；
  slotchange.html 'Slotchange should be fired' 1 案 Pass→挂起——控制面预判的 diff 化
  迁移案，s2 直读分配不变、仅 flatten 变化的嵌套形态）
- 全量 JSON：`2026-09-10-wc-m3s5.json`
- 门禁：fmt/clippy + `make test` 全绿
- 载荷注记：slotchange.html 在低载过滤跑下可达 28 Pass（全量并行载下 runner 页级
  超时截断 async 完成数）——记录为已知测量噪声，证据以全量 JSON 为准

## 修复内容

### ① slotchange 派发口径：eager 全树派发 → flatten 序列 diff

- 依据 upstream 期望矩阵收敛出的唯一自洽口径：**slot 的 flatten 分配序列变化才 fire**。
  - must-not-fire 簇（他 slot 变化我不 fire）：flatten 不变不标 ✅
  - fallback 内容变化 fire 簇：direct 读不变但 flatten 变 ✅（direct-only 口径挂此簇）
  - eager 口径挂 must-not-fire 簇（此二者不可兼得，flatten 是唯一双簇自洽口径）
- plain（part03 `__zwQueuePlainSlotchange`）与 handle（part05
  `__zwQueueSlotchangeForRoot`）双域同构：marked-transition 链（轮内相邻快照 diff 增量
  并入 marked）+ 微任务双基 sweep（prev 链 + last 终态）+ 树序派发（出树 slot 追加）。

### ② innerHTML 宿主子替换接入队列

- part04 innerHTML setter 桥接后补 `__zwMaybeQueueSlotchange`（宿主子整体替换的分配面
  变化此前不入队——innerHTML 家族前置断言转绿，尾断言仍差一）。

## 余项（挂账更新）

- slotchange-event 尾 12 案三族：
  - mutation-observer 交错（4）——需 MutationObserver 集成（独立特性）
  - 嵌套 slotchange retarget（4，"must bubble"——事件路径经 slot 分配 retarget 到
    上层 slot 侦听器）——dispatch 层 composed-path 深化
  - transient slot（4）+ innerHTML 尾断言——wrapper 身份二相（同节点双 wrapper 各持
    `__n78` 标签）+ R100 文本节点不入 registry 的可见性交互
- Rust `resolve_slots` 接线（shadow.rs → engine 查询消费）+ DC-5 终判

## 累计

| 阶段 | Pass/Total | 通过率 |
|---|---:|---:|
| M1 基线 | 437/4730 | 9% |
| M3 切片 2 | 3050/4752 | 64% |
| M3 切片 3 | 3105/4763 | 65% |
| M3 切片 4 | 3137/4763 | 66% |
| **M3 切片 5** | **3142/4762** | **66%** |
