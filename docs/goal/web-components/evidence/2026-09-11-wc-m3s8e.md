# M3 切片 8 第二增量（第四小步）— every-slots 全清 + composedPath mutation 稳定视图（web-components）

- 日期：2026-09-11
- 对照：`2026-09-11-wc-m3s8d.md`（3525/4763）
- 成绩：**3529/4763（74.1%）净 +4，零回归**
- 全量 JSON：`2026-09-11-wc-m3s8e.json`
- 门禁：fmt/clippy 全过；`make test` 全绿

## 修复内容（纯 JS shim：part03/part05）

### ① R366 walk 的 template content 下钻消除（every-slots 2F 根因，结构性修复）

- **根因定位**（attachShadow 调用序列探针）：createTestTree walk 对 6 个 template 触发
  11 次 attachShadow（host2 ×2 / host4 ×2 / host5 ×4）——ShadowRoot.prototype 的 R366
  子树 walk（part03:708）对每个元素子递归，**下钻 template 元素的 content childNodes**
  （真实 qSA 不下钻 contents——contents 在独立 fragment）→ 同一嵌套 template 的多代
  拷贝被重复装配 → ids 表跨代混写（n.host4 / n.sr4 / n.slot4 来自不同代）→
  `host.shadowRoot !== attachShadow 返回值` 身份分裂 → detour 站无页面 listener。
- 修：R366 walk + 其镜像（template content 视图 qSA，"镜像 R366 walk"）对 template
  元素**不再下钻**（与 `_zwMOuterHtml` 的 WC-M3 切片 3 序列化排除同口径）。content 内
  查询走 `template.content.querySelector` 入口（root = content fragment 本体）不受影响。

### ② composedPath() mutation 稳定视图（spec dom-event-composedpath getter 直译）

- 旧 `__zwCpClosedFilter` 在**调用时**活爬 parentNode 判 closed 隐藏——listener 内
  DOM mutation 后再读 composedPath 结果漂移（WPT event-composed-path-after-dom-
  mutation 的 path_before/path_after 断言）。
- 重写为 spec 的 **hidden-level 计数算法**：per-item 预计算 flags（`rootClosed` = 该站
  是 closed shadow root；`slotInClosed` = 该站是 closed 树内 slot，spec
  slotInClosedTree）在派发期随站记录（`_composedPathFlags` 与 path 对位，finally 清理），
  getter 按 currentTargetIndex 前后向扫描 + level 计数组装可见路径。两个 getter
  （part03 工厂 + part05 R138 原型面）同源接入。
- closed 树形态（test3/5/6/7/9/11 的 per-station 截断面）经新算法逐一复核等价。

## 成绩分布

| 文件 | 前 | 后 |
|---|---:|---:|
| event-composed-path-with-related-target | 11/13 | 13/13 |
| event-composed-path-after-dom-mutation | 0/2 | 2/2 |

## 余项

- event-post-dispatch 尾 1F（listener 移动 target 后 relatedTarget 未清——handle 世界
  clearTargets 的 relatedTarget 维度待补）、slotchange 尾 12 案、disabledFeatures×
  attachShadow 尾 2 案、reactions/ 表格族与 customized-builtins 尾面照旧。
- DC-5 终判（make test + clippy + reftest 持续全绿）。

## 累计

| 阶段 | Pass/Total | 通过率 |
|---|---:|---:|
| M1 基线 | 437/4730 | 9% |
| M3 切片 8 第二增量第三小步 | 3525/4763 | 74.0% |
| **M3 切片 8 第二增量第四小步** | **3529/4763** | **74.1%** |
