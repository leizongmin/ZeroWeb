# M3 切片 8 第二增量（第十二小步）— 跨文档 CE 连接 + 数值维度反射（web-components）

- 日期：2026-09-11
- 对照：`2026-09-11-wc-m3s8l.md`（3608/4764）
- 成绩：**3617/4764（75.9%）净 +9 零回归**
- 全量 JSON：`2026-09-11-wc-m3s8m.json`

## 变更（纯 JS shim：part03/part04/part05）

### ① 跨文档 insertAdjacentElement 的 CE adopted/connected（part03/part05）

- **R293 Element.prototype 的 parent 链记账**（part03:1933）：handle 子的裸
  `element.parentNode = this` 被 proxy set trap 吞（getter-only 静默拒绝——removeChild
  已摘链 → parentNode 恒 null，插入後链断）。改记 R180 plainParent 槽
  （`_zwNodeParent[h] = { plainParent }`——`_parentNodeFor` 消费），plain 子保持裸赋值。
- 末段补 `_ceApplyConn(element, true)`：前树 disconnect 已由 removeChild 面派发，
  连接 was 门去重；adopted 由 `_ceApplyConn` 的 old/new doc 比对面承接。
- **detached doc 工厂 docEl.parentNode 接 doc**（part05 R207 块）：docEl 无 parent 链时
  `_ceApplyConn` 连接的 adopted old/new doc 判定 walk（node.parentNode 上行）拿不到
  新 doc → `_wcNewDoc` null → adopted 永不派。spec：documentElement 的 parent 即文档。
- detached doc 工厂 `docEl.insertAdjacentElement`（part05:1293）末段补 CE 连接派发。
- 探针实证全序：`['disconnected', 'adopted', 'connected']` + parent 链
  `1>1*>9`（instance → docEl → doc）。

### ② width/height 数值维度反射收口（part04/part05）

- set trap width/height 分支 handle 路径 moAttr 汇流（同 loop/muted 修复模式——旧仅
  sel 路径汇流，createElement 产物无 attributeChanged/无 R45 record）。
- 分支 gate 补 EMBED/VIDEO（spec 两元素 width/height 亦反射；旧 IMG/IFRAME/CANVAS 三形
  → embed 落 expando 分支不写属性）；get 侧 IMG/IFRAME 反射读门同步补（set→get 对称）。
- `_zwCeReflectionAttr` 补 width/height（CE 反应映射——customized built-in 实例的
  attributeChanged 派发面）。

## 实测结论

- reactions 306 → 320/529（Element.html 46/47——仅余 insertAdjacentHTML 两案，同
  handle-only 面但 markup 解析产物的 CE 构造已由第十一小步覆盖，余面待查）。
- 跨文档 connector 变体 4 变体全绿（adopted 派发首次工作）。

## 门禁

- `make test` 全绿（本轮复跑）；fmt 过（无 Rust 变更）。

## 累计

| 阶段 | Pass/Total | 通过率 |
|---|---:|---:|
| M1 基线 | 437/4730 | 9% |
| M3 切片 8 第二增量第十一小步 | 3608/4764 | 75.7% |
| **M3 切片 8 第二增量第十二小步（跨文档 CE + 数值反射）** | **3617/4764** | **75.9%** |
