# M3 切片 6 — slottable 规范过滤 + 文本 slottable 可见性（web-components）

- 日期：2026-09-11
- 对照：`2026-09-10-wc-m3s5.md`（3142/4762）
- 成绩：**3145/4762（66%）零回归，净 +3**（HTMLSlotElement-interface 15/18 → 18/18）
- 全量 JSON：`2026-09-11-wc-m3s6.json`
- 门禁：fmt/clippy + `make test` 全绿

## 修复内容

### ① slottable = Element or Text（spec 过滤）

- part03 plain / part05 handle 双域 matched 循环：Comment / ProcessingInstruction 不再
  作为 slottable 收编（spec DOM「slottable」= Element 或 Text——WPT
  HTMLSlotElement-interface 'assignedNodes must update when slot elements are inserted
  or removed' 的 [p, text, b] 排除断言，3 options 全绿）。

### ② 纯文本 innerHTML 的文本 slottable 可见性（handle 域）

- `__zwSlotAssignedNodes` ② host 子收集：registry 空（旧判定 [] 为 truthy 恒短路）时
  回落 R100 本地文本视图（`_zwLocalChildNodes`）——纯文本 innerHTML 的文本子是宿主的
  slottable（'baz' 分配/移除序列可见）。slotchange-event innerHTML 家族的 target 断言
  链推进至计数段（余差 = Chrome async host apply 语义差，见余项）。

## 余项（挂账更新）

- slotchange-event 尾 12 案：innerHTML 尾计数（Chrome async host apply——upstream 测试
  自带 FIXME 同域）、transient slot（dispatch 层 wrapper 身份二相）、
  mutation-observer 交错（需 MutationObserver 集成）、嵌套 retarget（composed-path
  深化）——均为 dispatch/observer 层工作，非 slot 语义本身
- Rust `resolve_slots` 接线：消费面为渲染级查询（等用户点名专项后按 run-rules §9 协调），
  JS 观测语义（DC-4 assignedNodes/flatten/查询）已由 shim 满足
- DC-5 终判（make test + clippy + reftest 持续全绿基础上）

## 累计

| 阶段 | Pass/Total | 通过率 |
|---|---:|---:|
| M1 基线 | 437/4730 | 9% |
| M3 切片 3 | 3105/4763 | 65% |
| M3 切片 4 | 3137/4763 | 66% |
| M3 切片 5 | 3142/4762 | 66% |
| **M3 切片 6** | **3145/4762** | **66%** |
