R519 — R513-R518 后 clean lever 再扫描（read-only 调研，零代码变更）
日期: 2026-06-23
承接: R518 §9.7 inline-level blockification on float LANDED (+5)。本会话继续寻 clean lever。
性质: read-only 调研 + 负面裁决。无代码落地（候选全 entangled / fontdue-blocked / structural）。

============================================================
1. 候选扫描结果（全 ruled out）
============================================================

A. float-replaced-width 007/008/009/011（floats-clear）
   - 001-006 PASS（R518 块化附带翻转了 002/003/004）。
   - 007/008/011 = `svg/img { float:left; margin-left:auto; margin-right:auto }`。
   - 排查：auto-margin→0 on float **已实现**（converter convert_length_to_lpa 的
     resolve_auto_as_zero=is_float，mod.rs:437-463）。残余 diff 非 auto-margin，
     而是 **svg/img 替换元素固有尺寸**（§10.3.6 float replaced width 依赖 intrinsic
     sizing，svg 默认 300×150 / viewBox，img 取文件）。结构性，非单点。

B. visuren（61.8%）
   - left/right-offset-position-fixed-001 + top-114（均 2.08%）= position:fixed
     `left/right/top:auto` 的 **静态位置计算**（§10.3.7/§10.6.4），abspos/fixed
     static-position territory，与 R500-series / R506 static-inside-inline-block
     Phase-A entangled。
   - box-offsets-rel-pos-002（8.33%）= relative 偏移；anonymous-boxes-001a/b、
     remove-from-split-inline-2 = R109/IFC；percent-height-1 = 百分比高度。
   - 全 entangled（abspos static-pos / IFC）。

C. clear-applies-to 004/005/006/012/014/015（floats-clear + visuren）
   - assert（012）明言「clear does NOT apply to inline-block」→ 测试期望 clear
     不适用于非 block-level。
   - clear 代码 float_positioning.rs:527-535 **已 guard is_block_level**（非块级
     continue，不处理 clear）。
   - 014（inline-table）/ 015（table-caption）FAIL = is_block_level **误含**
     InlineTable + TableCaption（engine.rs:719-720）→ clear 被施加。
   - 004/005/006（table-internal）/ 012（inline-block）NOT in is_block_level，
     clear 不应施加却 FAIL → 非 clear-application 缺陷，是 IFC/R109/表格结构。
   - 修 014/015 须改 is_block_level（移除 InlineTable/TableCaption），但该字段
     **load-bearing**：margin-collapse(line58)/float-shrink(line337)/float-exclusion
     (line616) 均依赖。移除有回归风险。仅 +2 案，非 clean slice。勿以单会话改 is_block_level。

D. generate / generated-content（content-* + before-after-*）
   - content-048（100%）= `<body bgcolor="#ffff00">` + `body:before{content:attr(bgcolor)}`
     → **presentational hints bgcolor 未实现**（R502 REFUTED/blocked，draw_order +
     margin-collapse 双阻塞）。100% diff 主因 = body 背景未黄（bgcolor HTML 属性
     未映射为 background-color），非 attr() 缺陷。
   - content-113/122/123（attr(onload/onunload/onX)，属性不存在）~1.84% = fontdue
     噪声。attr(absent) 路径 **已正确**（pipeline.rs:526-529 get_attribute absent →
     None → 伪元素不注入）。
   - before-after-positioned-001-004（2.08%）= 定位伪元素（abspos static-pos entangled）；
     before-after-table-parts-001（91.86%）= 表格伪元素（结构性）。
   - 全 presentational-hints-blocked / fontdue / abspos-entangled。

E. css-sizing / css-colors（现代模块）= **未导入** wpt-data（ls 无此目录）。

============================================================
2. 结论
============================================================

R513-R518（负值校验 + §9.7 display 计算值）style-system correctness vein 的 clean
yield 已穷尽。本会话扫 float-sizing / visuren / clear / generate 全 ruled out
（structural / fontdue / presentational-hints-blocked / is_block_level-load-bearing /
abspos-static-pos-entangled）。

★ is_block_level 误含 InlineTable/TableCaption 是 clear-applies-to-014/015 的精确
机制，但 is_block_level load-bearing（4 处依赖），非单会话 clean slice——记录防重投。

真实剩余 lever 均多会话结构性：IFC Phase-A（paint 多行行盒存储统一）、multicol Phase-2
（列碎片化）、JS-DOM-bridge（50+ 动态案）、baseline-export（taffy 0.8+）。

零代码变更。基线不变。
