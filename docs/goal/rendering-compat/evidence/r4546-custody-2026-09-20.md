# R4546 证据：全目录守成巡检轮（ZRG 低频巡检，零代码变更）——R4539-R4545 七轮组合态无回归证据

- 日期：2026-09-20（R4546 守成轮——R4545 下轮方向②；本流 R4539-R4545 七连轮后收口）
- 巡检范围：R4539（clip-path 改写 z 序）/ R4540（v2 条带机制）/ R4541（border-shape
  slice 2）/ R4542（replaced 裁剪 + 资产）/ R4544（flex item definite CB）的累积组合态。

## 巡检面六点全绿

1. **make test**：19,376P / 0F（68 组，树代码面与 R4544 提交一致）；
2. **reftest-upstream 全语料**：1454 fail——对 R4544 记账 1453 的 diff 仅
   box-shadow-overlapping-003（**已记档 ±1 相位 flake**，红相 10.62%；R4544 run 绿相
   0.12%）——真实 fail-list **逐行为空零漂移**；
3. **make reftest（本地 687）**：0 failed；
4. **product-smoke**：welcome 15.40% 同值 struct PASS ×2（welcome + morning-work）；
5. **product-smoke-legacy**：39 fixture struct=PASS 全绿、struct-check failures: 0
   （37-form-controls 既有绿维持）；
6. **bench-gate 定向**（zero-engine + zero-layout-engine）：**GATE PASS 32 指标**。
   首跑 FAIL（6 指标）系巡检自身 make test 收尾的 browser 集成测试（compositor/
   renderer debug 进程）并窗争用——空载复跑即 PASS，四项超标指标（paint/layout 系）
   争用形态与 R4544 首跑同型。

## 结论

R4539-R4545 七连轮组合态（clip-path 覆盖改写 z 序修复 + v2 条带机制 + border-shape
slice 2 + replaced 内形状裁剪 + flex item definite CB 传播 + 资产激活）在当前树**无
回归证据**。corpus 记账 **1453**（15141/16594 = 91.24%；本轮 flake 红相位不计）。

## 下轮方向

1. corner-shape 余 6 fail 深域勘察（backdrop-filter ×2/iframe/video/inset-shadow/
   render-corner-shape 多 variant）；
2. clip-text mask 管线 RFC 级评估（background-clip:text 多行/descendants/blend ~12 案）；
3. 或继续守成轮。
