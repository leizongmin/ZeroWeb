# R407 — css-tables 全量导入 read-only 健全性核查

日期：2026-06-22
HEAD：6379e70（R406）；并行 agent 63 未追踪 css-tables 未提交（in-flight）
性质：read-only；承接 R404（position 健全性）/ R406（position fold-in）后的 tables 导入核查。零代码变更、零 master.md 编辑（tables 数据未提交，fold-in 待并行 agent 提交 + defer）。

## 导入规模

- 62 个 css-tables .html（59 test + 3 reference）+ tables/reference/ + tables/support/ 同目录资源。

## 健全性核查（R389 `../support/` 断裂 + R401 root-relative 缺失 两类 bug）

| 检查 | 结果 |
|------|------|
| `<link rel=match>` 覆盖 | 59/62 有（3 无 = reference 文件，expected）✓ |
| `../reference/` 引用（47） | 3 distinct 目标（ref-filled-green-100px-square-only.html / .xht / ref-filled-green-200px-square.html）→ **全部存在于 css/reference/**（R401 共享 ref）✓ |
| `/css/reference/` root-relative（3） | ref-filled-green-100px-square.xht → 存在 ✓ |
| 同目录 `reference/` / `support/` | tables/reference/ + tables/support/ 均 EXISTS ✓ |
| R389 `../support/` 跨目录断裂 | **无**（tables 用同目录 support/ 或 ../reference/ 共享 ref）✓ |

## 结论

css-tables 全量导入**健全**，0 缺陷——与 R404（position）一致。R401 共享 ref 基础设施（`css/reference/`）继续正确支持第 3 个目录。

**方法论复用确认**：discover-import→reftest→capture-oracle-per-dir→oracle 流水线 + 共享 ref 基础设施对 grid/position/tables 三目录均稳健，R401「2 grid root-relative ref 缺失」确认为 grid 个案非系统性。

## 未做（避免 race / 待数据）

- 未跑 reftest（并行 agent 未提交 + 冲突/OOM）。
- 未做 tables 失败簇 pre-confirm 映射——tables 簇**已有详尽历史归档**（R49 border-collapse / R117 auto-cell-column-width / R168 height-as-minimum / R177b colspan 5 部件 / R342c table_borders 拆分 / 卡点#5 外边缘半边框），边际价值低；待数据落地直接用真通过率对照已知簇。
- 未编辑 master.md（tables 未提交，并行 agent owns fold-in）。

## 代码变更

零（read-only）。
