# R404 — css-position 全量导入 read-only 健全性核查

日期：2026-06-22
HEAD：7bd7531（R403）；并行 agent 90 未追踪文件未提交（css-position 扩量 in-flight）
性质：read-only 核查并行 agent 未提交的下一批扩量（css-position）导入健全性；零代码变更、零 master.md 编辑（避免与并行 agent 活跃编辑踩踏）
目的：在并行 agent 跑 reftest 前，提前发现可能的导入缺陷（参考 R401「2 root-relative ref 缺失」+ R389「../support/ 路径断裂」两类历史 import bug）

## 导入规模

- 86 个 css-position test/ref 文件 + 3 个 css/reference/ 共享 ref + 1 个 CSS2/reference/ ref（共 90 未追踪）。
- 89 个 .html 中 81 有 `<link rel=match>`，8 无——**8 个全是 reference 文件**（5 个 css-position 内 `-ref.html` + 3 个 css/reference/），reference 本身即 match 目标无需 match link，**非 broken test**。✓

## 路径解析核查（R389 `../support/` 断裂 + R401 root-relative 缺失 两类 bug）

| 引用模式 | 出现 | 解析目标 | 存在？ |
|----------|------|----------|--------|
| `../reference/ref-filled-green-100px-square.xht` | 26 | css/reference/...xht | ✅ R401 已导入 |
| `../reference/ref-filled-green-100px-square-only.html` | 25 | css/reference/...only.html | ✅ R401 已导入 |
| `../reference/ref-filled-green-200px-square.html` | 1 | css/reference/...200px...html | ✅ 存在 |
| `../CSS2/reference/ref-filled-green-100px-square.xht` | 1 | css/CSS2/reference/...xht | ✅ 本批已导入 |
| `/css/reference/green.html`（root-relative） | 1 (replaced-object-backdrop) | wpt-data/css/reference/green.html | ✅ R401 已导入 |
| `/css/reference/pass_if_pass_below.html`（root-relative） | 1 (position-absolute-multicol-001) | wpt-data/css/reference/pass_if_pass_below.html | ✅ R401 已导入 |
| `../resources/position-absolute-iframe-child*.html` | 2 | css/resources/... | iframe/JS 资源，minor（非 reftest ref） |
| 同目录 `support/` | — | css/css-position/support/ | ✅ 存在 |

**无 R389 `../support/` 断裂**（position 测试用同目录 `css-position/support/` 或 `../reference/` 共享 ref，非 `../support/`）。
**无 R401 root-relative 缺失**（2 个 `/css/reference/` root-relative ref 的目标均已被 R401 的共享 ref 导入覆盖）。

## 结论

并行 agent 的 css-position 全量导入**健全**：match link 全有效、所有 ref 解析、无路径 bug。

**方法论验证**：R401 建立的共享 ref 基础设施（`css/reference/` + `discover-reftests-authoritative.py` 权威发现）**跨目录可复用**——css-position 的 52 个 `../reference/` + 2 个 root-relative ref 全部依赖该基础设施并正确解析。这印证 Track-1（DC-14 分母去子集化）的导入工具链对后续 8 目录（position 此批 + 余 tables/flexbox/...）是稳健的，R401 的「2 grid root-relative ref 缺失」是 grid 个案（那 2 个 ref 未导入），非系统性缺陷。

## 未做（避免 race / 待数据）

- 未跑 reftest（并行 agent 未提交 + 冲突/OOM）。
- 未做 css-position 失败簇映射（R396/R400 grid 谱系）——待并行 agent 提交 + 跑出真通过率后，用真实数据做 data-grounded 簇映射（预测：position 失败大概率落 abspos-CB-resolution / stacking-order(R380) / sticky / multicol-interaction 等已知簇，但须数据确认）。
- 未编辑 master.md（并行 agent 正活跃编辑，避免踩踏）。

## 代码变更

零（read-only）。
