R633 — 字体度量死锁分界诊断（行盒度量启发式部分已穷尽）
日期: 2026-06-25
性质: read-only 诊断（零代码变更）
承: /loop 第 4 轮「彻底修复字体问题」+ R630/R631/R632 三轮

================================================================================
背景：R630-R632 三轮已修行盒度量启发式部分
================================================================================

R630（commit d31cf03a）：paint Path B 多行 y 分行（all_fragments_with_line_y），同源 net +24
R631：字体选择 A/B 证伪（DejaVu vs NotoSansCJK 零变化），推翻 R374 字体不匹配归因
R632（commit 0911a2ac）：paint Path B line-height 忽略 CSS 修复（compute_final 存 override），
      reftest net +5，welcome -1.11pp，morning +0.99pp（中文字体度量诚实暴露）

================================================================================
本轮诊断：剩余字体相关残余的根因分界
================================================================================

1. **morning 中文 modest 退步（+0.99pp）非行间距错**：ZW vs CHR morning 正文行间距均
   ~21-29px（中文 line-height:normal），无显著差异。退步是分行后行级字体度量（frag.height
   字体自然行高）诚实暴露，<1pp，无单 session clean fix。

2. **baseline v_offset = font_size 已是较好近似**：text.rs:1044/1240 `v_offset = is_ahem?0:font_size`。
   拉丁字体真实 ascent ≈ 0.8em < font_size(1.0em)，但 layout strut_ascent = line_height×0.8
   （mod.rs:1756 启发式，R393）更偏离。v_offset=font_size 比 strut_ascent 更近真实 ascent，
   非 morning/welcome 残余主因。

3. **真实 ascent/advance 需 IFC 接 FontLoader（Phase A 多会话架构）**：
   - layout-engine 依赖 zero-render-foundation（Cargo 确认），能访问 FontLoader/fontdue
   - 但 IFC 内部用 estimate_char_width（启发式）+ strut_ascent（0.8 启发式），不调 fontdue
   - 真实 ascent（fontdue horizontal_line_metrics）/ advance（fontdue measure_advance）需
     IFC 内部接 FontLoader 替代启发式 = R223 advance-width plumbing + R188 font-family→FontId
     预解析 = Phase A IFC 统一核心，多会话架构
   - R225 曾证伪 advance-width（R632 前，paint fontdue 掩盖），R632 后 paint 用 layout override
     （同源），advance 影响显现——R225 死路结论可能过期（同 R246/R627 谱系），但验证需
     IFC 接 FontLoader 改动（高风险，本轮未试）

================================================================================
裁决：行盒度量启发式部分穷尽，剩余全字体度量统一（Phase A 多会话）
================================================================================

**死锁分界（清晰）**：
- ✅ 已修（不依赖字体度量，启发式可得）：R630 多行 y 分行 + R632 line-height CSS 响应
- ⛔ 未修（依赖字体度量，需 IFC 接 FontLoader）：
  - baseline 真实 ascent（v_offset 用 fontdue ascent）
  - advance-width（IFC 用 fontdue advance 替代 estimate）—— R225 可能过期但高风险
  - morning 中文 frag.height 字体自然行高（NotoSansCJK metric ≠ chromium）
  - font-features rustybuzz 同源（R331/R332）

**对 /loop「彻底修复字体问题」的诚实结论**：
字体方向四层全 characterized：
  (1) font-weight 加粗 — R229c 证伪
  (2) 字体选择 — R631 证伪
  (3) 光栅化 — R388 证伪（fontdue≈chromium）
  (4) 字体度量（ascent/advance/自然行高）— 本轮 R633 确认为 Phase A 多会话死锁区
「字体问题」作为单 session 优化目标已穷尽。真正改善需承诺 Phase A IFC 统一（IFC 接
FontLoader，layout+paint 同源用 fontdue 度量），多会话架构工程。

R630-R632 三轮已把**不依赖字体度量**的行盒度量修复全部落地（net +29 reftest）。
继续 /loop 在字体方向是死锁区；建议停止 /loop 或承诺 Phase A 多会话。
================================================================================
