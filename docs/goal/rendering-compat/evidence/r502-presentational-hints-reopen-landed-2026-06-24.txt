R502-reopen — CSS2 Appendix D presentational hints 重投 LANDED（block A 已被 R504 解除）；4 目录 A/B net +7 PASS / 0 回归
==================================================================================================
日期：2026-06-24
类型：code-side CSS correctness（重投 R502，验证 doc-agent「R504 解禁 block A」假设）
状态：LANDED（doc-agent e91e7924 评估 + 本轮 code 实证 block A 确已由 R504 解除）

承接
----
R502（presentational hints：`<img width=/height=>` → CSS）实现 VERIFIED CORRECT 但 ZERO-YIELD，因
**双重阻塞**：block A（draw_order 覆写——abspos img 图元正确生成+绘制 225px，但 render_draw_order
位置过早被 #div1 border/bg 整片覆写 → blue_px=0）+ block C（margin-collapse-through-border，R490-
entangled）。R502 REFUTED revert。**R503/R504 之后 LANDED CSS Appendix E paint-order 修复**：
R504（ea08de51）global positioned-descendant deferral——painter scope 收集所有 positioned 后代 step 6
flush（晚于 in-flow content + CB bg/border）。doc-agent e91e7924 评估：block A 机制正是 R504 所修，
很可能已解禁。本轮 code-side 重投验证。

block A 实证解除（R504 效果确认）
--------------------------------
重投 R502（collect_presentational_hints）后 LAYOUT_DUMP absolute-replaced-width-006：
- img 计算尺寸 15×15（intrinsic）→ **96×96**（`width="50%"` × #div1 192px ✓，hint CORRECT，同 R502 实证）。
- diff 2.24%（archive，img 被覆写 blue_px=0）→ **3.52%**（现）= img 现已可见（block A 解除！），残余
  3.52% = img 在 abs_y=150（test）vs 70（ref）= **block C margin-collapse 80px 垂直偏差**（R490，仅此结构）。
- 3.52% ≈ 2×96×96/480000 = 一个可见 img 在错位的 diff，与「img 可见但位置错」一致。

修复（style-system/lib.rs，~30 行）
-----------------------------------
1. 新增 `collect_presentational_hints(doc, element)`：仅 img 的 width/height 属性 → 声明；
   裸数字→px、`%`/长度原样透传。
2. 步 1.5a（expanded_with_layer 创建后、matched 规则前）注入：Author origin + specificity (0,0,0)
   + 最早位置（cascade 按 origin/layer/specificity/position 排序，(0,0,0) 低于任意真实选择器 ≥(0,0,1)
   与 inline (1,0,0)，高于 UA 默认）= CSS2 App D「插入作者样式表起始处」语义。

A/B 测量（test-guard，git stash baseline / pop fix，4 目录全量 FAIL-list diff）
-----------------------------------------------------------------------------
| 目录         | baseline (no hint) | with hint | yield (NEWLY PASS) | regression |
|--------------|--------------------|-----------|--------------------|------------|
| normal-flow  | 636/110            | 639/107   | +3                 | 0          |
| positioning  | 396/123            | 396/123   | 0                  | 0          |
| backgrounds  | 284/55             | 287/52    | +3                 | 0          |
| borders      | 431/75             | 432/74    | +1                 | 0          |
| **合计**     |                    |           | **+7**             | **0**      |

NEWLY PASS（+7，全 spec-correct，img w/h hint 现生效且 R504 让 img 可见）：
- normal-flow: block-replaced-width-006 / inline-block-replaced-width-006 / inline-replaced-width-006
- backgrounds: background-image-cover-002 / -004 / background-image-cover-attachment-001
- borders: border-color-005

★ block C 仍阻塞 positioning/absolute-replaced-width-* 簇（006/013/022/027/029 等 3.52%
margin-collapse signature）= R490-entangled 子集，独立 lever，不阻塞 broad yield。
★ doc-agent「285 文件 broad +N」乐观——实测 block C + 各案独立 blocker 限制到 +7（positioning 0、
每目录 1-3）。但 block A 解除本身是关键发现（R504 paint-order 修复对 abspos replaced 元素生效）。

门禁
----
make test 全绿（与 R502 change 一致），clippy clean，product-smoke welcome 16.97%（welcome 无 img
w/h 属性，neutral），fmt clean。零回归（4 目录 FAIL-list diff 全 0 newly-failing）。

★ 教训：REFUTED lever 须在「后续 paint-order/基础修复」落地后重评——R502 block A 被 R504 解除是
R502 REFUTED 时不可预见的。doc-side read-only 评估（e91e7924）+ code-side 重投验证 = 复活 verified-
correct lever 的有效流程。
