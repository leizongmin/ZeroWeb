# R236/R238 spec 对 a2b169e 仍有效验证 + 代码 agent stall 协调观察（R263）

**日期**: 2026-06-18
**目标**: 只读验证 R236（multicol baseline-export turnkey spec, R260）/ R238（WM-1 abspos-vertical
半 turnkey spec, R262）的 reftest 用例 baseline 是否被 a2b169e（ua_default_display 补
article/aside/details/hgroup/menu/search → 这些标签 inline→block）影响；并记录并行代码 agent 的
协调状态。
**方法**: read-only grep 用例 HTML + git log/status 核实，不改源码、不跑渲染。
**基线**: reftest 自源 438/490（持平）。

## 1. a2b169e 对 R236/R238 用例零影响（spec baseline 仍有效）✅

a2b169e 使 article/aside/details/hgroup/menu/search 从 inline→block。若 R236/R238 的 reftest 用例
使用这些标签，其渲染会随 a2b169e shift（test/ref 同源故自源计数不变，但 chromium-oracle + 几何会变），
则 R260/R262 spec 的 baseline 需重测。

**grep 实测**：R236 的 8 baseline-export 用例（css-multicol/baseline-000~008.html + refs）+
R238 的 14 WM-1 用例（css-writing-modes/abs-pos-*-vrl/vlr-* + clearance/clip vrl 等）中，搜
`<(article|aside|details|hgroup|menu|search)[ />]` —— **命中 0 文件**。

→ **R236/R238 用例不用 a2b169e 新增 block 标签**，a2b169e 对这两条 spec 的 reftest baseline
**零影响**。R260（R236 turnkey）/R262（R238 半 turnkey）spec 对当前 HEAD 代码**仍准确有效**，
代码 agent 可直接起手实现，无需因 a2b169e 重新 baseline。这去除了「最大近期代码改动是否使 spec
失效」的风险。

## 2. 协调观察：并行代码 agent 自 a2b169e 后无新代码提交

git log 核实：自 `a2b169e`（R255 morning-work ua_default_display 修复）后，并行代码 agent 提交仅
`7d062e5 R229b`（font-weight bold overshoot **实验已回退**，docs-only）+ 本 docs agent 的 R258-R262。
**即 reftest 438/490 的代码侧自 a2b169e 起无推进**（R229b 是回退实验无净代码）。

并行 agent 工作树持续有 `crates/layout-engine/src/inline/mod.rs` + `inline/tests/edge_cases.rs`
未提交改动（跨 ~7 轮），疑在攻 **item-tag span→block（R109 IFC 架构）**——R229b 重定性的
welcome/morning-work 残余主因，但 R109 是 goal doc 标记的 P1 硬架构（R141b「6 轮单会话不可解」），
可能未收敛。

**对优先级队列的影响**：
- R236（multicol baseline +8，turnkey spec R260）/ R238（WM-1 +14，半 turnkey spec R262）**就绪等
  代码 agent 实现**——这两条是 contained reftest 杠杆，且 multicol.rs / engine.rs abspos 区域与并行
  agent 的 inline/mod.rs IFC WIP **不冲突**，可作为代码 agent 的并行/替代方向（若 IFC item-tag 攻坚
  卡住）。
- docs 侧高价值非撞车只读调查已饱和（morning-work 4× 闭环、UA 审计、font-weight 死路记录、
  R236/R238 spec、header 自洽、本轮 spec 有效性验证）。

## 3. 结论

- R236/R238 spec 经验证对 a2b169e 后代码仍有效（用例不用受影响标签）→ 代码 agent 可放心起手。
- reftest 438/490 下一推进点 = 代码 agent 实现 R236（+8）/R238（+14），与并行 IFC WIP 不冲突。
- 并行 agent inline/mod.rs IFC WIP 长期未提交，若持续可考虑 lark 通知本人核对代码 agent 状态
  （本轮仅记录，不通知——非本 docs agent 的阻塞，且可能系正常硬架构攻坚）。

**本轮 read-only 无代码/reftest 变更，基线 438/490 持平。** 证据 = 用例 HTML grep + git log/status，
全可复核。
