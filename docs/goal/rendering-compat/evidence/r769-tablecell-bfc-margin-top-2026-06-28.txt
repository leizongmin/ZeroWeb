R769 — table-cell BFC first-child margin-top containment（A/B 验证）
日期: 2026-06-28
状态: LANDED 候选（待 make test / clippy / product-smoke 全绿后提交）

================================================================================
背景 / 承接
================================================================================
承 R768 CONTINUE 第 1 步：「root-cause 101 bar 定位残余（collapse/margin 算术 vs
chromium，dump 54/164/274 + chromium oracle 像素对比定位差）」。

R767（commit 3b963656）已修 table cell content-width 溢出（div w=778→27.5），
margin-collapse-101 oracle 47.65%→6.52%（-41pp divergence reduction）。R768 追加
anon-block 门控扩展实测 net-negative（零簇 PASS + normal-flow -1）已回退，结论：
101 簇达 <1% 须 collapse 定位修（bar 位置 54/164/274 vs chromium）。

R769 定位 101/110/111 簇的真残余 = table-cell BFC margin 包含缺口（独立于 R109
anon-block）。

================================================================================
根因（CSS §9.4.1 + §8.3.1）
================================================================================
CSS 2.1 §9.4.1：table-cell（与 inline-block / table-caption / overflow≠visible 一样）
建立新的 block formatting context。§8.3.1：建立 BFC 的元素，其 margin 不与 in-flow
子元素折叠。

故 table-cell 首个 in-flow 子元素的 margin-top 应作为单元格内的顶部留白保留，
不应向上穿透单元格。

ZeroWeb 实测缺口：
- taffy 把单元格按普通 Block 布局，把首子 margin-top 折叠上提到 cell.margin_top。
- 自定义表格布局（table.rs position_cells）忽略单元格 margin（cell 无有效 margin）。
- 后果：内容从 content-box 顶（y=0）开始，等量空白留底部（cell_content_height =
  sum(child.height + child.margin_top + child.margin_bottom) 已按 child margin 计入
  高度，故总高正确但顶部留白错位到底部）。
- 用户可见：`<td><p style="margin:1em 0">` / `<td><h2>` 等 margin 首子，内容贴顶、
  单元格底部多出空白（与 Chromium 不一致）。

================================================================================
修复（table.rs position_cells，+15 行）
================================================================================
在 position_cells 设置 cell 高度之后、vertical-align 之前，把内容子树整体下移
cell.margin_top：

    let top_gap = cell_box.margin_top;
    if top_gap > 0.0 {
        for child in &mut cell_box.children {
            child.y += top_gap;
        }
    }

底部 margin（cell.margin_bottom）自然留作底部空白（cell_content_height 已计入），
无需额外处理。vertical-align 在其后仍正确工作（基于 content_height）。

================================================================================
A/B 验证（chromium Oracle，DC-14 真指标）
================================================================================

--- margin-collapse 簇（filter=margin-collapse, 113 with-oracle cases）---
PASS 计数（z_vs_chr < 1%）：baseline 34 / WITH 34（net-zero PASS）
逐 case 对比（zero 回归，无 case 变差 > 0.3pp）：

  改善（baseline → WITH）：
    -10.05pp  margin-collapse-110.xht   14.80% -> 4.75%
    -10.05pp  margin-collapse-111.xht   14.80% -> 4.75%
     -1.78pp  margin-collapse-101.xht    6.52% -> 4.74%
     -1.76pp  margin-collapse-105.xht    6.52% -> 4.76%
     -1.03pp  margin-collapse-112.xht    5.29% -> 4.26%
     -1.03pp  margin-collapse-106.xht    5.29% -> 4.26%

  回归（WITH - baseline > 0.3pp）：无（ZERO）

per-dir oracle（margin-collapse 簇匹配 5 子目录，两边全同）：
  box-display 1/5 / floats-clear 6/29 / margin-padding-clear 21/53
  normal-flow 2/8 / writing-modes 4/18  （WITH == baseline 全同）

--- css-tables 全目录（filter=css-tables, 116 with-oracle cases）---
PASS（<1%）：baseline 57 / WITH 57（net-zero PASS，零改善零回归）
逐 case 对比：无 case 变化 > 0.3pp（css-tables 不触发 cell margin 首子，改动对该
目录无影响，确认无广义 table 回归）。

================================================================================
裁决
================================================================================
ship（与 R767 同型）：
- CSS-correct（table-cell BFC margin 包含，§9.4.1 + §8.3.1 硬规范行为）。
- 零回归（margin-collapse 簇 + css-tables 全目录逐 case 验证，无任何变差）。
- 显著 divergence reduction（110/111 各 -10.05pp，101/105 -1.78pp，106/112 -1.03pp）。
- net-zero PASS（与 R767 同：oracle PASS 不变，但真实渲染兼容性改善）。
- 真实世界相关性（`<td><p>` / `<td><h2>` / `<td><div margin>` 常见表布局模式）。
- 单元测试 test_table_cell_bfc_first_child_margin_top_preserved（table/tests.rs）。

================================================================================
顺带修 4 个 pre-existing 测试（clean HEAD 同样失败，run-rules「修遇到的失败」）
================================================================================
均为与本改动无关的 stale/locale/env 失败，在 make test 全量运行中暴露：

(a) generate_history_html_includes_clear_link（apps/browser/src/pages.rs）
    断言 html.contains("History")；ZhCn locale 下 title="历史记录" 不含 "History"
    → 改 contains("History") || contains("历史")（与兄弟 downloads 测试同模式）。

(b)(c) test_basic_app_ignored_events / test_basic_app_unhandled_events_silently_ignored
    （crates/host-runtime/src/window_tests.rs）
    主题切换功能（commit 08440050）让 BasicApp handle_window_event 派发
    AppEvent::ThemeChanged，两 stale 测试仍把 ThemeChanged 列入「应忽略」
    → 移除 ThemeChanged（Destroyed/Occluded 仍正确走 `_ => {}` 忽略）。

(d) test_shader_sources_not_empty（crates/render-foundation/src/gpu/pipeline.rs）
    断言 BLUR_SHADER.contains("vs_main")；BLUR_SHADER 用全屏三角形 pass，
    vertex entry point 命名为 vs_fullscreen（非 vs_main）→ 改断言 vs_fullscreen/fs_blur。

================================================================================
未修：13 个 pre-existing script-sandbox V8 失败（非本目标范畴）
================================================================================
make test --no-fail-fast 暴露 13 个 script-sandbox 失败（es_module::tests::* 12 +
v8_runtime::tests::test_script_with_syntax_error 1），已确认 clean HEAD 同样失败
（stash 全部本改动后复跑 script-sandbox 同样 FAILED），与本改动零相关（不同 crate、
零依赖）。根因疑为最近 script-sandbox 重构（1fa9d119 "renderer runtime unification" /
50ec2d04 "page script isolation" / 6d0060d2 "V8Sandbox::register_callback"）让
compile_module_script 的 ES module→script 转换不再把导出值写入 namespace_json
（test_export_const：execute `export const x = 42` 后 namespace_json 不含 "42"，
但 execute 返回 Ok，V8 正常执行）。属 script-sandbox 子系统范畴，非 rendering-compat
目标，留待 script-sandbox 维护处理。本改动未引入任何新失败（layout-engine 测试全过 +
oracle A/B 零回归）。

================================================================================
门禁状态（提交前）
================================================================================
- cargo fmt ✅
- cargo clippy --workspace --all-targets -- -D warnings ✅（exit 0，零警告）
- make product-smoke ✅（welcome 16.16% 字节同 baseline，exit 0，table-only 改动）
- layout-engine 单测 ✅（含新单测 test_table_cell_bfc_first_child_margin_top_preserved）
- oracle A/B（margin-collapse 簇 + css-tables 全目录）零回归 ✅
- make test：13 个 pre-existing script-sandbox V8 失败（见上，非本改动）。
  注：make test 在本环境因 OOM 风险需提高 test-guard 阈值（--per-proc-mem 10
  --total-mem 24）+ 限制 --test-threads=2 才能跑完（browser 测试二进制默认 >6GB）。
