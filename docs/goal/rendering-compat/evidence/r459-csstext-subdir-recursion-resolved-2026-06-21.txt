R459 — css-text 全量导入工具阻塞 RESOLVED（1568cb0 子目录递归已落地 + 本轮独立实测）+ 新 gate=GITHUB_TOKEN
日期: 2026-06-21（doc-maintenance rollover 轮，只读 + dry-run probe，零代码变更）
承接: R458（css-text 30 子目录 / CSS2 43 子目录，顶层 discover 得 0 对 = 工具阻塞）
本轮角色: rollover 维护；并行 agent 负责导入/代码，本轮仅验证 + 记录方向。

================================================================
1. 阻塞 RESOLVED（代码层）— 1568cb0
================================================================
并行 agent commit 1568cb0 `feat(reftest): subdir recursion + optional GITHUB_TOKEN in
discover script` 实现 R458 预测的可执行清单全部三项：
  (a) collect_test_paths(): 递归走 category 子树（每个 subdir 1 次 gh_api），收集全部 test 文件
  (b) discover() ref 路径解析改为「相对 test 文件所在目录」（顶层目录 test_dir==category 不受影响）
  (c) gh_api() 可选 GITHUB_TOKEN（60→5000/hr）
import/fetch 路径本就 subdir-safe（R458 已核），零改动。

================================================================
2. 本轮独立实测验证（只读 dry-run probe）
================================================================
命令: `python3 discover-reftests-authoritative.py --category css/css-text`（无 --import，dry-run）
环境: source ~/use-proxy（urllib 需 http_proxy/https_proxy，docstring 所称代理）；本 shell 无 GITHUB_TOKEN
实测:
  - collect_test_paths() **递归已生效** — 脚本深入 css-text 子目录（white-space/segment-break/...）
    逐个 gh_api 列目录。**R458「css-text 得 0 对」工具阻塞在代码层 RESOLVED。**
  - **全量 pair 计数未取得** — 递归中途（某 subdir gh_api 调用）触发
    `urllib.error.HTTPError: HTTP Error 403: rate limit exceeded`。与 1568cb0 commit msg
    「Full css-text run confirmed the recursion executes but hit the 60/hr rate limit mid-way」一致。
  - rate_limit 实测: 起始 60/60，dry-run 耗尽至 0/60（reset 后恢复）。
  - 未带 proxy 首跑: IncompleteRead（网络半截断），非脚本 bug；带 proxy 二跑到达 403（逻辑正确）。

================================================================
3. 新 binding gate = GITHUB_TOKEN（R458「optional」项现为实际约束）
================================================================
css-text 顶层 + ~30 子目录 = ~31+ 次 gh_api 调用，超过 60/hr 未认证预算；CSS2（43 子目录）
~75 调用更甚。**无 GITHUB_TOKEN 无法完成 css-text/CSS2 全量 dry-run 或 import。**
- 本轮 shell 无 token → 无法取得 css-text 权威对计数（R458 预测「数百 reftest」待 token 后量化）。
- 解法（均非本轮 rollover 域）: (1) 供给 GITHUB_TOKEN 环境变量；(2) 等待 60/hr 窗口逐批；(3) 缓存
  gh_api 响应（代码改动，并行 agent 域）。
**非 BLOCK** — code-ready，仅 credential/env 依赖；并行 agent 下一导入批次的前置。

================================================================
4. stale untracked 文件对账
================================================================
`evidence/r459-writing-modes-oracle-crossvalidate-2026-06-21.txt`（untracked）= 上一 session
R459（writing-modes 全量 chromium-Oracle cross-validate），429 rate-limit 中断未 commit。
其结论**已完整并入 R457 header**（9af26d8 fold-in）：44/786=5.6% / 8-dir 673/2512=26.8% /
分布（[1,5)% 73.5% 近似正确非像素精确）/ 0 单会话 lever / R447 预测证伪。
本轮 R459（css-text 子目录递归）与该 stale 文件**主题不同**，仅 R 编号撞车。
处置: 本轮不 commit 非 own 文件（run-rules 约束）；master.md R459 entry 显式标注 stale 文件
已 superseded，避免未来读者混淆。文件保留 untracked 待上轮 owner 处置。

================================================================
5. 结论 + 下一步
================================================================
- R458 工具阻塞: **RESOLVED（code）+ 实测验证递归生效**。
- css-text 全量导入（第 9 目录）: **code-ready，待 GITHUB_TOKEN**；并行 agent 下一导入批次前向。
- 全量 oracle 预测: 不变 R458（line-breaking 核心 ZW 已有 CJK 换行或高于 text-decor 28.9%，
  hyphens/justify/autospace 精度=字体/度量下拉风险）——须 token 后全量 dry-run 取权威对再量化。
- 输出: CONTINUE（非 BLOCK；credential 依赖非代码/平台根本性不可用）。
