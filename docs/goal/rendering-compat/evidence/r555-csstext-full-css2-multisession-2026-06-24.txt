R555 — 轨道1（DC-14 分母去子集化）css-text 全量确认 + CSS2 多会话级确认 + R551 exposure 全结构性
日期: 2026-06-24
代码变更: 无（纯诊断，零代码/资产变更）

================================================================================
1. css-text 分母去子集化 = DONE（0 import gap）
================================================================================

discover-reftests-authoritative.py --category css/css-text --import（幂等）：
  「发现 1302 个权威 reftest 对（link rel=match 解析）」
  「导入 1302 对到 wpt-data（0 文件新下载）」

→ css-text 上游 1302 权威 reftest 本地**全部已导入**，0 缺失。轨道1 此目录
「分母去子集化」= 完成。runner 加载 css-text 1826 cases > 1302 是因 ref-as-test
噪声（ref 文件含 link 也当 test，已知 runner 行为），非分母缺口。

R514 pre-confirm 预测 css-text oracle ~25-33%；css-text self-source R463 已测
1402/1572=89.2%。css-text 分母全量（1302/1302），剩余 DC-14 价值 = chromium
oracle（capture-oracle-per-dir.mjs 抓 1302 截图 + cross-validate，慢/OOM-prone）。

================================================================================
2. CSS2 全量 = 多会话级（discover 超时）
================================================================================

discover-reftests-authoritative.py --category css/CSS2（dry-run）：280s 超时
（exit 124）。CSS2 ~5000-7000 reftest，git/trees recursive + fetch raw 数千文件
单次超时。轨道1 终点（CSS2 全量）须**分批 discover/import**（按子目录 css/CSS2/{normal-flow,positioning,...} 分批，每批 ≤ 几百对），多会话级。

================================================================================
3. R551 暴露的 7 个 self-FAIL = 全结构性（非单点 clean lever）
================================================================================

R551（abs-path ref 解析）重新入分母的 7 个 self-FAIL 逐案确认结构性：
  - cascade-import-001 (3.20%) = 嵌套 @import + data: URI（复杂 CSS 特性）
  - padding-applies-to-016/017 (12.69%/11.72%) = inline span padding in table
    = IFC/Phase-A 结构性（master plateau）
  - flexbox-align-self-baseline-compatability (2.08%) = baseline-export
    （R266/R313/R316 ruled out）
  - multicol-on-broken-image-alt-text (10.42%) = multicol + broken image alt
  - replaced-object-backdrop (100%) = dialog::backdrop 伪元素 DOM/JS gap
    （R349 已判 out of rendering-compat scope）
  - dynamic-table-cell-height (7.12%) = JS-dynamic（class="reftest-wait"
    + <script> 设 style.top，需 reftest-wait 同步 + 动态样式）

7/7 全结构性，0 单点 clean lever。印证 master.md「单会话 clean lever 趋尽」。

================================================================================
2b. CSS2 子目录分批 discover = 全部已全量（0 gap，修正「CSS2 全量未做」）
================================================================================

逐 CSS2 子目录 discover --import（幂等，看 0 文件新下载 = 已全量）：
  css/CSS2/cascade:  32 对 / 0 新下载
  css/CSS2/syntax:  275 对 / 0 新下载
  css/CSS2/colors:  19 对 / 0 新下载
  css/CSS2/visuren: 34 对 / 0 新下载
  css/CSS2/uri:     目录不存在（非 reftest 目录）

→ CSS2 子目录**全部已全量导入**（0 gap）。R546（+14 共享 ref）+ 历史 import 已覆盖
CSS2 全部分母。「CSS2 全量 discover 超时」是工具限制（一次性 git/trees + fetch 数千
文件），非真实分母 gap——分批 discover 证实 CSS2 分母基本全量。

★ **轨道1 import yield 基本穷尽**：css-text（1302/1302）+ CSS2 子目录（cascade/syntax/
colors/visuren 等 0 gap）分母均已全量。轨道1 剩余 DC-14 价值 = chromium oracle
（真目标验证，慢/OOM-prone），非 import。

================================================================================
本会话裁决
================================================================================

R554（content-list/伪元素）回退后转轨道1 css-text：
- css-text 分母去子量 = DONE（0 gap，确认）。
- CSS2 全量 = 多会话级（discover 超时）。
- R551 exposure = 全结构性。

本会话净代码 yield = 0（R554 全回退 + css-text 0 import gap + 纯诊断），
但诊断有价值：
  ① 确认 css-text DC-14 分母完成（下轮不需再导 css-text，直接 oracle）。
  ② 确认 CSS2 全量须分批（多会话，避免单次 discover 超时）。
  ③ 确认 R551 exposure 全结构性（非下轮 lever）。

================================================================================
下一步（轨道1 + 渲染架构）
================================================================================

轨道1（唯一非纯架构前向路径，R395 裁决）剩余：
  - css-text chromium oracle（DC-14 真目标，capture-oracle-per-dir.mjs，
    慢/OOM-prone，须分批或限量）。
  - CSS2 全量分批导入（按 css/CSS2/<subdir> 子目录分批 discover/import，
    每批 ≤ 几百对，多会话）。

渲染架构（多 session，R554 排除伪元素 content 后）：
  - Phase A IFC 统一（释放 ~2200 fontdue/IFC near-threshold 案）。
  - 伪元素 generated content 独立 box（layout 侧，R554 前置阻塞）。
  - multicol fragmentation / baseline-export。
