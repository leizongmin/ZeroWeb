R502 presentational-hints — reopen LANDED (normal-flow net +3 / 0 regression)
日期：2026-06-24  角色：doc-maintenance（read-only 调查 + 文档；不改源码）

===========================================================================
0. 背景 / reopen 链
===========================================================================
R502（HTML presentational hints → CSS，CSS2 Appendix D）此前 REFUTED：
  - code-side 实现 verified CORRECT（img 计算尺寸 15→96，LAYOUT_DUMP 实证）
  - 但 ZERO reftest yield：positioning/absolute-replaced-width-* 全量 byte-identical。
  - 双重阻塞：(A) draw_order 覆写（abspos img 图元被 draw_order 后绘图元整片盖掉）
              (C) margin-collapse-through-border（abs_y 偏 80px，R490-entangled）
  - 代码已 REVERT（R370 先例）。

doc-side reopen 评估（commit e91e7924 / 3370f863）：block A「draw_order 覆写」的
本质（abspos positioned descendant 绘制过早被 in-flow 覆盖）正是 R504
（global positioned-descendant deferral, CSS Appendix E step 6, commit ea08de51）
所修 → R504 LANDED 后 block A 疑被解除 → 重开候选。
code agent 采纳并实现 R502（lib.rs +42 `collect_presentational_hints`），本 run 验证。

===========================================================================
1. R502 实现（code agent，未提交 @ 调研时）
===========================================================================
文件：crates/style-system/src/lib.rs（+42 行，git diff 核实）
  - collect_presentational_hints(doc, element)：
      * 仅 <img>（pseudo.is_none() 守卫）
      * 读 width/height 属性；裸数字（全 digit/'.'）→ "{v}px"；含单位/% 原样透传
      * 返回 Vec<(property, value)>
  - compute 步 1.5a：hints 经 expand_shorthands → Author origin + specificity (0,0,0)
    + 最早位置（None layer）。cascade 按 origin/layer/specificity/position 排序，
    (0,0,0) 低于任何真实选择器 ≥(0,0,1) 与 inline (1,0,0)，高于 UA 默认。CSS2 App D 语义正确。
  - 裸数字与 % 均路由进正常 CSS 解析（% 走 width 百分比 resolve；不需要 tree.rs 特殊路径）。

===========================================================================
2. broad reftest 实测（test-guard 包裹）
===========================================================================
命令：./target/test-guard -- cargo run -q --bin zero-wpt-runner -- reftest-upstream CSS2/normal-flow
输出：/tmp/nf_r502.txt（923 行，DONE）
Duration: 414.25s    Total: 746    Passed: 639    Skipped: 0    Pass Rate: 85.7%
  CSS2/                          639/746 (85.7%)

baseline（R548，无 R502）：/tmp/nf_r548b.txt → 636/746 (85.3%)

===========================================================================
3. A/B diff（fail list 对比，精确）
===========================================================================
R548 fails = 110；R502 fails = 107；net +3。
NEWLY PASS（R548 FAIL → R502 PASS，R502 wins）：
  css/CSS2/normal-flow/block-replaced-width-006.xht
  css/CSS2/normal-flow/inline-block-replaced-width-006.xht
  css/CSS2/normal-flow/inline-replaced-width-006.xht
NEWLY FAIL（regression）= 空（0 回归）。

3 wins 全是 §10.3.2 replaced-width 的 -006 子集（<img width=/height=> sizing 算法）。
  - inline-replaced-width-006 = width:50% 百分比案（master item 4 line 565 已记录）。

===========================================================================
4. 关键诊断：block A 确被 R504 解除 + tree.rs:191 冗余假设部分成立但未封顶
===========================================================================
(A) block A 解除确认：3 wins 全在 normal-flow（in-flow img，无 abspos paint-order 问题）。
    原 REFUTED 的 block A（abspos img 被 draw_order 后绘 in-flow 覆盖）在 normal-flow 不触发；
    R504 的全局 positioned-descendant deferral 使 abspos 不再过早绘制 → block A 解除。
    ⇒ 推翻 R502「实现 correct 但永久零 yield」结论。

(B) tree.rs:191 冗余（doc-side read-only 发现，crates/layout-engine/src/tree.rs:190-219）：
    tree.rs:191 早已读 img width/height 属性（`v.parse::<f32>()`）并在 CSS Auto 时
    设 taffy_style.size / aspect_ratio（intrinsic size）。
    ⇒ 裸数字 img w/h 已被 tree.rs:191 覆盖 → R502 对裸数字 REDUNDANT（无 flip）。
    ⇒ R502 真实 yield 范围 = %/override 案（tree.rs parse::<f32> 失败丢弃 "50%"；
       R502 lib.rs 透传 % 走正常 CSS width 百分比 resolve 才生效）。
    ⇒ 本 +3 wins（含 inline-replaced-width-006=50%）正落在 %/override 范围，与假设一致。
    （原 R502「15→96」LAYOUT_DUMP 实证即 %/override 案的尺寸修正，非裸数字。）

===========================================================================
5. 未决 / 后续
===========================================================================
- R502 提交状态：code agent 正跑 `cargo test --workspace`（make test 门禁，PID 74343 @04:56）
  待提交（lib.rs +42 @ 调研时仍未 commit）。本 +3/0-reg 为 reftest（rendering-compat 权威
  度量）确认；make test 为单元测试门禁（R502 42 行纯加性 cascade 变更，低风险）。
- positioning/absolute-replaced-width-006 子集（原 block C margin-collapse）仍可能阻塞，
  与 normal-flow 3 wins 是不同目录、不同机制（abspos + margin-collapse-through-border）。
  待 code agent 用 reftest-upstream absolute-replaced-width 验证；若仍 blocked = block C 独立。
- 本次仅测 normal-flow（285 个 img-w/h 文件的 35）；backgrounds(22)/borders(19)/positioning(52)
  等其它目录的 img-w/h yield 待后续 broad run。
