R342c — table.rs 拆分（table_borders 抽出），纯移动零回归
==========================================================

日期: 2026-06-19
类型: code-health（纯重构，零行为变更）
承接: R342（engine.rs）/ R342b（inline/mod.rs）。本轮收尾 2000 行规则——剩余最大违例
      `table.rs`（2694 行）。

动作（纯移动）
--------------
从 `crates/layout-engine/src/table.rs` 抽出 collapsed-border 解析集群到新模块
`crates/layout-engine/src/table_borders.rs`（740 行）：

  - resolve_collapsed_borders（513 行主函数，CSS 2.1 §17.6.2.1 边框冲突解析）
  - BorderSource enum + border_style_priority + resolve_border
  - get_cell_border_color / get_row_group_border_info
  - length_to_px / color_value_to_u32 工具

依赖处理（集群自包含，仅 1 处外部调用 + 共享类型/访问器）：
  - 集群内部互调（同模块，bare 引用不变）；**零调用出**到 table.rs 非集群函数。
  - 唯一外部调用方：table.rs layout_table（line 251）`resolve_collapsed_borders(...)` →
    table.rs 加 `use crate::table_borders::resolve_collapsed_borders;`（具体 import）。
  - 共享类型 TableCell/TableRow/TableGrid（table.rs 定义，build_grid 等也用）→ struct 与
    全部字段改 `pub(crate)`；table_borders.rs `use crate::table::{TableGrid, ...}`。
  - 共享访问器 get_row_box / get_cell_box / get_row_box_mut（table.rs 定义，build_grid 也用）
    → 改 `pub(crate)`；table_borders.rs 经具体 import 访问。
  - BorderSource（集群内 enum，resolve_border 返回）→ `pub(crate)`（否则 visibility warning）。

验证（零回归硬门禁）
--------------------
1. `cargo build --workspace` —— 零 error/warning。
2. `cargo clippy --workspace --all-targets -- -D warnings` —— 干净。
3. `cargo test -p zero-layout-engine` —— **888 passed / 0 failed**。
4. `reftest-upstream`（490 上游真实 WPT）—— **438/490 (89.4%)**，失败集与 R342b 基线
   **字节级一致**（`diff` = 空）。
   - **css-tables/ 51/55 (92.7%) 不变**——border-collapse/collapsed-border/subpixel 等
     全部边框用例 byte-identical，确认 border 解析逻辑零行为变更。
5. `cargo fmt` —— 已格式化。

结论
----
`table.rs` 现 1973 行（<2000）。至此 layout-engine 全部主要生产源码均 <2000：
  engine.rs 1717 / inline/mod.rs 1916 / table.rs 1973 / inline_finalization.rs 958 /
  float_positioning.rs 742 / table_borders.rs 740 / text_metrics.rs 229 / multicol.rs 628。
  剩余 >2000：gpu/renderer/mod.rs（2359，render-foundation crate）、app_render.rs（2156，
  browser crate）、若干 test 文件——独立 crate / 独立项。

**零行为变更**（纯移动，reftest 字节级一致，css-tables 边框用例全保）。非 pass-rate lever
——rendering-compat plateau（self-source 438/490 / strict 295/490 / Oracle ~35.6%）本身不变。

代码变更：table.rs -747/+27；lib.rs +1（pub mod table_borders）；新增 table_borders.rs（740）。
