R556 — backgrounds near-miss 单点扫描 = 0 clean win（全特性/结构性，印证 near-miss clean-win 穷尽）
日期: 2026-06-24
代码变更: 无（纯诊断）

================================================================================
扫描
================================================================================

R555（轨道1 import 穷尽）后转 CSS-correctness 单点扫描（R351 方法，R525-R546 证明
会再发现 clean win）。跑 css/CSS2/backgrounds self-source（339 cases）= 287 PASS /
52 FAIL。低 diff（near-miss）失败聚类：

  background-intrinsic-004.xht (1.37%) = background image **intrinsic ratio**
    （support/green-intrinsic-ratio-portrait.svg 无固有尺寸，按比例 cover bg
    positioning area）→ SVG intrinsic-ratio sizing 特性缺口
  background-applies-to-014.xht (1.39%) = display:**inline-table** 的 background
    （+ color/image/repeat-applies-to-014 同 1.39-1.46% 簇）
  background-*-applies-to-006.xht (1.58%) = display 簇
  background-attachment-applies-to-001~014.xht (2.40% ×14) = display:table-row-group
    等 table-internal 的 background-attachment:**fixed** + repeat-x

================================================================================
REFTEST_BBOX 逐案定位（全特性/结构性）
================================================================================

background-attachment-applies-to-001（2.40%，簇根）：bbox x=[8,103] y=[16,159]
（整个 table 区域），diff 11520px ≈ table cell 区域 background 整体渲染差异。
#test=table-row-group background-color:orange + image:blue96x96.png +
attachment:fixed + repeat-x。ZeroWeb 未正确画 table-row-group 的 background
（taffy 0.7 table 模型对 table-row-group box 几何/background 绘制，R177 table 谱系）。

background-intrinsic-004（1.37%）：bbox x=[26,91] y=[61,160]，div 60×80 的
intrinsic-ratio-only SVG background sizing 差异（SVG 无固有尺寸按比例 cover，
background-image intrinsic sizing 特性缺口）。

================================================================================
裁决：near-miss clean-win 再次穷尽（0 clean win）
================================================================================

backgrounds 低 diff 失败全落**特性缺口/结构性**：
  - table-internal（table-row-group/inline-table）background 绘制 = taffy table
    模型结构性（R177 谱系）。
  - background-attachment:fixed = 视口固定背景特性（DC-12 高级视觉，未完全）。
  - SVG intrinsic-ratio background sizing = background-image intrinsic sizing 特性。

0 单点 clean lever。印证 master.md「near-miss clean-win frontier 穷尽」
（R352/R354/R363/R365 多轮 0/N 衰减 + R369b false-negative 亦结构性）。
R525-R546 的 clean win 来自 CSS-correctness 单点 + 缺失资产，**非** near-miss。

================================================================================
本会话总结（R554-R556）
================================================================================

- R554 content-list/伪元素：实证 net-negative 回退（generated-content -1 +
  content-counter-001 未解，伪元素需 layout 独立 box 架构）。
- R555 轨道1 import 穷尽：css-text（1302/1302）+ CSS2 子目录（0 gap）分母全量。
- R556 backgrounds near-miss：0 clean win（全特性/结构性）。

本会话净代码 yield = 0（R554 全回退 + 纯诊断），但**诊断全面确认 lever 空间**：
单会话 clean lever（CSS-correctness 单点 + near-miss + 分母 import）已穷尽。
剩余 forward motion 全是多 session 架构（Phase A IFC 统一 ~2200 案 / 伪元素独立
box / multicol fragmentation / baseline-export）或慢/OOM-prone（chromium oracle）。

================================================================================
下一步
================================================================================

渲染架构多 session lever（master 迄今最大）：
  - Phase A IFC 统一（paint 不重跑 IFC，复用 layout 存储行盒；释放 ~2200
    fontdue/IFC near-threshold 案 + welcome/morning 文本度量残余）。设计文档
    phase-a-IFC-unification-design.md；R355/R358/R362 三个 narrow viable-slice
    先例（多行存储/per-fragment color/float 侵入）。
  - 伪元素 generated content 独立 box（R554 前置阻塞）。
  - multicol fragmentation / baseline-export。

DC-14 真目标验证（慢/OOM-prone，须分批/限量）：
  - chromium oracle 重跑 css-text/CSS2（覆盖 R551/R552 新导入测试，R471 已有
    css-text 基线 18.8%）。
