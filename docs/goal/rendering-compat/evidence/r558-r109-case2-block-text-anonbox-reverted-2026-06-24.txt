R558 — R109 case#2（block 容器直接文本子匿名块盒）实测 net-negative REVERTED；零代码落地
日期: 2026-06-24

================================================================================
背景
================================================================================

R557b 确认 content-047（body:before content:attr 注入文本）真根因 = body（block）
含 [inline text 注入] + [p block] 混合内容，ZeroWeb 不生成匿名块盒（R109 谱系）。
tree.rs:640-649 block 容器（非 flex/grid）else 分支**仅收集 Element 子**（line 645
`NodeKind::Element(_)`），跳过直接文本子 → inject 注入的 body:before 文本被丢弃
（不在 layout 树，LAYOUT_DUMP 实证）。

R558 尝试最小切片：block 容器 else 分支对非空直接文本子创建匿名 block taffy leaf
（仿 flex/grid 分支 tree.rs:530-543），让文本进 layout 树。

================================================================================
实现
================================================================================

tree.rs block 容器 else 分支：children_with_order 收集 Element 子 + 非空 Text 子；
Text 子创建 `new_leaf_with_context(display:Block, child_dom)` 匿名 leaf（context=文本
NodeId，供 IFC 测量 + inline_finalization）。编译通过（修一处闭括号）。

================================================================================
A/B 实测（test-guard 包裹 reftest-upstream）= net-negative
================================================================================

- **content-047**：1.29% → **1.19%**（仅 -0.10pp，仍 FAIL）。text 进 layout 树但
  位置/渲染细节仍差（text 可能本就在 paint IFC Path B 渲染，layout 匿名 leaf 仅微调）。
- **css/CSS2/normal-flow：baseline ~639/746 → WITH-FIX 633/746 = -6 PASS 回归**。
  block 容器直接文本子进匿名 leaf 改变了大量 normal-flow 用例的布局（block 直接
  文本普遍存在，匿名 leaf + paint IFC 双重/几何变化致回归）。

→ **REVERTED**（git checkout tree.rs，工作树 = R557b HEAD 6727db9b）。

================================================================================
裁决
================================================================================

R109 case#2（block 容器混合 inline+block 内容匿名块盒）实证 net-negative（content-047
-0.10pp + normal-flow -6），**再次印证 R361「taffy 块堆叠 / IFC 误定位均错，单会话
高风险」**。R558 = R554 之后第二个 net-negative 回退的复杂 layout lever。

根因深化：
  - block 容器直接文本当前通过 **paint IFC Path B**（paint_text 重跑 IFC 读 DOM 文本）
    渲染（不依赖 build_subtree taffy 子节点）。故 build_subtree 跳过文本（line 645）但
    css-text 89% PASS（文本在 paint 渲染）。
  - R558 让文本进 build_subtree 匿名 leaf（layout 高度）+ paint IFC 仍渲染 = 双重/
    几何不一致 → normal-flow -6 回归。
  - content-047 的 1.29% diff 主要来自 **paint IFC Path B 对混合内容（text + block 子）
    的文本定位**，非 build_subtree 跳过（text 在 paint 渲染但位置差）。layout 匿名 leaf
    改动触及不到 paint IFC 定位 → 仅 -0.10pp。

★ **真解 R109 case#2 须协调 layout 匿名块盒 + paint IFC Path A/B 统一**（Phase A IFC
统一前置），非 build_subtree 单点。与 R554（伪元素独立 box）、Phase A（墙② multicol）
同属多 session 架构，勿再以 build_subtree 匿名 leaf 单点重试。

================================================================================
本会话总结（R554-R558）
================================================================================

- R554 content-list/伪元素：net-negative 回退。
- R555 轨道1 import 穷尽（css-text/CSS2 全量）。
- R556 backgrounds near-miss 0 clean。
- R557/R557b generate/cascade 落伪元素 + R109（content-047 真根因 R109 非 R554）。
- R558 R109 case#2 build_subtree 匿名 leaf：net-negative 回退（content-047 -0.10pp +
  normal-flow -6）。

本会话净代码 yield = 0（R554/R558 全回退 + 纯诊断）。**五轮独立实验/诊断全面确认**：
单会话 clean lever 穷尽；R109 case#2 / 伪元素独立 box / Phase A IFC 全是多 session
架构（layout + paint IFC 协调），单点尝试均 net-negative（R554/R558 双证）。

下一步唯一可行 = 多 session 架构（Phase A IFC 统一前置解 R109 case#2 + 伪元素 +
large-font + welcome/morning 文本度量，~2200 案，迄今最大 lever）或慢 OOM-prone
chromium oracle（DC-14 真目标量化）。
