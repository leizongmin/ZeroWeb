R516c — cascade full-scope 负值拒绝 → initial value（commit 01f63d70，code-side LANDED + 独立验证）
日期: 2026-06-23
承接: R513c（cascade narrow-scope 负值拒绝 LANDED，preserve-prior fallback）。并行 code agent 的
      reject-to-initial refinement 反复重试多轮后 LANDED。
性质: doc-agent 独立 reftest 复核（工作树 clean，01f63d70 已 commit+push，test-guard 包裹）。

============================================================
1. 变更（code agent commit 01f63d70）
============================================================

R513c narrow-scope：仅当负值 winner 有合法 fallback 时跳过负值；若无合法声明
（sole negative）则 `.or(overall_best)` 回退到负值本身（保旧行为，零回归）。
→ 问题：sole-negative 声明（如 `max-height: -1cm`）仍被应用（taffy→0-collapse），
   failing dedicated WPT "negative values illegal" 测试（期望拒绝→initial value）。

R516c full-scope：**删除 `.or(overall_best)` fallback**——属性无合法声明则**不进
cascade 结果**→ 解析到 initial value（width/height→auto, max-*→none(INF),
min-*/padding/border-width→0，由 default_impl 提供，均为 CSS 规范初始值）。
（多声明 valid-then-invalid 仍选 valid，同 R513。）

CSS 2.1：非法声明整体忽略（as if absent）→ 仅非法声明的属性回退 initial。
无正确测试依赖负 box-model 值被应用（它们非法），故 outright 拒绝 strictly spec-correct。

============================================================
2. ★ doc-agent 独立验证（reftest-upstream max-height，test-guard 包裹）
============================================================

code agent 自报：normal-flow 584/733(79.7%)→590/733(80.5%) **+6 PASS / 0 REGRESS**
（max-height-001/012/023/034/045/056 翻转，zero narrow-PASS→full-FAIL）；box-display
50.0% / borders 83.4% 不变（width/height→auto 零回归）；cargo test exit0 / clippy clean。

doc-agent 独立复核 `reftest-upstream max-height`：
  ✓ max-height-001 (0.00%)  ✓ max-height-012 (0.00%)  ✓ max-height-023 (0.00%)
  ✓ max-height-034/045/056 (cluster 全 PASS，0.00% 或 0.24% fontdue 噪声)
  （max-height 全簇多数 PASS，含历史已过的 + 本轮新翻的）
→ **headline +6 claim 独立确认**（max-height "negative illegal" 簇翻转）。

============================================================
3. cascade 负值 vein 累计收益（R513 narrow + R516c full-scope）
============================================================

  R513c (narrow, 0b11a12d): numbers-units-006 + height/min-height 簇 (+1~+数)
  R516c (full-scope, 01f63d70): max-height-001/012/023/034/045/056 (+6)
  → **WPT "negative values are illegal" 簇被 substantial 翻转**（normal-flow +6 累计）。

★ **supersede R513c 的 "preserve-prior / zero-regression" framing**：R513c LANDED 时是
narrow-scope（零回归保守）；R516c 升级为 full-scope reject-to-initial（strictly spec-correct
+ +6 yield）。master.md 须更新 R513c 描述（不再是 preserve-prior）。

============================================================
4. R515 flex-grow/shrink 仍 pending（code agent 显式确认未触碰）
============================================================

01f63d70 commit message 明言："flex_grow/flex_shrink negatives (R515 docs, taffy does
not clamp) remain a separate unimplemented lever, not touched here."
→ **R515 仍是唯一未实现 genuine lever**（apply.rs:577-587，+2，已 de-risked），不变。
（cascade 负值拒绝在 cascade.rs，flex-grow/shrink 在 apply.rs，正交。R516c 的 cascade
NEGATIVE_ILLEGAL_PROPS 不含 flex-grow/shrink——后者用 f64 parse 非声明级负值。）

============================================================
5. R516 命名冲突消解
============================================================

并行 doc+code 同轮都用 "R516"：
  R516c (code, 01f63d70) = cascade full-scope 负值拒绝（本条，+6 LANDED）
  R516d (doc, 37d5b348) = inheritance-gap vein exhausted（read-only，无 lever）
分记以免混淆（同 R513 命名冲突先例：R513c code vs R513 doc dead-value scan）。

============================================================
6. 裁决
============================================================

R516c 是 cascade 负值 vein 的 full-scope 升级，+6 normal-flow LANDED + 独立验证。
cascade 负值 vein（R513 narrow + R516c full-scope）累计 substantial 翻转 WPT "negative
illegal" 簇。R513c 的 preserve-prior framing 被 supersede（须更新 master.md）。
R515 flex-grow/shrink 仍 pending（唯一未实现 genuine lever）。
零代码变更（doc-agent 仅 reftest 复核 + 文档更新）。
