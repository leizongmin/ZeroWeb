# R396 — grid 全量分母缺口簇抽样（read-only doc-maintenance）

日期：2026-06-22
HEAD：f2c9fae (R395)，无新 commit
性质：read-only 核查 R395 CONTINUE「更大分母是否重开 clean-win 面」；零代码 / 零 reftest 变更

## 状态

并行 agent mid-work（**未提交**）：
- css-grid 已导入 ~70 顶层 .html（~40+ 真测试，已过 R395 的 63 目标）；
- `tests/wpt-runner/scripts/discover-reftests-authoritative.py` 已落地——按 `<link rel="match">`
  权威解析 test→ref 对（替代 `discover-wpt-reftests.py` 的文件名启发式，后者 miss 非标准 ref 命名）；
- quirks/ 目录起步（`reference/green-100px-square-no-red.html`）。

本轮**无法**跑 reftest 量化全量 grid 真通过率：并行 agent 未提交 + run-rules 禁裸跑 +
`make reftest` 与并行 agent 冲突 / OOM 风险。待提交后折回。

## 抽样：2 个 R395 标记的已知缺口簇测试（read-only 读 .html 测试数据，非 .rs 源码）

### 1. grid-container-baseline-synthesized-001.html（grid-baseline 簇）
- 测 inline-grid 空项 baseline 合成，跨 5 writing-mode（horizontal-tb / vertical-rl / vlr /
  sideways-rl / lr），子元素含 flex/grid/table，依赖 Ahem 字体。
- 映射 = **baseline-export 簇**（R266 / R310 / R312 / R313 / R316 五轮 ruled out）——需
  taffy 0.8+ `baseline_overrides` 或自建 inline-level-box baseline 合成；叠加 writing-mode
  垂直轴（R114 / R164 谱系）。
- **非单会话 clean win**。

### 2. replaced-element-percentage-height-in-grid-nested-in-flex-001.html（replaced-element-in-grid 簇）
- 测 `<img height:100%>` @ `grid(height:100%; grid-template-rows:1fr 1fr)` @
  `flex(height:200px; flex-direction:column)`。
- 映射 = **flex/grid definite-size resolution 簇**（R363 fixed-table-in-flex / R97 max-content /
  R304 taffy 升级 DEFER）——grid height:100% 需对 flex definite 200px 求解，img height:100% 需对
  grid track 求解，taffy 0.7 不跨层传递；+ 需图片子资源 `grid-items/support/100x100-green.png`。
- **非单会话 clean win**。

## 结论

R395 caveat「R384 单会话 clean-win 47/47 穷尽是**子集范围**（490 案）；全量集合含未检新失败模式，
clean-win 面在更大分母下可能重开」——在「更大分母含真实缺口簇」方向**成立**：新 grid 测试确覆盖
baseline / replaced-element / fragmentation / grid-within-flex / form-control stretch 等簇
（R395 预测的缺口簇在导入集中真实存在）。

但抽样显示这些新失败**映射到已 ruled-out 的多会话架构族**（baseline-export / flex-definite-size /
fragmentation Phase 2），**非「重开单会话 clean-win 面」**——是同族更多实例，非新 lever。

- 轨道 1（DC-14 分母去子集化）方向不变：既满足 DC-14 硬门禁，又为轨道 2-4 优先级提供全量真通过率校准。
- 轨道 2-4（渲染架构）的全局 ROI 须待轨道 1 揭示全量数字后校准。
- 待并行 agent 提交 Phase 2（grid 全量 + reftest 复测 + chromium-Oracle 补抓）后，把全量 grid
  真通过率折回 master.md 基线 / 前向轨道。

## 代码变更

零（read-only；未触碰并行 agent 的 .py / .html / .rs）。
