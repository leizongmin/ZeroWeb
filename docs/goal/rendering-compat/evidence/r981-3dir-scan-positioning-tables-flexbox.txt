R981 — fresh-oracle 3 dir 系统扫（CSS2/positioning 578 + CSS2/tables 1139 + css-flexbox 506）
═══════════════════════════════════════════════════════════════════════════════════════════

承 R980 下会话 ①「fresh-oracle 扫未扫大 dir（positioning/tables/selectors，taffy-backed）」。
本轮系统扫 3 dir + 深查 5 lever，**零 net 源码**（纯调查，全部 lever 落 font-墙/structural/复杂 spec 算法缺口）。
关键价值：① 闭 R980 open item ①（3 大 unswept dir 系统扫完）；② de-risk abspos auto-margin（0-yield，font-墙阻挡）；
③ 识别 flexbox top-1 standout flex-minimum-width-flex-items-013（82%）= csswg #5663 transferred-size-suggestion 算法缺口（未来多 session 头号 lever）。


① CSS2/positioning fresh oracle：296/520 = 56.9%（cf. self-source ~56.5%）
─────────────────────────────────────────────────────────────────────────
top-15 worst 全部落 font-墙 / XML 命名空间 trap / shrink-to-fit+rtl / abspos-auto-margin（0-yield）：
  - abspos-008 17.52% = `.container{font-size:100px}` → large-font 墙（R101/R125 谱系）
  - abspos-float-with-inline-container 16.81% = abspos+float+relative 结构性
  - top-091/092 + bottom-091/092（15-16% 对称对）= `6ex` 单位 = ex-font-metric 墙（R544/R547 已优化 0.8em，残余）
  - absolute-non-replaced-width-021..024（9.73% 4 案簇）= abspos width:auto + direction:rtl + static-pos right + 子 max-width:4em
    = shrink-to-fit（§10.3.7，taffy-blocked R180/R370）+ rtl，非单点
  - absolute-replaced-width-020/039/053/067（7.66-9.6% 簇）= `<svg:svg>` XML 命名空间前缀 trap（R980 已 ruled out，html5ever HTML 模式 local_name="svg:svg"→CSS `svg{}` 不匹配）
  - absolute-non-replaced-height-004 7.60% = abspos vertical auto-margin（§10.6.4，见下 de-risk）

  ★ positioning 无 font-墙外 clean sizing lever（无 R976 式 standout：top-1 17.5% 远低于 R976 的 64%）。

  abspos auto-margin（§10.6.4/§10.3.7）de-risk（0-yield，font-墙阻挡）：
    height-003/004/005 测 abspos top+bottom fixed + height fixed + margin-top/bottom:auto → auto 吸收 leftover 居中。
    现状：abspos.rs::adjust_absolute_pct_to_viewport（line 113）的 auto-margin handler（line 180-247）
    **仅覆盖无 positioned ancestor 的 abspos（CB=视口）+ 仅 horizontal max-width-clamp 特例**。
    height-003/004/005 的 CB 是 positioned ancestor（`#div1{position:relative}`），走 taffy 路径（不 solve auto-margin）。
    机制实证（probe）：margin-top:auto → 0（未 solve）。height-005（margin-top:*explicit* 0.5in）= 1.90% = `<p>` font-墙
    + 正确居中；height-004（margin-top:*auto*）= 7.60% = 错位（0.5in=48px 下偏）。修 margin-top:auto → 0.5in 可正位，
    但 003/004 仍卡 ~1.9%（`<p>` font-墙）= **0 新 pass**（同 R979 模式但可视收益更弱：51→41 vs 7.6→1.9）。
    须新 post-process pass（找 positioned-ancestor CB + 其尺寸 + solve 两轴），moderate effort + abspos 回归风险，
    yield 0 → **本轮不投，flag 待 font-墙解后该簇（height-003/004/005 + width 类比）可批量翻 PASS**。


② CSS2/tables fresh oracle：48/364 = 13.2%（cf. css-tables module 59%——经典 CSS2.1 suite 远难于现代 module）
─────────────────────────────────────────────────────────────────────────
top-15 worst = border-collapse 结构性 + table-backgrounds 列层架构 + large-font：
  - collapsing-border-model-010b 44.40%（standout）= border-collapse:collapse + cell border/bg 盖红 = §17.6.2 结构性（ZW border-collapse 部分实现，R977 残余 border 精度）
  - border-collapse-006 29.08% / collapsing-border-model-011/013 17.28% 对 = border-collapse 结构性
  - table-vertical-align-baseline-009 24.66% = vertical-align in cell（font-墙/baseline）
  - table-backgrounds-bs/bc-{table,colgroup,rowgroup}-001 簇（6 案 9-20%）= §17.5.1 表格背景层绘制
    （table→colgroup→col→rowgroup→row→cell 层序）。painter/mod.rs:454 `is_table_internal` 含
    TableRowGroup/HeaderGroup/FooterGroup/Row（bg 绘制 ✓）但**不含 TableColumn/TableColumnGroup**——
    列/列组背景须 §17.5.1 层架构（列盒无真实几何，cell 透明透出列 bg）= 多 session 架构非单点
  - table-anonymous-objects-214 9.08% = `font:200px/1 Ahem` large-font 墙

  near-pass 簇（128 案 1-2% + 33 案 2-3%）= **font-墙主导**（instruction `<p>` + cell 文本 serif/monospace）：
    - table-anonymous-objects-0{09..20,167..204,211..213}（50+ 案 ~1.05%）= 共享 `<p>` + monospace 文本 font-墙
    - fixed-table-layout-003a/d 系列（24 案 1.04-1.41%）= `font:1.25em/1.2 serif` + cell 文本 font-墙
      （column width 实测正确——若错 80px vs 200px 应 ~50% diff 非 1%；1.04% = serif text font-墙）
    - caption-side-applies-to-008..014（7 案 2.05%）= caption-side:bottom **已实现**（table.rs:1298-1306）
      + 测的 caption-side 在 row-group（非 caption 祖先，不继承）+ `<p>`/caption default-font font-墙
    - border-conflict-element-001a/b/c（2.96/2.79/2.61%）= border 冲突 §17.6.2 结构性 + font

  ★ tables 无 clean sizing lever（13% 低通过率 = border-collapse 结构性 + 列背景层架构 + font-墙三重阻塞）。
    R978 table-fixup（bare text）是 tables 已发现的唯一 clean lever，已 LANDED。


③ css-flexbox fresh oracle：284/497 = 57.0%（cf. self-source ~57%）
─────────────────────────────────────────────────────────────────────────
★ top-1 standout = **flex-minimum-width-flex-items-013 82.04%**（远高 #2 的 33.65%）= R976 式 clean lever 信号！
  但深查 = 复杂 spec 算法缺口（非单点）：
    `<img src=300x150.png style="width:999px">` in `<div style="display:flex;width:0;height:50px">`。
    预期 img width=100px（flex item min-width:auto = transferred-size suggestion = 拉伸 cross-size 50px × 固有比 300/150=2 = 100px，
    csswg #5663「content size suggestion accounts for def stretched cross size」）。
    ZW converter（mod.rs:96-104）传 aspect_ratio + min_size 给 taffy 0.7，但 taffy 0.7（vendored ~2023）未实现 #5663。
    ZW 82% diff = img 未被 clamp 到 transferred min-size（render 999px 溢出或塌缩）。
    = **flex §4.5 transferred-size-suggestion 算法实现**（taffy-blocked，须 post-taffy flex-item min-size 计算，
    chicken-and-egg w/ taffy cross-axis stretch，多 session 类 R717）。

  top-15 余簇多 aspect-ratio（007/003/004/011/014 15-33% + flex-aspect-ratio-cross-size-001 + flex-item-transferred-sizes）
    = **aspect-ratio + flex sizing 系统簇**（区别 R976 css-grid 的 aspect-ratio-overrides-intrinsic，那是单行 fix；
    flex 的 transferred-size-suggestion 是 §4.5 完整算法）。
  - flexbox-flex-flow-001/002 23% = flex-flow 简写展开（可能 parser gap，待查）
  - flex-abspos-inset-nested-002/cross-size-001 19.4/17.4% = abspos in flex（R98/R500 谱系，nested CB）
  - flexbox-collapsed-item-horiz-001 15% = visibility:collapse flex item（R111 已修 strut，残余）
  - content-height-with-scrollbars 17.6% = overflow scroll 容器（host-layer 缺口）


综合裁决（fresh 证据再证 doc 演进·10+ 次 clean-lever 穷尽确认）
═══════════════════════════════════════════════════════════════════════════════════════════
3 dir 系统扫**零 clean single-session measurable lever**。clean sizing lever 现**确系稀缺**：
  - R976 css-grid aspect-ratio（4× 误差 + 单行 fix）是罕见命中（stale 基线 + 大误差偶然）
  - R978 css-tables table-fixup（orphan text）是 tables 唯一 clean 命中
  - R979 backgrounds canvas-propagation（color absorption）是 correctness 0-pass 命中
  本轮 positioning/tables/flexbox 3 dir 余 residual 全 font-墙 / structural / 复杂 spec 算法（transferred-size / 列背景层 / border-collapse）

clean single-session lever 在 CSS2 + css-flexbox 已扫 dir 内**确系耗尽**（R974/R977/R980 + 本轮 R981 累计确认）。
勿再以「fresh-oracle 必中 clean lever」盲目扫已扫 dir。

▶ 下会话（measurable 推进路径，全多 session 架构，按 rally 跨会话协议续跑）：
  ① **flex transferred-size-suggestion（csswg #5663）多 session 攻坚**——flexbox top-1 standout 82% + aspect-ratio 簇
    （007/003/004/011/014 等 15-33%）。须 post-taffy flex-item min-size:auto 算法（§4.5：transferred = stretched cross × ratio，
    min(specified, transferred, content)）。taffy 0.7 未实现，ZW 须自行实现 + 处理 cross-axis stretch chicken-and-egg。
    头号未来 lever（standout 82% + 簇广）。先 RFC + Phase 0 probe（量 ZW 当前 img width）再改码。
  ② **Phase-A IFC 度量统一**（font-墙，DC-2~5 主线，R975 Phase 0 契约：frag_baseline_y 锚 + fontdue 度量）——
    解后 positioning ex/large-font 簇 + tables near-pass 128 案 + 多 dir font-墙批量翻 PASS。最高杠杆但 6 次 net-negative 先例。
  ③ **R717 full ratio-signal fix**（4 crate，replaced SVG intrinsic，R980 decode-trap 已 ruled out decode-level）
  ④ **真 CSS Tables §3.1 匿名 cell**（R978 partial 之上）+ 列背景层架构（§17.5.1）
  ⑤ abspos auto-margin（§10.6.4/§10.3.7）待 font-墙解后批量 yield（本轮 de-risk：0-yield 因 font-墙）

single-session clean measurable lever 勿再扫（10+ 次确认）。
