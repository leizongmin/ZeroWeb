R464 css-text oracle 捕获脚本子目录递归 bug 定位（同 R458/R459 谱系）—— oracle 缺失根因
================================================================================
日期: 2026-06-21 17:25 CST
承接: R463（css-text self 89.2% 完成；oracle 待捕获验证 R461 30-38%）
状态: oracle 数据仍缺；本轮定位根因 = 捕获脚本 bug，非 chromium/oracle 本身问题
本轮产出: oracle 缺失根因定位 + 修复在途（并行 agent working tree）+ 全量耗时预判

--------------------------------------------------------------------------------
1. 现象：17:21 oracle 试捕只抓到 8 个（应 1914）
--------------------------------------------------------------------------------
- /tmp/csstext_oracle.log（mtime 17:21，reftest 完成时刻触发）:
    css/css-text: 8 ok, 0 fail
    TOTAL: 8 ok, 0 fail
- 8 ok 0 fail = 脚本基础功能正常（chromium + HTTP server + 截图链路通），但范围严重不足
- 当前无 oracle 进程在跑（ps chromium/oracle/puppeteer 全空），无 cross-validate 结果

--------------------------------------------------------------------------------
2. 根因：capture-oracle-per-dir.mjs 顶层 readdirSync bug（同 R458/R459 discover 谱系）
--------------------------------------------------------------------------------
- 旧脚本（HEAD）: `readdirSync(dir).filter(f => f.endsWith('.html'|'.xht') && !ref...)`
  → 只读 category 目录**顶层**文件
- css-text 结构 = 9 顶层 .html + 30 子目录（test 散落在 white-space/i18n/hyphens/... 子目录）
- 实测（find -maxdepth 1 excl ref）: css-text **顶层 test-ish = 8** ← 与 oracle log "8 ok" 精确一致
- 实测（递归 excl ref）: css-text **全量 test-ish = 1914**（= R461 test-ish 数，略大于 runner 1572 权威对，因含无 link rel=match 的 manual/crashtest）
- → 顶层 readdirSync 漏掉 30 子目录的全部 test，只捕到 8 个顶层
- **同构 bug 谱系**: R458/R459 已发现 discover-reftests-authoritative.py 的顶层 bug（css-text 0 对），
  1568cb0 修 discover；本轮发现 oracle 捕获脚本有**完全相同**的顶层 bug，尚未修

--------------------------------------------------------------------------------
3. 修复在途：并行 agent working tree collectTests 递归（未提交）
--------------------------------------------------------------------------------
- git diff capture-oracle-per-dir.mjs（working tree，未 commit）:
  - 新增 `collectTests(dir, base)` 递归收集所有子目录 .html/.xht（excl -ref/notref/reference）
  - 扁平目录（grid/flex/...）退化为仅文件名，行为不变（无回归风险）
  - 替换 `readdirSync(dir).filter(...)` → `collectTests(dir)`
- 比 discover R458 更简单: oracle 只捕 **test 侧**（line 2 注释「全量捕获 test 文件」），
  不需 ref 路径相对 test 目录解析（discover R458 的第二项修复）→ collectTests 单点修复即完整
- 状态: 修复在 working tree（未提交），待并行 agent commit + 跑全量

--------------------------------------------------------------------------------
4. 预判：全量 oracle 耗时 ~50min（node 脚本不经 test-guard，可长跑）
--------------------------------------------------------------------------------
- 全量 css-text oracle = 1914 截图（chromium headless 逐个）
- 参考 writing-modes 787 oracle 捕获（R457 完成）≈ 21min → 单截图 ~1.6s
- css-text 1914 × 1.6s ≈ 3062s ≈ **51min**（2.4× writing-modes）
- oracle 捕获是 node + puppeteer 脚本，**不经 test-guard / make reftest**（oom-guard 针对内存型
  reftest/cargo test），无 1800s time-limit → 可长跑（~50min 可行，只要 chromium 不挂）
- 风险: chromium 长跑偶发挂起/泄漏（writing-modes 787 已验证可行，1914 量级未测）；
  若挂可分批（如按子目录 white-space/i18n/... 分次 --category 子路径，但脚本按 category 顶层目录）

--------------------------------------------------------------------------------
5. 系统性印证：css-text/CSS2 嵌套目录暴露三脚本同构顶层 bug
--------------------------------------------------------------------------------
- discover-reftests-authoritative.py: R458/R459 发现 + 1568cb0 修复（递归 + ref 路径）
- import-wpt-reftests 流程: R459 确认 subdir-safe（import 用 git/trees recursive=1，10db810）
- capture-oracle-per-dir.mjs: **本轮 R464 发现**（顶层 readdirSync），working tree collectTests 修复
- → css-text/CSS2 这类「test 散落子目录」结构系统性触发顶层 readdirSync bug；
  CSS2（43 子目录）下批 oracle 捕获同样依赖此修复（并行 agent 提交后 CSS2 直接受益）

--------------------------------------------------------------------------------
结论
--------------------------------------------------------------------------------
- oracle 缺失根因 = capture-oracle-per-dir.mjs 顶层 readdirSync bug（8/1914，同 R458/R459 谱系）
- 修复 = 并行 agent working tree collectTests 递归（未提交，扁平目录无回归，oracle=test 侧单点完整）
- 8 ok 0 fail = 脚本基础功能正常，仅范围 bug
- 全量 oracle 预判 ~51min（1914 截图，node 脚本不经 test-guard 可长跑）
- 零代码变更（read-only + 日志/diff 观测，未触碰 .mjs/并行 agent 工作树）
- 下一步: 并行 agent 提交 collectTests → 全量 css-text oracle（~51min）→ cross-validate chr<1%
  → 验证 R461 30-38% → 9 目录 oracle 聚合更新（当前 26.8% @8 目录）→ 最后 CSS2（同需此修复）
