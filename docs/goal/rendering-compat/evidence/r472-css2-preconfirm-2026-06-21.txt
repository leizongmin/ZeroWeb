# R472 css/CSS2 全量导入前 pre-confirm（read-only，第 10 目录·最后一个 DC-14 目录）

**日期**: 2026-06-21 18:13–18:20（R471 css-text 完成后）
**方法**: read-only（git/trees API + 仓库 grep；零代码变更；并行 agent 拥有 capture/import）

## 1. 上游 css/CSS2/ 结构（git/trees HEAD:css/CSS2，非 recursive）

- **43 子目录** + 22 顶层 blob（确认 R458；css/css2/ 上游 404，仅 css/CSS2/ 存在）
- 子目录：abspos, backgrounds, bidi-text, borders, box, box-display, cascade, cascade-import,
  colors, css1, css21-errata, csswg-issues, floats, floats-clear, fonts, generate, generated-content,
  i18n, linebox, lists, margin-padding-clear, media, normal-flow, other-formats, page-box, pagination,
  positioning, reference, run-in, sec5, selectors, stacking-context, support, syntax, tables, text, ui,
  values, visudet, visufx, visuren, zindex, zorder

## 2. 上游全量规模（git/trees recursive=1，1 调用，13123 entries，非 truncated）

- .html=805 + .xht/.xhtml=10633 = **11438 html+xht**
- reference-ish（含 -ref/ref-/-reference）= 2185
- **test-ish（非 ref）= 9253**（reftest 对上界；真实 link=match 对须 discover 解析）

**结论**：CSS2 是迄今最大 DC-14 目录（4× css-text 2994 文件 / 11× writing-modes 1051）。

## 3. 子目录 test-ish 分布（fixed counter：path 相对 css/CSS2/，subdir=parts[0]）

| 子目录 | test-ish | 子目录 | test-ish |
|--------|---------|--------|---------|
| tables | 1139 | floats-clear | 249 |
| normal-flow | 857 | ui | 239 |
| borders | 763 | floats | 144 |
| margin-padding-clear | 739 | box-display | 140 |
| backgrounds | 626 | bidi-text | 108 |
| selectors | 621 | pagination | 103 |
| positioning | 578 | visufx | 102 |
| text | 565 | cascade | 101 |
| fonts | 324 | i18n | 57 |
| generated-content | 316 | visuren | 57 |
| lists | 286 | zindex | 52 |
| syntax | 280 | visudet | 40 |
| css1 | 262 | page-box | 36 |
| linebox | 250 | abspos | 31 |

余 sec5 29 / values 26 / colors 22 / cascade-import 19 / generate 17 / media 17 / (top) 16 / box 11 /
css21-errata 9 / reference 7 / stacking-context 7 / other-formats 4 / csswg-issues 3 / zorder 1。

**聚类**：~93% 在核心基础（tables/normal-flow/borders/margin-padding/backgrounds/selectors/
positioning/text/fonts/lists/syntax/linebox/floats 等 ZeroWeb 主要已实现区）；~7% 在结构性缺口子目录
（pagination 103 + page-box 36 = 139 page-break 未实现；bidi-text 108 BiDi 生产未接；cascade 101；
generated-content 316 content/counters 部分；zindex 52 / stacking-context 7）。

## 4. 当前 wpt-data 导入（subset）

- 仅 **10 内容子目录**（abspos/backgrounds/borders/box/colors/floats/floats-clear/fonts/linebox/normal-flow）+ reference
- 537 文件 / ~132 match-declaring / ~22 test-ish html（顶层 only discover 漏子目录，同 R458 谱系）
- **覆盖率 ~6%**（537/11438），缺 33 子目录含大头部（tables/selectors/positioning/text/generated-content/lists/syntax/...）

## 5. 实现核查（read-only grep，style-system/css-parser/engine）

- ✅ 已实现：z-index（142 refs）/ content（31）/ quotes（144）/ 基础盒模型/颜色/背景/边框/linebox/normal-flow/abspos
- ❌ 缺口（CSS2-专属）：
  - `run-in`：0 refs（display:run-in 未实现 → run-in 子目录 fail）
  - `counter-reset`/`counter-increment`：0 refs（属性未注册；paint 有 counters.rs 部分处理 → generated-content 计数器部分 fail）
  - `page-break`/`break-`：0 refs（→ pagination 103 + page-box 36 = 139 tests fail）
- ⚠️ 未核：selectors（621，选择器引擎覆盖度）/ lists（286，list-style）/ cascade（101，@import/cascade，R175 var 继承已修但 @import？）/ syntax（280，CSS 解析边界）

## 6. chromium-Oracle chr<1% 预测

**预测 ~30-42%（居中 ~36%，可能为迄今最高/次高目录）**

依据：
- ~93% 案在核心基础（tables/normal-flow/borders/margin-padding/backgrounds/selectors/positioning/text），
  类 css-tables 43.8% / css-flexbox 50.6%（布局基础类 40-50%）；CSS2 基础占比更高（纯 CSS 2.1 核心，ZeroWeb 围绕它构建）
- 下拉项：~7% 结构性缺口子目录（pagination/bidi/cascade/counters）+ 普遍 near-miss font/metric 噪声（每目录 chr<5% broad 都高但 chr<1% 被 1-5% 桶压低）+ selectors/lists/cascade 覆盖度不确定
- 预测乐观偏差警示：R461 css-text（30-38%→18.8%）/R447 writing-modes（33-42%→5.6%）均 MISS low；CSS2 若 selectors/cascade 缺口大可能触下沿 ~30%

## 7. ⚠️ 聚合影响（关键）

CSS2 分母（est ~5500-6500 matched pairs，9253 test-ish × ~60-70%）**远超当前 9-dir 聚合 3912**。
- 若 CSS2 ~36%：10-dir 聚合 = (936 + ~2160) / (3912 + ~6000) ≈ **31%**（自 23.9% 跃 +7pp，CSS2 单目录主导上拉）
- 若 CSS2 ~25%：10-dir ≈ (936 + 1500) / 9912 ≈ **24.6%**（基本不动）
→ **CSS2 是迄今最大聚合 mover**；其 chr<1% 落点决定 10-dir 基线是 25% 还是 31%。

## 8. 工具链核查

- ✅ 子目录递归 RESOLVED：capture-oracle-per-dir（b18223de）+ discover git/trees recursive=1（10db810）
- ✅ GITHUB_TOKEN 非硬门控（10db810 移除；raw 不限速，trees 1 调用/目录）
- ⚠️ **TIME**：9000+ test-ish @ ~50/min（css-text 实测速率）→ oracle 捕获 **~3hr** + self dump **~3hr**
  （3-6× css-text 的 ~35min/each）；**多小时操作**，并行 agent 须规划
- ⚠️ JS 依赖占比 TBD（css-text 24.5%）；CSS2 可能含 testharness-JS 用例无法捕获

## 9. 结论

- **0 新单会话 lever**：CSS2 缺口全结构性（run-in/counters/pagination）或基础已实现；无单点 clean-win
- **下一步**：并行 agent 启动 CSS2 全量导入（discover 解析权威 link=match 对）+ oracle 捕获 + self dump（多小时）；
  doc-side 追踪。完成后 10-dir 聚合 = 最终 DC-14 全量基线（pre-95% 目标）
