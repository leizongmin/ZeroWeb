# R409 — css-text-decor 全量前 pre-confirm 簇映射（只读）

**日期**：2026-06-22
**承接**：R408（css-tables 折回，3/9 目录全量完成）。并行 agent 尚未提交下个目录
（ae15f02 仍 HEAD，工作树干净，css-text-decor 0 未追踪）。本轮按 R405/R407 节奏，
对最可能的下个目录 css-text-decor（356）做只读 pre-confirm，为数据落地后即时对照备基线。

## 子集现状（78 文件，已提交）

按特性簇量化（test 侧，排除 -ref）：

| 簇 | test 数 | 占比 |
|------|---------|------|
| text-emphasis-*（style/position/ruby/punctuation/color） | 32 | **82%** |
| text-decoration-*（underline-position/offset/thickness/line-color） | 4 | 10% |
| text-shadow-* | 0 | 0% |
| 其它 | 3 | 8% |

**关键观察**：子集 82% 是 text-emphasis——而 text-emphasis 是生产**完全缺失**的簇（见下）。
故子集通过率被 text-emphasis 失败结构性压低，**不能从子集外推全量真实通过率**。

## 实现状态锚定（read-only 代码核查）

| 特性 | parse/store | paint | 裁决 |
|------|-------------|-------|------|
| **text-emphasis** | ❌ 空（R392 完整回退：style parse/store/inherit + paint 全删，9 文件 grep=0） | ❌ 空 | **生产全无**——所有 emphasis 测试渲染无标记，对 chromium 必败 |
| **text-decoration**（underline/overline/line-through + color） | ✅ | ✅ `paint_text_decoration_from_style`（text.rs:1096/1223/1426）+ effects.rs:155「支持 text-decoration-color 和装饰样式」 | 已实现，基础用例应过 |
| **text-shadow** | ✅（R170 rgba 解析修复） | ✅ text.rs:665-670 读 style.text_shadow + 1056/1184/1400 应用偏移到 glyph | 已实现（DC-12/M9 ✅），低风险 |

## 全量预测（356，待并行 agent 导入+测量）

1. **通过率带预测**：全量 css-text-decor 将**再平衡**——text-decoration/text-shadow（已实现）占比上升、
   text-emphasis（缺失）占比相对子集下降。故全量 oracle 一致率**不会如子集般被 text-emphasis 压到极低**，
   预测落 **~35-43% oracle** 区间（与 grid/position/tables 的 ~38-44% 基线**可比**），但**方差更大**
   （取决于全量 text-emphasis 真实占比）。
2. **主导失败簇（预期）**：
   - **text-emphasis**（R392，完全缺失，Phase A line-box 定位阻塞）= 全量最大失败源候选；
     解锁=Phase A IFC 统一（行盒垂直定位），非单会话。
   - **text-decoration 垂直定位**（baseline/thickness/offset 依赖 baseline 与 line-box）= Phase A 耦合。
   - **text-shadow**（已实现，低风险；残余=R174 blur σ 映射耦合，R170 已修 rgba）。
3. **预期与 grid/position/tables 的差异**：tables 因 colspan/border-collapse 等历史修复偏高（67%/43.8%）；
   text-decor 无等量级真实修复累积（text-emphasis 是净回退），预测**接近或略低于** position（64.2%/37.9%）。

## 裁决

- css-text-decor 子集严重偏斜（82% text-emphasis），**子集通过率不具代表性**——全量导入是去偏的必要步骤
  （进一步佐证 DC-14 去子集化的必要性，非仅 grid/position 那样「子集高估」）。
- 0 新单会话 lever：text-emphasis 缺失解锁 = Phase A；text-decoration/text-shadow 已实现。
- 待并行 agent 提交 css-text-decor 全量后，折回 master.md（4/9 目录），用真通过率对照本预测
  （text-emphasis 簇是否如预期封顶、是否需在 R392 上重开 line-box 定位调查）。

## 本轮变更

- 零 Rust 变更；零 reftest 运行（只读代码核查 + 文件计数）。
- 新增本证据文件；master.md 加 R409 行（综合裁决表）。
