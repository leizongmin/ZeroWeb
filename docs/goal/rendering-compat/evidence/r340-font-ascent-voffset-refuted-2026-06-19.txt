R340 — font-ascent v_offset 实验：全 plumbing 实现后 welcome 零改善（实证证伪 R339 角度，已回退）
================================================================================================

日期: 2026-06-19
基线: welcome chromium-Oracle 17.06%；rendering plateau loose 438/490 / strict 295/490（实验已回退，基线恢复）
承接: R339 确认 font-ascent 前提（sans-serif 真实 ascent = 92.8% × font_size，ZeroWeb v_offset 用 100%
      当 ascent → 推断每行基线低 7.2% = welcome 分布式 diff）。本轮实现完整实验裁决。

实验实现（已 100% 回退）
------------------------
完整 plumbing（区别于 R331 probe 仅 plumbing 无 consumer）：
1. FontLoader::build_ascent_ratios()（loader.rs）→ HashMap<font_id, ascent/size>。
2. Painter.ascent_ratios 字段（默认空 → ratio 1.0 → 旧行为；browser 未设则不变）。
3. RenderPipeline.set_ascent_ratios + 4 处 render 克隆到 painter。
4. reftest harness（render_to_framebuffer_with_base）set_ascent_ratios(font_loader.build_ascent_ratios())。
5. text.rs Path B 非-Ahem baseline_offset = glyph_fs × ascent_ratio（Ahem ratio≈1.0 → no-op）。

实测结果（决定性）
------------------
- **welcome chromium diff 完全不变**：17.06%（81901 px）vs 修复前 17.06%（81889 px）= 净差 12 px（噪声级）。
- **但 ZeroWeb 自身渲染位移 27495 px**（修复前后 welcome PNG old-vs-new diff）= 改动确实生效，把文本
  垂直移了位。
- = **垂直基线位移对 chromium diff 净零效应** → welcome 的 17% diff **非**垂直基线位置（font ascent）所致。

裁决（证伪 R339 font-ascent 角度）
----------------------------------
1. **font-ascent v_offset 角度实证证伪**：即便用理论正确的 actual_ascent（fontdue line_metrics）替代
   font_size 近似，welcome chromium diff 零改善。v_offset = font_size **load-bearing**（R306 谱系：
   不论 frag.height 还是 actual_ascent，v_offset 都不可单点改——它与 fontdue 光栅化内部的 ascent/y_offset
   耦合，font_size 是经验调谐值）。
2. **welcome diff 来源纠正**：垂直位移 27495px 对 chromium diff 零效应 → welcome 17% **非垂直**，
   是**水平（advance/字形位置）或字形形状**源。R339 推断「非-Ahem ascent = welcome 分布式 diff」**错误**
   ——真因在水平/字形，非垂直基线。
3. 实验**已 100% 回退**（git checkout 5 文件：loader/painter/pipeline/text/reftest，engine 1145 测试全过）。
   无代码变更残留。

对 R339 的纠正
--------------
R339 标 font-ascent 为「未测试的 fresh promising 角度」并验证了前提（92.8%）。R340 实现实验后**实证证伪**
——前提（92.8%）成立但**不构成 welcome diff 来源**（垂直位移零效应）。这印证 R164 教训：架构性推断须实验
落地，不能据推断接力（R339 据 92.8% 前提推断 promising，R340 实测推翻）。

全局
----
font-ascent 角度关闭（垂直方向）。welcome 17% 真因 = 水平/字形（advance-width R225 已证死路、字形形状
fontdue ≈ chromium 但仅 W/i 验证过——welcome 全字符集可能差异，独立子域）。rendering-compat 渲染目标
plateau 不变。唯一 forward motion 仍 = 多会话 Phase A IFC 统一。

代码变更：零（实验已回退，git diff 空）。基线 loose 438/490 / strict 295/490 持平。
