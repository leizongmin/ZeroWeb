R549-land 实验验证 — 3 部件修复实证 RESOLVES block C（positioning +19 含 006）；net +14；5 PASS→FAIL 暴露 + 8 单测涟漪；revert 待 clean-land
==================================================================================================
日期：2026-06-24
类型：code-side 实验验证（应用 3 部件 fix → A/B 实证 → revert 保 0 回归；零代码落地）
承接：r549-block-c-border-width-medium-rootcause（根因 + 3 部件计划）

实验：应用 fix1（types.rs parse_length thin/medium/thick→Px(1/3/5)）+ fix2（converter/mod.rs:113-118
border_lp 闭包按 border_*_style==None|Hidden 抑制 border 为 0）+ fix3（registry.rs:36 + default_impl.rs:45-48
border-*-width 初始值 Px(0.0)→Px(3.0)=medium），build clean，全量 A/B。

★ 实证 block C RESOLVED（positioning +19）
------------------------------------------
A/B（test-guard，R502-with-hint baseline vs +3fix，FAIL-list diff）：
- **positioning 396→414 = net +18（+19 yield / -1 regression）**。yield 含 **absolute-replaced-width-006**
  （block C！长期死路 RESOLVED）+ 013/022/027/029/036/041/043 + absolute-replaced-height-010/017/024/031。
  ★ 证实 block C 根因确是 border-width medium（border: solid→0 不阻断 margin 折叠），3 部件 fix 后
  border=3px 正确阻断 → img 位置正确 → 006 等 PASS。
- normal-flow 639→638 = -1（blocks-017，`border: solid blue` 布局偏移暴露）
- backgrounds 287→287 = 0
- borders 432→429 = -3（见下）
- **reftest 净 +14**（positioning +18 主导）

5 PASS→FAIL 暴露（DC-14 anti-false-pass：border=0 假通过被正确 border 渲染揭穿）
-----------------------------------------------------------------------------
全 5 个 = `border: <隐式宽度>` 假通过（border 渲染 0→3px/1px 后暴露**独立** bug，非 border-width 缺陷）：
- borders: border-color-011/012（`border:none`+`border-color:inherit`）、shand-border-000（`border:medium solid`）=
  **currentColor/border-color 继承解析** bug（border 现可见，颜色解析暴露）。
- normal-flow: blocks-017（`border:solid blue`，margin 折叠测试，3px border 偏移暴露）。
- positioning: abspos-width-005（`border:fuchsia thin solid`，thin→1px 布局偏移暴露）。
→ 5 个是 DC-14 正面实例（揭穿假通过），各指向独立 lever（currentColor 解析 / border 布局偏移）。
   clean-land 须先修这 5 个暴露的独立 bug，或据 DC-14 接受为 honest exposure（goal 认可）。

8 单测涟漪（fix2 style=none 抑制；机械修复）
--------------------------------------------
zero-layout-engine 8 单测 FAIL：test_computed_style_border / test_convert_margin_padding_border /
test_border_values_in_layout / test_border_and_padding_effect_on_content_size /
test_box_sizing_content_box_with_padding / test_absolute_stretch_in_inline_block_container /
test_inline_child_box_bleeds_upwards_from_ifc / test_build_with_padding_border_margin。
根因：这些测试设 `border_*_width = Px(N)` 但**不设 border_*_style**（默认 None）→ fix2 抑制 border=0
→ 断言 border=N 失败（实测 test_border_values_in_layout left=0.0 right=5.0）。**修复 = 各测试加
`border_*_style = BorderStyleValue::Solid`**（CSS 正确：测 border 布局效果须显式设 style；旧测试依赖
width-without-style→border 的 buggy 行为）。机械，~8 测试 ×4 边。

裁决：REVERT（保 0 回归 + 干净 handoff）
----------------------------------------
fix 实证 resolves block C（+14 净，长期死路破解）但：5 PASS→FAIL（违反 rally 0 回归纪律，虽 DC-14
认可 honest exposure）+ 8 单测涟漪（须机械修）。本轮已 revert（git checkout 4 文件恢复 255a7f3d
baseline），build clean，零代码落地。

★ hand to 后续会话 clean-land 清单：
1. 重应用 fix1+fix2+fix3（见 r549-block-c-rootcause evidence 的精确 seam）。
2. 修 8 单测（各加 border_*_style=Solid）。
3. 决策 5 暴露：(a) 先修 currentColor/layout-shift 独立 bug 求 0 回归，或 (b) 据 DC-14 接受为
   honest exposure（master.md 标注，self-source pass-rate 不计达标）。
4. A/B 守 borders+normal-flow+positioning+backgrounds，make test + clippy + product-smoke。
5. 预期：reftest 净 +14（block C resolved）— 迄今最高价值单 lever（解长期死路）。

★ 核心价值：本轮实证了「block C 不是死路，是 border-width medium bug，3 部件 fix 可解（+19 positioning
含 006）」。纠正 archive「R490-entangled 死路」误判（同 R502 block A 先例：死路标签须实证复验）。
