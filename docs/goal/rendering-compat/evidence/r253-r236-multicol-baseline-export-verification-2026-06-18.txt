R253 — R236 multicol baseline-export spec 对当前代码实证核实：plumbing-gap 确认，最高 reftest 杠杆就绪
日期: 2026-06-18
模式: read-only（读 engine.rs + types/mod.rs + multicol.rs；不碰并行 agent text.rs WIP）
基线: reftest 自源 438/490（持平）；DC-14 严格真通过 188/475=39.6%

================================================================
目的
================================================================
R236 multicol baseline-export spec 写于 c53a541（数轮前）。本轮核实其对当前 HEAD 代码
仍准确（plumbing-gap pattern 是否仍成立），为「最大化 reftest 进展」路径（R250 确立的
最高 reftest 杠杆候选）去风险。engine.rs/multicol.rs 区域，与并行 agent text.rs/paint 无冲突。

================================================================
逐条核实（R236 plumbing-gap pattern，全部确认）
================================================================
【claim 1: taffy_baseline 字段 + extract 写入】✅ 确认
  crates/layout-engine/src/types/mod.rs:236  pub taffy_baseline: Option<f32>
  engine.rs:474  fn extract_baselines_recursive(...)
  engine.rs:482    if let Some(baselines) = taffy.cached_baselines(taffy_id)
  engine.rs:483        if let Some(y_baseline) = baselines.y
  engine.rs:484            box_node.taffy_baseline = Some(y_baseline)
  注释（471-472）：「通过 taffy 补丁 cached_baselines() 存储到 LayoutBox.taffy_baseline」。

【claim 2: flex/grid 消费 child.taffy_baseline】✅ 确认
  engine.rs:963-964  // CSS Flexbox §8.5: 容器基线从第一个 flex line 合成；
                     // 优先使用 taffy first_baselines（cached_baselines 补丁）
  engine.rs:988      if let Some(taffy_bl) = child.taffy_baseline { ... }
  → flex baseline-alignment 路径消费 child.taffy_baseline（消费点存在）。

【claim 3: multicol.rs 零 baseline = gap】✅ 确认
  grep -cn "baseline|Baseline" crates/layout-engine/src/multicol.rs = **0**
  → multicol.rs 完全无 baseline 处理。自建 column layout（taffy 把 multicol 当 plain block 的
    post-pass）从不计算/存储 baseline → multicol 导出 baseline 回退到 taffy 通用 block baseline
    （非 §baseline-export 列导出值）→ 8 例 baseline-export 用例系统性 ~1.1% 偏移。

================================================================
可行性核实（修复路径成立）
================================================================
- taffy cached_baselines 对 multicol 节点返 taffy 通用 block baseline（taffy 视 multicol 为
  plain block，不知列布局）→ 当前错误值来源。
- multicol.rs 已做 column post-pass（列分配），可在列布局后计算 §baseline-export 值：
  各列首行 baseline（IFC 已 track line baselines）取最高 = first；末列末行 = last；
  写入 multicol LayoutBox.taffy_baseline 覆盖 taffy 通用值。
- 字段/extract/consume 路径全在，仅缺 multicol.rs 填充 = 标准 plumbing-gap（同构 R229
  font-weight / R234 font-kerning）。

================================================================
结论
================================================================
R236 spec 对当前代码**完全准确**（c53a541→HEAD 无相关变更）。multicol baseline-export =
**最高 contained reftest 杠杆**（R235 确立 8 例 ~1.1% 系统性偏移，结构性最 tractable；
plumbing-gap pattern，bounded feature）。修复=multicol.rs 列布局后算 §baseline-export
写 taffy_baseline 覆盖。实现 agent「最大化 reftest 进展」路径可直接起手 R236（字段/
extract/consume 路径就绪，仅需 multicol.rs 填充），无需再调研。
成功标准=baseline-000~008 类用例 ~1.1% 偏移消除（reftest 438/490 上升 +8 潜力，
须 zero-regression 全量验证）。

无代码变更。基线 438/490 持平。
