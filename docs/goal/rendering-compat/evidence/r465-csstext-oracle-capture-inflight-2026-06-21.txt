R465 css-text 全量 oracle 捕获 IN-FLIGHT + 精确 self dump + cross-validate 准备（read-only 观测）
================================================================================
日期: 2026-06-21 17:28 CST
承接: R464（oracle 缺失根因 = 捕获脚本顶层 readdirSync bug，collectTests 修复在途）
状态: 并行 agent 已启动修复后的全量流程，oracle 捕获进行中（无实测 chr<1% 数据）
本轮产出: 修复生效验证 + 并行流程全景（oracle 捕获 + 精确 self dump + cross-validate 准备）

--------------------------------------------------------------------------------
1. 全量 oracle 捕获已启动（R464 修复生效验证）
--------------------------------------------------------------------------------
- PID 72483 `node capture-oracle-per-dir.mjs --category css/css-text`（17:25 启动）
- chromium headless 活跃（主进程 + 3 renderer + gpu + zygote + network + storage，ver 149.0.7827.102）
- ⚠️ capture-oracle-per-dir.mjs **仍未 commit**（git status ` M` working tree）——并行 agent 直接用
  working tree 的 collectTests 版本跑了（修复在途未提交，但已生效）
- **修复生效验证**: 已跑 2:39（17:25→17:28）csstext_oracle2.log 仍 0 字节（捕获中未汇总）
  → 若旧顶层版 ~13s 完成 8 个即退出；2.7min 未完成 = collectTests 递归版在遍历 1914 个 test ✓
- 预判: 1914 截图 × ~1.6s ≈ 51min（R464 预测），node 脚本不经 test-guard 无 1800s 限，当前进度 ~5%

--------------------------------------------------------------------------------
2. 并行 agent 同时跑精确 css-text self dump（修正 R463 decor 误匹配）
--------------------------------------------------------------------------------
- /tmp/csstext_dump.log（17:27，REFTEST_DUMP 模式）: `Running 1572 upstream reftest cases`
- **关键修正 vs R463**: R463 filter `contains("css-text")` 误匹配 css-text-decor（1822=1572+250）；
  本轮并行 agent 用更精确 filter（`css/css-text/` 带斜杠）→ **精确 1572**（排除 decor 250）
- 这是 cross-validate 的 **ZW-test 像素 dump** 准备（与 chromium oracle 截图比对算 chr<1%）
- self 通过率预期与 R463 一致（1402/1572=89.2%，同口径同数据源）

--------------------------------------------------------------------------------
3. cross-validate 流程在准备（两输入齐即算 chr<1%）
--------------------------------------------------------------------------------
- cross-validate（scripts/cross-validate.py）需两输入:
  ① chromium oracle 截图（全量捕获中，PID 72483，~51min）
  ② ZW-test 像素 dump（self dump 跑中，1572 cases）
- 两输入齐 → 逐案算 z_vs_chr diff% → chr<1% 一致率 = DC-14 真指标（验证 R461 30-38% 预测）
- 参考 writing-modes 流程（R457）: oracle 捕获 + cross-validate 两步，writing-modes 787 oracle≈21min

--------------------------------------------------------------------------------
4. 系统性观察：并行 agent 工作树未提交但已生效
--------------------------------------------------------------------------------
- capture-oracle-per-dir.mjs working tree 修改（collectTests）未 commit，但 PID 72483 已用其运行
- → 并行 agent 工作模式: 先用 working tree 验证修复生效，再 commit（避免提交未验证代码）
- 文档侧影响: 本轮 R465 记录"修复生效"，但 commit 历史里 collectTests 提交可能在并行 agent 验证后
  → 下一轮若见 collectTests commit，确认 R464 修复正式落地

--------------------------------------------------------------------------------
结论
--------------------------------------------------------------------------------
- R464 修复（collectTests 递归）已生效: 全量 oracle 捕获进行中（1914，~51min，当前 ~5%）
- 并行 agent 同时跑精确 self dump（1572，修正 R463 decor 误匹配）备 cross-validate ZW 像素
- cross-validate 待两输入齐（oracle ~51min + self dump ~22min）→ 算 chr<1% → 验证 R461 30-38%
- 零代码变更（read-only + 进程/日志观测）
- 下一步: 等 oracle + self dump 完成 → cross-validate → R461 30-38% 验证 → 9 目录 oracle 聚合 → CSS2
