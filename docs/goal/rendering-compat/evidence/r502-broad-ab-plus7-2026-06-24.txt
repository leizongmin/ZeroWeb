R502 presentational-hints — broad A/B (WITH vs WITHOUT R502) 证实累计 +7 / 0 回归
日期：2026-06-24  角色：doc-maintenance（read-only 调查 + 文档；不改源码）

===========================================================================
0. 目的
===========================================================================
R502 reopen（lib.rs collect_presentational_hints：img w/h → Author origin (0,0,0)）
normal-flow 已 A/B 证 +3/0（evidence/r502-presentational-hints-landed-normalflow-plus3-2026-06-24.txt）。
本轮对其余 CSS2 子目录做 WITH vs WITHOUT A/B，精确隔离 R502 broad yield。

===========================================================================
1. 方法（两 run，clean-set diff）
===========================================================================
code agent 先 reset R502（git reset --hard HEAD；reflog 证实）→ working tree = 无 R502 baseline
→ 跑 WITHOUT-R502 baseline；先前 WITH-R502 run 仍在。
  WITH R502 : /tmp/broad_r502.txt     （test-guard CSS2/positioning+backgrounds+borders）
  WITHOUT   : /tmp/baseline_r502.txt  （同三目录，reset 后）
提取每 run 的 ✗ 失败测试集（排除 "No such file" ref 错误行），clean-set diff：
  R502 FIXES     = (fail WITHOUT) − (fail WITH)   [fail WITHOUT, pass WITH]
  R502 REGRESSION= (fail WITH)    − (fail WITHOUT) [pass WITHOUT, fail WITH]

★ 关键方法论：单 run 的失败列表抓不到「R502 修复的测试」（WITH 下它们 PASS，不在 WITH 失败集），
  它们是 baseline-only 失败 → 必须 A/B（两 run clean-set diff）。早先单-run grep「无 img-sizing
  失败」误推「broad 零增量」即此陷阱（commit 4a7071e7 内已被纠正）。

===========================================================================
2. 结果
===========================================================================
positioning : WITH 396/519 (76.3%)  ==  WITHOUT 396/519 (76.3%)   → +0 / 0 回归
backgrounds: WITH 287/339 (84.7%)  vs  WITHOUT 284/339 (83.8%)    → +3 / 0 回归
borders    : WITH 432/506 (85.4%)  vs  WITHOUT 431/506 (85.2%)    → +1 / 0 回归

broad 小计：+4 / 0 回归。加 normal-flow +3 = 累计 +7 / 0 回归。

R502 FIXES（fail WITHOUT → pass WITH，全 0 回归）：
  backgrounds:
    css/CSS2/backgrounds/background-image-cover-002.xht
    css/CSS2/backgrounds/background-image-cover-004.xht
    css/CSS2/backgrounds/background-image-cover-attachment-001.xht
    （三者 test/ref 含 <img> sizing 影响 background-size:cover 比对）
  borders:
    css/CSS2/borders/border-color-005.xht
    （其 test/ref 含 <img> 影响 layout；非 border 渲染本身）
  positioning: （无）

R502 REGRESSIONS：全三目录均空（WITH-fail-set ⊆ WITHOUT-fail-set）。

===========================================================================
3. 残余失败性质（R502 不修，独立 lever）
===========================================================================
- positioning：absolute-replaced-width-* 多数 FAIL（006=3.52%）= block C「margin-collapse-through-border」
  未被 R504/R502 解除，独立阻塞（R490-entangled 死路，非 R502 范围）。
- backgrounds：background-attachment-applies-to-*（29.92% 等，fixed-bg 滚动属性专项）、
  background-color-applies-to-006(1.58%) — 属性专项。
- borders：border-bottom-width-* / border-*-applies-to-*（border width 精度 + inline-element border
  渲染缺口 R359/R490 + fontdue/IFC 精度天花板）— 精度/结构性。

===========================================================================
4. R502 代码状态 + 结论
===========================================================================
- lib.rs +42（collect_presentational_hints）当前 NOT in HEAD / NOT in working tree（git reset --hard
  后；reset 是为跑 baseline A/B，非放弃）。grep collect_presentational_hints = 0 核实。
- A/B 既证累计 +7 / 0 回归（normal-flow +3 + backgrounds +3 + borders +1）= clean win，
  re-land 预期（code agent 侧负责提交；doc-agent 不提交源码）。
- 证实 block A「draw_order 覆写」被 R504（global positioned-descendant deferral, App E step 6,
  commit ea08de51）解除：normal-flow +3 wins 在 in-flow img（无 abspos paint-order 问题）；
  backgrounds/borders 的 4 案亦 in-flow img sizing 连带。
- 推翻 R502「实现 correct 但永久零 yield」旧结论（原 R502 在 R503/R504 前被双重阻塞 refuted）。
