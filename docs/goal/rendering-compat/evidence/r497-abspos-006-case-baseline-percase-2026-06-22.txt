R497 — abspos-containing-block 001-006 逐案基线（pre-fix / post-R491）
日期：2026-06-22（git ground-truth；header 旧标 06-22 实 +0800 即当日）
性质：read-only PIL 实测（零代码变更、零 build；复用 R484 dump @ 03:54 = post-R491 + pre-stretch-fix
      + chromium oracle @ 06-21 20:09，二者均独立于本会话）。为 code agent 当前 re-applied
      stretch_fixed_to_viewport_size（engine.rs step 11.6，is_fixed-only guard）的 post-fix
      delta 提供逐案测量基线。

================================================================================
1. 6 案 CB 分类（读 6 个 test 源文件 + ref + CSS §10.1.4 复核）
================================================================================
共享 ref（abspos-containing-block-001-ref.xht）：body{margin:0} + 无背景 div + 绿文本
  → ref 画布白、无红。test 通过条件 = 「无红」= 白 div 必须覆盖整画布。

  001  body{bg:red;margin:100} + div{position:FIXED;    top/right/bottom/left:0}        auto-size
  002  body{bg:red;margin:100} + div{position:ABSOLUTE; top/right/bottom/left:0}        auto-size
  003  body{bg:red;margin:100} + div{position:FIXED;    top/right:0;height:100%;width:100%}
  004  body{bg:red;margin:100} + div{position:ABSOLUTE; top/right:0;height:100%;width:100%}
  005  html{pos:relative;w/h:100%} + body{bg:red;margin:100 20%} + div{pos:ABSOLUTE;top/right:0;w/h:100%}
  006  html{pos:relative;w/h:100%} + body{bg:red;margin:100 20%} + div{pos:ABSOLUTE;top/right/bottom/left:0}

CB 分类（§10.1.4）：
  - 001/003 fixed → CB=视口（无条件）。
  - 002/004 absolute，body+html 均 static → 无 positioned 祖先 → CB=初始包含块=视口
        （sizing 等价 fixed，但 is_absolute 而非 is_fixed）。
  - 005/006 absolute，html{position:relative} → CB=html（positioned 祖先，w/h:100%→800×600）。
→ 印证 R496c 的 CB 分类；纠正点见下（as-applied 覆盖面）。

================================================================================
2. pre-fix 逐案 PIL diff（ZW-test vs ZW-ref = self；ZW-test vs oracle = chr）
   阈值 ±5 RGB（与 cross-validate.py 一致）。全 800×600 无 SIZE-MISMATCH。
================================================================================
  001  self 96.65%   chr 97.00%    fixed auto-inset
  002  self 97.49%   chr 97.84%    absolute auto-inset（viewport CB）
  003  self 99.97%   chr 100.00%   fixed percentage
  004  self 12.81%   chr 12.82%    absolute percentage（残余小，≈ right:0 位置泄漏，见 R496c）
  005  self 100.0%   chr 100.00%   absolute-in-relative percentage（CB=html）
  006  self 98.16%   chr 98.16%    absolute-in-relative auto-inset（CB=html）

解读：
  - 001/002/003/005/006 ~97-100% diff = ZW 渲染「小红 div 在红画布」（div 塌缩未 stretch）
    vs chromium「大白 div 覆盖红画布」→ 几乎全像素红 vs 白。
  - 004 仅 12.82% = adjust_absolute_pct_to_viewport（engine.rs:1649，对 absolute Percentage 活跃）
    已把 size 大致设对（800×600），残余 ~12.8% = right:0 位置未处理致右侧红边泄漏（R496c 记）。

================================================================================
3. as-applied fix 覆盖面 + post-fix 预测（code agent 当前 working-tree 未提交版本）
================================================================================
applied stretch_fixed_to_viewport_size（engine.rs step 11.6 post-pass）guard = `child.is_fixed`：
  - height:Auto + 全 Px top+bottom → child.height = vh - top - bottom
  - width:Auto  + 全 Px left+right  → child.width  = vw - left - right
  - Percentage height/width → 相对视口重算（fixed CB=视口 §10.1）

逐案覆盖（按 is_fixed guard）：
  001 fixed auto-inset      → 覆盖（height=600-0-0=600, width=800）→ 预测 ~0% FLIP PASS
  003 fixed percentage      → 覆盖（h=100%·600=600, w=100%·800=800）→ 预测 ~0% FLIP PASS
  002 absolute auto-inset   → 未覆盖（is_absolute 非 is_fixed）→ 维持 97.84% FAIL
  004 absolute percentage   → 未覆盖 → 维持 12.82% FAIL
  005 absolute-in-rel pct   → 未覆盖 → 维持 100% FAIL
  006 absolute-in-rel inset → 未覆盖 → 维持 98.16% FAIL

★ 关键纠正 R496c：R496c 标「R491 潜在净值上修至 +3~+5」「001-004 全翻净 +3」是建立在
  「guard 扩为 is_fixed || (is_absolute && no_positioned_ancestor)」的【建议】上。
  code agent 实际 as-applied 只取 is_fixed（保守，避 R493 旧 x/y 回归）→ 仅 001/003 翻。
  → as-applied post-fix chr<1% 净 = +2（001/003 翻回），R491 由 R492 实测 net −1 → net +1，
    破 R484 后 plateau streak。R496c 的 +3~+5 需 code agent 进一步扩 guard 到 absolute
    （002 viewport-CB 等价，~5 行；005/006 CB=html 须 tree-resolution post-layout，更重）。

================================================================================
4. 待实证（code agent 拥有；本侧 read-only 不 build）
================================================================================
- code agent PID 45919 全量 reftest-upstream（--time-limit 3600，04:55 起，~05:55 完/杀）
  验证 self-source 001/003 翻 PASS + 全套件零回归（fixed+percentage/auto-inset 普适 post-pass
  须确认不伤其他 fixed 用例）。
- 实证后本侧跑 scoped re-dump（positioning/abspos-containing-block-00[1-6]）+ 本文 PIL 复算，
  落定 R497 正式逐案 post-fix 数 + chr<1% 净值。
- 风险：fixed percentage 普适重算（不限于全-inset）可能改变其他 fixed+% 测试的 size；
  全量 reftest 是唯一可靠回归闸。

================================================================================
5. post-fix 实证（本会话 scoped re-dump + PIL，05:03；engine.rs hash dd2853… 前后不变=无并发编辑）
================================================================================
方法：REFTEST_DUMP=1 REFTEST_DUMP_PASS=1 test-guard 包裹 `reftest-upstream abspos-containing-block-00`
（filter 命中 001-009，9 案，15.77s；binary = 当前 working-tree engine.rs 含 fix，cargo 无 rebuild）。
self-source reftest：**2/9 PASS = 001 + 003**（001/003 翻 PASS；002/004-009 FAIL）。
PIL（post-fix ZW-test vs chromium oracle；阈值 ±5 RGB）：

  case  type                    pre-fix chr   post-fix chr     Δ        verdict
  001   fixed auto-inset          97.00%         0.42%      -96.58%   FLIP→PASS (<1%)
  002   absolute auto-inset       97.84%        97.84%       +0.00%   still FAIL
  003   fixed percentage         100.00%         0.42%      -99.58%   FLIP→PASS (<1%)
  004   absolute percentage       12.82%        12.82%       +0.00%   still FAIL
  005   abs-in-rel pct           100.00%       100.00%       +0.00%   still FAIL
  006   abs-in-rel inset          98.16%        98.16%       +0.00%   still FAIL

★ R497 预测全确认：is_fixed-only guard → 仅 001/003 翻（0.42% 残余=fontdue 绿文本度量噪声）；
  002/004/005/006（is_absolute）纹丝不动。self-source 与 vs-oracle 双口径一致。

净值：001/003 翻回 = R492 的 6 positioning 回归修复 2/6 → R491 由 R492 实测 net −1 → **net +1**
（+8 improved − 7 regressed；原 9 regressed 减 2）。= **R484 后首个 net-positive chr<1%**（破 plateau streak）。
⚠ 净 +1 / 全量 2671→2673 contingent on code agent 全量回归扫描（positioning/ 已跑、css-position/ 在跑、
其他 fixed-用例目录）确认 universal fixed+% post-pass 零新回归——本 scoped 仅覆盖 001-009。

下一 lever（+1，002）：adjust_absolute_pct_to_viewport（engine.rs:1649）已线程化 has_positioned_ancestor
+ 已对 is_absolute&&!positioned-ancestor 解析 Px left/top 位置（视口 CB）+ Percentage size——
仅缺 auto-inset size stretch。在该函数的 absolute-viewport-CB 分支补 ~8 行
（auto height + Px top+bottom → child.height=vh-top-bottom；width 同理）即翻 002（位置已由现有 Px 分支设好，
size-only 不触 R493 旧 x/y 回归）。004 残余(right:0)/005/006(CB=html) 需另路，更重。

================================================================================
6. 残余分类 + 002 +1-lever 实证（本会话 PIL，05:11；零 build 非冲突）
================================================================================
6.1  001/003 残余 = 纯 fontdue 文本噪声（clean pass，非 near-miss）
     - red_ZW=0, red_ORC=0 → 白 div 完全覆盖红画布，零红泄漏，fix 结构完整。
     - 2012 diff px 全在文本 bbox x[0,233] y[2,26]（绿文本 "Test passes if there is no red."），
       颜色为绿字 glyph 位置/AA 差异（ZW (64,160,64) 绿 vs ORC (255,255,255) 白底，或反）。
     - 001 与 003 diff 模式逐像素相同（同结构）。= 已知 fontdue 度量/AA 噪声底（R174/R229 谱系）。
     - 结论：001/003 是结构正确的 clean pass（0.42% 远低 1% 阈值），非侥幸 near-miss。

6.2  002 +1-lever 实证（白 div 定位 PIL）：div 在 (0,0)（视口原点）但 size=600×20
     - 位置 (0,0) → top:0/left:0 已由 adjust_absolute_pct_to_viewport 的 Px 分支正确解析到视口 ✓。
     - width=600（=body content 宽，taffy 用 static body 作 CB，非视口 800）✗；height=20（固有单行）✗。
     - → +1 fix 须同时 stretch width(600→800) + height(20→600)（size 双轴，非仅 height）。
       adjust_absolute_pct_to_viewport 的 absolute-viewport-CB 分支补 auto-inset w+h stretch ~8 行
       即翻 002（position 已设好，size-only 不触 R493 x/y 回归）。spec 经验 grounded。

================================================================================
7. fix 的 % 分支 blast radius（read-only grep，05:14；界定 net +1 回归风险）
================================================================================
fix 的 Percentage 分支普适（所有 fixed+% 相对视口重算）是最大回归风险源。grep 实测 CSS2：
  - 58 文件用 position:fixed；其中 **11 文件** 同时含 % width/height（fixed+% blast radius）：
    positioning/{position-fixed-005, abspos-containing-block-003, position-fixed-004}
    visuren/{right-offset-position-fixed-001, left-offset-position-fixed-001}
    backgrounds/background-alpha-002 / normal-flow/blocks-025 / visufx/overflow-009
    abspos/abspos-in-block-in-inline-in-relpos-inline
    + 2 floats/crashtests（float-rewind-parallel-flow-2/3-crash，crashtest 无像素 reftest）
  - 像素相关 ~9 案。code agent 回归扫描（positioning/ ✓ + css-position/ + box-display/ + 须覆盖
    visuren/backgrounds/normal-flow/visufx/abspos）即覆盖这 ~9 案。
  - 风险评估：fixed CB=视口是 spec 正确（CSS §10.1），WPT reftest 多期望 spec 行为 → fix 应
    改善或保持，非回归；但 003 本是 fixed+% 翻 PASS 的目标，其余 8 案须 sweep 实证零回归。
  - 结论：net +1 回归风险 bounded 且小（~9 案），全量 sweep 通过即坐实 2671→2673。



