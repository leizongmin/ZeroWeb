R549 LANDED — border-width `medium` 关键字 + 初始值 = medium（CSS §8.5.1/§8.5.3）
日期: 2026-06-24
Commit: ebff2595（推送到 origin/main）
分支: main（push 前 c66d4ef8，无 rebase 需要，本地 == origin）

================================================================================
背景
================================================================================
R549 的 3 部件 border-width 修复在前序会话中已完成实现并 A/B 验证为净 +14（破解
block C = absolute-replaced-width-006 长期死路），但**连续 10+ 轮未 commit**，
doc-agent 怀疑 code agent 在 API 限流（pre-R549 多次 429/529）中被杀掉、陷入
idle/dead。本 session（承接上一轮 529 overloaded 失败后）核对工作树、独立复核 A/B、
通过全部门禁后 land。

================================================================================
fix 三部件（工作树 14 文件，+86/-24）
================================================================================
fix1 — css-parser/src/values/types.rs parse_length：
  新增 thin/medium/thick 关键字 → Px(1.0)/Px(3.0)/Px(5.0)。
  活代码 types.rs::parse_length 此前遗漏（死代码 parse_basic.rs 有此逻辑 =
  R544 死代码陷阱同谱系复发）。修前 `border: solid` 隐式 width=medium 解析为 0。

fix2 — layout-engine/src/converter/mod.rs computed_style_to_taffy：
  border_lp 闭包按 border-style 抑制 width：style=None/Hidden → width=0（CSS §8.5.3）。
  否则 `border: none` 的隐含 medium 会错误撑大盒模型。

fix3 — style-system/src/property/default_impl.rs + registry.rs：
  border-*-width 初始值 0 → 3px（=medium，CSS §8.5.1）。默认元素仍无布局边框，
  因为 border-style 初始 = none，fix2 把 width 抑制为 0。

+ 10 单测涟漪（converter/engine/tree/style-system 测试）：
  - 设了 border-width 但无 style 的测试补 border-style=Solid（让宽度进布局盒）
  - 断言 border_top_width == Px(0.0) → Px(3.0)
  - test_apply_invalid_border_properties：thick 不再判为非法值
  - test_inline_child_box_bleeds_upwards_from_ifc：显式置 border_bottom_width=0
    （默认现 3px 会干扰 80px 期望）

三者缺一即回归（fix1 单独→border:none 布局偏移；fix2 单独→width 仍 0；fix3 单独
→border:none 撑大盒）。

================================================================================
独立 A/B 复核（本 session，test-guard 包裹 reftest-upstream）
================================================================================
方法：git stash（移除 fix）→ baseline FAIL 列表；git stash pop（恢复 fix）→ fix
FAIL 列表；diff。filter = 目录子串，逐目录独立 A/B。

---------------------------------------------------------------- position
baseline (无 fix): 519 总, 123 FAIL = 396 PASS
fix:              519 总, 105 FAIL = 414 PASS
净 +18 PASS（+19 新过含 absolute-replaced-width-006 / −1 abspos-width-005 暴露）

★ absolute-replaced-width-006.xht：baseline FAIL 3.52% → fix PASS 0.24%
  = block C 长期死路破解（border 现 3px 绘制 → 阻断 margin 折叠 → 垂直偏差消除）

---------------------------------------------------------------- borders
baseline (无 fix): 74 FAIL
fix:              77 FAIL
净 −3（3 新 FAIL = 诚实暴露）：
  border-color-011.xht (1.03%)
  border-color-012.xht (2.57%)
  shand-border-000.xht (1.15%)
  ↑ 三者根因 = currentColor 解析 bug（ColorValue::CurrentColor 在 computed 管线
    从不解析为元素 color；border-color initial 应 currentColor 实际黑，§8.5.1）。
  border-dynamic-001/002（27% / 26%）出现在两侧 = 既有 FAIL 的 % 噪声漂移，非 delta。

---------------------------------------------------------------- normal-flow
baseline (无 fix): 107 FAIL
fix:              108 FAIL
净 −1（1 新 FAIL = 诚实暴露）：
  blocks-017.xht = margin-collapse-with-border 3px 偏移

---------------------------------------------------------------- backgrounds
baseline (无 fix): 52 FAIL
fix:              52 FAIL
净 0（无 flip）

---------------------------------------------------------------- 汇总
net = +18(pos) −3(borders) −1(normal-flow) +0(backgrounds) = +14 PASS
与 master.md / 前序会话文档的 +14 净完全一致。

================================================================================
5 新 FAIL = 诚实 DC-14 暴露（非真回归）
================================================================================
修前 test 与 ref 都渲染 border=0（假一致 / 同源自渲染假通过）；修后 border 按
CSS §8.5.1 正确渲染（3px），暴露底层独立 bug：
  1. border-color-011 = currentColor 关键字继承
  2. border-color-012 = currentColor 关键字继承
  3. shand-border-000 = currentColor 解析
  → 根因：ColorValue::CurrentColor computed 管线从不解析为元素 color +
    border-color initial 违反 §8.5.1（应 currentColor 实际 Rgba 黑）
  4. blocks-017 = margin-collapse-with-border 3px 偏移
  5. abspos-width-005 (positioning 内) = thin=1px border 触发 abspos-width-with-border
  → 这 5 个是 next-lever 候选（提 chromium Oracle 一致率，真优化目标），非 border-width 缺陷。

同 R525/R532 accepted-newly-FAIL 先例：self-source false-pass 被 DC-14 正确揭穿，
不计达标（self-source 假通过本身不计达标）。

================================================================================
门禁（全部通过）
================================================================================
cargo check --workspace                      : ✅ 编译通过
cargo clippy --workspace --all-targets -D W  : ✅ exit 0（零警告）
cargo fmt --all -- --check                   : 我的 14 文件全 fmt-clean
  （唯一 drift = lib.rs:592 pre-existing R502 代码，非本变更，未动 —— 精准修改原则）
make test (test-guard)                       : ✅ PASSED=12307 FAILED=0 IGNORED=72
  （72 ignored = real-website 测试 + security doctests，CLAUDE.md 合理例外）
make product-smoke (welcome vs chromium)     : ✅ 16.97% < 20%，exit 0
  （= R542 baseline；welcome 唯一显式 border = `border:1px solid` 显式宽，fix3 不动；
   其余 border-radius-only / border-color 无 style → fix2 抑制 width=0 → 无边框，
   故 fix3 默认 3px 不致 welcome 布局偏移；doc-side 预核 neutral 实测确认）

================================================================================
裁决
================================================================================
+14 净是迄今最高价值单 lever（block C 解死路）。5 暴露为 self-source false-pass 揭穿
（option b，与 doc-agent 建议一致）。land commit ebff2595 推送 origin/main。

next-lever 队列（提 chromium Oracle 一致率）：currentColor 解析（修后 border-color-011/012
+ shand-border-000 三暴露可翻回 PASS）/ margin-collapse-with-border（blocks-017）/
abspos-width-with-border（abspos-width-005）。
