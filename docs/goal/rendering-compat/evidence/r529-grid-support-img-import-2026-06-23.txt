R529 — css-grid 缺失支持图片导入（WPT 导入完整性，DC-14 分母真实性；R527 同类）
==============================================================================
日期：2026-06-23
类型：code-side asset import（reftest 驱动，零代码变更）
状态：LANDED（css-grid net +1 PASS / 0 回归）

承接
----
R528（§4.5 transferred-size）后续扫 css-grid 失败聚类（17 FAIL）。grid-in-table-cell-with-img
+ replaced-element-percentage-height-in-grid-nested-in-flex-{001,002} 引用 `grid-items/support/`
下图片，但该目录**为空**（grid-items/ 不存在 support 子目录）。与 R527（css-flexbox
300x150-green.png 缺失）同类 WPT 导入缺口。

缺口确认
--------
`ls wpt-data/css/css-grid/grid-items/support/` → 目录不存在。
css-grid 测试引用的支持图片：
  - grid-items/support/100x100-green.png —— 3 测试（replaced-element-...-001/002, img-src-changes）
  - grid-items/support/200x200-green.png —— 1 测试（grid-in-table-cell-with-img）
  - support/colors-8x16.png —— nested-grid-item-block-size-001 + 其 ref（test/ref 都缺，
    对比对称，非本 lever）

修复
----
创建：
  - grid-items/support/100x100-green.png（100×100 RGBA8 绿）
  - grid-items/support/200x200-green.png（200×200 RGBA8 绿）
零代码变更——仅新增 2 个二进制资产。

A/B 验证（test-guard，reftest-upstream css-grid 全量 48 案）
-----------------------------------------------------------
  R528 baseline: 31 pass
  WITH R529 imgs: 32 pass
  newly PASS (+1):
    - css/css-grid/grid-in-table-cell-with-img.html（img height:100% in 100px grid → 100×100）
  newly FAIL (回归): 0  ★

★ replaced-element-percentage-height-in-grid-nested-in-flex-{001,002} 仍 FAIL
（图片补齐后 img 取得固有尺寸，但 percentage-height-in-grid-nested-in-flex 解析仍错——
001 2.08%→6.25%、002 2.08%→31.25%，diff 变大但 FAIL 不变，同 R527 width-013 现象：
图片缺失→img=0→小 diff；图片补齐→img 按错误尺寸渲染→大 diff；底层 grid percentage-height
解析是真 lever，须独立修）。img-src-changes 是 JS 依赖（R523 JS-bridge 目标，harness 不跑 JS），
图片补齐不翻转。

意义
----
DC-14「分母真实性」要求 support 文件齐全。grid-items/support/ 全缺使引用它的 reftest
不可正确运行。补齐释放 grid-in-table-cell-with-img（percentage-height-in-grid 在 table-cell
内本就工作）。

门禁
----
代码 byte-identical to R528（git diff 仅新增 2 PNG）。make test / clippy 同 R528 绿基线
（提交前确认）。

残余（独立 lever，非本 import scope）
-------------------------------------
- replaced-element-percentage-height-in-grid-nested-in-flex-001/002：grid（1fr 行）nested
  in flex（height:100%）的 img height:100% 解析。须 grid track + flex definite-size + 百分比
  解析三路协同，多会话。
- img-src-changes：JS 依赖（R523 JS-DOM-bridge milestone）。
