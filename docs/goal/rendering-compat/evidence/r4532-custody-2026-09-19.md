# R4532 证据：全目录守成巡检轮（ZRG 低频巡检节奏，零代码变更）

- 日期：2026-09-19（R4532 巡检轮——R4531 下轮方向②，连续六轮代码轮后收口）
- 树态：37f2c1477（R4531 + 兄弟流 S1312 docs；tracked clean，零本流代码变更）。

## 巡检结果

1. **reftest 全量 corpus**：15123/16594 = **91.13%**，fail-list 1471；对 R4531 基线
   diff = **唯一 box-shadow-overlapping-003 移除**（家族已知 ±1 相位 flake，本轮
   翻绿相位；零新红）。
2. **make test**：EXIT=0，19,361P/0F（67 组 result 全 ok；树代码面与 R4531 轮一致
   ——计数同值预言兑现）。
3. **product-smoke**（DC-13 welcome vs chromium oracle）：EXIT=0，diff 15.40% 同值，
   struct-check PASS ×6（0 issues）。
4. **product-smoke-legacy**（DC-13 Tier 1，42 fixture vs chrome-127）：EXIT=0，
   struct-check failures: 0（42/42 全 PASS；含 37-form-controls 既有绿维持）。

## 运维注记

`make test`（后台）与 `make product-smoke`（前台）并发首跑时，两条入口同时执行
`target/test-guard` 编译规则 → rustc 直连编译竞态 → 链接期 `undefined symbol: main`
（非仓库代码缺陷，产物文件互踩）。串行执行两入口即免。本轮 smoke 复跑即 EXIT=0。

## 结论

守成轮全绿：corpus 91.13%（1471 fail）、make test 全绿、双 product smoke struct
全 PASS——R4526..R4531 六连代码轮的组合态（per-layer clip + 墨迹环带 + alpha 家族
修复）在当前树上无回归证据。下轮可择 clip-text mask 管线 RFC 评估或 corner-shape
域勘察或继续守成。
