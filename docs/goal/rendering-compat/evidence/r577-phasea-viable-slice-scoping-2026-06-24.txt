R577 — Phase A IFC 统一 viable-slice scoping：读设计 doc v1.2/v1.3 确认无 clean slice（存储已 R207/R355 放宽；真阻塞 = multicol fragmentation + 换行精度，非 IFC 统一）；clean lever 第 5 证穷尽；零代码 yield
日期: 2026-06-24
承接 R576 CONTINUE「读 phase-a-IFC-unification-design.md 识别最窄 viable-slice」。本轮按 R355/R358/R362
viable-slice 先例 scoping，结论 = **设计 doc 自身（v1.2/v1.3 post-R306/R378）已确认无 clean slice**。

================================================================================
Phase A 设计 doc 关键结论（v1.2/v1.3，无需重新 scoping）
================================================================================

1. **存储已 R207/R355 放宽**（v1.3 勘误）：实际存储条件（inline_finalization.rs:300-339）
   已覆盖①有直接文本子节点的 block 容器；②纯 inline-level 叶文本子容器，仅排除混合 inline+block
   （line 308）。Gate 2 `lines.len()<=1 && is_pure_ahem` **已过时**（R207 PHASEA_STORE_EXT + R355 多行放宽）。
   → 原 Phase A「放宽存储」核心目标**大部分已完成**。

2. **offset/baseline 非 Phase A 阻塞点**（v1.2 R306 证伪）：原 Phase 1「baseline_y=几何基线
   （frag.y+height）」经 env-gated A/B **证伪**——font-051 用 v_offset=frag.height 渲染 **16.67% FAIL**，
   默认 v_offset=is_ahem?0:font_size **0.00% PASS**。geometric baseline ≠ fontdue render baseline。
   当前 Path A offset **load-bearing 正确**，勿动。

3. **真硬阻塞 = 墙② multicol + 换行精度**（v1.2 §0/§6.3B）：非 offset/baseline 统一。
   - 墙②（multicol）：R327 resolved 机制——multicol-fill-auto 回归因 REF 侧 float 容器切 Path A
     （layout 无法区分 ref 上下文），真解 = Phase 2 multicol 也走 Path A（column-aware）= multicol
     fragmentation 架构（独立 RFC multicol-fragmentation-design.md）。
   - 换行精度：advance-width plumbing（R225 已证伪独立死路）。

4. **墙③（混合内容存储移除）非 lever**（v1.3 §0）：当前 47 self-source 失败案**无一依赖**该排除被移除
   （border-bottom-width-006 是匿名块盒生成 R361/R109 谱系，非存储；余皆 multicol/baseline/writing-modes/
   taffy/large-font 簇）。移除只会重演 R206 回归（ifc-001/002/003），无 upside。

5. **M2（multicol Path B 用真实 font_size）撞 R125/R198/R205 耦合**：font_size + line-height 耦合，
   5 轮单点修 font_size 全净负向回归。M2 同机制 → net-negative 风险。

================================================================================
裁决：Phase A 无 clean viable-slice，clean lever 第 5 证穷尽
================================================================================

★ **Phase A IFC 统一 viable-slice scoping = 无 clean slice**：设计 doc 自身（v1.2/v1.3）已确认
   ①存储已放宽②offset 非阻塞③真阻塞 = multicol fragmentation + 换行精度（非 IFC 统一）④墙③非 lever
   ⑤M2 撞耦合。**原 Phase A「消灭 Path B / baseline-resolved 统一」重心已证非真阻塞**，scoping 无新切入点。

★ **clean 单会话 lever 第 5 次确证穷尽**（R305-R558 + R568×2 + R576 + R577 Phase A scoping）。
   剩余前向路径全多 session 架构，且**互相阻塞**：
   - **multicol column-aware IFC**（Phase A 真阻塞；但 master 记 R157/R198/R203/R317/R122 五轮 paint-side
     死路，须 layout 侧 column-aware；multicol-fragmentation-design.md 独立 RFC）。
   - flex-baseline-export（R266 ruled out）/ flex-abspos-shrink-to-fit（R568 复杂）。
   - writing-modes 轴交换（R114/R164 4 轮证否）。
   - taffy intrinsic sizing max/min-content（R304 DEFER 升级）。
   - R177 width:auto 表盒宽度（line1675 load-bearing）。
   - 伪元素/marker inline box（R554/R576 谱系）。

★ **47 self-source 失败分布**（v1.3 §0）= multicol 碎片化 13 / flex-baseline 3 + flex-abspos 2 /
   writing-modes 5 / taffy-blocked / large-font 2（empty-inline-002 R378 判 large-font/嵌套定位结构性 +
   ifc-011 1.23% near-miss）。全各架构簇，无单点。

================================================================================
下一步
================================================================================

Phase A viable-slice scoping 闭环（无 clean slice，设计 doc 自证）。剩余前向 = multicol column-aware
IFC 架构（Phase A 真阻塞，master 记 5 轮 paint-side 死路须 layout 侧；多 session）；或零风险续 CSS2
剩余子目录测量完成聚合。本会话零代码 yield（纯 scoping 负结果，防 Phase A viable-slice 再调查）。
