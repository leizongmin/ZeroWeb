R659 — DC-13 line 322 产品 fixture 窄屏 viewport 覆盖（welcome/morning/wintertc）
日期：2026-06-26 | 类型：DC-13 覆盖（零源码）| 承：R658（legacy 窄屏子集）

================================================================================
0. 目标与裁决
================================================================================
DC-13 line 322 要求「至少覆盖桌面和窄屏两个 viewport」。R658 addendum 完成了 legacy-html
子集的窄屏（375×667）覆盖；本轮补齐 3 个产品 fixture（welcome / morning-work / wintertc）
的窄屏覆盖，至此 DC-13 line 322 对全部产品 + legacy fixture 完成。

★ 裁决：3 个产品 fixture 在 375×667 窄屏下**结构完整渲染**（无崩溃/空白/错乱），diff% 升高
纯**字体墙放大**（窄宽→更多换行→更多字形像素 + 行度量漂移累积），无新响应式布局 bug。

================================================================================
1. 窄屏 375×667 diff%（chromium oracle vs ZeroWeb CPU）
================================================================================
fixture        桌面800×600    窄屏375×667    thick_core%(窄屏)   说明
welcome         16.16%        25.35%        33.5%              卡片 reflow + 字体漂移；桌面 26% thick 同源
morning-work    18.15%        22.42%        10.7%              低 thick = 字体/边噪声（font-wall）
wintertc        13.59%        25.74%        15.8%              +12pp 升高 = hero/nav/logo 区窄屏 reflow + 字体墙

================================================================================
2. 结构完整性验证（逐行 ASCII，375×667）
================================================================================
wintertc 窄屏 ASCII 体检（diff 最大，最可疑）：
- 三条全宽 `+++` 带（nav/section 背景，y≈192–224）在 CHR 与 ZW 间**完全一致** → 结构骨架 intact。
- hero logo（SVG）+ nav 按钮区（y≈48–128）两侧均渲染，仅字形位置差异（字体漂移）。
- 参与方 logo grid（底部）两侧均有内容，flex-wrap 在窄屏换行更多行（预期行为）。
→ 无 2 列→堆叠破损、无 logo 缺失、无结构错乱。

thick_core 方法学（同 R658 §3）：高 thick_core ≠ 布局 bug——垂直字体漂移使 glyph 整体下移
形成大块实心 diff 带。welcome 窄屏 33.5% thick 与其桌面 26% thick 同源（卡片背景 + 字形漂移），
经 ASCII 证伪为字体墙非布局 bug。morning(10.7%)/wintertc(15.8%) 低 thick 直接 = 字体/边噪声。

================================================================================
3. 与 wintertc README 已知 flex-wrap logo bug 的关系
================================================================================
wintertc README 记录已知问题：「参与方 `flex flex-wrap justify-evenly` 多 logo 未布局
（800×2000 仅 2 个 ImagePrimitive）」。窄屏 375×667 可视区内该 logo grid 在折叠线以下
（y≈400+），窄屏 diff 未新增该 bug 的可视暴露（仍待独立诊断 flex-wrap 多 item 布局精度，
非本轮范围）。本轮窄屏 wintertc diff 主因 = 顶部 hero/nav/正文字体墙 + 窄屏 reflow，非该
flex-wrap bug。

================================================================================
4. 结论
================================================================================
- DC-13 line 322「桌面 + 窄屏两 viewport」对全部产品（welcome/morning/wintertc）+ legacy（20 页）
  fixture **覆盖完成**：窄屏结构完整，diff 升高纯字体墙放大，无响应式布局 bug。
- 工具：capture-legacy-oracle.mjs VW/VH env（R658 addendum 加）+ product-smoke --width/--height/--base-dir。
- forward motion：全部产品 + legacy fixture 残余 diff 主导 = **字体墙**（R633 Phase A 死锁，
  须 IFC↔FontLoader 真实 ascent/advance 度量统一，多会话）。无新单 session clean lever。

零源码变更（3 fixture × {chromium narrow oracle PNG + ZeroWeb narrow CPU PNG} + 本 evidence +
master.md R659）。
