# 协调记录：master.md 碰撞（并行 agent 未提交 R228b）+ morning-work 图片加载核实（R233）

**日期**: 2026-06-18
**性质**: 协调 + read-only 核实，docs-only（**本轮不动 master.md**，见下），无代码变更，基线 438/490 持平。

## 1. master.md 工作区碰撞 → 本轮 defer master.md
- 工作区 `docs/goal/rendering-compat/master.md` 现含**并行 agent 未提交的 R228b 条目**（半透明圆角矩形 alpha 丢失修复，cpu/mod.rs + cpu/tests.rs + table.rs WIP），插在 line ~2108（老段），命名用 **R228b**（**未与我的 R228-R232 撞号**，良好）。
- 该条目记录的代码尚未提交（table.rs/cpu WIP 仍未提交，第 7 轮）。按「只提交自己改的文档」约束，commit master.md 会把其未提交-feature 的文档裹进我的 commit（doc 早于 code 的不一致态），故**本轮 defer master.md**，待并行 agent 提交其 code+doc 后再同步。
- 我的 R228-R232（welcome font-weight / DC-14 183 聚类 / text-emphasis 字体 plumbing 模式）均已提交，master.md 顶部 round 段对最新提交现实仍准确。

## 2. morning-work 67% 压缩：read-only 核实（细化并行 agent R228b 假设）
并行 agent R228b 疑「img 固有尺寸塌缩」或「pre code min-width:700px 溢出」致内容堆顶。本轮 read-only 核实排除前者简化版：
- **smoke 路径确会加载图片**：`render_to_framebuffer_with_base`（reftest.rs:916-927）调 `build_image_cache(html, base_dir)` + `extract_image_sizes` → 经 `pipeline.rs:128 build_img_intrinsic_sizes`（NodeId→(w,h)）+ `compute_with_img_sizes`（engine.rs:109）注入布局。morning-work README 亦标 images ✅（base_dir 加载）。
- → **morning-work 压缩非「图片未加载」**（排除 naive 假设）。真因更可能在：① **layout 侧 img-size 应用 bug**（compute_with_img_sizes 路径，R176 谱系，intrinsic 尺寸虽传入但未正确撑开 img 盒）—— 仍可能是「img 塌缩」但根因在 layout 应用非加载；或 ② **pre/code 溢出**（article.html 有 **21 个 `<pre><code>`** 块 + `.article pre code { white-space: pre-wrap }`，若 ZeroWeb pre-wrap 不正确致长 bash 命令撑宽/溢出推布局）。前者（img 应用）与后者（pre/code）可用单变量探针区分。
- **此为并行 agent 主动调查项（R228b「下一轮排查」），本轮不接手**，仅记录以锐化其下一轮方向：先排除「图片未加载」，在「layout img-size 应用 bug」与「pre/code 溢出」间二分。

## 3. GPU alpha 全局丢弃（DC-9 子项，并行 agent R228b 已记录）
`gpu/mesh.rs:11 color_to_f32` 仅返 RGB 三通道丢弃 alpha → GPU fill/圆角均无法半透明。属 DC-9 GPU 全量图元覆盖子项（需 WGSL blend 配置 + 顶点带 alpha），与 CPU fill_rounded_rect 修复（并行 agent R228b）同源不同层。本轮仅复核确认，未修。

## 本轮交付
仅本证据文件（无 master.md 改动，避碰撞）。下一轮：待并行 agent 提交其 R228b code+doc 后，我再同步 master.md；或我继续 DC-14 字体 plumbing（R229 font-weight → font-kerning）调研。
