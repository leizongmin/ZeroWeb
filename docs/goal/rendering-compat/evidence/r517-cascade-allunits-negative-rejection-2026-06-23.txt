R517 — cascade 负值拒绝 all-units 泛化（em/rem/ch/%）
======================================================
Date: 2026-06-23  Round: R517  Commit: 3c195635 (code-side, parallel code agent)
Role: doc-agent 独立验证 + 文档纠正（本会话零源码改动）

1. WHAT (code agent 实现，read-only 核查)
------------------------------------------
R513c / R516 仅拒绝**负 px**（cm/in/mm/pt/pc/Q 在 parse 时转 px，故已隐含覆盖）。
WPT「negative values are illegal」测试另用 em/%/ch 单位（height-089、
max-width-089、max-height-067 …）。同一 CSS 规则适用：box-model 尺寸属性
（width/height/min-/max-/padding/border-*-width）上的负长度/百分比非法 →
级联时按未声明处理 → initial value。

修复（cascade.rs is_invalid_negative_length）：在原 LengthValue::Px(p) if p<0
match arm 之外，新增 Em/Rem/Ch/Percentage 负值 arm。calc() 负值仍排除（须求值，
非声明级 lever）。+32/-4 行（含 1 个 em/% 回归单测）。

2. VERIFICATION（doc-agent 独立 targeted reftest，test-guard 包裹）
-----------------------------------------------------------------
命令：./target/test-guard -- cargo run --bin zero-wpt-runner -- reftest-upstream <name>
（test-guard = make reftest 同款 wrapper，非裸 cargo）

  height-089      → ✓ css/CSS2/normal-flow/height-089.xht      (0.00%)
                   ✓ css/CSS2/normal-flow/max-height-089.xht   (0.00%)  [同簇 bonus]
                   ✓ css/CSS2/normal-flow/min-height-089.xht   (0.00%)  [同簇 bonus]
  max-height-067  → ✓ css/CSS2/normal-flow/max-height-067.xht  (0.00%)
  max-width-089   → ✓ css/CSS2/normal-flow/max-width-089.xht   (0.00%)

3 named driving tests 全 PASS（0.00% diff）= R517 claim 独立坐实。
（max-height-089 / min-height-089 同 em/% 负值簇，一并 PASS，与 R517 机制一致。）

code-agent A/B（vs R516 px-only 同基，自报）：
  normal-flow 590/733 (80.5%) → 593/733 (80.9%)  +3 PASS / 0 REGRESS
  diff-confirmed: height-089 / max-height-067 / max-width-089 翻转
  零 px-only-PASS → em%-FAIL 回归；cargo test --workspace exit 0；
  clippy -p zero-style-system clean；53 cascade 单测通过（+1 em/%）。

3. FORWARD-LOOK 纠偏（核心文档结论）
------------------------------------
本会话上一轮 forward-look（commit 5b201326）据 grep 预测「~35 genuine driving
tests / substantial 额外 yield > R516c 的 +6」。**实测仅 +3（< R516c 的 +6）。**

★ 教训 = **候选文件 grep 计数高估实际翻转数**：
  - grep 命中 ~35 个含「box-model 负 em/%」的测试文件；
  - 但多数文件**早已因他因 PASS**（负值声明只是测试中众多声明之一，非 pass/fail
    关键路径），或 **fail 于无关模式**（IFC/fontdue/结构性）；
  - 真正 hinge 于「负 em/% 是否被拒绝」的只有 3 个。
  → grep candidate-count 是 yield 的上界而非估计；预测须以「该声明是否为测试
    关键路径」过滤，不能直接用文件计数。

4. CUMULATIVE cascade 负值 vein（系统闭合）
------------------------------------------
  R513c (0b11a12d) narrow  : numbers-units-006                    +1
  R516c (01f63d70) full-px : max-height-001/012/023/034/045/056   +6
  R517  (3c195635) em%     : height-089 / max-height-067 / max-width-089  +3
  ---------------------------------------------------------------
  self-source normal-flow + values 累计                              +10
  （code-agent 跨目录 broad 累计 ~12，另含 height-034 / min-height-034）

vein 现系统闭合：所有可解析长度单位（px 系 + em/rem/ch/%）的负值均拒；
唯一剩余 = calc() 负值（须求值，非声明级，out-of-scope）。

5. STATUS
--------
- R517 LANDED + doc-agent 独立验证（3 targeted PASS）。
- doc/research 单会话 lever 空间：4 大 vein 全穷尽（CSS-correctness /
  validity-rejection【含 R517，现闭合】/ dead-value / inheritance）。
- 唯一剩余 genuine lever = R515 flex-grow/shrink（apply.rs:577-587，+2，
  已 de-risked，pending code agent 腾手）。
- R515 后须用户决策多会话架构（Phase A IFC / font-feature shaper.rs:82 /
  multicol fragmentation）。
