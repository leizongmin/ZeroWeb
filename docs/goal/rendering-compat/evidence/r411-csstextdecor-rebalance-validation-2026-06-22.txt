# R411 — css-text-decor 在途导入量化：R409「再平衡」预测验证（只读）

**日期**：2026-06-22
**承接**：R409（css-text-decor pre-confirm，预测全量再平衡离开子集 82% text-emphasis 偏斜）。
并行 agent 仍在导入 css-text-decor（未追踪第 4 轮传输中，67→98→214→215，未提交）。
本轮量化在途导入的实际簇占比，直接检验 R409 核心预测。

## 在途导入（untracked）簇量化（test-side，排除 -ref）

在途 test-side 文件数：**191**（占全量 356 的 ~54%，并行 agent 仍在加，但样本足够大）。

| 簇 | 在途数 | 在途占比 | 子集占比（R409） | 变化 |
|------|--------|----------|------------------|------|
| text-emphasis | 91 | **47.6%** | 82% | **−34pp** |
| text-decoration（inset 24/skip-spaces 9/propagation 5+2/subelements 4/thickness 4/skip-ink 2/shorthands 2/dotted 2 等） | 81 | **42.4%** | 10% | **+32pp** |
| text-shadow | 0 | 0% | 0% | — |
| other（underline-offset/position 等） | 19 | 9.9% | 8% | +2pp |

## 验证结论

**R409「再平衡」预测 CONFIRMED**：
- 子集 82% text-emphasis 偏斜**在全量中消失**——全量是**平衡集**（47.6% emphasis / 42.4% decoration）。
- text-decoration（已实现，R409 锚定 `paint_text_decoration_from_style`）从子集 10% 升至全量 42.4%，
  是**真实的大通过项**，而非子集暗示的边缘簇。

## 预测修订

R409 原预测「全量 oracle ~35-43%，text-emphasis 簇封顶」——本轮数据**上修至上限端**：
- text-decoration 42.4% 大部分通过（基础 underline/line-through/color/dotted/thickness 已实现；
  残余 skip-ink/propagation 子特性可能有缺口，估 ~70-80% 通过）→ 贡献全量 ~30% 通过。
- text-emphasis 47.6% 全失败（R392 生产全无）→ 0 贡献。
- **修订预测：全量 text-decor oracle ~38-46%**，大概率**接近/触及 41% 基线**（text-decoration 托底），
  text-emphasis 封顶在 tables(43.8%) 之下。较 R409 原下限（35%）更乐观，因 implemented
  text-decoration 占比远大于子集所示。

## text-decoration 在途子簇明细（验证「已实现特性占大头」）

| 子簇 | 数 | 实现状态 |
|------|----|----------|
| text-decoration-inset | 24 | （text-decoration-style 渲染，基础已实现） |
| text-decoration-skip-spaces | 9 | skip-spaces 子特性，可能有缺口 |
| text-decoration-propagation | 5 (+2 display-contents) | decoration 传播到后代，可能有缺口 |
| text-decoration-subelements | 4 | 子元素传播 |
| text-decoration-thickness(-vertical/-length-rounding) | 4 | thickness 已实现 |
| text-decoration-skip-ink | 2 | skip-ink（字形下沉避开），可能有缺口 |
| text-decoration-dotted | 2 | dotted 样式，已实现 |
| text-decoration-shorthands | 2 | 简写解析 |

→ text-decoration 子簇中，基础样式（inset/dotted/thickness）已实现占大头；
  skip-ink/skip-spaces/propagation 为潜在子缺口（Phase A / 新特性），非主导。

## 裁决

- R409 再平衡预测经在途量化**确认并上修**：全量 text-decor 非子集所示 emphasis 主导，
  而是平衡集，text-decoration（已实现）42.4% 是真实通过项 → 全量 oracle 预测 ~38-46%。
- 待并行 agent 提交后，用真通过率对照此修订预测（验证 oracle 是否触及 41% 基线、
  text-decoration 子簇 skip-ink/propagation 缺口占比）。
- **本轮是最后一轮前向准备**（pre-confirm/验证）：R409 text-decor + R410 fonts + 本轮 R411 验证
  已覆盖下两个文字类目录的全部基线。后续等并行 agent 提交 text-decor，折回 4/9 并对照预测。

## 本轮变更

- 零 Rust 变更；零 reftest 运行（只读文件计数 + 在途 git ls-files）。
- 新增本证据文件；master.md 加 R411 行（综合裁决表）。
