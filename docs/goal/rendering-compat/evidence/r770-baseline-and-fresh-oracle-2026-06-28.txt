# R770 — baseline green + fresh chromium-Oracle measurements (css-tables/css-grid/css-flexbox)

Date: 2026-06-28
Round: R770 (continuation; prior R769j)
Net source: 0 (investigation + measurement round; confirms single-session clean-lever plateau)

## Baseline
`./target/test-guard --per-proc-mem 10 --total-mem 28 -- cargo test --workspace -- --test-threads=2`
=> BASELINE3_EXIT=0, 48 test binaries, **0 failures**. Workspace green on clean main HEAD (1b54ce3f).
NOTE: `make test` (Makefile:52) hard-codes `test-guard -- cargo test` with no mem flags, so the
browser binary (>6GB RSS) trips the default 6GB per-proc limit and test-guard kills the tree
(exit 124). For full-workspace runs use test-guard DIRECTLY with `--per-proc-mem 10 --total-mem 28`
and `cargo test -- --test-threads=2` (R769b established this; still required).

## Fresh chromium-Oracle measurements (DC-14 independent Oracle, chr<1%)
Stale master.md aggregate (pre-R525–R546 / pre-R769) updated for measured dirs:

- css-tables: **58/115 = 50.4%** (was stale 49/112=43.8%). +9 PASS / +6.6pp.
  Quantifies R767 (table cell content-width clamp, -41pp on margin-collapse-101) +
  R769 (table-cell BFC first-child margin-top, -10pp on 110/111) + R769c (anonymous
  table-cell shrink-to-fit, -14pp). Divergence REDUCTION confirmed at Oracle level
  (these rounds were net-zero PASS self-source but real chr divergence reduction).
- css-grid: 16/49 = 32.7% (unchanged vs R560; reconfirmed). Top all structural:
  baseline-export (78%), aspect-ratio (64%, R692), R691 %height-in-grid (34%, taffy-gated),
  form-control stretch. No clean single-session lever.
- css-flexbox: **265/497 = 53.3%** (was stale 251/496=50.6%). +14 PASS / +2.7pp.
  Reflects R525–R542 self-source +108 (batch_fills paint-order R525 +5 / abspos-order R526 /
  flex-shrink min-size R530 +9 / flexbox_flex R536 +52 / nowrap inline-block R537 +9) now
  visible at Oracle level. Top all structural: flex-minimum-width-flex-items-013 (82%,
  auto-min-width R428 family) / aspect-ratio-intrinsic-size-* (R692 family) /
  flexbox-baseline-align-self-baseline-* (baseline-export) / flex-abspos-inset-* (R363/R711)
  / flexbox-flex-flow + direction-row-reverse (axis-swap R57/R109). No clean lever.

## Lever investigation this round (all confirmed multi-session/structural)
- R695 height-percentage-005 (88.44%): %height→auto when CB height indefinite (CSS §10.5).
  `clamp_percentage_max_height` (engine.rs:1404) ALREADY propagates CB-definiteness
  (my_definite_content_height recursion) and handles max/min-height Percentage, but NOT
  plain height:Percentage→auto. Post-layout fixup can't cleanly recompute (children laid
  out with wrong height); needs pre-layout CB-definiteness cascade OR replaced-element-
  specific intrinsic fallback. Complex, multi-session.
- R692 nested-grid-item-block-size-001 (64.36%): block-size:55vw + aspect-ratio:2/1 on img.
  vw IS resolved (convert_length_to_dimension → resolve_viewport_px, converter/mod.rs:377);
  aspect-ratio IS passed to taffy (mod.rs:104); tree.rs:213/260 `is_none()` guards correctly
  skip intrinsic-ratio override when CSS aspect-ratio set. Real blocker = grid nesting
  constrains available width / taffy 0.7 aspect-ratio with grid = taffy-gated structural
  (R304 territory). NOT a clean single-session win.
- css-grid top (78/64/34/26/17%): all baseline-export / aspect-ratio / R691 / form-control.
- css-tables top (20/17/15/15/15/12/9.7%): R680 br-anon-block / %height-in-cell (R695-quirks
  family) / baseline-vertical / R109 row-group-color / table-cell-overflow. All structural.
- percentages-grandchildren-quirks-mode-001/002 (14.95% ×2): quirks-mode + table-cell +
  %height CB-definiteness nuance ("cell children don't apply %height quirk to their
  children"). Subtle; same CB-definiteness family as R695, multi-session.

## Conclusion (reconfirms firm plateau, R591/R740/R769h lineage)
Single-session clean-lever frontier CLOSED across measured dirs (css-tables/css-grid) +
investigated levers (R695/R692). Every top divergence is structural (R109 anon-block /
baseline-export / multicol / taffy-gated R304) or font-wall-gated (Phase A IFC font metrics).
Forward motion = multi-session architecture: R109 §9.2.1.1 anon-block + collapse/paint
coordination (highest yield: R702 cluster 7 + insert-block-in-inlines) needs spec-rfc;
Phase A IFC font-metric interface is font-wall-deadlocked (R633/R668 5-proof permanent).
