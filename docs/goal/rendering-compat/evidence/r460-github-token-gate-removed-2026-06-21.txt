R460 — R459 GITHUB_TOKEN gate REMOVED（10db810 git/trees recursive=1：每目录 2 调用 vs 30-75）
日期: 2026-06-21（doc-maintenance rollover 轮，read-only 代码核查 + dry-run 复验，零代码变更）
承接: R459（css-text 全量导入 code-ready，credential 层待 GITHUB_TOKEN）
本轮角色: rollover 维护；并行 agent 负责 code/import，本轮验证新 commit + 纠正 stale 结论。

================================================================
1. 新 commit 10db810（并行 agent）= gate 移除
================================================================
commit 10db810 `feat(reftest): discover uses git/trees recursive=1 (2 calls vs 30-75)`
把 `collect_test_paths`（discover-reftests-authoritative.py）从逐子目录 `gh_api(contents)`
递归（css-text ~30 subdir / CSS2 ~43 subdir 各 1 调用 = 30-75 次/目录，超 60/hr 共享 IP 限速）
改为**单次 `git/trees/{sha}?recursive=1`**:
  - get_tree_sha(): contents(parent) 取 category 目录 tree SHA = 1 调用
  - gh_tree(): recursive=1 一次性返回整棵子树（含全部子目录 blob）= 1 调用
  - 合计 **2 调用/目录**（flat 目录 writing-modes 经新路径输出 identical）
  - 截断守卫: tree.truncated（>100k 条目，WPT 任一 css 目录不发生）则 raise，拒绝静默
    部分导入保 DC-14 分母完整性。
依赖更新: docstring 改「子目录枚举用 git/trees?recursive=1，每目录 2 调用，绕开 60/hr 限速」。

================================================================
2. gate 移除实证（commit msg 载 + 代码自证）
================================================================
10db810 commit msg: "verified: css-text = 2994 blobs / 1914 test files, not truncated;
clears the rate-limit blocker without a GITHUB_TOKEN"。
- css-text 全量子树 2994 blob / 1914 test 文件 = 单次 git/trees 返回，无截断。
- 2 调用 << 60/hr 未认证预算 → **css-text/CSS2 全量导入无需 GITHUB_TOKEN**。

================================================================
3. 本轮独立 dry-run 复验（部分受阻于本 shell 自身 budget）
================================================================
命令: `python3 discover-reftests-authoritative.py --category css/css-text`（无 --import）
实测: 起始 core budget **0/60**（上轮 R459 dry-run 递归耗尽，38.2min 后 reset 至 60）→
首次 `gh_api(parent="css")`（get_tree_sha 第 1 调用）即 403 rate limit exceeded。
**关键定性**: 此 403 **非 gate 问题**——10db810 设计本就 2 调用 << 60，是本 rollover shell
自身 budget 0/60（继承上轮耗尽）所致；fresh budget 下 2 调用秒完成（并行 agent commit msg
已实证 css-text 1914 test 文件 / 2994 blobs）。本轮不空等 38min reset（doc-maintenance 角色，
代码核查 + commit msg 实证已足够定性）。

================================================================
4. 纠正 R459（stale 结论）
================================================================
R459 称「css-text 全量导入 code-ready，credential 层待 GITHUB_TOKEN（或等 60/hr 窗口/缓存
gh_api）」。**10db810 后此结论 stale**:
  - 「credential 层待 GITHUB_TOKEN」→ **gate 移除**（git/trees 2 调用绕开限速）。
  - css-text 全量导入**完全 unblocked**（code + 无 rate 限制，无 credential 依赖）。
R459 的「子目录递归 RESOLVED（1568cb0）」部分仍成立（10db810 是其 perf 优化，非推翻）。
R458「css-text 得 0 对工具阻塞」= 早已 RESOLVED（1568cb0 递归 + 10db810 perf）。

================================================================
5. css-text 权威规模 + 下一步
================================================================
- css-text: **1914 test 文件 / 2994 blobs**（commit msg），权威对（link rel=match）≤1914，
  精确对数待 fresh-budget dry-run（本轮 budget 0 未取得；R458 spot-check 估「数百」）。
- **下一步**: css-text 全量 import + reftest + chromium-Oracle 捕获（第 9 目录，**无任何 gate**）
  = 并行 agent 下一导入批次；之后 CSS2（亦 2 调用，原 ~75→2）= 最终目录。
- 输出: CONTINUE（gate 已移除，css-text/CSS2 完全 runnable；非 BLOCK）。
