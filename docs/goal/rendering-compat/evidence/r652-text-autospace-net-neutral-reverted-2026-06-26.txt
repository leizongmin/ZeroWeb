R652 — text-autospace 特性实现·CORRECT 但 NET-NEUTRAL·REVERTED（零 landing）
日期: 2026-06-26
状态: 实现正确但 0 reftest flip，按 zero-yield 惯例 REVERT

================================================================
背景
================================================================
R647 评估 text-autospace（CSS Text 4）为最高潜在 yield feature（advance 数学匹配 ref）。
本轮 dedicated 实现 + A/B 验证。

================================================================
实现（已 REVERT，~110 行 11 文件）
================================================================
完整实现 text-autospace: normal（默认 no-autospace 控 blast radius）：
- css-parser: TextAutospaceValue { Normal, NoAutospace } + parse_text_autospace
  （ideograph-alpha/numeric 等同 Normal）。
- style-system: TextAutospaceComputedValue + PropertyId variant + registry（initial
  NoAutospace + is_inherited + known list）+ ComputedStyle field + default + apply +
  inherit（2 处）。
- layout-engine: AutospaceCategory { Ideograph, Letter, Numeric, Other } +
  autospace_category + needs_autospace（Ideograph↔Letter/Numeric 双向）;
  InlineFormattingContext.text_autospace + with_text_autospace; break_items_into_lines
  跨 run/word 追踪 prev_autospace_cat，在边界词前加 0.125em×fs。
- 接线 5 处 IFC 构造：compute_final（engine.rs）+ paint Path B（text.rs）+ measure/
  remeasure×3（inline_finalization.rs，含 balance col_ctx）。

================================================================
A/B 结果（test-guard，text-autospace 簇 34 case + css-text 全量）
================================================================
  text-autospace 簇: 30 PASS / 4 FAIL —— **BASE 与 WITH 完全相同（0 flip）**
  css-text 全量: 90 fail 名 —— BASE 与 WITH 相同（0 回归 / 0 win）

**实现被证实 CORRECT**（match 变体验证）：
  text-autospace-no-001#0（match，test `.normal` 自动间距 vs ref `.no-as` manual 0.125em
  margin）: BASE 0.51% PASS → WITH 0.54% PASS（间距生效，匹配 ref manual margin）。
  即 0.125em 自动间距正确施加，与 ref 手动 margin 一致。

**但仍 0 flip，因 mismatch 变体被阈值挡住**：
  text-autospace-no-001#1（mismatch，test `.no-as` 无间距 vs mismatch-ref `.normal` 有间距）:
  BASE 0.00%（ZW 忽略属性→两侧均无间距→字节同）→ WITH **0.20%**（间距生效产生差异）
  —— 改善但 **< min_mismatch_ratio 0.5% 阈值 → 仍 FAIL**。0.125em 在小文本（默认 16px，
  0.125em=2px）上仅产生 ~0.20% 像素差，结构性低于 0.5% 阈值。
  text-autospace-dynamic-001#1 同（0.00%→~0.20%，阈下）。

**match 测试（break-001 8.50% / first-line-001 6.01%）远离阈值**：须 break opportunities
+ 精确 shaping/字体度量，非仅 advance；即使扩展实现 break 机会，8.5%/6.0% 仍远高于 match
阈值，无法 flip。

================================================================
裁决：NET-NEUTRAL，REVERT
================================================================
text-autospace 实现正确（match 变体证 0.125em 生效且匹配 ref），但 **0 reftest flip**：
mismatch 变体被 0.5% 阈值结构性挡住（0.125em 小文本仅 0.20% 差异），match 变体远离阈值。
即「正确实现也无法 yield」——非实现 bug，是 harness 阈值 × 小文本间距效应的固有限制。

按 rally zero-yield 惯例（R502/R524/R651）+ code-guidelines §2（不留零价值代码）REVERT。
代码 git stash drop 丢弃（工作树 clean = HEAD）。

**防重投**：text-autospace 在当前 harness（0.5% mismatch 阈值 + 默认小字号）下**不可 yield**，
即使正确实现 0.125em + break opportunities。真 yield 须满足之一：(a) 测试用大字号使 0.125em
差>0.5%；(b) harness 降 mismatch 阈值（违反 DC-14 锁定，不可）；(c) match 测试降至阈值内
（须 break+shaping+字体度量全套，多会话 Phase A）。故 text-autospace 单 session **关闭**。

意外修复：恢复误删的 R376 stash（git stash store 9432afdb），无代码影响。

零 landing（实现 + A/B + revert，工作树 clean）。
