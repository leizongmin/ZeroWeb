R254 — R238 WM-1 abspos-vertical spec 对当前代码实证核实：准确，第二 reftest 杠杆就绪（完成三部曲）
日期: 2026-06-18
模式: read-only（读 converter/mod.rs + engine.rs；工作树干净，无并行 agent WIP 碰撞）
基线: reftest 自源 438/490（持平）；DC-14 严格真通过 188/475=39.6%

================================================================
目的
================================================================
完成 reftest 杠杆验证三部曲（R251 R244 alpha ✓ / R253 R236 multicol baseline ✓ / 本轮 R238
WM-1）。核实 R238 WM-1 abspos-vertical spec（写于 5dff3c4）对当前 HEAD(000a462) 代码仍准确，
为「最大化 reftest 进展」路径补齐最后一个 spec 去风险。converter/engine.rs abspos 区域无碰撞。

================================================================
逐条核实（R238 机制，全部确认）
================================================================
【claim 1: apply_vertical_writing_mode 在 converter 侧】✅ 确认
  crates/layout-engine/src/converter/mod.rs:196  pub fn apply_vertical_writing_mode(style)
  tree.rs:429 调用  crate::converter::apply_vertical_writing_mode(&mut taffy_style)
  engine.rs:715 注释「与 tree.rs 中的 apply_vertical_writing_mode 轴交换配合使用」
  → css-writing-modes §7.1 dimension-swap（inset/size 轴交换）已落地。

【claim 2: engine.rs abspos static-position fix 守 all_inset_auto】✅ 确认
  engine.rs:1394-1426：
    1396  all_inset_auto = style.top==Auto && style.bottom==Auto
    1401  if all_inset_auto { 用 IFC fragment 位置修正静态位置（child.x/y=fragment.x/y）}
  → static-position 修正存在且守 all_inset_auto（仅 top+bottom auto 时；注：未查 left/right，
    R238 residual A「ltr all-auto 1.28%」可能部分源于此）。

【claim 3: §10.3.7 处理在但仅 height shrink-to-fit，缺 direction 分支】✅ 确认（关键）
  engine.rs:1411-1425：§10.3.7 height shrink-to-fit——height:auto 时 child.height 收缩到
    fragment.width（内容 inline 跨度）max font_size，替代 taffy 的 cross-axis stretch(320)。
    注释「CSS §10.3.7 + writing-modes §7.1：vertical-rl 下 abspos 物理 height 应 shrink-to-fit」。
  → §10.3.7 处理**存在但仅 height 维度**；**无 direction(rtl/lr) 分支**——静态位置修正(1401-1409)
    直接用 fragment.x/y 不按书写方向(rtl)调整。**R238 residual B「缺 §10.3.7 direction 分支→rtl
    5.03%=4×ltr」= 当前最清晰修复信号**（confirmed：direction 分支确实缺）。

================================================================
结论 + R164 教训
================================================================
R238 spec 对当前代码**完全准确**（5dff3c4→HEAD 无相关变更；§10.3.7 height-shrink 在但
direction 分支缺= residual B 确认）。WM-1 abspos-vertical = **第二 reftest 杠杆**（14 例
css-writing-modes abspos vrl/vlr），修复=给 static-position 修正加 direction(rtl/lr) 分支
（contained to engine.rs:1394-1426，无 converter/taffy/paint 改）。
⚠️ **R164 教训适用**：per-case diff 混合 positioning + Ahem 噪声，**勿未实验就声称 clean +14**
（R238 本身已带此告诫）。实现 agent 须先跑当前 per-case diff 确认 residual B（rtl 5.03%）仍在
+ 实验加 direction 分支后该 residual 消除且无回归，方可声称。

================================================================
reftest 杠杆验证三部曲完结
================================================================
- R251 R244 DC-9 alpha（P1 contained+硬DC）：spec 准确，就绪（≈0 reftest 杠杆，DC 硬门禁）。
- R253 R236 multicol baseline（P1' 最高 contained reftest 杠杆）：spec 准确，就绪，+8 潜力。
- R254 R238 WM-1 abspos-vertical（P2 第二 reftest 杠杆）：spec 准确，就绪，14 例（须实验确证，
  R164 教训）。
三条 reftest/实现路径全 spec+验证就绪，实现 agent 可按「contained 优先(DC-9)/reftest 分数优先
(R236→R238)」选择，无需再调研。

无代码变更。基线 438/490 持平。
