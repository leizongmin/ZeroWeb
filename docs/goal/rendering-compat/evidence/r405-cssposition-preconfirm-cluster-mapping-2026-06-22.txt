# R405 — css-position 失败簇 pre-confirmation 映射（read-only，待数据确认）

日期：2026-06-22
HEAD：d517247（R404）；并行 agent 91 未追踪 css-position 未提交（3 轮 in-flight，可能在跑 reftest）
性质：read-only；承接 R404 import-健全性（已证 sound）后的失败簇**预判**（待并行 agent 提交真通过率后用数据 confirm）。零代码变更、零 master.md 编辑（避免踩踏）。

## 背景

R396/R400 对 grid 做 pre-commit 簇映射，后被 R401 数据 confirm（grid 真通过率 62.5%，失败落 baseline-export / definite-size / fragmentation 已知簇，0 新单会话 lever）。本轮对 css-position 应用同方法——但因 position 子领域与 grid 不同（abspos CB / Phase A inline / table-internal / dynamic），簇映射是**新内容**非重复。

## css-position 导入子领域分布（86 test/ref）

- **position-relative-table-***（~24 案最大簇）：table 内部（tr/thead/tfoot/td/tbody）× top/left × 有/无 absolute-child——**table-internal 作 abspos containing block**。
- position-absolute-dynamic-relayout/static-position/overflow（~14 案）：**JS 驱动动态**（img-src-changes / dynamic-display 类）。
- position-absolute-center（6 案）：abspos 居中 + sizing。
- position-absolute-in-inline（6 案）：abspos 在 inline 容器内。
- hypothetical-dynamic-change（2 案）：abspos 静态位置 hypothetical box。

## 2 案 read-only 抽样 + 簇映射

### 1. position-absolute-in-inline-003.html — abspos in inline CB
- 结构：`<span position:relative>`（内含 "AA" 文本）内嵌 `<div.abs position:absolute top:-60px>`，span 在 div 的 inline 流中。
- 映射 = **Phase A / R336 abspos-text 双路径**（paint-IFC 把 abspos 后代 inline 文本当正常流绘制 + abspos CB 解析）——R334/R335/R336 三角度已收敛至此，单会话 ruled out。
- **非单会话 clean win**（多会话 Phase A）。

### 2. position-absolute-center-001.html — abspos sizing × flex × justify-content:center
- 引 csswg-drafts #4659 + css-align-3 §abspos-sizing；abspos 在 inner-flex(justify-content:center) 内，测 abspos available-space / shrink-to-fit + flex definite-size 交互。
- 映射 = **taffy-definite-size / abspos shrink-to-fit 簇**（R363 fixed-table-in-flex / R98 abspos-viewport / R180 shrink-to-fit 谱系）——taffy 0.7 grid/flex definite-size 不跨层传递，R304 DEFER。
- **非单会话 clean win**（taffy-blocked）。

## 预判（待数据 confirm）

css-position 失败大概率落 **4 个已知 ruled-out 架构簇**：
1. **Phase A abspos-text / inline-CB**（in-inline 簇 + 部分 hypothetical）—— R336 谱系。
2. **taffy-definite-size / abspos shrink-to-fit**（center 簇 + 部分 static-position）—— R363/R98/R180/R304 谱系。
3. **table-internal 作 abspos CB**（position-relative-table-* 最大簇）—— table + abspos CB 交互，已知硬架构。
4. **JS 驱动动态 relayout**（dynamic-* 簇）—— 需 JS 执行，独立轴（非渲染 bug，是 JS/DOM 时序）。

**预测**：0 新单会话 lever（与 grid R396/R400 一致）；position 真通过率或显著低于子集 ~100%（如 grid 62.5%），主要被上述架构簇拖低。

## 待 confirm（数据到达后）

- 并行 agent 提交 css-position 全量 + 跑 reftest 后，用真通过率 + 逐案 REFTEST_DUMP confirm 上述映射。
- 重点核查：table-internal-abspos-CB 簇是否如预测是主失败源（这是 position 独有的最大簇，grid 无对应）。
- 若发现某案**不**落上述 4 簇 → 可能是新 lever，深查。

## 代码变更

零（read-only；未触碰并行 agent 的 .html/.rs；未编辑 master.md）。
