R461 — css-text 全量导入 COMPLETE（DC-14 完整性已验）+ 聚类映射（第 9 目录备基线，oracle 预测）
日期: 2026-06-21（doc-maintenance rollover 轮，read-only 文件统计 + 实现核查，零代码变更）
承接: R460（token gate 移除，css-text 完全 unblocked）。并行 agent 已把 css-text 全量拉到
      wpt-data（2994 文件，untracked 未提交——本轮只读核验完整性，不 commit 非 own）。

================================================================
1. 导入完整性（DC-14 denominator integrity）
================================================================
wpt-data/css/css-text/ 实测:
  - 总文件 **2994** = 与 10db810 commit msg「css-text = 2994 blobs」精确一致 → **完整导入，非截断/部分**。
  - test-ish 文件（.html/.xht 排除 -ref/reference/notref）**1914** = 与 commit msg「1914 test files」一致。
  - 顶层 28 子目录（white-space/i18n/text-transform/line-breaking/word-break/text-align/line-break/
    hyphens/parsing/overflow-wrap/shaping/letter-spacing/text-autospace/text-spacing-trim/text-wrap-style/
    text-indent/text-justify/tab-size/hanging-punctuation/word-space-transform/word-spacing/text-fit/
    text-group-align/writing-system/text-encoding/boundary-shaping/animations/crashtests/support/i18n）。
⇒ DC-14 分母完整性满足（truncation guard 10db810 已防静默部分；实测对账 2994/1914 双 match）。
   注意：1914 test-ish 含 crashtests/parsing/testharness 非reftest；权威对（link rel=match）<1914，
   精确对数待 runner 跑出（R425/R427/R457 方法：runner 运行数 ≈ 权威对）。

================================================================
2. 聚类分布（top subdirs by file count）
================================================================
  595 white-space/    539 i18n/          219 text-transform/   213 line-breaking/
  181 word-break/     170 text-align/    127 line-break/       119 hyphens/
   87 parsing/         75 overflow-wrap/  70 shaping/           67 letter-spacing/
   60 text-autospace/  57 word-space-transform/  48 text-fit/   ...
主导簇：white-space + i18n = 1134/2994 = 38%（i18n 多为 BiDi/混合脚本草稿，潜在重拖累）。

================================================================
3. 实现状态锚定（read-only grep，复核 R458）
================================================================
核心换行属性已注册+解析+apply（R458 已核）：white-space✅ / word-break✅（break-all·keep-all·
break-word·normal 全值）/ overflow-wrap✅ / hyphens✅（default None）/ tab-size✅（default 8）。
缺口预计（R458）：layout 消费精度 + 较新属性（text-autospace / text-spacing-trim /
text-wrap-style / text-justify / line-break 禁则）。另 i18n BiDi/复杂脚本 → rustybuzz 生产未接
（R330/R331 text.rs:405 仍逐字符 glyph_id），与 fonts 主拖累同谱系。

================================================================
4. oracle 预测（chr<1%，待全量复测）
================================================================
- 上拉项：line-breaking(213) / word-break(181) / text-align(170) / white-space(595) =
  ZW M5「CJK 换行 + justify」已实现区，self 应较高（区别 text-decor emphasis 全缺失）。
- 下拉项：i18n(539, BiDi/复杂脚本，rustybuzz 生产未接) / hyphens(119, 连字符词典) /
  text-autospace(60) + text-spacing-trim/text-wrap-style(较新属性)。
- 预测 **chr<1% ~30-38%**（straddle text-decor 28.9% / fonts 34.8%；line-breaking/word-break/
  text-align/white-space 托底 vs i18n/hyphens/autospace 下拉）。i18n 539 是最大不确定项
  （若 BiDi 大面积发散可触 28% 下沿；若 line-breaking 簇强势可冲 38%）。
- 9-dir 聚合影响：css-text 分母（~1900 test，权威对估数百-千级）大于 writing-modes 786，
  若落 ~34% 则把 8-dir 26.8% 上拉（区别 writing-modes 5.6% 下拽）。

================================================================
5. 结论 + 下一步
================================================================
- css-text 导入 **COMPLETE + 完整性已验**（DC-14 OK）；R460 unblock 链（R458 阻塞→R459 递归→
  R460 gate 移除）产出真完整全量分母。
- **0 新单会话 lever**（缺口在 layout 消费精度 + 较新属性 + i18n BiDi=rustybuzz 生产接线 Phase A）。
- 下一步：并行 agent 跑 css-text self-source reftest + chromium-Oracle 捕获 → 全量折回（R425/R427/
  R457 方法）；本轮 pre-confirm 为该折回备基线。输出 CONTINUE。
