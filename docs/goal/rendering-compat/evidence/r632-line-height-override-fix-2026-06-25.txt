R632 — compute_final 存 font/line-height overrides 修复 paint Path B 行间距（LANDED）
日期: 2026-06-25
commit: 0911a2ac
性质: 代码修复 + A/B（reftest net +5 / product-smoke mixed / 结构正确）
承: /loop「彻底修复字体问题」+ R630（多行 y 分行）+ R631（字体归因证伪，转行盒度量）

================================================================================
根因（确证）
================================================================================

R630 修复 paint Path B 多行 y 分行后，残余 wrap 用例 y 跨度 50px vs 期望 72px
（3行×line-height 24px）。决定性实验：line-height:1.5 与 2.0 产出**完全相同**行位置
（y=[31,50,69]，行间距固定 19px）——**paint Path B 完全忽略 CSS line-height**。

定位：store_font_sizes_from_ifc（inline_finalization.rs:80，存 text_node_line_heights）
的 4 个调用点**全在 remeasure 路径**（engine.rs:1140/1266 + inline_finalization.rs:801/935）。
compute_final_inline_layouts（line 212，wrap div 等 non-pure-Ahem 容器走的路径）**从不调**
→ text_node_line_heights/font_sizes 空 → paint Path B IFC override 全空 → mod.rs:707-712
fallback (16, 16×1.2=19.2)。line_height 固定 19.2，忽略 CSS line-height。

================================================================================
修复（commit 0911a2ac，inline_finalization.rs:490 后 Gate 2 return 之前）
================================================================================

compute_final 在 inline_ctx.layout 后调 store_font_sizes_from_ifc（存 font_size/
line_height/is_ahem/letter_spacing/inline_element_metrics 到 LayoutBox），使 paint Path B
override 命中。即使 Gate 2（!is_pure_ahem）return 不存 inline_layout，override 仍存。

================================================================================
A/B 验证
================================================================================

行间距正确响应 CSS（修复前都固定 19px）：
  line-height:1.5 → 行间距 [24, 24]（期望 24）✓
  line-height:2.0 → 行间距 [32, 32]（期望 32）✓

reftest（net +5，R627 pre-wrap -15 被 R630 吸收反成 +6）：
  css-text:        1399/1576 → 1405/1576  (+6)
  css-text-decor:  244/250   → 245/250    (+1)
  normal-flow:     645/746   → 644/746    (-1)
  generated-content: 205/226 → 204/226    (-1)
  positioning:     415/520   → 415/520    (0)
  net = +5

product-smoke（mixed，结构正确）：
  welcome: 17.27% → 16.16%  (-1.11pp 改善，英文 DejaVu 行高更近 chromium)
  morning: 17.16% → 18.15%  (+0.99pp 退步，中文 NotoSansCJK frag.height 行高 ≠ chromium)

质量门禁：zero-engine 1149 tests pass / clippy clean。

================================================================================
裁决：LANDED（结构修复 + reftest net +5）
================================================================================

line-height 忽略 CSS（固定 fallback 19.2）是 bug，修复正确（响应 CSS）。reftest net +5
（R627 的 -15 被 R630 with_line_y 吸收），welcome 真实改善。

morning 中文退步（+0.99pp）= R374 字体**度量**谱系（非字体选择，R631 已证伪选择）：
line_height override 用 frag.height（ZW NotoSansCJK 字体行高），其 ascent/descent 度量
≠ chromium 字体，分行后每行偏移累积发散。这是字体**度量**问题（独立多会话），非本修复的错
——本修复让 line-height 响应 CSS（符合规范），残余 frag.height 度量分歧是下一步。

================================================================================
纠正 R627 死锁结论
================================================================================

R627（2026-06-25）定论：compute_final 存 font_size override 致 paint IFC char-width 行断
变化 → pre-wrap net -15 → 死锁，须 Phase A 统一。
R632 纠正：R630（paint Path B 用 all_fragments_with_line_y 多行分行）吸收了该 -15——
post-R630 重试，css-text 反成 +6（pre-wrap 不再回归）。R627 死锁结论过期（同 R246/R630 谱系：
单点 net-negative 结论随基础设施修复过期）。

★ 连续 2 轮 /loop 证明：行盒度量（line-height/baseline/多行 y）是 morning/welcome 17% 真因，
非字体（R631 三证）。R630（多行 y）+ R632（line-height）是行盒度量统一的两步实质进展。
================================================================================
