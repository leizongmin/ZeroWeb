R447 — css-writing-modes 全量前 pre-confirm 聚类映射（只读，为第 8 目录备基线）
日期: 2026-06-21  轮次: R447（doc-maintenance，承 R446 确认 writing-modes 仅 21 文件偏斜子集）
代码零变更（grep + 已知结论推理；discover 脚本 dry-run 网络超时不可用，改用 R409/R410 推理法）。

导入现状
--------
- 当前 wpt-data css-writing-modes 仅 **21 文件**（远非全量；header 标上游 ~855 含非 reftest）。
- 21 文件**严重偏斜难簇**：~50% abs-pos-*（abs-pos-border-offset-001/002/003、abs-pos-replaced-vrl-001、
  abs-pos-vlr-border/padding-001）+ bidi-dynamic-iframe + direction-propagation +
  flexbox_align-items-stretch-writing-modes。非代表性，不可据子集定全量 oracle。

实现状态核查（read-only grep，本轮新查）
----------------------------------------
- **base axis-swap ✅**：writing-mode 广泛接入 layout-engine（r109.rs / engine.rs / tree.rs /
  float_positioning.rs / inline_finalization.rs）；R114 实测 50 子集 49/59 self-pass（83%）。
- **logical properties ✅**：margin-block / inset-block 等已在 apply_advanced.rs 映射。
- **BiDi ⚠️ 部分**：unicode-bidi 在 inline/text_metrics.rs 接入（M5 谱系），但 rustybuzz
  TextShaper 生产未接（R330/R331 paint-only 净负向）；BiDi reorder 部分工作。
- **vertical-rl clearance ❌ 死路**：float_positioning.rs 仍在，R114/R164 共 4 轮证伪
  （vrl-004/008 同源 REF 怪异，正确 CSS vertical-rl 反使 4/4 变差）。
- **abs-pos-vrl/vlr ❌ 穷尽**：R237/R238/R334/R335/R336 三角度收敛 Layout/Paint IFC 双路径（Phase A）。
- **writing-mode+flex/grid ❌ 架构**：R109 axis-swap 不与 flex main-axis 复合。
- **【新发现】text-combine-upright ❌ 完全未实现**（grep 全空）—— writing-modes 专属缺口，
  CJK 竖排文本中的横排片段（§9.x），此前综合裁决表未单独标记。整簇 case 预期失败。

聚类 → 预测（推理，待全量+oracle 复核）
----------------------------------------
全量 ~855（含非 reftest，权威 link-rel-match 对应更少）粗略聚类权重与每簇 oracle 估计：
  - 竖排/方向基础 + logical properties（~40%）：base axis-swap 工作 → ~55-65% oracle
  - bidi-*（~15%）：BiDi 部分接入 → ~40-50%
  - text-combine-*（~6%，新发现未实现）→ ~0-10%
  - abs-pos-vrl/vlr（~6%，穷尽）→ ~10-20%
  - clearance/floats-vertical（~6%，死路）→ ~10-20%
  - writing-mode+flex/grid（~6%，架构）→ ~15-25%
  - 余 direction/unicode-basic（~21%）→ ~55-65%
加权 → **预测 chromium-Oracle chr<1% ~33-42%**（居中 ~37%，近 position 37.9% / fonts 34.8%），
**text-combine 未实现是 writing-modes 专属下拉项**（其他目录无此簇）。

裁决
----
- **0 新单会话 lever**：text-combine 需新特性实现（多会话）；clearance/abs-pos/wm+flex 均
  已多轮 ruled out；BiDi rustybuzz 生产接线 = Phase A 4 子任务（R331/R332）。
- **对 7→8 目录聚合影响**：若 writing-modes 落 ~37%，8 目录聚合 oracle 从 7-dir 36.0% 基本持平
  （writing-modes 与平均接近，不像 multicol 23.5% 那样下拉）。
- **目录优先级提示**：writing-modes 是迄今最 rehash 的目录（R109/R114/R125/R164/R237/R334-R336
  全在此），已知结构性结论多；并行 agent 下一目录选 writing-modes vs css-text vs CSS2 仍开放，
  writing-modes 的 text-combine 新缺口或可作为后续多会话特性 lead。

方法学教训
----------
discover-reftests-authoritative.py dry-run 对大目录不可用（逐文件 raw fetch，~855 文件超时）；
pre-confirm 须依赖并行 agent in-flight 数据（如 R430 multicol 用 parallel agent 的 331 对）或
推理法（本轮）。待并行 agent 启动 writing-modes 全量导入后再用 in-flight 数据 refine 本预测。
