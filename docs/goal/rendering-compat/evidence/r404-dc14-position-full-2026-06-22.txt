# R404 — DC-14 Phase 2：css-position 扩到全量 + 可复用 oracle 捕获工具

**日期**：2026-06-22
**承接**：R401（grid 全量样板）+ R403（master.md 自洽）。本轮扩 css-position 全量并固化可复用
oracle 捕获工具，第 2 个完整 DC-14 目录。

## css-position 全量结果

- 权威发现：**103 个 reftest 对**（vs 子集 24，vs 上游 149 test 文件中 46 非 reftest）。
- 导入 103 对，runner 运行 95（8 缺 ref）。
- **self-source 真通过率：61/95 (64.2%)**（vs 24-子集 ~90%+）。
- **chromium-Oracle 真一致：36/95 (37.9%)** chr<1%（捕获 105 oracle）。

## 已完成 DC-14 全量目录汇总（2/9）

| 目录 | self-source (full) | chromium-Oracle chr<1% (full) | 子集 self 高估 |
|------|-------------------|------------------------------|---------------|
| css-grid | 30/48 = 62.5% | 19/48 = 39.6% | ~100%→62.5% (−37pp) |
| css-position | 61/95 = 64.2% | 36/95 = 37.9% | ~90%+→64.2% (−26pp) |

**一致信号**：两目录全量后 chromium-Oracle 真一致均 ~38-40%（vs 子集口径 42.1% 广义）。
self-source 全量均 ~62-64%（vs 子集 88%+）。子集系统性高估，全量真通过率显著更低。

## 新增可复用工具：`capture-oracle-per-dir.mjs`

捕获指定目录**全部** test 文件的 chromium Oracle 截图（区别于 `chromium-oracle-shot.mjs`
的 per-dir 抽样）。DC-14 去子集化的全量 oracle 捕获用。

```
node capture-oracle-per-dir.mjs --category css/css-position [--category <dir2> ...]
```

## 可复用流水线（DC-14 per-dir，2 目录已验证）

1. `discover-reftests-authoritative.py --category <dir> --import`（权威发现+导入）
2. reftest-upstream `<dir>`（self-source 真通过率）
3. `capture-oracle-per-dir.mjs --category <dir>`（全量 oracle）
4. REFTEST_DUMP + chromium-Oracle 比对（真一致率）

## 下一步

css-tables (203) 全量；然后中目录 text-decor (356) / fonts (391)；大目录 flexbox (586) /
multicol (537) / writing-modes (855)；最后 CSS2。

## 本轮变更

- 新增 `tests/wpt-runner/scripts/capture-oracle-per-dir.mjs`（全量 oracle 捕获，~75 行）。
- 导入 css-position 103 对 + 共享 ref。
- 零 Rust 变更。
