R460 — css-text 全量导入 + DC-14 chromium-Oracle 真通过率（9/9 目录最后 1 个）
日期: 2026-06-21
本轮交付: (1) discover 脚本 git/trees recursive=1 重构（commit 10db810，绕开 60/hr 共享 IP 限速）；
         (2) capture-oracle-per-dir.mjs 子目录递归修复（commit 本轮，同 R458 谱系的嵌套目录 bug）；
         (3) css-text 全量导入 + self-source / chromium-Oracle / cross-validate 全量测量。

================================================================
0. 导入（git sparse-checkout，API-free）
================================================================
css-text 是 9 目录中唯一「test 全在子目录」的文字类目录（30 子目录：white-space/line-break/
word-break/text-align/hyphens/i18n/...）。前 8 目录用 discover --import（顶层 raw 抓取），但
discover 的 gh_api 目录列表受 60/hr 共享 IP 限速阻塞（R458 实测中途超时）。

本轮把 discover 的 collect_test_paths 从「逐子目录 gh_api(contents) 递归」改为「单次
git/trees/{sha}?recursive=1」（commit 10db810）。实测 css-text 子树 2994 blob / 1914 test 文件，
**不截断**，2 次 API 调用（contents(parent) 取 tree SHA + recursive trees）替代原 30-75 次。
但本轮导入本身用 git sparse-checkout（API 完全免限）一次性拉全 css-text + fonts：
  git clone --depth 1 --filter=blob:none --sparse ... wpt.git
  git sparse-checkout set css/css-text fonts
  → 2994 文件入 wpt-data/css/css-text/（含 support/ 共享资源）。

reftest 分母（link rel=match）= **1572**（runner 运行 1572；test-like 文件 1914 中 342 非 reftest）。

================================================================
1. self-source（runner 文字类容差）
================================================================
- **1402/1572 = 89.2%**（Duration 1229s）
- @0.5% strict 待算（runner strict 口径），loose 文字容差下 89.2% 高于 multicol 43.2% / flexbox 52.4%
  （布局类），与 text-decor 99.2% / fonts 99.3%（文字容差抬升）同口径但偏低——css-text 换行/对齐/
  hanging-punctuation 精度缺口多于纯字体/装饰。
- 4 例 missing-ref（hanging-whitespace-001..004 引用 /css/reference/ 共享 ref 未导入）→ 假阴性，
  0.25% 分母噪声，跨目录共性（共享 css/reference/ 未导入），非 css-text 专属。

================================================================
2. chromium-Oracle cross-validate（DC-14 真指标）
================================================================
对账集 = 1404（1572 dump test ∩ 1912 oracle，单页 reftest；168 差额 = 多页 test 带 #0/#1 分页
后缀，oracle 单截图无法配对——跨目录共性，0 size-mismatch）。

z_vs_chr 分布（DC-14 决定性证据）:
  [0,1)%   :  259 (18.4%)  ← 像素精确（DC-14 真一致）
  [1,5)%   :  927 (66.0%)  ← 近似正确但超 1%（near-miss 带）
  [5,20)%  :  214 (15.2%)
  [20,50)% :    4 (0.3%)
  >50%     :    0
⇒ chr<1% = **259/1404 = 18.4%**；chr<5% broad = 1186/1404 = 84.5%。
⇒ self-pass&chr<1%（严格真通过）= 224/1404 = 16.0%。
⇒ 污染（self-pass 但 chr>=1%）= 470/698 self-pass = 67%。

**css-text 非「大多坏」**：84.5% 在 5% 内 = 近似正确，但仅 18.4% 像素精确——gap 均匀为
**文本度量定位**（letter-spacing/word-spacing/line-height/hanging-punctuation/text-align justify
的亚像素精度），Phase A font-metric / line-box strut_ascent 谱系（同 writing-modes R459 /
text-decor R417），非结构死锁。

================================================================
3. 预测对照（R458 过乐观，同 writing-modes/text-decor 谱系）
================================================================
R458 预测「line-breaking 核心 ZW 已有 CJK 换行故 oracle **或高于 text-decor 28.9%**」。
实测 18.4% **低于** text-decor 28.9%（差 -10.5pp）。失败归因同 R459 writing-modes：
水平文本 self-source 89% 不能外推到「像素精确一致」——near-miss 带（66% 在 1-5%）是字体/度量
系统性偏移，self-source loose 容差（~5%）吸收了它但 chr<1% 严苛阈值暴露。

================================================================
4. 9 目录聚合 + 排名
================================================================
chr<1% oracle 排名（低→高）:
  writing-modes 5.6% < **css-text 18.4%** < multicol 23.5% < text-decor 28.9% < fonts 34.8%
  < position 37.9% < grid 39.6% < tables 43.8% < flexbox 50.6%
css-text = 9 目录第 2 低（仅高于 writing-modes 5.6%）。

- 8-dir aggregate: 673/2512 = 26.8%（R457/R459）
- +css-text 259/1404 ⇒ **9-dir = 932/3916 = 23.8%**（−3.0pp，css-text 18.4% 下拽）
- 文字类 3 目录（writing-modes 5.6 + text-decor 28.9 + fonts 34.8 + css-text 18.4）均低于
  布局类 band（37.9-50.6），证实「文字类 oracle 系统性更低」假设（文字度量噪声）。

================================================================
5. 杠杆结论（0 单会话 lever）
================================================================
**0 新单会话 lever**：css-text near-miss 带（66% 在 1-5%）= 文本度量定位精度，Phase A
font-metric / line-box 多会话谱系，非可单点翻转的 clean-win。主导子簇：
- white-space 354 / i18n 158 / text-transform 106 / word-break 101 / line-break 75 / text-align 73
  / line-breaking 64 / hyphens 55 / overflow-wrap 49 / text-autospace 32
均文字换行/对齐/断字精度（rustybuzz 生产未接 R331 + paint/layout IFC 不同源 Phase A）。

**9/9 目录全量 DC-14 Phase 2 完成**：grid/position/tables/flexbox/multicol/text-decor/fonts/
writing-modes/css-text 全部上游全量 + chromium-Oracle 测量。下一步前向：
- 渲染架构轨道 2-4（Phase A IFC 统一 / multicol fragmentation / baseline-export）需多会话承诺，
  其全局 ROI 现有 9 目录全量数据可校准（轨道 1 DC-14 去子集化已完成）。
- CSS2（~5000-7000，嵌套 43 子目录）是唯一未导入的范围内目录，discover/capture 脚本现已
  subdir-safe（本轮 + 10db810 + 1568cb0），可下一轮导入（git sparse-checkout API-free）。

================================================================
附：本轮工具链修复（2 处嵌套目录 bug，同 R458 谱系）
================================================================
- discover-reftests-authoritative.py: collect_test_paths 改 git/trees recursive=1（10db810）。
  验证：css-text 1914 test 路径正确（含子目录），writing-modes 等扁平目录输出不变。
- capture-oracle-per-dir.mjs: readdirSync（顶层）→ collectTests（递归）。原 bug 致 css-text 仅
  捕获 8 张顶层 oracle（1914 test 散落子目录全漏）。验证：css-text 1912 ok/2 fail，css-flexbox
  扁平目录 506 不变。两修复仅影响嵌套目录（css-text/CSS2），扁平 8 目录零变化。
