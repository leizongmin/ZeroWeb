R246 — 阶段状态收口 + pre-wrap 修复 reftest bonus footprint（read-only，无代码提交）
================================================================================================
日期：2026-06-18
角色：rendering-compat 调研与文档维护（本角色不改源码）
触发：R236-R245 调研链已覆盖主要可规整候选；morning-work 根因闭合（R245）；本轮收口 + 新发现。

────────────────────────────────────────────────────────────────────
1. 新发现：R245 pre-wrap 修复 reftest bonus footprint 极小（值集中在 DC-13）
────────────────────────────────────────────────────────────────────
R245 morning-work 根因 = break_into_lines 无条件 collapse_whitespace 破坏 pre/pre-wrap 换行符。
本轮扫描该修复对**上游 reftest 的 bonus 影响**（并行 agent 提交后会顺带翻转哪些用例）：
  - 全 wpt-data 用 `white-space: pre/pre-wrap/break-spaces` 的文件仅 **4 个**；css/css-text 目录 **0 个**。
⇒ **pre-wrap 修复 reftest bonus ≈ 0**——上游 reftest 几乎不用 pre/pre-wrap（white-space 测试多以
  `white-space: pre` 单行或 normal 为主，少有多行 \n pre-wrap reftest）。**该修复价值集中在 DC-13
  morning-work 产品 smoke（67% 压缩主因）**，非 reftest-multiplier。
- 含义：实现轮验证该修复应聚焦 product-smoke morning-work 区域 diff（R245 配方），**勿期待 reftest
  438/490 显著上升**（同源 test==ref 对称 + bonus 文件仅 4）。修后 morning-work 预期从 67% 降，剩余残差
  来自独立问题（font-weight R229、img 固有尺寸 R233、.item-tag 已 R228b 修）。

────────────────────────────────────────────────────────────────────
2. R236-R245 调研链可执行状态收口
────────────────────────────────────────────────────────────────────
**实现就绪（read-only spec 已交付，待实现轮）**：
  - R236 multicol baseline-export（8 例 1.1% POLLUTED，结构性最 tractable；multicol.rs 写列派生 taffy_baseline）
  - R244 DC-9 GPU alpha groundwork（GPU-R228b-analog：color_to_f32 丢 alpha + 顶点 vec3f + fragment 硬填 1.0）
  - R238 WM-1 abspos-vertical（14 例，§7.1 direction 分支缺失，rtl 5.03% 最清晰信号）

**阻塞于并行 agent 提交（待其提交后我可验证）**：
  - R242/R243/R245 morning-work 压缩（根因=pre-wrap 换行符，并行 agent WIP 已 4 轮未提交）。
    验证配方就绪（product-smoke 区域 diff + LAYOUT_DUMP 12 pre content_height + reftest 不回归）。
    ⚠️ 本轮仍不跑 make reftest：并行 agent WIP 未提交，构建含其 IFC 改动会污染测量。

**deferred 结构多轮（非 contained，R241 plateau 确证）**：
  - DC-14 剩余：writing-mode（R237/R238 + R114 clearance 四轮证伪）、multicol-breaking（R113/R131 列感知
    IFC 碎片化）、block-in-inline（R109）、table-grid-dynamic-003（JS relayout）、feature-gap
    #2/#6/#10/#14（::backdrop + 原生表单控件）、vertical table collapsed border #15/17/18。
  - DC-9 ping-pong（R244 alpha groundwork 之后的 transform/filter/blend，第 2 纹理+post-process）。

────────────────────────────────────────────────────────────────────
3. 优先级队列（read-only 建议，给实现 agent）
────────────────────────────────────────────────────────────────────
  P0  morning-work pre-wrap 修复（并行 agent 进行中）→ 提交后验证 DC-13 morning-work 67% 下降。
  P1  R244 DC-9 GPU alpha groundwork（contained，zero-regression，修 GPU 全局不透明 + 为 blend 铺路）。
  P2  R236 multicol baseline-export（contained 结构性，8 例系统性 1.1%，multicol.rs 单点）。
  P3  R238 WM-1 abspos-vertical direction 分支（14 例，contained）。
  长  DC-9 ping-pong / DC-14 结构多轮聚类（writing-mode/multicol-breaking/block-in-inline）。

────────────────────────────────────────────────────────────────────
4. 当前无 open 阻塞
────────────────────────────────────────────────────────────────────
- master.md 无碰撞（并行 agent 改 painter/text.rs + inline/mod.rs，非 master.md）。
- 调研 agent 全程未碰源码；本轮 read-only，未跑 reftest（WIP 污染）。
- 调研侧主要候选已 spec 完；下一进展依赖并行 agent 提交 morning-work 修复（解锁验证）或实现轮起手
  P1/P2/P3 之一。若并行 agent 持续不提交，调研侧可继续独立 read-only 线（DC-9 ping-pong 接线点深挖 /
  native form-control 评估 / DC-6 quirks mode reftest 覆盖核查）。

【交付】仅本 evidence + master.md R246 段；仅提交我的文档文件。
