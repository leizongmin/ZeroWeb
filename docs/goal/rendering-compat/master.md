# 渲染兼容性目标 — 运行时控制面板

> **▶ 当前裁决（2026-07-29 用户两次指令：① 永不停 / 待决策记账 / 继续推进；② 主做轻量修复、调文档方向防跑偏）**
>
> - **主线 = 轻量修复**（用户第 2 次指令「我们主要做轻量修复」）：沿用 `rendering-compat.md` 的 `2026-07-28 方向裁决` 允许范围——(1) 有 driving test、低风险、A/B 零回归的 CSS2/parser/selector clean lever；(2) 产品/legacy smoke 可见稳定性修复；(3) **文档与代码不一致的纠偏**（本 goal 文档滞后严重，R2202 实证 `loader.rs`〔现 render-foundation/src/font/〕/ `text_metrics.rs` 等多处注释过时误导——纠偏本身是高价值轻量活）。每修一个跑 `make test` + 相关 smoke + 必要 dir oracle，net≥0 即 land。
> - **永不停原则**（用户第 1 次指令）：轻量修复持续做；遇需用户拍板事项记「待用户决策」清单并跳过，立刻转下一个轻量修复，**不因单项阻塞停整个 goal**。
> - **深结构护栏·防跑偏**（用户第 2 次指令；纠正本 session 早些「一律放行深结构直接开工」的措辞——那会让后续 agent 跑偏去啃深改）：以下**深结构多会话方向不自主开工，记待决策清单等用户点名**——font-metric 生产激活+A/B（R2202 dormant 基础设施 webview+renderer 已落地、env-gated 未激活；2026-08-01 用户授权 A/B 已完成 = **net 负保持 dormant**，见 R2393 evidence）、vertical-mode native R1043、taffy replaced-element border-box R2174、Phase A slice-3 IFC 深构造、font-stack C-dep rebuild。clean lever 九重穷尽（R2183-R2190）后若轻量修复暂无新候选，做文档纠偏 + plateau-guard，**不要**借机跳入深结构。
> - **A/B 门禁照旧**：每切片守 kill-switch + 三态 A/B（self-source reftest + product/legacy smoke + 相关 dir oracle）net≥0 才 land；net 负 → 回退、记 evidence、转下一切片。**「net 负」是转下一切片的信号，不是停下等用户的理由。**
> - **真正需用户拍板才记「待用户决策」并跳过**（清单见 `rendering-compat.md` 顶部）：不兼容/闭源许可证、破坏性 git/文件操作、改变 Mission/Done/范围边界、超大磁盘网络下载工具审批无法覆盖——**加上**上述深结构方向。

> **▶ 主线切换裁决（2026-08-04 用户决策）**：工作切回父目标 zero-web（恢复 P1 DOM/JS Bridge 原生化 = P1a 事件循环补全 + fetch/MutationObserver 真实化，见 zero-web/master.md「下一步优先级」），**本 goal 降频守成**：① 低频 plateau-guard——父目标侧有 .rs 变更或每 ~10 轮跑一次 `make test` triple-guard，守护 13192/0/74 基线；② 待用户决策清单原样保留（R1043/R2174/Phase A IFC/font-stack/srcset 等深结构等点名，点名即切回）；③ 文档纠偏停止主动排程（design-doc 引用核验近收官），仅 .rs drift 时顺手修；④ 不推翻 2026-07-29「永不停」——守成 + 待命即执行形态。

> **📍 当前 rally 进度态（R2872·2026-08-07）= 【主线切换裁决】rendering-compat → 降频守成（plateau-guard held baseline 全绿）；clean-lever surface **9+1 vein 全 exhaust**〔**2026-08-04 用户裁决**「工作切回父目标 zero-web（P1 DOM/JS Bridge 原生化），渲染兼容性先缓一缓」→ 本 goal 降频守成：低频 plateau-guard（每 ~10 轮 / 父目标 .rs 变更时）+ 待用户决策清单原样保留 + 文档纠偏停止主动排程；活跃主线转 zero-web。第 11 vein「initial-value VALUE correctness」**R2637 开 + R2638 全扫收官 definitively 关闭**：全 property `PropertyRegistry::initial_value()`↔`ComputedStyle::default()` 比对，drift = box 4 维（width/height/min-width/min-height，R2637）+ column-gap（R2638）共 5 处全修（`Px(0.0)`→`Auto`）+ 2 守卫测试；`make test` **13193/0/74** net +1 零回归。自主 lever surface 经 9 vein clean-lever + 第 10 候选轴退役 + 第 11 initial-value vein 关闭 = definitively 穷尽；rendering-compat 无新自主推进面，唯 plateau-guard 守成 + 用户点名深结构（Phase A IFC line-box-metric 最高 value）切回；详见 §最近轮次摘要 R2637/R2638〕（10th vein initial-value + css-position + css-tables + center-002 + function-resolution + percentage-basis + @media-eval 七轴零产出/既实现，候选轴 R2602 全退役）——第 9 vein（inheritance-flag correctness）R2597 出 1 lever（text-emphasis-style/position）后 R2598 系统差分核验 exhaust（known_properties(215)−is_inherited(42) 仅 image-rendering spec-继承但 no-op indicator defer，余全 correct/未实现）·自主推进面 = plateau-guard（R2604 + R2613 + R2625 + R2635 会话边界 triple-guard 全绿，待新 .rs drift）+ 文档纠偏（窗口健康 16 轮〔R2636 vertical-mode-table-rl-transpose 13 引用核验（3 行号 + 1 路径 drift + α-4b-1 LANDED 确认，vertical-mode 群收官）；R2634 inline-element-wrap 13 引用核验（7 行号 + 1 路径 drift，4 切片均未实施状态一致）；R2633 vertical-mode-text-positioning 9 引用核验（6 行号 + 1 路径 drift + 切片 2 Bug B LANDED 状态纠偏）；R2632 font-wall-cdep-scoping 11 引用核验（4 行号 + 2 路径 drift）；R2631 multicol-phase2-spec 11 引用核验（multicol cluster 第 4 doc 全清）；R2630 css-url-image-fetch 12（Part A+B 已实施）；R2629 phase-a-inline-box-model 13（DONE）；R2628 line-clamp 14；R2626 vertical-mode-ifc 16；R2624 phase-a-IFC 40；R2623 pending-clean-levers 28；R2622 font-face 31〕；10th vein 候选轴 R2602 全退役；R2605 phase-a-IFC design v1.5 addendum 同步 R885 dormant→已激活 stale 纠偏；R2606 v1.5 精度纠偏锁定 R1004 ascent_ratio_overrides 为 step-2 purpose-built 注入点；R2607 font_metric_map 激活 drift 探针 css-fonts flat + css-flexbox -2 deep + snapshot 刷新）·会话边界守卫矩阵完整（R2583 make test + R2584 legacy 产品 0 struct FAIL；R2597 make test 13191/0/74；R2604 + R2613 会话边界 plateau-guard triple-guard 全绿复跑）·reftest fresh 度量 css-pseudo 185/261（R2588）+ css-borders 43/83 flat + css-backgrounds 364/710（R2590）·legacy smoke 计数订正（R2589）·本 session 六连 lever R2572-R2577 + R2592 + R2597 既 land**（顶部导航锚；详情见 §最近轮次摘要 R2580-R2598）：R2572-R2577 经 directed probe + Explore-agent fan-out + **exhaustive field 审计（R2576）+ exhaustive variant 审计（R2578）** 四法 land 6 clean levers（counters/::marker/list-style-type:string/border-image-outset/border-image-width/word-break:break-word）；**R2592 第 8 vein（shorthand→longhand 展开完整性）land text-decoration-thickness shorthand 接线；R2597 第 9 vein（inheritance-flag correctness）land text-emphasis-style/position 继承修正（R2598 系统差分核验 9th vein 既穷尽）**。**9 vein 全 audit exhaust**：7 consumed vein（R2578-R2586 零产出）+ 第 8 展开轴 vein（R2592 出 1 lever / R2593 核验 exhaust）+ 第 9 继承标志 vein（R2597 出 1 lever / R2598 系统差分核验 exhaust）。**结论：clean-lever surface 经 9 vein 审计 exhaust**；自主推进面 = ① 低频 plateau-guard（待 .rs drift）+ ② 文档纠偏；**唯一推向 95% = 用户点名授权深结构**（最高 value Phase A IFC line-box-metric 统一；次 = R1043/R2174/font-stack/Phase 2 multicol/individual-transforms/3D-transforms）。深结构可切片性核验仍成立（全项非自主 tractable 或 net-negative 史）。**post-split triple-guard 绿**（R2544/R2545/R2554/R2563/R2572-R2577/R2592/R2597）。

## 最近轮次摘要

> **📍 R2872（2026-08-07）🔍 R2871 假设证伪复核（code-reading）= underline 实绘非裁剪，skip-ink 簇 threshold 失败机制未明（须 render-viz），非 clean lever 再确认**。承接 R2871。R2871 称 skip-ink-003 根因「text-underline-offset:.5em 把 underline 推到 ZW 不绘制/裁剪位置」为**未验假设**——本轮读 `paint/painter/effects.rs:393-401` 确认 underline **实绘**：`paint_decoration_line(line_x, baseline_y + font_size*0.15 + underline_offset_px, ...)` 无早返回无裁剪，offset 正确 resolve（Em→fs）。故 R2871「offset 裁剪/未绘制」假设**证伪**。实测 test vs no-decorate notref 仅 **160px (0.03%) diff** = underline 渲染了但 diff < 1% mismatch threshold 致 fail；为何 chromium 同案 >1% 而 ZW 0.03% **机制未明**（几何不拢：80px 宽 "XXXX" underline 无论厚度难达 1%/4800px，疑测试渲染另有因子如行盒/装饰 span/full-width，**须 render-viz 方可定论**，headless 无 GUI-CDP 复现受限）。upright/vertical 变体更深。**结论加固**：css-text-decor skip-ink 簇 = paint 层 subtle（机制未明 + skip-ink 全未实现须 glyph-ink-box infra），**非 clean lever**，再证 exhaust。**无 .rs land**——held baseline 13484/0/74（零 .rs drift）。**下一步**：守成 + 等用户就 Phase A IFC + font-stack coherence rebuild 授权。

> **📍 R2871（2026-08-07）🔍 fresh css-text-decor 扫描（前未深扫 dir）= text-decoration-skip-ink 全未实现 + skip-ink 簇真根因 = text-underline-offset 定位（paint 层 moderate，非 clean lever）**。承接 R2870。doc「clean-lever exhaust」前未深 triage css-text-decor（98.0% self-source，仅 R2592 触 text-decoration shorthand），fresh `make reftest-upstream FILTER=css-text-decor` = **245/250（98.0%），5 fail**：① `text-decoration-dotted-002` 6.53% match-fail = dotted 装饰精度（paint-deep）；② **3 mismatch-fail = skip-ink-003 / -upright-002 / -vertical-002**（皆「test≈notref，须 >1% diff」）；③ `text-underline-offset-scroll-001` = host-layer scroll（OOS）。**核心发现**：`text-decoration-skip-ink` 在 ZW **全未实现**（css-parser/style-system/render-foundation/engine 零命中），但 **skip-ink-003 真根因非 skip-ink**——读 test+ref+notref：test = `text-underline-offset:.5em` underline，notref = 无装饰；test vs notref 仅 **160px** diff → **ZW 未渲染 offset:.5em 的 underline**（offset 把 underline 推到 ZW 不绘制/裁剪位置）；match-ref（skip-ink:none + offset:.5em）PASS 仅因 test/ref 同样缺可见 offset underline。**根因假设**（未深验机制）：text-underline-offset:.5em 装饰未绘制（line-box 裁剪 / 误定位 / offset 未应用 三选一），paint 层 moderate。upright/vertical 变体涉 vertical-mode 交互更深。**结论**：css-text-decor 残余全 paint 层（dotted 精度 / offset 定位 / skip-ink glyph-clipping）+ host-layer scroll，**非 clean lever**；fresh 角度再证 clean-lever exhaust；skip-ink 全未实现记入「future moderate feature」（待 paint 层 glyph-ink-box infra，非单点切片）。**无 .rs land**——held baseline 13484/0/74（零 .rs drift）。**下一步**：守成 + 等用户就 Phase A IFC + font-stack coherence rebuild 授权（唯一理论 oracle unlock）。

> **📍 R2869（2026-08-07）⚠️ 勘误 R2867/R2868：Skia/raster C-dep 非 font-wall unlock（R1560 已证伪 net-24）；font-wall 真因 = layout/metric coherence（Phase A IFC）+ full font-stack rebuild**。承接 R2868。**自查发现 R2867/R2868 战略结论有误**——二者称「font-wall 唯一 unlock = Skia C-dep」，依据是 `font-wall-cdep-scoping.md §7.4`，但该 §7.4 属 **R1552b（pre-R1560）**，已被**更晚的 R1560 实测决定性证伪**（读 `skia-cdep-rfc.md` 状态行 + `unified-font-stack-design.md` v0.2.2/v0.2.3 勘误）。**R1560 实测**：real **skia-safe 0.80（chromium 实际光栅器）over FreeType hinted A/B** = writing-modes net+1（噪声）/ welcome −0.09pp / **css-text net−24（font-wall 文本簇真回归）**→ **光栅化 coverage 算法 swap 非 font-wall unlock**（FreeType 自 R1068/R1159 default-on 已 +232 对齐 chromium 光栅层）；**font-wall 残余在 layout/metric coherence（Phase A IFC territory）**。**光栅化 C-dep（Skia/FreeType/fontdue）永久移出 lever 清单**（skia-cdep-rfc.md = 「RFC 作废」）。**corrected 真实路径**（unified-font-stack-design v0.2.3）：font-wall unlock 须 **Phase A IFC line-box-metric 统一 + full font-stack coherence rebuild（layout/paint/wrap metric）整体做**——二者皆 deep multi-week、**isolated slice 全 net-negative 证伪**（line-height R1175/R1185/R1636 ×3 + advance R225/R375/R1946/R1950 ×4 + raster R1560 ×1 = 八轮，不可切片蹭须整体）、RFC-ready（v0.2.3）、**implementation 待用户授权**。**EV 上修**（R1947）：font-wall 经描述性 `<p>` 指令文本主导**全 dir**（非仅 text dir/产品 fixture）→ rebuild yield **universal cross-dir**。**勘误结论**：① R2867/R2868「Skia critical-path」**作废**（Skia net-24 ruled out）；② **Phase A IFC 恢复为 font-wall unlock 核心组成**（R1560「font-wall 残余在 layout/metric coherence (Phase A)」，非 R2867 所降格的 secondary）；③ DC-2~5/2026 65% oracle 里程碑 absent **用户授权 Phase A IFC + font-stack coherence rebuild** = unreachable（皆 user-gated multi-week 不可切片）；④ **勿重试清单**追加 real-Skia（R1560 net-24）。**rendering-compat.md font-stack 待决策项已同步纠偏**（去 R2867 误标）。**无 .rs land**——held baseline 13484/0/74（零 .rs drift）。**下一步**：守成 + 等用户就 **Phase A IFC + font-stack coherence rebuild 授权**（唯一理论 oracle unlock，multi-week 不可切片）决策。

> **📍 R2868（2026-08-07）🔒 plateau 诊断封顶：font-wall Rust 侧 quick win 全证伪（scoping §7/§8 实证），唯一 unlock = Skia C-dep（user-blocked）**。⚠️ **【已由 R2869 勘误 — 「Skia 唯一 unlock」结论错误：R1560 real-skia A/B net-24 已证伪 Skia，font-wall 真因 = layout/metric coherence（Phase A IFC）+ full font-stack rebuild，勿据此条结论实施】**承接 R2867。**深读 `font-wall-cdep-scoping.md` §7（R1552b 勘误）+ §8（R1553 实测）**——R2867 推理获实证封顶：font-wall **无 Rust 侧 quick win**，① metric coherence（§4 原荐）**DEAD**：四轮 R1090/R1095/R1160/R1206 全 net-negative（根因 R876：font metric 须 rasterizer-metric + baseline-ratio + paint-v_offset 三方同改，单轴 plumb 必负）；② LoadFlag tuning **穷尽**（R1069 DEFAULT 最优）；③ gamma-correct blend **net-neutral**（R1553 实测 −1；font-wall AA 差 = FreeType coverage vs Skia coverage 位图生成阶段，非 compositing）；④ `RenderMode::Lcd` 子像素**预期负**（显示相关 + chromium 设置未知）。残余 = FreeType（ZW `LoadFlag::DEFAULT` 灰度）vs FreeType+Skia（chromium 自有 AA + LCD + gamma）**算法差**，非 flag 可调。**唯一 unlock = (iv) Skia C-dep**（10-20 人日大 C++ + 跨平台 CI），**user-blocked R2025**（2026-07-16 decision #1 虽授权 font C-dep 调研/小切片，但用户 R2025 在 scoping 结论「Skia 唯一路径」后显式 block）。**封顶结论**：DC-2~5（oracle ≥95%）与 2026 65% oracle 里程碑 **absent Skia unblock = unreachable**——layout 深结构（Phase A IFC/R2174/R1043）提升 self-source/几何 oracle/产品 smoke 但 text-heavy 测 oracle 仍卡 font-wall。**⚠️ 2026 65% 时间线风险**：Skia 即便立即 unblock 亦 10-20 人日 + 跨平台 CI，2026 年内达标 65% oracle 高度紧张（须用户 ASAP 决策）。**勿重试清单（防 re-tread）**：metric coherence / LoadFlag tuning / gamma-correct blend / LCD subpixel — 全 DEAD/穷尽/证伪。**无 .rs land**——held baseline 13484/0/74（零 .rs drift）。**下一步**：守成 + 等用户就 **Skia unblock（唯一 oracle unlock）** 或 layout 深结构点名（secondary）决策。

> **📍 R2867（2026-08-07）🎯 战略澄清：DC-2~5 + 2026 65% oracle 里程碑的 critical-path = font-stack unblock（非 layout 深结构）**。⚠️ **【已由 R2869 勘误 — 「font-stack(Skia) critical-path / Phase A IFC 降为 secondary」结论错误：R1560 real-skia net-24 ruled out Skia，font-wall 残余在 layout/metric coherence = Phase A IFC 核心（非 secondary），勿据此条结论实施】**承接 R2866。**核验 .rs drift**：`git fetch` + `git log cee1bfc8(R2862)..origin/main -- '*.rs'` = **零 .rs commit（local+remote，父目标 zero-web 亦无 .rs 落地）** → held baseline 13484/0/74 必然不变，`make test` 复跑为 ceremony → 跳过。**战略澄清（纠 master 既有「唯一推向 65% = Phase A IFC」的不完整表述）**：DC-2~5 与 Mission 2026 65% 里程碑均以 **chromium Oracle 一致率**为口径（rendering-compat.md §Mission + DC-14），当前 oracle ~47.5%；R2455 确证 oracle 低**真因 = fontdue 光栅化 font-wall**（非布局 bug）——self-source reftest font-wall 相消故 css-text 95.4% 等高，oracle 因 fontdue≠chromium 字体渲染而低。**关键逻辑链**：font-wall **仅 font-stack C-dep rebuild 可解**（user-blocked R2025「踩坑勿推」），非 C-dep 的 font-metric 激活（R2202/R2393）已实测 **net-negative**；**text-heavy 测 oracle-pass 须「layout 正确 AND font 匹配」**，font-wall 致 layout-perfect 的 text 测仍 oracle-fail → **layout 深结构（Phase A IFC/R2174/R1043）提升 self-source + 几何型（无文本）oracle + 产品 smoke，但无法独力把 oracle 推到 65%**。**结论（战略纠偏）**：唯一推向 DC-2~5 ≥95% / 2026 65% oracle 的 **critical-path = 用户 unblock font-stack C-dep**；layout 深结构为 secondary（self-source/几何 oracle/产品 smoke 收益，且解 first-letter/line-clamp 等可见布局 bug）。**待用户决策清单**：font-stack C-dep 从「等点名」**升格为「DC-2~5/65% 里程碑 critical-path，最高杠杆用户动作」**。**无 .rs land**——held baseline 13484/0/74（R2862 守，tree clean，零 .rs drift 已验证）。**下一步**：守成（.rs drift 时 plateau-guard）+ 等用户就 font-stack unblock（critical-path）或 layout 深结构点名（secondary）决策。

> **📍 R2866（2026-08-07）🔍 css-ui box-sizing-007/008/009 簇独立实证复核 = replaced×border-box×intrinsic×padding 深集成（R2174 territory 确证 / 非 clean lever）**。承接 R2865。**独立实证**复核 css-ui box-sizing 簇（R2858 标 R2174 deep）——`make reftest-upstream FILTER=css/css-ui/box-sizing-00`：box-sizing-001/003/005 **PASS 0.00%**（纯 div+bg，证 `box-sizing:border-box` 非替换元素**本身正确**）；box-sizing-007/008/009 **FAIL 41.67%/15.62%/31.25%**（max diff 255），三测**全用 SVG `<img>`**（support/w100_h100.svg 等 6 变体）。**逐层核验 ZW 各 piece**：① SVG 栅格化 = resvg+tiny-skia 全实现（image_cache.rs DC-13）；② `svg_intrinsic_kind`（image_cache.rs:643）6 变体分类全正确（w100_h100→BothAbs / w100_r1-1,h100_r1-1→ComputedIntrinsic / r1-1→RatioOnly / w100,h100→NoRatio）；③ box-sizing 经 converter/mod.rs:95 全传播给 taffy（单测证 BorderBox→BorderBox）；④ `apply_replaced_element_sizing`（tree.rs:366）replaced sizing 路径**高度成熟**（R325/R976/R1363/R1366/R2062/R2172/R2428/R2440/R2441 十+ 轮精修，NoRatio 默认 300×150 正确、both-abs 固有比推导正确）。**结论**：所有 piece 齐备，缺口 = **replaced-element × box-sizing:border-box × intrinsic-size × padding × content-box 栅格映射 的深集成**（R2174 territory 确证，非单点 clean lever）——box-sizing-007 的 20 图含 t_1/t_3 padding 变体，chromium 经完整 CSS §10.3.2/§10.6.2 算法得一政绿方，ZW piece 组合未达一致。**澄清价值**：css-ui box-sizing 簇主因非「box-sizing 未实现」（已证正确），乃 SVG-replaced 深集成，**防后续 agent 误以「box-sizing clean lever」重查此簇**。**无 .rs land**（深集成非单点可切片，触碰全 replaced 路径 high blast radius，盲改易回归 welcome/product 图）——held baseline 13484/0/74（R2862 守，tree clean）。**下一步**：守成 + 用户点名深结构（Phase A IFC line-box-metric 最高 value / R2174 replaced-border-box 深集成〔须完整 §10.3.2/§10.6.2 replaced sizing + content-box 栅格映射多会话〕/ R1043 / font-stack）。

> **📍 R2864（2026-08-07）🔍 revert-layer 深特性独立复核 + 文档自洽纠偏（rendering-compat 守成续 / 新 session 承接 R2863）**。**独立复核** R2863 oracle 标记的 css-cascade `revert-layer`（7-13 案）是否漏判 clean lever——读 `inheritance.rs:99-111` 确认 RevertLayer 现「简化=unset」，且 `compute_style` 入参 `cascaded: HashMap<String,String>` 仅持获胜值串、层信息（`CascadeOrder.layer_index`）已丢；真 layer-aware 须把 per-layer tier 历史 thread 进 compute 点 = cascade 架构深改，**确证非 clean lever**（与 R2863「深 feature」结论一致，独立验证非盲信文档）。**文档自洽纠偏**（治理原则「master/entry 各章节必须自洽，矛盾必先纠」）：rendering-compat.md 关键状态摘要 13193→**13484** + 日期 R2612→R2863；master.md 通过率快照 make-test 13414→**13484**（与同文测试覆盖率节 13484 对齐）；DC-7 前两项 held baseline 13484/0/74 标 [x]（上 session 未提交 WIP 收尾）。**无 .rs land**——held baseline 13484/0/74（R2862 当日 plateau-guard 守，R2863 后纯 doc 零 .rs drift；纯 doc 提交按 run-rules 豁免 cargo 门禁，仅 git diff --check + pre-commit-guard）。**下一步**：守成（.rs drift 时 plateau-guard）+ 用户点名深结构（Phase A IFC line-box-metric 最高 value / font-stack C-dep / R2174 / R1043 / Shadow DOM）；三法 exhaust + 本轮 revert-layer 独立复核 = 自主推进面确为零。

> **📍 R2865（2026-08-07）✅ DC-7 收官 + clippy 权威复跑（rendering-compat 守成续 / 承接 R2864）**。承接 R2864 文档自洽纠偏。**DC-7 最后一项未闭环子项关闭**——`cargo build 零错误 / cargo clippy 零警告` 经 `cargo clippy --workspace --all-targets -- -D warnings` 权威复跑 = **全绿 Finished（3.45s；cee1bfc8〔R2862〕..HEAD 零 .rs drift 故 clippy 态不变，mostly-cached）**；标 [x] + 注 R2865 复跑确认。**DC-7（测试与质量不可退让）现全部子项闭环**：held baseline 13484/0/74、real-website `#[ignore]` 保留、新增渲染修复须 unit test + WPT 资产化、build/clippy 零警告、reftest 报告持久化 + 历史可追溯。**剩余未闭环 DC = DC-2/3/4/5**（CSS2 / Flexbox+Grid / 布局模式 / 文字排版 通过率 ≥95%），全卡深结构（IFC line-box-metric / R2174 replaced-border-box / R1043 vertical-mode / font-stack C-dep），2026-08-04 用户裁决 user-gated 等点名。**无 .rs land**——held baseline 13484/0/74（R2862 守，tree clean）。**下一步**：守成（.rs drift 时跑 plateau-guard `make test`）+ 用户点名任一深结构（最高 value = Phase A IFC line-box-metric 统一）即切回本 goal 主动推进。

> **📍 R2863（2026-08-07）🔍 DC-14 oracle path 双 dir（css-cascade + css-fonts）= 全深结构/深 feature/font-stack（clean-lever exhaust 第三法确认）：承接 R2862。oracle-shots 640 张覆 css-fonts(281)/css-cascade(134)/css-tables(112)/css-grid(49)/css-nesting(45)——**oracle 覆盖 dir 全深/font-wall**，无 CSS2 dir oracle。**css-cascade oracle** top 发散：scope-part(17.97%)/scope-shadow-sharing(10.12%)/revert-layer ×7(2.69%)/layer-media-toggle/@layer —— 全 @scope（未实现）+ revert-layer（真层感知简化≈unset）+ @layer 交互（master.md R2462 既记深 feature）。**css-fonts oracle** top 发散：**font-size-adjust 簇（8 案 size-adjust-03/font-size-adjust-order/-012/-013/-ic-height/-009/-010/-011，9-18%）= 须字体 metric 测量（ex-height）= font-stack C-dep 深**；font-family-name-025/font-feature-resolution/font-weight-bolder = 字体匹配/feature/weight 解析 = font-stack。**结论（第三法）**：cluster-scan（R2854-R2860 十一 dir）+ moderate-fix 双负（R2859/R2861）+ oracle 双 dir（R2863）**三法一致确认 clean-lever surface definitively exhaust**——残余全深结构（IFC/R2174/R1043/line-clamp line-breaking/grid track）/ 深 feature（@scope/revert-layer/@layer/font-size-adjust）/ host-layer（Shadow DOM/caret/scroll）/ font-wall（Ahem/metric）。**无 .rs land**——held baseline 13484/0/74（R2862 plateau-guard 守，tree clean）。**下一步**：守成（.rs drift 时 plateau-guard）+ 用户点名深结构（Phase A IFC 最高 value / font-stack C-dep / R2174 / R1043 / Shadow DOM）；三法 exhaust 后自主推进面确为零，续轮无新 clean-lever 候选。
> **📍 R2862（2026-08-07）🛡️ plateau-guard make test 13484/0/74 持平 + css-nesting triage（rendering-compat 守成确认）：承接 R2861。`make test` 全量复跑 = **13484/0/74** 精确持平 R2857 held baseline——证 R2856-R2861 七轮（含 R2859 grid-margin-trim + R2861 block-ellipsis 两 net-zero 实验，均已 git checkout 干净回退）**零 .rs drift 零回归 artifact**，baseline 稳。附 css-nesting 扫描（最后未扫主 dir）：12/17（基础 nesting 工作），5 fail = `host-nesting-001..005` 全 2.08%（= 100×100 绿方块缺失）需 **Shadow DOM**（`attachShadow`/`:host`，host-layer deep，Web API 表面 defer）——非 clean lever。**结论**：所有主 WPT 目录（css-tables/content/pseudo/conditional/box/logical/sizing/lists/overflow/ui/transforms/nesting）已扫尽，clean-lever surface definitively exhaust（R2859/R2861 双负结果 + 本轮 plateau-guard 确认 baseline 稳）。**下一步**：守成（.rs drift 时跑 plateau-guard）+ 用户点名深结构（Phase A IFC line-box-metric 最高 value / R1043 vertical-mode / R2174 replaced-element border-box / font-stack C-dep / Shadow DOM）。
> **📍 R2861（2026-08-07）🔍 block-ellipsis word-boundary 截断 = net-zero → 回退（rendering-compat 负结果，第二次确认 clean-lever exhaust）：承接 R2860。投 line-clamp ellipsis refinement——ZW 末行 ellipsis 溢出时字符级 mid-word 截断，spec 要「最后一个能容纳 ellipsis 的 break opportunity（空格）」处截。**实现**：text.rs line-clamp ellipsis else-branch（溢出路径）改 word-boundary cut（找 cut_x 前最后空格，截空格及之后，ellipsis 置空格前文本末尾；无空格回退字符级）。**安全分析**：else-branch 仅末行文本+ellipsis 溢出时触发，passing line-clamp-001-004（短行）走 fits-after-text 分支不进 else → 改动对它们 dormant。**验证**：css-overflow **net-zero**（274/518，244 fail 持平，**零 flip 零回归**）——block-ellipsis-001 等**未 flip**。**根因**：block-ellipsis diff 来自 line-clamp **line-breaking**（ZW 与 chromium 第 N 行容纳的词不同），非 ellipsis 截断位置；word-cut 改 ellipsis 位置不触及 line-breaking → 无效。**回退**：text.rs 还原字符级 cut（line 1672）。**无 .rs land**——held baseline 13484/0/74（R2857 make test 守，tree clean）。**结论（R2859+R2861 双负结果）**：clean-lever surface **definitively exhaust 确认**——line-clamp ellipsis / grid margin-trim 等 moderate 尝试均 net-zero，根因全深结构（IFC line-breaking / replaced-element border-box / grid track sizing）。**下一步**：守成（plateau-guard .rs drift 时）+ 用户点名深结构（Phase A IFC line-box-metric 最高 value，亦解 line-clamp line-breaking + first-letter/first-line；次 R1043/R2174/font-stack）。
> **📍 R2860（2026-08-07）🔍 css-overflow line-clamp 簇 triage = slice-3/interaction territory（clean-lever definitively exhaust 收官）：承接 R2859。css-overflow 274/518（52.9%，244 fail）——**line-clamp 子目录独占 131 fail**（css-overflow 最大簇）。**现状核验**：line-clamp slice 1+2（IFC cap `inline/mod.rs:640` + ellipsis `text.rs:1605-1680`，kill-switch `ZW_LINE_CLAMP` **default-on 确认**）**已 LANDED 且基本工作**——line-clamp-001/002/003/004 + webkit-line-clamp-003/004/005 均 **0.00% PASS**。**131 fail 全 slice-3/interaction**：① line-clamp-auto（36，auto/max-height 交互）；② line-clamp-with-abspos（15）/ fixed-pos（12）/ floats（7）= 脱流元素 line-clamp 交互；③ line-clamp-005 等 block-children 同 BFC 跨块计行；④ webkit-line-clamp-001/002（需 `-webkit-box` legacy flexbox，独立 feature gap）；⑤ line-clamp-032/033 atomic inline 子（inline-block/flex/grid/table）交互；⑥ block-ellipsis（7，1-2.86%）= ellipsis 须「截在最后一个能容纳 ellipsis 的 break opportunity」——ZW 现截在末行文本末尾（仅溢出才截尾），spec 要 break-opportunity 感知截断（paint 期 moderate 改）。**scroll-markers 49 fail** = 独立 CSS4 feature（OOS）。**结论**：line-clamp 残余全 slice-3/interaction/ellipsis-refinement，非 clean lever。**clean-lever cluster surface 经 R2854-R2860 扫描 css-tables/content/pseudo/conditional/box/logical/sizing/lists/overflow/ui/transforms + grid-margin-trim 负结果 = definitively exhaust**——残余 cluster 全 R2174（replaced border-box）/ R1043（vertical-mode）/ R109（block-in-inline）/ line-clamp slice-3 / host-layer（caret/scroll）/ font-wall（Ahem）/ moderate-feature（ellipsis-refinement 等）。**无 .rs land**——held baseline 13484/0/74（R2857 make test 守，tree clean，零回归）。**下一步明确**：**唯一推向分阶段里程碑（2026 65%）= 用户点名深结构专项**（最高 value = Phase A IFC line-box-metric 统一，亦解 grid/flex margin-track 交互 + first-letter/first-line + line-clamp block-children 跨块计行；次 = R1043 vertical-mode / R2174 replaced-element border-box / font-stack C-dep）；守成形态 = plateau-guard（.rs drift 时跑 make test）+ 文档纠偏。**


> **📍 R2859（2026-08-07）🔍 grid margin-trim spec-correct 实现但 net-zero → 回退（rendering-compat 负结果防 re-tread）：承接 R2858。投 R2857 留作下轮候选的 grid margin-trim。**实现**：`grid_template_columns_count`（parse `auto auto`/`repeat(N,..)` 列数，动态列数→None）+ grid row-flow 无显式放置分支（按文档序 index 计 row/col，裁边缘 item margin：block_start→首行 top / block_end→末行 bottom / inline_start→首列 left / inline_end→末列 right；column/dense flow + 显式 grid-area 放置 + collapsed tracks defer）。**验证**：css-box **net-zero**（47/69 持平，22 fail 不变）——**单边案改善**（grid-block-start 0.85→0.00、grid-inline-end 0.21→0.10，证分支运行正确裁了 margin）但**组合案 grid-block/grid-inline 未 flip**（仍 ~2.40%）。**根因**：ZW grid 布局对 item margin→auto-track sizing 的应用方式与 margin-trim 预期不同——trim 已正确归零边缘 margin，但 grid-track 几何未相应收缩（taffy grid auto track 对 item margin 的响应差异），故组合案可见结果不变。**结论**：grid margin-trim **spec-correct 但对当前 ZW grid 布局 ineffective**，须先解 ZW grid margin→track sizing（深，类 R2174 territory）才能 flip——非 clean lever。**回退**：grid 分支 + margin_trim.rs 模块抽取全部 revert（tree.rs 回 1914 行 R2857 clean 态，margin_trim.rs 删除）。**无 .rs land**——held baseline 13484/0/74（R2857 make test 守，tree clean，零回归）。**下一轮**：clean-lever cluster surface 经 R2854-R2859 十一 dir 扫描 + grid margin-trim 负结果 **趋 definitively exhaust**——残余 cluster 全 R2174（replaced border-box，含本轮 grid margin-trim 证伪）/ R1043 vertical-mode / R109 block-in-inline / host-layer / font-wall / moderate-feature；可守 plateau-guard（.rs drift 时跑），或扫剩余 dir（css-cascade/css-fonts/css-text/css-backgrounds 多已 plateau）；**唯一推向分阶段里程碑（2026 65%）= 用户点名深结构专项**（最高 value = Phase A IFC line-box-metric 统一，亦解 first-letter/first-line + grid/flex margin-track 交互；次 = R1043/R2174/font-stack）。**
> **📍 R2858（2026-08-07）🔍 cluster 扫描 css-lists/css-overflow/css-ui/css-transforms = 零 clean lever（rendering-compat vein 探查 / 负结果防 re-tread）：承接 R2857。续扫 4 dir：**css-lists**（0 测试，空 dir）/ **css-overflow**（274/518，52.9%，244 fail——scroll/fragmentation/host-layer interactive 深 territory）/ **css-ui**（926/986，93.9%，60 fail——近 95% 但簇全非 clean lever）/ **css-transforms**（547/849，64.4%，302 fail——matrix 精度/3D/perspective 深 territory）。**css-ui 簇 triage**（60 fail 分类）：① **box-sizing 簇（15+ 案，含 box-sizing-007/008/009 大 diff 42%/16%/31%）= replaced element + box-sizing:border-box + auto size + padding/max-size 交互**——精确映射 **R2174**（taffy replaced-element border-box sizing，待用户决策清单深结构项），**不自主开工**；② **caret-color/caret-shape 簇（15 案）= 光标形状渲染，host-layer interactive**（caret 需 focus + 光标绘制 defer）；③ **text-overflow 簇（6 案）= Ahem 字体墙**（ellipsis 已实现 text.rs:834/1538，fail 是 Ahem 1em 方块字体度量非 ellipsis 逻辑）；④ **outline 簇（10 案）= outline-style:auto UA focus ring + 浮动/零宽 outline 子像素**（非 spec clean fix）。**grid margin-trim 评估**（R2857 留作下轮候选）：css-box 余 grid-block/grid-inline/grid-trim-ignores-collapsed-tracks 3 案；`grid_template_columns` 为 `Option<String>`（未结构化）→ 取列数须 parse `auto auto`/`repeat()`/`<track-list>` + 计算每 item 行列 index + 处理显式 grid-area 放置 + collapsed-tracks → **moderate 复杂非 clean lever，defer**（仅 2-3 案，回归风险）。**结论**：本轮 4 dir 零 clean lever；css-ui 93.9% 簇全 R2174/host-layer/font-wall/UA-behavior，css-overflow/transforms 深。**无 .rs 改动**——held baseline 13484/0/74（R2857 make test 守，tree clean，零回归）。**下一轮**：clean-lever cluster surface 经 R2854-R2858 五轮扫描（css-tables/content/pseudo/conditional/box/logical/sizing/lists/overflow/ui/transforms）趋 exhaust，残余 cluster 多 R2174/R1043/R109/host-layer/font-wall/moderate-feature；可投 grid margin-trim 作 dedicated feature（2-3 案，moderate），或扫剩余 dir（css-cascade/css-fonts/css-text/css-backgrounds 已 plateau），或守 plateau-guard；深结构（Phase A IFC line-box-metric 最高 value）仍待用户点名。**
> **📍 R2857（2026-08-07）🔧 flex 容器 margin-trim 主轴裁剪 land（rendering-compat 真渲染修复，driving WPT reftest +5 flip）：承接 R2856。cluster 扫描 css-box（42/69，27 fail）/ css-logical（3 fail）/ css-sizing（232/562，41.3% deep）/ css-display（0）。css-box 发现 **flex margin-trim 簇**（flex-row/column-grow/shrink + flex-trim-all-margins 共 5 案 ~1.04-1.12% consistent，diff = flex item 因 margin 占空间无法 grow/shrink 填容器 = 缺 margin-trim）。**调研**：margin-trim（css-box-4）仅实现 block 容器（tree.rs:1064 首末子 block-start/end），flex/grid/multicol 显式 defer（line 1069）。**land**：build_subtree 加 flex 主轴分支——parent display=Flex + horizontal-tb + flex-direction Row/Column 时，按文档序首子/末子裁主轴 margin（Row→margin_trim.inline_start/end 裁首子 margin-left/末子 margin-right；Column→block_start/end 裁首子 margin-top/末子 margin-bottom）；单项时首末同一子→两侧均裁，flex-grow/shrink 得空间。**bounded defer**：row-reverse/column-reverse（物理首末反向）/ RTL row / cross 轴 trim / 多行 wrap 逐行——需 taffy 行信息或方向映射，暂不处理（driving tests 皆 LTR 单行）。kill-switch 同 `ZW_MARGIN_TRIM=0`。**验证**：css-box **5 flip ✅ PASS**（flex-row-grow/shrink/column-grow/shrink 0.00% + flex-trim-all-margins 0.62%）；css-box 42→47（+5，**零回归**——block margin-trim 案不受影响〔不同 parent display〕，余 fail 皆 grid margin-trim / multicol-spanner / block-in-inline R109 deep）；css-flexbox 341/497 **持平**（0 margin-trim 测试，本改动 dormant）。**门禁全绿**：fmt clean / workspace clippy 零警告 / `make test` **13484/0/74**（held baseline 精确持平，零回归）/ `make product-smoke` EXIT=0 welcome **17.03%** 持平 + 全 struct PASS（layout 改动验零回归）。**门禁**：.rs（layout-engine tree.rs flex margin-trim 分支）+ rendering-compat master.md；pre-commit guard PASS。**下一轮**：css-box 余 fail = grid margin-trim（grid-block/grid-inline/grid-trim-ignores-collapsed-tracks，可同模式扩 grid 主轴但 taffy grid track 信息更复杂）/ multicol-spanner（multicol deep）/ block-in-inline（R109 deep）；css-logical float-clear = vertical-mode R1043 deep；css-sizing 41.3% deep；续扫 css-lists/css-overflow/css-ui 或 plateau-guard；深结构（Phase A IFC line-box-metric 最高 value）仍待用户点名。**
> **📍 R2856（2026-08-07）🔧 @supports 条件求值（全局关键字 + background:url()）+ `<q>` 按内容语言引号 land（rendering-compat 真渲染修复，driving WPT reftest +2 flip + spec-correctness）：承接 R2855（cluster 方法论建议续扫 css-content/css-pseudo/css-conditional）。**cluster 扫描**：css-content 49/65（16 fail）/ css-pseudo 185/261（76 fail）/ css-conditional 116/166（50 fail）。css-conditional 出现 **~20 案精确 2.08% 一致簇**（= 100×100 方块色差 = @supports 条件不满足致 block 内样式未应用）。**根因 1a（@supports 全局关键字，flip）**：`is_property_supported`（matcher/mod.rs:1424）对 length 族属性仅 `parse_length`，拒绝 CSS 全局关键字 inherit/initial/unset/revert/revert-layer——`(padding: inherit)` 误判 false → `((margin:0) and (background:blue) and (padding:inherit))` 整体 false → at-supports-012 红方块。CSS spec：全局关键字对所有属性合法。**land**：is_property_supported 顶部加全局关键字短路（任意属性 + 全局关键字 → true）。**验证**：at-supports-012 **flip ✅ PASS**（+1，零回归）。**根因 1b（@supports background:url()，flip）**：background/background-image arm 仅 `parse_gradient || parse_color`，拒绝 `url()` 图——`(background: url("..."))` 误判 false → at-supports-017 红方块。ZW 支持 background 图（R318 图片贯通 + M7 image 图元）。**land**：background arm 加 `url(` 前缀识别（大小写不敏感）。**验证**：at-supports-017 **flip ✅ PASS**（0.00%，+1，零回归）。**根因 1 合计 css-conditional 116→118（+2）**。**根因 2（`<q>` 引号按内容语言，spec-correctness net-zero）**：css-content quotes-034 测 `lang` 区域 fallback（fr/de/fi/he/ja 各异引号 + fr-FR→fr）；旧 `q_default_quote_pair` 硬编码英语。**land**：`lang_quote_table`（en/fr/de/fi/he/ja，CLDR 引号数据）+ `lang_quote_pairs`（BCP 47 区域 fallback：最长前缀逐级缩短）+ `effective_lang`（沿祖先链解析 xml:lang/lang）。**关键 spec 点（quotes-030 驱动）**：`<q>` 引号按**父元素**内容语言解析（not 自身 lang——`<q lang="ja">` 在英语父级仍用英语 depth-1 单引号，自身 lang 仅影响子级）；首版用自身 lang 致 quotes-030 回归（+1 fail），改父元素解析后回归消除。**验证**：quotes-030 持平 PASS（0.50%）、quotes-034 改善 1.58→1.43%（仍 fail——残余 = self-source 同字体两侧渲染，diff 来自 `<q>` 生成内容 inline 定位 vs 字面字符的 IFC positioning 差异，非 lang 逻辑；属 Phase A IFC territory）；css-content 16→16 持平、css-pseudo 76→76 持平 = **零回归**。**门禁全绿**：fmt clean / workspace clippy 零警告 / `make test` **13484/0/74**（held baseline 精确持平，零回归；三修均扩既有单测 test_resolve_q_quotes + test_property_supported_edge_cases 覆盖）/ `make product-smoke` EXIT=0 welcome **17.03%** 持平 + 全 struct PASS（style-system 改动验零回归——welcome 无 `<q>`/`@supports` 故新路径不触发）。**门禁**：.rs（style-system lib.rs + matcher mod.rs + 1 测试文件）+ rendering-compat master.md；pre-commit guard PASS。**下一轮**：cluster 方法论对 @supports 簇 yield 部分成功（global-keyword + background:url() 共 2 flip；残余 2.08% 簇 = @keyframes-in-@supports 动画 / malformed-condition 恢复 / container-type 未支持 / canvas-as-container 需 container queries + rAF/focus JS，多 OOS 或中复杂，非 clean lever）；续扫其他 dir 或 plateau-guard；深结构（Phase A IFC line-box-metric 最高 value）仍待用户点名。**
> **📍 R2855（2026-08-07）🔍 reftest cluster 方法论 triage + plateau-guard（rendering-compat 真渲染修复 vein 探查续 / 负结果记录防 re-tread）：承接 R2854（clear display-gate 突破）。续用「fresh scoped reftest + consistent-diff cluster 分析」方法论挖 discrete 渲染 bug。**css-tables**（104/115 self-source，11 fail）——fails 全 scattered（diff 1.01-8.16%，各异 table 特性：cell-width-0 / collapsed-border-paint-phase / baseline-vertical / margin-collapsing / visibility-collapse），无 clear-applies-to 式统一簇；table-cell-width-0（8.16%，table auto-layout min-content vs width:0）+ collapsed-border-paint-phase-002（4.21%，collapsed border paint-order 覆盖 content）均 deep table 布局/绘制 territory。**floats-clear margin-collapse-165/166 pair**（均 3.91% consistent）= margin-collapse + float + clear + border clearance（R109/adjoining deep territory）。**结论**：R2854 clear-gate 为相对 isolated 的 discrete find（incomplete property-applies-to-display gate）；css-tables/margin-clear 残余 fail 经 post-resource-gap-fix 复审确认 deep，master「clean-lever exhaust」结论基本成立（R2854 漏的 gate gap 已补）。**plateau-guard**：并行 session image-decoder crate（f9fced0a）+2 测试，combined 态 `make test` **13484/0/74** 全绿（R2854 13482 + image-decoder 2，零回归）。**下一轮**：cluster 方法论 yield 有限（R2854 为 isolated gate-gap），续扫其他 mid-rate dir（css-content/css-pseudo/css-conditional）找 consistent-diff cluster，或转向并行 session D2 亚像素统计（<2% fail 分类 font-wall/sub-pixel vs real）辅助；深结构（Phase A IFC/R1043/R2174）仍待用户点名。**
> **📍 R2854（2026-08-07）🔧 clear display-gate 补全 land（+TableCaption +InlineTable / rendering-compat 真渲染修复，driving WPT reftest flip）：承接 R2853。fresh scoped reftest `css/CSS2/floats-clear`（166/214 self-source）发现 clear-applies-to-004/005/006/014/015 五案 ~3.83% 簇。**调研**：existing clear display-gate（engine.rs:1098）已覆 TableRowGroup/HeaderGroup/FooterGroup/Row/Cell/Column/ColumnGroup → clear=None（CSS 2.1 §9.10 clear 仅适用 block-level），**漏 TableCaption + InlineTable**。**land**：gate 扩 +TableCaption + InlineTable（spec-correct：internal table caption + inline-level inline-table 非 block-level）。**验证**：clear-applies-to-015（table-caption）reftest **flip ✅ PASS**（self-source 167/214，+1，零回归）；004/005/006/014 余量来自 table 布局精度（非 clear，R109/table territory deep）故仍 fail。**门禁全绿**：fmt clean / clippy 零警告 / `make test` **13482/0/74**（R2853 13479 + 3 新 layout-engine 单测 r2854_clear_display_gate，零回归）/ floats-clear reftest 167/214（+1 = 015 flip）/ `make product-smoke` welcome desktop **17.03%** 持平 + 全 struct PASS（layout 改动验零回归）。**已知限制（记录）**：clear-applies-to-014（inline-table）clear 已被 gate 忽略但其 fail 是 inline-table 布局精度（非 clear）；004/005/006 已在既有 gate（clear=None），fail 是 table 布局。**门禁**：.rs（layout-engine engine.rs gate + 3 单测）+ rendering-compat master.md；pre-commit guard PASS。**
> **📍 R2853（2026-08-07）🔧 document.contentType + Node.normalize land（剩余 Web API 表面收尾 / 主线在 zero-web）：承接 R2852。close 最后剩余 Web API 表面缺口：① `document.contentType` = 'text/html'（HTML 文档 MIME，content-sniffing / HTTP-utility 代码读，旧 fallthrough undefined）；② `Node.normalize()` no-op——snapshot 模型元素文本为单一串（无独立 Text 子节点暴露），故 DOM 态已「normalized」，no-op 语义正确（非误导 stub），防 rich-text 编辑器 / innerHTML 后清理的 `el.normalize()` 防御性调用抛 TypeError。**门禁全绿**：fmt clean / clippy 零警告 / `make test` **13479/0/74**（R2851 13478 + 1 新测试，零回归；engine lib 1578 零回归）。**已知限制**：normalize no-op（无 Text 子节点可合并，架构既限）；canvas/video width-height defer（特殊缺省/bitmap）。**门禁**：.rs（engine shim+测试）；pre-commit guard PASS。**

> **📍 R2852（2026-08-07）🔧 reftest-smoke grep -P 可移植性修复（run-reftest-smoke.sh / DC-1 reftest 基建）：承接 R2851。run-reftest-smoke.sh 用 `grep -oP '...\K...'`（Perl 正则）提取 Passed 计数——BSD grep（macOS）不支持 -P/\K，会致 smoke 门禁在 macOS 恒误报 FAIL（CI reftest job 跑 ubuntu 故 latent，但破 macOS dev / 矩阵扩展）。**改可移植 awk**（`awk '/^  Passed:/{print $2; exit}'`，GNU+BSD 通用）；行为验证 PASS→1→✓ / FAIL→空→✗。补并行 session 074f87e4 的 `$1` unbound 修复（同文件不同 bug）。**端到端验证**：`make reftest-smoke` 跑并行 session 填充的 15 representative case **15/15 PASS**（awk fix + populated list 协同 functional）。**门禁**：bash -n + 行为 mock OK / fmt clean / clippy 零警告 / `make test` 13478/0/74（.sh-only，cargo suite 不受影响）；pre-commit guard PASS。**

> **📍 R2851（2026-08-07）🔧 IMG/IFRAME 维度 IDL land（width/height reflected unsigned long + naturalWidth/Height / 缺失 Web API 续，主线在 zero-web）：承接 R2850。probe 发现 IMG/IFRAME `.width`/`.height`（reflected unsigned long，旧 fallthrough undefined）+ IMG `.naturalWidth`/`.naturalHeight`（缺失）全无 get-trap handler（仅 Image ctor 的 setAttribute + pointer event coord 两处无关命中）。响应式/布局 JS 读 img.width 高频，css-images WPT driving。**land**：① `.width`/`.height`（IMG/IFRAME gate）—— reflected unsigned long：`parseInt(attr)` 缺省/负/NaN→0（spec「reflect unsigned long」算法）；sync set→get 优先读 `_reflectedAttrs` 缓存。② setter parseInt 归一 → 缓存数值 + 写 width/height 内容属性。③ `.naturalWidth`/`.naturalHeight`（IMG gate）—— 恒 0（headless 无真图加载 / onload 不触发 → 固有尺寸 0，spec unloaded→0 一致）。**门禁全绿**：fmt clean / workspace clippy 零警告 / `make test` **13478/0/74**（R2850 13477 + 1 新测试，零回归；engine lib 1577 测试零回归）/ `make product-smoke` welcome desktop **17.03%** 持平 + 全 struct PASS（纯 additive IDL getter/setter，welcome 无 IMG/IFRAME 故新路径不触发，无 layout/render .rs 改动）。**已知限制（记录）**：naturalWidth/Height 恒 0（headless 无图加载，需 onload+decode infra defer）；CANVAS width/height（缺省 300/150 且 setter 改 bitmap，特殊）+ VIDEO/EMBED defer；setter 用 parseInt 近似 ToUint32（超界/负数规范 ToUint32 defer）。**门禁**：.rs（engine shim+测试）+ 两 master doc；pre-commit guard PASS。**下一轮**：缺失 Web API 纯 JS tractable 表面近穷尽；剩余全深/host-layer（attachShadow Shadow DOM / createRange Range / scrollIntoViewIfNeeded 滚动 / 真 files 上传 / fetch 非 GET〔net〕）或边际 no-op（node.normalize）；rendering-compat 侧续降频守成（held baseline 13478 全绿）。**

> **📍 R2850（2026-08-07）🔧 reflected 全局属性续 land（inert + autocomplete / 缺失 Web API 续，主线在 zero-web）：承接 R2849。probe 发现 `inert`（boolean attr，缺省 false）+ `autocomplete`（enumerated 串，spec missing-default **"on"**）旧 fallthrough 返 undefined。延续 R2848 reflected-attr 模式。**land**：扩 R2848 getter/setter 块——`inert` 同 autofocus（boolean presence via `__zw_has_attr(_handle)`，truthy 设空值 / falsy 真移除）；`autocomplete` enumerated 串反射（attr 值，缺省 / `__zw_get_attr` 返 `""` → `"on"`，spec 一致；setter 写任意串到 attr）。模态/无障碍（inert 隔离交互）/ 表单自动填充（autocomplete）读这些属性高频。**TDD red→green**：初版 autocomplete 缺省判定 `acRaw == null` 漏 `__zw_get_attr` 缺省返 `""`（非 null）→ 返 `""` 非 `"on"`；改 `(acRaw == null || acRaw === '')` 修正。**门禁全绿**：fmt clean / workspace clippy 零警告 / `make test` **13477/0/74**（R2849 13476 + 1 新测试，零回归；engine lib 1576 测试零回归）/ `make product-smoke` welcome desktop **17.03%** 持平 + 全 struct PASS（reflected attr getter/setter，welcome 不读这些属性故新路径不触发，无 layout/render .rs 改动）。**已知限制（记录）**：autocomplete 不做 detail token 规范化（spec 对 `off`/`on`/section-* tokens 有 canonical 算法——本实现原样返/写 attr 值，常见用法足）；inert 仅 IDL 反射（无真交互隔离，host-layer defer）。**门禁**：.rs（engine shim+测试）+ 两 master doc；pre-commit guard PASS。**下一轮**：缺失 Web API 纯 JS tractable 表面续扫（更多 reflected 全局属性 enterkeyhint/itemscope / img naturalWidth/Height〔headless 低价值〕 / node.normalize）或 plateau-guard；rendering-compat 侧续降频守成（held baseline 13477 全绿）。**

> **📍 R2849（2026-08-07）🔧 `<option>`.index land（HTMLOptionElement / 表单 IDL 续，主线在 zero-web）：承接 R2848。probe 发现 `<option>`.index 缺失（旧 fallthrough undefined）。form 库读 option.index 定位选项位置高频。**land**——同 R2842 rowIndex 模式：OPTION gate + `_ancestorChain` 找 owning SELECT + 元素作用域 `querySelectorAll('option')` + proxy identity 计位（0-based document order）；optgroup 内 option 仍按 document order 计；detached / handle-based / 无 select → 0（Chromium detached→0 一致，非 -1——option spec 无 -1 语义）。零新 host infra（复用 _ancestorChain + querySelectorAll + _wrapSelector identity）。**门禁全绿**：fmt clean / workspace clippy 零警告 / `make test` **13476/0/74**（R2848 13475 + 1 新测试，零回归；engine lib 1575 测试零回归）/ `make product-smoke` welcome desktop **17.03%** 持平 + 全 struct PASS（纯 additive OPTION getter，welcome 无 `<select>` 故新路径不触发，无 layout/render .rs 改动）。**已知限制（记录）**：handle-based option（`new Option()` 未挂载）→ 0（handle 不在 DOM 快照，querySelectorAll 不含——同 rowIndex handle 限制；挂载后 selector-based 正确）；option 集合非 live（snapshot 架构）。**门禁**：.rs（engine shim+测试）+ 两 master doc；pre-commit guard PASS。**下一轮**：缺失 Web API 纯 JS tractable 表面近穷尽（R2820-R2849 覆表单反射 + 表格结构 + text-control 选区 + 事件基建 + 观察者 record + reflected 全局属性 + option.index）；剩余元素特定 IDL 全极低价值（img naturalWidth/Height〔headless 无真图加载〕）或全深/host-layer；rendering-compat 侧续降频守成（held baseline 13476 全绿）。**

> **📍 R2848（2026-08-07）🔧 reflected 布尔/枚举全局属性 land（autofocus/draggable/spellcheck/translate / 缺失 Web API 续，主线在 zero-web）：承接 R2847。probe（get-trap 未显式 handle 的 string prop 落 undefined）发现 4 个常见 reflected 全局属性旧 fallthrough 返 **undefined**（spec 须布尔）：`autofocus`（boolean attr，缺省 false）、`draggable`（enum，缺省 false）、`spellcheck`（boolean，spec 缺省 **true**）、`translate`（enum，spec 缺省 **true**）。延续 R2840 reflected-attr 模式（htmlFor/defaultValue/defaultChecked）。**land spec-correct**：① get-trap 加 4-prop 块——优先读 `_reflectedAttrs` 缓存（setter 写解析布尔，同步 set→get 即时），无缓存则 host attr 解析：autofocus 走 `__zw_has_attr(_handle)` presence；draggable `"true"`→true 余→false；spellcheck `"false"`→false 余→true（含缺省）；translate `"no"`→false 余→true。② set-trap 加 4-prop 块——归一布尔 → 缓存 + 写 attr：autofocus=boolean presence（truthy 设空值 / falsy 真移除），draggable/spellcheck=`"true"`/`"false"`，translate=`"yes"`/`"no"`。表单（autofocus）/ 拖拽库（draggable）/ 编辑器（spellcheck）/ 本地化（translate）读这些属性高频。**门禁全绿**：fmt clean / workspace clippy 零警告 / `make test` **13475/0/74**（R2847 13474 + 1 新测试，零回归；engine lib 1574 测试零回归）/ `make product-smoke` welcome desktop **17.03%** 持平 + 全 struct PASS（reflected attr getter/setter，welcome 不读这些属性故新路径不触发，无 layout/render .rs 改动）。**已知限制（记录）**：draggable 缺省（auto）简化为 false（spec：img/a[href] 等原生 draggable 元素缺省 true——需 tag 感知，罕见 defer）；spellcheck/translate 缺省 true 经 host attr 缺失判定（无 attr → true，spec 一致）；`inert`/`autocomplete` 等更多 reflected 全局属性未含（可续同模式）。**门禁**：.rs（engine shim+测试）+ 两 master doc；pre-commit guard PASS。**下一轮**：缺失 Web API 纯 JS tractable 表面续扫（更多 reflected 全局属性 inert/autocomplete/contentEditable边缘 / option.index〔selector-based 可续 R2842 模式，handle-based 需 host 树遍历〕）；rendering-compat 侧续降频守成（held baseline 13475 全绿）。**

> **📍 R2847（2026-08-07）🔧 MutationRecord instanceof + spec 字段完整性 land（Web IDL 接口 / 缺失 Web API 续，主线在 zero-web）：承接 R2846。probe 发现 MutationObserver 回调收到的 record 为 plain object（`var rec = {}`）——`record instanceof MutationRecord` 假、`Object.prototype.toString` 返 `[object Object]` 非 `[object MutationRecord]`、且缺 spec 字段（previousSibling/nextSibling/attributeNamespace/oldValue 缺省 null，addedNodes/removedNodes 缺省 []）。**根因**：MO 实现功能正确（attributes/childList 通知 + microtask 派发既 land），但 record 未建为 MutationRecord 实例 + 字段不全。**land spec-correct**：① 定义 `globalThis.MutationRecord` 构造器（plain prototype + `Symbol.toStringTag='MutationRecord'`）；② `_mo_notify` record 改 `Object.create(MutationRecord.prototype)` + 归一化全 spec 字段（addedNodes/removedNodes 缺省 []，previousSibling/nextSibling/attributeName/attributeNamespace/oldValue 缺省 null）。库做 `record instanceof MutationRecord` 特征检测 / 读 `record.previousSibling` 须得 null 非 undefined。**门禁全绿**：fmt clean / workspace clippy 零警告 / `make test` **13474/0/74**（R2846 13473 + 1 新测试，零回归；engine lib 1573 测试 MO record 变更零回归）/ `make product-smoke` welcome desktop **17.03%** 持平 + 全 struct PASS（纯 additive record 构造，welcome 不用 MO 故新路径不触发）。**已知限制（记录）**：addedNodes/removedNodes 为真 Array 非 NodeList（shim 近似，`instanceof NodeList` 不成立，罕见不究）；MutationRecord 构造器无公开入参（字段由 _mo_notify 注入，spec 构造器不暴露）；characterData/subtree/attributeOldValue 通知未实现（既有 MO 限制）。**门禁**：.rs（engine shim+测试）+ 两 master doc；pre-commit guard PASS。**下一轮**：缺失 Web API 纯 JS tractable 表面已穷尽（R2820-R2847 覆表单反射 + 事件基建 + 观察者 record spec-completeness）；剩余元素特定 IDL 全极低价值（img naturalWidth/Height〔headless 无真图加载〕/ option.index〔需 host 父-select 索引回调〕）或全深/host-layer；rendering-compat 侧续降频守成（held baseline 13474 全绿）。**

> **📍 R2846（2026-08-07）🔧 HTMLOutputElement.value + defaultValue land（spec-correct：value 独立于 textContent / 缺失 Web API 续，主线在 zero-web）：承接 R2845。**承接并修复上一 session 遗留未提交 WIP**——原 R2846 用「textarea-mirror」模型（设 output.value 写 DOM text），但其测试 `make test` 失败：设 `o.value=99` 后立即读 `o.textContent` 得 stale '12'（host 异步 mutation 未 apply）。**根因双重**：① 代码层面 textarea-mirror 模型在异步快照架构下致 immediate-read 时序失败；② **spec 层面 textarea-mirror 本身错误**——HTML LS §4.10.12：`<output>` 按 **children** 渲染非 `value`，设 `.value` **永不触碰 DOM text 节点**（与 textarea 区分：textarea value↔text，output value 独立于 text）。**改 spec-correct 模型**：新增 `_outputValue`（dirty 当前值，key 存在即 dirty）+ 复用 `_outputDefault`（lazy 捕获 textContent = 默认值，跨 value 变更稳定）；value getter = `dirty?_outputValue:_outputDefault`；value setter = dirty+store **不写 text**；defaultValue getter/setter lazy default；**零 DOM text mutation**。表单计算器 `<output>` 显示结果高频。**Chromium 150 oracle 锚定**：`<output>12</output>` → value=defaultValue='12'；`o.value=99` → value='99'、textContent **仍 '12'**（独立性）、defaultValue '12' 稳定；`o.defaultValue='dd'` → defaultValue='dd'、dirty value 仍 '99'。**门禁全绿**：fmt clean / workspace clippy 零警告 / `make test` **13473/0/74**（R2845 13472 + 1 新测试，零回归）/ `make product-smoke` welcome desktop **17.03%** 持平 + 全 struct PASS（纯 additive JS getter/setter，welcome 无 `<output>` 故新路径不触发，无 layout/render .rs 改动）。**已知限制（记录）**：`<output>` 无 `value` 内容属性（纯 IDL，故 defaultValue 亦无 attr 反射，= lazy textContent 非 attr）；form reset 不重置 dirty value（无 reset 接线，defer）；`htmlFor`（DOMSettableTokenList）复杂罕见 defer。**门禁**：.rs（engine shim+测试）+ 两 master doc；pre-commit guard PASS。**下一轮**：缺失 Web API 纯 JS tractable 表面已穷尽（R2820-R2846 覆表单反射全表面）；剩余元素特定 IDL 全极低价值（img naturalWidth/Height〔headless 无真图加载低价值〕/ option.index〔需 host 父-select 索引回调〕）或全深/host-layer（elementFromPoint / customElements lifecycle / Shadow DOM / 真 WAAPI / 真 files 上传 / fetch 非 GET〔net 跳过〕）；rendering-compat 侧续降频守成（held baseline 13473 全绿）。**

> **📍 R2845（2026-08-06）🔧 table.caption/tHead/tFoot + section.rows land（表格结构表面收尾 / 缺失 Web API 续，主线在 zero-web）：承接 R2844。land 表格结构表面收尾——`table.caption`/`table.tHead`/`table.tFoot`（首 caption/thead/tfoot 子元素或 null）+ `section.rows`（thead/tbody/tfoot 作用域内行）。表格分析 / 序列化库读结构高频。延续 R2843 querySelector(All) 模式。caption/tHead/tFoot（TABLE gate，getter-only）：`querySelector('caption'|'thead'|'tfoot')` 首匹配或 null（Chromium 150 oracle 锚定）；setter 复杂罕见落 catch-al（documented）。`rows` 扩 gate（+THEAD/TBODY/TFOOT），section-scoped `querySelectorAll('tr')`（tbody.rows=2 非 table.rows=4）；textarea.rows 落 catch-al 不冲突。**门禁全绿**：fmt clean / workspace clippy 零警告 / `make test` **13472/0/74**（R2844 13471 + 1 新测试，零回归）/ `make product-smoke` welcome desktop **17.03%** 持平 + 全 struct PASS / **`make product-smoke-legacy` 42 fixture 0 struct FAIL**（表格结构性元素按 run-rules 跑 legacy）。**已知限制（记录）**：caption/tHead/tFoot 仅 getter（setter 落 catch-al 反射内容属性，defer）；querySelector('caption') 嵌套 table 内层误返（罕见不究，同 R2843 querySelectorAll 限制）；section.rows 含嵌套 table 内层 tr（spec 应排除，罕见不究）；集合静态真数组非 live。**门禁**：.rs（engine shim+测试）+ 两 master doc；pre-commit guard PASS。**下一轮**：缺失 Web API 纯 JS tractable 表面**已穷尽**（R2820-R2845 覆表格结构全表面 + text-control 选区）；剩余元素特定 IDL 全极低价值（output value / img naturalWidth〔headless 低价值〕）或全深/host-layer；rendering-compat 侧续降频守成（held baseline 13472 全绿）。**

> **📍 R2844（2026-08-06）🔧 text-control 选区 IDL land（selectionStart/End/Direction + setSelectionRange + select / 缺失 Web API 续，主线在 zero-web）：承接 R2843。land text-control（input text-type / textarea）选区 IDL——文本编辑器（CodeMirror/Monaco/Slate/Quill）/ 自动选择 / Range 算法读选区高频。**Chromium 150 oracle 锚定**（dump-dom 法）：默认 `{0,0,'forward'}`（未聚焦 text control 选区折叠在 **0** 非「值末」）；`select()`→`{0, value.length, 'forward'}`；`setSelectionRange(s,e,d)`：clamp `[0,len]` + end<start 折叠到 end + direction 缺省 `'forward'`；属性 setter 保 `0≤start≤end≤len` 不变式。**JS 端 `_textSelection` per-element map**（headless 无真 caret 故纯 JS 跟踪，+reset），getter 纯读默认、setter/method ensure 后 mutate。gate `_isTextControl`：TEXTAREA 或 INPUT type∈{text,search,url,tel,password,空}（number/email/date/range/color/checkbox→null，非 text control→undefined）。既有 `select`（R2826 no-op）升级——text-control 触发全选路径。**门禁全绿**：fmt clean / workspace clippy 零警告 / `make test` **13471/0/74**（R2843 13470 + 1 新测试，零回归）/ `make product-smoke` welcome desktop **17.03%** 持平 + 全 struct PASS（纯 additive JS shim 无 layout/render .rs 改动）。**已知限制（记录）**：负数 start 的 setSelectionRange 与 Chrome 古怪归一分歧（Chrome `setSR(-5,-1)`→`{5,5}` 病态，本实现按 spec clamp——无真实代码依赖，documented）；非选区 type 返 undefined（Chrome null，`!= null` 兼容）；无真 caret/选择渲染（selectionchange 不触发）；setRangeText 未实现（defer）。**门禁**：.rs（engine shim+测试）+ 两 master doc；pre-commit guard PASS。**下一轮**：缺失 Web API 纯 JS tractable 表面近穷尽；剩余元素特定 IDL 全低价值/中复杂（table.caption/tHead/tFoot + tbody.rows〔同 querySelectorAll 模式，可续〕/ output value / img naturalWidth〔headless 低价值〕）或全深/host-layer；rendering-compat 侧续降频守成（held baseline 13471 全绿）。**

> **📍 R2843（2026-08-06）🔧 `<tr>`.sectionRowIndex + `<table>`.rows/tBodies land（表格结构表面收尾 / 缺失 Web API 续，主线在 zero-web）：承接 R2842。延续表格结构表面——land sectionRowIndex + table.rows/tBodies。data-table 库迭代 table.rows 高频。sectionRowIndex（TR）：同 rowIndex 模式——_ancestorChain 找最近 thead/tbody/tfoot section → 元素作用域 querySelectorAll('tr') + identity（各 section 独立计）。table.rows（TABLE gate）：_wrapSelector(sel).querySelectorAll('tr') 元素作用域返**真数组**（querySelectorAll 返 split|map(_wrapSelector) 真 Array，原生支持 Array.map.call，R2843 测验）。table.tBodies（TABLE）：querySelectorAll('tbody')。gate rows 仅 TABLE——textarea.rows 落 catch-al 反射不冲突。**门禁全绿**：fmt clean / workspace clippy 零警告 / `make test` **13470/0/74**（R2842 13469 + 1 新测试，零回归）/ `make product-smoke` welcome desktop **17.03%** 持平 + 全 struct PASS / **`make product-smoke-legacy` 42 fixture 0 struct FAIL**（表格结构性元素按 run-rules 跑 legacy）。**已知限制（记录）**：table.caption/tHead/tFoot + tbody.rows（section-scoped 行）未含（同模式可续）；table.rows 返静态真数组非 live HTMLCollection（snapshot 架构无 live）；无 .item/.namedItem 方法（用索引访问）。**门禁**：.rs（engine shim+测试）+ 两 master doc；pre-commit guard PASS。**下一轮**：缺失 Web API 纯 JS tractable 表面近穷尽；剩余元素特定 IDL（table.caption/tHead/tFoot + tbody.rows / textarea selectionStart/End / output value / img naturalWidth〔headless 低价值〕）或全深/host-layer；rendering-compat 侧续降频守成（held baseline 13470 全绿）。**

> **📍 R2842（2026-08-06）🔧 `<tr>`.rowIndex + `<td>`/`<th>`.cellIndex land（表格结构位置 / 缺失 Web API 续，主线在 zero-web）：承接 R2841。probe 确认表格结构位置 IDL 缺失。land——data-table / 表格操作库读这些定位行/列高频。**client-side 实现**（零新 host 回调）：`_proxyCache` identity-stable（R2841 验证）故 identity 比较计位。rowIndex（TR）：_ancestorChain→TABLE + 元素作用域 querySelectorAll('tr')（R2673，document order）+ identity——跨 thead/tbody/tfoot 全行；detached/无 table→-1。cellIndex（TD/TH）：_ancestorChain→TR + querySelectorAll(':is(td,th)')（:is 内部支持逗号，td+th 混计 document order）+ identity；无 row→-1。**关键发现**：HTML 解析器丢弃 table 外的 `<tr>`（非法），故「无 table 的 tr」用 createElement('tr') detached 测 -1（orphan tr 不存在）。**门禁全绿**：fmt clean / workspace clippy 零警告 / `make test` **13469/0/74**（R2841 13468 + 1 新测试，零回归）/ `make product-smoke` welcome desktop **17.03%** 持平 + 全 struct PASS / **`make product-smoke-legacy` 42 fixture 0 struct FAIL**（表格结构性元素按 run-rules 跑 legacy 验零退化）。**已知限制（记录）**：sectionRowIndex + table.rows/tBodies/tbody.rows 集合未含（同模式可续）；嵌套 table querySelectorAll 含内层行（spec 应排除，罕见不究）。**门禁**：.rs（engine shim+测试）+ 两 master doc；pre-commit guard PASS。**下一轮**：缺失 Web API 纯 JS tractable 表面近穷尽；剩余元素特定 IDL（sectionRowIndex + table 集合 / textarea selectionStart/End / output value / img naturalWidth〔headless 低价值〕）或全深/host-layer；rendering-compat 侧续降频守成（held baseline 13469 全绿）。**

> **📍 R2841（2026-08-06）🔧 `.form`（form-associated 控件 form owner）land（INPUT/SELECT/TEXTAREA/BUTTON / 缺失 Web API 续，主线在 zero-web）：承接 R2840。probe 确认 `.form` 缺失。land——form 校验/序列化库读 input.form 找 owner form 上下文高频。**spec 顺序**：① `form` 属性关联优先（<input form="id"> → getElementById，即使无 ancestor form）；② 否则最近 ancestor <form>（经 _ancestorChain 上行 + __zw_get_tag 判 FORM）。gate = INPUT/SELECT/TEXTAREA/BUTTON；返 form proxy（identity 一致）；handle-only detached / 无 owner → null。复用既有 _ancestorChain + getElementById，零新 host infra。**门禁全绿**：fmt clean / workspace clippy 零警告 / `make test` **13468/0/74**（R2840 13467 + 1 新测试，零回归）/ `make product-smoke` welcome desktop **17.03%** 持平 + 全 struct PASS / **`make product-smoke-legacy` 42 fixture 0 struct FAIL（37-form-controls PASS 3.85%）**。**已知限制（记录）**：gate 未含 OUTPUT/FIELDSET/LABEL/OBJECT（罕用，可续）；form 属性关联用 getElementById（近似同 document 查找）；ancestor 为 snapshot-based parent（form 关联读通常一次性，可接受）。**门禁**：.rs（engine shim+测试）+ 两 master doc；pre-commit guard PASS。**下一轮**：缺失 Web API 纯 JS tractable 表面近穷尽；剩余元素特定 IDL（table rowIndex/cellIndex〔需 host 结构回调〕/ textarea selectionStart/End / img naturalWidth〔headless 低价值〕/ output value）或全深/host-layer；rendering-compat 侧续降频守成（held baseline 13468 全绿）。**

> **📍 R2840（2026-08-06）🔧 反射属性 IDL land（label.htmlFor / input.defaultValue / input.defaultChecked / 缺失 Web API 续，主线在 zero-web）：承接 R2839。probe 确认 htmlFor/defaultValue/defaultChecked 全缺。land——form reset / 校验库读这些判「值/选中态是否改过」高频。getter（_realTag gate）：label.htmlFor 反射 for；input.defaultValue 反射**初始** value 属性（区别 .value 当前态——改 .value 不影响 defaultValue）；input.defaultChecked 反射 checked 属性存在性。setter（attr 名映射）：htmlFor→for / defaultValue→value / defaultChecked→checked boolean——映射必须显式（catch-all 会用属性名作 attr 名致 spurious 属性）。**门禁全绿**：fmt clean / workspace clippy 零警告 / `make test` **13467/0/74**（R2839 13466 + 1 新测试，零回归）/ `make product-smoke` welcome desktop **17.03%** 持平 + 全 struct PASS / **`make product-smoke-legacy` 42 fixture 0 struct FAIL（37-form-controls PASS 3.85%）**。**已知限制（记录）**：input.form（所属 form，需父链遍历）未含；.checked 与 defaultChecked 在 shim 同返属性存在性（shim 无独立 toggle 跟踪，.checked 既有 limitation）；defaultValue setter 不联动 .value（罕见 defer）。**门禁**：.rs（engine shim+测试）+ 两 master doc；pre-commit guard PASS。**下一轮**：缺失 Web API 纯 JS tractable 表面近穷尽；剩余元素特定 IDL（input.form 父链 / textarea selectionStart/End / table rowIndex/cellIndex〔需 host 结构回调〕/ img naturalWidth〔headless 低价值〕）或全深/host-layer；rendering-compat 侧续降频守成（held baseline 13467 全绿）。**

> **📍 R2839（2026-08-06）🔧 HTMLFormElement 反射 IDL 属性 land（action/method/enctype/target / 缺失 Web API 续，主线在 zero-web）：承接 R2838。probe 发现 `<form>` 反射 IDL 属性（action/method/enctype/target）全缺。land——form 序列化 / AJAX 提交库读 form.action/form.method 构提交请求高频。仅 getter（FORM gate）：action/target 纯串反射；method 小写归一 + spec enum（get/post/dialog，无效→'get'）；enctype 小写归一 + enum 默认。setter 经 catch-al 近似。**门禁全绿**：fmt clean / workspace clippy 零警告 / `make test` **13466/0/74**（R2838 13465 + 1 新测试，零回归）/ `make product-smoke` welcome desktop **17.03%** 持平 + 全 8 struct PASS / **`make product-smoke-legacy` 42 fixture 0 struct FAIL（含 37-form-controls PASS 3.85%）**——form 变更按 run-rules 跑 legacy 验零结构性回归。**已知限制（记录）**：仅 getter（method/enctype setter 不小写归一，罕见 defer；action/target setter 正确）；form.name/acceptCharset 未含（可续）。**门禁**：.rs（engine shim+测试）+ 两 master doc；pre-commit guard PASS。**下一轮**：缺失 Web API 纯 JS tractable 表面近穷尽；剩余元素特定 IDL（img naturalWidth/Height〔headless 低价值〕/ label htmlFor / input defaultChecked/defaultValue / textarea selectionStart/End / table rowIndex/cellIndex〔中频，需 host 结构回调〕）或全深/host-layer；rendering-compat 侧续降频守成（held baseline 13466 全绿）。**

> **📍 R2838（2026-08-06）🔧 HTMLAnchorElement/HTMLAreaElement URL 分解 IDL 属性 land（链接 URL 组件 / 缺失 Web API 续，主线在 zero-web）：承接 R2837。probe 发现 `<a>`/`<area>` URL 分解 IDL 属性（href/pathname/search/hash/host/hostname/port/protocol/origin/username/password）全缺。land——经既有 `__zw_parse_url`（R2778 url crate）解析 href 属性（base = location.href）取组件。**SPA 路由**（读 a.pathname/a.search）/链接分析/analytics/jQuery .prop('href') 高频。**关键**：a.href getter 返**绝对** URL（区别 getAttribute('href') 返原始串——.prop vs .attr 经典区分；相对 href 经 base 解析）；仅 getter（A/AREA gate），href setter 经既有 set-trap catch-al 正确工作；复用既有 infra 零新 host 回调。**门禁全绿**：fmt clean / workspace clippy 零警告 / `make test` **13465/0/74**（R2836 13464 + 1 新测试，零回归）/ `make product-smoke` welcome desktop **17.03%**（精确 baseline 持平，纯 additive getter 无 layout/render .rs 改动）+ 全 8 struct PASS。**已知限制（记录）**：仅 getter（URL 组件 setter 经 catch-al 误设 spurious 属性，罕见 defer；href setter 正确）；origin opaque scheme 返 "null"；reftest 裸 sandbox 无 __zw_parse_url → href 回落原始串。**门禁**：.rs（engine shim+测试）+ 两 master doc；pre-commit guard PASS。**下一轮**：缺失 Web API 纯 JS tractable 表面近穷尽，剩余全深/host-layer 或极窄；rendering-compat 侧续降频守成（held baseline 13465 全绿）。**

> **📍 R2837（2026-08-06）🔍 reftest plateau-guard css-borders flat 43/83（R2832-R2836 回归验证 / rendering-compat 守成）：承接 R2836。本 session R2832-R2836 五轮 additive JS（Option/select.add、document 集合、Image ctor、Audio+media 方法、input.valueAsNumber）均编辑共享 `js_dom_shim.js`，按守成 cadence（每 ~10 轮 / .rs 变更时）跑 plateau-guard 验渲染零回归。**`make reftest-upstream FILTER=css-borders`**（self-source，9.56s）= **43/83 (51.8%)**，与 prior baseline（R2588 css-borders 43/83）**精确持平 = 零回归**。DC-14 三态：真通过-可信 29/83 (34.9%) / 近似 10 / 不一致 40。css-borders 不使用新 API（additive JS 不影响无关渲染路径），flat 符合预期——主要验证共享文件编辑未误伤既有渲染。证据 `evidence/reftest-plateau-guard-css-borders-2026-08-06.txt`。**限制**：self-source（非 oracle，css-borders 无 oracle-shots），绝对率含 ~46% 假通过，但同方法论前后比对对回归检测有效；css-images（R2834 Image 相关）无 oracle-shots + 无 prior baseline 未跑。**结论**：R2832-R2836 零渲染回归，held baseline 稳。**下一轮**：tractable Web API 表面已穷尽；rendering-compat 侧无新自主推进面（clean-lever 9+1 vein exhaust），唯 plateau-guard 守成 + 用户点名深结构（Phase A IFC 最高 value）；或续极窄项（option.index 需 host 父-select 索引回调 / scrollIntoViewIfNeeded / pointer lock）。**

> **📍 R2836（2026-08-06）🔧 `input.valueAsNumber` IDL 属性 land（number 输入值↔数值 / 缺失 Web API 续，主线在 zero-web）：承接 R2835。probe 确认 valueAsNumber 缺失（input IDL 补全）。land input.valueAsNumber（getter+setter）——number/range 输入值↔数值转换（计算器/数量输入/校验库读 NaN 判非法）。getter（INPUT gate）：type=number/range parseFloat(value)（空/无效→NaN），其他 type→NaN（date/time defer）；setter：NaN→''，否则 String(n)→设 value 属性+缓存（复用 value 同步路径），其他 type/非 INPUT no-op（分支终止不 fallthrough 致误设 'valueAsNumber' 属性）。**门禁全绿**：fmt clean / workspace clippy 零警告 / `make test` **13464/0/74**（R2835 13463 + 1 新测试，零回归）/ `make product-smoke` welcome desktop **17.03%**（精确 baseline 持平，纯 additive IDL 属性无 layout/render .rs 改动）+ 全 8 struct PASS。**已知限制（记录）**：parseFloat 宽容（'12px'→12，spec 严格应 NaN，number input 罕见非数字可接受）；date/month/week/time/datetime-local 类型 valueAsNumber 未实现（各类型格式↔时间戳转换 defer）；setter 对 number/range 外 type 静默忽略（lenient）。**门禁**：.rs（engine shim+测试）+ 两 master doc；pre-commit guard PASS。**下一轮**：缺失 Web API 纯 JS tractable 表面已穷尽，剩余全深/host-layer（elementFromPoint / customElements lifecycle / Shadow DOM / node.normalize / 真 WAAPI / date/time valueAsNumber / media 时长属性 / 真 files 上传 / fetch 非 GET〔net 跳过〕）或极窄（option.index〔需 host 父-select 索引回调〕/ scrollIntoViewIfNeeded / pointer lock）；rendering-compat 侧续降频守成（held baseline 13464 全绿）。**

> **📍 R2835（2026-08-06）🔧 `new Audio()` ctor + HTMLMediaElement 方法桩 land（音频构造 + 媒体方法 / 缺失 Web API 续，主线在 zero-web）：承接 R2834。probe 确认 Audio/Video 全缺（Video 非 HTML 标准命名构造器——仅 Image/Option/Audio 三者）。land `new Audio([src])`——音效/播客/通知音频构造高频（`new Audio(url).play()`）。返 createElement('audio') proxy（镜像 Image R2834），设 src。**HTMLMediaElement 方法桩**（play/pause/load/canPlayType）加 element get-trap，仅 AUDIO/VIDEO（_realTag gate，sel+handle 双身份）：headless 无音视频设备，**play 返 Promise.resolve(undefined)**（spec 一致，链式 .then 经 execute 末 microtask checkpoint 派发），pause/load no-op，canPlayType 返 ''（保守）。gate 仅 AUDIO/VIDEO——非 media 元素 play=undefined（不污染通用元素 surface）。**门禁全绿**：fmt clean / workspace clippy 零警告 / `make test` **13463/0/74**（R2834 13462 + 1 新测试，零回归）/ `make product-smoke` welcome desktop **17.03%**（精确 baseline 持平，纯 JS shim 无 layout/render .rs 改动）+ 全 8 struct PASS。**已知限制（记录）**：`instanceof Audio`=false（Proxy）；headless 无真播放（play 仅 resolve 不发声）；media 时长/音量属性未实现（defer，需 media 解码 infra）；canPlayType 恒 ''；无真 media 事件（canplay/ended 不触发）。**门禁**：.rs（engine shim+测试）+ 两 master doc；pre-commit guard PASS。**下一轮**：缺失 Web API 纯 JS tractable 表面已穷尽，剩余全深/host-layer（elementFromPoint / customElements lifecycle / Shadow DOM / node.normalize / 真 WAAPI / media 时长属性 / 真 files 上传 / fetch 非 GET〔net 跳过〕）或极窄（valueAsNumber / option.index〔需 host 父-select 索引回调〕/ scrollIntoViewIfNeeded / pointer lock）；rendering-compat 侧续降频守成（held baseline 13463 全绿）。**

> **📍 R2834（2026-08-06）🔧 `new Image()` ctor 增强 → 真 img 元素 land（图片预加载 + DOM 挂载 / 缺失 Web API 续，主线在 zero-web）：承接 R2833。probe 发现 `new Image()` 已 land 但为低质 stub——返 plain object（非 DOM 元素，无 tagName，`appendChild(new Image())` 失效）。**高频 + WPT 影响**：图片预加载 + DOM 挂载；上游 WPT css-images/css-backgrounds/content-visibility 多 fixture 经 `new Image()` 构造（旧 stub 致图不挂载）。**land**：改 `new Image(w,h)` 返 createElement('img') proxy（镜像 Option R2832），设 width/height 内容属性，允许 new 与无 new。**行为变化（improvement）**：旧 plain object onload 设直接属性（读回 fn）；新 proxy 经 set-trap 写 onload 属性（get-trap 不反射 on* 读回——element proxy 既有限制，非 Image 特有；on* 读回非真实需求，settable 不抛即满足；headless 无真图加载故 onload/onerror 不触发）。**门禁全绿**：fmt clean / workspace clippy 零警告 / `make test` **13462/0/74**（R2833 13461 + 1 新测试，零回归）/ `make product-smoke` welcome desktop **17.03%**（精确 baseline 持平，纯 JS shim 无 layout/render .rs 改动）+ 全 8 struct PASS。**已知限制（记录）**：`instanceof Image`=false（Proxy）；headless 无真图加载（onload/onerror 不触发）；on* 读回 element proxy 既有限制。**门禁**：.rs（engine shim+测试）+ 两 master doc；pre-commit guard PASS。**下一轮**：缺失 Web API 纯 JS tractable 表面已穷尽，剩余全深/host-layer（elementFromPoint / customElements lifecycle / Shadow DOM / node.normalize / 真 WAAPI / 真 files 上传 / fetch 非 GET〔net 跳过〕）或极窄（valueAsNumber / option.index / scrollIntoViewIfNeeded / pointer lock / new Audio）；rendering-compat 侧续降频守成（held baseline 13462 全绿）。**

> **📍 R2833（2026-08-06）🔧 document 集合完整性 + 正确性 land（embeds/plugins/anchors 补缺 + links 修正 + 迭代 has trap / 缺失 Web API 续，主线在 zero-web）：承接 R2832。probe 发现 forms/images/scripts/links 已 land（`_liveQueryCollection`），但 embeds/plugins/anchors 缺、links 错误（返全部 `<a>` 含 name-only 锚，spec 仅 a[href]+area[href]）、且 `_liveQueryCollection` Proxy 缺 has trap 致 `Array.prototype.map/forEach.call(coll)` 迭代得空（HasProperty 判索引存在性，{length:0} target 恒判 absent）。**land**：① `_liveQueryCollection(sel)` 扩展 sel 支持单串或数组（多 tag 逐选择器 concat——querySelectorAll 顶层不支持逗号列表）+ 抽 snapshot() + 加 has trap（数值索引经 snapshot 判 [0,len)）；② links 改 `['a[href]','area[href]']`（spec 修正）；③ 新增 embeds/plugins（`['embed','object']`）+ anchors（`'a[name]'`）。**门禁全绿**：fmt clean / workspace clippy 零警告 / `make test` **13461/0/74**（R2832 13460 + 1 新测试，零回归）/ `make product-smoke` welcome desktop **17.03%**（精确 baseline 持平——_liveQueryCollection 增 has trap 经 welcome smoke 证零回归）+ 全 8 struct PASS。**已知限制（记录）**：has trap 每次索引判定 re-query（大集合 O(n²) 查询，典型集合可接受）；`.namedItem` 未实现（legacy）；非真 live（属性级 re-query 非 mutation 驱动）。**门禁**：.rs（engine shim+测试）+ 两 master doc；pre-commit guard PASS。**下一轮**：续缺失 Web API 纯 JS tractable 表面已穷尽，剩余全深/host-layer（elementFromPoint / customElements lifecycle / Shadow DOM / node.normalize / 真 WAAPI / 真 files 上传 / fetch 非 GET〔net 跳过〕）或极窄（valueAsNumber / option.index / scrollIntoViewIfNeeded / pointer lock / new Audio / Image 增强）；rendering-compat 侧续降频守成（held baseline 13461 全绿）。**

> **📍 R2832（2026-08-06）🔧 `new Option()` + `select.add()` + HTMLOptionElement 读属性 land（动态 select 填充表面 / 缺失 Web API 续，主线在 zero-web）：承接 R2831（input.indeterminate，主线 = 父目标 zero-web P1a DOM/JS Bridge，本 goal 降频守成）。land 动态 select 填充表面——`new Option(text,value,defaultSelected,selected)` 构造器 + `select.add(el,before?)` + `option.text`/`label`/`defaultSelected`/`selected` 读（级联 select / 动态下拉表单高频）。**handle-based has-attr 缺口修复**：句柄元素（createElement 创建）不在 HTML 快照，sel-based `__zw_has_attr` 对其恒 false——原 option.selected/defaultSelected 对 `new Option()` 创建 option 恒 false（既有 documented 限制）；新增 host `__zw_has_attr_handle` 回调 + `has_attr_from_mutations` helper（扫 `SetAttrOnHandle` 按名判定存在性），selected/defaultSelected 读 handle 走之、sel 走 `__zw_has_attr`。**门禁全绿**：fmt clean / workspace clippy 零警告 / `make test` **13460/0/74**（R2831 13459 + 1 新测试，零回归）/ `make product-smoke` welcome desktop **17.03%**（精确 baseline 持平，纯 additive 新 global + option 读 + 新回调不改既有 JS 分支）+ 全 8 struct PASS。**已知限制（记录）**：`instanceof Option`=false（shim 返 Proxy）；handle has-attr 按 SetAttrOnHandle 名判定（无 RemoveAttrOnHandle 变体，setAttribute 空值仍判 present，boolean 属性正确）；select.add before 仅 selector-based。**门禁**：.rs（engine shim+bridge+测试）+ 两 master doc；pre-commit guard PASS。**下一轮**：续缺失 Web API 纯 JS tractable 表面已穷尽（R2820-R2832 主表面全覆盖），剩余全深/host-layer（elementFromPoint / customElements lifecycle / Shadow DOM / node.normalize / 真 WAAPI timeline / 真 files 上传 / fetch 非 GET〔net 跳过〕）或极窄（valueAsNumber / option.index / scrollIntoViewIfNeeded / pointer lock）；rendering-compat 侧续降频守成（held baseline 13460 全绿）。**

> **📍 R2786（2026-08-06）🔧 document.cookie get/set land（缺失 Web API 续 / 状态存储）：承接 R2785（CSS namespace）。probe 确认 document.cookie 缺失（auth / 偏好状态常见）。**纯 JS in-JS 存储**（不接真 cookie jar——网络/origin 集成属 host-layer defer；set-then-read 常见模式 tractable）。**land**：document 对象加 `cookie` accessor——getter 序列化 store 为 `"n=v; n=v"`（仅 name=value 无属性）；setter 解析 `"n=v; Path=...; Max-Age=..."` 取首 `name=value`（split 首 '='，value 保留后续 '='）存/覆盖（name 唯一）；无 '=' 串忽略。模块级 `_doc_cookies`（name→value）per-上下文。test 验 初始空 + set 带属性（仅取 n=v）+ 多 cookie + 同名覆盖 + value 含 '='（保留）+ 无 '=' 忽略。**门禁全绿**：fmt clean / workspace clippy 零警告（修 `assert_eq!(...,false)` → `assert!(!...)` 避 bool_assert_comparison lint）/ `make test` **13414/0/74**（R2785 13413 + 1 新测试，零回归；held baseline 自 R2638 的 13193 推进至 13414）/ `make product-smoke` welcome desktop **17.03%**（精确 baseline 持平，纯 additive 新 accessor）+ 0 struct FAIL / 8 PASS。**已知限制（记录）**：in-JS 存储（不接 zero-net cookie jar / 不随 fetch 发送 / 无 origin 隔离 / 无 expiry 淘汰 / 不消费 Set-Cookie 响应头——网络集成 host-layer defer）；不识别属性名（`Path=/` 当 cookie 存，spec 浏览器识别属性名——罕见误存）。**门禁**：.rs（engine shim+测试）+ 两 master doc；pre-commit guard PASS。**下一轮**：续缺失 Web API——ResizeObserver·IntersectionObserver 已 REAL 复核 / Promise.any·allSettled V8 原生复核 / history.pushState·replaceState〔host-layer〕/ document.cookie 真 cookie jar 集成〔host-layer〕；rendering-compat 侧续降频守成（held baseline 13414 全绿）。

> **📍 R2785（2026-08-06）🔧 CSS namespace land（CSS.escape 选择器转义 + CSS.supports 特性检测）：承接 R2784（location 读侧 spec）。probe 确认 CSS namespace 缺失（escape/supports 均无）。**高频**（escape：库动态选择器构造；supports：modern progressive enhancement 特性检测）。**land**：① `CSS.escape(str)` 纯 JS（CSSOM escape 算法，本地 Chromium 150 oracle 锚定：特殊符 `\char` / 首数字 `\hex+space` / `-a` 直留 / 非 ASCII 直留 / 控制字符 `\hex+space` / 空串返空不抛）；② `CSS.supports(prop,val?)` 委托 host `__zw_css_supports`（Rust `css_supports`：两参 = known-property gate〔`PropertyRegistry::known_properties().contains`〕+ `apply_property_value_with_quirks` 成功即支持；单参 supports-condition 求值 `not`/括号/声明 `prop:val`；`and`/`or` 深嵌套 defer）。**TDD red→green**：初版 shim `!!__zw_css_supports(...)` 把返串 "0" 当 truthy 恒 true → 改 `=== '1'` 修正。test 验 escape 全 case（oracle 锚定）+ supports 两参（合法 true / 非法值·未知属性 false）+ 单参（括号/声明/not）。**门禁全绿**：fmt clean / workspace clippy 零警告 / `make test` **13413/0/74**（R2784 13412 + 1 新测试，零回归；held baseline 自 R2638 的 13193 推进至 13413）/ `make product-smoke` welcome desktop **17.03%**（精确 baseline 持平，纯 additive 新 global + 新回调不改既有 JS 分支）+ 0 struct FAIL / 8 PASS。**已知限制（记录）**：supports 的 `and`/`or` 深嵌套未实现（罕见，单声明/not/括号覆盖主流）；supports 语义近似「ZW 能 apply」（偏乐观，对 ZW 解析但部分实现的特性返 true，优于假阴性）；escape 对 NUL 等极边字符 hex 转义（chromium 显 U+FFFD，罕见不究）。**门禁**：.rs（engine bridge+shim+测试）+ 两 master doc；pre-commit guard PASS。**下一轮**：续缺失 Web API——`document.cookie` get-set（storage crate 可能有 cookie 存储）/ ResizeObserver·IntersectionObserver 已 REAL 复核 / Promise.any·allSettled V8 原生复核 / history.pushState〔host-layer〕；rendering-compat 侧续降频守成（held baseline 13413 全绿）。

> **📍 R2784（2026-08-06）🔧 location 读侧 spec 化（精度提升·非 host-layer）：承接 R2783（BroadcastChannel）。probe 发现 location 读侧用 `_parseLocation`（朴素 regex：无 percent-encoding/IDNA/默认端口归一/端口解析）；写侧 assign/replace/reload 真 navigation 属 host-layer（无 JS→nav 回调）defer。**本轮读侧 spec 化**：`_parseLocation` 优先走 `new URL`（R2778，spec-correct）——仅 `__zw_parse_url` 已注册时；否则/失败回退既有 regex（reftest/裸 sandbox 零回归）。**附带修正**：`location.href`/`toString` 旧返**原始** `__zw_get_page_url` 串（如含 :443），现经 `_parseLocation().href` 返**规范化的** URL（默认端口归一，与 browser 一致）。test 验注册两回调后 location 全组件 spec-correct（默认端口 443 归一：host=example.com 非 example.com:443）+ href/toString canonical。**门禁全绿**：fmt clean / workspace clippy 零警告 / `make test` **13412/0/74**（R2783 13411 + 1 新测试，零回归；held baseline 自 R2638 的 13193 推进至 13412）/ `make product-smoke` welcome desktop **17.03%**（精确 baseline 持平——location 读侧变更经 welcome smoke 证零回归）+ 0 struct FAIL / 8 PASS。**已知限制（记录）**：location.assign/replace/reload 触发真导航需 host-layer nav 接线（JS→nav 回调 + renderer/browser 消息契约，defer，等点名或下轮 host-layer）；href/toString 每次访问经 new URL 解析（O(parse)，常见用法可接受）；无 __zw_parse_url 时回退 regex（旧精度，零回归）。**门禁**：.rs（engine shim+测试）+ 两 master doc；pre-commit guard PASS。**下一轮**：续缺失 Web API——CSS namespace（CSS.supports 特性检测 + CSS.escape 选择器转义，均缺失，中频）/ `document.cookie` get-set / Promise.any·allSettled V8 原生复核 / location 写侧 host-layer nav（深，等评估）；rendering-compat 侧续降频守成（held baseline 13412 全绿）。

> **📍 R2783（2026-08-06）🔧 BroadcastChannel land（缺失 Web API 续 / 同源广播）：承接 R2782（MessageChannel）。probe 确认 BroadcastChannel 缺失（同源广播通信，pub-sub 模式）。**纯 JS tractable**（复用 EventTarget R2779 + structuredClone R2773 + queueMicrotask R2782 + MessageEvent R2782）。**land**：BroadcastChannel（extends EventTarget，postMessage 经 structuredClone 深拷贝 + queueMicrotask 异步派发到所有**同名其他**实例——**sender 不收自己** spec 一致；name 属性；close 移出注册表；onmessage setter）；模块级 `_bc_registry`（name → channel 数组）同 JS 上下文共享。test 验 typeof/name/instanceof + 三实例广播（sender 不收自己）+ structuredClone 发时克隆 + 跨 name 无串扰 + close 移出不再收。**门禁全绿**：fmt clean / workspace clippy 零警告 / `make test` **13411/0/74**（R2782 13410 + 1 新测试，零回归；held baseline 自 R2638 的 13193 推进至 13411）/ `make product-smoke` welcome desktop **17.03%**（精确 baseline 持平，纯 additive 新 global）+ 0 struct FAIL / 8 PASS。**已知限制（记录）**：仅同 JS 上下文广播（跨 worker/进程广播需 host 接线，defer）；sender 不收自己（spec 一致）；派发经 microtask 非 task（sandbox 无独立 task 队列）。**门禁**：.rs（engine shim+测试）+ 两 master doc；pre-commit guard PASS。**下一轮**：续缺失 Web API——location 读侧 spec 化（`_parseLocation` 朴素 regex→`new URL` 提精度，写侧 host-layer defer）/ ResizeObserver·IntersectionObserver 已 REAL 复核 / Promise.any·allSettled V8 原生复核 / `document.cookie`；rendering-compat 侧续降频守成（held baseline 13411 全绿）。

> **📍 R2782（2026-08-06）🔧 MessageChannel + MessagePort + MessageEvent land（缺失 Web API 续 / 双端口通信）：承接 R2781（matchMedia）。probe 确认 MessageChannel/MessagePort/MessageEvent 全缺失（部分库经 MessagePort 做结构化通信 / postMessage 协议）。**纯 JS tractable**（依赖 EventTarget R2779 + structuredClone R2773 + queueMicrotask R2774）。**land**：① MessageEvent（extends Event，加 data/origin/source/ports）；② MessagePort（extends EventTarget，postMessage 经 structuredClone **深拷贝** + queueMicrotask **异步**派发 'message' 事件到配对端口——spec 为 task，sandbox 经 execute 末 microtask checkpoint 派发下 execute 可读；onmessage 属性 setter 内部走 addEventListener('message')；start/close/close 断开配对）；③ MessageChannel（双端口互连 port1/port2）。test 验 instanceof + port1→port2/post2→port1 双向异步派发 + structuredClone 发时克隆（后续 mutate 原对象不影响）+ MessageEvent 字段（instanceof/ type/source）+ close() 停止派发。**门禁全绿**：fmt clean / workspace clippy 零警告 / `make test` **13410/0/74**（R2781 13409 + 1 新测试，零回归；held baseline 自 R2638 的 13193 推进至 13410）/ `make product-smoke` welcome desktop **17.03%**（精确 baseline 持平，纯 additive 新 global）+ 0 struct FAIL / 8 PASS。**已知限制（记录）**：无 transfer 列表（Transferable 移植，罕见用法）；同执行上下文端口对（跨 worker/进程通信需 host 接线，defer）；派发经 microtask 非 task（sandbox 无独立 task 队列，microtask checkpoint 近似异步语义）。**门禁**：.rs（engine shim+测试）+ 两 master doc；pre-commit guard PASS。**下一轮**：续缺失 Web API——location 读侧 spec 化（`_parseLocation` 朴素 regex→`new URL` 提精度，写侧 host-layer defer）/ BroadcastChannel（同源广播，类 MessageChannel）/ ResizeObserver·IntersectionObserver 已 REAL 复核 / Promise.any·allSettled V8 原生复核；rendering-compat 侧续降频守成（held baseline 13410 全绿）。

> **📍 R2781（2026-08-06）🔧 window.matchMedia land（缺失 Web API 续 / 响应式设计高频缺口）：承接 R2780（URL setter）。缺口扫描（V8Sandbox+shim typeof 批量核）发现 matchMedia 全局缺失（ResizeObserver/IntersectionObserver/MutationObserver/rAF/fetch/navigator/history/localStorage 均已在；Promise.any/allSettled V8 原生；location 真 navigation=host-layer 重活 defer）。**高频**（modern 响应式站点 gate 行为于 viewport：`(min-width:768px).matches` / orientation / prefers-color-scheme）。**方法论续 R2778/R2780**：host callback `__zw_match_media(query,w,h)` 委托既有 `zero_css_parser::media_query::{parse_media_query, evaluate_media_query, MediaContext}`（spec-correct：min/max-width/height、orientation、resolution、prefers-color-scheme；逗号 query list 取或）。**Rust 侧**：`match_media_to_json(query,w,h)` 纯函数（MediaContext::new + parse + any(eval)）+ register `__zw_match_media` 回调（JS 传 query + innerWidth/innerHeight）。**shim 侧**：`matchMedia(query)` 返 MediaQueryList（**extends EventTarget R2779**：media/matches + addEventListener('change') + legacy addListener/removeListener）。**已知限制（记录）**：change 事件需 host resize 跟踪派发（当前无，addListener 注册有效但不触发；matches 为查询时刻快照，spec 一致）；prefers-color-scheme 恒 light（无 dark-mode host 接入）。test 验 min/max-width @1280 + orientation（landscape/portrait）+ prefers-color-scheme（light/dark）+ 逗号 OR + viewport 覆盖 @500 + instanceof EventTarget + addListener/removeListener。**门禁全绿**：fmt clean / workspace clippy 零警告 / `make test` **13409/0/74**（R2780 13408 + 1 新测试，零回归；held baseline 自 R2638 的 13193 推进至 13409）/ `make product-smoke` welcome desktop **17.03%**（精确 baseline 持平，纯 additive 新 global + 新回调不改既有 JS 分支）+ 0 struct FAIL / 8 PASS。**门禁**：.rs（engine bridge+shim+测试）+ 两 master doc；pre-commit guard PASS。**下一轮**：续缺失 Web API——MessageChannel（postMessage 双端口，中频，纯 JS tractable）/ location 读侧完整化（`_parseLocation`→`new URL` 提精度，写侧 host-layer defer）/ ResizeObserver·IntersectionObserver 已 REAL 复核；rendering-compat 侧续降频守成（held baseline 13409 全绿）。

> **📍 R2780（2026-08-06）🔧 URL 组件 setter + 双向 searchParams 同步（URL 故事完整化）：承接 R2779（EventTarget）。补 R2778 URL 的只读限制——组件可写 + searchParams 双向同步。**高频**（SPA 路由〔pathname/hash setter〕/ 查询参数操纵 / URL 构造，现代前端核心）。**方法论续 R2778**：host callback `__zw_set_url_part(prev_href, part, value)` 委托 url crate 的 Url setters（set_scheme/set_host/set_path/set_query/set_fragment/set_username/set_password/set_port，spec-correct 含 percent-encoding/IDNA/默认端口归一）。**Rust 侧**：抽 `serialize_url`（parse_url_to_json + set_url_part 共用序列化）+ `set_url_part`（part 分派 + host:port 拆分 helper `set_url_host_and_port`）+ register `__zw_set_url_part` 回调。**shim 侧**：URL 改用 `_`-prefixed 内部字段 + accessor（Object.defineProperty 循环定义 10 组件 get/set + origin/searchParams 只读 get）；`_setPart(part,value)` 回调重解析 + `_load` 全字段重载；`_applySearchParams`（searchParams 变更 → search/href，直写字段不调 _setPart 故无递归）；searchParams 加 `_onchange` hook + `_zw_reinit`（set search/href → reinit searchParams 不触发 _onchange 故无递归）+ 提取 `_zw_usp_parse` helper。**无递归设计**：两方向同步经独立路径（params→`_applySearchParams` 直写；search→`_setPart`+reinit），`_zw_reinit` 不触发 `_onchange`，`_applySearchParams` 不调 `_setPart` → 无环。test（JS）验 pathname/hash/protocol/hostname/port setter + search→params + params→search（append/set 多次不爆栈/delete）+ href 整体替换 + 无效 href 抛 TypeError + 稳定实例；test（Rust）验 set_url_part 纯函数 + 非法 scheme/href 返空串**不 panic**（set_scheme Result 不 panic 验证）。**门禁全绿**：fmt clean / workspace clippy 零警告 / `make test` **13408/0/74**（R2779 13406 + 2 新测试，零回归；held baseline 自 R2638 的 13193 推进至 13408）/ `make product-smoke` welcome desktop **17.03%**（精确 baseline 持平）+ 0 struct FAIL / 8 PASS。**已知限制（记录）**：href setter 按绝对 URL 重解析（无 base 上下文，相对值失败抛 TypeError）；username/password setter 对 cannot-be-a-base URL 返空串抛 TypeError（spec 边角）；host setter 端口仅当 `:` 后全数字视为端口（IPv6 字面量未支持，罕见）。**门禁**：.rs（engine bridge+shim+测试）+ 两 master doc；pre-commit guard PASS。**下一轮**：续缺失 Web API——location 完整化（href 可写 + assign/replace/reload，host-layer 导航须核 nav bridge）/ Promise.any·allSettled 复核（V8 原生？）/ ResizeObserver·IntersectionObserver 已 REAL 复核；rendering-compat 侧续降频守成（held baseline 13408 全绿）。

> **📍 R2779（2026-08-06）🔧 EventTarget 独立构造器 + Event/CustomEvent spec-completeness：承接 R2778（URL）。probe 发现 ZW 既有 Event/CustomEvent/KeyboardEvent（`_makeEvent` 工厂）+ Element/document/window addEventListener，但缺：① EventTarget 独立构造器（库常用 `new EventTarget()`/`extends EventTarget` 做事件总线）；② Event spec 字段（composed/eventPhase/isTrusted/timeStamp/defaultPrevented）；③ `instanceof Event/CustomEvent`（构造器无原型链）。**高频**（事件是 DOM 交互核心，几乎所有真实站点依赖）。**本地 Chromium 150 oracle 锚定**。**低风险设计**（不破坏工作中的 dispatch 系统——`_makeEvent` 被 input 合成事件 + bubbling/capture dispatch 重度消费）：① `_makeEvent` **additive** 加 spec 字段（composed=false/eventPhase=0/isTrusted=false/timeStamp=performance.now|Date.now/defaultPrevented 公开）+ preventDefault 同步镜像公开 defaultPrevented（dispatch 读 _-prefixed 私字段不变，零行为变化）；② Event/CustomEvent/KeyboardEvent 构造器置 `[[Prototype]]` 使 instanceof 成立（dispatch 不依赖原型链，零影响）+ CustomEvent/KeyboardEvent.prototype 经 `Object.create(Event.prototype)` 建 Event 子类型链 + Event.prototype.initEvent legacy API；③ **新增 EventTarget 独立构造器**（self-contained listener map，add/removeEventListener/dispatchEvent，dispatch 设 target/currentTarget + 返 `!defaultPrevented` + stopImmediatePropagation 支持 + `class extends EventTarget` 可用）。test 验 Event spec 字段/instanceof/CustomEvent detail/preventDefault cancelable 语义/EventTarget typeof/dispatch 触发+target/返回值/removeEventListener/stopImmediatePropagation/class extends 模式。**既有 bubbling/capture/stop 事件测试作回归守卫全过**（dispatch 零破坏）。**门禁全绿**：fmt clean / workspace clippy 零警告 / `make test` **13406/0/74**（R2778 13405 + 1 新测试，零回归；held baseline 自 R2638 的 13193 推进至 13406）/ `make product-smoke` welcome desktop **17.03%**（精确 baseline 持平——事件系统变更经 welcome smoke 证零回归，welcome 用事件仍正常）+ 0 struct FAIL / 8 PASS。**已知限制（记录）**：EventTarget 仅 target 阶段（无 DOM 父链，capture listener 在 target 同 fire）；EventTarget 构造器允许无 new 调用（polyfill 宽容，chromium 严格抛——宽容更兼容旧 transpile）；composed/eventPhase 无真传播（EventTarget 无父链）；timeStamp 经 performance.now 或 Date.now 兜底（reftest 无 `__zw_performance_now` 走 Date.now）。**门禁**：.rs（engine shim+测试）+ 两 master doc；pre-commit guard PASS。**下一轮**：续缺失 Web API——location 完整化（assign/replace/reload + location.href 可写，host-layer 导航，须核导航 bridge）/ URL setter（组件可写 + searchParams 双向同步）/ Promise.allSettled 核（V8 原生复核）/ ResizeObserver·IntersectionObserver 已 REAL 复核；rendering-compat 侧续降频守成（held baseline 13406 全绿）。

> **📍 R2778（2026-08-06）🔧 URL 构造器 land（缺失 Web API 续 / WHATWG URL 解析，host callback 委托 url crate）：承接 R2777（AbortController）。probe 确认 URL 构造器全局缺失（ZW 有 URLSearchParams〔R2772〕无 URL）。**高频**（location.href 操纵 / fetch 相对 URL / 链接解析 / 路由库，几乎所有真实站点依赖）。**方法论升级**：前序纯 JS polyfill（R2770-R2777）适合自包含算法，但 URL 解析需完整 WHATWG state machine（base 解析 / percent-encoding / IDNA / 默认端口归一 / dot-segment 规范化）——**改用 host callback 委托 Rust `url` crate**（engine 已依赖，spec-correct de-facto 实现），零纯 JS 解析错误面。**land**：① 加 `parse_url_to_json(input, base) -> String` 纯函数（pub(crate)，url crate 解析 → JSON 串：protocol/username/password/hostname/port/host/origin/pathname/search/hash/href；失败返空串）；② `register_dom_callbacks` 注册 `__zw_parse_url(url, base)` 回调调之（生产接线，browser/renderer/reftest 共用 register_dom_callbacks 即覆盖）；③ shim `URL` 构造器（属性只读 + searchParams 复用 URLSearchParams + toString/toJSON + canParse 静态 + 允许无 new）；失败抛 TypeError（spec）。**本地 Chromium 150 oracle 锚定**各属性形态。test 注册回调（复用 production 纯函数）验 绝对 URL 全属性/href+toString+toJSON/searchParams get+has/base 绝对路径解析/scheme-relative 解析/端口默认归一（:80→省）/无效抛 TypeError/canParse true+false/无 new。**门禁全绿**：fmt clean / workspace clippy 零警告（中途修两处：① 误重复定义 json_str → 复用既有 json_str；② parse_url_to_json 误插 register_dom_callbacks doc 与 fn 间致 missing_docs 警告 → 重排 doc 依附）/ `make test` **13405/0/74**（R2777 13404 + 1 新测试，零回归；held baseline 自 R2638 的 13193 推进至 13405）/ `make product-smoke` welcome desktop **17.03%**（精确 baseline 持平，纯 additive 新 global + 新回调不改既有 JS 分支）+ 0 struct FAIL / 8 PASS。**已知限制（记录）**：组件无 setter（只读属性，set 须重新构造，defer）；searchParams 与 search 属性不双向同步（修改 searchParams 不回写 search，常见读用法不受影响）；URL.parse 静态（modern，返 URL|null）defer；opaque/non-special scheme origin 返 "null"（spec 一致）；reftest harness 若不调 register_dom_callbacks 则 new URL 抛 TypeError（同其他 __zw_ 回调，reftest 默认无 JS 回调路径不受影响）。**门禁**：.rs（engine bridge+shim+测试）+ 两 master doc；pre-commit guard PASS。**下一轮**：续缺失 Web API——Event/CustomEvent/EventTarget（DOM 事件基建，高频，须核既有 DOM 事件系统集成）/ URL setter（组件可写 + searchParams 双向同步，中频）/ location 完整化（assign/replace/reload，host-layer 导航）；rendering-compat 侧续降频守成（held baseline 13405 全绿）。

> **📍 R2777（2026-08-06）🔧 AbortController/AbortSignal land（缺失 Web API 续 / fetch 中止 + 异步流程控制）：承接 R2776（DOMException）。probe 确认 AbortController/AbortSignal 全局缺失（V8 embed 不提供，R2712 recon 误记 Real 实为 undefined——P1a fetch 中止能力缺口）。**高频**（现代 JS 库 cancel token 模式 / fetch AbortSignal / 异步取消协议，几乎所有新框架依赖）。**本地 Chromium 150 oracle 锚定**核确切行为：① fresh signal.aborted=false/reason=undefined；② abort() 无参 → reason 默认 AbortError DOMException（"signal is aborted without reason"）；③ abort(val) → reason 即 val（不包装）；④ 重复 abort() 静默 no-op（不抛）；⑤ addEventListener('abort') 在 abort 时触发回调；⑥ throwIfAborted() aborted 时抛 AbortError DOMException（未 aborted 不抛）；⑦ AbortSignal.abort(reason)/timeout(ms) 静态工厂。**land AbortController + AbortSignal**：signal.aborted/reason getter + abort(reason) + add/removeEventListener('abort') + dispatchEvent + throwIfAborted + AbortSignal.abort/timeout 静态；默认 reason 经 DOMException（R2776 依赖）。test 验 typeof/fresh 状态/signal+abort 访问器/abort() 默认 AbortError/abort(msg)+abort(DOMException)/addEventListener 触发/重复 abort no-op/throwIfAborted 抛/AbortSignal.abort 静态/timeout 存在/removeEventListener。**TDD red→green**：初版 throwIfAborted 断言 `e instanceof DOMException + '|' + e.name` 触 JS 运算符优先级 bug（`+` 优先于 `instanceof` 解析为 `instanceof (DOMException+'|')` → RHS 非对象抛错）——加括号 `(e instanceof DOMException)` 修正（测试代码 bug 非实现 bug，实现 red→green 一次过除此外）。**门禁全绿**：fmt clean / workspace clippy 零警告 / `make test` **13404/0/74**（R2776 13403 + 1 新测试，零回归；held baseline 自 R2638 的 13193 推进至 13404）/ `make product-smoke` welcome desktop **17.03%**（精确 baseline 持平，纯 additive 新 global 不改既有 JS 分支）+ 0 struct FAIL / 8 PASS。**已知限制（记录）**：signal 非真 EventTarget 子类（`instanceof EventTarget`=false，但 add/removeEventListener/dispatchEvent API 齐备——shim 无全局 EventTarget 基类，self-contained listener 列表）；AbortSignal.timeout(ms) 依赖 setTimeout 回调真触发（sandbox 事件循环须驱动定时器，本 polyfill 已接 setTimeout 但异步触发不在此断言）；fetch（GET-only，R2764 记）尚未消费 AbortSignal——controller land 后 fetch AbortSignal 集成待 net/bridge 层（深，需 fetch 扩 method/body 时一并）。**门禁**：.rs（engine shim+测试）+ 两 master doc；pre-commit guard PASS。**下一轮**：续缺失 Web API——URL 构造器（URL 解析：pathname/search/hash/origin，location.href 操纵高频，纯 JS 但须完整 WHATWG URL 解析）/ Event/CustomEvent/EventTarget（DOM 事件基建，高频，须与既有 DOM 事件系统集成核验）/ Promise.allSettled 核（应为 V8 原生，probe 复核）；rendering-compat 侧续降频守成（held baseline 13404 全绿）。

> **📍 R2776（2026-08-06）🔧 DOMException land + 已 land API 错误类型 spec-aligned 升级：承接 R2775（getRandomValues）。probe 确认 DOMException 全局缺失（V8 embed 不提供，同 atob/structuredClone 谱）。**高频 + 高 spec-aligned 价值**——DOMException 是 Web IDL 异常基类型，众多 Web API（fetch/storage/crypto/atob/structuredClone）抛出它，各 API 用 name 子类区分语义；land 后可回头升级已 land API 的错误类型。**本地 Chromium 150 oracle 锚定**（`--headless --dump-dom` 写 DOM 法，R2754+ 解锁的方法论）核确切行为：① DOMException(msg,name) → name/message/code；无 name 参数时 name='Error'/code=0（非 'DOMException'）；② name→legacy code 映射（InvalidCharacterError=5 / DataCloneError=25 / QuotaExceededError=22 / NotSupportedError=9 / SecurityError=18 等 22 个 legacy name）；③ 25 个 legacy 常量（INDEX_SIZE_ERR=1..DATA_CLONE_ERR=25）；④ instance **非 Error 子类**（浏览器 DOMException 亦非 Error 子类，prototype 继承 Object 非 Error）；⑤ toString="name: message"（message 空时仅 name）。**land `DOMException` 构造器**：plain prototype（非 Error）+ name/message/code 属性 + 22 legacy name→code 表 + 25 legacy 常量 + 允许无 new 调用。**升级 3 处已 land API 错误类型（spec-correct，oracle 锚定）**：① btoa 非 Latin-1 → InvalidCharacterError DOMException（自裸 Error）；② getRandomValues >65536 → QuotaExceededError DOMException（自 RangeError）；③ structuredClone function/symbol → DataCloneError DOMException（自 TypeError）。**保持**：getRandomValues 非 TypedArray → TypeError（oracle 亦为 plain TypeError 非 DOMException，spec 一致）；queueMicrotask 非 callable → TypeError（spec）。test 验 DOMException 本体（typeof/name/message/code/toString/无参默认/instanceof 非 Error/legacy 常量/name→code/无 new）+ 3 处升级验收（btoa InvalidCharacterError + getRandomValues QuotaExceededError code=22 + structuredClone DataCloneError code=25）；既有 R2770/R2773/R2775 错误测试全用 try/catch 通配不破。**门禁全绿**：fmt clean / workspace clippy 零警告 / `make test` **13403/0/74**（R2775 13402 + 1 新测试，零回归；held baseline 自 R2638 的 13193 推进至 13403）/ `make product-smoke` welcome desktop **17.03%**（精确 baseline 持平，错误类型升级不改 welcome JS 控制流——welcome 不触发这些错误）+ 0 struct FAIL / 8 PASS。**已知限制（记录）**：无 .stack 属性（V8 Error 有 stack，DOMException polyfill 未接；罕见依赖）；错误 message 文本非 Chromium 精确措辞（name/code spec-correct 已足够，message 文本浏览器各异且非程序依赖）；非标准/未来 name 的 code=0（spec 一致）。**门禁**：.rs（engine shim+测试）+ 两 master doc；pre-commit guard PASS。**下一轮**：续缺失 Web API——AbortController（fetch 中止 / 异步流程控制，中频，纯 JS signal 可行）；或 Event/CustomEvent/EventTarget（DOM 事件基建，高频，但需与既有 DOM 事件系统集成核验）；或 URL 构造器（URL 解析，location 高频）；rendering-compat 侧续降频守成（held baseline 13403 全绿）。

> **📍 R2775（2026-08-06）🔧 crypto.getRandomValues land（缺失 Web API 续 / crypto 完整化）：承接 R2774（queueMicrotask）。probe 续 crypto 缺口——getRandomValues（TypedArray 字节填充）缺失，randomUUID 既 land（R2770）。**高频**（随机字节 / 测试 fixture / nonce / 随机化算法）。land `crypto.getRandomValues(typedArray)`：spec 限 TypedArray（`ArrayBuffer.isView` 校验，非 TypedArray 抛 TypeError）+ ≤65536 字节（超抛 RangeError，spec §8.2.1）；填**底层字节 buffer**（`new Uint8Array(arr.buffer, arr.byteOffset, arr.byteLength)` 视图）→ 任意 typed 视图（Int8..Uint32 / Float32 等 / 共享 buffer 偏移）得随机值；返同一数组（spec 同对象引用）。Math.random 字节级（**非 CSPRNG**，同 randomUUID 一致，对测试/nonce/非安全随机足够；安全敏感场景〔token/密钥〕需 host OS-random〔getrandom crate〕接入，届时 randomUUID + getRandomValues 一并升级 CSPRNG）。test 验 返回同一数组 + 长度不变 + 字节 ∈[0,256)（Uint8Array）+ Uint32Array 经字节填充得随机 Uint32 ∈[0,2^32) + >65536 字节抛 RangeError。**门禁全绿**：fmt clean / workspace clippy 零警告 / `make test` **13402/0/74**（R2774 13401 + 1 新测试，零回归；held baseline 自 R2638 的 13193 推进至 13402）/ `make product-smoke` welcome desktop **17.03%**（精确 baseline 持平，纯 additive 新 global 不改既有 JS 分支）+ 全 struct PASS。**已知限制（记录）**：Math.random-based（**非 CSPRNG**）；Float32/Float64 填随机字节可能产生 NaN/denormal（spec 同样行为，罕见用法）；BigInt64/BigUint64 经字节填充亦正确（ArrayBuffer.isView true，未单测但字节路径覆盖）。**门禁**：.rs（engine shim+测试）+ README.md（主线切换文档纠偏：页面 JavaScript→当前主线 / 渲染兼容性→降频守成，反映 2026-08-04 裁决）+ 两 master doc；pre-commit guard PASS。**下一轮**：续缺失 Web API——DOMException（probe 缺失，atob/structuredClone/getRandomValues 抛错现用 TypeError/RangeError 替，DOMException 更 spec-aligned，中频，且可回头升级已 land API 的错误类型）；或 AbortController（fetch 中止 / 异步流程控制）；rendering-compat 侧续降频守成（held baseline 13402 全绿）。

> **📍 R2774（2026-08-06）🔧 queueMicrotask land（缺失 Web API 续）：承接 R2773（structuredClone）。re-probe 发现 **queueMicrotask 全局缺失**（V8 embed 未暴露，probe 确认 undefined；R2712 recon 误记「V8 原生」实为 undefined）——**高频**（每个异步库/polyfill/框架都用）。land `globalThis.queueMicrotask` = `Promise.resolve().then(cb)` polyfill（V8 execute 末 perform_microtask_checkpoint 派发，同 spec「当前 task 末下 task 前」语义）；非 callable 抛 TypeError。**附带回益**：shim 内部 `_defer`（MutationObserver/requestIdleCallback fallback）已检查 `typeof queueMicrotask==='function'` 走真分支——本轮定义后 _defer 从 Promise.then fallback 切到真 queueMicrotask（行为等价，均 Promise.then microtask，零变化）。fetch bridge 侧核：`__zw_fetch(id,url)` 仅 GET（无 method/body）——POST/PUT 需扩 bridge + net 层，defer（real-website 高频但非纯 JS）。test 验 typeof function + callback 在 execute 末派发（下 execute 可读）+ 非 callable 抛。**门禁全绿**：fmt clean / workspace clippy 零警告 / `make test` **13401/0/74**（R2773 13400 + 1 新测试，零回归；held baseline 自 13193 推进至 13401）/ `make product-smoke` welcome desktop **17.03%**（精确 baseline 持平）+ 全 struct PASS。**门禁**：.rs（engine shim+测试）+ 两 master doc；pre-commit guard PASS。**下一轮**：续缺失 Web API——DOMException（probe 缺失，atob/structuredClone 抛错用 TypeError 替，DOMException 更 spec-aligned，中频）；或 crypto.getRandomValues（TypedArray 填充，需 CSPRNG 决策）；或 fetch POST（需 net 层，real-website 高频）；rendering-compat 侧续降频守成（held baseline 13401 全绿）。

> **📍 R2773（2026-08-06）🔧 structuredClone land（缺失 Web API 续）：承接 R2772（URLSearchParams）。续缺失 Web API 子线程——land **structuredClone**（深拷贝，postMessage / React state / immer-like 高频）。递归 `_zw_structured_clone`：primitive 原样 / array+plain object 逐属性深拷贝（保留 prototype）/ Date（getTime 重建）/ RegExp（source+flags 重建）/ Map（键值递归）/ Set（值递归）/ ArrayBuffer（byte 复制）/ TypedArray+DataView（copy 构造）；**循环引用经 WeakMap 记忆**不爆栈（seen.set 先于子属性递归）；function/symbol 抛 TypeError（spec DataCloneError 语义）。test 验 primitive/嵌套对象独立/数组独立/Date 保类型/RegExp flags/循环引用 self-ref 解到 clone 自身/function 抛。**门禁全绿**：fmt clean / workspace clippy 零警告 / `make test` **13400/0/74**（R2772 13399 + 1 新测试，零回归；held baseline 自 13193 推进至 **13400** 跨门槛）/ `make product-smoke` welcome desktop **17.03%**（精确 baseline 持平，纯 additive 新 global）+ 全 struct PASS。**已知限制（记录）**：symbol-keyed 属性不拷（Object.keys 仅 string-keyed，罕见）；class 实例 prototype 保留但构造器不重跑（同 spec 平台对象外行为）；Error/Blob/File 等平台对象未专项处理（走 plain-object 路径，常见 state 足够）。**门禁**：.rs（engine shim+测试）+ 两 master doc；pre-commit guard PASS。**下一轮**：续缺失 Web API——crypto.getRandomValues（TypedArray 填充，crypto 完整化，可 CSPRNG via host）；或转 fetch POST/其他 HTTP method（real website 高频，但需 net 层）；或 AbortController（fetch 中止）；rendering-compat 侧续降频守成（held baseline 13400 全绿）。

> **📍 R2772（2026-08-06）🔧 URLSearchParams land（缺失 Web API 续）：承接 R2771（TextEncoder/TextDecoder）。续缺失 Web API 子线程——land **URLSearchParams**（query string 解析/序列化，location.search / fetch query 高频）。纯 JS（V8 原生 encodeURIComponent/decodeURIComponent + Symbol.iterator）。constructor 支持 string（`?` 前缀可省）/ 对象 / [k,v] 可迭代；方法 append/delete（含 value 参数）/get/getAll/has（含 value 参数）/set/sort/forEach/entries/keys/values/toString；`[Symbol.iterator]`=entries 使实例可 `for...of`。application/x-www-form-urlencoded 语义：space↔`+`。**实现关键**：entries/keys/values 返**真迭代器**（`_zw_iter` helper：next() 返 {value,done} 并自增游标 + [Symbol.iterator] 返 self）——初版误返裸数组致 for...of 调 array.next() 抛错，后修正（TDD red 暴露迭代协议细节）。**已知限制（记录）**：`size` getter 未实现（rare）；`entries/keys/values` 返迭代器非数组（spec 一致，但 `[...x.entries()]` spread 仍可用因迭代器有 [Symbol.iterator]）。test 验 构造+get/getAll/has/append/set/delete/toString round-trip/对象构造/for...of。**门禁全绿**：fmt clean / workspace clippy 零警告 / `make test` **13399/0/74**（R2771 13398 + 1 新测试，零回归；held baseline 自 13193 推进至 13399）/ `make product-smoke` welcome desktop **17.03%**（精确 baseline 持平，纯 additive 新 global）+ 全 struct PASS。**门禁**：.rs（engine shim+测试）+ 两 master doc；pre-commit guard PASS。**下一轮**：续缺失 Web API——structuredClone（深拷贝，postMessage/state 高频，JSON-able 子集 tractable）/ crypto.getRandomValues（TypedArray 填充，crypto 完整化）；或转 fetch POST/其他 HTTP method；rendering-compat 侧续降频守成。

> **📍 R2771（2026-08-06）🔧 TextEncoder/TextDecoder UTF-8 land（缺失 Web API 续）：承接 R2770（atob/btoa/crypto.randomUUID）。续缺失 Web API 子线程——land **TextEncoder（str→UTF-8 Uint8Array）+ TextDecoder（bytes→str）**（fetch body / 字符串↔字节互转高频）。纯 JS UTF-8 算法：encode 处理 ASCII（1B）/ BMP（2-3B）/ astral 代理对（4B）；decode 多字节重组 + astral→代理对，非法序列替 U+FFFD（fatal=false 容错）。constructor + prototype（`new TextEncoder()` / `instanceof` 正确），TextEncoder.encodeInto 近似（read/written）。**已知限制（记录）**：仅 UTF-8（TextEncoder spec 恒 utf-8；TextDecoder 标签忽略恒按 utf-8 解，非 utf-8 label 不支持）；fatal=false 容错（spec fatal=true 抛错未实现）。test 验 encoding='utf-8' + encode('ZeroWeb')=7 ASCII 字节 + 中文 3B/字 + 字面字节 decode + round-trip 保真（含中文混排）。**门禁全绿**：fmt clean / workspace clippy 零警告 / `make test` **13398/0/74**（R2770 13397 + 1 新测试，零回归；held baseline 自 13193 推进至 13398）/ `make product-smoke` welcome desktop **17.03%**（精确 baseline 持平，纯 additive 新 global）+ 全 struct PASS。**门禁**：.rs（engine shim+测试）+ 两 master doc；pre-commit guard PASS。**下一轮**：续缺失 Web API——URLSearchParams（query 解析高频，纯 JS tractable）/ structuredClone（深拷贝，postMessage/state 高频）/ crypto.getRandomValues（TypedArray 填充，crypto 完整化）；择最高频 tractable 续 land；rendering-compat 侧续降频守成。

> **📍 R2770（2026-08-06）🔧 atob/btoa + crypto.randomUUID land（缺失 Web 平台 API 补齐）：承接 R2769（rAF slice 收尾）。probe（V8Sandbox+shim typeof 扫）确认 V8 不提供一组高频 Web API（全 undefined）：crypto/atob/btoa/structuredClone/TextEncoder/TextDecoder/URLSearchParams/AbortController（Promise.allSettled 为 V8 原生无需）。择**最高频 + 最 tractable + 纯 JS 自包含**两项 land（零 host 回调/零新依赖）：① **atob/btoa**（Base64 编解码，data: URL/JWT/二进制载荷高频）——纯 JS（ZW engine 无 base64 crate，复用 fetch `_b64decode` 同款算法），btoa 对 >255（非 Latin-1）抛 InvalidCharacterError（spec），atob 容错（忽略空白/padding，best-effort）；② **crypto.randomUUID**（UUID v4，id 生成/analytics/React key 高频）——纯 JS（h 查表 + Math.random，version=4/variant∈89ab 格式）。**已知限制（记录）**：randomUUID 为 Math.random-based（**非 CSPRNG**），对 id 生成主流用途足够，安全敏感场景（token 生成）需 host OS-random 接入（follow-up）；getRandomValues（TypedArray 填充）/ TextEncoder/Decoder / URLSearchParams / structuredClone defer（TypedArray/UTF-8/深拷贝更多工作）。test 验 round-trip + btoa 抛 + UUID v4 正则 + 唯一性。**门禁全绿**：fmt clean / workspace clippy 零警告 / `make test` **13397/0/74**（R2769 13396 + 1 新测试，零回归；held baseline 自 13193 推进至 13397）/ `make product-smoke` welcome desktop **17.03%**（精确 baseline 持平，纯 additive 新 global 不改既有 JS 分支）+ 全 struct PASS。**门禁**：.rs（engine shim+测试）+ 两 master doc；pre-commit guard PASS。**下一轮**：续缺失 Web API——getRandomValues（crypto 完整化，TypedArray 填充）/ TextEncoder+TextDecoder（UTF-8，fetch body 高频）/ URLSearchParams（query 解析高频）/ structuredClone（深拷贝，postMessage/state 高频）；择最高频 tractable 续 land；rendering-compat 侧续降频守成。

> **📍 R2769（2026-08-06）🔧 rAF tick 接 performance.now() 真 timestamp（rAF slice 收尾）：承接 R2768（performance.now() land）。精读 rAF 设计 doc（p1a-event-loop-raf-slice-design-2026-08-05.md）核实现进度——**rAF frame-driven 基建 R2713a/b 全 land**（shim `_rafPending` 队列 + `__zw_raf_tick` + `__ZW_RAF_FRAME_DRIVEN` 分支 + renderer `tick_observers` 附带 tick + worker env 注入 + ON/OFF/cancel 单测，default-OFF 保 reftest 兼容）。**doc §8 三 follow-up 复盘**：① performance.now〔R2768 land〕② IO/RO 持续跟踪〔**R2714 既 land**，doc stale，IO threshold 越界再派发单测在〕③ rAF 真 timestamp〔本轮〕。**唯剩**：`page_scripts.rs:145` `tick_observers` 现 `__zw_raf_tick(0)`（传 0）。修：改 `__zw_raf_tick(globalThis.performance?performance.now():0)`——ON 路径 rAF 回调收真 DOMHighResTimeStamp（R2768 performance.now），performance 缺失兜底 0；OFF 路径 shim `__zw_raf_tick` 早返（line 586）零影响（product-smoke OFF 持平证）。+renderer worker 单测 `renderer_js_worker_performance_now_available`（验 renderer 上下文 performance.now 为 function 且非负，证 tick 真参数有效）。+设计 doc §8 标记 performance.now/IO-tracking/rAF-ts 三项 LANDED（doc stale 纠偏）。**门禁全绿**：fmt clean / workspace clippy 零警告 / `make test` **13396/0/74**（R2768 13395 + 1 新测试，零回归；held baseline 自 13193 推进至 13396）/ `make product-smoke` welcome desktop **17.03%**（精确 baseline 持平，OFF 路径零影响）+ 全 struct PASS。**门禁**：.rs（renderer page_scripts tick + js_worker 测试）+ 设计 doc + 两 master doc；pre-commit guard PASS。**下一轮**：rAF slice 全收尾（frame-driven 基建 + 真 ts 全就绪，default-OFF）；唯一剩余 §8 = default-on A/B 决策（需 reftest 改造代价评估，非自主）；转其他 P1a tractable 项（fetch POST/其他 HTTP method / Promise.allSettled / structuredClone 等常用 API gap 核查）；rendering-compat 侧续降频守成。

> **📍 R2768（2026-08-06）🔧 performance.now() land（P1a 事件循环 slice，pivot 自 getComputedStyle）：承接 R2767。先 spot-check 3 批高频 getComputedStyle 属性（font-weight/text-align/text-overflow/vertical-align/font-style/white-space/font-family/transform-origin/line-height/tab-size/animation/transition/word-break/text-wrap）核 diverge——**仅残余三类非 clean diverge**：① font-family 默认 ZW 空 vs Chromium "Times New Roman"（**platform/font-stack 依赖**，非通用 fix）；② transform-origin/width/height 百分比 Chromium 返 **used px**（**geometric，需 layout**，ZW gCS 不跑布局，fundamental 限制，已记）；③ font-variant/font-stretch ZW 不支持返 ""（unsupported feature）。**结论：getComputedStyle 序列化 spot-check surface 经 R2767 letter-spacing 后穷尽**，余全一致或非 clean。**pivot 事件循环**：Explore-agent 全量侦察 P1a 异步管线状态——fetch/timer/setTimeout/setInterval/requestIdleCallback/MutationObserver/microtask 队列全 REAL；rAF frame-driven PARTIAL（off by default，timestamp=0）；**performance.now() 完全缺失**（analytics/动画高频）。TDD red→green land **performance.now()**（DOMHighResTimeStamp）：register_dom_callbacks capture `Instant` time origin + 注册 `__zw_performance_now` 回调（elapsed ms，子毫秒 f64 串）；shim 加 `globalThis.performance.now`（host 回调优先，未注册走 Date.now() 兜底）。test 验 typeof number / 非负 / 单调非减。**门禁全绿**：fmt clean / workspace clippy 零警告 / `make test` **13395/0/74**（R2767 13394 + 1 新测试，零回归；held baseline 自 13193 推进至 13395）/ `make product-smoke` welcome desktop **17.03%**（精确 baseline 持平，纯 additive 新 global 不改既有 JS 分支）+ 全 struct PASS。**门禁**：.rs（engine bridge 回调+shim+测试）+ 两 master doc；pre-commit guard PASS。**下一轮**：续 P1a 事件循环——rAF frame-driven 激活 + 真 timestamp（设计 doc 既就绪 docs/goal/zero-web/p1a-event-loop-raf-slice-design-2026-08-05.md，依赖 performance.now〔本轮 land〕）；或 IO/RO scroll/resize 持续追踪（M）；rendering-compat 侧续降频守成。

> **📍 R2767（2026-08-06）🔧 getComputedStyle letter-spacing 0→normal diverge 修复（spot-check 高频属性发现）：承接 R2766。pivot 事件循环前对高频 getComputedStyle 属性做 spot-check（font-weight/letter-spacing/word-spacing/z-index/opacity/font-size）核 diverge——font-weight（400/700）/word-spacing 默认（0px）/z-index（auto）/opacity/font-size 全一致，**仅 letter-spacing 默认 diverge**：ZW 旧返 "0px"、Chromium 返 "normal"。根因：ZW `parse_letter_or_word_spacing`（parse_misc.rs:597）把 `normal`→`LengthValue::Px(0.0)`（layout 等价存储），default 亦 `Px(0.0)`，故序列化得 "0px"。**oracle 关键发现**：Chromium 对**所有 0 形** letter-spacing（默认 / normal / 显式 `0`/`0px`）恒归一为 "normal"（normal 与 0 layout 等价，getComputedStyle 归一），非 0 长度才 "Npx"。故 `Px(0.0)→"normal"` 精确对齐（非启发式——显式 `letter-spacing:0` 亦返 "normal"，无边角 diverge）。word-spacing 不归一（恒 "0px"，与 letter-spacing 行为不同，对齐 Chromium）。修：新增 `letter_spacing_to_css`（Px(0.0)→"normal"，否则 length_to_css），letter-spacing arm 改用之。oracle 锚定：默认/normal/0/0px→"normal"、2px→"2px"、word-spacing normal/0→"0px"。**门禁全绿**：fmt clean / workspace clippy 零警告 / `make test` **13394/0/74**（R2766 13393 + 1 新测试，零回归；held baseline 自 13193 推进至 13394）/ `make product-smoke` welcome desktop **17.03%**（精确 baseline 持平）+ 全 struct PASS。**methodology 价值**：又一处「storage 简化（normal→Px(0.0)）非序列化阻塞」——序列化层 `Px(0.0)→"normal"` 逆映射精确对齐（同 R2754/R2763 diverge 谱），零 layout/parse 改动。**门禁**：.rs（engine 序列化+测试）+ 两 master doc；pre-commit guard PASS。**下一轮**：spot-check 续（其他高频属性潜在 diverge：text-overflow/line-height-normal/font-family 等可续核）；或 pivot 回父目标 zero-web P1a 事件循环/fetch/Observer 真实化主线；rendering-compat 侧续降频守成。

> **📍 R2766（2026-08-06）🔧 getComputedStyle grid-template 简写 land（rows/columns/areas 三 longhand 重组）：承接 R2765（border-image 簇收尾）。续用本地 Chromium 150 oracle 提取确切串，TDD red→green land **grid-template 简写**（Option<String> 原始串三 longhand 重组）。oracle 揭示核心规则：① 全 none（rows/cols/areas 均 None）→ "none"；② **areas==none** → 恒 `"<rows> / <cols>"`（rows/cols 各自可 none，仅设列→`none / 1fr 1fr`、仅设行→`100px 200px / none`）；③ **areas!=none** → 引号区域串（`"a a" "b b"`）逐行交错进行尺寸（`<area0> <size0> <area1> <size1> ...`）+ ` / ` + cols，**仅当 area 数 == 行尺寸数可重组**（不等→返 "" 空串，Chromium 同样不可序列化）。新增 `split_grid_area_strings`（引号串拆分）+ `interleave_grid_template_areas`（计数匹配交错）两 helper。oracle 锚定：默认→"none"、`100px 200px / 1fr 1fr 1fr`→"100px 200px / 1fr 1fr 1fr"、仅列→"none / 1fr 1fr"、仅行→"100px 200px / none"、areas 三行→`"a a a" 50px "b b b" 1fr "c c c" 2fr / 1fr 1fr 1fr`。**门禁全绿**：fmt clean / workspace clippy 零警告 / `make test` **13393/0/74**（R2765 13392 + 1 新测试，零回归；held baseline 自 13193 推进至 13393）/ `make product-smoke` welcome desktop **17.03%**（精确 baseline 持平）+ 全 struct PASS。**已知 diverge（pre-existing，记录不阻塞）**：① repeat() ZW 不展开（apply.rs 直接 to_string，oracle 展开，R2740 既记 longhand 限制）；② line-name `[x]` 混入 rows 时本交错器按空白拆 size 会被 `[x]` 当一个 token（罕见，areas 形式少混 line-name）；③ areas 形式 cols 缺省时本实现拼 `... / none`（未 oracle 验证该极边，areas 形式惯例带 cols）。均 pre-existing/极边，序列化对常见形式正确。**门禁**：.rs（engine 序列化+测试）+ 两 master doc；pre-commit guard PASS。**下一轮**：续 getComputedStyle 残余简写（grid 全简写〔template+auto-flow+auto-rows/cols，复杂〕/ mask 多层简写〔类 background，未存储〕）多为深/未存储项；建议 pivot 回父目标 zero-web P1a 事件循环/fetch/Observer 真实化主线；rendering-compat 侧续降频守成。

> **📍 R2765（2026-08-06）🔧 getComputedStyle border-image 简写 land（border-image 簇收尾）：承接 R2764（4 longhand + slice 默认 diverge 修复既 land）。续用本地 Chromium 150 oracle 提取确切串，TDD red→green land **border-image 简写**（5 子分量 CSSOM 重组：source + slice[/width] + /outset + repeat）。oracle 揭示**远比预期简单**的核心规则：① **source==none 短路**——border-image-source==none → 整值 "none"，**不论其余 slice/width/outset/repeat 是否非初值**（设了 slice 但 source=none 仍返 "none"）；② **source!=none 恒全量**——不省任何初值，恒 `"<source> <slice> / <width> / <outset> <repeat>"`（slice↔width 一个 `/`、width↔outset 一个 `/`、outset↔repeat 空格）。复用 R2764 既 land 的 5 longhand helper（source/slice/width/outset/repeat），仅 `/`+空格拼接 + source None 短路，零新子分量逻辑。oracle 锚定（linear-gradient 源避 url() 相对/绝对 longhand 既存 diverge）：默认/仅 slice→"none"、source-only→"linear-gradient(-45deg, rgb(255, 0, 0), rgb(0, 0, 255)) 100% / 1 / 0 stretch"、全设→"... 10 fill / 20px / 5px round"、source+repeat round→"... 100% / 1 / 0 round"。**门禁全绿**：fmt clean / workspace clippy 零警告 / `make test` **13392/0/74**（R2764 13391 + 1 新测试，零回归；held baseline 自 13193 推进至 13392）/ `make product-smoke` welcome desktop **17.03%**（精确 baseline 持平）+ 全 struct PASS。**已知 diverge（pre-existing longhand，记录不阻塞）**：url() 源 ZW 存相对 vs Chromium 解析绝对（R2757 background 既记），本切片用 linear-gradient 源绕开。**border-image 簇（source + 4 longhand + 简写）全收官**。**门禁**：.rs（engine 序列化+测试）+ 两 master doc；pre-commit guard PASS。**下一轮**：getComputedStyle 序列化主体收官（R2704-R2765 系列，~38 属性 + 6 diverge）；残余全 host-layer（scroll-snap）/未存储（motion-path/mask-pos）/深简写（grid-template areas）；建议 pivot 回父目标 zero-web P1a 事件循环/fetch/Observer 真实化主线；rendering-compat 侧续降频守成。

> **📍 R2764（2026-08-06）🔧 getComputedStyle border-image 切片族 longhand land + border-image-slice 默认值 diverge 修复：承接 R2763。审 longhand 缺口（R2762 既记 border-image 切片族缺序列化），TDD red→green land **4 longhand** + **slice 默认值 diverge 修复**：① **border-image-slice**——4 值最小化（Number→`n`/Percent→`n%`）+ fill 真→末尾 ` fill`；② **border-image-width**——4 值最小化（Auto/Number/Length→px/Percent）；③ **border-image-outset**——4 值最小化（Number/Length→px）；④ **border-image-repeat**——水平/垂直 2 值（stretch/repeat/round/space，相等单值）；⑤ **★ slice 默认值 diverge 修复**——oracle 揭示默认 border-image-slice="100%"（CSS 初值 Percent），ZW 旧 default_impl 存 `Number(100.0)`（→"100" diverge）；核 paint 仅读 `.fill`（9-slice 采样未实现，border.rs:251），Number→Percent **paint-neutral**，修 default_impl 4 组件对齐 Chromium。oracle 锚定：默认 slice→"100%"、`10 20 30 40`→"10 20 30 40"、`10% fill`→"10% fill"、width 默认→"1"、outset 默认→"0"、repeat 默认→"stretch"。**门禁全绿**：fmt clean / workspace clippy 零警告 / `make test` **13391/0/74**（R2763 13390 + 1 新测试，零回归；slice 默认 Number→Percent 全 workspace 零回归，welcome smoke 持平）/ `make product-smoke` welcome desktop **17.03%**（精确 baseline 持平）+ 全 struct PASS。**注**：border-image **简写**（source+slice+width+outset+repeat 5 子分量复杂 CSSOM 重组）defer；本轮 4 longhand + slice 默认修复。**门禁**：.rs（engine 序列化+测试 / style-system default_impl slice Percent）+ 两 master doc；pre-commit guard PASS。**下一轮**：border-image 简写（5 子分量 CSSOM 重组，须 oracle 核各分量省初值/`/` 分隔规则）；或 pivot 回父目标 zero-web P1a 主线；rendering-compat 侧续降频守成。

> **📍 R2763（2026-08-06）🔧 getComputedStyle text-emphasis 簇 land（style char→keyword 逆映射 + color + position + 简写）：承接 R2762。续用本地 Chromium 150 oracle 提取确切串，TDD red→green land **CJK 文本强调簇**（3 longhand + 简写）：① **text-emphasis-style**——ZW 存解析后 `Char(char)`（标记字符），oracle 返 keyword 形（"dot"/"open circle"/"sesame"）；核 ZW `parse_text_emphasis_style` 用标准 CSS 字符表（parse_misc.rs:530，U+2022/U+25CF/U+25CB/U+FE45 等），故序列化**逆映射** char→keyword（10 标准字符→"filled 省/open 显 + shape"，非标准字符→`<string>` 引号化）；② **text-emphasis-color**——currentcolor→rgb；③ **text-emphasis-position**——over/under 恒显，left 显（right 初值省）；④ **text-emphasis 简写**——`<style> <color>`（恒双段）。oracle 锚定：`dot`→"dot"、`open circle`→"open circle"、`sesame`→"sesame"、`"*"`→"\"*\""、`under left`→"under left"、`text-emphasis:filled circle green`→"circle rgb(0, 128, 0)"。**门禁全绿**：fmt clean / workspace clippy 零警告 / `make test` **13390/0/74**（R2762 13389 + 1 新测试，零回归；held baseline 自 13193 推进至 13390）/ `make product-smoke` welcome desktop **17.03%**（精确 baseline 持平）+ 全 struct PASS。**methodology 价值**：ZW 的简化 Char 存储初看似阻塞（不可恢复 keyword），但核 parse 用标准 CSS 字符表后**逆映射可行**——又一处「storage 简化非序列化阻塞」实证（同 R2754/R2755 diverge 谱）。**门禁**：.rs（engine 序列化+测试）+ 两 master doc；pre-commit guard PASS。**下一轮**：getComputedStyle 常用属性序列化**全面收官**（R2704-R2763 系列，~33 属性 + 5 diverge）；残余全 host-layer（scroll-snap）/未存储（motion-path/mask-pos）/深简写（grid-template areas）；建议 pivot 回父目标 zero-web P1a 事件循环/fetch/Observer 真实化主线（2026-08-04 裁决活跃主线），getComputedStyle 转维护态；rendering-compat 侧续降频守成。

> **📍 R2762（2026-08-06）🔧 getComputedStyle backdrop-filter + text-underline-offset longhand 补齐（常用 longhand 缺口收尾）：承接 R2761（font 简写 + 简写主体收官）。审 getComputedStyle longhand 缺口，TDD red→green land **2 个高频 longhand**（既有 storage+parse 仅缺序列化）：① **backdrop-filter**——与 filter 同 `Vec<FilterComputedValue>`，复用 `filter_to_css`（空→none / 函数列表空格分隔 / saturate 百分比→数字），glass/frosted 效果高频；② **text-underline-offset**（CSS Text Decoration 4 §2.5）——`TextUnderlineOffsetValue` Auto→"auto" / Length→px。oracle 锚定：`backdrop-filter:blur(5px) saturate(180%)`→"blur(5px) saturate(1.8)"、`text-underline-offset:3px`→"3px"。**门禁全绿**：fmt clean / workspace clippy 零警告 / `make test` **13389/0/74**（R2761 13388 + 1 新测试，零回归；held baseline 自 13193 推进至 13389）/ `make product-smoke` welcome desktop **17.03%**（精确 baseline 持平）+ 全 struct PASS。**审 longhand 缺口结论**：残余缺 longhand（offset-path/distance/rotate〔motion path 未存储〕、scroll-behavior/snap-type/snap-align〔host-layer scroll defer〕、contain-intrinsic-size〔有 width/height longhand 已覆〕、text-emphasis-style/color/position〔有 storage 待补，下轮候选〕、mask-position/size/repeat/clip/origin〔未存储〕）多为 host-layer/未存储/niche，非高频查询；getComputedStyle **常用 longhand+简写序列化主体收官**（R2704-R2762 系列，~30 属性 + 5 diverge 修复）。**门禁**：.rs（engine 序列化+测试）+ 两 master doc；pre-commit guard PASS。**下一轮**：候选 = 补 text-emphasis-style/color/position longhand（有 storage，类 text-underline-offset 易补）；或 pivot 回父目标 zero-web P1a 事件循环/fetch/Observer 主线；rendering-compat 侧续降频守成。

> **📍 R2761（2026-08-06）🔧 getComputedStyle font 简写 land + line-height number→used px diverge 修复：承接 R2760。续用本地 Chromium 150 oracle 提取确切串，TDD red→green land **font 简写**（CSSOM 重组省初值）+ **line-height longhand diverge 修复**：① **font 简写**——`<style> <variant> <weight> <stretch> <size>[/<line-height>] <family>`，省初值（style normal / weight 400 / line-height normal）；font-variant/font-stretch ZW 不支持（恒初值→恒省略）；oracle 锚定 `font:italic bold 14px/1.5 Arial`→"italic 700 14px / 21px Arial"；② **★ line-height number→used px diverge 修复**——oracle 揭示 `line-height:1.5`（font-size 16px）Chromium 返 **"24px"**（used 值 = font-size×number），ZW 旧 `line_height_str(Number)` 返 **"1.5"**（spec computed 值）diverge。改 `line_height_str(Number(n))`→`format_num(font_size_px × n, "px")` 对齐 Chromium getComputedStyle used-value 语义（display-only 序列化层，layout 用 enum 不受影响，welcome smoke 持平证零渲染回归）。**门禁全绿**：fmt clean / workspace clippy 零警告 / `make test` **13388/0/74**（R2760 13387 + 1 新测试〔font 简写〕+ 1 旧测试 #k line-height 期望 1.5→24px 纠偏，零回归；held baseline 自 13193 推进至 13388）/ `make product-smoke` welcome desktop **17.03%**（精确 baseline 持平）+ 全 struct PASS。**已知 diverge（pre-existing，记录不阻塞）**：① 默认 font-family ZW 为空（vs Chromium "Times New Roman"）——longhand 层 diverge，序列化本身正确；② ZW `expand_font` 不解析**数值** font-weight（`font:700 ...` 把 700 当 size）——关键字 weight（bold/normal）正常；③ font-variant/stretch 不支持（含 `small-caps`/`condensed` 的 font 简写 diverge，罕见）。均 pre-existing parser 限制，序列化对支持的子集正确（测试覆盖关键字 weight / line-height / family）。**注**：getComputedStyle 序列化 = 父目标 zero-web P1a，「computed CSS 值对齐 Chromium」维度属 rendering-compat territory。**门禁**：.rs（engine 序列化+测试）+ 两 master doc；pre-commit guard PASS。**下一轮**：残余 getComputedStyle 简写均为深/复杂项（grid-template/grid 全简写〔areas 嵌入 rows〕/ mask〔类 background 多层〕）；或 pivot 回父目标 zero-web P1a 事件循环/fetch/Observer 真实化主线（2026-08-04 裁决的活跃主线，getComputedStyle 序列化主体已收官）；rendering-compat 侧续降频守成。

> **📍 R2760（2026-08-06）🔧 getComputedStyle inset 简写 land（top/right/bottom/left CSSOM 4 值最小化）：承接 R2759。续用本地 Chromium 150 oracle 提取确切串，TDD red→green land **inset 简写**——CSSOM 4 值最小化（复用 `box_4_to_css`，同 margin/padding/border-radius）：top/right/bottom/left 全等→单值 / top==bottom&&right==left→2 值 / left==right→3 值 / 否则 4 值。ZW 既已解析 inset 简写（`parse_rect_values`，shorthand/mod.rs:347），4 longhand（top/right/bottom/left）早覆，仅缺简写序列化重组。oracle 锚定：`inset:10px`→"10px"、`inset:10px 20px`→"10px 20px"、`inset:10px 20px 30px 40px`→"10px 20px 30px 40px"。**门禁全绿**：fmt clean / workspace clippy 零警告 / `make test` **13387/0/74**（R2759 13386 + 1 新测试，零回归；held baseline 自 13193 推进至 13387）/ `make product-smoke` welcome desktop **17.03%**（精确 baseline 持平）+ 全 struct PASS。**已知 diverge（pre-existing，记录不阻塞）**：auto/默认 inset（`position:absolute` 无显式偏移）Chromium 返 used-px 偏移（如 "13px 772px 480px 8px"），ZW 返 "auto"（ZW 不计算 auto inset 的 used 值）——属 longhand 层 used-value diverge，非本简写引入；显式设置的值序列化正确（测试覆盖）。**注**：getComputedStyle 序列化 = 父目标 zero-web P1a，「computed CSS 值对齐 Chromium」维度属 rendering-compat territory；纯只读 JS bridge 不触及渲染/布局。**门禁**：.rs（engine 序列化+测试）+ 两 master doc；pre-commit guard PASS。**下一轮**：续用本地 chromium oracle land 残余 getComputedStyle 简写——剩余多为复杂项（grid-template/grid 全简写〔areas 嵌入 rows，Option<String> 存原始串不捕获内联结构，tractable 性待核〕/ font 简写〔7+ 子分量复杂重组〕/ mask 简写〔类 background 多层〕）；或转父目标 zero-web P1a 事件循环主线；rendering-compat 侧续降频守成。

> **📍 R2759（2026-08-06）🔧 getComputedStyle grid 线定位 longhand + 简写 land（grid-column/row-start/end + grid-column/row/area CSSOM 最小化）：承接 R2758。续用本地 Chromium 150 oracle 提取确切串，TDD red→green land **4 longhand + 3 简写**：① 4 longhand（grid-column-start/end、grid-row-start/end）——`<grid-line>` 单值 Auto→"auto" / Line(n)→"n" / Span(n)→"span n" / Name(s)→"s"；② grid-column/grid-row 简写（start/end 2 值最小化）——start==end→单值，否则 end==auto 且 start 非 Name→单值，Name 保留 "name / auto"（避歧义）；③ grid-area 简写（rs/cs/re/ce 4 槽 trailing-drop 最小化）——全等→单值；否则从末尾 drop ce（iff ce==auto 且 cs 非 Name）/ re（iff ce 已 drop 且 re==auto 且 rs 非 Name），rs/cs 恒留。oracle 锚定：`grid-column:2`→"2"、`grid-column:main`→"main / auto"、`grid-area:1/1/3/3`→"1 / 1 / 3 / 3"、`grid-area:2/3`→"2 / 3"。**门禁全绿**：fmt clean / workspace clippy 零警告（合并 grid_line_pair 同体 if 分支避 identical_blocks）/ `make test` **13386/0/74**（R2758 13385 + 1 新测试，零回归；held baseline 自 13193 推进至 13386）/ `make product-smoke` welcome desktop **17.03%**（精确 baseline 持平）+ 全 struct PASS。**已知 diverge（pre-existing parser，记录不阻塞）**：单值 `grid-area: header`（CSS 规范四值同设）受 ZW `expand_grid_area`（shorthand/mod.rs:1363）仅设 row-start 限——序列化本身正确（测试覆盖 4 值/2 值形式），修 parser 须 css-grid reftest 验布局影响超本切片范围，记入待办。**注**：getComputedStyle 序列化 = 父目标 zero-web P1a，「computed CSS 值对齐 Chromium」维度属 rendering-compat territory。**门禁**：.rs（engine 序列化+测试）+ 两 master doc；pre-commit guard PASS。**下一轮**：续用本地 chromium oracle land 残余 getComputedStyle 简写（grid-template / grid 全简写〔Option<String> 存原始串〕/ font 简写〔含 line-height/font-family，复杂〕/ mask 简写〔类 background 多层〕等），或转父目标 zero-web P1a 事件循环主线；rendering-compat 侧续降频守成。

> **📍 R2758（2026-08-06）🔧 getComputedStyle place-content/items/self 简写 land（align+justify 二合一 CSSOM 2 值最小化）：承接 R2757。续用本地 Chromium 150 oracle 提取确切串，TDD red→green land **3 个 place-* 简写**——CSSOM 2 值最小化：align==justify→单值，否则 `"align justify"`。`place-content`=align-content+justify-content / `place-items`=align-items+justify-items / `place-self`=align-self+justify-self，6 longhand 早覆仅缺 CSSOM 重组，单值无列表/多层复杂度（本 session 最干净的 lever）。oracle 锚定：`place-content:center start`→"center start"、`place-self:center`→"center"（单值设两轴同值）、`place-self` 默认→"auto"。**门禁全绿**：fmt clean / workspace clippy 零警告 / `make test` **13385/0/74**（R2757 13384 + 1 新测试，零回归；held baseline 自 13193 推进至 13385）/ `make product-smoke` welcome desktop **17.03%**（精确 baseline 持平）+ 全 struct PASS。**已知 diverge（pre-existing，记录不阻塞）**：place-content/items **默认值**受 ZW layout-coupled 默认影响——ZW `justify_content` 默认 FlexStart / `align_items` 默认 Stretch vs Chromium 均默认 normal（故 ZW place-content 默认="normal flex-start"、place-items 默认="stretch normal"，diverge Chromium "normal"）。根因为布局耦合默认值，非本序列化引入；**显式设置的值（含单值同值）序列化正确**（测试覆盖）。修默认须 reftest+smoke 验证布局影响，超本切片范围，记入待办。place-self 默认（auto/auto）匹配 Chromium 无 diverge。**注**：getComputedStyle 序列化 = 父目标 zero-web P1a，「computed CSS 值对齐 Chromium」维度属 rendering-compat territory。**门禁**：.rs（engine 序列化+测试）+ 两 master doc；pre-commit guard PASS。**下一轮**：续用本地 chromium oracle land 残余 getComputedStyle 简写（grid 简写〔grid-template·areas 等，Option<String> 存原始串易重组〕/ font 简写〔含 line-height/font-family，复杂〕/ mask 简写〔类 background 多层〕等），或转父目标 zero-web P1a 事件循环主线；rendering-compat 侧续降频守成。

> **📍 R2757（2026-08-06）🔧 getComputedStyle background 简写 land（CSSOM 完整规范形重组）：承接 R2756。续用本地 Chromium 150 oracle 提取确切串，TDD red→green land **background 简写**——CSSOM 序列化恒为完整规范形（**无省略**）：`"<color> <image> <repeat> <attachment> <position> / <size> <origin> <clip>"`。oracle 揭示 origin/clip 即使相等也恒双显（`background: content-box`→"content-box content-box"），与 transition/animation 的省初值模式不同。复用 8 个 background longhand 序列化（image/position/size/repeat/attachment/clip/origin + color）做纯拼接；Vec 族 longhand 空 Vec 时用 CSS 初值兜底（0% 0% / auto / repeat）。oracle 锚定：默认→"rgba(0, 0, 0, 0) none repeat scroll 0% 0% / auto padding-box border-box"。**门禁全绿**：fmt clean / workspace clippy 零警告 / `make test` **13384/0/74**（R2756 13383 + 1 新测试，零回归；held baseline 自 13193 推进至 13384）/ `make product-smoke` welcome desktop **17.03%**（精确 baseline 持平）+ 全 struct PASS。**已知限制（记录不阻塞，pre-existing 非本切片）**：① **多层 background**——ZW 对 attachment/clip/origin 存单值（非多层 Vec），多层 background 无法正确 round-trip（单层正确，多层 longhand 亦单值，diverge 一致）；② **简写 parser 限制**——ZW `expand_background` 对含 `rgb()`/`var()` 的值 bail-out（整体作 color，丢 attachment），且 box token 故意 drop（R2479/R2481 A/B 证 net −3），故 `background: rgb(0,128,0) fixed`/`background: content-box padding-box` 等简写声明不会完整展开（序列化本身正确，测 attachment/box 时经 longhand 设置隔离）；③ url() 图层 ZW 存相对 URL，oracle 解析绝对 URL（pre-existing longhand 差异）。**注**：getComputedStyle 序列化 = 父目标 zero-web P1a，「computed CSS 值对齐 Chromium」维度属 rendering-compat territory；纯只读 JS bridge 不触及渲染/布局。**门禁**：.rs（engine 序列化+测试）+ 两 master doc；pre-commit guard PASS。**下一轮**：续用本地 chromium oracle land 残余 getComputedStyle 简写（grid 简写〔grid-template·areas 等，Option<String> 存原始串易重组〕/ font 简写〔含 line-height/font-family，复杂〕/ place-content·place-items·place-self〔align+justify 二合一〕等），或转父目标 zero-web P1a 事件循环主线；rendering-compat 侧续降频守成。

> **📍 R2756（2026-08-06）🔧 getComputedStyle transition/animation 简写 land + transition-property/animation-name「none」存储 diverge 修复：承接 R2755。续用本地 Chromium 150 oracle 提取确切串，TDD red→green land **2 列表简写 + 1 存储层 diverge 修复**：① **transition 简写**（CSSOM 列表 zip：每条目 property/duration/timing-function/delay 省初值；property=all 仅在其余全初值时显避免空串；默认空列表→"all"）；② **animation 简写**（顺序 duration/timing-function/delay/iteration-count/direction/fill-mode/play-state/name 省初值；全初值→"none"；多条目逗号连接）；③ **★ transition-property/animation-name「none」存储 diverge 修复**——oracle 揭示 `transition: none`→"none"、`animation: 2s`（name 省略）→"2s"，但 ZW 旧 apply（apply_advanced.rs）对两 longhand `.filter(s != "none")` 致 `transition: none` 被吞成空列表（→"all" diverge）、`animation: 2s` name "none" 被吞成空列表（→"none" diverge）。核验该 filter **冗余**：transition 引擎（transition.rs:70 `if property=="none" skip`）+ 动画管线（pipeline/mod.rs:1015 `n != "none"` 过滤）**均已有 "none" 守卫**，apply 层 filter 是重复防御。移除两处 filter 后：`transition: none`→["none"]→长hand "none" + 简写 "none"（diverge 修复）；`animation: 2s`→["none"] name→简写 "2s"（diverge 修复）；引擎行为零变化（守卫既在）。**门禁全绿**：fmt clean / workspace clippy 零警告 / `make test` **13383/0/74**（R2755 13382 + 1 新测试〔transition/animation 简写〕；5 个 style-system 旧测试断言 `is_empty()`→`vec!["none"]` 纠偏，零回归；held baseline 自 13193 推进至 13383）/ `make product-smoke` welcome desktop **17.03%**（精确 baseline 持平）+ 全 struct PASS（**「none」存储层变更经产品 smoke 证引擎零回归**）。**已知 diverge（记录不阻塞）**：`animation: bounce 0s`（显式 0s duration）Chromium 返 "0s bounce"，ZW computed duration=0s 与 `animation: bounce`（省略 duration）不可区分→均返 "bounce"（computed 不追踪 specified-ness，同 R2754/R2755 谱）。**注**：getComputedStyle 序列化 = 父目标 zero-web P1a，其「computed CSS 值对齐 Chromium」维度属 rendering-compat territory。**门禁**：.rs（engine 序列化+测试 / style-system apply filter+测试）+ 两 master doc；pre-commit guard PASS。**下一轮**：续用本地 chromium oracle land 残余 getComputedStyle 简写（background 简写〔多层 CSSOM 重组，ZW 单值存储 attachment/clip/origin 限单层〕/ grid 简写 / font-variant 简写〔需扩子属性存储〕等），或转父目标 zero-web P1a 事件循环主线；rendering-compat 侧续降频守成。

> **📍 R2755（2026-08-06）🔧 getComputedStyle 残余简写批量 land（columns/column-rule/list-style/text-decoration）+ column-rule-width longhand diverge 修复：承接 R2754（本地 Chromium 150 oracle 解锁）。续用 `/usr/bin/chromium --headless --dump-dom`（结果写 DOM 法）提取确切串，TDD red→green land **4 简写 + 1 longhand diverge 修复**：① **columns 简写**（width||count）——CSSOM 省 auto，全 auto→"auto"；② **column-rule 简写**（width||style||color）——oracle 揭示 **style=none 省略（hidden 保留）**、width 恒显、color 恒显；③ **list-style 简写**（position||image||type）——恒 3 段；④ **text-decoration 简写**（line||style||color||thickness）——line=none→"none"，否则顺序 line/thickness(!auto)/style(!solid)/color(!currentcolor)；⑤ **★ column-rule-width longhand diverge 修复**——oracle 揭示 column-rule-width 的 computed **独立于 style**（Chromium 对 style=none/hidden 仍返 medium→3px，与 border-width 的 none→0px 语义**不同**），R2737 曾误套 border-width 规则致 default 返 "0px" diverge；本轮移除 style→0px 短路纠偏为 "3px"（又一处被「需 Web oracle」挡住未暴露的真 diverge，同 R2754 outline-width/flex-basis 谱）。**门禁全绿**：fmt clean / workspace clippy 零警告 / `make test` **13382/0/74**（R2754 13381 + 1 新测试〔4 简写全覆盖〕+ R2737 multicol 测试 column-rule-width default 期望 0px→3px 纠偏，零回归；held baseline 自 13193 推进至 13382）/ `make product-smoke` welcome desktop **17.03%**（精确 R2635 baseline 持平）+ 全 struct PASS。**注**：getComputedStyle 序列化 = 父目标 zero-web P1a（DOM/JS Bridge），其「computed CSS 值对齐 Chromium」正确性维度属 rendering-compat territory；序列化纯只读 JS bridge 不触及渲染/布局，welcome smoke 持平证零产品影响。**门禁**：.rs（engine 序列化+测试）+ 两 master doc；pre-commit guard PASS。**下一轮**：续用本地 chromium oracle land 残余 getComputedStyle 项（font-variant 简写〔需扩 style-system 子属性存储〕/ grid 简写〔grid-template·areas 等〕等深项，或转父目标 zero-web P1a 事件循环主线）；rendering-compat 侧续降频守成。

> **📍 R2754（2026-08-05）🔧 getComputedStyle 简写序列化批量 land + 本地 Chromium 150 oracle 解锁（methodology 突破）：承接 R2753（border-image-source gradient）后续，重新审视「残余 gap 全需 Web oracle」结论——发现本机有可用的 **Chromium 150 headless 二进制**（`/usr/bin/chromium`，本地无网络），即项目既定 oracle 工具链可直接驱动。用 headless `--dump-dom` 提取 `getComputedStyle` 简写确切串（写 DOM 后 dump，绕开 console.log 不捕获），**彻底解锁简写序列化的 oracle 验证**（前序「无本地 oracle 待 Web」结论就此作废）。经 oracle 核实 land 7 项**（均 TDD red→green 对齐确切 chromium 串）**：① **gap 简写 2 值 bug 修复**——旧实现仅读 legacy `style.gap`（= row-gap）致 `gap: 5px 10px` 丢 column-gap 返 "5px"；改用 row_gap/column_gap longhand 做 CSSOM 2 值最小化；② **text-decoration 4 longhand 序列化补齐**（line 多 flag 规范序 underline overline line-through 空→none / style 5 关键字 / color currentcolor 解析 / thickness auto/length——storage 早有但 gCS 返 ''）；③ **outline-width divergence 修复**——旧误套 border-width 的 `border-style:none→used 0px` 规则致 default outline-width 返 "0px"，但 Chromium 返 "3px"（outline-width 保留 computed medium，不归零；与 border-width 行为不同）；④ **flex 简写** `"grow shrink basis"`（恒 3 段）+ **expand_flex basis `0`→`0%` 修复**——spec §7.1.1 省略 basis 时 flex-basis=0%（百分比），旧 ZW 用 "0"（→0px）致 `flex: 1` flex-basis diverge（Chromium "0%"）；⑤ **flex-flow 简写** `"direction wrap"`（恒 2 段）；⑥ **outline 简写** `"color style width"`（**注意与 border 的 width-style-color 顺序相反！**恒 3 段含 none）；⑦ **border + border-top/right/bottom/left 简写**（per-side `"width style color"`；全边 border 仅 4 边全等时返单边值否则 ''，CSSOM 不一致不可序列化）。**门禁全绿**：fmt clean / workspace clippy 零警告 / `make test` **13381/0/74**（R2753 13376 + 5 新测试〔gap/text-decoration/flex/flex-flow/outline·border 简写〕+ 4 style-system flex-basis 断言更新 0→0%，零回归；held baseline 自 13193 推进至 13381）/ `make product-smoke` welcome desktop **17.03%**（精确 R2635 baseline 持平）+ 全 struct PASS。**关键 methodology 发现（最高价值）**：前序 17 轮（R2737-R2753）把「简写序列化需 Web oracle」当作硬约束并 defer 全部简写，实为本机 Chromium 二进制可直接作 oracle——本轮用其 land 7 项简写（含 outline-width / flex-basis 两处真 diverge 修复）证明该约束不成立。**残余简写（columns/column-rule/list-style/text-decoration 简写等）同理可用本地 chromium oracle 续 land，下轮接续**。**注**：getComputedStyle 序列化 = 父目标 zero-web P1a（DOM/JS Bridge），其「computed CSS 值对齐 Chromium」正确性维度属 rendering-compat territory。**门禁**：.rs（engine 序列化+测试 / style-system expand_flex+测试）+ master doc；pre-commit guard PASS。**下一轮**：续用本地 chromium oracle land 残余简写（columns/column-rule/list-style/text-decoration 简写），每项 TDD red→green 对齐确切 chromium 串；rendering-compat 侧续降频守成。

> **📍 R2740（2026-08-05）📋 父目标 .rs 变更触发 plateau-guard + 推进 getComputedStyle 序列化 R2737→R2753（highest-value in-flight 项）：承接上一轮 session（crashed 于 API 429 限流）遗留的 R2737 WIP——`getComputedStyle` 补 CSS Multi-column 簇（column-rule-width/style/color + column-count/width/fill/span）+ font-variant-numeric + image-rendering 序列化，但遗留编译错误（`column_rule_width_to_css` 调用缺第 3 参数 `font_size_px`）。本 session 累计十六轮：① R2737 修复编译错误 + 补单元测试（9 属性全覆盖含默认值 + thick→5px UA used px + column-rule-color currentcolor 解析）+ 落地；② R2738 续补 `border-radius` **简写**（4 角 longhand 早由 R2707 覆盖；简写复用 `box_4_to_css` 做 CSSOM 4 值最小化）+ 单元测试 + 落地；③ R2739 续补 `box-shadow` + `text-shadow`（WPT 格式锚定：color 在前经 currentcolor 解析 + 全长度，box 4 长+inset / text 3 长，多阴影逗号分隔，空→none）+ 单元测试 + 落地；④ R2740 续补 grid 轨道簇（grid-template-columns/rows/areas + grid-auto-columns/rows，Option<String>→原样 specified 串 / None→CSS 初值 template·areas=none / auto-*=auto）+ 单元测试 + 落地；⑤ R2741 续补 containment 簇（content-visibility Visible/Hidden/Auto + contain-intrinsic-width/height Option<LengthValue>→px/none）+ 单元测试 + 落地；⑥ R2742 续补 counter-increment/reset（Vec<CounterActionValue>→name integer 列表，默认 1/0，空→none）+ 单元测试 + 落地；⑦ R2743 续补 transition/animation 簇 10 属性（property/duration/delay + name/duration/delay/iteration-count/direction/fill-mode/play-state，列表逗号分隔 + 各初值）+ 单元测试 + 落地；⑧ R2744 续补 transition/animation-timing-function（Vec<TimingFunctionValue>，ease/linear/cubic-bezier/steps；steps 位置 spec-aligned 待 Chromium A/B 核实），transition/animation 簇 12 属性全覆 + 单元测试 + 落地；⑨ R2745 续补 overflow 简写（x==y 单值 / 否则两值，overflow-x/y longhand 早覆）+ 单元测试 + 落地；⑩ R2746 续补 scroll-margin/padding（4+4 长）+ mask-mode（9 属性）+ 单元测试 + 落地；⑪ R2747 续补 background-image + mask-image（None/Url 逐层，gradient 层 defer→）+ 单元测试 + 落地；⑫ R2748 续补 margin + padding 简写（CSSOM 4 值最小化，复用 box_4_to_css）+ 单元测试 + 落地；⑬ R2749 续补 linear-gradient 序列化（dir/interp/stops，radial/conic 仍 defer）+ 单元测试 + 落地；⑭ R2750 续补 radial + conic gradient 序列化（radial 锚定 WPT oracle，conic spec-aligned），gradient 故事完整 + 单元测试 + 落地；⑮ R2751 修 radial-gradient position-首位 config parser bug（css-parser，匹配 WPT oracle）；⑯ R2753 border-image-source gradient 支持（5 处修复 + oracle 验证，image-source 家族全支持 gradient）。**门禁全绿（十六轮分别验证）**：fmt clean / workspace clippy 零警告 / `make test` **13376/0/74**（R2736 13361 + R2737-R2753 各 +1，零回归——**held baseline 自 R2638 的 13193 推进至 13376**，+183 全为父目标 zero-web getComputedStyle 序列化测试，rendering-compat 自身 surface 零变更零回归）/ `make product-smoke` welcome desktop **17.03%**（≤20% 门禁，精确 R2635 baseline 持平）+ 全 struct PASS（十六轮分别跑）。**结论**：父目标 getComputedStyle .rs 变更经 triple-guard（make test + product-smoke；legacy smoke 跳过——序列化纯只读 JS bridge，不触及 UA 样式/表单/legacy 元素，welcome 真实页面 JS 分支 smoke 持平原证零产品渲染影响）确认零 rendering-compat 回归；held baseline 推进至 13376/0/74，rendering-compat 降频守成态无变化（自主推进面仍 11 vein exhaust）。**注**：getComputedStyle 序列化 = 父目标 zero-web P1a 主线（DOM/JS Bridge），但其「computed CSS 值对齐 Chromium」正确性维度属 rendering-compat territory；R2737 是上一轮 in-flight crashed 工作（最高收益待办首推），R2738 border-radius 简写、R2739 box-shadow/text-shadow（WPT 锚定）、R2740 grid 轨道簇、R2741 containment 簇、R2742 counter-increment/reset、R2743 transition/animation 簇 10 属性、R2744 timing-function（簇 12 属性全覆，steps 位置待 A/B 核实）。**门禁**：.rs（engine 序列化+测试）+ 两 master doc；pre-commit guard PASS。**下一轮**：rendering-compat 续守成待命（plateau-guard 刚绿重置间隔，下次 ~R2750 或父目标 .rs 变更或用户点名深结构 Phase A IFC）；活跃主线续父目标 zero-web P1a（getComputedStyle 序列化主体收尾；残余 gap：font-variant 简写〔需扩 style-system 子属性存储〕）/ 事件循环。

> **📍 R2639（2026-08-04）📋 降频守成 plateau-guard triple-guard 全绿（守 held baseline·R2637/R2638 dead-path style-system 提交后零 drift 确认）：product-smoke + product-smoke-legacy（make test 上轮 R2638 刚确认 13193/0/74，跳过避免冗余）**。承接 R2638（主线切换裁决落实 + 第 11 vein 关闭）。按 2026-08-04 主线切换裁决 rendering-compat = 降频守成，本轮跑 triple-guard 守 held baseline：R2637/R2638 连续两次 style-system registry 提交（dead-path，无生产 value 消费），按 R541 教训（style-system 变更可能非显然影响产品渲染）防御性核验。**结果全绿**：① `make product-smoke`——welcome desktop struct PASS（800px diff **17.03%**=81737/480000px ≤20% 门禁，精确 R2635 baseline 持平；card:4/shortcut:6/link-tile:4/footer:1/title:1/tagline:2）+ wintertc desktop struct PASS（bg-orange-500:4/img-visible/text-justify≥2）+ morning desktop struct PASS（article:1/item-tag:3/lang-bash:1）+ 窄屏 welcome 375/320 card:4 + morning 375/320 article:1 + wintertc 375；② `make product-smoke-legacy`——**0 struct-check failures**（39 compared + 3 skip=49-details-summary/50-mark/51-blockquote-dl-figure HTML5 越界 Tier-1 by-design 无 oracle）/ avg diff 2.36% / worst 19-testpage-minimal 17.23%（精确 R2635 持平，font-wall trend-only）/ 37-form-controls struct PASS 3.85%。**结论**：triple-guard 全绿确认 R2637/R2638 dead-path registry 改值零产品渲染 drift；held baseline（make test 13193/0/74 + product-smoke 17.03% + legacy 0 FAIL）稳固；rendering-compat 降频守成态无变化，自主推进面仍 definitively 穷尽（11 vein 全 exhaust）。**门禁**：纯核查零 .rs；master.md doc-only 更新；pre-commit guard PASS。**下一轮**：rendering-compat 续守成待命（低频 plateau-guard 间隔刚核验绿，下次 ~R2645 或父目标 .rs 变更或用户点名深结构）；**活跃主线 = 父目标 zero-web P1 DOM/JS Bridge**（P1a 事件循环/fetch/Observer 真实化）。

> **📍 R2638（2026-08-04）🔧 第 11 vein 全扫收官 + column-gap drift 纠偏 + 【主线切换裁决】落实（rendering-compat → 降频守成）：承接 R2637（box 4 维 drift 已修），完成 `PropertyRegistry::initial_value()` **全 property** vs `ComputedStyle::default()` 一致性系统比对（length/number/color/enum 全类）。**全扫仅再出 1 处 drift**：`column-gap` registry 返 `Length(Px(0.0))` vs default `column_gap: LengthValue::Auto`（default R1040 注释：column-gap 初始 = normal，multicol 解析 1em、flex/grid 解析 0；Px(0.0) 对 multicol spec-wrong）。**纠偏**：registry `column-gap` → `Length(Auto)` + 守卫测试 `test_column_gap_initial_value_is_auto`（锁 column-gap==registry==default==Auto，并固定 gap/row-gap==Px(0.0) 一致）。**第 11 vein definitively 关闭**：全扫 drift = box 4 维（R2637）+ column-gap（R2638）= 5 处全修，余 property registry↔default 全一致（mask-* 仅 default 非 registered，非 drift）。**门禁**：scoped initial_value 测试簇 55 全绿 / 新测试 1 全绿 / fmt clean / clippy clean / `make test` **13193/0/74**（R2637 13192 + 1 新测试，net +1 零回归，exit=0）。**【2026-08-04 主线切换裁决落实】**：用户裁决「工作切回父目标 zero-web（P1 DOM/JS Bridge 原生化），渲染兼容性先缓一缓」→ 本 goal 进入**降频守成**：① 低频 plateau-guard（每 ~10 轮或父目标 .rs 变更时跑，守 13193 全绿基线）；② 待用户决策清单原样保留（R1043/R2174/Phase A IFC/font-stack/srcset 等深结构等点名，点名即切回）；③ 文档纠偏停止主动排程（design-doc 引用核验近收官），仅 .rs 变更引发 drift 顺手修；④ 第 11 vein 既关闭，自主 lever surface definitively 穷尽，rendering-compat 无新自主推进面。**门禁**：纯核查 .rs（registry 1 值 + 测试）+ master doc；pre-commit guard PASS。**下一轮**：rendering-compat 守成待命（低频 plateau-guard / 用户点名深结构）；**活跃主线已转父目标 zero-web P1 DOM/JS Bridge**（事件循环/fetch/Observer 真实化）——后续 rally turn 应转 zero-web master 推进。

> **📍 R2637（2026-08-04）🔧 第 11 vein「initial-value VALUE correctness」审计 + 4-property registry↔default drift 纠偏（代码纠偏主线·非 rendering lever）：spot-check 7 property（white-space 6 值 / text-transform 6 值 / caption-side〔top|bottom 仅，left/right Chrome 不支持故 R2382 反 diverge、inline-start/end 属 R1043〕/ unicode-bidi 6 值 / float〔含 inline-start/end〕/ clear〔含 inline-start/end，live @ parse_misc.rs〕/ resize 6 值）全 complete 或 Chrome-aligned → **9-vein exhaust 经独立 spot-check 再确认（防后续 agent 重扫同批属性）**。续开正交第 11 vein = initial-value **VALUE** 正确性（区别于 anchor 既退役的「initial-value」**driving-test/reftest** 轴——后者查「有无 reftest 驱动 fail」零产出；本 vein 查「initial_value() 返回值 vs spec vs ComputedStyle::default() 是否一致」）。**发现 4-property drift**：`PropertyRegistry::initial_value()` 对 `width`/`height`/`min-width`/`min-height` 返回 `Length(Px(0.0))`，而真实初始值来源 `ComputedStyle::default()`（经 `apply_initial_value` 消费）用 `LengthValue::Auto`——min-* 的 `Px(0.0)` 尤其有害（default_impl.rs R428-R437 注释明载「短路 taffy 内容下限使 flex/grid item 可缩至 0」）。**核验 registry value-死路**：全 repo `initial_value()` 调用仅测试 `.is_some()/.is_none()`（含 core.rs:1425 全 known-property 覆盖守卫），生产零消费返回值 → 改值零行为风险（无测试断言旧 `Px(0.0)`）。**纠偏**：4 值改 `Length(Auto)` 与 default 一致 + 守卫测试 `test_box_dimension_initial_values_are_auto`（断言 registry==default==Auto，锁未来漂移）。**门禁**：scoped core test +1 / fmt clean / clippy（style-system+parser）clean / `make test` **13192/0/74**（baseline 13191 + 1 新测试，net +1 零回归）。**意义**：registry 作为「initial-value 来源」文档化函数现 4-property 与 default/spec 一致，消除「未来代码若消费 registry 得 width=0 短路布局」latent landmine；属 master 认可的「文档/代码不一致纠偏」高价值自主面 #3（非 rendering lever，net=0 渲染影响但净正可维护性）。**附带确认**：parse_basic.rs 仍为 parse_misc.rs 的死副本（`parse_clear` 双份共存），既记 master 待用户点名清理，本轮不动（CLAUDE.md「提及不删除」）。**下一轮**：续第 11 vein 全 property registry↔default 一致性扫描（仅 box 维度出 drift，余大概率一致，快速核验即结）/ 低频 plateau-guard（R2635 刚绿延长间隔）/ 文档纠偏 / 用户点名深结构。

> **📍 R2636（2026-08-04）🔧 design-doc 引用核验 + 实施状态确认（R2202 模式·vertical-mode table-rl-transpose RFC）：vertical-mode-table-rl-transpose-rfc.md（v0.2，~13 引用，R1107/R1108 α-4b vertical 表轴转置 multi-session 切片计划，R1043 user-gated 深方向）逐条源码核验：3 行号 + 1 路径 drift 全纠偏 + **α-4b-1 LANDED 实施确认**（文档 v0.2 状态准确无漂移）；纯 .md 零 .rs 零回归**。承接 R2635（plateau-guard triple-guard）。本轮续 design-doc citation sweep，择 density 最高未验 doc（13 引用；vertical-mode 群收官——R2626 vertical-mode-ifc + R2633 vertical-mode-text-positioning + 本轮 table-transpose 三 doc 全清），保持 R1043 授权时无行号/路径误导。**核验**：drift 全为 R1108 后 table/ 子模块拆分（position_cells_vertical 迁 table/table_vertical.rs）+ table.rs 增长（position_cells 931→1072）+ converter/types 重构累积。**3 行号纠偏**：① `table.rs:931` position_cells（§0/§4.1/§7.1 共 3 处）→**:1072**（+ WM 分派 @ table.rs:303-309：VerticalRl/Lr→position_cells_vertical，else position_cells）；② `converter/mod.rs:248` apply_vertical_writing_mode（§1.1）→**:344**（R2633/R2634 既验）；③ `types/mod.rs:159` writing_mode 字段（§4 依赖图前）→**:232**。**1 路径纠偏**：④ §4.1 `position_cells_vertical`（table.rs）→**`table/table_vertical.rs:17`**（def；table/ 子模块拆分迁移；调用点 @ table.rs:307）。**经核 valid 保留**：`table_types.rs` TableGrid rowspan（TBD-2：rowspan 字段 @ :28，struct @ :56，get_rowspan @ :136）。**WM gate 名澄清**：§0/§1.3 `is_vertical_table_wm` 是概念门控名（代码无同名 fn）；实际 = table.rs:303-306 inline match `VerticalRl|VerticalLr`。**α-4b-1 实施确认（与文档一致）**：position_cells_vertical（table/table_vertical.rs:17）+ WM 分派（table.rs:305-307）+ 3 单测（writing_mode_tests.rs:353/445/508）全在码中——文档 v0.2 §4.1「★ R1108 LANDED」准确。R1043 vertical-mode native 整体仍 user-gated（css-writing-modes 80.1%；R1965-R1979 vertical 增量 lever 全 net-negative/dormant）。**意义**：vertical-mode table-transpose RFC 引用全 precise（含 table/ 子模块拆分路径漂移 + WM gate 概念名澄清）；**vertical-mode design-doc 群（vertical-mode-ifc R2626 + text-positioning R2633 + table-transpose R2636）行号/路径/状态全清**，R1043 授权实施时无误导。**门禁**：纯 .md doc 纠偏零 .rs；pre-commit guard PASS。**下一轮**：续 design-doc 引用核验（no-ratio-replaced-sizing 8 / r109-inline-block-anon-block-float-exclusion 7 / unified-font-stack 6 等）/ 低频 plateau-guard（R2635 刚绿）/ 用户点名深结构。

> **📍 R2635（2026-08-04）📋 会话边界 plateau-guard triple-guard 全绿复跑（守住已获得收益·9 轮纯 .md 后零 drift 确认）：make test 13191/0/74（精确 R2597/R2604/R2613/R2625 持平，零回归零环境 drift）+ product-smoke 全绿（welcome desktop 17.03%=81737/480000px ≤20% 门禁 + wintertc/morning desktop 全 struct PASS + welcome 375/320 card:4 + morning article 320）+ product-smoke-legacy 全绿（39 compared+3 skip，**0 struct FAIL**，avg 2.36%，worst 19-testpage 17.23%，精确 R2584/R2604/R2613/R2625 持平）；纯核查零 .rs 零回归**。承接 R2634（inline-element-wrap + doc-control archive）。距上次 triple-guard（R2625）已累积 9 轮纯 .md（R2626-R2634 全纯文档纠偏 + 1 次 doc-control archive），本轮按 run-rules point 1「守住已获得收益：每轮先确认 make test、产品 smoke、legacy smoke 没有新回归」+ R2583/R2604/R2613/R2625 会话边界 precedent 跑 triple-guard 确认无环境/工具链 drift。**实施**：`make test` + `make product-smoke` + `make product-smoke-legacy`（均 test-guard 包裹，release 缓存命中）。**结果全绿**：① `make test`（test-guard）= **13191 passed / 0 failed / 74 ignored**（精确 R2597 baseline，0 FAILED，exit 0）；② `make product-smoke`——welcome desktop struct PASS（800px diff 17.03%=81737/480000px ≤20% 门禁，card:4/shortcut:6/link-tile:4/footer:1/title:1/tagline:2）+ wintertc desktop struct PASS（bg-orange-500:4/img-visible/text-justify≥2）+ morning desktop struct PASS（article:1/item-tag:3/lang-bash:1）+ 窄屏 welcome 375/320 card:4 + morning 375 article:1 + wintertc 375；③ `make product-smoke-legacy`——**0 struct-check failures**（run-all.sh 显式 `struct-check failures: 0`；39 struct=PASS + 0 struct=FAIL + 3 skip=49-details-summary/50-mark/51-blockquote-dl-figure HTML5 越界 Tier-1 by-design 无 oracle）/ avg diff 2.36% / worst 19-testpage-minimal 17.23%（精确 R2604/R2613/R2625 持平，font-wall trend-only）/ 37-form-controls struct PASS 3.85%（R2156+R2162 Phase A default-on 后既稳定）。**结论**：triple-guard 全绿确认 R2625 既绿状态经 9 轮纯 .md（含 R2632-R2634 design-doc citation sweep + R2634 doc-control archive）无环境/工具链/行为 drift；clean-lever surface 维持 9 vein exhaust；自主推进面仍 = plateau-guard（刚绿重置间隔）+ 文档纠偏；**唯一推向 95% = 用户点名授权深结构**（最高 value Phase A IFC line-box-metric，设计文档 R2624 已 precise 待命）。**门禁**：纯核查零 .rs；master.md doc-only 更新；pre-commit guard PASS。**下一轮**：长间隔 plateau-guard（刚绿延长间隔）/ 文档纠偏（续低密度 design-doc 引用核验）/ 用户点名深结构。

> **📍 R2634（2026-08-04）🔧 design-doc 引用核验（R2202 模式·R109 inline-element-wrap RFC）：inline-element-wrap-rfc.md（v0.1，~13 引用，R1735 R109 inline-element 匿名块包裹 spec CSS2 §9.2.1.1，4-slice 计划，R109/Phase A IFC 深方向）逐条源码核验：3 valid + 7 行号 + 1 路径 drift 全纠偏；**4 切片均未实施**（env-gate `ZW_R109_INLINE_WRAP`/`ZW_R109_INLINE_BORDER_BLEED` 代码中不存在，R1735/R1737 既 revert）状态与文档一致无漂移；纯 .md 零 .rs 零回归**。承接 R2633（vertical-mode-text-positioning）。本轮续 design-doc citation sweep，择 IFC/R109 territory 高杠杆 doc（border-width-008 driving case，R109 多 session 深方向）。**核验**：drift 全为 R1735-R2202 累积（tree.rs ctx_node 区下移 + painter/text.rs border-溢出 paint 区扩 + inline/ 拆分 collect_items 迁移 + inline_finalization.rs 增 measure 上下文）。**3 valid 保留**：`tree.rs:94 r109_wired()` / `inline_block_split.rs:77 block_container_has_mixed_content` / `:122-123` 注释。**7 行号纠偏**：① `tree.rs:1131`(§2)+`:1128-1132`(§3 Slice 1b) ctx_node Text fallback→**:1398-1402**；② `painter/text.rs:1207-1282`(§2 border 溢出 paint 区)→**:1271-1355**；③ gate `painter/text.rs:1207`(§2/§3 bleed + §3 Slice 2)→**:1290**；④ `:1213`「避 R638 双计」注释→**:1300**；⑤ `:1205` owner_style.display==Inline→**:1292**；⑥ `inline_finalization.rs:967 measure_text_content`→**:1118**；⑦ `postprocess.rs backfill_r109_anon_block_heights` 补行号 **:1016** + 路径 `postprocess.rs`→**`engine/postprocess.rs`**（postprocess 已迁 engine/ 子目录）。**1 路径纠偏**：⑧ §3 Slice 3 `collect_inline_items`（`inline/mod.rs`）→**`inline/collect_items.rs:6`**（inline/ 拆分迁移，同 R2632/R2633）。**实施状态（与文档一致·无漂移）**：4 切片均未 land（env-gate 不存在）；gate 现 `(owner_h > $frag_fs * 1.5 || phasea_orphan_fire)` 但 :1300 注释确认「仍多行，不触 single-line」→ Slice 2 仍 OPEN，本文分析准确。**意义**：R109 inline-element-wrap RFC 引用全 precise（含 postprocess 迁 engine/ + collect_items 迁 inline/ 两处路径漂移）；R2202「doc 滞后致盲推」防护再加固。**门禁**：纯 .md doc 纠偏零 .rs；pre-commit guard PASS。**下一轮**：续 design-doc 引用核验（其他低密度 doc）/ 低频 plateau-guard（R2625 后 9 轮纯 .md，临近 triple-guard）/ 用户点名深结构。

> **📍 R2633（2026-08-04）🔧 design-doc 引用核验 + 实施状态纠偏（R2202 模式·vertical-mode text-positioning RFC）：vertical-mode-text-positioning-rfc.md（v0.2，9 引用，R1116-R1120 七轮调查 + R1135/R1136 增补的 4-bug（A/B/C/D）+ Bug E 耦合系统 RFC，R1043 user-gated 深方向）逐条源码核验：6 行号 + 1 路径 drift 全纠偏 + **切片 2（Bug B）已 LANDED（R1122）状态纠偏**；纯 .md 零 .rs 零回归**。承接 R2632（font-wall-cdep-scoping）。本轮续 vertical-mode 群（R2626 vertical-mode-ifc 既清），保持 R1043 授权时无行号/状态误导。**核验**：drift 全为 R1116-R1122 实施期 + 后续 `inline_formatting.rs` 拆 `inline/`（collect_inline_items 迁 `collect_items.rs`）+ painter/text.rs 缩行累积。**6 行号纠偏**：① `converter/mod.rs:271`(§1)+`:248`(§4 表)→**:344**（`apply_vertical_writing_mode`）；② `inline/mod.rs:1659`(§1 Bug B vrl col-x)→**:1144-1154**；③ `inline/mod.rs:1456`(§4 表 `break_items_into_columns`)→**:940**（R2626 既验）；④ `inline_finalization.rs:591`(§1 Bug D compute_final gate)→**:726**（fn @607）；⑤ `inline_finalization.rs:330`(§7.2 `store_font_sizes_from_ifc` 调用)→**:333**；⑥ `painter/text.rs:1119`(§7.2 + §7.3 表 Bug E Path B)→**:812**。**1 路径纠偏**：⑦ §7.2 `inline/mod.rs:598-619` `collect_inline_items`→**`inline/collect_items.rs:6`**（inline/ 拆分）。**★ 实施状态纠偏（最高价值）**：**切片 2（Bug B column-x block_extent）已 LANDED = R1122**——`inline/mod.rs:1144-1154` 现 `let mut x = self.block_extent`（非 `container_width`，附 R1122 注释）；本文 §1 Bug B / §3 切片 2 / §7.6 描述为 open 属历史，防后续 agent 重做已落切片。**切片 4'（Bug E Path B 真实 styles）仍 OPEN**——`painter/text.rs:812` 仍 `HashMap::new()`（§7.2 准确）。R1043 vertical-mode native 整体仍 user-gated（css-writing-modes 80.1%）。**意义**：vertical-mode text-positioning RFC 引用全 precise + Bug B 实施状态对齐；R2202「doc 滞后致盲推/重做已落切片」防护再加固（含 collect_inline_items 路径迁移 + 切片 2 已 LANDED 两类状态漂移）。**门禁**：纯 .md doc 纠偏零 .rs；pre-commit guard PASS。**下一轮**：续 design-doc 引用核验（inline-element-wrap 9 / 其他低密度）/ 低频 plateau-guard（R2625 后 9 轮纯 .md，临近 triple-guard）/ 用户点名深结构。

> **📍 R2632（2026-08-04）🔧 design-doc 引用核验（R2202 模式·font-wall C-dep scoping）：font-wall-cdep-scoping.md（11 引用，font-wall C-dep scoping 报告 2026-07-16，§7/§8 既勘误 §4 metric-coherence DEAD + gamma blend net-neutral）逐条源码核验：4 行号 + 2 路径 drift 全纠偏 + §1-§2 font-stack map 经核 valid；纯 .md 零 .rs 零回归**。承接 R2631（multicol-phase2-spec）。本轮转 density 排名次高未验 doc（font-wall-cdep-scoping，font-wall territory 唯一 Skia C-dep 路径文档，保持精准对未来 RFC 授权有高杠杆）。**核验**：自 R2597 起零 .rs 变更，drift 全为 R1068-R2202 font-stack 演进累积（`line_metrics_full` 下移 + freetype_raster 模块 doc 扩展 + `inline_formatting.rs` 拆 `inline/` + 测试增 70+ 行）。**4 行号纠偏**：① 度量 `loader.rs:418-443`→**:441-445**（`line_metrics_full`，§1.1 表 + §1.2 各 1 处）；② freetype_raster 模块 `loader.rs:37-92`→**:17-92**（37 实为模块内 `rasterize` fn，模块起点 `mod freetype_raster` @17）；③ 单测 `loader.rs:755-785`→**:827-855**（`test_line_metrics_full_exposes_line_gap`）；④ §8 `blend_pixel` 补 **`cpu/mod.rs:616`**。**2 路径纠偏**：⑤ §4.2 `inline_formatting.rs`→**`crates/layout-engine/src/inline/` 模块**（旧单文件已拆分，IFC strut/half-leading 在 `inline_finalization.rs:971-979`）；⑥ §5.1 同路径漂移。**经核 valid 保留**：`shaper.rs:74-132`（`shape_with_rustybuzz`，74=doc/75-132 body）/ `loader.rs:298-329`（`rasterize_glyph`）/ `:79-80`（y_offset 注释）/ `:341-346`（Ahem ascent 块）/ §1.1「`freetype-raster` default-on」（匹配 `Cargo.toml:40` `default=["freetype-raster"]` 自 R1159）。**附带发现（提及不改·code comment 非本文档）**：`loader.rs:6` 模块 doc 自称「`freetype-raster` feature，**默认关**」**stale**——实为 default-on（`Cargo.toml:40` 自 R1159）；按 CLAUDE.md「精准修改」本轮纯 .md 不动 .rs，记录待后续 .rs 轮。**意义**：font-wall C-dep scoping 报告 font-stack 引用全 precise（§1-§2 live map + §4 DEAD 推荐均精确化）；唯一 font-wall unlock = Skia C-dep（§7.4/§8.1 既裁决）授权实施时无行号/路径误导。**门禁**：纯 .md doc 纠偏零 .rs；pre-commit guard PASS。**下一轮**：续 design-doc 引用核验（vertical-mode-text-positioning 9 / inline-element-wrap 9）/ 低频 plateau-guard（R2625 后 8 轮纯 .md）/ 用户点名深结构。

> **📍 R2631（2026-08-04）🔧 design-doc 引用核验（R2202 模式·multicol Phase 2 spec）：multicol-phase2-unified-column-flow-spec.md（11 引用，R1099 multicol Phase 2 统一 column-flow 碎片化 spec，user-gated 草稿）逐条源码核验：全 11 漂移纠偏（multicol cluster 符号复用 R2617-R2619 既验值）；纯 .md 零 .rs 零回归**。承接 R2630（css-url-image-fetch）。本轮转 density 排名次高未验 doc（multicol Phase 2 spec，与已扫 multicol cluster 同族）。**核验**：该 doc 11 引用全为 multicol cluster 既验符号（painter/text.rs multicol gate / balance target_h / types InlineLayoutLine / break_items_into_columns），drift 全为 R1099 后 painter/text.rs + types/mod.rs + inline/mod.rs 重构累积。**全 11 漂移纠偏**：① `text.rs:713`→**:569**（multicol paint gate `!has_in_flow_children && is_balance_mode && height_auto`，4× 含 painter/ 全路径）；② `text.rs:948`→**:854**（balance target_h，2×）；③ `types/mod.rs:371`→**:502**（InlineLayoutLine struct）；④ `types/mod.rs:186`→**:267**（LayoutBox.inline_layout 字段，2×）；⑤ `painter/text.rs:700-1010`→**:569-964**（multicol 门控+分配 region）；⑥ `inline/mod.rs:1462 break_items_into_columns`→**:940**（垂直 WM 换行，命名碰撞澄清）+ line 1463→941。**意义**：multicol Phase 2 spec（user-gated）11 引用全 precise；**multicol design doc 群（handoff R2617 + column-aware-IFC R2618 + fragmentation R2619 + phase2-spec R2631）行号 drift 全清**。**门禁**：纯 .md doc 纠偏零 .rs；pre-commit guard PASS。**下一轮**：续 design-doc 引用核验（font-wall-cdep-scoping 11 / vertical-mode-text-positioning 9）/ 低频 plateau-guard / 用户点名深结构。

> **📍 R2630（2026-08-04）🔧 design-doc 引用核验 + 实施状态纠偏（R2202 模式·css-url-image-fetch RFC）：css-url-image-fetch-rfc.md（12 引用，R1794 CSS `url()` 图片抓取 + painter 查找一致性）逐条源码核验：Part A+B 全 LANDED 状态行同步 + 全漂移纠偏；纯 .md 零 .rs 零回归**。承接 R2629（phase-a-inline-box-model）。本轮转 density 排名次高未验 doc（css-url-image-fetch RFC）。**核验**：该 doc 为 R1794 设计，drift 全为 R1794 后 painter 迁 `paint/painter/` + pipeline 拆 `mod.rs`/`extract.rs`（R2511）+ apply_advanced 迁 `property/` 累积。**实施状态纠偏（最高价值）**：状态行原「Active 实施中」stale——Part A（painter 查找一致性 4 处全改 `image_resource_key`：`effects.rs:243/274` + `text_list.rs:437` + `border.rs:197`）+ Part B（`extract_css_image_urls`〔`pipeline/extract.rs:231`〕+ fetch 接线 + inline `<style>` 覆盖 R2411）全 LANDED。**全漂移纠偏**（9 unique sites，含 bare/full-path 重复）：① webview.rs:371→**402**（fetch_image_subresources）；② effects.rs:121→**243** / :143→**274**（background-image）；③ text_list.rs:110→**437**（list-style-image）；④ border.rs:182→**197**（border-image-source）；⑤ text.rs:322→**396**（`<img>` image_resource_key）；⑥ apply_advanced.rs:991→**property/apply_advanced.rs:1115**（path+line）；⑦ extended.rs:1409→**tests/tests_2.rs:1175**（文件迁移，bg.png test）；⑧ pipeline.rs:177→**pipeline/mod.rs:177**（path）；⑨ extract_css_image_urls pipeline.rs→**pipeline/extract.rs:231**（R2511 抽出）。**意义**：css-url-image-fetch RFC 12 引用全 precise + Part A+B 实施状态对齐（Active→✅ 已实施）；R2202「doc 滞后致盲推」防护再加固（含 painter/pipeline/style-system 三处 path drift）。**门禁**：纯 .md doc 纠偏零 .rs；pre-commit guard PASS。**下一轮**：续 design-doc 引用核验（multicol-phase2-spec 11 / font-wall-cdep 11）/ 低频 plateau-guard / 用户点名深结构。

> **📍 R2629（2026-08-04）🔧 design-doc 引用核验 + 行号勘误标注（R2202 模式·phase-a-inline-box-model）：phase-a-inline-box-model-full-design-2026-07-29.md（13 引用，R2157 Phase A inline-box-model coherence 完整设计，**DONE/default-on**）逐条源码核验：4 valid + 9 漂移纠偏至当前实现 + 加 R2629 行号勘误 banner；纯 .md 零 .rs 零回归**。承接 R2628（line-clamp RFC）。本轮转 density 排名次高未验 doc（phase-a-inline-box-model，DONE/default-on）。**核验**：该 doc 已有 R2420「DONE/default-on」实施状态 banner（slice 2 R2160-R2162 + slice 3 R2197 + struct-check paint_skip-aware R2198 全 default-on），本轮聚焦 R2156-R2198 era 代码定位行号 drift（drift 全为 R2198 后 tree.rs/painter 重构累积）。**4 处经核 valid 保留**：`postprocess.rs:112`（ib_sizes）/ `hit_test.rs:192`（collect_hit_test_nodes，crates/engine/src/）/ `collect_items.rs:439`（node_id: child_id）/ `inline_finalization.rs:861≈:862`（compute_final IFC）。**9 处漂移纠偏**：① tree.rs element-child loop 1284→**1476**；② leaf-path 1271→**1505**；③ R1480 注释 1266→**1504**；④ gate 1411→**1536**；⑤ text.rs Path B IFC 748→**771**；⑥ font_size 361→**432**；⑦ R639 gate 1202→**1270**；⑧ painter/mod.rs collect_box_heights 383→**484**；⑨ inline_finalization.rs backfill_phasea_orphan_boxes 1022→**1027**（R2616 既验）。**R2629 行号勘误 banner**：加在 R2420 banner 下，列全部 drift fix + valid 保留。**附带发现（提及不改）**：`painter/text.rs:625-626` 有 stale 代码注释自称「font_size 已在 line 361 保证」（实际现 :432），属代码注释非本文档，按 CLAUDE.md「提及但不删除」记录。**意义**：Phase A inline-box-model（DONE/default-on）设计文档代码定位全 precise；R2202「doc 滞后致盲推」防护再加固。**门禁**：纯 .md doc 纠偏零 .rs；pre-commit guard PASS。**下一轮**：续 design-doc 引用核验（css-url-image-fetch-rfc 12 / multicol-phase2-spec 11 / font-wall-cdep 11）/ 低频 plateau-guard / 用户点名深结构。

> **📍 R2628（2026-08-04）🔧 design-doc 引用核验 + 实施状态纠偏（R2202 模式·line-clamp RFC）：line-clamp-rfc-2026-08-02.md（14 引用，R2431 line-clamp 实现 RFC）逐条源码核验：slices 1-2 已 LANDED 状态行同步 + 全漂移纠偏至当前实现；纯 .md 零 .rs 零回归**。承接 R2627（doc-control archive）。本轮转 density 排名次高未验 doc（line-clamp RFC）。**核验**：该 doc 为 R2431 line-clamp 实现 RFC，drift 全为 R2431-R2467 实施期 + 后续 style-system `property/` 拆分 + painter ellipsis 重构累积。**实施状态纠偏（最高价值）**：状态行原「待切片实施」stale——slice 1（IFC `line_clamp` cap + `apply_line_clamp_cap`，`inline/mod.rs:623/654`）+ slice 2（ellipsis，painter `text.rs:1622-1642` 消费 `box_node.line_clamp_clamped`，R2431/R2432/R2467 LANDED，kill-switch `ZW_LINE_CLAMP` default-on）已落地；slice 3（block-ellipsis string / vertical / multicol 嵌套 / abspos-floats 精确交互）仍 defer。**全漂移纠偏**：① apply path `apply_advanced.rs:1057`→**property/apply_advanced.rs:1100**（×2）；② computed path `computed_style.rs:476`→**property/computed_style.rs:490**；③ `parse_line_clamp` parse_extended_visual.rs:521→**539**；④ text.rs ellipsis 簇全漂移：触发 762→**834** / ahem 763→**837** / measure 1447→**1561** / cutoff 1452→**1564** / render 1474→**1590** / add_glyph 1475→**1590** / line-clamp ellipsis 消费 @ **1622**；⑤ IFC `layout()` inline/mod.rs:615→**623** + break_items_into_lines call mod.rs:633→**637**；⑥ inline_finalization height-from-lines :798→**:930**（`line_clamp_clamped` set）。**经核 valid 保留**：postprocess.rs:272（paint IFC re-run）/ inline_finalization.rs:862（layout IFC run）。**意义**：line-clamp RFC 14 引用全 precise + slices 1-2 实施状态对齐（slice 3 defer 标注）；R2202「doc 滞后致盲推」防护再加固（含 style-system `property/` 拆分致 path drift）。**门禁**：纯 .md doc 纠偏零 .rs；pre-commit guard PASS。**下一轮**：续 design-doc 引用核验（phase-a-inline-box-model 13 / css-url-image-fetch 12 等）/ 低频 plateau-guard / 用户点名深结构。

## 轮次详记归档

> **R2167–R2197 + R2199c 轮次详记已归档**（2026-07-29 文档结构性精简）：原 master.md 顶部轮次详记区（R2167→R2197 + R2199c，~190 行 blockquote，含多行续行）已逐字保留 → [`archive/master-pre-slimdown-2026-07-29.md`](./archive/master-pre-slimdown-2026-07-29.md) 顶部轮次详记区（零删减）。更早轮次归档见 [`archive/`](./archive/)。
>
> **R2201–R2458 顶部轮次详记区已归档**（2026-08-04 R2561 doc-control 瘦身——top-block 最大块）：原 master.md anchor 之后、§最近轮次摘要 之前顶部轮次详记区 258 轮（R2391→R2223 file-order，interleaved 非严格时序；含 font-matching 授权/落地 R2391-R2417、color/gradient/shorthand lever era R2266-R2402、Phase A/doc-vs-code audit era R2420-R2458）已逐字保留 → [`archive/rounds-r2201-r2458-topblock-summary.md`](./archive/rounds-r2201-r2458-topblock-summary.md)（零删减）；关键决策（font-metric R2393 dormant / font-weight·style matching landed / Phase A inline-box-model DONE）已沉淀于本文结构化章节，迁出详记不丢失当前指引；master.md 顶部现仅留 ruling + anchor（598→229 行）。
>
> **R2459–R2495 详记已归档**（2026-08-04 R2555 doc-control 策略「master.md 不允许无限增长」瘦身）：原 §最近轮次摘要 底部 36 轮（R2495→R2459，含 R2486–R2495 旧 plain-「承接」format 体 + R2459–R2485 blockquote 体）已逐字保留 → [`archive/rounds-r2459-r2495-summary.md`](./archive/rounds-r2459-r2495-summary.md)（零删减）；§最近轮次摘要 现仅留 R2496–R2555 全 blockquote 干净体。
>
> **R2496–R2524 详记已归档**（2026-08-04 R2556 doc-control 瘦身续 R2555）：原 §最近轮次摘要 底部 29 轮（R2524→R2496，全 blockquote 体）已逐字保留 → [`archive/rounds-r2496-r2524-summary.md`](./archive/rounds-r2496-r2524-summary.md)（零删减）；§最近轮次摘要 现仅留 R2525–R2556（~28 轮健康 recent 窗口）——**doc-control 瘦身收官**（后续待新轮累积再迁）。
>
> **R2525–R2539 详记已归档**（2026-08-04 R2571 doc-control 瘦身续）：原 §最近轮次摘要 底部 14 轮（R2539→R2525，全 blockquote 体）已逐字保留 → [`archive/rounds-r2525-r2539-summary.md`](./archive/rounds-r2525-r2539-summary.md)（零删减）；§最近轮次摘要 现仅留 R2540–R2570（~28 轮健康 recent 窗口，本 session R2562-R2570 累积后 R2571 再瘦身）。
>
> **R2540–R2553 详记已归档**（2026-08-04 R2578 doc-control 瘦身续）：原 §最近轮次摘要 12 轮（R2540–R2553，含 R2540-R2548 + R2551-R2553，全 blockquote 体）已逐字保留 → [`archive/rounds-r2540-r2553-summary.md`](./archive/rounds-r2540-r2553-summary.md)（零删减）；§最近轮次摘要 现仅留 R2554–R2578（~24 轮健康 recent 窗口，本 session R2572-R2578 累积后 R2578 再瘦身）。
>
> **R2554–R2562 详记已归档**（2026-08-04 R2585 doc-control 瘦身续）：原 §最近轮次摘要 底部 9 轮（R2562→R2554，全 blockquote 体）已逐字保留 → [`archive/rounds-r2554-r2562-summary.md`](./archive/rounds-r2554-r2562-summary.md)（零删减）；§最近轮次摘要 现仅留 R2563–R2584（~20 轮健康 recent 窗口，本 session R2582-R2584 累积后 R2585 瘦身）。
>
> **R2563–R2570 详记已归档**（2026-08-04 R2591 doc-control 瘦身续）：原 §最近轮次摘要 底部 7 轮（R2570→R2563，全 blockquote 体，R2567 不存在）已逐字保留 → [`archive/rounds-r2563-r2570-summary.md`](./archive/rounds-r2563-r2570-summary.md)（零删减）；§最近轮次摘要 现仅留 R2571–R2590（~19 轮健康 recent 窗口，本 session R2586-R2590 累积后 R2591 瘦身）。
>
> **R2571–R2578 详记已归档**（2026-08-04 R2596 doc-control 瘦身续）：原 §最近轮次摘要 底部 8 轮（R2578→R2571，全 blockquote 体，R2579 不存在〔R2580 backfill〕）已逐字保留 → [`archive/rounds-r2571-r2578-summary.md`](./archive/rounds-r2571-r2578-summary.md)（零删减）；§最近轮次摘要 现仅留 R2580–R2595（~16 轮健康 recent 窗口，本 session R2591-R2595 累积后 R2596 瘦身）。
>
> **R2580–R2587 详记已归档**（2026-08-04 R2601 doc-control 瘦身续）：原 §最近轮次摘要 底部 8 轮（R2587→R2580，全 blockquote 体）已逐字保留 → [`archive/rounds-r2580-r2587-summary.md`](./archive/rounds-r2580-r2587-summary.md)（零删减）；§最近轮次摘要 现仅留 R2588–R2600（~13 轮健康 recent 窗口，本 session R2596-R2600 累积后 R2601 瘦身）。
>
> **R2588–R2595 详记已归档**（2026-08-04 R2608 doc-control 瘦身续）：原 §最近轮次摘要 底部 8 轮（R2595→R2588，全 blockquote 体）已逐字保留 → [`archive/rounds-r2588-r2595-summary.md`](./archive/rounds-r2588-r2595-summary.md)（零删减）；§最近轮次摘要 现仅留 R2596–R2607（~12 轮健康 recent 窗口，本 session R2599-R2607 累积后 R2608 瘦身）。
>
> **R2596–R2603 详记已归档**（2026-08-04 R2615 doc-control 瘦身续）：原 §最近轮次摘要 底部 8 轮（R2603→R2596，全 blockquote 体）已逐字保留 → [`archive/rounds-r2596-r2603-summary.md`](./archive/rounds-r2596-r2603-summary.md)（零删减）；§最近轮次摘要 现仅留 R2604–R2614（~11 轮健康 recent 窗口，本 session R2609-R2614 累积后 R2615 瘦身）。
>
> **R2604–R2611 详记已归档**（2026-08-04 R2621 doc-control 瘦身续）：原 §最近轮次摘要 底部 8 轮（R2611→R2604，全 blockquote 体）已逐字保留 → [`archive/rounds-r2604-r2611-summary.md`](./archive/rounds-r2604-r2611-summary.md)（零删减）；§最近轮次摘要 现仅留 R2612–R2621（~10 轮健康 recent 窗口，本 session R2616-R2621 累积后 R2621 瘦身）。
>
> **R2612–R2619 详记已归档**（2026-08-04 R2627 doc-control 瘦身续）：原 §最近轮次摘要 底部 8 轮（R2619→R2612，全 blockquote 体）已逐字保留 → [`archive/rounds-r2612-r2619-summary.md`](./archive/rounds-r2612-r2619-summary.md)（零删减）；§最近轮次摘要 现仅留 R2620–R2626（7 轮健康 recent 窗口，本 session R2620-R2626 累积后 R2627 瘦身）。
>
> **R2620–R2627 详记已归档**（2026-08-04 R2634 doc-control 瘦身续）：原 §最近轮次摘要 底部 8 轮（R2627→R2620，全 blockquote 体，含累积行间空行规范化）已逐字保留 → [`archive/rounds-r2620-r2627-summary.md`](./archive/rounds-r2620-r2627-summary.md)（零删减）；§最近轮次摘要 现仅留 R2628–R2634（7 轮健康 recent 窗口）。

## 通过率快照

- **make test**：13484 passed / 0 failed / 74 ignored（**R2862 复跑确认：held baseline 自 R2638 的 13193 经 getComputedStyle 序列化 + P1a 事件循环 + 缺失 Web API 系列推进至 13484**——R2638→R2862 间 +291 全为父目标 zero-web P1a〔R2704-R2767 getComputedStyle + R2768-R2769 事件循环 + R2770-R2786 atob/btoa/crypto〔randomUUID+getRandomValues〕 + TextEncoder/Decoder + URLSearchParams + structuredClone + queueMicrotask + DOMException + AbortController/AbortSignal + URL〔解析+setter+双向 searchParams〕 + EventTarget/Event spec + matchMedia + MessageChannel/MessagePort + BroadcastChannel + location 读侧 spec + CSS.escape/supports + document.cookie〕，rendering-compat 自身 surface 零回归；74 ignored 仍 = real_website_compat 网络用例。历史链：R2523 text-emphasis-color +1 unit / R2522 text-underline-offset +4 unit / R2517 text-decoration-line +1 unit，累加自 R2509 的 13184；每轮零回归 EXIT=0；R2563 周期复跑逐位确认；**R2572 counters() / R2573 ::marker / R2574 list-style-type:&lt;string&gt; / R2575 border-image-outset / R2576 border-image-width / R2577 word-break:break-word 零回归 13190/0/74 精确持平**；**R2583 会话边界 plateau-guard 复跑逐位确认**；**R2592 text-decoration shorthand thickness 接线 +1 新测试 → 13191/0/74 零回归**；**R2597 text-emphasis-style/position 继承标志修正（加 is_inherited + core.rs 断言）零回归 13191/0/74 持平**；**R2604 会话边界 plateau-guard 复跑 triple-guard 全绿 13191/0/74 精确持平**；**R2613 会话边界 plateau-guard 复跑 triple-guard 全绿 13191/0/74 精确持平（R2605-R2612 八轮纯 .md 后零环境/工具链 drift 确认）**；**R2625 会话边界 plateau-guard 复跑 triple-guard 全绿 13191/0/74 精确持平（R2614-R2624 十二轮纯 .md 后零环境/工具链 drift 确认）**；**R2635 会话边界 plateau-guard 复跑 triple-guard 全绿 13191/0/74 精确持平（R2626-R2634 九轮纯 .md 后零环境/工具链 drift 确认）**；**R2637 第 11 vein initial-value VALUE correctness 审计 + registry↔default 4-property drift（width/height/min-width/min-height Px(0.0)→Auto）纠偏 + 守卫测试 +1 → 13192/0/74 零回归**；**R2638 第 11 vein 全扫收官 + column-gap drift（Px(0.0)→Auto）纠偏 + 守卫测试 +1 → 13193/0/74 零回归，第 11 vein definitively 关闭**）
- **reftest oracle**：58.8% oracle-pass（5969/10397，+0.6pp vs R2185 baseline），57.6% credible
- **product-smoke**：welcome 17.03% / wintertc / morning item-tag:3 全 PASS
- **product-smoke-legacy**：42 fixture 迭代（39 compared + 3 skip）/ **0 struct FAIL** / 3 skip = 49-details-summary/50-mark/51-blockquote-dl-figure（**HTML5 元素越界 Tier-1（HTML 3.2/4）scope，故 by-design 无 oracle**，非缺失非待补）/ avg diff 2.36%（sum/42），19-testpage 17.23%（−5.16pp），20-mixed 11.49%（−1.64pp）；R2584 会话边界复跑确认；R2589 订正旧「9 skip」计数错误（实 3，HTML5 越界）；R2604 + R2613 会话边界复跑 0 struct FAIL 持平

---

## 当前状态概览

| 维度 | 状态 | 说明 |
|------|------|------|
| 渲染管线 | ⚠️ 全链路贯通但非一致 | HTML→CSS→Style→Layout→Paint→Composite 可运行；但 layout IFC、paint IFC 和 ZeroBrowser glyph 消费仍存在多套坐标/度量路径，`welcome.html` 已暴露用户可见错位 |
| WPT Runner | ✅ reftest 级 | 1,341 个手写 TestCase + 685 个内联 reftest（13 目录 ≥50） |
| Reftest Harness | ✅ 可用 | 分类容差、per-test fuzzy 注解、match/mismatch 模式 |
| CPU 软件渲染 | ✅ 全量图元 | render_full_scene() 支持全部 13 种图元（fills, rounded_rects, gradients, shadows, images, strokes, path_fills, path_strokes, glyphs, clips, transforms, filters, blend_modes） |
| 产品/真实静态页面视觉 smoke | ✅ 证据已持久化·持续修复 | welcome/morning.work/wintertc fixture + product-smoke + chromium Oracle 工具链就绪；**证据已持久化 `evidence/product-static/`**；**post-R632 diff（R2544 re-baseline 复测）**：welcome **17.03%**（81737/480000px，旧快照 16.16% stale）、wintertc 13.59%、morning-work 800×600 **18.15%**；★R630 修复用户痛点「文字堆叠看不清」；残余 diff = 中文字体度量（R633 Phase A 死锁）+ R109 IFC + hljs（需 JS），非证据缺口 |

---


## 下一步

> **轻量修复主线（用户 2026-07-29 裁决；R2572 框架订正 update）**：持续做有 driving test、低风险、A/B 零回归的 CSS2/parser/selector clean lever。~~R2542-R2564 经 clean-lever 8 distinct angle + 深结构可切片性全项核验 = 穷尽~~ **R2572 订正：非穷尽**——本轮第 9 angle（候选清单残留 bounded feature 定向 code-reading）**发现并 LANDED `counters()` 嵌套计数器函数**（75 driving test，A/B 三态 net 正），证明 8 angle 漏扫「候选清单列但未深验的 bounded feature」vein（R2372→R2373-R2383 谱再现）。深结构可切片性核验仍成立（Phase A IFC line-box-metric 不可切片 / DC-9 infrastructure-blocked / Phase 2 multicol 深 RFC / font-metric·@counter-style-slice2·individual-transforms net-negative 史 / R1043·R2174·font-stack user-gated）。**R2582-R2586 订正：directed code-reading probe surface 经 7 vein（directed probe + agent fan-out + exhaustive field/variant + missing-property 批量 + 伪元素 parse-vs-apply + HTML presentational-attribute 完整性，R2582-R2586 全零产出）穷尽——原 ① 不再活跃**。**R2592 订正：clean-lever surface 非穷尽——第 8 vein（shorthand→longhand 展开完整性审计，正交于前 7 consumed-轴）首验即出 lever**（`text-decoration` 简写漏展开 `text-decoration-thickness` longhand，longhand 全链路既实现仅缺接线，A/B 三态 net 正 land；详见 R2592）。**R2593 订正：第 8 vein 经全简写核验 exhaust**——逐个核验 `expand_one` 全简写 longhand 展开完整性：animation/transition/overflow/overscroll/outline/columns/column-rule/list-style/flex-flow/border-image/background **全 complete**，font（coupled font-variation infra）/border-radius（单 LengthValue 存储）**深**；text-decoration-thickness 是 8th vein 唯一 clean lever。**当前活跃自主面 = ① 低频周期 plateau-guard（make test 13191/0/74 + product-smoke + product-smoke-legacy 回归守，R2583/R2584 会话边界既绿，待行为变更/drift 累积延长间隔）+ ② 文档与代码不一致纠偏**。**后续轮勿再做系统性 field/variant/missing-property/pseudo-element/presentational-attribute/shorthand-expansion 审计（8 vein 全 exhaust，重跑零 yield）**；**R2597 开第 9 vein（inheritance-flag correctness：`is_inherited` 列表 vs spec 继承性核验），land text-emphasis-style/position 继承修正（两 longhand 全实现+paint 真消费+spec 继承但漏标；net=0 既有测试但真渲染效果）；R2598 系统差分核验 9th vein exhaust（known_properties(215)−is_inherited(42) 仅 image-rendering spec-继承但 no-op indicator defer，余 172 全 correct 非继承 + 42 inherited 全 spec-继承无误标）**。**R2602 driving-test-first 核验 10th vein 三候选轴（CSS function-resolution 完整性 / percentage-basis correctness / @media feature eval）全零产出或既实现，候选轴退役**：① function-resolution——calc 在非 length 上下文（opacity/font-weight/transition-duration/animation-delay/z-index/flex-grow/flex-shrink/flex-basis）静默丢弃但 wpt-data 仅 2 driving 全 OOS（@font-face descriptor + testharness.js 动画）+ flex-basis:calc 0 driver + translate(calc) 1 driver = testharness.js+individual-transforms user-gated → reftest 零 driving；② percentage-basis——margin/padding/abspos/translate % 经 taffy+abspos 正确按 containing-block 解析无 bug；③ @media-eval——`evaluate_media_query` 接真实 viewport 全实现，残余 fail 全 dynamic-JS。**10th vein 候选轴清空，后续勿再做 10th vein 系统审计（重跑零 yield）**。产品/legacy smoke 可见稳定性修复仍适用。**唯一推向 95% = 用户点名授权深结构专项**（最高 value = Phase A IFC line-box-metric 统一，须接受多轮 A/B 收敛风险；次 R1043/R2174/font-stack/Phase 2 multicol/individual+3D transforms）。

### 待用户决策清单

- ~~**font-metric 生产激活+A/B**~~（**R2393 已完结 = net 负，保持 dormant**）：R2202 dormant 基础设施完整，2026-08-01 用户授权激活 + product-smoke A/B（本 session R2393 复核验证）= **welcome 17.03%→17.47%（+0.44pp 恶化）、morning 中文零变化、8/8 struct PASS** → **net 负，不 land，保持 env 默认关**。根因（R2202 两假设证伪）：① welcome `sans-serif` 已从 DejaVu 换 Liberation Sans（R1263，真值≠1.164）→ 激活后 normal 行高变；② morning 全显式 line-height（无 normal）→ 不经 font-metric provider → 零变化。证据 [`evidence/font-metric-activation-ab-2026-08-01.md`](evidence/font-metric-activation-ab-2026-08-01.md)。**后续 agent 勿再以 font-metric 激活为 lever**（已实测 net 负）；若推进须与 IFC strut/half-leading 真实化打包（深结构 R834 谱系）。
- **vertical-mode native R1043**：深结构性方向，等用户授权
- **taffy replaced-element border-box R2174**：深结构性方向，等用户授权  
- **Phase A slice-3 IFC 深构造**：深结构性方向，等用户授权
- **font-stack C-dep rebuild**：user-blocked（R2025 踩坑勿推），等用户明确授权
- **margin-trim 残余深路径**（R2384 记、R2385 订正）：CSS Box Model 4 §8.3 `margin-trim` **块轴（block / block-start / block-end）已实现并消费**（R2248：`tree.rs:1029` build_subtree〔margin-trim 块 1064-1100〕布局前归零首/末 in-flow block-level 子 margin，kill-switch `ZW_MARGIN_TRIM=0` default-on；block-container-block-001 等 driving 案 self-source 0.00% PASS）。R2384 原文「layout 完全未消费」**不准确（doc-vs-code 不一致）**。实证（R2385 `make reftest-upstream css-box`）css-box 38/67 (56.7%)；29 案 margin-trim fail **全是 R2248/R2249 已 defer 的深子路径**：① 5 self-collapsing（须 margin_collapse.rs 整合）；② 7 block-in-inline（R109 Phase A）；③ 3 replaced（R2174）；④ 8 flex + 3 grid + 3 multicol-spanner（各布局模式独立 margin-trim 语义，非 build_subtree 单点）。Chrome 137+ aligned；这些深路径 = substantial 跨 layout-mode 深改，等用户授权专项
- **系统性「文档 vs 代码实际状态」核对**：R2202 发现文档滞后正导致反复盲推空转，等用户授权
- ~~**全局关键字 + `all` 简写 cascade**~~（**R2386 订正 = 本条作废；R2462/R2463 系统审计闭环**）：R2233 原文「ZW 无 per-property initial/inherit/unset/revert 处理 → all 简写无法实现（需先建全局关键字基础设施）」**两处均 stale**——① 全局关键字解析+应用**早已完整实现**（`inheritance.rs` `KeywordResolution` 五态 Inherit/Initial/Unset/Revert/RevertLayer + `compute_inherited_style_with_quirks` 全消费 + 数十测试）；② `all` 简写**R2386 已 LANDED**（cascade 展开，css-cascade 17→19，+2 真 flip 零回归）。**R2462/R2463 系统审计**（对所有 known property 检查三路径 case 覆盖）：`inherit_property`（inherit 关键字）原缺 box-shadow + contain-intrinsic-width/height → **R2462/R2463 修复，现 0 missing**；`apply_initial_value`（initial/unset 关键字）**0 missing**；apply 派发（value 路径）**0 missing**。**wide-keyword 三路径覆盖全闭环验证，勿再系统审计**。css-cascade 残余 30 fail 全非 global-keyword/all 问题：revert-layer 真层感知（13，现简化≈unset）、@scope 未实现（8）、@import-conditional/@layer 交互（9）、revert-val（1）。
- ~~**`<q>` 引号 + open-quote/close-quote 机制**~~（**R2386 订正 = 本条作废**）：R2233 原文「ZW 无 UA q::before + 无 quote 深度计数；quotes 无消费方」**stale**——`<q>` 自动引号**R2246 已 LANDED**（`resolve_q_quotes`/`count_q_ancestors`/`q_default_quote_pair` + `lib.rs:368` compute_styles 期注入 before/after 伪元素 content，复用既有 pipeline）。css-content 残余 = per-language CLDR quotes（hu/ar/lo 等）+ element-replacement（`:root{content:url()}`）+ `attr(type(<color>))`，**非核心 `<q>` 机制缺口**。**勿再以「`<q>` 引号」为核心待实施 lever**。
- **`@counter-style` 自定义计数器 at-rule**（R2233 发现）：~~css-counter-styles 73.6% 残余主因（自定义 counter at-rule + 内置样式 range/fallback 语义）；feature，等用户授权~~ **R2392 slice 1 已 land**（cyclic/fixed/symbolic/alphabetic/numeric 5 系统 + parse/registry/Custom arm/paint）；**残余 additive/extends 系统 R2394 defer**（A/B net-negative，等用户授权 slice 2）+ 内置样式 range/fallback/pad/negative 完整应用
- **individual `translate`/`rotate`/`scale` 属性**（R2368 发现）：CSS Transforms 2 §3.1，ZW 完全未实现（css-transforms 65.2%，individual-transform 子 dir 55.6%）；实现尝试 net -2 回退（须先正确处理 3D 形式 + transform-box 交互 + renderer 非可逆矩阵 apply_transform_post det≈0 清空语义）；moderate feature，等用户授权专项

### 轻量修复候选

**当前活跃轻量主线 = CSS 合规缺口定向 code-reading probe（多 vein 并行）**。R2373-R2383 连续 11 lever **彻底推翻 R2372「双路径穷尽」结论**，跨多 vein：(1) color-mix 标准空间补全+feature 收官（R2376-R2378 空间 + R2381 hue method）；(2) **CSS 属性 value-级 parse-gap vein**（R2379 outline-style:auto、R2380 aspect-ratio、R2382 justify-items/self left/right、R2383 alignment normal）；(3) CSS Color 系统色/单位/gradient-hint（R2373-R2375）。

★ **R2204 洞见 + R2373-R2383 十一连实证**：「exhausted」结论**局限于 reftest-visible + 已扫维度**；unit-test 驱动的 CSS 合规缺口经定向代码阅读持续可发现。**两类高产出模式**：① 转换/常量「已存在仅缺接线」（color-mix 空间）；② Explore 短阵广扫属性表定位 value-级缺失关键字。

**★ CSS 属性 value-级 gap vein 经多轮探针（~50 属性：Tables/Text/UI/Overflow/Grid/Flex/Multicol/Backgrounds 全扫）已 Chrome-aligned 穷尽**——勿再盲扫；caption-side 等 Chrome 不支持者补反 diverge；subgrid/宽色域需 layout/gamut（defer）。**下一 lever 须 pivot 到其他 vein**。

下一轮可接续的具体入口（逐条 verify-then-fix，TDD red→green，优先 unit-test 驱动不强依赖 wpt-data；每修一个跑 scoped test + clippy + fmt + 全量 `make test`，net≥0 land）：
- **reftest-driven vein（pivot 首选）**：scoped `make reftest-upstream FILTER=<dir>` 量未深扫 dir（css-box / css-values / css-backgrounds 边角）找离散 fail → 追踪到 parse/render bug（R2369/R2370 模式；R2372 称 exhausted 但 R2369+ 仍出 lever）。**R2460 实测 css-ui = 926/986=93.9% self-source，60 fail 全深**（box-sizing=R2174 / caret=host-layer / outline/text-overflow=IFC+font-wall / widgets=深，详见 R2460 摘要）——css-ui 无 clean lever。**R2499 实测 css-logical = 3/6=50%（cascading-001 离散 lever → cascade canonical 化 +4 真 flip css-logical+css-box）；余 3 fail 全 vertical-rl R1043-gated**——未扫 dir 仍可出离散 lever。**css-sizing(881) 与 R2174 territory 交集大、css-values(空壳)**，scoped 探针预期深案主导；若续扫须先估深案比例勿盲跑全量。**下一未扫 dir 候选**：~~css-overflow / css-ruby / css-writing-modes~~（**R2501 全扫完，均非 clean lever 来源**——css-overflow 52.9% 余 fail 全布局/host-layer/video；css-writing-modes 80.1% fail 全 vertical R1043-gated + baseline 深；css-ruby 75.9% fail 全 ruby-layout RFC defer；css-values 0 reftest；css-display 未导入）。**可达未扫 dir clean-lever surface R2501 实证穷尽**——下一 lever 须 pivot 到 doc-drift 纠偏（R2482 模式）或待用户点名深结构。
- **CSS Counter / 生成内容**：~~`counter()`/`counters()` 解析+渲染配套~~（**R2572 LANDED**：`counters(name, sep, [style])` 嵌套计数器完整实现——parse + ContentComputedValue::Counters + counter 作用域栈模型[flat HashMap→Vec，push-on-reset/pop-on-subtree-exit] + paint 祖先作用域拼接；A/B 三态门禁 net 正，75 driving test @ css/CSS2/generated-content flip PASS）；`@counter-style`（R2233 待授权）；`<q>` quotes（R2233 待授权）。
- ~~**CSS `env()` 解析**~~（**R2459 核验 = 本条作废**）：原文「`env(x,fallback)` 现被丢」**stale**——`computed.rs` `resolve_env_reference` 已正确消费 fallback（未定义 env 名 → 用 fallback，递归解析嵌套 env/var，`resolve_env_and_var` 先 env 后 var）；测试覆盖 `env(safe-area-inset-top, 10px)`→0px + `env(undefined-x, env(safe-area-inset-left))`。**勿再以「env fallback 丢失」为 lever**。
- **孤儿/死代码清理（R2383 发现，R2460 再确认）**：`parse_basic.rs` 整文件未被 `mod` 声明（非 build），是 color.rs/parse_layout.rs 重导出版本的死副本且有 auto 不一致——R2460 复核 `parse_tab_size` 在 `parse_basic.rs:1008` 与 `parse_layout.rs:610` 双份共存确证死副本。**CLAUDE.md 准则：发现无关死代码「提及但不删除」，须用户明确要求方可清理**——记此待用户点名。
- **CSS Values 4 单位表续**：~~`dppx`/`dpi`/`dpcm`~~ **R2459 已 LANDED**（`<resolution>` 全单位 + `x` 别名 + 大小写不敏感，见上 R2459）。**R2460 核验 `lh`/`rlh` 非 clean lever**：`resolve_length`（`computed.rs:18`）签名仅 `(font_size, vw, vh)`，无 line-height 上下文 → 加 `lh` 须 thread 元素 computed line-height 进所有调用点（borderline-deep 签名改）；`rlh` 在根 line-height=`normal` 时依赖 font 度量（font-wall territory）；`cap`/`ic` 同需字体度量 deferred。**勿再以 lh/rlh 为快修 lever**。
- **CSS Syntax §4/§5 其他合规缺口**：~~MQ L4 比较运算符 token 化~~（**R2542 作废**，`media_query.rs` 全 5 运算符 + 双侧范围已完整实现）；~~@layer cascade layers~~（**R2542 核验已完整实现**，CascadeOrder.layer_index + sort_key + 44 WPT 文件）；~~CDO/CDC 非顶层上下文~~（**R2543 核验 = cosmetic 非缺口**：R2204 已将 CDO/CDC tokenize 为 `Token::Comment`，parser `skip_whitespace`〔mod.rs:331〕随处跳过 Whitespace+Comment——CSS 注释本就所有上下文 ignorable，故 CDO/CDC 非顶层「parse error」仅诊断标志零渲染影响，Comment 处理已 spec-correct）；CSS Values 4 数学函数亦经 R2543 re-confirm R2280 全实现（round/mod/rem/sign/abs + trig 7 + pow/sqrt/hypot/exp/log + π/e/infinity/NaN）。**该候选条目 R2542-R2543 全清零**。
- **文档 vs 代码行号漂移续修**（R2203 verify-then-fix 模式）。
- 每修一个：仅文档→跳过昂贵 make test；连带 `.rs`→跑 scoped test-guard + 必要时全量 `make test`，net≥0 land。

期间不要借机跳入深结构（见「深结构性方向」与「待用户决策清单」）。

### 深结构性方向（等用户授权）

以下方向不自主开工，记待决策清单等用户点名：

1. **Phase A（两条独立线程，R2546 代码核验区分）**：
   - ✅ **(a) inline-box-model coherence**（[`phase-a-inline-box-model-full-design-2026-07-29.md`](./phase-a-inline-box-model-full-design-2026-07-29.md)）= **已 DONE default-on**（R2156 slice 1 + R2160-R2162 slice 2 + R2197/R2198 slice 3；env `ZW_PHASEA_MULTI_INLINE`〔`!=Ok("0")`=default-on〕+ `PHASEA_STORE_EXT` + `backfill_phasea_orphan_boxes`〔inline_finalization.rs:1027〕全在码中）→ **R2545 legacy smoke 增益之源**（37-form-controls struct PASS 3.85%、19-testpage 22.39%→17.23%）。勿再以本线程为 lever（已落地）。
   - ⛔ **(b) IFC line-box-metric unification**（[`phase-a-IFC-unification-design.md`](./phase-a-IFC-unification-design.md)）= **唯一推向 95% 的 gated 高风险深结构路径**：解 large-font（ifc-008/009/011）+ welcome/morning 文本度量残余 + R109 inline-block content 独立 layout/paint 路径（R2332 定论）。**5 轮单点解锁（R125/R198/R205/R209/R213）全 net-negative 回归**，唯 R207 narrow（pure-inline 叶文本容器 +1 font-051）成功；v1.4 裁决「fix 须随整体 unification（消灭 Path B / 墙③ baseline 语义分歧 + 墙② multicol 反向依赖）非独立可切片」；**不自主开工**（repeats R209/R213 net-negative 史），等用户授权专项 + 接受多轮 A/B 收敛风险。**R2605 v1.5 addendum design-vs-code 同步**：R885 font_metrics bridge **已激活**（v1.3「dormant」stale；`set_font_metric_map` U1b-wiring + `resolve_normal_line_height` 真消费 per-family hhea for line-height:normal），但三方协调（strut ① 0.8·fs / v_offset ② font_size / ③ tight-ink）**仍未实施**（v1.3 §12.6 step-2）——该 strut/v_offset 度量三方协调 slice 是 v1.4 整体 unification 的**可独立先行的度量层子切片**（不触及 tree-build/匿名块），provider 已 live + proven 零回归 → readiness 提升；仍 user-gated。
2. **Phase 2 嵌套 multicol fragmentation**（[`multicol-fragmentation-design.md`](./multicol-fragmentation-design.md)、[`column-aware-IFC-spec.md`](./column-aware-IFC-spec.md)）— 解 multicol-breaking（css-multicol 最大失败聚类）。R319 证纯 inline 迁移零增益，真实价值在嵌套 / 混合内容碎片化。
3. **baseline-export 真修复** — taffy 0.12.1 已支持 flex §8.5 first-baseline；fresh 实测 baseline-000~008 + flexbox-baseline-synthesis 全 near-pass font-wall，非结构性缺口。**勿再以 baseline-export 为 lever**。
4. **DC-9 blend_mode** — paint-isolation 架构（offscreen 子树渲染 + source/dest 双纹理 blend pass），低 reftest footprint（~2-4 案）。**R2548 可切片性核验 = infrastructure-blocked 非 tractable 自主深 lever**：render-foundation **无 offscreen/intermediate-buffer/render-to-texture 能力**（grep 零命中）；现状 `apply_blend_mode` 仅 flat per-element primitive（`add_blend_mode(BlendModePrimitive{rect,mode})`，painter/effects.rs:537）非真 paint-isolation；mix-blend-mode parse/store/apply/SC-trigger（R2302/R2309）齐备但**缺 offscreen 子树成组渲染基建**。须先建 render-to-texture 子树 buffer（major multi-session 基建）+ 仅 ~2-4 案价值 = 低 ROI，**不自主开工**。

---

## Done Criteria 进度

**Done Criteria 进度**：详见 [rendering-compat.md §Done Criteria](../rendering-compat.md#done-criteria)

---


## 通过率详细快照

### reftest 通过率（最新）

- **self-source 全量目录**（R2219 scoped 复测，wpt-data v1.1）：css-grid 37/49=75.5%（**R2609 fresh 复测，flat vs R2219**） + css-position 83/97=85.6% + css-tables 104/115=90.4%（**R2601 fresh 复测，+1 vs R2219 旧 103/115=89.6%，正向 drift**） + css-flexbox 337/497=67.8%（**R2607 fresh 复测，-2 vs R2219 旧 339/497，deep flex territory 非 clean lever**） + css-multicol 265/452=58.6%（**R2609 fresh 复测，+1 正向 drift vs R2219 旧 264/452**） + css-text-decor 245/250=98.0%（**R2592：text-decoration-shorthands-002 near-pass 0.95%→strict 0.00%，count 不变**）+ css-fonts 281/287=97.9%（**R2607 fresh 复测，flat vs R2219，R2413 font_metric_map 激活 line-height:normal 经证 contained**）。**7 目录聚合 self-source 1352/1747=77.4%**（**R2609 全 7-dir sweep 完成 recompute：tables +1 / flex -2 / multicol +1 / grid·position·fonts flat = net 0 = R2219 base**）（旧快照 56.5% 严重 stale——position/tables/multicol/grid 自那以后大幅改善；flexbox 68.2% 低于旧 74.2% 但 R2213-R2217 selector 改动经核验非因——flexbox fail 全 flex 布局/font-wall 深结构，无失败案用所改选择器）
- **css/selectors self-source 187/224=83.5%**（wpt-data v1.2，R2221 specificity + R2223 nth-of-invalid 后；R2221 实测 186/224=83.0%，本轮 nth-of-invalid fail→pass +1，零回归——css-flexbox 68.2% 跨 crate 复核无回归）。**R2503 全簇 triage**：37 fail 全已实现或 OOS——`:focus-within`（已 parse，需 focus 状态 host-layer defer）/`:dir()`（已 parse+match，Shadow DOM OOS）/`:nth-child of S`（已 parse，edge 非 clean）/`featureless`（@scope OOS）/`popover-has`（host-layer）/`invalidation/*`（JS dynamic defer）/`selection-image`（::selection mismatch edge）；**无 clean lever**。
- **css/css-variables self-source 173/182=95.1%**（wpt-data v1.3，R2224 var() 环 OOM 修复 + 链完全解析 + R2284 @property 注册 + R2285 light-dark dark-scheme 后；旧 0%/OOM——variable-declaration-48/49 var() 环致 6GB OOM，现 0.00% PASS；registered-property-light-dark R2285 fail→pass；9 残余 fail 全深：`@font-face`+var/CSS counters/wide-keyword 语义/shorthand var 代换）
- **css/css-conditional self-source 116/166=69.9%**（wpt-data v1.4，R2225 `@supports` and/or 混用 +3、R2226 not 混用/嵌套括号/裸 feature +4、R2227 general-enclosed + `!important` + 括号深度 prelude +9、R2228 `font` 简写求值 +1（css-supports 44/44=100% 全绿）、R2229 `@media` 空查询≡all +1、**R2230 畸形 `@supports;` 恢复 +3**；残余多 @container feature gap + at-supports selector()/font-*() 函数求值 + @media dynamic JS）
- **css/css-color 287/308=93.2% / css-pseudo 185/261=70.9%（R2588 fresh 复测，essentially flat vs 旧 186/261=71.3%；::marker R2573 既 land 稳定，残余 76 fail = first-letter/first-line Phase A IFC-deep + selection/placeholder host-layer，per R2582 伪元素审计）/ css-nesting 12/17=70.6%**（css-color R2253-R2259 +104 + R2267 color-mix +1 + R2268 RCS identity +14 + R2269 lab/lch/oklab/oklch +46 + R2270 display-p3-linear +6 + R2271 RCS rgb/hsl non-identity +2 + R2272 rebeccapurple +1 + R2273 color-mix(in lch) +2 + R2274 wide-gamut RCS + R2285 light-dark dark-scheme + **R2323 Lab/LCH/OKLab/OKLCH L 边界 gamut 映射** + **R2324 color() 百分比分量 +5** + **R2325 OKLab/OKLCH L 边界容差 +4（oklab/oklch-l-almost-0/1）**：CSS Color 4/5 色彩空间 + RCS + light-dark dark-scheme + 边界 gamut 映射基本完备；残余 opacity/filter 合成（7 案深）、contrast-color（CSS Color 6 feature）、currentcolor-003 nested、color-mix（JS Web Animations API）、tagged-images ICC、t425-hsla alpha 合成、t31 font-wall——clean lever 在 css-color 已穷尽）。
- **css/css-backgrounds 364/710=51.3%（R2590 fresh，+1 vs 旧 363/710=51.1%；border-image-outset/width R2575/R2576 dir 级影响 ~+1-2 test 小）/ css-borders 43/83=51.8%（R2483 首测，R2590 复测持平；border-image 测试在 css-backgrounds 非 css-borders 故 css-borders 无 session lever 影响）**（wpt-data v1.5；R2469 +12 body-propagation；R2476 +7 box-shadow inset；R2477 text/box-shadow 颜色位置；R2478 background-position 3/4 值语法 = +2 真 flip − 4 false-pass unmask；**R2481 background 简写 `/size` 解析 = net 0（/size parse 正确化，paint 层 size 渲染瓶颈故 background-334 仍 fail；box 累积经 A/B 证 net −3 回退保持 drop）**）——css-borders fail 簇 = **corner-shape/border-shape（bevel/scoop/notch/superellipse 角几何，paint 层深，~2% diff）** + border-image/sub-pixel；css-backgrounds fail 多 background-size/clip/origin 的 bg-image 定位裁剪（paint 层深）、background-repeat:space/round、border-width sub-pixel round；box-shadow 残余 = radius/fragmentation/table/paint-deep。**R2484 核验 image-loading 就绪**：`build_image_cache`（reftest/resources.rs:528）解码 data-URI（SVG/PNG/JPEG/GIF）+ 文件 URL（base_dir + percent-decode + 站点根 R2427）+ 提取 intrinsic size，`get_support_image_color` 提供小 swatch 色——故 bg-image/bg-size fail 簇 = **paint 渲染精度**（size/clip/intrinsic-sizing），**非 image 加载问题**，勿再查加载路径。
- **css/css-counter-styles 183/250=73.2% / css-cascade 26/49=53.1% / css-content 49/65=75.4%**（R2473 复测 css-cascade；R2233 原 184/250=73.6% / 44/65=67.7% 已 stale；旧快照 css-cascade 16/49=32.7% 亦 stale）——counter-styles：R2392 `@counter-style` slice 1 + R2445-R2451 + R2471（11 numeric）+ R2472（cjk-decimal/cambodian）预定义 cluster 已 land，**dir 整体受 CJK/Indic tofu（DejaVu 不覆盖=须 CJK 字体基建）+ @counter-style additive/extends deep（R2394 defer）+ CJK formal/informal 须 i64 counter（R2473 已 widening）+ ~100 行算法（defer）cap 在 ~73%**；cascade 残余 = `revert-layer`（R2388 基础已 land，残余 !important 交互 defer）+ `all` 简写（R2386 已 land）+ @scope（Shadow DOM OOS）+ @layer（深 cascade）；content 残余 = `<q>` 引号**已实现**（R2246）+ element-replacement dynamic/gradient deep + quotes 残余（paint_quotes A/B 证非 double-render，须另查 DOM-injection 定位）；均非新 clean lever。
- **css/css-box 41/67=61.2%**（wpt-data v1.10，R2248 margin-trim 块轴裁剪 → 38/67=56.7%；**R2499 logical sizing cascade canonical 化 → 41/67=61.2%（+3 真 flip，0 回归）**——同元素 logical+physical 尺寸 cascade 冲突一并修复；css-box reftest 即 margin-trim 子集；block-container-block-001/002 + block-start-001~004 + block-end-001/002 全 0.00% strict 真通过；残余 = grid-*/multicol-spanner-004/005/006 + 自折叠/嵌套深案，全 deferred 范围）
- **css/css-logical 3/6=50.0%**（wpt-data，R2499 首测入档，原不在快照）：cascading-001 fail→pass（logical+physical sizing cascade canonical 化）；logical-values-float-clear-1/3 horizontal 已 PASS（logical float/clear 值水平模式已实现）；**余 3 fail 全 `writing-mode: vertical-rl`**（float-clear-2/4/reftest）= R1043 vertical-mode-gated，defer；css-logical 仅 6 reftest（余为 parsing/animation 非图）。
- **css/css-overflow 274/518=52.9%**（wpt-data，R2500 首测入档，原不在快照）：line-clamp R2431/R2467 在前；**R2500 overflow-clip-margin 特性 land**（overflow-clip-margin-010 fail→pass，CSS Overflow 3 §3 `<visual-box>||<length>` 4-crate plumbing）；余 fail 簇 = overflow-clip-margin-001/013-022（**既有 flex/定位布局差异**，001 diff 4.50% 远大于 clip-margin 环 ~4400px，非特性缺口）+ clip-002..005（axis-specific clip，**R2501 实证死胡同**：HUGE-range no-clip 改动 net −1 = clip-002 布局根因无变化 + line-clamp-019 回归，全 revert；须先拆单 rect per-axis + 解 line-clamp ellipsis 交互，深 defer）+ scroll-markers（bleeding-edge 特性 defer）+ scrollbar-gutter/scroll-into-view（host 层 scroll defer）+ overflow-video-hidden（video 播放+async JS，OOS）。
- **css/css-writing-modes 631/788=80.1%**（wpt-data，R2501 首测入档，原不在快照）：157 fail 几乎全 `vrl`/`vlr`/`slr`/`srl`（vertical writing-mode = **R1043 user-gated**）；11 非 vertical fail 全 `inline-block/table-alignment-*`（6.94-16.12% = baseline/font-wall 深）；**无 clean lever**。
- **css/css-ruby 107/141=75.9%**（wpt-data，R2501 首测入档，原不在快照）：34 fail 全 ruby 布局特化（ruby-overhang-spaces / ruby-intrinsic-isize / ruby-annotation / ruby-box-generation / ruby-layout-internal-boxes = **ruby-layout RFC defer territory**）；无 clean lever。
- **css/css-values 0 reftest / css-display 未导入**（R2501 确认）：css-values 仅 parsing-only 无图测；css-display wpt-data 无该 dir——均非 reftest clean-lever 来源。
- **css/css-contain 312/429=72.7%**（wpt-data v1.10，R2369 contain-on-html/body 抑制画布背景传播后：contain-body-bg-001..004 + contain-html-bg-001..004 共 8 case 87.50%→0.00%；残余多 contain:layout/paint/size/inline-size containment feature 深实现）
- **css/css-sizing 230/562=40.9%**（wpt-data v1.10，R2468 首测 self-source + logical longhands +1：原完全不在快照；333 fail 全 structural——aspect-ratio/* ~139 = R2174 taffy border-box 同脉、intrinsic-percent-replaced 26、fit-content() 11 = intrinsic two-pass；clean lever 仅 contain-intrinsic-logical 已 land，余 defer，勿重扫）
- **css/css-images 253/460=55.0%**（wpt-data v1.10，R2370 gradient color-stop currentColor 后：color-stop-currentcolor + invalidation 96.08%→0.00%；残余 cross-fade/object-view-box/gradient-colorspace/missing-component 深特性）
- **css/css-ui 926/986=93.9%**（wpt-data v1.10，R2460 首测 self-source：原完全不在快照；60 fail 全深结构——box-sizing-007~025(~20)=R2174 replaced-element border-box / caret-*(~15)=host-layer 光标 / outline-*(~10)=sub-pixel+font-wall+outline-013 负 offset clamp 单案 / text-overflow-*(~6)=IFC+font-wall / widgets=深；value-level 特性 accent-color/caret-color/outline/tab-size 全已实现 → 无 clean lever）
- **CSS2/normal-flow 687/746=92.1%**（wpt-data v1.10，R2372 量证：DC-2 CSS 2.1 块布局基础强；59 fail 主为 inline-replaced-width/height + inline-block-replaced-width/height 聚类（replaced-element sizing = R2174 territory）+ min-height-106 等 overflow:auto scrollbar（host 层 scroll deferred）；无清洁 lever）
- **CSS2/generated-content 211/226=93.4%**（wpt-data，**R2572 首测入档**：`counters()` 嵌套计数器 R2572 LANDED 后 content-counters-001~018+/content-021~032 全 PASS（75 driving test，此前 counters() 未实现全 fail）；15 残余 fail 全非 counters——before-after-* `display:table-*` 伪元素深布局（table-parts-001 90.87% = 表格伪元素 ZW 未支持）+ content-004/113/122/123/152/171 等 1-2% font-wall/IFC 定位；**counters() clean lever 既 land，generated-content 从 counters() gap 主导升 93.4%**）
- **css/css-text 1742/1826=95.4%**（R2456 复测 self-source：**DC-5 文字排版 css-text 组件已达 95% 目标**；84 fail 残余 = IFC/font-wall/feature 深，无 clean lever）——**plateau 重框**：self-source reftest（test+ref 同经 ZW 渲染）**font-wall 相消**，故 self-source 高通过率 = 布局/算法正确性强（css-text 95.4% / CSS2 92.1% / css-counter-styles 73.2% 受 CJK tofu+@counter-style deep 限非 font-wall）；**chromium-Oracle 一致率低（~47.5%）真因 = fontdue 光栅化 font-wall**（R2455 确证），非布局 bug。
- **7 目录全量 chromium-Oracle 真一致 47.5%**（post-font-wall 全幅，旧 stale 36.4%）：grid 20/49=40.8% / position 57/97=58.8% / tables 74/115=64.3% / flexbox 298/497=60.0% / text-decor 118/242=48.8% / fonts 100/282=35.5% / multicol 157/452=34.7%（最低，chr<1%，聚合 **824/1734=47.5%**）
- **self-source strict**：295/490 (60.2%) @ 锁定 0.1%/0.5%（DC-14 真通过口径，pre-grid-expand）
- **chromium-Oracle 广义一致**：200/475 (42.1%) @ chr<1%（R391 锁定诚实基线）；严格 self-pass&chr<1% **177/475 (37.3%)**；污染 46.5%

### 产品 smoke（最新）

- **welcome**：17.03%（R2544 re-baseline 复测 post-split 无回归，81737/480000px；R632 后稳定；旧快照 16.84% stale）
- **wintertc**：13.59%（R227+R255 后）
- **morning-work**：18.15%（R630 多行 y 分行 + R632 line-height 让中文行级度量差异诚实显现）
- **fullpage**：48.65%（R255 ua_default_display 修 4× 幻影盒 89.14%→48.65%）

### 测试覆盖率

- **cargo test**：13000+ 测试全部通过（`make test`：**13484 passed / 0 failed / 74 ignored，截至 R2855**；R2638 后 held baseline 13193 经 zero-web + rendering-compat + image-decoder 系列〔R2704-R2855〕+291 推进至 13484；R2854 clear display-gate + 3 单测 13479→13482；image-decoder crate（f9fced0a）+2 13482→13484；R2554 + R2563 + R2583 + R2604 + R2613 + R2845-R2855 周期复跑逐位确认）
- **cargo clippy**：`cargo clippy --workspace --all-targets -D warnings` 通过
- **#[ignore] 测试**：74 个 ignored（real_website_compat.rs 等因本地网络不稳定的用例，不计入通过率）

---


## 历史轮次归档索引

> **为避免 master.md 无限膨胀，逐轮详记按 era 迁出 `archive/`**（内容 100% 保留，未去重、未重排）；master.md 现仅保留当前活跃状态 + 最近 5 轮摘要 + 跨会话架构入口。**2026-07-29 精简前的完整 master.md**（含 R2167–R2201 全部轮次详记 + 旧章节，零删减保底）→ [`archive/master-pre-slimdown-2026-07-29.md`](./archive/master-pre-slimdown-2026-07-29.md)。逐轮详记按 era 分档：
>
> - **R2167–R2197 + R2199c**（2026-07-29 瘦身迁出，含多行 blockquote 续行）→ [`archive/master-pre-slimdown-2026-07-29.md`](./archive/master-pre-slimdown-2026-07-29.md) 顶部轮次详记区
> - **R2201–R2458**（2026-08-04 R2561 瘦身迁出，顶部轮次详记区 258 轮 interleaved）→ [`archive/rounds-r2201-r2458-topblock-summary.md`](./archive/rounds-r2201-r2458-topblock-summary.md)
> - **R2459–R2495**（2026-08-04 R2555 瘦身迁出，§最近轮次摘要 底部 36 轮，含 R2486–R2495 plain-format 体）→ [`archive/rounds-r2459-r2495-summary.md`](./archive/rounds-r2459-r2495-summary.md)
> - **R2496–R2524**（2026-08-04 R2556 瘦身续迁，§最近轮次摘要 底部 29 轮全 blockquote 体）→ [`archive/rounds-r2496-r2524-summary.md`](./archive/rounds-r2496-r2524-summary.md)
> - **R2525–R2539**（2026-08-04 R2571 瘦身续迁，§最近轮次摘要 底部 14 轮全 blockquote 体）→ [`archive/rounds-r2525-r2539-summary.md`](./archive/rounds-r2525-r2539-summary.md)
> - **R2540–R2553**（2026-08-04 R2578 瘦身续迁，§最近轮次摘要 12 轮全 blockquote 体）→ [`archive/rounds-r2540-r2553-summary.md`](./archive/rounds-r2540-r2553-summary.md)
> - **R2554–R2562**（2026-08-04 R2585 瘦身续迁，§最近轮次摘要 底部 9 轮全 blockquote 体）→ [`archive/rounds-r2554-r2562-summary.md`](./archive/rounds-r2554-r2562-summary.md)
> - **R2563–R2570**（2026-08-04 R2591 瘦身续迁，§最近轮次摘要 底部 7 轮全 blockquote 体，R2567 不存在）→ [`archive/rounds-r2563-r2570-summary.md`](./archive/rounds-r2563-r2570-summary.md)
> - **R2571–R2578**（2026-08-04 R2596 瘦身续迁，§最近轮次摘要 底部 8 轮全 blockquote 体，R2579 不存在〔R2580 backfill〕）→ [`archive/rounds-r2571-r2578-summary.md`](./archive/rounds-r2571-r2578-summary.md)
> - **R2580–R2587**（2026-08-04 R2601 瘦身续迁，§最近轮次摘要 底部 8 轮全 blockquote 体）→ [`archive/rounds-r2580-r2587-summary.md`](./archive/rounds-r2580-r2587-summary.md)
> - **R2588–R2595**（2026-08-04 R2608 瘦身续迁，§最近轮次摘要 底部 8 轮全 blockquote 体）→ [`archive/rounds-r2588-r2595-summary.md`](./archive/rounds-r2588-r2595-summary.md)
> - **R2596–R2603**（2026-08-04 R2615 瘦身续迁，§最近轮次摘要 底部 8 轮全 blockquote 体）→ [`archive/rounds-r2596-r2603-summary.md`](./archive/rounds-r2596-r2603-summary.md)
> - **R2604–R2611**（2026-08-04 R2621 瘦身续迁，§最近轮次摘要 底部 8 轮全 blockquote 体）→ [`archive/rounds-r2604-r2611-summary.md`](./archive/rounds-r2604-r2611-summary.md)
> - **R2612–R2619**（2026-08-04 R2627 瘦身续迁，§最近轮次摘要 底部 8 轮全 blockquote 体）→ [`archive/rounds-r2612-r2619-summary.md`](./archive/rounds-r2612-r2619-summary.md)
> - **R2620–R2627**（2026-08-04 R2634 瘦身续迁，§最近轮次摘要 底部 8 轮全 blockquote 体）→ [`archive/rounds-r2620-r2627-summary.md`](./archive/rounds-r2620-r2627-summary.md)
> - **R2086–R2095**（reftest autonomous forward 穷尽调查）→ [`archive/rounds-r2086-r2095-plateau-exhaustion.md`](./archive/rounds-r2086-r2095-plateau-exhaustion.md)  
> - **R1651–R2025**（逐轮详记，~660 行）→ [`archive/rounds-r1651-r2025-accumulated-detail.md`](./archive/rounds-r1651-r2025-accumulated-detail.md)
> - **R1579–R1601**（font-wall plateau era）→ [`archive/rounds-r1579-r1601-fontwall-plateau-era.md`](./archive/rounds-r1579-r1601-fontwall-plateau-era.md)
> - **R1602–R1650**（lever scan / chrome127 era）→ [`archive/rounds-r1602-r1650-lever-scan-chrome127-era.md`](./archive/rounds-r1602-r1650-lever-scan-chrome127-era.md)
> - **R142–R302** → [`archive/rounds-r142-r302.md`](./archive/rounds-r142-r302.md)
> - **R894–R990**（multicol Phase 2 / harness JS vein / R109 §9.2.1.1 backfill / aspect-ratio / R990 ascent era）→ [`archive/rounds-r894-r990-multicol-harness-r109-aspect-era.md`](./archive/rounds-r894-r990-multicol-harness-r109-aspect-era.md)
> - **R991–R1093**（multicol spanner·Phase 2 / logical props / vertical-mode / FreeType C-dep / ::first-letter Phase A / nbsp·word-spacing / plateau era）→ [`archive/rounds-r991-r1093-multicol-spanner-freetype-firstletter-era.md`](./archive/rounds-r991-r1093-multicol-spanner-freetype-firstletter-era.md)
> - **R569–R881**（master preamble 摘要段）→ [`archive/rounds-r569-r881-master-preamble-summaries.md`](./archive/rounds-r569-r881-master-preamble-summaries.md)
>
> 更早 era（R11–R718）的归档清单见上述文件内链接。逐轮结论摘要亦见顶部「综合裁决」表。

