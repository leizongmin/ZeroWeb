# 渲染兼容性目标 — 运行时控制面板

> **▶ 当前裁决（2026-07-29 用户两次指令：① 永不停 / 待决策记账 / 继续推进；② 主做轻量修复、调文档方向防跑偏）**
>
> - **主线 = 轻量修复**（用户第 2 次指令「我们主要做轻量修复」）：沿用 `rendering-compat.md` 的 `2026-07-28 方向裁决` 允许范围——(1) 有 driving test、低风险、A/B 零回归的 CSS2/parser/selector clean lever；(2) 产品/legacy smoke 可见稳定性修复；(3) **文档与代码不一致的纠偏**（本 goal 文档滞后严重，R2202 实证 `loader.rs:429` / `text_metrics.rs:344` 等多处注释过时误导——纠偏本身是高价值轻量活）。每修一个跑 `make test` + 相关 smoke + 必要 dir oracle，net≥0 即 land。
> - **永不停原则**（用户第 1 次指令）：轻量修复持续做；遇需用户拍板事项记「待用户决策」清单并跳过，立刻转下一个轻量修复，**不因单项阻塞停整个 goal**。
> - **深结构护栏·防跑偏**（用户第 2 次指令；纠正本 session 早些「一律放行深结构直接开工」的措辞——那会让后续 agent 跑偏去啃深改）：以下**深结构多会话方向不自主开工，记待决策清单等用户点名**——font-metric 生产激活+A/B（R2202 dormant 基础设施 webview+renderer 已落地、env-gated 未激活；2026-08-01 用户授权 A/B 已完成 = **net 负保持 dormant**，见 R2393 evidence）、vertical-mode native R1043、taffy replaced-element border-box R2174、Phase A slice-3 IFC 深构造、font-stack C-dep rebuild。clean lever 九重穷尽（R2183-R2190）后若轻量修复暂无新候选，做文档纠偏 + plateau-guard，**不要**借机跳入深结构。
> - **A/B 门禁照旧**：每切片守 kill-switch + 三态 A/B（self-source reftest + product/legacy smoke + 相关 dir oracle）net≥0 才 land；net 负 → 回退、记 evidence、转下一切片。**「net 负」是转下一切片的信号，不是停下等用户的理由。**
> - **真正需用户拍板才记「待用户决策」并跳过**（清单见 `rendering-compat.md` 顶部）：不兼容/闭源许可证、破坏性 git/文件操作、改变 Mission/Done/范围边界、超大磁盘网络下载工具审批无法覆盖——**加上**上述深结构方向。

> **📍 当前 rally 进度态（R2566·2026-08-04）= 自主推进面多轴穷尽·持守 plateau-guard（纯 holding 模式）**（顶部导航锚；详情见 §下一步 / §深结构性方向）：经 R2542-R2564，**clean-lever surface 多轴确证穷尽**——① feature-existence 6× probe（MQ L4 / @layer / math / CDO-CDC / env / font-matching 族）+ IFC correctness-review deep-review 全负；② 深结构可切片性全项核验非自主 tractable（Phase A IFC line-box-metric 不可切片[5 轮 net-negative 史+v1.4 须整体 unification] / DC-9 infrastructure-blocked[render-foundation 无 offscreen] / Phase 2 multicol 深 RFC / font-metric·@counter-style-slice2·individual-transforms 皆 net-negative 史 / R1043·R2174·font-stack user-gated）；**③ R2562 fresh marked-todo（TODO/FIXME）vein 第 7 angle 核验 8 候选全 stale 历史注释 / provably-safe unwrap / niche-gated defer，零 clean bounded lever**；**④ R2564 table-layout correctness-review（correctness-review 第 2 子系统，IFC R2547 之后）subagent 报 6 候选大半自撤 + 2 残留全 MODERATE/无 driving test（orphan 嵌套 row-group 罕见 / collapsed-border width-only `>=` 乃 R291/R292 文档化故意简化非 bug），零 clean bounded lever**。**post-split triple-guard 绿**（R2544/R2545：make test 13190/0 + product-smoke welcome 17.03% + product-smoke-legacy 0 struct FAIL；R2545-R2553 零 .rs 行为变更，**R2554 周期 plateau-guard `make test` 经验复跑 13190/0/74 持平确认；R2563 周期复跑再次逐位持平**）。file-size 合规收官 / engine.rs doc-drift 清零 / 候选清单清零 / Phase A inline-box-model coherence 已 DONE default-on（= legacy 增益之源）。**后续轮：勿 re-probe clean-lever（已 2 模式穷尽）**；活跃自主面仅 ① 低频周期 plateau-guard（待行为变更/drift 累积）+ ② 文档与代码纠偏；**唯一推向 95% = 用户点名授权深结构专项**（最高 value = Phase A IFC line-box-metric 统一，须接受多轮 A/B 收敛风险）。

## 最近轮次摘要

> **📍 R2566（2026-08-04）📋 holding-tick + archive 完整性核查（R2561 顶部块迁移后首次全量 archive index link 复验）= 全绿（12/12 link resolve，0 broken）；lark-cli re-verify 仍不可用（re-confirm R2559）；零 .rs 行为变更**。承接 R2565（master.md 自洽复审零矛盾 + §下一步 freshness 刷新）。自主 surface（8 angle 穷尽）+ 控制面（自洽 + freshness 既落地）既全面完成，本轮执行 holding-tick 两项 bounded 核查（非 micro-fix spin）。**① archive 完整性核查**：§历史轮次归档索引 12 distinct archive link **全 12 resolve 至非空文件（0 broken）**；3 session-era 文件核验 intact（R2561 rounds-r2201-r2458-topblock-summary 375 行 258 轮 / R2555 rounds-r2459-r2495 77 行 / R2556 rounds-r2496-r2524 63 行）；archive/ 共 99 .md；R2558 既核 11 文件，本轮 R2561 加第 12 文件后首次全量复验 → R2561 顶部块大迁移（sed 抽 369 行）无断链无内容丢失；git status clean。**② lark-cli re-verify**：`command -v lark-cli` = NOT available（auth list `command not found`）→ re-confirm R2559 事实——run-rules 卡点通知机制本环境仍不可执行（本 session 第 2 次 re-verify，后续勿再重试省无谓 attempt）；决策内容（用户须授权 Phase A IFC）已文档化于 §待用户决策清单 + §深结构性方向，不依赖通知。**state 仍稳**：远端 sync、工作树 clean、无 .rs 行为变更 since R2541（plateau 13190/0/74 定义上成立，R2563 既经验确认）。**门禁**：纯核查零 .rs 行为变更；pre-commit guard PASS。**下一轮**：长间隔 plateau-guard（R2563 刚绿，待 .rs drift 累积；续 zero 故延长间隔）/ fresh probe（低概率，8 angle 既穷尽）/ 用户点名授权深结构（最高 value = Phase A IFC line-box-metric 统一，唯一推向 95% 路径）。

> **📍 R2565（2026-08-04）📋 master.md 自洽性复审（本 session R2562-R2564 三轮密集编辑 + anchor/snapshot 多处更新后）+ §下一步 exhaustion framing 新鲜度刷新 = 零跨章节矛盾通过；1 处 freshness fix；零 .rs 行为变更**。承接 R2564（table correctness-review 负结果）。R2557 自洽审计后历经 R2558-R2564 七轮编辑 + 本 session R2562-R2564 三轮密集编辑（已自捕并修正 R2562 anchor 重复 ② 缺陷），doc-control 自洽 mandate 要求「各章节必须自洽…发现矛盾必须先纠正」——本轮执行聚焦复审。**交叉核验**（make test 计数 / welcome diff / exhaustion angle 计数 / round-range refs / 当前轮次序 跨 anchor + §通过率快照 + §通过率详细快照 + §最近轮次摘要 + §下一步）：① **make test 计数** = 全 13190/0/74（无 stale 13184/13129 当前声称；historical 值在轮次记时间戳内）✓；② **welcome diff** = 17.03% 跨 anchor + §通过率快照 + §通过率详细快照 + 多轮次记全一致；16.84%(L51/L208)/16.16%(L116) 全标「stale」，17.47%/16.23% 系 R2393 font-metric A/B historical ✓；③ **exhaustion angle 计数**（R2564「8 distinct angle」vs R2562/R2563「7 angle」）= **非矛盾乃历史递进**（每轮记述其当时态；R2563 plateau-guard 轮在 R2564 加第 8 angle 前）✓；④ **round-range refs**（R2542-R2543 / R2542-R2548 / R2542-R2564）= 各为轮次记 contemporaneous 快照非矛盾 ✓；⑤ **当前轮次序** = R2564→R2563→R2562→R2561→R2560 反向时序正确 ✓。**结论：零跨章节矛盾**。**1 处 freshness fix**：§下一步（主导航章节）原「R2542-R2548…clean-lever 2 模式 [6× feature-existence + 1× correctness-review] = 双轴确证穷尽」**understated**（R2548 后 R2562 marked-todo + R2564 table correctness-review 等加 6 probe 未反映）→ 刷新为「R2542-R2564 经 clean-lever 多轴（feature-existence×6 + correctness-review×2[IFC+table] + shorthand + value-gap + applied-consumed + reftest-triage + doc-drift + marked-todo = 8 distinct angle）= 穷尽」，防后续 agent 误读「仅 2 模式」重探。**意义**：doc-control 自洽 mandate 经本 session 密集编辑后再核验通过（R2562 重复 ② 既修，本轮零新矛盾）；§下一步 freshness 防误读；控制面健康。**门禁**：纯 .md 审计 + freshness 刷新零 .rs 行为变更；pre-commit guard PASS。**下一轮**：长间隔 plateau-guard（R2563 刚绿延长至 .rs drift）/ fresh probe（低概率）/ 用户点名授权深结构（最高 value = Phase A IFC line-box-metric 统一，唯一推向 95% 路径）。

> **📍 R2564（2026-08-04）📋 table-layout correctness deep-review（correctness-review 第 2 子系统，IFC R2547 之后；8th distinct angle）= subagent 报 6 候选大半自撤「actually CORRECT」+ 2 残留全 MODERATE/无 driving test，零 clean bounded lever；exhaustion 经新子系统再确认；零 .rs 行为变更**。承接 R2563（周期 plateau-guard 绿）。R2547 correctness-review 仅做 IFC 一子系统（零 bounded lever），本轮 pivot 第 2 子系统 = **table layout**（ZW 自实现 ~5400 行 table.rs/table_grid.rs/table_types.rs/table_cell_content.rs/table_shrink.rs/table_borders.rs/table_float_fix.rs/table_vertical.rs，非 vendored taffy；css-tables 89.6% 12 fail 未分类，有 R1653 caption bug 史）。**general-purpose subagent 审列宽/border-collapse/cell-sizing/span/baseline，报 6 候选**——逐一代码核验（守 R2547/R2562 教训「subagent finding 须代码核验方行动」）：① table_borders.rs:252 rowspan 底边 `>=` / ② table_grid.rs:324 direct cell col cursor / ③ table.rs:1095 perimeter spacing / ④ table.rs:856 col_max_widths `.take().skip()` —— **4 候选 subagent 自撤「actually CORRECT」**（核验确证：rowspan `>=` per CSS 2.1 §17.6.2 正确 / direct cell cursor 累加正确 / perimeter spacing 既修正确 / `.take().min(col_count)` bounds 安全）；⑤ **table_grid.rs:99-110 orphan 嵌套 row-group cell indexing**（`child_index: rg_child_idx` + `parent_rg_idx: Some(*child_idx)`）= **标准层级 indexing**（child_index = 嵌套 rg children 内 cell 位，parent_rg_idx = 嵌套 rg 在 orphan children 内位，consumer `row_box.children[parent_rg_idx].children[child_index]` 一致），**非 bug**；且 orphan 嵌套 row-group（tbody-in-tbody）极罕见无 driving reftest，非 clean lever 无论对错；⑥ **table.rs:1700-1728 collapsed-border table-size 修正 `table.border >= cell.border` width-only** = **R291/R292 文档化故意简化非 bug**——注释明示「table border 胜出（>=）时 cell border 被覆盖才扣」；agent 称应改用 CSS 2.1 §17.6.2.1 完整 conflict resolution（含 style priority）= **MODERATE 非 bounded**（须协调 resolve_collapsed_borders 输出 10-20 行 + A/B 风险回归 R291/R292 subpixel-collapsed-borders-001 fix + niche border-conflict-equal-width-different-style driving test 未验证），**非 clean lever**。**结论**：table-layout correctness-review（8th distinct angle / correctness-review 第 2 子系统）零 clean bounded lever——6 候选 4 自撤 + 2 残留全 MODERATE/无 driving test / 文档化简化非 bug。**exhaustion 经 8 distinct angle 再确认**（feature-existence×6 + correctness-review×2[IFC+table] + shorthand + value-gap + applied-consumed + reftest-triage + doc-drift + marked-todo）。**教训钉案（控制面记）**：① table.rs:1700 collapsed-border width-only `>=` 乃 R291/R292 故意简化（注释明示），完整 §17.6.2.1 style-priority conflict resolution = MODERATE + A/B 风险，**勿以之为 clean lever**；② subagent correctness-review 报告 unreliable（R2547 IFC + R2564 table 均 self-retracting/waffling + 零存活 clean lever）—— **correctness-review angle 经 2 子系统穷尽，后续轮勿再 dispatch correctness-review subagent**（低 yield）。**门禁**：纯核查零 .rs 行为变更（R2541 后仍 zero），不跑 redundant make test（R2563 刚绿）；pre-commit guard PASS。**下一轮**：长间隔 plateau-guard（待 .rs drift，R2563 刚绿延长间隔）/ fresh probe 其他 vein（低概率）/ 用户点名授权深结构（最高 value = Phase A IFC line-box-metric 统一，唯一推向 95% 路径）。

> **📍 R2563（2026-08-04）📋 周期 plateau-guard = `make test` 13190/0/74 全绿（R2554 后 ~9 轮 gap 首次经验复跑；零 .rs 行为变更 since R2541，推理 plateau 稳经实证确认）— R2554 周期节奏既到期，sanctioned holding 健康守；零 .rs 行为变更**。承接 R2562（fresh marked-todo probe 负结果）。自主 surface（clean-lever 7 angle 穷尽 + 深结构可切片性全项核验）既尽，本轮执行锚点「低频周期 plateau-guard（make test 回归守）」——R2554（最后一次经验 make test）后 ~9 轮 gap（R2555-R2562 全 .md/comment 零 .rs 行为变更，推理上零回归，但周期 guard 意义正是 holding 期偶发经验确证，非每轮靠推理跳过致 guard 永不运行〔R2554 rationale〕）。**结果**：`make test`（test-guard 包裹 `--per-proc-mem 10 --total-mem 28`，workspace `--exclude zero-render-foundation`〔Makefile 既定 wgpu headless GPU 阻塞〕，`--test-threads=2`）**exit 0 / 0 FAILED / 0 error/panic / 13190 passed / 0 failed / 74 ignored**——**与 R2554 baseline 逐位一致**（per-binary 顶级：css-parser 2819 / style-system 2121 / layout-engine 1338+1335 / engine paint 532[31s] / wpt-runner reftest 131+10ignored[137s]），实证 plateau 稳（R2554→R2563 期间 R2555-R2562 纯 .md/comment 零回归）。**build 缓存**：最后 .rs mtime = R2551 text.rs:649 注释 fix（2026-08-04T01:35），R2554 已 build 含此；R2554 后零 .rs 变更 → cargo 跳过编译仅跑测试，wall-clock 合理（首跑 EXIT=0 确认完成；二跑全量捕获取权威 summary，两跑均绿）。**意义**：周期 plateau-guard 经验绿（holding 健康）；13190/0/74 经 R2544→R2554→R2563 三次复跑确认 stale 非虚；自主推进面仍 7 angle 穷尽。**门禁**：纯回归守零 .rs 行为变更；pre-commit guard PASS。**下一轮**：续长间隔 plateau-guard（R2563 刚经验绿，下次再延长间隔至 .rs drift 累积）/ fresh probe（低概率）/ 用户点名授权深结构（最高 value = Phase A IFC line-box-metric 统一，唯一推向 95% 路径）。

> **📍 R2562（2026-08-04）📋 fresh marked-todo（TODO/FIXME/XXX/unimplemented）probe（第 7 distinct angle，避免 holding-tick spin）= 8 候选代码核验全 stale 历史注释 / provably-safe unwrap（非 bug）/ niche-gated defer，零 clean bounded lever；exhaustion 经新 vein 再确认；零 .rs 行为变更**。承接 R2561（doc-control 瘦身 FULLY 收官）。既 feature-existence×6 + correctness-review + shorthand + value-gap + applied-consumed + reftest-triage + doc-drift 全穷尽，本轮 pivot 唯一未扫 vein = **developer-marked TODO/FIXME/XXX/`unimplemented`/`todo!()`**（code-reading 而非 reftest-visible）。**Explore agent 扫 6 rendering crate（engine/style-system/css-parser/layout-engine/dom/render-foundation）报 8 候选，逐一代码核验**（守 R2547 教训「subagent finding 须代码核验方行动」）：① **cascade.rs:271 `all` 简写「未实现」= STALE 历史注释非 live TODO**——lines 273-293 **全实现** `all` 展开为 longhand 虚拟声明（kill-switch `ZW_ALL_SHORTHAND=0` default-on，排除 `direction`/`unicode-bidi` per CSS All 1 §3），R2386 已 LANDED；注释 line 271「未实现前…从未生效」描述 PRE-R2386 历史上下文，agent 误读为 live TODO；**非 lever**。② **collect_items.rs:193 `style.unwrap()` = 非 bug**——前置 `is_some_and` 闭包同步求值，`unwrap` provably safe；改 `if let Some` 纯 churn 零行为变更（CLAUDE.md §3「不重构无问题代码」）；**非 lever**。③ **parse_extended_border.rs:557 `.strip_prefix(',').unwrap()` = 非 bug**——`if after.starts_with(',')` 前置则 `strip_prefix(',')` provably 不 fail；defensive-cleanup churn；**非 lever**。④ **parse_extended.rs:493 多 item content 含 url()/attr() defer** = 真缺口但 niche：单 item `Url(String)` variant 已存在（R1988），多 item List fallback None；css-content 残余 = element-replacement dynamic/gradient deep 无 driving reftest；speculative feature add 违 code-guidelines §2；**非 clean lever**。⑤ **parse_extended.rs:134 `<inset()>` margin-trim = agent 误读**：`<inset()>` 非 margin-trim 形式（CSS Box 4 §margin-trim 仅 keyword），margin-trim 已在待决策清单「残余深路径」defer；**非 lever**。⑥ inherit.rs:445 column-count/width inherit = MODERATE（multicol 结构缺口，agent 自判 deferred）；⑦ parse_misc.rs:376 sideways glyph rotation = DEEP（vertical R1043 paint-side）；⑧ renderer/mod.rs:1688 GPU filters = DEEP infra。**结论**：marked-todo vein（第 7 distinct angle）零 clean bounded lever——8 候选全 stale 历史注释 / provably-safe unwrap（非 bug）/ niche-gated defer / DEEP infra。**exhaustion 经 7 distinct angle 再确认**（feature-existence×6 + correctness-review + shorthand + value-gap + applied-consumed + reftest-triage + doc-drift + **marked-todo**）。**教训钉案（控制面记）**：① 历史注释「未实现前…」描述 PRE-fix 状态非 live TODO，勿误读为缺口（cascade.rs:271 谱）；② `if cond { x.unwrap() }` 中 cond 前置的 unwrap 为 provably safe，非 bug（collect_items:193 / parse_extended_border:557 谱）。**门禁**：纯核查零 .rs 行为变更（R2541 后仍 zero），不跑 redundant make test（R2554 13190/0/74 既绿 + 零 .rs 变更）；pre-commit guard PASS。**下一轮**：长间隔 plateau-guard（待 .rs drift，R2554 后仍 zero 变更故延长间隔）/ fresh probe 其他 vein（低概率）/ 用户点名授权深结构（最高 value = Phase A IFC line-box-metric 统一，唯一推向 95% 路径）。

> **📍 R2561（2026-08-04）🔧 doc-control 瘦身最大块：顶部轮次详记区 258 轮（R2201–R2458）迁出至 [`archive/rounds-r2201-r2458-topblock-summary.md`](./archive/rounds-r2201-r2458-topblock-summary.md)（100% 逐字保留）；master.md 598→229 行 / ~838→115KB（-62%）；纯 .md 零 .rs 行为变更**。承接 R2560（fresh shorthand probe 负结果）。R2555-R2556 已迁 §最近轮次摘要 底部（R2459-R2524），但**顶部轮次详记区**（anchor 后、§最近轮次摘要 前，lines 12-379）仍是最大块（258 轮 R2201-R2458，~370 行 dense blockquote，interleaved 非严格时序）。本轮迁出该最大块。**实施**（同 R2555-R2556 sed 按行号低风险模式，先核 12-379 全 blockquote/blank 无 must-stay 内容）：① 抽取 lines 12-379（R2391→R2223 file-order，258 轮）→ 新 `archive/rounds-r2201-r2458-topblock-summary.md`（8 行 header + 368 行原文 = 375 行，首行 R2391 / 末行 R2223 / 258 📍 / era min R2201 max R2458 核验，**100% 逐字保留未删减未重排未改格式**）；② master.md `sed -i '12,380d'`（删 369 行含 1 trailing blank）→ 598→229 行，boundary 核验 anchor(11)→blank→`## 最近轮次摘要`(13) 干净（单 blank）；③ §轮次详记归档 + §历史轮次归档索引 各加 R2201-R2458 era 条目。**关键设计**：迁出前实证关键决策已沉淀于结构化章节（font-metric R2393 dormant / font-weight·style matching landed / Phase A inline-box-model DONE 在 §下一步 + §深结构性方向 + §通过率快照），迁出详记不丢失当前指引；跨轮引用「见 R2417」等仍 resolve 至 archive 文件。**验证全绿**：master.md 229 行 / 115KB；H2 结构 8 section 全在序；§最近轮次摘要 R2525-R2560（recent 窗口）+ 结构化章节全在；archive 文件 375 行含 R2201-R2458 全 258 轮。**意义**：**doc-control 瘦身 FULLY 收官**——三轮累计（R2555 R2459-R2495 + R2556 R2496-R2524 + R2561 R2201-R2458）master.md **710→229 行（-68%）/ 1MB→115KB（-89%）**，迁出 323 轮详记；控制面从 1MB 难导航降至 115KB 精简（Read 工具可 1-2 chunk 读完结构化章节）；master.md 现仅留 ruling + anchor + recent 轮次摘要 + 结构化章节（状态/下一步/DC/通过率）= 真正 navigable。自主推进面仍穷尽（archive 非向 95% 推进，乃控制面健康维护）。**门禁**：纯 .md doc 重组零 .rs 行为变更，不跑昂贵 make test；pre-commit guard PASS。**下一轮**：doc-control 瘦身既 FULLY 收官，自主侧仅余 ① 长间隔 plateau-guard（待 .rs drift）+ ② fresh probe（低概率）+ ③ 用户点名授权深结构（最高 value = Phase A IFC line-box-metric 统一，唯一推向 95% 路径）。

> **📍 R2560（2026-08-04）📋 fresh shorthand probe（R2476-R2486 sibling vein 续，避免 holding-tick spin）= 8 个 under-checked shorthand 核验全已 dispatch 或 niche/gated，无 clean lever；exhaustion 经新角度再确认；零 .rs 行为变更**。承接 R2559（holding-tick，lark-cli 不可用）。R2557-R2559 既声明 holding 全面完成，本轮不做 holding-tick spin，转用唯一正期望值动作 = fresh probe（低概率但 upside = 破 plateau）。**探测**（productive「sibling function / under-checked shorthand」vein，code-reading grep 核 `expand_one` dispatch table）：核 8 个 R2476-R2486 未覆盖 shorthand——① **`columns`**（column-width+count）→ `expand_columns`（mod.rs:427/1641）✓；② **`outline`**（width+style+color）→ `expand_outline`（:420/1589）✓；③ **`place-{items,content,self}`** → `expand_place`（:395-397/1403）✓；④ **`column-rule`** → `expand_column_rule`（:444）✓；⑤ **`gap`**（row+column，:432 block）✓；⑥ **`flex-flow`**（direction+wrap，:325 block）✓；⑦ **`grid-area`** → `expand_grid_area`（:392）✓；⑧ **`grid-template`** → `expand_grid_template`（:400）✓。**未 dispatch（落 `_ => mk(prop,val)` fallback）**：**`grid`** mega-shorthand（CSS Grid 最复杂简写，template rows/cols/areas + auto-flow 位置敏感文法，reftest footprint 低——grid-template/grid-area 已覆盖常见用例，niche）+ **`mask`** shorthand（CSS Masking 1，**mask 渲染 gated**：R2487 核 mask URL 图像加载未实现 + luminance 合成深 → 即使展开 longhand 亦不消费，非 clean lever）。**结论**：8 shorthand 全已 dispatch 或 niche/gated，**无 clean bounded lever**——shorthand vein 经 fresh 角度（under-checked 候选）再确认穷尽（R2476-R2486 + 本轮 R2485-R2486 模式无遗漏）。**意义**：fresh probe 负结果入档（防后续轮重探这 8 shorthand）；exhaustion 经第 N 角度（feature-existence + correctness-review + reftest-triage + sliceability + **under-checked-shorthand**）再确认；本轮为正期望值尝试（非 holding spin），结果如预期低概率。**下一轮**：续 fresh probe 其他 vein（概率低）/ 长间隔 plateau-guard（待 .rs drift）/ 用户点名授权深结构（最高 value = Phase A IFC line-box-metric 统一）。

> **📍 R2559（2026-08-04）📋 holding-tick：lark-cli 本环境不可用 → run-rules 卡点通知机制无法执行（环境事实记录）；自主 surface 仍穷尽，holding 继续；零 .rs 行为变更**。承接 R2558（post-archive 完整性核查全绿）。R2557 既声明自主 holding 全面完成，本轮无新 lever/doc-drift/archive 候选。**环境事实**：本轮拟触发 run-rules「卡点通知」（项目 reftest plateau 长期阻塞、唯一向 95% 推进 = 用户授权深结构），但 `command -v lark-cli` = NOT available → 飞书通知机制本环境不可执行（后续本环境 agent 勿重试，省无谓尝试）。**state 仍稳**：远端 up-to-date、工作树 clean、无 .rs 行为变更 since R2541（plateau 13190/0/74 定义上成立）。**卡点决策已文档化**：rendering-compat.md §待用户决策清单 + master.md §深结构性方向 已列全 gated 深结构 5 项（Phase A IFC / R1043 / R2174 / font-stack / Phase 2 multicol），用户 review 时可见——通知机制仅为主动触达，决策内容不依赖通知。**门禁**：纯 .md 零 .rs 行为变更；pre-commit guard PASS。**下一轮**：长间隔周期 plateau-guard（待 .rs drift）/ 用户点名授权深结构专项（最高 value = Phase A IFC line-box-metric 统一）。

> **📍 R2558（2026-08-04）📋 holding-tick + post-archive 完整性核查（R2555-R2556 archive 工作验证）= 全绿；零 .rs 行为变更**。承接 R2557（自洽审计通过 + 自主 holding 全面完成声明）。本轮 R2557 既声明自主 surface 尽 + holding 完，无新 lever/doc-drift/archive 候选——执行 post-archive 完整性核查（验证近两轮 R2555-R2556 archive 重组未破坏控制面）：① **远端 up-to-date**（git pull --rebase 无新提交，无 drift）；② **工作树 clean**；③ **无 .rs 行为变更 since R2541**（R2551/R2542 纯注释）→ plateau 13190/0/74 定义上成立（R2554 已经验确认）；④ **11 个 era archive 文件全存在且非空**（含本轮 R2555 rounds-r2459-r2495-summary.md 77 行 + R2556 rounds-r2496-r2524-summary.md 63 行；index 链接全 resolve，无悬空引用）。**结论：post-archive 完整性绿，控制面健康**。**门禁**：纯核查零 .rs 行为变更；pre-commit guard PASS。**意义**：post-archive 完整性验证入档（archive 重组无内容丢失/无断链）；确认 rally 处于**纯 holding 模式**（自主 surface 既尽，等用户深结构授权），后续 holding-tick 轮可轻量（无新工作则仅核查 + CONTINUE，避免 micro-fix spin）。**下一轮**：长间隔周期 plateau-guard（待 .rs drift）/ 用户点名授权深结构专项（最高 value = Phase A IFC line-box-metric 统一，唯一推向 95% 路径）。

> **📍 R2557（2026-08-04）📋 master.md 自洽性审计（doc-control 策略 mandate「各章节必须自洽…发现矛盾必须先纠正」）= 零矛盾通过；自主 holding 工作全面完成声明；零 .rs 行为变更**。承接 R2556（doc-control 瘦身收官，master.md 710→590）。自主 surface（clean-lever 双轴穷尽）+ holding 维护（doc-drift 三分类尽 R2551-R2553 / plateau-guard 经验绿 R2554 / archive 收官 R2555-R2556）既全面落地，本轮执行 doc-control 策略明示 mandate 的自洽性审计（经 6 轮 .md 编辑 + archive 重组后核验无跨章节矛盾）。**交叉核验**（anchor §当前裁决/当前 rally 进度态 vs §通过率快照 vs §当前状态概览 vs §下一步 待用户决策清单/深结构性方向）：① **make test 计数** = anchor 13190/0/74 + §通过率快照 13190/0/74（R2523+1/R2522+4/R2517+1 累加自 R2509 13184）+ §当前状态概览 R2544 re-baseline——**全一致**（lines 51-73 旧 13102/13117 等系历史轮次记时间戳非当前声称）；② **welcome diff** = 17.03% 跨 anchor + §通过率快照 + §当前状态概览 + 多轮次记全一致（17.47%/16.23% 系 R2393 font-metric A/B 实验 historical 值非当前）；③ **font-metric 状态** = §下一步 待决策清单 line 478「R2393 已完结 = net 负保持 dormant」+ anchor 一致（非 pending）；④ **font-weight matching** = R2417 已 LANDED（不在待决策 pending 列表，无「landed vs gated」矛盾）；⑤ **深结构 5 项**（Phase A IFC / R1043 / R2174 / font-stack / Phase 2 multicol）跨 anchor + §下一步 + §深结构性方向 列表一致。**结论：零跨章节矛盾，控制面自洽**（残余通过率数字新鲜度非「矛盾」范畴——属重测更新，须昂贵 reftest 重跑，非本审计 scope）。**意义**：doc-control 自洽 mandate 兑现（审计 trail 入档，后续轮无须重审除非新编辑引入矛盾）；**自主 holding 工作全面完成**（clean-lever 穷尽 + doc-drift 尽 + plateau-guard 经验绿 + archive 收官 + 自洽审计通过）——后续轮在用户授权深结构前将很轻（仅长间隔周期 plateau-guard 待 .rs drift）。**门禁**：纯 .md 审计记录零 .rs 行为变更；pre-commit guard PASS。**下一轮**：长间隔周期 plateau-guard（待 .rs 行为变更/drift 累积）/ 用户点名授权深结构专项（最高 value = Phase A IFC line-box-metric 统一，唯一推向 95% 路径）。

> **📍 R2556（2026-08-04）🔧 doc-control 瘦身续收官：§最近轮次摘要 底部 29 轮（R2496–R2524）迁出至 [`archive/rounds-r2496-r2524-summary.md`](./archive/rounds-r2496-r2524-summary.md)（100% 逐字保留）；master.md 643→~589 行，§最近轮次摘要 现 R2525–R2556（~28 轮健康 recent 窗口）；纯 .md 零 .rs 行为变更**。承接 R2555（doc-control 瘦身启动：R2459–R2495 已迁，710→643）。本轮续 fifo archive 模式：master.md 仍 643 行/910KB（§最近轮次摘要 仍 ~57 轮 R2496–R2555），继续迁 oldest 29 轮至健康 recent 窗口。**实施**（同 R2555 sed 按行号低风险模式）：① 抽取 §最近轮次摘要 底部 29 轮（R2524→R2496，lines 440-496，全 blockquote 体）→ 新 `archive/rounds-r2496-r2524-summary.md`（6 行 header + 57 行原文 = 63 行，首行 R2524 / 末行 R2496 核验，**100% 逐字保留**）；② master.md `sed -i '440,497d'`（删 58 行含 trailing blank）→ 643→585 行，boundary 核验 R2525→blank→`## 轮次详记归档` 干净；③ §轮次详记归档 + §历史轮次归档索引 各加 R2496–R2524 era 条目。**验证全绿**：H2 结构 8 section 全在序；§最近轮次摘要 现 R2525–R2556 ~28 轮（健康 recent 窗口）；archive 文件 63 行含 R2496–R2524 全条目。**意义**：doc-control 瘦身**收官**——master.md 710→589 行（-121 行 / -17%）/ 1MB→~0.83MB（两轮 R2555+R2556 累计迁出 65 轮 R2459–R2524），§最近轮次摘要 从 ~93 轮降至 ~28 轮健康窗口；后续瘦身待新轮累积（R2525+ 暂留 recent 上下文）。自主推进面仍双轴穷尽（archive 非向 95% 推进，乃控制面健康维护）。**门禁**：纯 .md doc 重组零 .rs 行为变更，不跑昂贵 make test；pre-commit guard PASS。**下一轮**：doc-control 瘦身既收官，自主侧仅余 ① 低频 plateau-guard（R2554 刚经验绿，延长间隔）+ ② 用户点名授权深结构（最高 value = Phase A IFC line-box-metric 统一，唯一推向 95% 路径）。

> **📍 R2555（2026-08-04）🔧 doc-control 策略瘦身：§最近轮次摘要 底部 36 轮（R2459–R2495）迁出至 [`archive/rounds-r2459-r2495-summary.md`](./archive/rounds-r2459-r2495-summary.md)（100% 逐字保留）；master.md 710→~642 行，§最近轮次摘要 现 R2496–R2555 全 blockquote 干净体；纯 .md 零 .rs 行为变更**。承接 R2554（周期 plateau-guard 经验绿）。自主 surface 既尽 + plateau 既经验确认稳，转 doc-control 策略明示 mandate「master.md 不允许无限增长 — 过时内容须迁移到 archive」——master.md 已 1MB/710 行（§最近轮次摘要 占 ~185 行 R2459–R2554 + R2486–R2495 旧 plain-「承接」format 体致格式不一致）。**实施**（sed 按行号抽取，内容不进上下文 = 低风险机械迁移）：① 抽取 §最近轮次摘要 底部 36 轮（R2495→R2459，lines 496-566）+ trailing blank → 新 `archive/rounds-r2459-r2495-summary.md`（6 行 header + 71 行原文 = 77 行，首行核验 = R2495 plain、末行 = R2459 blockquote，**100% 逐字保留未删减未重排未改格式**）；② master.md `sed -i '496,567d'`（删 72 行）→ 710→638 行，boundary 核验 R2496→blank→`## 轮次详记归档` 干净（单 blank）；③ §轮次详记归档 + §历史轮次归档索引 各加 R2459–R2495 era 条目（指向新 archive 文件）。**关键设计**：sed 按行号抽取（非读入上下文）= 边界核验靠 grep marker（首 R2495 / 末 R2459）+ 行数对账（71 行），内容零篡改风险；archive 文件 100% 保留含 plain-format 体（doc-control「archive 只追加不改」）。**门禁**：纯 .md doc 重组零 .rs 行为变更，不跑昂贵 make test；pre-commit guard PASS。**意义**：master.md 瘦身（710→642 行 / 1MB→~0.9MB）改善长期 rally 可导航性（Read 工具单次限 ~40 行，1MB 控制面每轮多 chunk 读开销大）；§最近轮次摘要 清除 plain-format 不一致（现全 blockquote R2496–R2555）；doc-control 策略 mandate 兑现；established fifo archive 模式续用（后续轮可续迁 R2496+ 底部）。自主推进面仍双轴穷尽（archive 非向 95% 推进，乃控制面健康维护）。**下一轮**：续低频 plateau-guard（R2554 刚经验绿，下次延长间隔）/ 续 doc-control 瘦身（§最近轮次摘要 可续迁 R2496–R2520 底部）/ 用户点名授权深结构（最高 value = Phase A IFC line-box-metric 统一）。

> **📍 R2554（2026-08-04）📋 周期 plateau-guard = `make test` 13190/0/74 全绿（R2544 后首次经验复跑，~9 轮 gap；零回归实证 plateau 稳）— 自主 clean-lever/doc-drift surface 既尽后 prescribed 低频周期回归守；零 .rs 行为变更**。承接 R2553（gated 设计文档 stale ref 精细化收官）。本轮自主 surface（clean-lever 双轴穷尽 + doc-drift 三分类尽）既无新 .rs lever 亦无新 doc-drift 候选，转执行锚点「低频周期 plateau-guard（make test 回归守）」——R2544（最后一次经验 make test）后 ~9 轮 gap，期间仅 R2551 改 1 行 .rs 注释 + R2552/R2553 纯 .md，推理上零行为变更，但周期 guard 的意义正是 holding 期偶发经验确证（非每轮靠推理跳过致 guard 永不运行）。**结果**：`make test`（test-guard 包裹 `--per-proc-mem 10 --total-mem 28`，workspace `--exclude zero-render-foundation`〔Makefile 既定：wgpu headless GPU 测试 + test-guard 1800s 总超时〕，`--test-threads=2`）**exit 0 / 45 test binary 全 ok / 0 FAILED / 13190 passed / 0 failed / 74 ignored**——**与 R2544 baseline 逐位一致**，实证 plateau 稳（R2551 注释 + R2552/R2553 doc 零回归）。**意义**：周期 plateau-guard 经验绿（holding 健康）；自主推进面仍双轴穷尽，doc-drift surface 三分类尽；13190/0/74 经 R2544→R2554 复跑确认 stale 非虚。**下一轮**：续低频 plateau-guard（待 .rs 行为变更/drift 累积，刚跑绿下次延长间隔）/ 文档纠偏（surface 既尽概率低）/ 用户点名授权深结构（最高 value = Phase A IFC line-box-metric 统一）。

> **📍 R2553（2026-08-04）🔧 文档纠偏续 = gated 设计文档 stale ref 精细化分类（订正 R2552 过粗 defer）：r109 `extract_layout` 同文件机械漂移 1 fix（engine.rs:1452→1025）；phase-a-IFC 8 OOB 实证为「设计时位置 + R2298 自勘误」**非缺陷（修反违背 doc 指引）**；vertical-mode 1 跨文件迁移 defer；纯 .md 零 .rs 行为变更**。承接 R2552（10 gated 设计文档 OOB 标 defer 待精细化）。本轮对 R2552 一刀切标「defer(hunting=engaging 深结构)」的 10 gated 设计文档 stale ref 逐个 grep 核实，按「同文件机械漂移（安全修）vs 跨文件迁移（defer）vs 设计时锚（非缺陷）」三分类精细化。**分类结果**：① **r109-vertical-native-layout-design.md:47 `engine::extract_layout`（engine.rs:1452）= 同文件机械漂移 → FIXED**：函数仍在 engine.rs（唯一 `fn extract_layout` @ engine.rs:1025），仅因 R2513 sizing-helpers 抽出致 engine.rs 缩行 1452→1025；语义核验（doc comment「垂直书写模式…提取时交换回来…视觉坐标」= doc reverse-swap 描述）→ 修 1452→1025，re-scan r109 doc **0 OOB** ✓。② **phase-a-IFC-unification-design.md 8 OOB = 非缺陷，修反违背 doc 指引**：核实 `compute_final_inline_layouts` def 已**跨文件**迁 engine.rs:1668→`inline_finalization.rs:607`（R2298 拆分），且**该 doc line 18 已有 R2298 自勘误**明示「本文多处 `engine.rs:NNNN` 为**设计时位置**（engine.rs 未拆分前）…实施 Phase A 前**须按函数名重新定位**（行号已 stale，**勿直接引用**）」——故此 8 OOB 是**有意保留的设计时锚 + 自勘误指引读者按函数名重定位**，非 R2552 所框「待修 drift bug」；**修行号反会违背 doc「勿直接引用」指引**（正确范式 = 函数名 grep，非信行号）。③ **vertical-mode-text-positioning-rfc.md:33 `break_items_into_columns`（inline/mod.rs:1659）= 跨文件迁移 defer**：函数 def 已迁 `inline/break_lines.rs:46`，概念散布 break_lines.rs(def)+inline/mod.rs:100/308(注释)，精确映射 engagement-adjacent → defer（实施 R1043 时按函数名重定位，同 phase-a-IFC 范式）。**精细化订正 R2552**：R2552 将 10 gated OOB 统称「defer(hunting=engaging 深结构)」**过粗**——本轮实证三分类：r109 1 是安全同文件机械修（已修）/ phase-a-IFC 8 是「设计时锚+自勘误」**非缺陷（修反有害）**/ vertical-mode 1 是跨文件迁移 defer。**plateau-guard**：本轮纯 .md 1 行 fix，零 .rs 行为变更（R2544 后仅 R2551 改 1 行 .rs 注释），按常驻指令「待 drift」+「仅文档→跳过昂贵 make test」不跑 redundant make test。**门禁**：纯 .md；pre-commit guard PASS。**意义**：gated 设计文档 stale ref 三分类精细化（同文件机械修 vs 跨文件 defer vs 设计时锚非缺陷）——纠正「所有 OOB 都是 drift bug」隐含假设；**phase-a-IFC doc 的 R2298 自勘误模式（设计时行号 + 「按函数名重定位」指引）= gated 设计文档正确 stale 处理范式，后续轮勿再以「修 phase-a-IFC engine.rs 行号」为 lever**。**下一轮**：续低频文档纠偏 / plateau-guard（待 .rs drift）/ 用户点名授权深结构（最高 value = Phase A IFC line-box-metric 统一）。

> **📍 R2552（2026-08-04）🔧 文档纠偏续（用户常驻指令「文档纠偏+plateau-guard」）= plateau 健康独立核验（不盲信前轮）+ R2551 扫描面从 .rs 源注释扩展到 .md 文档 → 1 stale ref 修复（dc-progress.md:178 DC-14 防放宽不变量单测名+位）；29 OOB 跨 6 文件分类处置（18 历史/superseded 留存 + 10 gated 设计文档 defer + 1 fix）；plateau-guard 经评估本轮不跑（R2544 后零 .rs 行为变更）；纯 .md 零 .rs 行为变更**。承接 R2551（.rs 源注释 doc-drift 清零）。本轮独立核验控制面 plateau 健康声称 + 扩展 doc-drift 扫描面（R2551 仅扫 .rs 源注释，未覆盖 .md 控制面/设计文档）。**独立核验结果**：① **file-size 合规声称** = 全仓 `find` 仅 `flexbox.rs` 2553（vendored taffy 豁免）>2000，证实 master.md「file-size FULLY 收官」✓；② **R2551 text.rs:649 fix** = `mod.rs:1884→inline/mod.rs:1551` 已 land 于 `crates/engine/src/paint/painter/text.rs:649`，inline/mod.rs:1551 正落 R1456 vertical-mode line.y 注释块 ✓；③ **.rs 源注释 OOB 独立扫描** = 814 .rs 文件 504472 行 **0 OOB**，独立证实 R2551「.rs doc-drift surface 清零」✓。**.md 扫描扩展（R2551 未覆盖面）** = rendering-compat 65 .md（excl archive）867 resolvable `.rs:NNN` ref → **29 OOB 跨 6 文件**，分类：**(a) 18 历史/superseded → 留存**——master.md 16 OOB 全在 📍 R#### 历史轮次记（R2403/R2416/R2424/R2436/R2442/R2364/R2352/R2309/R2249/R2542 等时间戳描述该轮当时代码位；doc-control 策略「round notes 历史记录」+ code-guidelines §3「精准修改」→ 不擅改历史记录）+ pending-clean-levers.md 2 OOB（该 doc 本身是 R678-R709 superseded handoff，消费表全 LANDED 于 R689/R716/R720，header 自警「行号可能漂移 grep 核验」）；**(b) 10 gated 设计文档 → defer**——phase-a-IFC-unification-design.md 8 OOB（engine.rs:1668-1910，R2513 拆分后代码迁出至子模块；gated Phase A IFC，hunting moved refs = 接入 IFC 深结构 = 用户常驻指令「勿借机跳深结构」禁）+ r109-vertical-native-layout-design.md 1（engine.rs:1452，R109 gated）+ vertical-mode-text-positioning-rfc.md 1（inline/mod.rs:1659，R1043 gated）；**(c) 1 当前文档 stale → FIXED**：dc-progress.md:178 DC-14 防放宽不变量单测 ref `dc14_locked_strict_thresholds_invariant`（reftest.rs:2207）**双 stale**——test 名 `git log -S` 全空 = 从未存在 + 位 2207>1002 越界；实证真身 = `test_strict_tolerance_dc14_locked`（`tests/wpt-runner/src/reftest/tests.rs:1240`，断言 Layout 0.001/2 + Text 0.005/5 + Unknown 0.001/2 锁定值防放宽，正合 doc 描述「断言锁定值防回归放宽」）→ 修 test 名+位为 `reftest/tests.rs:1240`；re-scan dc-progress.md **0 OOB** 确认。**plateau-guard 评估**：R2544 make test 13190/0 + product-smoke welcome 17.03% 全 struct PASS + R2545 legacy 0 struct FAIL 后，仅 R2551 改 1 行 .rs 注释（text.rs:649），本轮纯 .md fix——零 .rs 行为变更；按常驻指令「plateau-guard 待 drift」+ master.md「仅文档→跳过昂贵 make test」**本轮不跑 redundant make test/product-smoke**（无新行为信号）。**门禁**：纯 .md doc fix 零 .rs 行为变更，不跑昂贵 make test；fmt/clippy 不适用 .md；pre-commit guard PASS。**意义**：plateau 健康独立核验（不盲信前轮结论）+ R2551 扫描面扩展到 .md（发现 .rs 源注释清零但 .md 控制面/设计文档仍有 stale）——doc-control 策略区分历史轮次记（留存）vs 当前指引（dc-progress DC-14 checklist = 当前须准），唯一可操作 stale 在当前 DC-14 文档已修；gated 设计文档 stale defer 至用户授权深结构时一并刷新（避免自主接入深结构）。**下一轮**：续低频文档纠偏（.md 当前指引面既扫，余 historical/gated，概率低）或 plateau-guard（待 .rs drift）或用户点名授权深结构（最高 value = Phase A IFC line-box-metric 统一）。

> **📍 R2551（2026-08-04）🔧 文档纠偏（用户常驻指令「clean-lever 穷尽后做文档纠偏+plateau-guard，勿跳深结构」）：thorough `.rs:NNN` doc-drift 扫描 = 1 stale ref 修复（text.rs:649 `mod.rs:1884`→`inline/mod.rs:1551`，R1456 vertical-mode line.y；inline/mod.rs 现 1593 行故 1884 越界）；全 path-qualified ref 经 out-of-range 扫描 + 抽查 valid（text.rs R2535 split 保生产线不变 / inline/mod.rs:1122/1189/95-98 正确）；纯注释零回归**。承接 R2550（顶部导航 callout）。本轮执行用户 §当前裁决 line 7 常驻指令「clean lever 九重穷尽后做文档纠偏 + plateau-guard，不要借机跳入深结构」的「文档纠偏」项——thorough 扫描全仓 39 个真实 `.rs:NNN` doc 引用（排除 edge_cases 测试串 main.rs:42/lib.rs:108），逐 path-qualified 目标 out-of-range 核验 + 高风险文件（inline_finalization/text.rs/lib.rs/sizing.rs）抽查：**唯一 stale = text.rs:649 `mod.rs:1884`**（歧义 mod.rs，内容为 R1456 vertical-mode line.y = 列 x 坐标 → 实际在 inline/mod.rs:1551，1884>1593 越界）→ 修为 `inline/mod.rs:1551`（消歧 + 现位）。其余全 valid（text.rs:581 text-align/737 preserve、inline/mod.rs:1122 Br/1189 基线/95-98 vertical 字段、engine.rs:1170 is_block_level[R2542 既修]、postprocess.rs:1652 resolve_relative_inset[R2542 既修] 全核对在位）。**doc-drift surface 经 R2542[engine.rs]+R2551[mod.rs:1884] 实质清零**。**门禁**：fmt OK + `cargo clippy --workspace --all-targets -D warnings` 干净（纯注释增量 zero-warning）；纯注释零行为变更不跑昂贵 make test。**下一轮**：续文档纠偏（surface 既近清零，低频）或 plateau-guard（待 drift）或用户深结构授权。

> **📍 R2548（2026-08-04）📋 DC-9 blend_mode 可切片性核验 = infrastructure-blocked 非 tractable 自主深 lever（render-foundation 无 offscreen 能力；现状 flat per-element blend 非真 paint-isolation）；深结构可切片性评估收官 = 全剩余项非自主 tractable；零代码**。承接 R2547（IFC deep-review 零 bounded lever）。本轮核 DC-9（master.md 深结构 #4，唯一标「低 footprint」似可切片项）：① mix-blend-mode parse/store/apply/SC-trigger 齐（R2302 isolation SC + R2309 mix-blend-mode SC + painter/effects.rs:517 `apply_blend_mode` 映射 16 模式 → BlendMode + render-foundation `apply_blend_mode`/BlendModePrimitive）；② **关键缺口 = render-foundation 无 offscreen/intermediate-buffer/render-to-texture 能力**（grep `offscreen|intermediate.?buffer|render.?to.?texture|paint.?isolation` 零命中，仅 1 无关 cull 测试）；现状 blend = flat per-element `add_blend_mode(BlendModePrimitive{rect,mode})` **非真 paint-isolation**（应 offscreen 子树成组渲染再与 backdrop blend）。**裁决**：DC-9 须先建 render-to-texture 子树 buffer 基建（major multi-session）+ 仅 ~2-4 案价值 = **低 ROI 非 tractable 自主深 lever**，不自主开工。**深结构可切片性评估收官**：全剩余项经核**均非自主 tractable**——Phase A IFC line-box-metric（5 轮 net-negative + v1.4「须整体 unification 非切片」）/ DC-9（infrastructure-blocked）/ Phase 2 multicol fragmentation（深 RFC）/ baseline-export（证非 gap）/ font-metric R2202（R2393 net 负）/ @counter-style slice 2（R2394 net 负）/ individual transforms（net -2）/ R1043·R2174·font-stack C-dep（user-gated）。**综合裁定**：rendering-compat 自主推进面经 clean-lever（2 模式：6× feature-existence + 1× correctness-review）+ 深结构可切片性（全项核验）**双轴穷尽**；后续仅 ① 低频周期 plateau-guard（make test + product-smoke + product-smoke-legacy 回归守，待行为变更/drift 累积）；② 用户点名授权深结构专项（最高 value = Phase A IFC line-box-metric 统一，接受多轮 A/B 收敛风险）。**下一轮**：plateau-guard 或待用户深结构授权。

> **📍 R2547（2026-08-04）📋 IFC 孤立 bug deep-review = 零 bounded lever（1 HIGH-conf finding 经代码核验 = hallucinated 错文件/错行；2 余非 bug 或 gated）；自主 surface 经第 2 模式（correctness-review）确证穷尽；零代码**。承接 R2546「IFC line-box-metric = gated 高风险不自主开工」。本轮 pivot 第 2 lever-发现模式——前 6 probe 全「feature-existence 是否实现」皆「已实现」；本轮用 deep-review 找 IFC **孤立有界 correctness bug**（非 gated broad Path B/Wall②③ 消除），审 inline_finalization.rs / inline/mod.rs / painter/text.rs（5413 行）。**subagent 报 3 候选，逐一代码核验**：① **inline/mod.rs:1248 vertical-mode is_ahem 倒置**（标 HIGH-conf）= **hallucinated**——`container_is_ahem` 变量在 inline/mod.rs **不存在**（仅在 text.rs:802/914/916）；:1248 实为 baseline_y ascent 计算（正确用 per-run `run.is_ahem`），:916 实为 text-align 与 is_ahem 无关；② **inline_finalization.rs:1050 orphan gate off-by-one** = **非 bug**——`if orphan_ids.len()<2 return` + 独立 `has_non_eligible_inline return` 双 guard 均 intentional 镜像 tree.rs（R2197 atomicity，注释明示）；③ **text.rs:914-916 multicol container_is_ahem** = 要么 intentional（Path B multicol 恒用 container-level is_ahem）要么 gated Wall② multicol territory，**非 clean bounded lever**（动 multicol v_offset 风险 multicol-fill-auto 回归）。**结论**：IFC deep-review 零 bounded lever——自主 clean-lever surface 经 **2 独立模式**（6× feature-existence + 1× correctness-review）确证穷尽，IFC 残余 = gated broad Walls（②③）非孤立 bug。**教训（控制面记）**：subagent 报告的 HIGH-conf finding 须**代码核验后方可行动**（本轮 HIGH-conf 直接 hallucinated 错文件/错行）。**下一轮**：续低频 plateau-guard 或用户点名授权 IFC line-box-metric unification 专项（最高 value，接受多轮 A/B 收敛风险）。

> **📍 R2546（2026-08-04）📋 Phase A 双线程代码核验 + 自主面天花板裁定：inline-box-model coherence = 已 DONE default-on（非 lever）；IFC line-box-metric unification = 唯一 gated 高风险 95%-路径（5 轮 net-negative 史，不自主开工）；零代码**。承接 R2545（post-split triple-guard 全绿）+ 本轮 rally directive「自主 surface 既穷尽则审慎启动最高 value 深结构」。本轮读两份 Phase A 设计文档 + **代码核验 ground-truth**：① `phase-a-inline-box-model-full-design`（inline-box-model coherence）经核 `ZW_PHASEA_MULTI_INLINE`（text.rs:1246 / tree.rs:1536 / inline_finalization.rs:1036，`!=Ok("0")`=default-on R2198）+ `backfill_phasea_orphan_boxes`（inline_finalization.rs:1026，:940 调用）+ `PHASEA_STORE_EXT`（text.rs:1786）**全在码中** = R2156-R2198 三 slice 全 DONE default-on（= R2545 legacy 增益之源）；② `phase-a-IFC-unification-design`（IFC line-box-metric）= 5 轮单点解锁（R125/R198/R205/R209/R213）**全 net-negative 回归**，唯 R207 narrow 成功，v1.4 裁决「须整体 unification 消灭 Path B（baseline 语义墙③ + multicol 反向依赖墙②）非独立可切片」→ **不自主开工**（repeat R209/R213 net-negative 史，5 轮实证）。**ifc-008/009/011 不在 wpt-data**（仅 font-051=R207 成功案在），large-font 簇无法经标准 harness 复测。**裁定**：自主 clean-lever surface 既穷尽（R2542-R2543 6 probe），唯一推向 95% 路径（IFC line-box-metric unification）= gated 高风险多 session 深结构；本轮不贸然开工（守 code-guidelines「先思考再编码：5 轮 net-negative 史 = 高风险，须用户授权专项」）。**控制面订正**：master.md 深结构 #1 原「Phase A IFC 统一」措辞**歧义**（两线程同名）→ R2546 拆双线程钉案（inline-box-model coherence 已 DONE / IFC line-box-metric gated），防后续轮误开已 DONE 线程文档。**下一轮**：续低频 plateau-guard 或用户点名授权 IFC line-box-metric unification 专项（最高 value，接受多轮 A/B 收敛风险）。

> **📍 R2545（2026-08-04）📋 post-split triple-guard 收官：product-smoke-legacy 42 fixture 0 struct FAIL（avg 2.36%，37-form-controls struct PASS 3.85%）— R2509-R2541 机械拆分链 modern+legacy 双向零回归确认；零代码**。承接 R2544「product-smoke 腿绿，legacy 腿待补」。本轮补齐 post-split 三腿回归守的 legacy 腿——`make product-smoke-legacy`（51 fixture vs chrome-127 oracle，trend-only exit 0）：**42 fixture run（3 SKIP oracle-missing 预存非回归）+ struct-check failures 0**（全 struct PASS）+ avg diff 2.36% + worst 19-testpage-minimal 17.23% + **37-form-controls（历史 known struct FAIL）struct PASS 3.85%**（与 R2163 一致）。**post-split triple-guard 全闭合**：① make test 13190/0（R2544）；② make product-smoke exit 0 welcome 17.03% 全 struct PASS（R2544，modern fixture）；③ make product-smoke-legacy exit 0 0 struct FAIL（本轮，legacy HTML 3.2/4 fixture）。→ **R2509-R2541 八连机械 file-split 经 modern+legacy 双向产品 fixture 验证 = ZERO 回归**（split 仅 mechanical byte-identical，UA/表单/legacy 元素未触；legacy avg 2.36% 与 R2505/R2163 持平 = font-wall plateau 趋势非新 issue）。**意义**：post-split 回归门禁三腿全绿收官，plateau 健康；split 链产品安全双向（modern+legacy）确认。**下一轮**：续低频 plateau-guard（三腿刚跑全绿，下次待行为变更/drift 累积）或用户点名深结构（唯一推向 95% = Phase A IFC line-box-metric 统一）。

> **📍 R2544（2026-08-04）📋 post-split plateau-guard（make test + product-smoke）= R2509-R2541 机械拆分链 ZERO 产品回归确认 + stale 快照订正（welcome 16.84%→17.03%、test 13129→13190）；零代码**。承接 R2543「续 plateau-guard / 待用户深结构」。本轮跑 sanctioned 回归守——**这是 R2509-R2541 八连机械 file-split（parser/shorthand/pipeline/document/engine/types/text/lib/tests/visual/app_render）后的首次 product-fixture 验证**（make test 各 split 已 byte-identical 绿，但 make test 不覆盖 product fixture；R541 先例 = welcome +7.65pp 回归藏 14 轮）。**结果全绿**：① `make test` exit 0（13190/0/74，自 R2541 持平，R2542/R2543 零测试增删）；② `make product-smoke` exit 0——**全 struct-check PASS**（welcome 800/375/320 + wintertc 800/375 + morning 800/375/320）+ welcome 桌面 diff **17.03%（81737px，与 R2402-R2436 一致）≤20%**；机械拆分 byte-identical 经 product-smoke 再证零产品渲染漂移。**快照订正**：master.md 旧 welcome 16.84%（stale，实际 17.03% 自 R632 稳定）+ test count 13129（截至 R2459 stale，实际 13190 截至 R2544）。**意义**：post-split 产品回归门禁首次闭合——R2509-R2541 拆分链产品安全确认；plateau 健康。**下一轮**：续低频 plateau-guard 或待用户点名深结构（唯一推向 95% = Phase A IFC line-box-metric 统一）。

> **📍 R2543（2026-08-04）📋 CSS Syntax §4/§5 候选条目清零（CDO/CDC 非顶层 cosmetic + CSS Values 4 math 全实现 re-confirm）— 自主 clean-lever surface 经本轮双 fresh probe 第 6 次确证穷尽；零代码**。承接 R2542「续 plateau-guard / 待用户深结构」。本轮 verify 两路 fresh probe（候选清单剩余项逐一钉案）：**(1) CDO/CDC 非顶层上下文 = cosmetic 非缺口**——R2204 已将 CDO（`<!--`）/CDC（`-->`）tokenize 为 `Token::Comment`（tokenizer.rs:854/909），parser `skip_whitespace`（mod.rs:331-333）随处跳过 `Whitespace | Comment`；CSS 注释本就所有上下文 ignorable（声明值内/选择器前导/块内均同），故 CDO/CDC 非顶层「parse error」仅 spec 诊断标志、零渲染影响，Comment 处理已产出 spec-correct output（如 `p { color: red<!-- --> }`=color:red 正确）；无 driving test 可写（cosmetic）。**(2) CSS Values 4 数学函数（round/mod/rem/sign/abs + trig 7 + pow/sqrt/hypot/exp/log + π/e/infinity/NaN 常量）= re-confirm R2280 已全实现**——`CalcExpr`/`UnaryMathOp`/`BinaryMathOp` 枚举 + `try_parse_unary_math`/`try_parse_binary_math` + `eval_calc_with_context` 全 arm（types.rs）+ 5 综合单测（abs/sign/sqrt/exp/log/constants、pow/hypot/round/mod/rem、trig 含 deg/grad/turn/rad 角度归一）；R2391/R2400 早记「R2280 全实现」，本轮 re-confirm 非新 lever。**意义**：CSS Syntax §4/§5 候选条目全清零（MQ L4/@layer R2542 作废 + CDO/CDC R2543 cosmetic + math R2280 早实现）；自主 clean-lever surface 经 6 次 fresh probe（env R2459 / MQ L4 / @layer / CDO/CDC / math / font-matching 族）确证穷尽——ZW CSS 解析/values/syntax 层 common surface feature-complete。**门禁**：零代码本轮不跑昂贵 make test。**下一轮**：续 plateau-guard（make test + product-smoke + product-smoke-legacy 回归守，R2504-R2542 链多 mechanical/comment/doc，product fixture 久未 re-baseline）或待用户点名深结构（唯一推向 95% = Phase A IFC line-box-metric 统一）。

> **📍 R2542（2026-08-04）🔧✅ engine.rs 行号 doc-drift 续修 3 处 LANDED（R2513 engine.rs 拆分 2163→1419 所致 stale `.rs:NNN` 引用；纯注释零行为变更）+ 自主 clean-lever surface 续证穷尽（MQ L4 比较运算符 / @layer cascade layers 双 fresh probe 均确认已完整实现）**。承接 R2541「file-size FULLY 收官，后续仅 plateau-guard / 待用户深结构」。本轮 verify-then-fix 两路：**(1) doc-drift**（R2482/R2502/R2515 模式）——全仓 `.rs:NNN` 注释引用对 R2513 拆分后越界核查：engine.rs 现 1424 行，3 处引用越界/错位 → 修：`table_layout_tests.rs:132 engine.rs:1007→1170`（is_block_level 现 @1170）、`anonymous_flex_item_tests.rs:793 engine.rs:1888→1170`（1888>1424 不存在）、`r1982_overflow_scroll_container_diag.rs:95 engine.rs:1905→postprocess.rs:1652`（resolve_relative_inset R2513 抽至 engine/postprocess.rs:1652，1905>1424 不存在）；全仓核 engine.rs N>1424 零残留 + shorthand/mod.rs/property/types.rs/painter/text.rs 等 split 文件越界零残留；postprocess.rs:844 `engine.rs:1170`（is_block_level）经核仍正确无需改。**(2) fresh clean-lever probe 双证 stale**——候选清单「MQ L4 比较运算符 token 化」经核 media_query.rs `MediaFeatureOp` 五态（Exact/GreaterThan/GreaterEqual/LessThan/LessEqual）+ `parse_op` + `parse_simple_range`/`try_parse_combined_range` 全 5 运算符 + 双侧范围 `(600px<=width<=1000px)` 已完整实现（R2205/R2437/R2446 多轮已记，候选清单 stale→订正作废）；@layer cascade layers 经核 css-parser `LayerRule`/`consume_layer_rule` + style-system `CascadeOrder.layer_index`+`sort_key`（unlayered>layered、later>earlier）+ matcher `collect_from_rules` layer_counter + revert-layer 全链 + 44 WPT 文件，**已完整实现非缺口**。**门禁**：fmt OK + `cargo clippy --workspace --all-targets -D warnings` 干净（纯注释增量 zero-warning）；纯注释零行为变更不跑昂贵 make test。**意义**：doc-drift 续修（R2513 拆分遗留越界引用清零，注释不再引不存在行号）；双 fresh probe 再证「自主 clean-lever surface 穷尽」。**下一轮**：续 plateau-guard / 待用户点名深结构（唯一推向 95% 路径 = Phase A IFC line-box-metric 统一）。

> **📍 R2541（2026-08-04）🔧✅ apps/browser `app_render.rs` split LANDED（CLAUDE.md §5 合规；2003→1915 + app_render_ui.rs 96；首 production 方法迁出，零回归）— file-size 合规第 8 fix；**全量重扫确认 file-size FULLY 收官——仅余 vendored flexbox.rs**；自主 surface 三向穷尽（rendering clean-lever + module triage + file-size）**。承接 R2540「app_render.rs 3-over marginal 评估」。**关键发现**：apps/browser **既有 `include!` 文本包含拆分谱**（app_render_geometry/address/primitives + app_input* 皆 `impl BrowserApp {}` 块经 `include!` 进 app.rs **同模块作用域**，注释明「从 app_render.rs 拆分以控制单文件体积」）——本轮抽 ui_* 方法组**非 forced split，乃既有谱自然续**。**实施**：抽 ui_* 6 方法（`ui_line_metrics` / `ui_text_centered_in_height` / `ui_text_top_in_box` / `draw_ui_text` / `measure_ui_text_width` / `truncate_ui_text`，原 1826-1913 = 88 行，UI 文本 helper 高内聚，互相 `self.method()` 调用跨 impl 块可达）→ 新 `app_render_ui.rs`（同 header 注释 + `impl BrowserApp { }`，经 `include!` 共享 app.rs 作用域 → `self.font_id`/`font_loader` + `Color`/`GlyphDraw` 直接可达，**无需 use**）；app_render.rs 删 1826-1913；`app.rs` 加 `include!("app_render_ui.rs");`（紧随 `app_render.rs`）。**关键**：`include!` 文本包含 = **编译产物 byte-identical**（方法仍在 `impl BrowserApp`、仍同模块作用域，仅源码文本位置变）→ 渲染 byte-identical by construction。**门禁全绿**：① `cargo clippy --workspace --all-targets -D warnings` clean（zero-browser 重编译证 include!+impl 链通）；② `cargo fmt --all --check` clean；③ **`make test` 13190 passed / 0 failed**（= R2528-R2540 baseline，零回归）。纯文本迁出（同作用域）→ byte-identical，无需 reftest/product-smoke。**app_render.rs 2003 → 1915；app_render_ui.rs 96（双 <2000 ✓）**。**全量重扫**（`find crates apps tests` excl target）：**仅 `crates/taffy-local/src/compute/flexbox.rs` 2553 > 2000**（vendored taffy 0.12.1，**Taffy 迁移裁决豁免**，不可动）——**所有非 vendored .rs 文件 §5 合规**。**意义**：第 8 个 file-size 合规 fix；**file-size 合规 FULLY 收官**（R2534-R2541 八连：production `types`/`text`/`lib`/`app_render` + test `tests_1`/`tests_4`/`tests_10`/`visual`，**全零回归**；唯余 vendored fork）；证既有 include!/submodule 谱（app_render_geometry/address + property/types/image + tests_N/tests_Nb）可续用以达 §5。**file-size lever 既 FULLY 尽** → **自主 surface 三向穷尽**（rendering clean-lever R2524-R2533 + module triage R2524-R2533 + file-size R2534-R2541）。**下轮可接续**：① 低频周期 plateau-guard（make test + product-smoke + product-smoke-legacy 回归守）；② **唯一推向 95% 路径 = 用户点名授权深结构**（最高 value Phase A IFC line-box-metric 统一：解锁 css-backgrounds origin/* + welcome IFC + css-pseudo ::first-letter/::first-line + R109 inline-ownership；次 = CSS Color 4 色彩空间渐变 / object-view-box / image-rendering 真光栅化 / font-stack C-dep / R1043 vertical-mode / R2174 taffy border-box / image-set / ruby / multicol-fragmentation），授权后 spec-rfc 出 RFC 多 session 推进。

> **📍 R2540（2026-08-04）🔧✅ engine `paint/tests/visual.rs` split LANDED（CLAUDE.md §5 合规；2092→1027 + visual_part2.rs 1085；纯测试迁出零回归）— file-size 合规第 7 fix，最复杂 split（helper fn + 跨半 use）；剩余 §5 违反仅 2 个**。承接 R2539「visual.rs 2092 须理 helper 归属」。**结构分析**：visual.rs = 73 `#[test]` fn + **4 helper fn**（`make_box`@22 **pub(super)** / `make_box_with_border`@59 / `setup_bg_test`@1032 / `make_box_with_padding`@1041）。helper 用途分区干净：`make_box_with_border` 仅 first-half 用；`setup_bg_test` + `make_box_with_padding` 定义在 1032/1041、调用全在 second-half → split @ 原 1028/1029 边界（`// ── background-position/size/clip` section comment 前）使每 helper 随其调用者。**关键坑**：初轮 `awk/grep \bmake_box\b` 判 make_box「0 调用」**错误**（word-boundary/awk 匹配 bug）→ split 后 visual_part2 **clippy 22 errors「cannot find make_box」**→ 实际 make_box 被 second-half 大量调用（first-half 定义 `pub(super)`）→ 补 `use super::visual::make_box;`（sibling module 经 `super::visual::` 访问 pub(super) fn）。**教训钉案**：**usage 分析信 clippy `cargo check --tests`，勿信 ad-hoc awk/grep**（R2537 use-trap 谱补遗——moved 半缺失 use 须补；此处 make_box 跨半引用经 pub(super)+sibling use）。**实施**：sed 提取原 1029-2092 → `paint/tests/visual_part2.rs`（+ 同 header `#![allow]` + 11 use + `use super::visual::make_box`；second-half = background + border-image section，CSS Backgrounds & Borders 模块）；visual.rs 留 1-1028（`make_box` + `make_box_with_border` + first-half test fn）；`paint/tests/mod.rs` 加 `mod visual_part2;`。**use 管理**：visual_part2 复用全 11 use（background+border-image 测试用全类型集，与 first-half overlap）→ **无 unused 须删**（R2539 kept-half pruning 此处不需——use 集两半重叠）；make_box 经 `use super::visual::` 引。**门禁全绿**：① `cargo clippy --workspace --all-targets -D warnings` clean（补 make_box use 后）；② `cargo fmt --all --check` clean；③ **`make test` 13190 passed / 0 failed**（= R2528-R2539 baseline，含 second-half ~30 测试在新位置，零回归）。纯测试迁出（零生产改动）→ byte-identical by construction，无需 reftest。**visual.rs 2092 → 1027；visual_part2.rs 1085（双 <2000 ✓）**。**意义**：第 7 个 file-size 合规 fix；**最复杂 split（helper fn + 跨半 use）既通**——证 tests 文件 split 可处理 shared helper（pub(super) 暴露 + sibling `use super::visual::fn`）；剩 §5 违反**仅 2 个**：`flexbox.rs` 2553（vendored taffy 0.12.1，Taffy 迁移裁决豁免，不可动）+ `app_render.rs` 2003（apps/browser 生产，**3 over 极 marginal**，cost>benefit defer）。**下轮可接续**：① app_render.rs（3-over，评估 trivial trim 或 defer）；② 低频周期 plateau-guard；③ **file-size 合规面基本收官**（production + test 文件既清，余 vendored + marginal）→ 后续重心转 plateau-guard / 待用户点名深结构（最高 value Phase A IFC line-box-metric 统一）。

> **📍 R2539（2026-08-04）🔧✅ layout-engine `engine/tests/tests_10.rs` midpoint split LANDED（CLAUDE.md §5 合规；2019→1004 + tests_10b.rs 1017；纯测试 fn 迁出零回归）— file-size 合规第 6 fix，末个 clean test 文件 split；use 双向核（moved 补 / kept 删）钉案**。承接 R2538「tests_10 2019 clean 可直拆」。**tests_10.rs**（layout-engine，2019 行）= 40 独立 `#[test]` fn（无 helper），**2 top use**（line 1 `use super::*;` + line 2 `use zero_css_parser::values::{ClearValue,DisplayValue,FloatValue,LengthValue,PositionValue,VisibilityValue}`），无 mid-file use。midpoint @ 原 line 1005/1006 边界（`test_inline_only_container_shrink_reflows_following_sibling` 前）。**实施**：sed 提取原 1006-2019 → 新 `engine/tests/tests_10b.rs`（+ 2 use 全保留）；tests_10.rs 留 1-1005；`engine/tests/mod.rs` 加 `mod tests_10b;`（alphabetize 至 tests_10 与 tests_11 间）。**新 use 陷阱（kept-half 变体，R2537 moved-half 的镜像）**：split 后 **kept tests_10.rs** 的 line 2 use 中 **PositionValue/VisibilityValue 变 unused**（其唯一使用者已迁 tests_10b；kept 半仍用 ClearValue 8/DisplayValue 38/FloatValue 13/LengthValue 59）→ clippy 1 error（`-D warnings` unused_imports）→ 从 kept 半 line 2 **删此 2 type**（保留其余 4）；tests_10b 保留全 6 type use（皆 used，clippy 未报）。**教训钉案**：split 须**双向核 use**——① moved 半**补缺失 use**（R2537 tests_1 mid-file use）；② kept 半**删变 unused use**（R2539）；`cargo clippy --tests` / `cargo check --tests` 兜底捕获。**门禁全绿**：① `cargo clippy --workspace --all-targets -D warnings` clean（删 unused 后）；② `cargo fmt --all --check` clean；③ **`make test` 13190 passed / 0 failed**（= R2528-R2538 baseline，含被迁 ~20 测试在新位置，零回归）。纯测试 fn 迁出（零生产改动）→ byte-identical by construction，无需 reftest。**tests_10.rs 2019 → 1004；tests_10b.rs 1017（双 <2000 ✓）**。**意义**：第 6 个 file-size 合规 fix；**末个 clean test 文件 split**（css-parser tests_1/tests_4 + layout-engine tests_10 模式既稳，use 双向核钉案）。**剩余 §5 违反 3 个**：flexbox.rs 2553（vendored taffy 不可动）+ app_render.rs 2003（3 over 极 marginal prod）+ **engine paint/tests/visual.rs 2092**（有 helper fn `make_box_with_border` 等，split 须处理 helper 归属——`pub(super)` 暴露或迁共享 helper module，较复杂，非 tests_N.rs 直拆模式）。**下轮可接续**：① visual.rs（须先理 helper fn 归属——split 时 helper 若两半共用，pub(super) 暴露 + sibling 引 `super::visual::helper`，或抽共享 helper module）；② app_render.rs marginal（3-over，cost>benefit defer）；③ 低频周期 plateau-guard；④ 待用户点名深结构（最高 value Phase A IFC line-box-metric 统一）。

> **📍 R2538（2026-08-04）🔧✅ css-parser `tests/tests_4.rs` midpoint split LANDED（CLAUDE.md §5 合规；2036→1009 + tests_4b.rs 1028；纯测试 fn 迁出零回归）— file-size 合规第 5 fix**。承接 R2537「余 3 test 文件 split（visual 2092 / tests_4 2036 / tests_10 2019）」。**tests_4.rs**（css-parser，2036 行）= 154 独立 `#[test]` fn，**单一** `use super::*;`（line 1，**无 mid-file use——R2537 trap 不触发**），无 helper fn。**clean split**（较 tests_1 更顺）。midpoint @ 原 line 1010/1011 边界（`test_parse_custom_properties_with_regular` 前）。**实施**：sed 提取原 1011-2036 → 新 `tests/tests_4b.rs`（+ `use super::*;`）；tests_4.rs 留 1-1010；`tests/mod.rs` 加 `mod tests_4b;`。**门禁全绿**：① `cargo clippy --workspace --all-targets -D warnings` **首过 clean**（无 use 陷阱，证 R2537 mid-file use 非「tests_N.rs 普适」，单 use 文件直拆可）；② `cargo fmt --all --check` clean；③ **`make test` 13190 passed / 0 failed**（= R2528-R2537 baseline，含被迁 ~77 测试在新位置，零回归）。纯测试 fn 迁出（零生产改动）→ byte-identical by construction，无需 reftest。**tests_4.rs 2036 → 1009；tests_4b.rs 1028（双 <2000 ✓）**。**意义**：第 5 个 file-size 合规 fix；css-parser tests_N.rs split 模式既稳（tests_1 R2537 + tests_4 R2538，须先核全 use：tests_1 mid-file use 须补 / tests_4 单 use 直拆）。**剩余 §5 违反 4 个**：flexbox.rs 2553（vendored taffy 不可动）+ app_render.rs 2003（3 over 极 marginal prod）+ **engine paint/tests/visual.rs 2092**（有 helper fn `make_box_with_border` 等，split 须处理 helper 共享/迁移，较复杂）+ **layout-engine tests_10.rs 2019**（40 fn，2 top use，clean）。**下轮可接续**：① tests_10.rs（clean，2 top use，直拆）；② visual.rs（须先理 helper fn 归属——`pub(super)` 暴露或迁共享 helper）；③ app_render.rs marginal（defer）；④ 低频 plateau-guard；⑤ 待用户点名深结构（最高 value Phase A IFC line-box-metric 统一）。

> **📍 R2537（2026-08-04）🔧✅ css-parser `tests/tests_1.rs` midpoint split LANDED（CLAUDE.md §5 合规；2128→1062 + tests_1b.rs 1067；纯测试 fn 迁出零回归）— file-size 合规第 4 fix，首 test 文件 split**。承接 R2536「file-size 转低优先：4 test 文件 / app_render marginal」。**tests_1.rs**（css-parser，2128 行，**最大剩余 §5 违反**）= 165 个独立 `#[test]` fn（106 parse + 5 specificity + 54 tokenize，interleaved），linear midpoint split @ 原 line 1063/1064 边界（`test_tokenize_custom_properties` 前）。**实施**：sed 提取原 1064-2128 → 新 `tests/tests_1b.rs`（+ `use super::*;`）；tests_1.rs 留 1-1063；`tests/mod.rs` 加 `mod tests_1b;`（cargo fmt 自动 alphabetize 至 `tests_12` 后）。**关键坑（首轮 clippy 183 errors E0425/E0433）**：原 tests_1.rs **非仅 line 1 `use super::*;` 独一 use**——line 785 有 **mid-file 第二个 use `use crate::values::*;`**，split 后第二半依赖它解析 `parse_display`/`DisplayValue`/`AlignmentValue` 等（mod.rs `use crate::values::{...}` 是具体列表非 glob，`super::*` 不全覆盖）；补 `use crate::values::*;` 至 tests_1b.rs 修复。**教训**：split 须核原文件**全部** top-level use（非仅 line 1），mid-file use（Rust 允许 module-scope use 任意位置）易漏——clippy/`cargo check --tests` 先于 make test 兜底捕获。**门禁全绿**：① `cargo clippy --workspace --all-targets -D warnings` clean（补 use 后）；② `cargo fmt --all --check` clean；③ **`make test` 13190 passed / 0 failed**（= R2528-R2536 baseline，含被迁 ~83 测试在新位置跑通，零回归）。纯测试 fn 迁出（零生产改动）→ byte-identical by construction，无需 reftest/product-smoke。**tests_1.rs 2128 → 1062；tests_1b.rs 1067（双 <2000 ✓）**。**意义**：第 4 个 file-size 合规 fix；**首 test 文件 split**——证 test 文件亦可机械 split（独立 fn），但**须核全 use**（mid-file use 陷阱）；file-size 合规面再收一（最大 test 违反清）。**剩余 §5 违反 5 个**：flexbox.rs 2553（vendored taffy 不可动）+ app_render.rs 2003（3 over 极 marginal prod）+ **3 test 文件**（engine paint/tests/visual.rs 2092 / css-parser tests_4.rs 2036 / layout-engine tests_10.rs 2019）。**下轮可接续**：① 余 3 test 文件 split（同模式，**必核全 use**含 mid-file）；② app_render.rs marginal（cost>benefit，defer）；③ 低频 plateau-guard；④ 待用户点名深结构（最高 value Phase A IFC line-box-metric 统一）。

> **📍 R2536（2026-08-03）🔧✅ style-system `lib.rs` inline test 模块迁出 LANDED（CLAUDE.md §5 合规；2014→1768；纯测试迁出零生产改动，零回归）— file-size 合规第 3 fix（clean production-source crate 收官；扫描纠偏：非「全仓收官」，余 vendored + marginal-prod + test 文件）**。承接 R2535「余 file-size：lib.rs 2014（14 over）按需」。**lib.rs 既有外部 test mod 谱**（`mod presentational_hint_tests;` / `mod tests;` 已外部，flat `src/*.rs`——lib.rs 为 crate root，`mod X;` 解析 `src/X.rs`）——本轮将 2 个 inline `#[cfg(test)] mod`（quirks_tests 原 1631-1765 + ua_display_tests 原 1768-1880，contiguous 原 1630-1880）迁出至 `src/quirks_tests.rs` + `src/ua_display_tests.rs`；lib.rs 原位改 `#[cfg(test)] mod X;` 外部声明（sed 脚本 `c` 命令一次替换 contiguous 区，零 transcription）。**关键**：两模块 `use super::*;` 引 lib.rs 项——外部子模块仍为 crate root child，`super::*` 解析不变；**纯测试迁出，零生产代码改动** → 渲染 byte-identical by construction。q_quotes_tests（1643）保留 inline（文件既已 <2000，无需再迁；自包含尾部模块）。**lib.rs 2014 → 1768 行（<2000 ✓）**。**门禁全绿**：① `cargo fmt --all --check` clean（首跑 fmt 漏删 leading blank，重跑修正——同 R2535 谱）；② `cargo clippy --workspace --all-targets -- -D warnings` clean；③ **`make test` 13190 passed / 0 failed**（= R2528/R2534/R2535 baseline，含 quirks/ua_display 测试在新位置跑通，零回归）。纯测试迁出零生产改动，无需 reftest/product-smoke。**扫描纠偏（code-guidelines §1 勿假设）**：R2534 早期扫描用了 `-not -path "*/tests/*"` 漏掉 test 文件——**全量重扫** `find crates apps tests`（excl target）实得 >2000 文件 6 个：① **flexbox.rs 2553**（vendored taffy 0.12.1，Taffy 迁移裁决豁免，不可动）；② **app_render.rs 2003**（apps/browser 生产，3 over 极 marginal）；③ **4 个 test 文件**（css-parser tests_1.rs 2128 / tests_4.rs 2036 + engine paint/tests/visual.rs 2092 + layout-engine engine/tests/tests_10.rs 2019）。**意义**：第 3 个 file-size 合规 fix；**clean production-source crate 文件**（types.rs R2534 + text.rs R2535 + lib.rs R2536）§5 合规收官；非「全仓收官」（余 vendored + marginal-prod + 大 test 文件——后者 splitting 为 test 组织活，价值低于生产源码拆分）。**file-size 合规 lever 由 production-source 转入低优先**（app_render 3-over 拆分 cost>benefit；test 文件拆分待需）。**下轮可接续**：① 低频周期 plateau-guard；② file-size 低优先（app_render.rs marginal / 4 test 文件按需）；③ 待用户点名深结构（最高 value Phase A IFC line-box-metric 统一）。

> **📍 R2535（2026-08-03）🔧✅ `painter/text.rs` 尾部 inline test 模块迁出 LANDED（CLAUDE.md §5 合规；2039→1965；纯测试代码零生产改动，零回归）— 续 R2534 file-size 合规第 2 fix**。承接 R2534「余 file-size：text.rs 2039（drift risky）/ lib.rs 2014（marginal）按需」。**text.rs 既有「减负」谱**（尾部注释 R1694：text_multicol/text_ruby 已迁 `text/` 子目录「text.rs 减负」）——本轮续此谱，将尾部 2 个 inline `#[cfg(test)] mod` 迁出。**实施**：① `r841_tests`（行高 em-box 门控单测，原 1955-1982）→ `painter/text/r841_tests.rs`；② `r2303_object_position_tests`（object-position 定位单测，原 1986-2036）→ `painter/text/r2303_object_position_tests.rs`（均落入既有 `text/` 子目录，与 text_multicol/text_ruby 同位置）；text.rs 原位改 `#[cfg(test)] mod X;` 外部声明（附迁出注记 doc）。**关键**：两模块 `use super::{ahem_uses_embox_position, compute_object_fit_rect}` 引 text.rs **私有 fn**——外部子模块仍为 `text` 的 child module，`super::` 解析不变（Rust child 可访 parent 私有项）；**纯测试代码迁出，零生产代码改动** → 渲染 byte-identical by construction。**R2531 ref-drift 不受影响**：迁出尾部（原 1954-2036），其上代码行号不变（R2531 drift 的 text.rs:1464/1015/884/581/737/853/605 等 ≤1527 仍原位）。**sed 机械提取**（零 transcription）+ `cargo fmt` 归一化（首次 fmt 漏删 leading blank，重跑 fmt 修正）。**text.rs 2039 → 1965 行（<2000 ✓）**。**门禁全绿**：① `cargo check --workspace` clean；② `cargo fmt --all --check` clean；③ `cargo clippy --workspace --all-targets -- -D warnings` clean（--all-targets 覆盖新 test 文件）；④ **`make test` 13190 passed / 0 failed**（= R2528/R2534 baseline，**含 r841/r2303 测试在新位置跑通**，零回归）。**纯测试迁出（零生产改动）→ 无需 reftest/product-smoke**（非渲染/布局行为变更，byte-identical by construction；make test 既含被迁测试本身 = 行为守）。**意义**：第 2 个 file-size 合规 fix；text.rs 完成既有 text_multicol/text_ruby「减负」谱的自然延续（尾部 inline test mod 全外置）；证 R2534 types.rs split 非孤例——既有子模块化谱的 file-size lever 仍可续。**剩余 file-size 违反**：flexbox.rs 2553（vendored taffy 不可动）/ **lib.rs 2014（14 over，marginal，模块根敏感）**。**下轮可接续**：① lib.rs marginal 合规（quirks_tests + ua_display_tests inline test mod 迁出 ~250 行，须核 `super::` 私有项可达；迁出获 comfortable margin）按需；② 低频周期 plateau-guard；③ 待用户点名深结构（最高 value Phase A IFC line-box-metric 统一）。

> **📍 R2534（2026-08-03）🔧✅ `property/types.rs` image-types 子模块拆分 LANDED（CLAUDE.md §5 / rally run-rules ≤2000 行合规；2022→1786；纯数据类型机械迁移，零回归）— breaks R2524-R2533 九连零代码 streak**。承接 R2533「下轮低频 plateau-guard / 待用户点名深结构」，本轮 rally directive「逐个击破收益最大事项」。**先 file-size 合规审计**（CLAUDE.md §5 + rally run-rules 明令 ≤2000 行）：① **color.rs 1943 < 2000**——R2402 既已 split 至 1943，**R2277「~2620 >2000 拆分」flag 为历史 stale**（R2396-R2398「下一轮拆 color.rs」所指已 resolve，记此防后续 agent 重做已 split 文件）；② **flexbox.rs 2553（最大违反）= taffy-local vendored fork**（Cargo.toml `name="taffy" v0.12.1` repo DioxusLabs/taffy）→ **不可拆**（与 Taffy 迁移裁决冲突，refactor 致迁移更难）；③ text.rs 2039（R2531 noted ref drift，risky）/ lib.rs 2014（marginal）；④ **types.rs 2022（declarative property types，最安全）→ 本轮拆**。**parse_basic.rs 复核**：确认仍为 dead-code orphan（`values/parse_basic.rs`，无 `mod parse_basic` 声明，仅 tests_5.rs:30 / types.rs:1535 注释提「死代码」）→ R2383/R2396 flag **准确非 stale**，deletion 仍 user-gated（code-guidelines §3 不自主删 pre-existing 死代码）。**split 实施**（sed 机械迁移零 transcription 风险）：抽出 image-types group（`Background{Image,Position,Repeat,Size,Attachment,Clip,Origin}ComputedValue` + `MaskModeComputedValue` + `BorderImage{Source,Slice,Width,Repeat,Outset}*` + `ListStyleImageComputedValue`，types.rs 原 804-1039 = 236 行，**纯 `#[derive]` 数据类型无 inherent impl**，仅外部依赖 `zero_css_parser::values::{GradientValue,CalcExpr,BackgroundEdge}` 全限定 + 组内自引用）→ 新子模块 `crates/style-system/src/property/types/image.rs`（含 `//!` 模块 doc）；types.rs 原位加 `pub mod image; pub use image::*;`——保 `property::types::*` 公共路径 + **16 处内部引用**（Property enum 变体 + ComputedStyle 字段，组外）不变（`pub use` 同时导入模块命名空间 + 对外 re-export）。**门禁全绿**：① `cargo check --workspace` clean（21 consumers 编译，公共 API preserved——证 re-export 透明）；② `cargo fmt --all --check` clean；③ `cargo clippy --workspace --all-targets -- -D warnings` clean；④ **`make test` 13190 passed / 0 failed**（= R2528 baseline，**零回归**）；⑤ scoped css-backgrounds reftest **363/710 = 51.1%**（= R2532，**byte-identical**——纯组织变更编译产物同构，被移动类型为 background/border-image paint 所用，零像素差）。**types.rs 2022 → 1786 行（<2000 ✓）**。**意义**：第 1 个 file-size 合规 fix；证 plateau 期仍有 sanctioned 合规 lever（CLAUDE.md §5 + rally run-rules 明令 ≤2000 行，非「重构没问题的代码」——硬约束违反即问题）；flexbox.rs 最大违反 vendored 不可动，text.rs/types.rs/lib.rs 余 3 文件 marginal（14-39 over）。**下轮可接续**：① 余 file-size marginal 合规（text.rs 2039 须先解 R2531 ref drift / lib.rs 2014）按需；② 低频周期 plateau-guard；③ 待用户点名深结构（最高 value Phase A IFC line-box-metric 统一）。

> **📍 R2533（2026-08-03）📋 css-images scoped reftest triage（fresh dir 续 R2532）= 460/256/204=55.7% / fail 全深 gated（5th 模块证「simple-look fail = deep/gated」普适；07-28 directive 2-3 轮 negative 信号 → 停 module triage）— 零代码**。承接 R2532「下轮候选 = css-images / css-values」。**css-images scoped reftest**（`make reftest-upstream FILTER=css-images`，51.6s，test-guard 无 OOM）：**460 case / 256 pass / 204 fail = 55.7%**（self-source；DC-14：真通过-可信 86 / 近似通过 149 / 不一致 204）。**204 fail 全 6 cluster 皆深/gated**：① **image-rendering**（0.00% mismatch fail + 1.04%~）——`rel=mismatch` 测试，ZW 0.00% diff = test 与 notref 渲染**完全相同**（ZW image-rendering 只绘 corner 质量指示器 effects_indicators.rs:66，**不改真实光栅化质量**）；真正实现须 GPU/CPU sampler 配置 = 深 render-foundation；② **object-view-box**（~10 案 uniform ~1.18-1.94%）——CSS Images 4 **新属性全未实现**（style-system/css-parser/engine 零匹配）；③ **gradient-eval-002~005 / lab / oklab interpolation**（2.08% cluster）——`linear-gradient(in lab,…)` **CSS Color 4 色彩空间渐变插值**，ZW 回退 sRGB gamma 插值致色差，深 color-pipeline；④ **image-orientation**（EXIF 方向，image-decode 层）；⑤ **image-set**（响应式图源选择 = R2412 srcset/DPR/sizes/layout-dependent 深，待授权）；⑥ **object-fit-cover-\*-00\*i / dyn-aspect-ratio**（1.19-1.48%，interactive/dynamic aspect-ratio = host-layer 动态）。**结论**：css-images = **第 5 个模块**证「simple-look fail 实为深结构 gated」普适规律（继 css-backgrounds R2532 / css-pseudo R2527 / css-logical R2529 / css-counter-styles R2395）——非「origin/consumed」式 clean lever，而是新属性 / 色彩空间 / 光栅化质量 / EXIF / 响应式图源 等深 plumbing。**07-28 directive「连续 2-3 轮 empirical 扫描 negative → 记录结论并转向，不继续消耗会话」信号触发**：R2532（css-backgrounds）+ R2533（css-images）连 2 fresh-dir triage 皆 negative（fail 全 deep/gated），**停 module-by-module triage**（css-values/css-ui 等剩余 dir 高概率同谱，不再逐个消耗 session）。**rally 状态**：directive 07-29 mandated 稳定 plateau 持守——自主轻量 landable 真尽（clean-lever surface 9 重穷尽 + 双 fresh probe + 5 模块 triage 闭环）。**真正推向 95% 须用户授权深结构**（最高 value = **Phase A IFC** line-box-metric 统一：解锁 css-backgrounds background-origin/* 9 + welcome IFC + css-pseudo ::first-letter/::first-line + R109 inline-ownership；次 = CSS Color 4 色彩空间渐变插值 / object-view-box 新属性 / image-rendering 真光栅化 / font-stack C-dep / R1043 vertical-mode / R2174 taffy border-box / image-set 响应式 / ruby-layout / multicol-fragmentation），授权后 spec-rfc 出 RFC 多 session 推进。**下轮可接续**：① 低频周期 plateau-guard（已无新 fresh-dir triage 价值，转 make test + product-smoke + product-smoke-legacy 周期回归守）；② 待用户点名深结构（唯一推向 95% 路径）。

> **📍 R2532（2026-08-03）📋 双 fresh clean-lever probe 穷尽 + css-backgrounds scoped reftest triage（fresh dir，未近期 triage）= 51.1% / fail 全深 gated（background-origin/* 9 案实证非 origin bug，真因 = ref inline-block+vertical-align+relative = Phase A IFC / R109 gated）— 零代码**。承接 R2531「下轮周期 plateau-guard / 待用户点名深结构」，本轮 rally directive「跳过网络、收益最大事项逐个击破」。**双 fresh clean-lever probe**（证 clean-lever surface 真 exhausted，非复 R2524 angle）：① **value/syntax shorthand vein**（R2396-R2398 同谱，Explore agent + 人工核验）—— background-clip:text / overflow 两值 / overscroll-behavior / place-{items,content,self} / flex-flow / gap 两值 / list-style / border-image / text-align:match-parent **全已实现**；唯一缺口 **scroll-padding/scroll-margin shorthand**（longhand 在、shorthand expand 落 catch-all）但属 rendering-compat.md line 363 **skip 区**（scroll-snap）+ 无视觉 driving reftest（snap 行为未实现），code-guidelines §2 反对 speculative，**defer**；text-justify 全缺 = 整新属性非 value lever。② **applied-consumed vein**（4 高潜候选 manual 核）—— caption-side（layout-engine caption 定位 + vertical 写回测试在）/ empty-cells（painter/mod.rs:571,741 实读 skip_empty_cell）/ object-position（text.rs:368 compute_object_fit_rect 实读）/ image-rendering（effects_indicators.rs:66 实读）**4/4 全 consumed**，非 lever。**css-backgrounds scoped reftest**（`make reftest-upstream FILTER=css-backgrounds`，81.2s，test-guard 无 OOM）：**710 case / 363 pass / 347 fail = 51.1%**（self-source；DC-14 三态：真通过-可信 170 / 近似通过 155 / 不一致 347）。**347 fail 全 6 cluster 皆深/gated**：① **background-attachment:local** ~10（scroll-aware bg painting = host-layer scroll，skip 区）；② **background-clip:border-area** ~20（CSS Backgrounds 4 新 `border-area` clip 值未实现）；③ **background-clip:text edge** ~20（text-clip 主体已实现 R-probe 确认，残余 = blend-mode/stacking-context/multiline/ellipsis 复合，深合成）；④ **background-origin/*** ~9 **uniform ~18%**——**实证非 origin bug**：effects.rs:106 origin box + :126 clip box（R2312 已修正 origin≠clip 误用）**均正确**；fail 真因 = `origin-border-box-ref.html` 用 `display:inline-block; vertical-align:bottom; position:relative; bottom:` 偏移结构 ref = **Phase A IFC / R109 inline-block ownership gated**（test 简单 block div vs ref inline-block+relative 结构差）；⑤ **background-color body/root propagation** 100%（root bg 传播边角，深）；⑥ **animations** ~50（CSS animations 出 reftest scope，Mission 明确排除帧级）。**build baseline GREEN**（release 二进制编译 + 710 reftest 跑通）。**结论**：css-backgrounds triage 钉案——第 N 个「simple-look 失败实为深结构 gated」模块证（与 R2527 css-pseudo 76 fail 全 deep/IFC / R2529 css-logical fail 全 R1043 gated 同谱）；**clean-lever surface 双 fresh vein（value/syntax + applied-consumed）再穷尽**。**rally 状态**：directive 07-29 mandated 稳定 plateau 持守——自主轻量 landable 真尽。**真正推向 95% 须用户授权深结构**（最高 value = **Phase A IFC** line-box-metric 统一：解锁 css-backgrounds background-origin/* 9 + welcome IFC 残余 + css-pseudo ::first-letter/::first-line + R109 inline-ownership；次 = font-stack C-dep / R1043 vertical-mode / R2174 taffy border-box / highlight-overlay / ruby-layout / multicol-fragmentation），授权后 spec-rfc 出 RFC 多 session 推进。**下轮可接续**：① 低频周期 plateau-guard（css-backgrounds landscape 既刷新，下一个 fresh dir triage 候选 = css-images / css-values / css-ui）；② 待用户点名深结构（唯一推向 95% 路径）。

> **📍 R2531（2026-08-03）📋 §3 doc-drift 自检：R2522/R2523 代码增量对 effects.rs/text.rs 行号漂移影响评估 = 混合 drift + 当前符号位置不可确信，非 clean fix — 零代码**。承接 R2530。**§3 责任**（「删除因自己修改而变得无用的引用」）：R2522（effects.rs ~line 374 underline_offset_px 块 +~13 行）+ R2523（text.rs site1 ~884 + site2 ~1098 emphasis_color 块各 +8）插入致 effects.rs:≥374 与 text.rs:≥884 行号下移。**核验 7 处 `.rs:NNNN` ref**：effects.rs:100（mod.rs:1651）/ text.rs:{581,737,853,605}（各 < 插入点，**未漂移，准确**）；**2 处漂移但不可确信修复**：① `text.rs:1629` 自引 `text.rs:1464-1481`（text-overflow cutoff 镜像）—— 当前 cutoff_start 在 1527 但属 **line-clamp** ellipsis 块非 text-overflow cutoff（text-overflow cutoff 块经 R2431/R2467 line-clamp 重构已移位/合并，pre-existing drift +本轮 +16 混合，当前确信位置不可定）；② `inline/mod.rs:1553` 引 `painter/text.rs:1015 stored 路径`（vertical run.y/line.y 处理，+8 漂移但 stored 路径经多轮演进取址不确信）。**裁决**：§1/§2 禁止猜测（猜错致 ref 更劣）；此 2 ref **非 clean fix**，留作低优先 pre-existing drift（grep 符号名仍可定位）。**结论**：§3 doc-drift 自检——R2522/R2523 所致 ref 漂移**无 clean high-confidence fix**（混合 drift + 符号移位）。**clean-lever surface + §3 doc-drift 双向穷尽**：自主可 land 工作真正尽（9 重 lever 钉案 + ref 漂移非 clean）。**rally 状态 = directive 07-29 mandated 稳定 plateau 持守模式**。**真正推向 95% 须用户授权深结构**（最高 value = Phase A IFC line-box-metric；次 = font-stack C-dep / R1043 vertical-mode / R2174 taffy border-box / highlight-overlay / ruby-layout / multicol-fragmentation），授权后 spec-rfc 出 RFC 多 session。**下轮可接续**：① 低频周期 plateau-guard；② 待用户点名深结构（唯一推向 95% 路径）。

> **📍 R2530（2026-08-03）📋 outside-marker-paint-order fail 根因核验（paint 顺序既正确，fail = 布局定位非 paint-order；clean-lever hunt 真正收尾）— 零代码**。承接 R2529「全模块 fail triage 闭环」。**核验**：css-pseudo `outside-marker-paint-order`（2.35% FAIL）看似 paint-order bug；查 `painter/mod.rs:632-637` —— `paint_list_marker` **先于** `paint_text` 调用（marker 先绘、内容后绘覆盖 = CSS 2.1 App E 正确顺序），**paint order 既正确**。真因 = `text-indent:-100px` + `margin-left:100px` + outside marker 的**空间重叠定位**（marker 与首行内容 x 位重叠，须 marker/content 定位精确对齐覆盖）= **布局定位**（marker outside 几何 + text-indent 交互），moderate-deep 非 clean paint-order 一行修。**结论**：clean-lever hunt 真正收尾——paint-order 角度（看似最易的候选）经核验亦非 clean（真因布局定位）。**clean-lever surface 穷尽经九重钉案**：R2524 systematic diff + R2525 ::marker/双 tier + R2526 4 angle + R2527 css-pseudo + R2528 文档纠偏 + R2529 css-logical + 本轮 paint-order 根因核验；**全模块 + 全候选角度（not-implemented / shorthand-drop / value-gap / UA / presentational / inheritance / ::marker / paint-order / moderate 重评）闭环**。**rally 状态 = directive 07-29 mandated 稳定 plateau 持守模式**（clean real-effect lever 真正尽，深结构勿借机跳；sanctioned = doc-drift 高价值既清 + plateau-guard 双 tier 既稳）。**真正推向 95% 须用户授权深结构**（最高 value = **Phase A IFC** line-box-metric 统一解 57% reftest plateau + welcome IFC + css-pseudo ::first-letter/::first-line；次 = font-stack C-dep / R1043 vertical-mode / R2174 taffy border-box / highlight-overlay / ruby-layout / multicol-fragmentation），授权后 spec-rfc 出 RFC 多 session。**下轮可接续**：① 低频周期 plateau-guard（九重穷尽 + 全角度闭环，新 clean lever 概率近零）；② 偶发 doc-drift；③ 待用户点名深结构（最高价值 = 唯一推向 95% 的路径）。

> **📍 R2529（2026-08-03）📋 css-logical reftest fail triage（6 reftest / 3 FAIL 全 logical-values-float-clear = R1043 vertical-mode gated）— 零代码，全模块 fail triage 闭环**。承接 R2528（测试计数快照刷新，已 commit）。**方法**：scoped `make reftest-upstream FILTER=css-logical`（98 文件多 JS/getComputedStyle 非 reftest，仅 6 reftest）+ 逐 fail 分类。**3 fail 全 `logical-values-float-clear-{2,4,reftest}`**（diff 1.40-2.36%）= float/clear 逻辑值（inline-start/inline-end）在**垂直书写模式**的轴映射 = **R1043 vertical-mode native 深**（horizontal 模式 logical float/clear 既 work，唯 vertical 模式轴交换 gated）。**结论**：css-logical 经 fail triage 钉案——无 clean lever（唯 3 fail 全 R1043-gated）。**clean-lever surface 穷尽经八重钉案 + 全模块 fail triage 闭环**：R2524 systematic diff + R2525 ::marker/双 tier + R2526 4 angle + R2527 css-pseudo 76 fail + 本轮 css-logical 3 fail；累计覆盖 css-text-decor（R2517/R2522/R2523 既 land）/ css-grid+css-tables oracle（R2516）/ css-pseudo（R2527）/ css-logical（本轮）/ 30-dir self-source（R2516）——**自主 fail triage 全模块闭环，clean lever 真正尽**。**rally 状态 = directive 07-29 mandated 稳定 plateau 持守模式**（深结构勿借机跳）。**真正推向 95% 须用户授权深结构**（最高 value = **Phase A IFC** line-box-metric 统一解 57% reftest plateau + welcome IFC + css-pseudo ::first-letter/::first-line；次 = font-stack C-dep / R1043 vertical-mode【含 css-logical float-clear】/ R2174 taffy border-box / highlight-overlay / ruby-layout），授权后 spec-rfc 出 RFC 多 session。**下轮可接续**：① 低频周期 plateau-guard（全模块 fail triage 闭环，新 clean lever 概率极低）；② 偶发 doc-drift；③ 待用户点名深结构（最高价值 = 唯一推向 95% 的路径）。

> **📍 R2527（2026-08-03）📋 css-pseudo reftest fail triage（261 案 185 PASS / 76 FAIL = 70.9%；fail 全 deep/highlight/form/IFC，无 clean lever）— 零代码，钉案省后续 agent 重扫 css-pseudo**。承接 R2526「plateau 多角度续证」。**方法**：scoped `make reftest-upstream FILTER=css-pseudo` + 逐 fail 分类。**76 fail 分类**：(1) **::selection / ::highlight / ::spelling-error / ::grammar-error / ::target-text**（~40+ 案）= **highlight overlay 系统**（文本选区/拼写语法高亮绘制）= 深 infra + 交互（须 selection state，reftest 静态截图无选区）；(2) **::first-letter / ::first-line** edge case（digraph/trailing-punctuation/dynamic/line-height/inherited/below-float ~15 案）= **R109 IFC / 伪元素深构造** territory；(3) **form pseudo**（::file-selector-button / ::slider-thumb/track/fill 7 案 0.00% diff = 结构未生成 / placeholder-input）= 表单控件伪元素；(4) **::marker pseudo style**（marker-content-012 = content 优先级 + url() content + `list-style-type:'string'` 综合案；marker-font-variant-numeric / marker-inherit-line-height / marker-unicode-bidi / outside-marker-paint-order ~7 案）= **::marker 伪元素 style 应用**（须 marker_pseudo 字段 + cascade + paint consult，moderate-deep，非 clean）；(5) before-as-flex-container / relative-box-order = 伪元素盒模型深。**关键反证**：marker-content-001~008 **全 PASS（0.00%）**（R2525 评估「002 须 content-list 会 fail」**实测推翻**——002 PASS，故 ::marker content 非 gap，仅 012 综合案 + marker-style 案 fail = moderate-deep 伪元素 plumbing）。**结论**：css-pseudo 经 fail triage 钉案——**无 clean lever**（fail 全 highlight/IFC/form/pseudo-style 深 infra）。**clean-lever surface 穷尽经七重钉案**：R2524 systematic diff + R2525 ::marker/双 tier + R2526 4 angle + 本轮 css-pseudo fail triage。**rally 状态 = directive 07-29 mandated 稳定 plateau 持守模式**（深结构勿借机跳）。**真正推向 95% 须用户授权深结构**（最高 value = **Phase A IFC** line-box-metric 统一解 57% reftest plateau + welcome IFC + css-pseudo ::first-letter/::first-line 残余；次 = font-stack C-dep / R1043 vertical-mode / R2174 taffy border-box / highlight-overlay 系统 / ruby-layout），授权后 spec-rfc 出 RFC 多 session。**下轮可接续**：① 周期 plateau-guard（product/legacy smoke 回归守，clean lever 概率极低）；② 偶发 doc-drift；③ 待用户点名深结构（最高价值 = 唯一推向 95% 的路径）。

> **📍 R2526（2026-08-03）📋 plateau 多角度续证（4 fresh angle 皆确认 clean-lever surface 穷尽，省后续 agent 重探）— 零代码**。承接 R2525。**4 角度探查全 negative**：① **inheritance 一致性 diff**（`inherit_property` 114 prop vs `is_inherited` 40 prop）= **false lead**——`inherit_property` 是**显式 `inherit` 关键字处理函数**（任意属性可显式 inherit，全集 114 正确），**非**「默认继承集合」；默认继承由 `is_inherited`（40 真 inherited prop）独立驱动；text-emphasis-style/position 不在 is_inherited 但 inherit_property 有 = 支持显式 inherit，默认继承经他路（spec 实测 emphasis 测试既 pass），非 bug。② **current-baseline.md / dc-progress.md 文档滞后查**：text-underline-offset / text-emphasis-color / text-decoration-line **未作 gap 列出**（R2522/R2523 前未被专项追踪），故无 stale gap claim 须纠。③ **doc-drift `.rs:NNNN` 续扫**：残余 ~30 处多引用未拆分文件（mod.rs/tree.rs/effects.rs/inline/mod.rs），R2516 既核「pre-existing 经 R2482/R2502/R2504 既核准确」；R2509-R2513 拆分所致 drift 仅 2 处已 R2515 修（postprocess engine.rs:1170 + print_pagination pipeline/mod.rs:751），**无新可修**。④ **moderate 候选重评**：background-blend-mode = per-layer 背景合成（moderate-deep，背景 paint 集成，稀疏 test，非 clean）；::marker content-list（R2525 既评 moderate-deep）。**结论**：clean-lever surface 经 R2524（systematic registry-vs-corpus diff）+ R2525（::marker 评估 + 双 tier 零回归）+ 本轮 4 fresh angle（inheritance / baseline / doc-drift / moderate 重评）**六重穷尽钉案**——自主 clean real-effect lever 真正尽；本会话链 3 连 fresh-angle lever（R2517/R2522/R2523）是 css-text-decor 同源真最后 cluster。**rally 状态 = directive 07-29 mandated 稳定 plateau**（持守模式：sanctioned = doc-drift 高价值既清 + plateau-guard R2525 既稳 + 周期 re-probe 低概率；**深结构勿借机跳**）。**真正推向 95% 须用户授权深结构**（最高 value = **Phase A IFC** line-box-metric 统一解 57% reftest plateau + welcome IFC 残余；次 = font-stack C-dep / R1043 vertical-mode / R2174 taffy border-box / ruby-layout / multicol-fragmentation），授权后 spec-rfc 出 RFC 多 session。**下轮可接续**：① 周期 plateau-guard（systematic diff 既尽新 lever 概率极低）；② 偶发 doc-drift；③ 待用户点名深结构（最高价值解锁 = 唯一推向 95% 的路径）。

> **📍 R2525（2026-08-03）📋 plateau-guard：product-smoke-legacy 51 fixture 确认 R2522/R2523 零回归（text-decor paint 变更对 legacy HTML 3.2/4 无结构退化）+ ::marker content moderate-deep 评估钉案 — 零代码**。承接 R2524「sibling vein 穷尽」。**两路工作**：(1) **legacy plateau-guard**（directive「每轮先确认 legacy smoke 无新回归」；R2522 text-underline-offset + R2523 text-emphasis-color 改 css-text-decor paint，legacy 有下划线链接故须验）：`make product-smoke-legacy` EXIT=0，**51 fixture / 0 struct-check FAIL / avg 2.36% 与 R2516/R2518/R2520 baseline 完全持平**（37-form-controls struct PASS 3.85% 历史已解；font-wall 趋势数据不变）→ R2522/R2523 **default byte-identical 在 modern（product-smoke welcome 17.03%）+ legacy（51 fixture）双 tier 实证**（auto=0 / currentColor 字节一致设计验证）。(2) **::marker content（CSS Pseudo 4 §marker）评估钉案**：`::marker` pseudo-element 已 parse（css-parser）+ `paint_list_marker` 存在 + matcher 通用支持任意 pseudo_name（`collect_pseudo_declarations_with_media` generic）；但 driving test `marker-content-001` ref = **默认 `<ol>` 编号** + test override `content:"1.\00a0"` = 默认 → **已 PASS**（ZW 忽略 override 回退默认 = ref，非 flip）；真 flip 的 `marker-content-002` `content:"a" counters(list-item,".") "b"` = **content-list + counter resolution**（R2437 paint_content 谱，moderate-deep，非 String-only clean）→ String-only ::marker content 无 reftest footprint（001 既 pass / 002+ 须 content-list），code-guidelines §2 反对未请求灵活性，**defer 钉案**。**结论**：clean-lever surface 经 R2524 systematic diff + 本轮 ::marker moderate-deep 评估**双重钉案穷尽**；R2522/R2523 双 tier 零回归实证。**rally 状态 = directive 07-29 mandated 稳定 plateau**（3 连 clean lever R2517/R2522/R2523 已 land + 双 tier 验证）。**真正推向 95% 须用户授权深结构**（最高 value = **Phase A IFC** line-box-metric 统一解 57% reftest plateau + welcome IFC 残余；次 = font-stack C-dep / R1043 vertical-mode / R2174 taffy border-box / ruby-layout / multicol-fragmentation），授权后 spec-rfc 出 RFC 多 session。**下轮可接续**：① 周期 plateau-guard（product/legacy smoke 回归守，systematic diff 既尽新 lever 概率极低）；② 偶发 doc-drift；③ ::marker content-list / background-blend-mode 作 moderate slice（低 ROI，须 A/B）；④ 待用户点名深结构（最高价值解锁）。

## 轮次详记归档

> **R2167–R2197 + R2199c 轮次详记已归档**（2026-07-29 文档结构性精简）：原 master.md 顶部轮次详记区（R2167→R2197 + R2199c，~190 行 blockquote，含多行续行）已逐字保留 → [`archive/master-pre-slimdown-2026-07-29.md`](./archive/master-pre-slimdown-2026-07-29.md) 顶部轮次详记区（零删减）。更早轮次归档见 [`archive/`](./archive/)。
>
> **R2201–R2458 顶部轮次详记区已归档**（2026-08-04 R2561 doc-control 瘦身——top-block 最大块）：原 master.md anchor 之后、§最近轮次摘要 之前顶部轮次详记区 258 轮（R2391→R2223 file-order，interleaved 非严格时序；含 font-matching 授权/落地 R2391-R2417、color/gradient/shorthand lever era R2266-R2402、Phase A/doc-vs-code audit era R2420-R2458）已逐字保留 → [`archive/rounds-r2201-r2458-topblock-summary.md`](./archive/rounds-r2201-r2458-topblock-summary.md)（零删减）；关键决策（font-metric R2393 dormant / font-weight·style matching landed / Phase A inline-box-model DONE）已沉淀于本文结构化章节，迁出详记不丢失当前指引；master.md 顶部现仅留 ruling + anchor（598→229 行）。
>
> **R2459–R2495 详记已归档**（2026-08-04 R2555 doc-control 策略「master.md 不允许无限增长」瘦身）：原 §最近轮次摘要 底部 36 轮（R2495→R2459，含 R2486–R2495 旧 plain-「承接」format 体 + R2459–R2485 blockquote 体）已逐字保留 → [`archive/rounds-r2459-r2495-summary.md`](./archive/rounds-r2459-r2495-summary.md)（零删减）；§最近轮次摘要 现仅留 R2496–R2555 全 blockquote 干净体。
>
> **R2496–R2524 详记已归档**（2026-08-04 R2556 doc-control 瘦身续 R2555）：原 §最近轮次摘要 底部 29 轮（R2524→R2496，全 blockquote 体）已逐字保留 → [`archive/rounds-r2496-r2524-summary.md`](./archive/rounds-r2496-r2524-summary.md)（零删减）；§最近轮次摘要 现仅留 R2525–R2556（~28 轮健康 recent 窗口）——**doc-control 瘦身收官**（后续待新轮累积再迁）。

## 通过率快照

- **make test**：13190 passed / 0 failed / 74 ignored（R2523 text-emphasis-color +1 unit / R2522 text-underline-offset +4 unit / R2517 text-decoration-line +1 unit，累加自 R2509 的 13184；每轮零回归 EXIT=0；R2563 周期复跑逐位确认）
- **reftest oracle**：58.8% oracle-pass（5969/10397，+0.6pp vs R2185 baseline），57.6% credible
- **product-smoke**：welcome 17.03% / wintertc / morning item-tag:3 全 PASS
- **product-smoke-legacy**：51/51 struct PASS，19-testpage 17.23%（−5.16pp），20-mixed 11.49%（−1.64pp）

---

## 当前状态概览

| 维度 | 状态 | 说明 |
|------|------|------|
| 渲染管线 | ⚠️ 全链路贯通但非一致 | HTML→CSS→Style→Layout→Paint→Composite 可运行；但 layout IFC、paint IFC 和 ZeroBrowser glyph 消费仍存在多套坐标/度量路径，`welcome.html` 已暴露用户可见错位 |
| WPT Runner | ✅ reftest 级 | 1,341 个手写 TestCase + 685 个内联 reftest（13 目录 ≥50） |
| Reftest Harness | ✅ 可用 | 分类容差、per-test fuzzy 注解、match/mismatch 模式 |
| CPU 软件渲染 | ✅ 全量图元 | render_full_scene() 支持全部 13 种图元（fills, rounded_rects, gradients, shadows, images, strokes, path_fills, path_strokes, glyphs, clips, transforms, filters, blend_modes） |
| 产品/真实静态页面视觉 smoke | ✅ 证据已持久化·持续修复 | welcome/morning.work/wintertc fixture + product-smoke + chromium Oracle 工具链就绪；**证据已持久化 `evidence/product-static/`**；**post-R632 diff（R2544 re-baseline 复测）**：welcome **17.03%**（81737/480000px，旧快照 16.16% stale）、wintertc 13.59%、morning-work 800×600 **18.15%**；★R630 修复用户痛点「文字堆叠看不清」；残余 diff = 中文字体度量（R633 Phase A 死锁）+ R109 IFC + hljs（需 JS），非证据缺口 |

---


## 下一步

> **轻量修复主线（用户 2026-07-29 裁决；R2542-R2564 多轴穷尽 update）**：~~持续做有 driving test、低风险、A/B 零回归的 CSS2/parser/selector clean lever~~（**R2542-R2564 经 clean-lever 多轴（feature-existence×6 + correctness-review×2[IFC R2547 + table R2564] + shorthand + value-gap + applied-consumed + reftest-triage + doc-drift + marked-todo = 8 distinct angle）+ 深结构可切片性全项核验 = 穷尽**——候选清单全清零，全剩余深结构项非自主 tractable：Phase A IFC line-box-metric 不可切片 [5 轮 net-negative 史 + 设计 v1.4 须整体 unification] / DC-9 infrastructure-blocked / Phase 2 multicol 深 RFC / font-metric·@counter-style-slice2·individual-transforms 皆 net-negative 史 / R1043·R2174·font-stack user-gated）。**当前活跃自主面 = 仅 ① 低频周期 plateau-guard（make test + product-smoke + product-smoke-legacy 回归守，待行为变更/drift 累积；post-split triple-guard R2544/R2545 三腿全绿）+ ② 文档与代码不一致纠偏**。产品/legacy smoke 可见稳定性修复仍适用。**唯一推向 95% = 用户点名授权深结构专项**（最高 value = Phase A IFC line-box-metric 统一，须接受多轮 A/B 收敛风险）。

### 待用户决策清单

- ~~**font-metric 生产激活+A/B**~~（**R2393 已完结 = net 负，保持 dormant**）：R2202 dormant 基础设施完整，2026-08-01 用户授权激活 + product-smoke A/B（本 session R2393 复核验证）= **welcome 17.03%→17.47%（+0.44pp 恶化）、morning 中文零变化、8/8 struct PASS** → **net 负，不 land，保持 env 默认关**。根因（R2202 两假设证伪）：① welcome `sans-serif` 已从 DejaVu 换 Liberation Sans（R1263，真值≠1.164）→ 激活后 normal 行高变；② morning 全显式 line-height（无 normal）→ 不经 font-metric provider → 零变化。证据 [`evidence/font-metric-activation-ab-2026-08-01.md`](evidence/font-metric-activation-ab-2026-08-01.md)。**后续 agent 勿再以 font-metric 激活为 lever**（已实测 net 负）；若推进须与 IFC strut/half-leading 真实化打包（深结构 R834 谱系）。
- **vertical-mode native R1043**：深结构性方向，等用户授权
- **taffy replaced-element border-box R2174**：深结构性方向，等用户授权  
- **Phase A slice-3 IFC 深构造**：深结构性方向，等用户授权
- **font-stack C-dep rebuild**：user-blocked（R2025 踩坑勿推），等用户明确授权
- **margin-trim 残余深路径**（R2384 记、R2385 订正）：CSS Box Model 4 §8.3 `margin-trim` **块轴（block / block-start / block-end）已实现并消费**（R2248：`tree.rs:1018-1047` build_subtree 布局前归零首/末 in-flow block-level 子 margin，kill-switch `ZW_MARGIN_TRIM=0` default-on；block-container-block-001 等 driving 案 self-source 0.00% PASS）。R2384 原文「layout 完全未消费」**不准确（doc-vs-code 不一致）**。实证（R2385 `make reftest-upstream css-box`）css-box 38/67 (56.7%)；29 案 margin-trim fail **全是 R2248/R2249 已 defer 的深子路径**：① 5 self-collapsing（须 margin_collapse.rs 整合）；② 7 block-in-inline（R109 Phase A）；③ 3 replaced（R2174）；④ 8 flex + 3 grid + 3 multicol-spanner（各布局模式独立 margin-trim 语义，非 build_subtree 单点）。Chrome 137+ aligned；这些深路径 = substantial 跨 layout-mode 深改，等用户授权专项
- **系统性「文档 vs 代码实际状态」核对**：R2202 发现文档滞后正导致反复盲推空转，等用户授权
- ~~**全局关键字 + `all` 简写 cascade**~~（**R2386 订正 = 本条作废；R2462/R2463 系统审计闭环**）：R2233 原文「ZW 无 per-property initial/inherit/unset/revert 处理 → all 简写无法实现（需先建全局关键字基础设施）」**两处均 stale**——① 全局关键字解析+应用**早已完整实现**（`inheritance.rs` `KeywordResolution` 五态 Inherit/Initial/Unset/Revert/RevertLayer + `compute_inherited_style_with_quirks` 全消费 + 数十测试）；② `all` 简写**R2386 已 LANDED**（cascade 展开，css-cascade 17→19，+2 真 flip 零回归）。**R2462/R2463 系统审计**（对所有 known property 检查三路径 case 覆盖）：`inherit_property`（inherit 关键字）原缺 box-shadow + contain-intrinsic-width/height → **R2462/R2463 修复，现 0 missing**；`apply_initial_value`（initial/unset 关键字）**0 missing**；apply 派发（value 路径）**0 missing**。**wide-keyword 三路径覆盖全闭环验证，勿再系统审计**。css-cascade 残余 30 fail 全非 global-keyword/all 问题：revert-layer 真层感知（13，现简化≈unset）、@scope 未实现（8）、@import-conditional/@layer 交互（9）、revert-val（1）。
- ~~**`<q>` 引号 + open-quote/close-quote 机制**~~（**R2386 订正 = 本条作废**）：R2233 原文「ZW 无 UA q::before + 无 quote 深度计数；quotes 无消费方」**stale**——`<q>` 自动引号**R2246 已 LANDED**（`resolve_q_quotes`/`count_q_ancestors`/`q_default_quote_pair` + `lib.rs:346` compute_styles 期注入 before/after 伪元素 content，复用既有 pipeline）。css-content 残余 = per-language CLDR quotes（hu/ar/lo 等）+ element-replacement（`:root{content:url()}`）+ `attr(type(<color>))`，**非核心 `<q>` 机制缺口**。**勿再以「`<q>` 引号」为核心待实施 lever**。
- **`@counter-style` 自定义计数器 at-rule**（R2233 发现）：~~css-counter-styles 73.6% 残余主因（自定义 counter at-rule + 内置样式 range/fallback 语义）；feature，等用户授权~~ **R2392 slice 1 已 land**（cyclic/fixed/symbolic/alphabetic/numeric 5 系统 + parse/registry/Custom arm/paint）；**残余 additive/extends 系统 R2394 defer**（A/B net-negative，等用户授权 slice 2）+ 内置样式 range/fallback/pad/negative 完整应用
- **individual `translate`/`rotate`/`scale` 属性**（R2368 发现）：CSS Transforms 2 §3.1，ZW 完全未实现（css-transforms 65.2%，individual-transform 子 dir 55.6%）；实现尝试 net -2 回退（须先正确处理 3D 形式 + transform-box 交互 + renderer 非可逆矩阵 apply_transform_post det≈0 清空语义）；moderate feature，等用户授权专项

### 轻量修复候选

**当前活跃轻量主线 = CSS 合规缺口定向 code-reading probe（多 vein 并行）**。R2373-R2383 连续 11 lever **彻底推翻 R2372「双路径穷尽」结论**，跨多 vein：(1) color-mix 标准空间补全+feature 收官（R2376-R2378 空间 + R2381 hue method）；(2) **CSS 属性 value-级 parse-gap vein**（R2379 outline-style:auto、R2380 aspect-ratio、R2382 justify-items/self left/right、R2383 alignment normal）；(3) CSS Color 系统色/单位/gradient-hint（R2373-R2375）。

★ **R2204 洞见 + R2373-R2383 十一连实证**：「exhausted」结论**局限于 reftest-visible + 已扫维度**；unit-test 驱动的 CSS 合规缺口经定向代码阅读持续可发现。**两类高产出模式**：① 转换/常量「已存在仅缺接线」（color-mix 空间）；② Explore 短阵广扫属性表定位 value-级缺失关键字。

**★ CSS 属性 value-级 gap vein 经多轮探针（~50 属性：Tables/Text/UI/Overflow/Grid/Flex/Multicol/Backgrounds 全扫）已 Chrome-aligned 穷尽**——勿再盲扫；caption-side 等 Chrome 不支持者补反 diverge；subgrid/宽色域需 layout/gamut（defer）。**下一 lever 须 pivot 到其他 vein**。

下一轮可接续的具体入口（逐条 verify-then-fix，TDD red→green，优先 unit-test 驱动不强依赖 wpt-data；每修一个跑 scoped test + clippy + fmt + 全量 `make test`，net≥0 land）：
- **reftest-driven vein（pivot 首选）**：scoped `make reftest-upstream FILTER=<dir>` 量未深扫 dir（css-box / css-values / css-backgrounds 边角）找离散 fail → 追踪到 parse/render bug（R2369/R2370 模式；R2372 称 exhausted 但 R2369+ 仍出 lever）。**R2460 实测 css-ui = 926/986=93.9% self-source，60 fail 全深**（box-sizing=R2174 / caret=host-layer / outline/text-overflow=IFC+font-wall / widgets=深，详见 R2460 摘要）——css-ui 无 clean lever。**R2499 实测 css-logical = 3/6=50%（cascading-001 离散 lever → cascade canonical 化 +4 真 flip css-logical+css-box）；余 3 fail 全 vertical-rl R1043-gated**——未扫 dir 仍可出离散 lever。**css-sizing(881) 与 R2174 territory 交集大、css-values(空壳)**，scoped 探针预期深案主导；若续扫须先估深案比例勿盲跑全量。**下一未扫 dir 候选**：~~css-overflow / css-ruby / css-writing-modes~~（**R2501 全扫完，均非 clean lever 来源**——css-overflow 52.9% 余 fail 全布局/host-layer/video；css-writing-modes 80.1% fail 全 vertical R1043-gated + baseline 深；css-ruby 75.9% fail 全 ruby-layout RFC defer；css-values 0 reftest；css-display 未导入）。**可达未扫 dir clean-lever surface R2501 实证穷尽**——下一 lever 须 pivot 到 doc-drift 纠偏（R2482 模式）或待用户点名深结构。
- **CSS Counter / 生成内容**：`counter()`/`counters()` 解析+渲染配套、`@counter-style`（R2233 待授权）；`<q>` quotes（R2233 待授权）。
- ~~**CSS `env()` 解析**~~（**R2459 核验 = 本条作废**）：原文「`env(x,fallback)` 现被丢」**stale**——`computed.rs` `resolve_env_reference` 已正确消费 fallback（未定义 env 名 → 用 fallback，递归解析嵌套 env/var，`resolve_env_and_var` 先 env 后 var）；测试覆盖 `env(safe-area-inset-top, 10px)`→0px + `env(undefined-x, env(safe-area-inset-left))`。**勿再以「env fallback 丢失」为 lever**。
- **孤儿/死代码清理（R2383 发现，R2460 再确认）**：`parse_basic.rs` 整文件未被 `mod` 声明（非 build），是 color.rs/parse_layout.rs 重导出版本的死副本且有 auto 不一致——R2460 复核 `parse_tab_size` 在 `parse_basic.rs:1008` 与 `parse_layout.rs:610` 双份共存确证死副本。**CLAUDE.md 准则：发现无关死代码「提及但不删除」，须用户明确要求方可清理**——记此待用户点名。
- **CSS Values 4 单位表续**：~~`dppx`/`dpi`/`dpcm`~~ **R2459 已 LANDED**（`<resolution>` 全单位 + `x` 别名 + 大小写不敏感，见上 R2459）。**R2460 核验 `lh`/`rlh` 非 clean lever**：`resolve_length`（`computed.rs:18`）签名仅 `(font_size, vw, vh)`，无 line-height 上下文 → 加 `lh` 须 thread 元素 computed line-height 进所有调用点（borderline-deep 签名改）；`rlh` 在根 line-height=`normal` 时依赖 font 度量（font-wall territory）；`cap`/`ic` 同需字体度量 deferred。**勿再以 lh/rlh 为快修 lever**。
- **CSS Syntax §4/§5 其他合规缺口**：~~MQ L4 比较运算符 token 化~~（**R2542 作废**，`media_query.rs` 全 5 运算符 + 双侧范围已完整实现）；~~@layer cascade layers~~（**R2542 核验已完整实现**，CascadeOrder.layer_index + sort_key + 44 WPT 文件）；~~CDO/CDC 非顶层上下文~~（**R2543 核验 = cosmetic 非缺口**：R2204 已将 CDO/CDC tokenize 为 `Token::Comment`，parser `skip_whitespace`〔mod.rs:331〕随处跳过 Whitespace+Comment——CSS 注释本就所有上下文 ignorable，故 CDO/CDC 非顶层「parse error」仅诊断标志零渲染影响，Comment 处理已 spec-correct）；CSS Values 4 数学函数亦经 R2543 re-confirm R2280 全实现（round/mod/rem/sign/abs + trig 7 + pow/sqrt/hypot/exp/log + π/e/infinity/NaN）。**该候选条目 R2542-R2543 全清零**。
- **文档 vs 代码行号漂移续修**（R2203 verify-then-fix 模式）。
- 每修一个：仅文档→跳过昂贵 make test；连带 `.rs`→跑 scoped test-guard + 必要时全量 `make test`，net≥0 land。

期间不要借机跳入深结构（见「深结构性方向」与「待用户决策清单」）。

### 深结构性方向（等用户授权）

以下方向不自主开工，记待决策清单等用户点名：

1. **Phase A（两条独立线程，R2546 代码核验区分）**：
   - ✅ **(a) inline-box-model coherence**（[`phase-a-inline-box-model-full-design-2026-07-29.md`](./phase-a-inline-box-model-full-design-2026-07-29.md)）= **已 DONE default-on**（R2156 slice 1 + R2160-R2162 slice 2 + R2197/R2198 slice 3；env `ZW_PHASEA_MULTI_INLINE`〔`!=Ok("0")`=default-on〕+ `PHASEA_STORE_EXT` + `backfill_phasea_orphan_boxes`〔inline_finalization.rs:1026〕全在码中）→ **R2545 legacy smoke 增益之源**（37-form-controls struct PASS 3.85%、19-testpage 22.39%→17.23%）。勿再以本线程为 lever（已落地）。
   - ⛔ **(b) IFC line-box-metric unification**（[`phase-a-IFC-unification-design.md`](./phase-a-IFC-unification-design.md)）= **唯一推向 95% 的 gated 高风险深结构路径**：解 large-font（ifc-008/009/011）+ welcome/morning 文本度量残余 + R109 inline-block content 独立 layout/paint 路径（R2332 定论）。**5 轮单点解锁（R125/R198/R205/R209/R213）全 net-negative 回归**，唯 R207 narrow（pure-inline 叶文本容器 +1 font-051）成功；v1.4 裁决「fix 须随整体 unification（消灭 Path B / 墙③ baseline 语义分歧 + 墙② multicol 反向依赖）非独立可切片」；**不自主开工**（repeats R209/R213 net-negative 史），等用户授权专项 + 接受多轮 A/B 收敛风险。
2. **Phase 2 嵌套 multicol fragmentation**（[`multicol-fragmentation-design.md`](./multicol-fragmentation-design.md)、[`column-aware-IFC-spec.md`](./column-aware-IFC-spec.md)）— 解 multicol-breaking（css-multicol 最大失败聚类）。R319 证纯 inline 迁移零增益，真实价值在嵌套 / 混合内容碎片化。
3. **baseline-export 真修复** — taffy 0.12.1 已支持 flex §8.5 first-baseline；fresh 实测 baseline-000~008 + flexbox-baseline-synthesis 全 near-pass font-wall，非结构性缺口。**勿再以 baseline-export 为 lever**。
4. **DC-9 blend_mode** — paint-isolation 架构（offscreen 子树渲染 + source/dest 双纹理 blend pass），低 reftest footprint（~2-4 案）。**R2548 可切片性核验 = infrastructure-blocked 非 tractable 自主深 lever**：render-foundation **无 offscreen/intermediate-buffer/render-to-texture 能力**（grep 零命中）；现状 `apply_blend_mode` 仅 flat per-element primitive（`add_blend_mode(BlendModePrimitive{rect,mode})`，painter/effects.rs:517）非真 paint-isolation；mix-blend-mode parse/store/apply/SC-trigger（R2302/R2309）齐备但**缺 offscreen 子树成组渲染基建**。须先建 render-to-texture 子树 buffer（major multi-session 基建）+ 仅 ~2-4 案价值 = 低 ROI，**不自主开工**。

---

## Done Criteria 进度

**Done Criteria 进度**：详见 [rendering-compat.md §Done Criteria](../rendering-compat.md#done-criteria)

---


## 通过率详细快照

### reftest 通过率（最新）

- **self-source 全量目录**（R2219 scoped 复测，wpt-data v1.1）：css-grid 37/49=75.5% + css-position 83/97=85.6% + css-tables 103/115=89.6% + css-flexbox 339/497=68.2% + css-multicol 264/452=58.4% + css-text-decor 245/250=98.0% + css-fonts 281/287=97.9%。**7 目录聚合 self-source 1352/1747=77.4%**（旧快照 56.5% 严重 stale——position/tables/multicol/grid 自那以后大幅改善；flexbox 68.2% 低于旧 74.2% 但 R2213-R2217 selector 改动经核验非因——flexbox fail 全 flex 布局/font-wall 深结构，无失败案用所改选择器）
- **css/selectors self-source 187/224=83.5%**（wpt-data v1.2，R2221 specificity + R2223 nth-of-invalid 后；R2221 实测 186/224=83.0%，本轮 nth-of-invalid fail→pass +1，零回归——css-flexbox 68.2% 跨 crate 复核无回归）。**R2503 全簇 triage**：37 fail 全已实现或 OOS——`:focus-within`（已 parse，需 focus 状态 host-layer defer）/`:dir()`（已 parse+match，Shadow DOM OOS）/`:nth-child of S`（已 parse，edge 非 clean）/`featureless`（@scope OOS）/`popover-has`（host-layer）/`invalidation/*`（JS dynamic defer）/`selection-image`（::selection mismatch edge）；**无 clean lever**。
- **css/css-variables self-source 173/182=95.1%**（wpt-data v1.3，R2224 var() 环 OOM 修复 + 链完全解析 + R2284 @property 注册 + R2285 light-dark dark-scheme 后；旧 0%/OOM——variable-declaration-48/49 var() 环致 6GB OOM，现 0.00% PASS；registered-property-light-dark R2285 fail→pass；9 残余 fail 全深：`@font-face`+var/CSS counters/wide-keyword 语义/shorthand var 代换）
- **css/css-conditional self-source 116/166=69.9%**（wpt-data v1.4，R2225 `@supports` and/or 混用 +3、R2226 not 混用/嵌套括号/裸 feature +4、R2227 general-enclosed + `!important` + 括号深度 prelude +9、R2228 `font` 简写求值 +1（css-supports 44/44=100% 全绿）、R2229 `@media` 空查询≡all +1、**R2230 畸形 `@supports;` 恢复 +3**；残余多 @container feature gap + at-supports selector()/font-*() 函数求值 + @media dynamic JS）
- **css/css-color 287/308=93.2% / css-pseudo 186/261=71.3% / css-nesting 12/17=70.6%**（css-color R2253-R2259 +104 + R2267 color-mix +1 + R2268 RCS identity +14 + R2269 lab/lch/oklab/oklch +46 + R2270 display-p3-linear +6 + R2271 RCS rgb/hsl non-identity +2 + R2272 rebeccapurple +1 + R2273 color-mix(in lch) +2 + R2274 wide-gamut RCS + R2285 light-dark dark-scheme + **R2323 Lab/LCH/OKLab/OKLCH L 边界 gamut 映射** + **R2324 color() 百分比分量 +5** + **R2325 OKLab/OKLCH L 边界容差 +4（oklab/oklch-l-almost-0/1）**：CSS Color 4/5 色彩空间 + RCS + light-dark dark-scheme + 边界 gamut 映射基本完备；残余 opacity/filter 合成（7 案深）、contrast-color（CSS Color 6 feature）、currentcolor-003 nested、color-mix（JS Web Animations API）、tagged-images ICC、t425-hsla alpha 合成、t31 font-wall——clean lever 在 css-color 已穷尽）。
- **css/css-backgrounds 363/710=51.1% / css-borders 43/83=51.8%（R2483 首测，原「待测」）**（wpt-data v1.5；R2469 +12 body-propagation；R2476 +7 box-shadow inset；R2477 text/box-shadow 颜色位置；R2478 background-position 3/4 值语法 = +2 真 flip − 4 false-pass unmask；**R2481 background 简写 `/size` 解析 = net 0（/size parse 正确化，paint 层 size 渲染瓶颈故 background-334 仍 fail；box 累积经 A/B 证 net −3 回退保持 drop）**）——css-borders fail 簇 = **corner-shape/border-shape（bevel/scoop/notch/superellipse 角几何，paint 层深，~2% diff）** + border-image/sub-pixel；css-backgrounds fail 多 background-size/clip/origin 的 bg-image 定位裁剪（paint 层深）、background-repeat:space/round、border-width sub-pixel round；box-shadow 残余 = radius/fragmentation/table/paint-deep。**R2484 核验 image-loading 就绪**：`build_image_cache`（reftest/resources.rs:528）解码 data-URI（SVG/PNG/JPEG/GIF）+ 文件 URL（base_dir + percent-decode + 站点根 R2427）+ 提取 intrinsic size，`get_support_image_color` 提供小 swatch 色——故 bg-image/bg-size fail 簇 = **paint 渲染精度**（size/clip/intrinsic-sizing），**非 image 加载问题**，勿再查加载路径。
- **css/css-counter-styles 183/250=73.2% / css-cascade 26/49=53.1% / css-content 49/65=75.4%**（R2473 复测 css-cascade；R2233 原 184/250=73.6% / 44/65=67.7% 已 stale；旧快照 css-cascade 16/49=32.7% 亦 stale）——counter-styles：R2392 `@counter-style` slice 1 + R2445-R2451 + R2471（11 numeric）+ R2472（cjk-decimal/cambodian）预定义 cluster 已 land，**dir 整体受 CJK/Indic tofu（DejaVu 不覆盖=须 CJK 字体基建）+ @counter-style additive/extends deep（R2394 defer）+ CJK formal/informal 须 i64 counter（R2473 已 widening）+ ~100 行算法（defer）cap 在 ~73%**；cascade 残余 = `revert-layer`（R2388 基础已 land，残余 !important 交互 defer）+ `all` 简写（R2386 已 land）+ @scope（Shadow DOM OOS）+ @layer（深 cascade）；content 残余 = `<q>` 引号**已实现**（R2246）+ element-replacement dynamic/gradient deep + quotes 残余（paint_quotes A/B 证非 double-render，须另查 DOM-injection 定位）；均非新 clean lever。
- **css/css-box 41/67=61.2%**（wpt-data v1.10，R2248 margin-trim 块轴裁剪 → 38/67=56.7%；**R2499 logical sizing cascade canonical 化 → 41/67=61.2%（+3 真 flip，0 回归）**——同元素 logical+physical 尺寸 cascade 冲突一并修复；css-box reftest 即 margin-trim 子集；block-container-block-001/002 + block-start-001~004 + block-end-001/002 全 0.00% strict 真通过；残余 = grid-*/multicol-spanner-004/005/006 + 自折叠/嵌套深案，全 deferred 范围）
- **css/css-logical 3/6=50.0%**（wpt-data，R2499 首测入档，原不在快照）：cascading-001 fail→pass（logical+physical sizing cascade canonical 化）；logical-values-float-clear-1/3 horizontal 已 PASS（logical float/clear 值水平模式已实现）；**余 3 fail 全 `writing-mode: vertical-rl`**（float-clear-2/4/reftest）= R1043 vertical-mode-gated，defer；css-logical 仅 6 reftest（余为 parsing/animation 非图）。
- **css/css-overflow 274/518=52.9%**（wpt-data，R2500 首测入档，原不在快照）：line-clamp R2431/R2467 在前；**R2500 overflow-clip-margin 特性 land**（overflow-clip-margin-010 fail→pass，CSS Overflow 3 §3 `<visual-box>||<length>` 4-crate plumbing）；余 fail 簇 = overflow-clip-margin-001/013-022（**既有 flex/定位布局差异**，001 diff 4.50% 远大于 clip-margin 环 ~4400px，非特性缺口）+ clip-002..005（axis-specific clip，**R2501 实证死胡同**：HUGE-range no-clip 改动 net −1 = clip-002 布局根因无变化 + line-clamp-019 回归，全 revert；须先拆单 rect per-axis + 解 line-clamp ellipsis 交互，深 defer）+ scroll-markers（bleeding-edge 特性 defer）+ scrollbar-gutter/scroll-into-view（host 层 scroll defer）+ overflow-video-hidden（video 播放+async JS，OOS）。
- **css/css-writing-modes 631/788=80.1%**（wpt-data，R2501 首测入档，原不在快照）：157 fail 几乎全 `vrl`/`vlr`/`slr`/`srl`（vertical writing-mode = **R1043 user-gated**）；11 非 vertical fail 全 `inline-block/table-alignment-*`（6.94-16.12% = baseline/font-wall 深）；**无 clean lever**。
- **css/css-ruby 107/141=75.9%**（wpt-data，R2501 首测入档，原不在快照）：34 fail 全 ruby 布局特化（ruby-overhang-spaces / ruby-intrinsic-isize / ruby-annotation / ruby-box-generation / ruby-layout-internal-boxes = **ruby-layout RFC defer territory**）；无 clean lever。
- **css/css-values 0 reftest / css-display 未导入**（R2501 确认）：css-values 仅 parsing-only 无图测；css-display wpt-data 无该 dir——均非 reftest clean-lever 来源。
- **css/css-contain 312/429=72.7%**（wpt-data v1.10，R2369 contain-on-html/body 抑制画布背景传播后：contain-body-bg-001..004 + contain-html-bg-001..004 共 8 case 87.50%→0.00%；残余多 contain:layout/paint/size/inline-size containment feature 深实现）
- **css/css-sizing 230/562=40.9%**（wpt-data v1.10，R2468 首测 self-source + logical longhands +1：原完全不在快照；333 fail 全 structural——aspect-ratio/* ~139 = R2174 taffy border-box 同脉、intrinsic-percent-replaced 26、fit-content() 11 = intrinsic two-pass；clean lever 仅 contain-intrinsic-logical 已 land，余 defer，勿重扫）
- **css/css-images 253/460=55.0%**（wpt-data v1.10，R2370 gradient color-stop currentColor 后：color-stop-currentcolor + invalidation 96.08%→0.00%；残余 cross-fade/object-view-box/gradient-colorspace/missing-component 深特性）
- **css/css-ui 926/986=93.9%**（wpt-data v1.10，R2460 首测 self-source：原完全不在快照；60 fail 全深结构——box-sizing-007~025(~20)=R2174 replaced-element border-box / caret-*(~15)=host-layer 光标 / outline-*(~10)=sub-pixel+font-wall+outline-013 负 offset clamp 单案 / text-overflow-*(~6)=IFC+font-wall / widgets=深；value-level 特性 accent-color/caret-color/outline/tab-size 全已实现 → 无 clean lever）
- **CSS2/normal-flow 687/746=92.1%**（wpt-data v1.10，R2372 量证：DC-2 CSS 2.1 块布局基础强；59 fail 主为 inline-replaced-width/height + inline-block-replaced-width/height 聚类（replaced-element sizing = R2174 territory）+ min-height-106 等 overflow:auto scrollbar（host 层 scroll deferred）；无清洁 lever）
- **css/css-text 1742/1826=95.4%**（R2456 复测 self-source：**DC-5 文字排版 css-text 组件已达 95% 目标**；84 fail 残余 = IFC/font-wall/feature 深，无 clean lever）——**plateau 重框**：self-source reftest（test+ref 同经 ZW 渲染）**font-wall 相消**，故 self-source 高通过率 = 布局/算法正确性强（css-text 95.4% / CSS2 92.1% / css-counter-styles 73.2% 受 CJK tofu+@counter-style deep 限非 font-wall）；**chromium-Oracle 一致率低（~47.5%）真因 = fontdue 光栅化 font-wall**（R2455 确证），非布局 bug。
- **7 目录全量 chromium-Oracle 真一致 47.5%**（post-font-wall 全幅，旧 stale 36.4%）：grid 20/49=40.8% / position 57/97=58.8% / tables 74/115=64.3% / flexbox 298/497=60.0% / text-decor 118/242=48.8% / fonts 100/282=35.5% / multicol 157/452=34.7%（最低，chr<1%，聚合 **824/1734=47.5%**）
- **self-source strict**：295/490 (60.2%) @ 锁定 0.1%/0.5%（DC-14 真通过口径，pre-grid-expand）
- **chromium-Oracle 广义一致**：200/475 (42.1%) @ chr<1%（R391 锁定诚实基线）；严格 self-pass&chr<1% **177/475 (37.3%)**；污染 46.5%

### 产品 smoke（最新）

- **welcome**：17.03%（R2544 re-baseline 复测 post-split 无回归，81737/480000px；R632 后稳定；旧快照 16.84% stale）
- **wintertc**：13.59%（R227+R255 后）
- **morning-work**：18.15%（R630 多行 y 分行 + R632 line-height 让中文行级度量差异诚实显现）
- **fullpage**：48.65%（R255 ua_default_display 修 4× 幻影盒 89.14%→48.65%）

### 测试覆盖率

- **cargo test**：13000+ 测试全部通过（`make test`：13190 passed / 0 failed / 74 ignored，截至 R2544；R2554 + R2563 周期复跑逐位确认；R2542/R2543 零测试增删故自 R2541 持平）
- **cargo clippy**：`cargo clippy --workspace --all-targets -D warnings` 通过
- **#[ignore] 测试**：74 个 ignored（real_website_compat.rs 等因本地网络不稳定的用例，不计入通过率）

---


## 历史轮次归档索引

> **为避免 master.md 无限膨胀，逐轮详记按 era 迁出 `archive/`**（内容 100% 保留，未去重、未重排）；master.md 现仅保留当前活跃状态 + 最近 5 轮摘要 + 跨会话架构入口。**2026-07-29 精简前的完整 master.md**（含 R2167–R2201 全部轮次详记 + 旧章节，零删减保底）→ [`archive/master-pre-slimdown-2026-07-29.md`](./archive/master-pre-slimdown-2026-07-29.md)。逐轮详记按 era 分档：
>
> - **R2167–R2197 + R2199c**（2026-07-29 瘦身迁出，含多行 blockquote 续行）→ [`archive/master-pre-slimdown-2026-07-29.md`](./archive/master-pre-slimdown-2026-07-29.md) 顶部轮次详记区
> - **R2201–R2458**（2026-08-04 R2561 瘦身迁出，顶部轮次详记区 258 轮 interleaved）→ [`archive/rounds-r2201-r2458-topblock-summary.md`](./archive/rounds-r2201-r2458-topblock-summary.md)
> - **R2459–R2495**（2026-08-04 R2555 瘦身迁出，§最近轮次摘要 底部 36 轮，含 R2486–R2495 plain-format 体）→ [`archive/rounds-r2459-r2495-summary.md`](./archive/rounds-r2459-r2495-summary.md)
> - **R2496–R2524**（2026-08-04 R2556 瘦身续迁，§最近轮次摘要 底部 29 轮全 blockquote 体）→ [`archive/rounds-r2496-r2524-summary.md`](./archive/rounds-r2496-r2524-summary.md)
> - **R2086–R2095**（reftest autonomous forward 穷尽调查）→ [`archive/rounds-r2086-r2095-plateau-exhaustion.md`](./archive/rounds-r2086-r2095-plateau-exhaustion.md)  
> - **R1651–R2025**（逐轮详记，~660 行）→ [`archive/rounds-r1651-r2025-accumulated-detail.md`](./archive/rounds-r1651-r2025-accumulated-detail.md)
> - **R1579–R1601**（font-wall plateau era）→ [`archive/rounds-r1579-r1601-fontwall-plateau-era.md`](./archive/rounds-r1579-r1601-fontwall-plateau-era.md)
> - **R1602–R1650**（lever scan / chrome127 era）→ [`archive/rounds-r1602-r1650-lever-scan-chrome127-era.md`](./archive/rounds-r1602-r1650-lever-scan-chrome127-era.md)
> - **R142–R302** → [`archive/rounds-r142-r302.md`](./archive/rounds-r142-r302.md)
> - **R894–R990**（multicol Phase 2 / harness JS vein / R109 §9.2.1.1 backfill / aspect-ratio / R990 ascent era）→ [`archive/rounds-r894-r990-multicol-harness-r109-aspect-era.md`](./archive/rounds-r894-r990-multicol-harness-r109-aspect-era.md)
> - **R991–R1093**（multicol spanner·Phase 2 / logical props / vertical-mode / FreeType C-dep / ::first-letter Phase A / nbsp·word-spacing / plateau era）→ [`archive/rounds-r991-r1093-multicol-spanner-freetype-firstletter-era.md`](./archive/rounds-r991-r1093-multicol-spanner-freetype-firstletter-era.md)
> - **R569–R881**（master preamble 摘要段）→ [`archive/rounds-r569-r881-master-preamble-summaries.md`](./archive/rounds-r569-r881-master-preamble-summaries.md)
>
> 更早 era（R11–R718）的归档清单见上述文件内链接。逐轮结论摘要亦见顶部「综合裁决」表。

