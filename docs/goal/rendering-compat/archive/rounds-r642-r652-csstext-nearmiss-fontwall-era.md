# 归档轮次 R642–R652 — css-text near-miss vein 收口 + font-wall advance-width 攻击 closed + text-autospace/measure-wordbreak REVERTED era

**归档日期**: 2026-06-26（doc-maintenance 轮迁出，R642–R652 超出最近 20 轮窗口）

**范围**: R642–R652（实际有逐轮详记者：R642/R643/R644/R645/R647/R651/R652，缺号轮次 R646/R648–R650 无独立详记）。下列逐轮详记从 master.md 100% 原样迁出，未修改。综合结论见 master.md「综合裁决」节（R592–R645 段 css-text near-miss vein + font-wall oracle blocker）与本归档顶部摘要。

---

**R652（text-autospace 特性实现·CORRECT 但 NET-NEUTRAL·REVERTED·零 landing）：承 R647（text-autospace 为最高潜在 yield feature）dedicated 实现 + A/B。**实现**（~110 行 11 文件，已 REVERT）：完整 text-autospace: normal（默认 no-autospace 控 blast radius）——css-parser TextAutospaceValue + parse；style-system（enum + PropertyId + registry initial NoAutospace + is_inherited + ComputedStyle field + default + apply + inherit×2）；layout-engine AutospaceCategory{Ideograph,Letter,Numeric,Other}+autospace_category+needs_autospace（Ideograph↔Letter/Numeric 双向）+ IFC.text_autospace + break_items_into_lines 跨 run/word 追踪 prev_autospace_cat 在边界词前加 0.125em×fs；接线 5 处 IFC 构造（compute_final + paint Path B + measure/remeasure×3 含 col_ctx）。**A/B**：text-autospace 簇 30/4 **BASE==WITH 完全相同（0 flip）**；css-text 全量 90==90（0 回归/0 win）。**实现证 CORRECT**（match 变体验证）：no-001#0（match，test `.normal` 自动间距 vs ref `.no-as` manual 0.125em margin）BASE 0.51%→WITH 0.54% 均 PASS = 0.125em 正确施加且匹配 ref。**但仍 0 flip**——mismatch 变体被阈值挡住：no-001#1（mismatch，test `.no-as` 无间距 vs mismatch-ref `.normal` 有间距）BASE 0.00%→WITH **0.20%**（间距生效但**< min_mismatch_ratio 0.5% 阈值→仍 FAIL**）；0.125em 在默认 16px 小文本（2px）仅产生 ~0.20% 像素差，结构性低于 0.5%。match 测试 break-001 8.50%/first-line-001 6.01% 远离阈值（须 break opportunities+精确 shaping/字体度量，非仅 advance）。**裁决 NET-NEUTRAL REVERT**（rally zero-yield 惯例 R502/R524/R651 + code-guidelines §2 不留零价值代码）。**防重投**：text-autospace 在当前 harness（0.5% mismatch 阈值 + 默认小字号）**不可 yield**，即使正确实现 0.125em + break opportunities——须大字号测试 / 降阈值（违 DC-14）/ match 测试降阈值内（多会话 Phase A），单 session 关闭。意外修复：恢复误删的 R376 stash。零 landing，详见 [`evidence/r652-text-autospace-net-neutral-reverted-2026-06-26.txt`](./evidence/r652-text-autospace-net-neutral-reverted-2026-06-26.txt)。

**R651（measure 路径 word_break/break_word 探针·NET-NEGATIVE·REVERTED·零 landing）：承 R645（measure 路径补 no_wrap = clean +7）同谱系假设——测量路径还忽略 word_break/break_word，若 break-all 容器被当 normal 测量致行数偏少 box 高度偏小，或为同类 clean lever。**探针**：扩展 R645 helper 为 IfcMeasureConfig { no_wrap, word_break, break_word }，measure_text_content + 2 remeasure（含 col_ctx）4 处补 .with_word_break()/.with_break_word()（与 store 路径一致解析）。**A/B（css-text + normal-flow，按测试名）**：css-text **1 REGRESSION / 0 WIN**（word-break-keep-all-011——keep-all 改 CJK 断行→box 高度变→破）/ normal-flow 0/0 = **NET -1 净负向**。代码已 git checkout REVERT。**关键裁决**：测量路径属性遗漏**非均匀 clean**——R645 no_wrap 是 clean（pre 不应换行是明确错误 + line-breaking-024~027 driving test），R651 word_break **非 clean**（测量用 normal vs 真 word_break 的差异无 driving test——paint 早正确设 word_break 渲染行数对，box 高度偏差不致可见失败，反破 keep-all）。后续勿以「测量路径遗漏属性 = R645 类 clean lever」笼统外推到 text_indent/tab_size/font overrides（同理须先确认 driving test）。本探针价值 = 排除假设 + 刻画 R645 clean 边界（no_wrap 特例非通用）。零 landing，详见 [`evidence/r651-measure-wordbreak-net-negative-2026-06-26.txt`](./evidence/r651-measure-wordbreak-net-negative-2026-06-26.txt)。

**R647（css-text near-miss 簇扫描收口评估·clean win 穷尽·零源码）：承 R639/R644/R645 三连 clean win，扫描全 css-text 0.00% FAIL mismatch 簇（12 case）逐簇评估。**结论：clean 单 session lever 已穷尽——剩余 12 case 全属三类，无一 clean：**(a) feature implementation**（text-autospace 4 测全未实现，但 29 测用 `normal` 现 30/34 PASS 因 ZW 忽略属性，实现 0.125em 间距 blast radius 大须逐测 A/B；-webkit-text-stroke 1 测全未实现；text-wrap-balance 精度；skip-ink-vertical 组合）；**(b) entangled whitespace/pipeline**（control-chars-00B VT / 085 NEL / 00C FF——R644 残留，探针证双重 entanglement：collapse_whitespace `ch.is_whitespace()` 折叠 + split_into_words `text.split_whitespace()` 也按 Rust 空白分割，单 VT 作整段 text 时 split 成 0 词永不抵达 paint R644 占位；CSS 仅认 TAB/LF/FF/CR/space 为空白但 Rust is_whitespace 含 VT/NEL/NBSP，修须改两处核心 + 影响 NBSP）；**(c) 须 pipeline trace**（object-replacement U+FFFC 2 测，0 宽贡献但 estimate_char_width 应给 0.5×fs，疑 rustybuzz/fontdue 或 combining mark 交互，须深 trace）。**最高潜在 yield** = text-autospace（advance 数学匹配 ref：0.125em×2 transition = manual margin 0.25em around Latin span，dedicated ~80 行多 crate + 29 测 A/B 预期 +2~4）。forward motion 选项：text-autospace feature / object-replacement trace / 方法论外推到 css-text-decor·writing-modes 等目录 0.00% 簇。零源码（read-only 评估 + 探针）。详见 [`evidence/r647-csstext-cluster-scan-exhausted-2026-06-26.txt`](./evidence/r647-csstext-cluster-scan-exhausted-2026-06-26.txt)。

**R645（SEA 词典分词文字 fallback line-breaking + IFC 测量路径 white-space 尊重·LANDED·css-text +7/0）：css-text near-miss 扫描第 3 个 clean win（继 R639/R644）。**目标簇**：line-breaking-024/025/026/027（Khmer/Lao/Thai/Burmese fallback 断行 mismatch 测，基线全 0.00% FAIL）。**根因双层**：① **缺 SEA fallback 断行**——split_into_words 把无空格 SEA 文本当 1 个不可拆单词（非 CJK），无断行机会 → 溢出单行；CSS Text 3 §line-break-details 要求需词典分词的文字 UA 无词典时**必须**做 fallback 断行（不允许溢出）。② **IFC 测量路径忽略 white-space（pre-existing bug，被①掩盖）**——measure_text_content（taffy MeasureFunc）+ 2 个 remeasure 函数构造 IFC 时从不传 no_wrap（恒 false），故 pre/nowrap 容器**测量高度**时被当可换行 → box content_height 偏大；该 bug 长期被「单 token 无法换行」掩盖，层①让每个 SEA 字符成独立断行点后才暴露（pre 容器被测成 8 行）。**修复**：① text_metrics.rs 新增 is_sea_word_script（Thai/Lao/Myanmar/Khmer 区块）+ is_per_char_break_script（CJK∪SEA），split_into_words 3 处 is_cjk_character→is_per_char_break_script（仅断行点，不影响 advance 宽度——SEA 非全角仍 0.5×fs）；② inline_finalization.rs 新增 resolve_no_wrap_for_ifc_measure，measure_text_content + 2 remeasure（含 balance col_ctx）4 处补 .with_no_wrap(no_wrap)。**关键 scoping**：仅传 no_wrap **不**传 preserve_whitespace——初版同传 preserve 致 letter-spacing-201（pre-wrap+多空格）测量行断变化回归；no_wrap 是修 pre/nowrap「测量不换行」最小充分条件，preserve 仅影响空白折叠非必要，测量路径保 preserve=false（与历史一致），空白保真由 paint 路径（text.rs:744）负责；scoped 后 letter-spacing-201 恢复 PASS。**验证 A/B vs HEAD（按测试名 strip %/#variant）**：css-text **+7/0**（line-breaking-024/025/026/027 + word-break-normal-002/003/th-001，后者 meta assert 明确「word-break:normal turns on SEA word boundaries... per character basis... lines should wrap」= 同源） / white-space 0 regression（1 win content-white-space-002；pre 类已失败用例仅 ±0.1% 抖动）/ normal-flow 0/0（blocks-018/019 ±0.3% 抖动仍 FAIL）；make test 全绿（+ 2 单测 test_r645_sea_word_script_classification / test_r645_sea_text_per_char_break）；product-smoke welcome 16.16% 持平；clippy -D warnings 干净（incidental 修 app_platform.rs:340 nonminimal_bool·parallel URL-resolution 遗留）+ fmt clean（incidental style-system drift）。**意义**：near-miss 簇扫描第 3 连胜（R639 +13/R644 +~56/R645 +7），0.00% mismatch 簇仍可持续 yield；层②是独立 pre-existing correctness 修复（pre/nowrap 容器 box 高度此前被系统性高估），为后续 pre 相关修复奠基。详见 [`evidence/r645-sea-fallback-linebreaking-landed-2026-06-26.txt`](./evidence/r645-sea-fallback-linebreaking-landed-2026-06-26.txt)。

**R644（Cc 控制字符可见性占位·LANDED commit bc512083·control-chars +~56 self-source）：css-text near-miss 扫描发现 control-chars-001..008+00B（9 显）至 control-chars-085（共 64 mismatch 测试）0.00% FAIL 簇。**根因**：CSS Text 3 §white-space-processing 要求 Cc 类控制字符（U+0000-001F、U+007F-009F，非空白）**必须可见**；但 fontdue 对 Cc 无字形（.notdef 空）→ ZW 渲染为空 → test == 空 ref（control-chars-000-ref 无 div 内容）→ mismatch FAIL（应 !=）。**修复**：render_fragment 宏 glyph loop 对 Cc 控制字符（is_cc_control_char helper，排除 TAB/LF/FF/CR 空白）渲染可见占位 em 方块 fill（font_size×font_size），使 test != 空 ref（diff > min_mismatch_ratio 0.5%；4em=64px 下 ~0.85%）。**验证**：control-chars **60/64 PASS（+~56 self-source）**，4 边界残留（000 NULL 0.38% 阈下、00B VT / 085 NEL 被空白折叠、00C FF 空白）；make test 全绿（+ 新单测 test_r644_cc_control_char_visible_placeholder）；product-smoke welcome 16.16% 持平；clippy/fmt clean。rebase 到并行 font-handling 工作（655d416a）之上无冲突。**意义**：继 R639（per-fragment inline bg +13）后又一 clean win，来自 css-text near-miss 扫描（R108 方法论 fresh 应用）——证明 plateau 下 near-miss 簇扫描仍可 yield。control-chars-000-ref 空（mismatch ref）使该簇对「可见性」修复高敏感。

**R643（fontdue-advance 字体墙攻击实测·确认 line-count 可修但 fontdue≠HarfBuzz 致 net-negative·方向关闭·零 landing）：承 R642（advance-width line-count 8v6 主因）实证测试 R642 候选——NotoSansCJK + fontdue 真实 advance。**实现**（env 门控，已 REVERT）：layout-engine estimate_char_width 加 thread_local NotoSansCJK fontdue Font + env FONTDUE_ADVANCE=1 用 fontdue advance（双侧：layout IFC + reftest paint fallback 均经此）；render-foundation build_font_resolver 加 env FONT_PRIMARY_CJK=1 把 sans-serif/serif/mono/system-ui → NotoSansCJK（glyph 切换，配套 advance）。**结果**：① **R642 假设确认**——FONTDUE_ADVANCE=1 使 text-indent-wrap **line-count 8→6 匹配 CHR**（PIL text-line 实测），z_vs_chr 13.91→11.38%（line-count 修复贡献 ~2.5pp）；② **但 net-negative**——advance-only（DejaVu glyphs + NotoSansCJK advance）welcome product-smoke **16.16→23.30%**（glyph/advance 不一致致文本错位）；combo（NotoSansCJK glyphs + advance）welcome **23.22%（≈ advance-only，未改善）**。**裁决根因**：**fontdue advance ≠ HarfBuzz advance**（metric 库差异）——fontdue 对 NotoSansCJK 的 advance 计算与 chromium 的 HarfBuzz 不同，致 ZW combo 换行仍偏离 CHR，反比 heuristic（0.55×fs 巧合近似 CHR）更差。**fontdue-advance 方向关闭**（R225/R375b DejaVu 负面先例扩展到 NotoSansCJK：fontdue 库本身的 advance ≠ HarfBuzz，与字体选择无关）。**结论**：advance-width 字体墙真修须 **rustybuzz（HarfBuzz-compatible）layout+paint 同源**（R331/R332 territory，paint-only rustybuzz 亦 net-negative 须 layout+paint 同源，多 session）；fontdue 仅可用于光栅化（R388 fontdue≈chromium 单 glyph），不可用于 advance/度量。零 landing（实现 + A/B + git checkout revert 3 文件 + Cargo.lock 重生成，工作树 clean）。

**R642（font-wall oracle blocker PIL 精细化：line-count 主因 + line-height + 首行偏移 + bg-gap 次因·零源码）：承 R641 PIL 分解 text-indent-wrap-001 的 ZW vs CHR 差异组件。**精确数据**：① **line-count 8 vs 6（主因，~6.2% = 2 额外行）**——ZW（DejaVu 宽）包 8 行，CHR（NotoSansCJK 窄）6 行，advance-width 差异（R225/R375b 谱系）；② **line-height 19 vs 18px**（次 ~1%）；③ **首行 y 偏移 ~10px**（ZW y=29 vs CHR y=19，strut/half-leading）；④ **bg 连续 vs 间隙**（R639 连续 full-line-height，CHR 内容区 per-line 带 half-leading 间隙，~1.1% 次因，R639 bg 非 oracle 主偏差源）。**裁决**：oracle blocker **确证 = advance-width font wall**（line-count 主因），非 R639 bg。真修须 **NotoSansCJK（CHR 同款字体）+ fontdue 真实 advance**（改 line-count）+ line-height（fontdue ascent+descent）= IFC 接 FontLoader Phase A。**区别 R225/R375b 负面先例**：彼用 DejaVu（错字体 advance≠CHR 致 worse/zero）；NotoSansCJK + fontdue advance **未测**（同字体 fontdue hmtx ≈ Skia HarfBuzz hmtx，Latin 无 shaping 差异）= 下 session 最高 ROI narrow slice。R631（NotoSansCJK + heuristic advance）零变化因 heuristic 字体无关不改 line-count；须**真实 advance**。零源码（PIL 像素分解）。
