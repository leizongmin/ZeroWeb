# 历史轮次归档 — R715（css-position revisit · top 全 out-of-scope/dynamic/structural · R711 inset percent-specific taffy 0.7 限制 disambig → R711 降级 complex/deferred era）

> 内容 100% 保留自主控文档 master.md（R715 块逐字迁出，仅作归档）。下文为该轮完整逐轮详记。

**R715（css-position revisit top 全 out-of-scope/dynamic/structural（唯 R689 clean lever）+ R711 inset minimal-repro disambig = percent-specific taffy 0.7 限制（LENGTH 相对 inset 工作 / PERCENT 不工作，definite CB 亦然）→ R711 降级 complex/deferred（非 clean lever）+ batch 归档 R694-R695 恢复 last-20·零源码）：承 R714 CONTINUE revisit css-position 次低案。`make reftest-oracle DIR=css-position`（97 案，read-only；**40.2% pass**）取 top-15：replaced-object-backdrop 100% / backdrop-inherit-rendered 47.54%（**backdrop-filter 完全未实现，out-of-scope**）/ position-absolute-semi-replaced-stretch-input 21.19%（**R688 form-control out-of-scope**）/ position-static-001 17.53%（**R689 已 harvest**）/ position-absolute-semi-replaced-stretch-other 13.66% / position-absolute-dynamic-relayout-005 11.84%（dynamic JS）/ position-absolute-in-inline-006 5.10% / position-relative-002/005 5% / change-insets-strict-containment 4.65% / hypothetical-dynamic 4.17%（JS）。查 **stretch-other（13.66%）** = 全 form-control（select/output/label/fieldset/textarea/progress/meter "semi-replaced"，csswg#6789 abspos stretch）→ **R688 family out-of-scope**（原生 form-control 渲染）。查 **in-inline-006（5.10%）** = `writing-mode:vertical-rl` + fragmented inline span + abspos CB（inset:0 green）→ **R114 axis-swap + R109 inline-fragmentation 结构性**。**★ 裁决：css-position top 全 out-of-scope（backdrop/form-control）+ dynamic（JS）+ structural（R114/R109），唯 R689 static-inset 为 clean lever（已 harvest）**——revisit 已采 top-level 目录次低案边际递减（印证 R701/R709 模式）。**续 R711 inset 簇 disambiguate（minimal repro，R713 方法论）**：R711（CSS2/positioning bottom-113 簇 ~8 案 15-47%）疑 relative 百分比 inset 未应用，须区分「percent-specific vs 通用」+「ZW vs taffy」。构造隔离 minimal repro：`#parent{height:200px}`（definite CB）> `.len{position:relative;top:50px}` + `.pct{position:relative;top:100%}`，LAYOUT_DUMP（占位 oracle 触发 render，temp 文件已删）。**dump（裁决：DECISIVE——LENGTH 工作 / PERCENT 不工作）**：`.len abs_y=58`（= parent 8 + **length offset 50 应用✓**）/ `.pct abs_y=48`（= parent 8 + in-flow 40，**percent offset 100%×200=200 未应用，应 248**）。**★ 裁决：R711 = percent-specific taffy 0.7 限制**——converter（convert_length_to_lpa:460）正确把 `top:100%` → `taffy::Percent(1.0)`；engine（engine.rs:275/459）依赖 taffy 把 relative inset 应用到 layout.location；taffy 0.7 **应用 LENGTH relative inset（inline-box-002 top:2in 双计证）但不应用 PERCENT relative inset**（definite CB height 200 亦然）。**非 ZW converter bug**（Percent 已正确产出），**非 clean ZW 一行 lever**。**R711 降级**：clean-ish lever（第 12）→ **complex/taffy-deferred**。ZW post-process 理论可行（仿 resolve_relative_inset:1597 扩展 Percent + 对所有 block-level relative 应用），但须 ① CB-height-definiteness cascade（%inset 对 indefinite CB height→0，类 R695 §10.5）；② **避 double-count**（taffy 已应用 LENGTH，ZW 须只补 PERCENT delta）——medium-high 复杂，非 quick win。cluster ~8 案（bottom/top-113/103/104/091/092）仅经 taffy 升级（R304）或 complex post-process 可 harvest。**★ 意义（双重）**：① **minimal repro 再证决定性**（R713 方法论续）——R711 疑似 clean lever 经隔离 repro 证伪为 taffy 限制，避免 code-agent 追 complex 当 clean；② **clean lever 队列精化**：12→11（R711 移出 clean，入 complex/deferred），code-agent 消费优先级更准。**★ batch 归档**：R694（css-grid 收口 STRUCTURAL）+ R695（height-percentage 88% clean lever，**lever 已在 pending-clean-levers.md Tier A**）迁出 → archive/rounds-r694-r695-...，master 恢复 last-20（R696-R715）。**hand-off code-agent**：R711 非 clean（complex/taffy-deferred），勿按 quick lever 消费；11 clean lever + R678 簇（≥5/3 目录）仍是主队列。**下一步**：revisit 边际递减，转 (a) fresh subdir（CSS2/visuren 或 backgrounds render-feature）/ (b) R692 oracle regen + img-load 等待 tooling / (c) 等 code-agent 消费 lever 后 A/B 复核。零源码（reftest-oracle read-only + LAYOUT_DUMP read-only + minimal repro temp 文件已删 + 代码 read-only 定位 convert_length_to_lpa:460 + engine.rs:275/459/1597 + resolve_relative_inset Px-only，未改任何文件；engine.rs WIP 是 table-cell overflow，对 position/relative inset no-op）。
