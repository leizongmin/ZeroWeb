# 归档：R668–R680（DC-14 独立 oracle 基础设施 + font-wall 诊断 + LAYOUT_DUMP 脉络起步）

> 从 master.md 迁出的 R668–R680 逐轮详记（按时间倒序，内容 100% 保留）。该区间建立当前调研基础设施：R669/R670 reftest-oracle 子命令（DC-14 独立 chromium-Oracle 真一致率测量）LANDED + VALIDATED；R671-R673 engine.rs:666 table-cell-overflow WIP 影响扫描；R674/R675 各布局目录 fresh 基线；R668/R676/R677 font-wall（rustybuzz x_advance）/font-size-adjust 诊断；R678-R680 LAYOUT_DUMP 脉络起步（css-tables 3 clean lever 候选 R678/R679/R680）。

---

**R680（LAYOUT_DUMP 第 3 案·table-cell-width-0 = R364 宽度已正确 + 真 `<br>` 行盒高度 bug·非 R678/R679 簇·非 R364·非 font-wall·零源码）：承 R679 hand-off 继续对 css-tables 第 2 大发散 table-cell-width-0（20.09%）做 LAYOUT_DUMP 抽查——hand-off 疑「R364 显式宽度列 vs 0-content shrink-to-fit 簇（R678 float+R679 table）」。方法：`LAYOUT_DUMP=1 make reftest-oracle DIR=table-cell-width-0`（read-only，test-guard 包裹；test 无 overflow→不受 engine.rs:666 WIP 影响=main HEAD honest 数）+ product-smoke 渲 ZW test PNG（同源 `render_to_framebuffer_with_base`）+ PIL 像素定位 + ASCII 体检。**test**：9 个 table（3 组×3 变体），cell 内容="1"/"2"（**非空**），`.zero{width:0}`/`.positive-width{width:2px}`/`.big-positive-width{width:20px}`，assert「width:0/2px cell 取 intrinsic 内容宽（min-content 作下限）」。**① R364 宽度处理正确（dump 实证）**：ZW `td.zero`(width:0) **w=9.6** / `td.positive-width`(width:2px) **w=9.6** / `td.big-positive-width`(width:20px) **w=20.0**——width:0 与 2px 均 floor 到 intrinsic min-content（"1" 字符 advance 9.6），20px 被尊重；table 宽（auto=23.2/percent=788/fixed=104）+ td.normal 填充（percent=774.4/fixed=90.4）亦正确。**裁决：R364（显式 cell 宽 floor 到 min-content）ZW 已正确实现**——width:0 cell 既非 0 也非满宽 784，取 intrinsic，与 R678/R679「0-content 取满宽」**根本不同**（内容非空 + 宽度正确）。**refute hand-off 双假设**：table-cell-width-0 **不属** R678/R679 簇，**亦非** R364 宽度 bug。**② 20.09% 发散根因 = `<br>` 行盒高度 bug（PIL+ASCII 精确定位）**：oracle diff **99.7% 集中在 y32-295 垂直带**，ZW=内容/边框 vs CHR=白（或反之）=**近全域垂直偏移**，非宽度差。量化：ZW 内容 y8-223（216px）/ CHR y8-295（288px），**CHR 栈高 72px**。逐组分解（br 数 × line-height 精确吻合）：**percent 组（0 个 br）ZW=70px = CHR=70px 完全一致**→table 行高本身正确（两侧 line-height ~19px+border 一致，无 font-wall 残留）；**auto 组（3 个 br）ZW=70px vs CHR=126px，CHR +56px ≈ 3×19px**；**fixed 组（1 个 br）ZW=71px vs CHR=91px，CHR +20px ≈ 1×19px**。**4 br × ~19px = 76px ≈ 实测 72px**——br 是唯一发散源。LAYOUT_DUMP 印证：每个 `<br>` box **h=0.0**，table 连续堆叠无间隙（y8/31/55…）= br **0 高度**。**机制**：CSS §9.2.1.1——inline（`<br>`）作 block 容器（body）直系子节点、与 block-level 兄弟（table）混排时应被**匿名块盒包裹**，匿名块 IFC 为 br 生成 line-height 行盒（chromium ~19px）；ZW **未生成**——br 作 h=0 inline 直入 flow（dump：br 为 body 直系子、与 table 同缩进、h=0、y=下一 table）。代码定位（read-only）：`inline/mod.rs:762` br→`InlineItem::Br`（IFC 内正确），但 **br-between-block-siblings 的匿名块行盒 strut 缺失**（R109 §9.2.1.1 territory）。**裁决**：table-cell-width-0 = **真 layout bug（br/inline-between-blocks 行盒高度）**，非 R364、非 R678/R679 簇、**非 font-wall**（两侧 line-height 值一致、行高本身正确，纯 br h=0 缺陷）。**refine R670「tables cell-width-0 = taffy-coupled (R168/R364)」泛判**——实证 R364 宽度已正确，发散是 br 高度（独立机制）。**hand-off code-agent**：修 br-between-block-siblings 匿名块行盒高度（§9.2.1.1）→ 本案 20.09%→~0%（PASS；量化：补 4 br × 19px 后 ZW 栈 292px ≈ CHR 288px，残余 <1%）；**但属 R109 匿名块 territory，须评 blast radius/回归**（其他依赖当前 h=0 br 行为的 case 可能受影响；br-between-blocks 模式常见，修后或惠及多 case 或致位移）。**意义**：LAYOUT_DUMP 方法论第 3 案——R678 float 真 bug + R679 table-width 真 bug + R680 br-height 真 bug / R364 宽度证正确，**3/3 命中可操作 layout bug**；R680 **同时 refute 2 个静态假设**（簇成员 + R364 宽度）并揭示第 3 独立机制（br 行盒），doc-maintainer 实证 dump 对「structural/cluster/taffy-coupled 泛判」的纠偏价值再证（cell-width-0 长期被判 R364/taffy-coupled，实为 br 高度 + R364 已正确）。零源码（LAYOUT_DUMP read-only + product-smoke 渲 PNG read-only + PIL/ASCII + 代码 read-only 定位，未改任何文件）。

**R679（LAYOUT_DUMP 方法论扩展·empty-table-height = 0-content display:table shrink-to-fit bug·与 R678 float 同「0-content shrink-to-fit」symptom 簇·零源码）：承 R678（LAYOUT_DUMP pin font-size-zero-3 = float 0-content shrink-to-fit bug）扩展方法论到 css-tables 头号发散 empty-table-height（30.74%）。`LAYOUT_DUMP=1 make reftest-oracle DIR=empty-table-height`（scoped read-only，不受 engine.rs:666 WIP 影响）+ test HTML 审查。**test**：`<div style="display:table; padding:155px; border:1px; background:green"></div>`（空 table，assert「empty tables account for paddings/borders」，Mozilla bug 1651530，match min-height-table-ref）。**ZW dump**：div（display:table）**w=784.0（满容器宽）** h=312.0（pt=155，高度 = padding 155×2 + border 2 = 312 **正确**）。**root cause**：`display:table` width:auto 应 **shrink-to-fit**（CSS 2.1 §17.5.2：table width = min(CB width, max-content)；空内容 max-content=0 → 应 ~padding+border ≈ 312 宽），但 ZW 给满宽 784 → green 多出 (784-312)×312 ≈ 147k px = **30.68% 发散（与 30.74% oracle 吻合，width 差异即发散源，height 正确）**。**★ 与 R678 同 symptom 簇**：float（font-size-zero-3）+ display:table（empty-table-height）均「**0-content shrink-to-fit 取满宽而非 ~content**」——可能共享根因（shrink-to-fit 计算 0-content 时 fallback 到 available-width）或独立（float 走 taffy+float_positioning.rs / table 走 table.rs R177/R364 sizing），**待 code-agent 定**。**裁决**：empty-table-height 是**真 layout bug**（display:table 0-content width shrink-to-fit 缺陷），**refines R670「tables empty-height taffy-coupled」泛判**——LAYOUT_DUMP 实证是 table width shrink-to-fit 缺陷（height 正确），非 taffy-noise；与 R678 组成「0-content shrink-to-fit」cluster = **2 个可操作 layout bug**（区别 font-wall dead-ends R676/R677）。**fix scope（hand-off code-agent）**：定位 0-content shrink-to-fit fallback——若共享根因（某处 `content==0 → full-width` fallback 或 `width.max(available)`），修一处或解两案；若独立，float path（R678）+ table width:auto path 分别修。**意义**：LAYOUT_DUMP 方法论连中 2 元（R678 float + R679 table），doc-maintainer 实证 dump 对「structural/font-wall/taffy-coupled 泛判」的纠偏价值再证——静态/聚合判「结构性」的高发散案值得逐案 LAYOUT_DUMP pin（部分实为可修 layout bug）。零源码（LAYOUT_DUMP read-only + test HTML + 代码 context read-only，未改任何文件）。

**R678（font-size-zero-3 根因 LAYOUT_DUMP pin·float 0-content shrink-to-fit bug·非 font-wall·真 layout bug·零源码）：R677 defer「font-size-zero-3 33.53% 须 render dump pin」。本轮 `LAYOUT_DUMP=1 make reftest-oracle DIR=font-size-zero-3`（scoped，read-only，test-guard 包裹，不受 engine.rs:666 WIP 影响 = 当前 main HEAD）dump 布局树。**结果（root cause PINNED）**：float div（`float:left; height:200px; font-size:0; background:red`）**w=784.0（满容器宽）**，应 shrink-to-fit 到 ~0（font-size:0 文本 intrinsic contribution = 0）；green square div w=200 正确。两 div 均 abs_y=51/h=200 → float（784 宽）覆盖 green（200 宽 x=8-208），**red 露出 x=208-792** = 33.50% 发散来源。**非 font-wall**（estimate_char_width 在 font_size=0 返回 0 已正确，R677 证），是 **float 宽度 sizing bug**：0-content float 取满宽而非 shrink-to-fit。**代码定位（read-only）**：`float_positioning.rs` 有 `shrink_vertical_blocks_to_content`（line 47，**仅垂直书写模式** §10.3.3）+ `shrink_inline_blocks_to_content`（line 111，**仅 inline-block**，R180），**无水平书写模式 float shrink 后处理**；line 40 comment 明言「taffy 把 width:auto 当作行内填充（填满容器宽度），违反规范」——水平 WM float 依赖 taffy，0-content 时 taffy 给满宽（784）而非 shrink-to-fit。**裁决**：font-size-zero-3 是**真 layout bug**（float 0-content shrink-to-fit 失败取满宽），非 font-wall、非 edge-case-noise——R677「须 dump pin」现 pin 为 float sizing 缺陷。**fix scope（hand-off code-agent）**：须加水平 WM float shrink-to-fit 后处理（R180 `shrink_inline_blocks` 模式扩展到 float，或 taffy 0-content float 测量 clamp）——可行性取决于 taffy float 测量能否 ZW 侧覆盖（taffy-coupling 待 code-agent 定）；若可 ZW 侧 post-process 则 **clean win 候选**（修 font-size-zero-3 + 可能其他 0-content float 案，区别 R676 font-size-adjust 多会话 / R677 font-family 匹配可用性）。**意义**：css-fonts 唯一非 font-wall 高发散案定位为可操作 float sizing bug；doc-maintainer LAYOUT_DUMP 方法论（R677 defer→R678 pin）价值实证——静态读断言「须 dump」的 case 用 `LAYOUT_DUMP=1 make reftest-oracle DIR=<case>` 一行可 pin。零源码（LAYOUT_DUMP read-only + 代码 read-only 定位，未改任何文件）。

**R677（css-fonts 残余高发散候选 triage·font-family-name-025 + font-size-zero-3 均非 clean win·css-fonts 根因穷尽·零源码）：承 R676（font-size-adjust = 未实现 feature，多会话 font-wall）triage R675 css-fonts 余下两高发散候选（非 font-size-adjust/font-feature 簇）。**read-only test HTML + 代码审查**：① **font-family-name-025（31.57%）**= `font-family: CSSTestBasic-Bold, Verdana-Bold, ahem, monospace` postscript-name 匹配测试（assert「postscript name should not match」，应 fallback ahem）+ 需 CSSTest 测试字体装好（verify span precondition）→ **font 匹配/可用性差异**，非渲染 bug，非 clean win（ZW font 匹配链 + 测试字体环境，narrow）；② **font-size-zero-3（33.53%）**= `float:left; height:200px; font-size:0; background:red` + lorem ipsum → assert「font-size:0 文本 intrinsic contribution = 0」（float 应 shrink-to-fit 到 0 宽，无红，仅 200×200 绿方块）。**静态审查**：`estimate_char_width(c, font_size=0, _)` 返回 **0**（text_metrics.rs:36/40 `font_size×factor`，ahem=font_size / 空格 0.25× / CJK 0.5× / Latin ~0.55×，font_size=0 全 → 0）+ advanced.rs:336 有「font_size=0.0 不 panic」测试 = **文本测量原则上正确处理 font-size:0**（0 宽贡献）；**但 oracle 仍 33.53% 发散** → 指向 **float shrink-to-fit / taffy MeasureFunc 路径未把 0 宽文本翻译成 0 宽 float**（疑似 float min-width 下限 或 taffy 测量路径 quirk，非 estimate_char_width 本身）→ **须 render dump（LAYOUT_DUMP 看 float computed width）pin**，本轮 defer。**裁决**：css-fonts 残余高发散候选**全非 clean win**——font-wall（font-size-adjust R676 / font-feature R330）/ font 匹配可用性（font-family-name-025）/ edge-case（font-size-zero-3，须 dump pin）。**R675 css-fonts -9.6pp 根因穷尽**：font-size-adjust 未实现 + font-feature 未接 + font 匹配/可用性 + font-size:0 edge = 全结构性/font-wall，无单 session clean lever，css-fonts 确认为 structural dir。零源码（read-only test HTML + grep + estimate_char_width 审查，未改任何文件）。

**R676（font-size-adjust 根因 read-only 定性·css-fonts 头号发散簇 = 完全未实现 feature·非 quick clean win（需扩 font 抽象 + 多 crate plumbing）·零源码）：承 R675（css-fonts fresh 基线 -9.6pp，font-size-adjust-009/010/011 51-53% 主导）定位该簇根因。**read-only 代码审计**（css-parser/style-system/layout/engine/registry 全 grep `font_size_adjust`/`FontSizeAdjust`）：**font-size-adjust 在 ZeroWeb 完全未实现**——无 parser value、无 style-system PropertyId/field、无 registry 注册、无 apply；唯一 `size_adjust` 命中是 layout-engine `apply_calc_size_adjustments`（calc() 尺寸调整，无关）。即 ZW **静默忽略** font-size-adjust（同 R652 text-autospace WIP 前态），css-fonts 头号发散簇 = **CSS-correctness feature gap**（非 fontdue 度量噪声）。**但非 quick clean win**——实现 scope 分析：① **两种语法**：`<number>`（ex-height 比，CSS Fonts 4 §9）+ 两值 `<metric> <number>`（ex-height/cap-height/ch-width/ic-width/ic-height，csswg-drafts #6160，011 实测用 `ic-width 1.0`）；② **font 抽象缺口**：ZW font 栈现仅暴露 `units_per_em()`（shaper.rs:93）+ `horizontal_line_metrics()`（loader.rs:229/307，ascent/descent/line_gap）+ `metrics(code_point,size)`（shaper.rs:253，per-glyph advance/bounds）——**无 x-height/cap-height**（需 OS/2 sxHeight/sCapHeight，fontdue API 不直接暴露，须 raw table 读或测 'x'/'X' glyph）；ic-width/height 理论可算（测 U+6C34 advance/height）但须 fallback 规则（字体不支持水→1em，011 注释明示）；③ **多 crate plumbing**：parser 加 value + 两值语法 + style-system PropertyId/field/apply + layout 用 adjusted_size（adjusted = number×specified / font_metric_ratio）+ font 抽象扩 x-height/cap-height/ic 度量。**裁决**：font-size-adjust 是真实 feature gap（R675 css-fonts 头号发散根因），但**非单 session clean win**——须扩 font 抽象（x-height/cap-height，font-wall 耦合）+ 多 crate plumbing，属 font-wall/Phase A 多会话谱系（R388/R633/R643），与 font-feature-resolution（rustybuzz 未接 R330）同列 css-fonts 结构性 backlog。**意义**：R675 css-fonts -9.6pp 根因精确化（font-size-adjust 未实现 + font-feature 未接，非可单 session 修）；forward motion 结论不变（结构性 plateau，css-fonts 真瓶颈 = font 抽象/度量限制 + feature 未实现，多会话）；另注 css-fonts 残余可独立查 candidate = font-family-name-025 31.57% / font-size-zero-3 33.53%（非 font-size-adjust，未本轮深查）。零源码（read-only 代码 grep + test HTML 审查 + fontdue API 审计，未改任何文件）。

**R675（css-text-decor + css-fonts chromium-Oracle 基线 fresh 复测·两 dir stale 基线均下行·★css-fonts font-size-adjust 簇新现（ex/ic 度量）·零源码）：续 R674（multicol fresh）刷新 master.md stale 基线。本轮 `make reftest-oracle DIR=css-text-decor` + `DIR=css-fonts`（read-only，test-guard 包裹；两 dir 无 table cell → 不受并行 agent engine.rs:666 WIP 影响 = 当前 main HEAD honest 数）。**结果（两 dir 均较 stale 基线下行，提示 master.md aggregate chr<1% 36.2% 偏乐观）**：① **css-text-decor 60/242=24.8% chr<1%**（vs R412 70/242=28.9%，**-4pp/-10 案**；8 案 no-oracle skip）——下行归因 font/metric 耦合（R630/R632 行盒度量 honestization 让 text-decoration/emphasis 定位发散显现）+ text-emphasis line-box 死锁（R392/R393 Phase A）；top 发散：text-decoration-inset-025 20.39% / -dotted-001/002 16.58/15.42% / -thickness-length-rounding-001/002/min-val 13.5-13.9%（R655 谱系）/ text-decoration-line 11.49% / text-emphasis-punctuation/position/style 6-10%（R392/R393）/ text-underline-offset 7.78%；② **css-fonts 71/282=25.2% chr<1%**（vs R421 98/282=34.8%，**-9.6pp/-27 案 显著下行**；5 案 no-oracle skip）——★ **font-size-adjust 簇新现主导**：font-size-adjust-009/010/011 **51-53%**（用 ex-height/ic-height 度量计算 adjusted size，fontdue ex/ic 度量 ≠ chromium 致大发散）/ font-size-adjust-order/-012/-013/-ic-height 12-18% / font-feature-resolution-001/002 10-13%（rustybuzz 未接生产 R330）/ font-family-name-025 31.57% / font-size-zero-3 33.53% / standard-font-family-2/7 10-12%。**裁决**：两 dir fresh 基线确认结构性 plateau + 揭示 stale 基线偏乐观（text-decor -4pp / fonts -9.6pp）；css-fonts 真瓶颈 = **font-size-adjust（ex/ic 度量，fontdue 限制）** + font-feature（rustybuzz 未接），均多会话 font-wall/Phase A 谱系（R388/R633/R643），非单 session clean lever。**意义**：master.md aggregate chr<1% 36.2%/36.4% 含 stale 高估（text-decor/fonts 下行 + R674 multicol 持平），真聚合或更低；R669 reftest-oracle 续用刷新第 6/7 dir（R670 4 dir + R674 multicol + 本轮 2 dir = 7 dir fresh）。零源码（make reftest-oracle read-only，未改任何文件）。

**R674（css-multicol chromium-Oracle 基线 fresh 复测·R427 23.5%→22.1% 持平 jitter·top 发散全结构性确认 plateau·零源码）：master.md 基线 multicol oracle（R427 106/451=23.5%）是 R669 reftest-oracle 落地前的旧口径，本轮用 `make reftest-oracle DIR=css-multicol`（452 案全扫，test-guard 包裹，read-only）fresh 复测（multicol 无 table cell → 不受并行 agent engine.rs:666 WIP 影响 = 当前 main HEAD honest 数）。**结果：100/452=22.1% chr<1%**（vs R427 23.5%，-1.4pp/-6 案 = jitter 级，flat）。**top 发散全 pre-existing 结构性**（无新模式，确认 multicol structural plateau）：column-balancing-paged-001-print 81.20%（平衡+paged，R128/R131）/ multicol-rule-nested-balancing-004/003/002 37.67/20.10/16.15%（嵌套 balancing，R112/R113 fragmentation）/ multicol-span-all-children-height-004a/003/004b/006/007 28-30%（column-span:all 子高度）/ multicol-breaking-005 23.48%（嵌套 breaking）/ multicol-contained-absolute 16.33%（abspos in multicol）/ multicol-overflow-clip-positioned 16.00%。**裁决**：multicol 基线刷新（23.5%→22.1% flat），structural plateau 再确认——forward motion 仍 = Phase 2 嵌套 multicol fragmentation / column-aware IFC（master.md 已记），无单 session clean lever（与 R305–R354 / R112 / R113 一致）。**意义**：R669 reftest-oracle 一等工具续用（R670 4 dir 后第 5 dir fresh 基线）；master.md 其余 dir（text-decor/fonts/writing-modes/css-text/CSS2）oracle 仍 stale，待续轮 fresh 复测。零源码（make reftest-oracle read-only 全 multicol 扫描，未改任何文件）。

**R673（engine.rs:666 WIP 全 css-tables 回归扫描·aggregate 47.0% 持平零回归·safe-to-commit 实证·零源码）：R672 确证 engine.rs:666 把 table-cell-overflow-explicit-height-001/002 从 41.53%→5.59%（partial win，仍 FAIL），但该改是**全表 cell** overflow:hidden/clip→Visible 旧 special-case 移除（非仅 001/002），有**潜在回归面**（其他依赖旧行为的 css-tables 案可能被破）。本轮 `make reftest-oracle DIR=css-tables`（115 案全扫，test-guard 包裹，工作树 as-is = 含并行 agent 未提交 engine.rs:666）做回归扫描。**结果：零回归，safe-to-commit 实证**：① **aggregate 54/115=47.0% chr<1% = 与 R670 baseline 完全持平**——strict 通过数不变，且 001/002 虽改善但 5.59% 仍 >1% 未 fail→pass，故 54 不变 = **必无 pass→fail 抵消**（decisive no-regression at strict level）；② **001/002 现 5.59%**，跌出 top 发散头部（top-15 排名 12-13，自 R670 headline 41.53% 显著下降，再确认 R672）；③ **-auto/-auto-scrolled（overflow:auto）不在 top-15**（passing），证实 engine.rs:666 仅触 hidden/clip 映射，auto/scroll 旧新一致；④ **top 发散全 pre-existing 结构性**：empty-table-height 30.74% / table-cell-width-0 20.09% / percent-height-overflow-auto-in-...cell.tentative 17.40%（overflow:auto 案不被 engine.rs:666 触，结构性 percent-height）/ anonymous-table-cell-margin-collapsing 16.53% / percent-height-replaced-...-003 14.95% / percentages-grandchildren-quirks-mode-001/002 14.95% / baseline-vertical 12.45% / table-row-group-color-inheritance-001 9.10% / dynamic-table-cell-height 7.99% / visibility-collapse-border-spacing-002 7.07%——**无一新出现**。**裁决（hand-off 给 code-agent）**：engine.rs:666 **safe partial win，可 commit**——aggregate 零回归 + 001/002 实质改善（41.53%→5.59%）+ 无新发散；残余 5.59%（R672）是独立 follow-up（非 commit 阻塞），需 cell-height/clip 矩形精确化才达 <1% strict pass（届时 css-tables aggregate 47.0%→~48.7%）。**学方法**：chr<1% strict pass-count 持平 = decisive no-regression 判据（改善案若未越 1% 阈值不计 fail→pass，count 不变必无 pass→fail 抵消）；overflow-mapping 类 spec-shift 改动（hidden 旧→新）须扫全目录确认 blast radius，勿仅看 driving case。零源码（make reftest-oracle read-only 全 css-tables 扫描，未改任何文件）。

**R671（table-cell-overflow 修复范围 read-only 定性·pin R670「entangled」3 点坐标·确认并行 code-agent engine.rs:666 单点改 necessary-but-insufficient·零源码·未测并行 agent 未提交代码）：R670 用 reftest-oracle 抓 table-cell-overflow-explicit-height-001/002 false-pass（self test==ref 假通过，oracle 41.53% FAIL：chromium clip 到 explicit 20px=blue 21340px vs ZW 长到 content 300px=blue 116614px），定性为「entangled」并 defer。**★ 本轮追踪到并行 code-agent 正在攻该任务**：工作树有未提交 `engine.rs:666-685` 改动（移除 table-cell 特殊 overflow 映射——旧 `Hidden|Clip→Visible` 旧 CSS 2.1 行为→改为 cell 与非 cell 同一 `convert_overflow_to_clip` 映射，即现代 Mozilla bug 1880550 clip 行为；comment 引 csswg-drafts）。**read-only 代码追踪确认这是 R670「多点协调」3 点中的第 1 点，单独 insufficient**：① **engine.rs:666 overflow 映射**（并行 agent 已改）——使 `overflow:hidden` 在 table cell 上真正映射为 `Clip` 而非 `Visible`，**necessary**；② **table.rs:1897-1916 cell 高度 finalization**（未改）——`cell_height = row_height.max(cell_content_height)`（line 1907）**无条件增长到 content**（300px），comment 明言「height 视为最小高度…必须增长含内容，不能裁剪到明确高度」+ line 1909 comment「确保 overflow 裁剪使用增长后的高度」= 当前设计**刻意 clip-to-grown-height**；故 engine.rs 改后 overflow:hidden 的 clip 目标仍是 grown 300px（content 恰填满→no-op），**非 chromium 的 clip-to-explicit-20px**；要 match chromium 须在 cell 有 explicit height + overflow≠visible 时**honour explicit height 作 clip 上限**而非增长；③ **table.rs:1769-1787 row-height 分布**（未改）——`row_height = row_height.max(cell_box.height)`（line 1780）聚合各 cell 高度，若某 cell 被 clip 到 20px 但他 cell content 更高，被 clip cell 的 300px content **不应**推高行——即 clip 须**先于** row 聚合发生，而当前 finalization（1907）**晚于** row 聚合（1780）= R670「chicken-and-egg」ordering 根。**裁决**：engine.rs:666 单点改是 R670「3 点协调」的第 1 点（necessary），但单独不 yield chromium 一致——table.rs cell-height finalization + row-height 分布 2 点须配套（正是 R670 defer 的 entangled 核心）。**method note（诚实）**：本轮**未运行**并行 agent 未提交代码（避免测 in-flight WIP + 不干扰 code-agent）；结论为 read-only 静态代码追踪（line 引用已核），empirical 验证（reftest-oracle DIR=css-tables 看 001/002 z_vs_chr 是否 41.53%→低）须待并行 agent 完成完整 3 点修复后由其或后续轮跑。**对 code-agent 的 hand-off**：若仅落 engine.rs:666，css-tables oracle 对 001/002 预计仍 ~41% FAIL（clip-to-grown-height no-op）；完整修复须 touch table.rs:1907（conditional honour explicit height when overflow≠visible）+ table.rs:1780 ordering（finalization 前置于 row 聚合，或 row 聚合用 clipped height）。零源码（read-only 代码追踪 + git diff，未 commit 任何文件）。

**R672（R671 预测 empirical 复验·engine.rs:666 单点改非 no-op 是大幅 partial win（41.53%→5.59%）但未达 pass·纠正 R671「no-op」过悲观预测·零源码·make reftest-oracle 实测）：R671 据静态代码读断言「并行 code-agent 的 engine.rs:666 overflow 映射单点改 alone 是 no-op（table.rs:1907 grow-to-content 致 clip-to-grown-height 无效）」。本轮用 R669 reftest-oracle 实测验证该预测。**方法**：`make reftest-oracle DIR=table-cell-overflow-explicit-height`（test-guard 包裹，substring 过滤命中 001/002 两案；跑在工作树 as-is = 含并行 agent 未提交 engine.rs:666 改动，未 stash/revert 其 WIP——不触碰并行 agent 代码）。**结果（ decisive REFUTATION of R671 no-op 预测）**：001 z_vs_chr **5.59%** / 002 z_vs_chr **5.59%**（R670 baseline 两者均 41.53%；R670→本轮唯一 code delta = engine.rs:666，故 41.53%→5.59% 全归 engine.rs:666）。即 engine.rs:666 **非 no-op，是大幅 partial win**——ZW 现 clip（match chromium 现代 bug 1880550 行为），blue 发散从「ZW 300px vs chr 20px clip」的大差距降到 5.59% 残余。**仍 FAIL**（5.59% > 1% strict pass_ratio，0/2 oracle-pass）。**R671 静态读需修订**：实证 clip 生效（否则应 ~41%），故 table.rs:1907「grow-to-content」在此案**未阻止有效 clip**——或 table.rs:1907 在此不增长（.tall div 子高度未被计为 cell_content_height，或 row_height 用 explicit 20px），或 render/paint clip 路径用 explicit height 而非 grown layout-box height；R671「clip-to-grown-height no-op」机制判断**实证不成立**，精确残余机制（height/border/text-clip 何致 5.59%）defer 给 code-agent。**hand-off 给 code-agent（纠正 R671）**：engine.rs:666 是真改善（41.53%→5.59%），**剩余工作 = 残余 5.59%（小，非 R671 暗示的 41% 级）**，无需 R671 过度主张的「3 点 table.rs 大手术」全部——可能仅需 cell height 精确化或 clip 矩形微调即可达 <1% pass。**学方法**：静态代码读的「load-bearing 机制」断言（如「1907 必然 grow 致 no-op」）须 empirical 复验，reftest-oracle（R669）现提供该验证路径——勿仅凭静态读下「no-op/insufficient」severity 判定。零源码（make reftest-oracle read-only 实测，未改任何文件）。

**R670（reftest-oracle 工具 VALIDATED·抓 table-cell-overflow false-pass + top-divergence 全结构性/entangled 定性·read-only）：用 R669 reftest-oracle 扫 4 布局目录（grid 32.7% / position 40.2% / tables 47.0% / flexbox 53.5%，与 R626 fresh-oracle 一致，数值可信）。**★ 工具 VALIDATED**：成功抓 **table-cell-overflow-explicit-height-001/002 false-pass**——self-source test==ref PASS（ZW-test blue 116614px ≈ ZW-ref 116828px，均渲染 300px blue div）但 oracle 41.53% FAIL（chromium blue 21340px = clipped to 20px），即 self-source 假通过、oracle 揭真失败——**reftest-oracle 抓到 self-source 漏掉的假通过，DC-14 anti-false-pass 价值实证**。**根因**：`table.rs:1880/1897-1907` 刻意遵循 CSS 2.1 旧行为（comment「overflow:hidden cell 仍须增长含内容」），chromium 用现代行为（Mozilla bug 1880550：clip）；master.md 2026-06-08「cell explicit height 保留」曾修但**仅 self-source match（假通过），未真修到 chromium 一致**（taffy 仍长到 300px content）。**fix entangled**：overflow_y 可访问（engine.rs:683-691），explicit height 可读，但 cell 高度 finalization（1907）与 row-height 分布（1780 用 content-based cell height）chicken-and-egg，clip cell 须先于 row-height 计算→多点协调，**defer 为 characterized table-layout 任务**。**其他 top-divergence 全结构性**：flex-minimum-width-flex-items-013（82%最高）= aspect-ratio-coupled min-size（taffy-blocked，csswg #5663）/ position backdrop 100%+47% = backdrop-filter 未实现（R663）/ tables empty-height+cell-width-0 = taffy-coupled（R168/R364）/ flexbox+grid baseline-synthesized = baseline-export 簇（R266）。**结论**：reftest-oracle 从 oracle 角度再确认 plateau——高 z_vs_chr 全结构性/entangled，无可单 session clean 修（与 master.md 文档一致）。详见 [`evidence/r670-...txt`](./evidence/r670-reftest-oracle-validation-2026-06-26.txt)。零源码（扫描 + PIL 根因 + 代码 read-only 定性）。 **doc-maintainer 配套（本轮）**：据 R669 落地 + R670 验证，纠正 DC-14 独立 Oracle 相关 stale 声明——rendering-compat.md DC-14 checklist「独立 Oracle」项 ✅(R669) 勾选 + line 20 注 `make reftest-oracle` 一等指标；master.md 综合裁决「真实剩余工作 (b)」+ R667 条目「DC-14 核心未解项」改标 R669 已解（DC-14 仍 open = 严格容差三态分类 / 非平凡性检查 / 容差锁定）。零源码（read-only 代码审计 + doc 纠正）。

**R669（DC-14 独立 Oracle 基础设施·reftest-oracle 子命令·chromium-Oracle 真一致率作 first-class runner 输出·LANDED）：DC-14 line 341 要求 reftest 参考基准为 chromium（非 ZeroWeb self-ref 的 46.5% 假通过）；此前 chromium Oracle 仅 cross-validate.py 事后分析，非 runner first-class 输出。**交付**：`zero-wpt-runner reftest-oracle [filter]` 子命令（main.rs `cmd_reftest_oracle`）——渲染上游 WPT test 页（`render_to_framebuffer_with_base`）→ 与 `oracle-shots/{safe_id}.png` chromium Oracle 对比（`compare_pixels`）→ 报告 **chromium-Oracle 真一致率**（z_vs_chr < `ORACLE_PASS_RATIO` 默认 1%）+ top-15 最不一致案（修复候选）+ 按目录聚合。env：`ORACLE_DIR`（默认 tests/wpt-runner/oracle-shots）/ `ORACLE_PASS_RATIO`。**验证**（css-grid 49 案 with oracle）：chromium-Oracle **16/49 = 33%**（cf. self-source grid ~66.7%——33% 真 vs 66.7% 同源，差距 = 假通过/污染）；top worst = grid-container-baseline-synthesized-001/003 **78.69%**（baseline-export 簇 R266 territory）+ replaced-element-percentage-height 34% + table-grid-item-dynamic-003 23.8%（与 master.md 已知 pollution 一致，验证工具正确）。make test 全绿（exit 0）+ clippy -D warnings 干净（零既有行为变更，纯新增子命令）。**意义**：DC-14 独立 Oracle 从「cross-validate 事后 python 脚本」升级为「runner first-class 子命令」——`reftest-oracle <dir>` 一行即可得某目录 chromium 真一致率，是 DC-14 anti-false-pass 门禁的可复用工具（未完全达标：`make reftest` 默认仍 self-ref，但 oracle 指标现 first-class 可查）。零既有行为变更。

**R668（font-wall 去风险·rustybuzz x_advance Phase A 诊断·「rustybuzz same-source」假设 REFUTED·省多 session 投入·零生产影响）：承 R660（rustybuzz same-source 是唯一未测 font-wall 路径）+ R643（fontdue advance net-negative，留「rustybuzz 同源 = 真修」未测）。**诊断**（`crates/render-foundation/examples/rustybuzz_xadvance_diag.rs`，零生产影响 examples 不入 make test）：对 text-indent-wrap-001（R642: ZW=8/CHR=6 行）三路测 advance——① rustybuzz `glyph_positions.x_advance`（真 HarfBuzz 含 GPOS）② fontdue `metrics(char)`（R643 方法）③ heuristic，各 greedy-wrap line-count。**结果**：rustybuzz **5120.8px→7 行** / fontdue **5146.0px→7 行**（两者差 0.5%）/ heuristic 5875.2px→8 行 / **chromium=6 行**。★**裁决：Phase A rustybuzz same-source REFUTED**——rustybuzz（HarfBuzz）与 fontdue 对同一 NotoSansCJK 给出**几乎相同** Latin advance（无 ligature/kerning 时 HarfBuzz≈fontdue 读同 hmtx），line-count **同为 7 ≠ CHR 6**。换 heuristic→rustybuzz **不比换 fontdue 更优**（fontdue 已 R643 证 net-negative → rustybuzz 同源亦 net-negative）。**纠正 R643/R660 留的「rustybuzz same-source = 唯一未测 font-wall 解法」框架——已测，不成立**。**font-wall 真因 = font-metric/字体版本对齐（非 shaper）**：rustybuzz/fontdue 一致读 NotoSansCJK hmtx 得 5120-5146px，chromium 更少（→6 行），差异在字体版本/hinting/子像素/wrap 精度，非 shaper 选择。font-wall 单 session 杠杆四角度（advance-source R643/R668 + baseline R633 + font-choice R631 + 本诊断）全测穷尽，**勿再以 rustybuzz 同源为 font-wall 解法投入多 session**。font-wall forward motion 重新定性为 font-stack 对齐（降优先级，残余 diff 可接受）。clippy --all-targets 干净。详见 [`evidence/r667-...txt`](./evidence/r668-rustybuzz-xadvance-phaseA-refuted-2026-06-26.txt)。
