# 归档：R696（CSS2/normal-flow · inline-replaced-width unified sizing lever）

> 从 master.md 迁出的 R696 逐轮详记（内容 100% 保留）。R696 = inline-replaced-width 簇 sub-group 1（★UNIFIED CLEAN-ISH LEVER，svg/canvas/object/iframe 标 is_replaced 却无 apply_replaced_element_sizing sizing，tree.rs:186 仅 img——**详情在 [`pending-clean-levers.md`](../pending-clean-levers.md) Tier A**）。R716 起与 R694–R695 同 normal-flow era，独立成档。

---

**R696（CSS2_normal-flow 续·inline-replaced-width 簇 = MIX·sub-group 1（008/009/ib-007/ib-008，svg 无 intrinsic→§10.3.2 默认 300×150）= ★UNIFIED CLEAN-ISH LEVER·apply_replaced_element_sizing 仅处理 img（tree.rs:186）致 svg/canvas(R683)/object/iframe 标 is_replaced 却无 sizing·第 8 lever·第 2 unified 簇·零源码）：承 R695 CONTINUE 探 CSS2_normal-flow top 的 inline-replaced-width 簇（007/008/009/012/013 + inline-block-replaced-width-007/008，12-22%，~5-7 案），判统一根因（类 css-tables table-width）还是 mix（类 R685/R686 aspect 簇）。**cluster sizing（读 7 案 test setup）**：簇实为 **2 sub-group MIX**——① **sub-group 1（008/009/ib-007/ib-008，4 案）**= inline `<svg>`（无 width attr、无 viewBox→无 intrinsic width/ratio）+ CSS `svg{display:inline}`，assert「replaced 元素无 intrinsic width 且无 intrinsic ratio → used width = 300px」（CSS §10.3.2 默认 object size）；② **sub-group 2（012/013）**= `<img>` width:% / px 围绕 float 排版（float wrapping，不同机制，本轮未深查）。选 sub-group 1 最高 **inline-replaced-width-009（22.08%）** LAYOUT_DUMP。**预检**：无 script✓/无 overflow✓（009 用 inline `<svg>` 非 `<img>`→oracle validity 验证 nonwhite=91845≈300×300 绿方+文本=valid）。**test**（Gérard Talbot，§10.3.2）：`div(600×300)` 内 `<svg:svg height="300">`（无 width attr、无 viewBox）含 red rect + `div#overlapping-green(300×300, position:relative bottom:300)` 覆盖，assert「大绿方块无红」（svg 无 intrinsic → width 默认 300px、height attr=300→svg 300×300，green 300×300 覆盖 red）。**dump（裁决：svg 被当非 replaced sizing）**：div h=300 w=600✓ / **`svg:svg` h=0 w=600**（应 300×300）/ svg:rect h=0 / div#overlapping-green abs_y=**-249**（position:relative bottom:300 上移，因 svg h=0 故 green 被推至屏幕外）。**PIL**：ZW nonwhite=16162 bbox(8,0)-(477,51)（**仅顶部文本**——svg h=0 + green off-screen）vs CHR nonwhite=91845 bbox(8,18)-(381,350)（300×300 绿方）；22.08% = ZW 几乎空白 vs CHR 绿方。**★ 根因（代码 read-only 精确界定 + 推翻初判「structural」）**：初看「inline SVG 渲染支持缺」似 structural，但 **engine.rs:646 把 `"img"|"video"|"iframe"|"embed"|"object"|"svg"|"canvas"` 全标 `is_replaced=true`**——svg **已**被当 replaced！缺陷在 `apply_replaced_element_sizing`（tree.rs:165）**line 186 `if tag != "img" { return }`**：仅 img 走 §10.3.2 sizing（读 width/height attr + intrinsic + 默认），**svg/canvas/object/iframe/video 虽 is_replaced=true 却 early-return 无 sizing**→taffy 当普通元素（w=block-stretch 到容器 600，h=content=0）。**裁决：clean-ish lever（非 structural SVG 渲染）**——svg 的 BOX sizing 缺陷，非内容渲染缺陷（即使 ZW 不渲 svg red 内容，修 box→300×300 后 green div 覆盖即 reftest PASS）。**★ UNIFIES R683 + sub-group 1 为单一根因**：R683（canvas 1×1 intrinsic 被忽略→满宽）+ inline-replaced-width-008/009/ib-007/ib-008（svg 无 intrinsic→应默认 300×150/300×300）**同根 = tree.rs:186 仅 img**——canvas/svg 均在 is_replaced 列表却无 apply_replaced_element_sizing sizing。**fix scope（hand-off code-agent，unified cluster lever）**：扩展 apply_replaced_element_sizing 超越 img——对 svg/canvas/object/iframe/video：① 读 width/height HTML attr（honour）；② 无 intrinsic width 且无 intrinsic ratio 时 used width 默认 **300px**（§10.3.2），无 intrinsic height 时默认 **150px**；③ canvas/img 已有 intrinsic 时按 intrinsic+ratio（R683 canvas 1×1 / R325 img）。**复杂度中等**（§10.3.2 多步算法：auto/ratio/intrinsic/default 组合），但 **code-located（tree.rs:186 一处）+ 簇 yield（R683 + 4 svg 案 ≥5 案）+ spec-correct**；blast radius 须 A/B（现有 img sizing 不变，新增 svg/canvas 路径）。**★ 意义**：① CSS2_normal-flow 第 2 案——R695（88% %height）+ R696（replaced sizing unified）；fresh pivot 双命中；② **LAYOUT_DUMP 脉络第 8 clean lever，且第 2 个 unified 簇 lever**（继 css-tables table-width R679/R681/R682 后，R696 unify R683+sub-group-1≥5 案）——「先按 static label 猜 structural，code 追踪常揭 clean 统一根因」方法论再证（初判 SVG structural→code 揭 tree.rs:186 unified sizing lever）；③ apply_replaced_element_sizing「仅 img」是 replaced 元素 sizing 系统性缺口，修之或惠及全语料 svg/canvas/object/iframe 案。hand-off code-agent：**tree.rs:186 扩展 replaced sizing 到 svg/canvas 是 unified 高价值 lever（≥5 案，§10.3.2）**。sub-group 2（012/013，img+float wrapping）独立机制待续轮。零源码（cluster sizing grep read-only + LAYOUT_DUMP read-only + product-smoke 渲 PNG read-only + PIL + 代码 read-only 定位 tree.rs:186 + engine.rs:646，未改任何文件）。
