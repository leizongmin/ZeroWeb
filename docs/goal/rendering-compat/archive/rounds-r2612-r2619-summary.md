# 轮次详记归档 — R2612-R2619

> 来源：master.md §最近轮次摘要，于 R2627 doc-control 瘦身时按 fifo 迁出。
> 范围：R2619 → R2612，共 8 轮（R2619/R2618/R2617/R2616/R2615/R2614/R2613/R2612）。
> 保留方式：100% 逐字保留，未去重、未重排（newest-first，与 master.md 原 file-order 一致）。
> 更早 era 归档见 master.md §历史轮次归档索引 与 archive/ 目录。

> **📍 R2619（2026-08-04）🔧 design-doc 行号漂移续修（R2202 模式·multicol cluster 收尾）：multicol-fragmentation-design.md（13 引用，最后一个 multicol design doc）8 处 drift pattern 纠偏（13 引用实例），全 precise 命中当前源码；multicol cluster 3/3 design docs 行号全 clean；纯 .md 零 .rs 零回归**。承接 R2618（column-aware-IFC 行号纠偏）。本轮收尾 multicol cluster 第 3 个 design doc（user-gated Phase 2 multicol 碎片化设计）。**核验**：自 R2597 起零 .rs 变更，drift 全为 pre-R2597 累积（painter/text.rs 高 drift 区 + text_multicol.rs 拆分）。**8 处 drift 纠偏**（13 引用实例）：① `text.rs:713`→`:569`（2×）+ `text.rs:711`→`:569`（5× 含 painter/ 全路径）+ `painter/text.rs:710-715`→`:560-569`（1×）= multicol paint gate `!has_in_flow_children && is_balance_mode && height_auto` 实在 :569（组件 :560-568）；② `painter/text.rs:948`→`:854`（target_h，1×）+ `painter/text.rs:948-984`→`:853-882`（列分配+裁剪，1×）= balance 均高分配实在 :853-868（target_h :854 / floor :860-868 / clip :881）；③ `text.rs:974-984`→`:881`（逐列裁剪 clip_rect，:974-984 实为 glyph 字段）；④ `painter/text.rs:185-197`→`text_multicol.rs:130`（column-rule §5.2 内容检测 = paint_column_rules，:185-197 实为 text 路径 early-return guard，且错文件——column-rule 在 text_multicol.rs 非 text.rs）；⑤ `engine.rs:482`→`:1002`（cached_baselines 补丁消费，:482 实为 `root_box.y += dy`）。**意义**：multicol cluster 3/3 design docs（handoff R2617 + column-aware-IFC R2618 + fragmentation R2619）行号 drift 全清——user-gated Phase 2 multicol 设计文档群授权实施时无行号误导。**门禁**：纯 .md doc 纠偏零 .rs；pre-commit guard PASS。**下一轮**：续其他高密度 design doc 行号核验（font-face-production-async-rfc 31 / pending-clean-levers 28 / print-layout-phase-p1-spec 26）/ 低频 plateau-guard / 用户点名深结构。

> **📍 R2618（2026-08-04）🔧 design-doc 行号漂移续修（R2202 模式·multicol cluster）：column-aware-IFC-spec.md（18 引用，Phase 2 multicol 设计）4 处 drift pattern 纠偏（17 引用实例），全 precise 命中当前源码；纯 .md 零 .rs 零回归**。承接 R2617（multicol handoff 行号纠偏）。本轮续 multicol cluster design-doc 行号核验（user-gated Phase 2 multicol 设计文档群）。**核验**：自 R2597 起零 .rs 变更，drift 全为 pre-R2597 累积（painter/text.rs / multicol.rs 频繁编辑区）。**4 处 drift 纠偏**（17 引用实例）：① `text.rs:713`→`:569`（multicol paint gate `!has_in_flow_children && is_balance_mode && height_auto`，9× 引用跨 bare + painter/ 全路径）；② `types/mod.rs:371`→`:502` + `:371-394`→`:502-520`（InlineLayoutLine/Fragment 定义，4×；:371 实为 LayoutBox 内 doc 注释行）；③ `text.rs:974-984`→`:881`（per-column `clip_rect = Rect::new(...)` 逐列裁剪构造，3×；:974-984 实为 glyph 字段 bitmap_width/rotation）；④ `multicol.rs:244-502`→`:1222`（block 侧碎片化 `assign_children_to_columns_with_breaking`，:244-502 实为 ColumnInfo 构造 + balance 逻辑）。**经核 valid 保留**：`painter/text.rs:696-717`（use_stored 分发区，:715 `let inline_ctx = if use_stored`，valid「stored-vs-fresh IFC 门控」pointer）。**意义**：multicol cluster 第 2 个 design doc（column-aware-IFC-spec）行号全 precise；与 R2617 handoff doc 合计 multicol 设计文档群行号 drift 清。**门禁**：纯 .md doc 纠偏零 .rs；pre-commit guard PASS。**下一轮**：续 multicol cluster 余项（multicol-fragmentation-design.md 13 引用）/ 低频 plateau-guard / 用户点名深结构。

> **📍 R2617（2026-08-04）🔧 design-doc 行号漂移续修（R2202 模式·multicol handoff）：multicol-layoutside-IFC-implementation-handoff.md（21 引用，最高密度未验 design doc，指导 user-gated Phase 2 multicol）9 处 drift pattern 纠偏，全 precise 命中当前源码；纯 .md 零 .rs 零回归**。承接 R2616（active 控制面 code-citation sweep）。本轮续 R2202 design-doc 行号核验，择 multicol handoff（painter/text.rs 频繁编辑致行号 drift 高发区）。**核验**：自 R2597 起零 .rs 变更（git log 确认 last .rs = 860bb2a1 R2597），故 drift 全为 pre-R2597 累积（handoff doc 早写、R2302-R2597 painter/text.rs 编辑所致）。**9 处 drift 纠偏**（painter/text.rs 7 + inline_finalization.rs 3 + types/mod.rs 1，跨 12 引用实例）：① `text.rs:713`→`:569`（multicol paint gate `!has_in_flow_children && is_balance_mode && height_auto`，4× 引用含「禁止改」门控）；② `text.rs:807`→`:849`（multicol_info `if let Some(ref mc)` 分支，2×）；③ `text.rs:993-1001`→`:878-882`（col_x_offset / clip_rect）；④ `text.rs:1046`→`:956`（`line.y - col_start_y`，:1046 实为 ruby rt_y）；⑤ `text.rs:966-990`→`:853-868`（even-split `target_h`/`floor(line.y/target_h)`，:966 实为 text-shadow push）；⑥ `inline_finalization.rs:284-286`→`:694`（`if root.is_multicol` gate）；⑦ `inline_finalization.rs:942-959`→`:322-324`（col_ctx.layout/lines，Step 2 捕获点）；⑧ `inline_finalization.rs:942-956`→`:1687`（`content_height = balance_column_geometry` 块，函名 `remeasure_inline_only_containers` 正确仅行号 stale）；⑨ `types/mod.rs:377-408`→`:502-520`（InlineLayoutLine/Fragment 定义，:377 实为 LayoutBox 尾）。**经核 valid 保留**：text.rs:934（TextEmphasisStyleValue）/ text.rs:821（stored-frag 展开范围 :826）/ inline_finalization.rs:944（R1280 float）/ multicol.rs:120（R904）/ multicol.rs:336（layout_multicol_with_spanners）/ types/mod.rs:192 + :355（均在 LayoutBox struct :27-379 内，valid「add field near here」planning anchor）。**意义**：user-gated Phase 2 multicol handoff doc 行号全 precise（授权实施时无误导）；painter/text.rs 高 drift 区经核清。**门禁**：纯 .md doc 纠偏零 .rs；pre-commit guard PASS。**下一轮**：续 design-doc 行号核验（column-aware-IFC-spec / multicol-fragmentation-design）/ 低频 plateau-guard / 用户点名深结构。

> **📍 R2616（2026-08-04）🔧 active-control-plane code-citation 核验 sweep（R2202 模式）：核 master.md §下一步/§轻量修复候选/§待用户决策清单/§深结构性方向 内 10 处带代码定位的语义声明，9 处经核 accurate、1 处 ambiguous（`text_metrics.rs:409` 两文件同名）disambiguate 到全路径；纯 .md 零 .rs 零回归**。承接 R2615（doc-control archive）。本轮比 design-doc 行号更高价值——这些 active 控制面声明会直接误导未来 agent 实施/审计。**实施**：提取 master.md 全部 `file.rs:NNN` 引用（排除 round-note 内已被 R2614 纠偏的 historical 旧值），逐一 `sed -n` 核当前源码。**10 处核验结果**：① **parse_basic.rs 死代码声明（R2460）accurate**——文件仍存 `crates/css-parser/src/values/parse_basic.rs`、仍无任何 `mod parse_basic` 声明（dead 非 build）、`parse_tab_size` 仍双份共存（parse_basic.rs:1008 + parse_layout.rs:610）；② **margin-trim 声明 accurate**——tree.rs:1029 `fn build_subtree` + ZW_MARGIN_TRIM kill-switch :1072；③ **image-loading 声明 accurate**——reftest/resources.rs:528 `build_image_cache`；④ **blend_mode 声明 accurate**——painter/effects.rs:537 `add_blend_mode(BlendModePrimitive{rect,mode})`；⑤ **`<q>` quotes 声明 accurate**——style-system/src/lib.rs:368 `<q>` 检测；⑥ **phase-a cluster（R2605/R2606）全 accurate**——inline/mod.rs:1228 R1004 ascent_ratio_overrides 注 + inline_finalization.rs:1027 `backfill_phasea_orphan_boxes` + pipeline/mod.rs:270 `set_font_metric_map` U1b-wiring doc；⑦ **text_metrics.rs:409 disambiguate**——两文件同名（crates/engine/src/text_metrics.rs 仅 58 行无 :409 vs crates/layout-engine/src/inline/text_metrics.rs:409 `resolve_normal_line_height`），R2605 note 裸 `text_metrics.rs:409` 误导风险 → 订正全路径 `crates/layout-engine/src/inline/text_metrics.rs:409`（design doc phase-a-IFC-unification-design.md:440 既已全路径正确，本轮 master.md 对齐）。**结论**：active 控制面代码定位声明 10/10 经核 accurate 或已 disambiguate；R2202「doc 滞后/歧义致盲推」防护再加固；parse_basic.rs 死代码（用户点名项）经核仍待清理（CLAUDE.md「提及但不删除」）。**门禁**：纯 .md doc 纠偏零 .rs；pre-commit guard PASS。**下一轮**：低频 plateau-guard / 文档纠偏 / 用户点名深结构。

> **📍 R2615（2026-08-04）🔧 doc-control 瘦身续：§最近轮次摘要 底部 8 轮（R2603→R2596）迁出至 [`archive/rounds-r2596-r2603-summary.md`](./archive/rounds-r2596-r2603-summary.md)（100% 逐字保留）；master.md 232→218 行（sed 删 16 + 2 era 条目净 +2），窗口 19→12 健康（archive -8 + R2615 note）；纯 .md 零 .rs 行为变更**。承接 R2614（doc-vs-code 行号漂移续修）。§最近轮次摘要 窗口达 19 轮（R2596-R2614），接近 R2608 既定 ~20 archive 触发点（R2601 @21→13 / R2608 @20→13 precedent），本轮按 R2555/R2556/R2561/R2571/R2578/R2585/R2591/R2596/R2601/R2608 sed 按行号低风险模式续 fifo archive（proactive 瘦身）。**实施**（先核边界）：① 边界核验 R2603@43 / R2596@57 + blank@58 + `## 轮次详记归档`@59 → 抽取 lines 43-57（8 轮 R2603→R2596，全 blockquote 体）→ 新 `archive/rounds-r2596-r2603-summary.md`（7 行 header + 15 行原文 = 22 行，首行 R2603 / 末行 R2596 / 8 📍 核验，**100% 逐字保留**）；② master.md `sed -i '43,58d'`（删 16 行含 trailing blank）→ 232→216 行，boundary 核验 R2604(41)→blank(42)→`## 轮次详记归档`(43) 干净；③ §轮次详记归档 + §历史轮次归档索引 各加 R2596-R2603 era 条目。**验证全绿**：H2 8 section 全在序；§最近轮次摘要 现 R2604-R2615 = 12 轮（健康 recent 窗口）；archive 文件 22 行含 R2596-R2603 全 8 轮。**意义**：doc-control 瘦身续（累计迁出 385 轮）；§最近轮次摘要 从 19 轮恢复至 12 轮健康窗口，master.md 保持 navigable。**门禁**：纯 .md doc 重组零 .rs 行为变更；pre-commit guard PASS。**下一轮**：长间隔 plateau-guard（R2613 刚绿延长间隔）/ 文档纠偏 / 用户点名深结构。

> **📍 R2614（2026-08-04）🔧 doc-vs-code 行号漂移续修（R2203 verify-then-fix 模式）：current-baseline.md + dc-progress.md 7 处 `file.rs:NNN` 引用经源码核验后纠偏，全部精确指向当前实现；5 处经核正确保留；纯 .md 零 .rs 零回归**。承接 R2613（plateau-guard triple-guard 全绿）。本轮按 §轻量修复候选「文档 vs 代码行号漂移续修（R2203 verify-then-fix 模式）」+ R2202 洞见「doc 滞后致盲推」做行号漂移 sweep。**实施**：提取 current-baseline.md（7 unique 引用）+ dc-progress.md（12 unique 引用）全部 `file.rs:NNN` 引用，逐一 `sed -n` 核当前源码。**7 处 drifted 纠偏**（行号随代码演进漂移，代码本身正确）：① current-baseline.md:47 + dc-progress.md:136 `effects.rs:134`（实为 background-clip ContentBox）→ `effects.rs:255`（`style.background_image.iter().enumerate().rev()` 多图层逆序渲染）；② dc-progress.md:118 `painter/mod.rs:1459`（实为 direction 指示器）→ `:1785`（`draw_order 是默认渲染路径` 注）；③ dc-progress.md:135 `painter/text.rs:1067` `if has_text_shadow`（stale 单值描述）→ `:966` `for &(shadow_ox,shadow_oy,shadow_color) in &active_text_shadows`（R2305 多阴影列表，snippet 同步）；④ dc-progress.md:143 `lib.rs:355`（实为 marker pseudo）→ `style-system/src/lib.rs:482`（`MediaContext::new` + `ctx.media_type`）；⑤ dc-progress.md:175 `reftest.rs:230-232`（实为 max_diff_ratio doc）→ `:278-282`（`run_reftest_with_base` 渲 ref，R2204 既修 entry doc :38 漏修 dc-progress 本处补齐）；⑥ dc-progress.md:126 `converter:418`（实为 Display Contents）→ `converter/mod.rs:434`（`PositionValue::Sticky|Relative|Static => Position::Relative`）。**5 处经核 correct 保留**：webview.rs:515（fetch_url）/ webview.rs:402（fetch_image_subresources）/ postprocess.rs:1652（resolve_relative_inset）/ postprocess.rs:1426（clamp_percentage_max_height）/ reftest.rs:98-109（strict_max_diff_ratio Layout0.001/Text0.005）/ tests.rs:1240（test_strict_tolerance_dc14_locked）/ cpu/gradient.rs:28（`if gradient.repeating`）/ app_render_primitives.rs:17/412（R2612 既验）。**archive/ 同名 stale 引用（effects.rs:134 / reftest.rs:230-232 / text.rs:1067 / lib.rs:355）按文档治理「archive append-only 历史记录」原则不动**。**核验**：全 active doc（current-baseline + dc-progress）`file.rs:NNN` 引用逐一 `sed -n` 复核 7 纠偏 + 5 保留全 precise 命中；entry doc rendering-compat.md 无 stale 引用（R2204 既清）。**意义**：doc-vs-code 行号漂移 sweep 收口（active doc 全部行号引用 precise 命中当前源码），R2202「doc 滞后致盲推」防护再加固；行号漂移本身非行为缺口（代码正确，仅引用过时）。**门禁**：纯 .md doc 纠偏零 .rs；pre-commit guard PASS。**下一轮**：低频 plateau-guard / 文档纠偏 / 用户点名深结构。

> **📍 R2613（2026-08-04）📋 会话边界 plateau-guard triple-guard 全绿复跑（守住已获得收益·8 轮纯 .md 后零 drift 确认）：make test 13191/0/74（精确 R2597/R2604 持平，零回归零环境 drift）+ product-smoke EXIT=0 全 struct PASS（welcome desktop/375/320 + morning article；welcome 17.03%=81737/480000px 精确 R2544 baseline）+ product-smoke-legacy EXIT=0 0 struct FAIL（42 fixture 39 compared+3 HTML5-skip，avg 2.36% / worst 19-testpage 17.23%，精确 R2584/R2589/R2604 持平）；纯核查零 .rs 零回归**。承接 R2612（line 145 glyph 状态跨文档纠偏收官）。距上次全量 triple-guard（R2604）已累积 8 轮纯 .md（R2605-R2612 全纯文档），本轮按 run-rules point 1「守住已获得收益：每轮先确认 make test、产品 smoke、legacy smoke 没有新回归」+ R2583/R2604 会话边界 precedent 跑 triple-guard 确认无环境/工具链 drift。**实施**：`make test` + `make product-smoke` + `make product-smoke-legacy`（均 test-guard 包裹，release 缓存命中）。**结果全绿**：① `make test`（test-guard，`cargo test --workspace --exclude zero-render-foundation`，本地按 Makefile comment 跳过 render-foundation 1800s 自身超时 crate）= **13191 passed / 0 failed / 74 ignored**（awk 聚合全 test-binary「test result:」行；74 ignored = real_website_compat 等网络型用例；精确 R2597 baseline，0 FAILED，0 panicked）；② `make product-smoke` EXIT=0——welcome desktop struct PASS（DC-13 800px diff 17.03%=81737/480000px ≤20% 门禁）+ welcome 375/320 窄屏 struct PASS（card:4，守 R1502/R1503）+ morning-work article 320 struct PASS（article:1；googletagmanager 外部脚本 fetch fail = 网络跳过预期非回归）；③ `make product-smoke-legacy` EXIT=0——**0 struct-check failures** / 42 fixtures run（39 compared + 3 skip=49-details-summary/50-mark/51-blockquote-dl-figure HTML5 越界 Tier-1 by-design 无 oracle）/ avg diff 2.36% / worst 19-testpage-minimal 17.23%（精确 R2604 持平，font-wall trend-only 非 pass/fail）/ 37-form-controls struct PASS 3.85%（R2156+R2162 Phase A default-on 后既稳定）。**结论**：triple-guard 全绿确认 R2597 既绿状态经 8 轮纯 .md 无环境/工具链/行为 drift；clean-lever surface 维持 9 vein + 10th vein exhaust/退役；doc-consistency + doc-freshness surface（R2612 既全 closure）经本轮复跑确认无新 stale；自主推进面仍 = plateau-guard（已确认绿，间隔重置可再延长）+ 文档纠偏；**唯一推向 95% = 用户点名授权深结构**（最高 value Phase A IFC line-box-metric 统一）。**门禁**：纯核查零 .rs；pre-commit guard PASS。**下一轮**：长间隔 plateau-guard（刚绿延长间隔）/ 文档纠偏 / 用户点名深结构。

> **📍 R2612（2026-08-04）🔧 rendering-compat.md line 145 P1-严重缺口跨文档状态矛盾纠偏（末处已知 doc-consistency 不一致收官）：「浏览器层 glyph 重排」源码核验 = R2004 已修（`transform_webview_primitives`〔app_render_primitives.rs:194/412 `for glyph in &primitives.glyphs`〕逐个映射仅 scale+offset+clip **无 sort/reorder/batch** + 单测 `transform_webview_primitives_preserves_glyph_order`〔tests.rs:1711〕守此不变量），入口 doc line 145 原列「open P1-严重缺口」为 pre-R2004 过时描述 → 移出 open 列表 + 标 R2004-fixed，使与 current-baseline.md:37（R2569 既注 ✅已修复·struck-through）+ line 253（DC-13 ✅glyph 重排保护已实现 R2004）一致；纯 .md 零 .rs 零回归**。承接 R2611（test-count 跨文档纠偏收官）。本轮收尾 R2611 提出的「最后一处已知跨文档状态矛盾」——入口 doc line 145「⚠️ P1-严重缺口：...、浏览器层 glyph 重排、滚动容器」与 current-baseline.md:37「~~浏览器层 glyph 重排~~ ✅已修复（R2004）」矛盾。**源码核验**（R2004 实际修复态）：① `transform_webview_primitives`（app_render_primitives.rs:194）glyph 段 = line 412 `for glyph in &primitives.glyphs {` 顺序遍历，仅 `glyph.x*s+x_offset` / `glyph.y*s+y_offset` / `font_size*s`（line 413-415）+ clip（:416-423），逐个 `out.glyphs.push`（:424）= **纯顺序映射无 sort/reorder/batch**（grep sort/reorder 零命中该函数）；② 回归单测 `transform_webview_primitives_preserves_glyph_order`（tests.rs:1711）= 4-glyph 序列 A/B/C/D（行1 y=10 ×3 + 行2 y=30 ×1，混 font0/font1），断言输出严格保输入顺序（`vec!['A','B','C','D']`），明确防 sort-by-Y-then-font / font-batch 两类未来优化破坏跨行布局语义（DC-13 line 328/335 永久 gate）。**结论**：R2004 修复真实且源码态无回退 → 入口 doc line 145 误标 open。**纠偏**：① line 145 P1 列表移除「浏览器层 glyph 重排」→ 仅留 3 项真实 open（Inline formatting 所有权分裂 / Layout/Paint IFC 双路径 / 滚动容器，精确对齐 current-baseline.md:33/34/42 open 三项）；② 列表尾加 R2004-fixed 注（`transform_webview_primitives` 无 reorder + 单测守护 + 详见 current-baseline/DC-13）；③ line 143 截至 R2611→R2612。**核验**：全 doc tree「glyph 重排」open 引用清零（line 253 ✅ + line 335 DONE-forbidden gate + current-baseline:37 ✅ 三处均既 current，line 145 现 aligned）；line 335「❌ ZeroBrowser 对 WebView glyph 做会改变布局语义的后处理重排」DONE-forbidden 永久 gate 不变（该 gate 描述「禁止做重排」与 R2004「确保无重排」一致，保留）。**意义**：跨文档状态矛盾末处收官（R2603 master test-count + R2610 current-baseline test-count + R2611 入口 test-count + R2612 入口 P1-glyph-status 四处 doc-consistency 全 closure）；doc-freshness + doc-consistency surface 全 current。**门禁**：纯 .md doc 纠偏零 .rs；pre-commit guard PASS。**下一轮**：低频 plateau-guard / 文档纠偏 / 用户点名深结构。
