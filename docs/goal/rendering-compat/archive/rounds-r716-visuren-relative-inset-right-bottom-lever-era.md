# 历史轮次归档 — R716（fresh CSS2/visuren 采样 · box-offsets-rel-pos-002 = resolve_relative_inset 仅读 left/top 丢 right/bottom CLEAN LEVER（第 12）· anonymous-boxes-001b R680/R702 structural era）

> 内容 100% 保留自主控文档 master.md（R716 块逐字迁出，仅作归档）。下文为该轮完整逐轮详记。

**R716（fresh CSS2/visuren 采样（第 6 个 CSS2 subdir，Visual formatting model §9）·box-offsets-rel-pos-002 = ★NEW CLEAN LEVER（第 12，替降级 R711）·resolve_relative_inset（engine.rs:1597）仅读 left/top 丢 right/bottom→inline-level & root relative right/bottom offset 全失·anonymous-boxes-001b = R680/R702 §9.2.1.1 anon-line-height structural（%height anon-skip 正确）·归档 R696 恢复 last-20·零源码）：承 R715 CONTINUE 采样 fresh CSS2 subdir **visuren**（CSS2 §9 Visual formatting model，display/position/float/BFC 核心 box-model，未采样）。`make reftest-oracle DIR=CSS2/visuren`（34 案，read-only；**58.8% pass**，较高）取 top-15：anonymous-boxes-001b **24.84%** / box-offsets-rel-pos-002 9.18% / position-absolute-percentage-inherit-001 9.05% / inherit-static-offset 簇 4-5% / percent-height-1 3.60% / top-114 2.95% / clear-applies-to-016/017 2.7/2.5%。查 **anonymous-boxes-001b（24.84%，§9.2.1.1 anon block box + §10.5 %height anon-skip）** LAYOUT_DUMP。**test**：`#closest-non-anonymous-ancestor{height:200}`（inline "Some " + block `#child{height:50%}` + inline " text" + p），assert「anon block box 被 %height 解析忽略→#child height:50% 对非匿名祖先 200→100px 覆红」。**dump（裁决：%height anon-skip 工作，position 错=§9.2.1.1 anon-line-height 结构性）**：`#child h=100`（=50%×200，**%height anon-skip 正确✓**，对非匿名祖先 200 解析非 anon box）但 **`#child abs_y=48`（= 祖先 top，应 122=祖 top+「Some 」anon 行盒 50px）**——「Some 」inline 文本的匿名块盒**未生成/未留行高**→#child 不下移→不覆红（红 122-222，#child 48-148）= 24.84%。**★ 裁决：R680/R702 §9.2.1.1 anonymous-block-line-height 簇**（inline 文本与 block 子混排时匿名块盒行高缺失，R680 br-between-blocks/R702 collapse-through-inline 同族）；%height 解析本身工作（补强 R695：当非匿名祖先有 definite height 时 %height anon-skip 正确）。**非新 lever（structural）**。查 **box-offsets-rel-pos-002（9.18%，§9.4.3 relative offset，img inline-level）** LAYOUT_DUMP。**test**：3×3 grid 内 `img{position:relative}` + `right/left/top/bottom:100px`（moves-toward-* 类），assert「8 红 + 1 绿方块重叠无红」。**dump（裁决：★CLEAN LEVER——left/top 应用，right/bottom 丢）**：`img.moves-toward-right`(left:100) **x=108✓**（8+100）/ `img.moves-toward-left`(right:100) **x=208✗**（应 108，right 未应用）/ `img.moves-toward-bottom`(top:100) **abs_y=151✓**（51+100）/ `img.moves-toward-top`(bottom:100) **abs_y=251✗**（应 151，bottom 未应用）——**left/top relative inset 应用，right/bottom 丢**。**★ 根因（代码 read-only 精确）**：`engine.rs:1597 resolve_relative_inset`（root relative engine.rs:192 + inline-level relative apply_relative_offsets_inline:1345 共用）`dx = style.left(Px)`/`dy = style.top(Px)`——**完全忽略 right/bottom**！CSS §9.4.3：relative 的 `right`（无 left 时）应向左偏移、`bottom`（无 top 时）应向上偏移。ZW 丢这两者→inline/root relative 的 right/bottom-only offset 全失。**裁决：真 isolable bug（spec §9.4.3）·CLEAN LEVER（第 12，替 R715 降级的 R711）**。**fix scope（hand-off code-agent，一行级函数）**：扩展 resolve_relative_inset——dx：left Px→+left，elif right Px→**-right**；dy：top Px→+top，elif bottom Px→**-bottom**（§9.4.3）。**低 effort**（一函数 ~6 行），spec-correct，blast radius 低（仅 right/bottom-only relative offset 元素受影响；block-level relative 由 taffy 处理不受此函数影响）。**cluster yield**：box-offsets-rel-pos-001/002（twin 9%）+ inline right/bottom relative reftest。**次级 note**：resolve_relative_inset 对 left/top 亦 **Px-only**（Em/Rem/Percent 丢），与 R715 percent-inset taffy 限制谱系；本次 lever 聚焦 right/bottom drop（confirmed），Px-only 扩展为可选 follow-up。**★ 意义（双重）**：① **fresh CSS2 subdir（visuren）采样首日即命中新 clean lever（第 12）**——印证「采样 fresh box-model subdir」策略持续高产（visuren §9 与 normal-flow §10/positioning 同 vein）；② **resolve_relative_inset「仅 left/top」是 inline/root relative positioning 系统性缺口**，修之惠及 inline img/span right/bottom 偏移。**★ batch 归档**：R696（inline-replaced-width unified sizing lever，**已在 pending-clean-levers.md Tier A**）迁出 → archive/rounds-r696-...，master 恢复 last-20（R697-R716）。**hand-off code-agent**：**resolve_relative_inset 加 right/bottom 是低-effort clean lever（第 12，9%→PASS，+twin cluster），建议早做**；clean-lever 队列恢复 12（R716 替降级 R711）。**下一步**：续 visuren 次高（position-absolute-percentage-inherit-001 9.05% abspos %inherit / inherit-static-offset 簇 4-5%）或转 visudet（§10 sizing details）fresh subdir。零源码（reftest-oracle read-only + LAYOUT_DUMP read-only + 代码 read-only 定位 engine.rs:1597/192/1345，未改任何文件；engine.rs WIP 是 table-cell overflow，对 relative inset no-op）。
