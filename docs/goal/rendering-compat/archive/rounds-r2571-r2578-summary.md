<!-- 归档：R2571–R2578 轮次摘要（2026-08-04 R2596 doc-control 瘦身迁出，100% 逐字保留）-->

# 轮次摘要归档 R2571–R2578

本文件由 master.md §最近轮次摘要 迁出（era 2571–2578，8 轮，R2579 不存在〔R2580 backfill 记 R2579 跨文档刷新〕），100% 逐字保留未删减未重排。
迁出详见 master.md R2596。原文件顺序（newest→oldest）：

> **📍 R2578（2026-08-04）📋 exhaustive value-variant 消费审计 = clean lever 零产出；clean-lever surface definitively 穷尽（feature/applied-consumed/value-gap × exhaustive 四法既证）；纯审计零 .rs 零回归**。承接 R2577（word-break:break-word，value-gap vein 既开）。**本轮 exhaustive value-variant 审计**（Python 全 `pub enum` 变体→engine/layout `Enum::Variant` 消费核验，扩展 R2577 ~30 枚举到全部）：**55 枚举 partial-consumption flag，逐一核验 = clean lever 零产出**——flag 主 false-positive：① **style-system computed 解析期消费**（LengthValue/ColorValue/FontWeight 等在 computed.rs resolve 非 engine/layout，审计漏扫）；② **boolean-negation 处理**（text-emphasis-position under 经 `emphasis_over=false`→mark_y else 分支〔text.rs:1011〕既正确处理，非 gap）；③ import 短形。残余 plausible 候选核验：**column-rule-style double/groove/ridge/inset/outset** = 0 driving test（25 file 全 solid/shorthand）；**BreakInside/Break avoid** = 分页/multicol 深；**PointerEvents/ScrollSnapStop** = host-layer；**LineBreak/TextAutospace** = CJK font-wall；**Appearance** = form widgets 深；**UserSelect** = host-layer。**结论：clean-lever surface 经 4 法（directed probe R2572-R2574 + agent fan-out R2575 + exhaustive field 审计 R2576 + exhaustive variant 审计 R2578）definitively 穷尽**——R2572-R2577 六连 lever 既 land，余 applied-consumed（field 审计 6 unconsumed 全 niche/deep）+ value-gap（variant 审计残余全 false-positive/deep/host-layer/0-test）。**后续自主推进面 = ① 低频 plateau-guard + ② 文档纠偏**；唯一推向 95% = 用户授权深结构。**门禁**：纯审计零 .rs 零回归不跑昂贵 make test；pre-commit guard PASS。**意义**：clean-lever surface 穷尽经 exhaustive 双审计（field+variant）rigorous 确证（非「likely」而「definitively 经核」）；后续轮勿重跑 variant 审计（false-positive 主导，manual 核验高成本低 yield）。**下一轮**：长间隔 plateau-guard（R2577 后零 .rs drift 待累积）/ 文档纠偏 / 用户点名授权深结构（最高 value Phase A IFC line-box-metric 统一）。
> **📍 R2577（2026-08-04）🔧✅ `word-break: break-word`（CSS Text 3 legacy ≡ overflow-wrap:break-word）3 处 break_word 计算别名 LANDED——系统化 value-variant 消费审计发现；A/B 三态门禁 net 正（make test 13190/0/74 零回归 + scoped reftest word-break-break-word-overflow-wrap-interactions flip PASS + product-smoke welcome 17.03% 精确持平）**。承接 R2576（border-image-width，applied-consumed vein 既穷尽）。**本轮转系统化 value-variant 消费审计**（Python 值枚举变体 → engine/layout `EnumName::Variant` 消费核验，校准既消费枚举 TextAlignValue/WhiteSpaceValue/BorderStyleValue 等全消费）：候选 UserSelect(host-layer 4/5 未消费)/BreakValue(break-inside:avoid 分页深)/WordBreakValue(Normal+BreakWord 未消费)/LineBreak(CJK font-wall)/BoxDecorationBreak(clone niche)。**`word-break: break-word` = 真 value-gap**：resolve_word_break_mode（inline_finalization.rs:140）仅 match BreakAll/KeepAll，BreakWord 落 default 无效；但 overflow-wrap:break-word 既工作（break_word flag）→ spec BreakWord 应 ≡ overflow-wrap:break-word（opportunistic 断词防溢出）；**26 driving test**。**实施**（clean lever，3 处 break_word 计算加 `|| word_break==BreakWord`，与 overflow_wrap::BreakWord 完全同处理）：inline_finalization.rs:803（主 IFC）+ :1307（measure 闭包）+ text.rs:582（paint IFC）三处 `matches!(style.overflow_wrap, BreakWord|Anywhere)` 加 `|| matches!(style.word_break, zero_style_system::WordBreakValue::BreakWord)`；normal/break-all/keep-all 及所有 overflow-wrap 值不变 → 零回归。**A/B 三态门禁全绿**：① `make test` **13190/0/74**（精确持平）；② `make reftest-upstream FILTER=word-break` **105/107 (98.1%)**，**word-break-break-word-overflow-wrap-interactions.html ✓ flip PASS**（此前 break-word 未别名无断词行为 → fail）；2 残留 fail = CJK keep-all/font-wall 深；③ `make product-smoke` welcome **17.03%（精确持平）** + 0 struct FAIL。**门禁**：cargo fmt + `cargo clippy --workspace --all-targets -D warnings` 全绿；pre-commit guard PASS。**意义**：**系统化 value-variant 消费审计 = 第 3 种 scalable gap 发现法**（field 审计→variant 审计递进）；word-break:break-word 是 CSS Text 3 legacy 别名，net 正 land。**下一轮**：续 value-variant 审计其他值枚举（剩余候选多 host-layer/分页深/CJK 低 yield）/ 低频 plateau-guard / 用户点名深结构。
> **📍 R2576（2026-08-04）🔧✅ `border-image-width`（CSS Backgrounds 3 §7.3）paint 消费 LANDED——系统化字段消费审计发现；A/B 三态门禁 net 正（make test 13190/0/74 零回归 + scoped reftest border-image-width-001 flip PASS + product-smoke welcome 17.03% 精确持平）**。承接 R2575（border-image-outset）。**本轮先 Explore-agent fan-out 续扫**（transforms/shapes/contain/masking/scroll-snap）**= 零 clean lever**（top 候选 perspective-origin/backface-visibility 皆 3D-dependent 实 deep 非 bounded[agent 误判] / scroll-snap 0 test host-layer）。**转系统化 ComputedStyle 字段消费审计**（Python grep 全 223 pub 字段 → engine/layout 消费核验）：7 字段零消费，逐个核验——**`border_image_width` = 真 applied-not-consumed gap**（parsed parse_border_image_width + applied apply_advanced.rs:1306 + computed 字段，但 paint_border_image 从未读；70 driving test；outset R2575 同脉）；content_visibility 经 `content_visibility_hidden_effective()` 方法消费（非 gap）；mask_mode 4 test niche；余 color_scheme_dark/animation_play_state/overscroll_behavior_y/text_decoration(简写聚合,子字段既消费) 皆 niche/host-layer。**实施**（clean lever，default Number(1.0)=1×border-width=边框宽度 → shadow bt/br/bb/bl 零回归 by construction）：paint_border_image outset 块后加 width_px 解析（BorderImageWidthComputedComponent Auto→border-width / Number×border-width / Length px / Percent×box-dim）→ shadow bt/br/bb/bl（9 宫格公式自动用新厚度，guard/outset 既用原 border-width 不变）；import BorderImageWidthComputedComponent。**A/B 三态门禁全绿**：① `make test` **13190/0/74**（精确持平）；② `make reftest-upstream FILTER=border-image-width` **19/25 (76.0%)**，**border-image-width-001.htm ✓ flip PASS**（此前 width 未消费用 border-width → mismatch fail）；border-image-width-005/006/007 (2.1%) / 008/009 (7%) / should-extend-to-padding (4.48%) 仍 fail = `auto` 图像固有尺寸[ZW paint 期无解码尺寸简化为 border-width] / percent / extend-into-padding 深交互 defer；③ `make product-smoke` welcome **17.03%（精确持平）** + 0 struct FAIL。**门禁**：cargo fmt + `cargo clippy --workspace --all-targets -D warnings` 全绿；pre-commit guard PASS。**意义**：**系统化字段消费审计 = 最可靠 applied-consumed gap 发现法**（exhaustive 全字段 grep，胜 agent 定向 fan-out sampling 漏扫——本轮 agent fan-out 漏 width，系统化审计捕到）；border-image-width 是 outset 同脉第 2 个 border-image paint 消费 lever。**下一轮**：系统化审计余 unconsumed 字段皆 niche/deep → applied-consumed vein 近穷尽；转评估 moderate-feature（shape-outside/mask-border/offset-* 等 not-implemented）或 plateau-guard / 用户点名深结构。
> **📍 R2575（2026-08-04）🔧✅ `border-image-outset`（CSS Backgrounds 3 §7.2）paint 消费 LANDED——pivot 出 list/counter 区首个 lever；A/B 三态门禁 net 正（make test 13190/0/74 零回归 + scoped reftest border-image outset 测试 flip PASS + product-smoke welcome 17.03% 精确持平）；Explore-agent fan-out applied-consumed 扫描方法学首验**。承接 R2574（list-style-type string，list/counter 区既尽）。**本轮先 manual probe pivot**（css-text-decor thickness/wavy、outline-offset、background-clip:text、@counter-style pad/negative、css-tables 11 fail）**全既实现或 net-negative/font-wall/deep → manual 1-at-a-time 趋 thin**。**转 Explore-agent 多模块 fan-out 扫描 applied-consumed gap**（css-backgrounds/position/text/ui/flex/grid/box/overflow，R2373-R2383 谱规模化）：agent 报 `border-image-outset` = parsed（parse_extended_border.rs:294）+ applied（apply_advanced.rs:1356，ComputedStyle.border_image_outset 字段）但 **paint_border_image（border.rs:180）从未消费**；**51 driving test**。**实施**（clean lever，default outset=0 零回归 by construction）：paint_border_image 加 outset 解析（BorderImageOutsetComputedComponent Number×对应边框宽度 / Length px）→ 调整 `bx/by/w/h` 外扩（9 宫格角/边/中心公式不变，仅外矩形偏移）；import BorderImageOutsetComputedComponent。**A/B 三态门禁全绿**：① `make test` **13190/0/74**（精确持平）；② `make reftest-upstream FILTER=border-image` **127/167 (76.0%)**，**border-image-outset-004 ✓ + 多 outset widget-* ✓ flip PASS**（此前 outset 未消费 border-image 画在 border-box → mismatch fail）；border-image-outset-003 (24%) 仍 fail = 大 outset×repeat 深交互 defer；③ `make product-smoke` welcome **17.03%（精确持平）** + 0 struct FAIL。**门禁**：cargo fmt + `cargo clippy --workspace --all-targets -D warnings` 全绿；pre-commit guard PASS。**附带**：本轮 Python 修 R2573/R2574 doc-consistency bug（R2574 edit 时 old_string 仅匹配 R2573 note prefix 致 R2573 body 误并入 R2574 line）→ R2573 body 复位 R2573 line、R2574 line 截正。**意义**：**pivot 出 list/counter 区生效 + Explore-agent fan-out applied-consumed 扫描 = scalable 高 yield 方法**（多模块并行核验 parsed-but-not-consumed，胜 manual 1-at-a-time）；后续轮续 fan-out 扫其他 applied-consumed gap。**下一轮**：Explore-agent fan-out 续扫 applied-consumed gap（其他 parsed-not-consumed 属性）/ 低频 plateau-guard / 用户点名深结构。
> **📍 R2574（2026-08-04）🔧✅ `list-style-type: <string>`（CSS Lists 3）实现 LANDED——A/B 三态门禁 **net=0**（零回归、无 self-source flip，真价值在 chromium-oracle 一致率）；list/counter 区 bounded-feature 高 yield 部分既尽，后续 probe 须 pivot 其他 CSS 模块**。承接 R2573（::marker）。**本轮续第 9 angle 定向 code-reading probe（list/counter 区伴生特性）**：核 `list-style-type: <string>`（CSS Lists 3）——`parse_list_style_type` 仅 match 命名关键字 + `<custom-ident>`（custom @counter-style 名），`_` arm 的 `is_custom_counter_name` 拒绝引号起始（首字符须字母/连字符）→ `list-style-type: "▶"` 丢 None；`symbols()` 函数亦未实现（仅 2 driving test，低 value defer）。**58 file 含 `list-style-type: "<string>"`** 但经核 **CSS2/lists 0 个**——全为 incidental（css-pseudo marker-text-transform-*/marker-word-* 用 string type 设深特性测试，CSS2/visudet 2 个），**无 dedicated feature reftest**。**实施**（clean lever，additive 新枚举变体）：(1) types.rs 加 `ListStyleTypeValue::String(String)` 变体；(2) parse_misc.rs `parse_list_style_type` 顶部加引号字符串检测（`"<s>"`/`'<s>'`，空串合法）→ `String(inner)`，须在 keyword match 前（引号起始非 ident 否则落 `_` 被丢）；(3) text_list.rs paint_list_marker 加 `String(s)` arm（固定字符串标记，非计数器每个 li 同值，按文本 marker 同位绘字 ≡ Decimal arm 字形循环；空串→无标记）；(4) color.rs test_parse_list_style_type 加 string 测试（"▶"/'Step '/空串/未闭合 None）。**A/B 三态门禁**：① `make test` **13190/0/74**（精确持平零回归）；② `make reftest-upstream FILTER=marker` **101/158 持平 R2573**（marker-text-transform-*/marker-word-* ✓ 但 R2573 既 ✓——self-source 两边同 string-type 渲染→恒 PASS 无视支持，故零 flip）；③ `make product-smoke` welcome **17.03%（精确持平）** + 0 struct FAIL。**net=0 评估**：无 dedicated self-source flip（string-type 测试皆 self-source），但**真价值 = chromium-oracle 一致率**（ZW 现渲染 string marker 匹配 chromium，oracle pass 率边际升；self-source reftest 无法测此差异）。**门禁**：cargo fmt + `cargo clippy --workspace --all-targets -D warnings` 全绿；pre-commit guard PASS。**意义**：list/counter/generated-content 区经 R2572-R2574 三轮密集开采，**bounded-feature 高 yield（多 dedicated test flip）部分既尽**（counters/::marker 已 land 大量 flip，list-style-type string 无 dedicated test）——**后续定向 probe 须 pivot 到其他 CSS 模块**（css-text-decor / css-backgrounds value 级 / css-tables / css-position 等）方续高 yield。**下一轮**：定向 probe pivot 其他 CSS 模块 bounded feature / 低频 plateau-guard / 用户点名深结构。

> **📍 R2573（2026-08-04）🔧✅ ::marker 伪元素 color+content 子集（CSS Lists 3 / CSS Pseudo 4）实现 LANDED——定向 probe vein 连续第 2 轮出 lever；A/B 三态门禁 net 正（make test 13190/0/74 零回归 + scoped reftest marker-color/marker-content-001/002/004 flip PASS + product-smoke welcome 17.03% 精确持平）**。承接 R2572（counters() LANDED + holding 框架订正）。**本轮续第 9 angle 定向 code-reading probe（counters() 天然伴生特性）**：核 `::marker` 伪元素——css-parser 已知伪元素列表含 `marker`（mod.rs:951，parse+match 通），但 **style-system 无 `marker_pseudo` 字段**（仅 before/after），`::marker` 规则计算样式从未存储/应用，paint_list_marker 用元素自身 color/list-style-type；**paint_list_marker gate = tag 名 `li`**（非 display:list-item，ZW 未建模 list-item display）。**160 driving test**（css/css-pseudo marker-color/marker-content-001~024/marker-font-properties 等 + CSS2/lists）。**实施**（clean lever，None-guard 零默认回归）：(1) computed_style.rs + default_impl.rs 加 `marker_pseudo: Option<Box<ComputedStyle>>`；(2) lib.rs compute_styles 对 `<li>` 计算 marker 伪元素（compute_element_style_internal pseudo=Some("marker")，同 before/after 谱），**仅当 ::marker 实际改变 content 或 color 时存储**（content!=Normal || color!=elem color）省内存 + paint 仅 Some 时走覆盖；(3) **paint_list_marker**：base color 改用 `marker_pseudo.color`（marker 继承自元素 → 默认等同元素 color，::marker 覆盖时变）；content 覆盖块（content:none→抑制；String/Counter/Counters/List→resolve_generated_content_text 解析文本绘字；normal/Attr/Url→落默认 list-style-type）；(4) **text.rs 抽 `resolve_generated_content_text` helper**（paint_content 与 marker content 覆盖共用计数器/拼接逻辑，消除重复）。**关键设计**：marker_pseudo 默认 None（无 ::marker 规则）→ paint 整块跳过 + color unwrap_or 元素 color → **默认 `<li>` marker 零行为变更**（welcome 等 fixture 无 ::marker → 零回归 by construction）。**A/B 三态门禁全绿**：① `make test` **13190/0/74**（精确持平）；② `make reftest-upstream FILTER=marker` **101/158 (63.9%)**，**marker-color.html ✓ / marker-content-001/002/004.html ✓ 全 flip PASS**（此前 ::marker 未应用 marker 用默认 color/list-style → mismatch 全 fail；现 color+content 覆盖 → match PASS）；57 残留 fail = marker-font-properties/text-align/line-height/word-break 等 **marker 盒布局深特性**（超出 color+content 子集，defer）；③ `make product-smoke` welcome **17.03%（81737px 精确持平）** + 全 struct PASS。**门禁**：cargo fmt + `cargo clippy --workspace --all-targets -D warnings` 全绿；pre-commit guard PASS。**意义**：**定向 code-reading probe vein 连续 2 轮出 lever**（R2572 counters() + R2573 ::marker）——强化「clean-lever surface 非穷尽」结论；::marker color+content 是 CSS Lists 3 核心伪元素特性，与 R2572 counters() 组合（`::marker { content: counters(item,".") }`）支持嵌套列表自定义标记。**下一轮**：续定向 code-reading probe（残余 bounded feature / CSS 函数伪元素）/ 低频 plateau-guard / 用户点名深结构。

> **📍 R2572（2026-08-04）🔧✅ clean-lever surface 再证非穷尽：`counters(name, sep, [style])` 嵌套计数器函数（CSS Lists 3 §counter-functions / CSS2 §12.4.3）实现 LANDED——A/B 三态门禁 net 正（make test 13190/0/74 零回归 + scoped reftest 211/226 零 content-counters fail + product-smoke welcome 17.03% 精确持平）；holding 框架订正**。承接 R2571（doc-control 瘦身）。**本轮 fresh-session 重新核验 plateau + 定向 code-reading probe（第 9 angle，R2373-R2383 谱——候选清单列但未深验的 bounded feature）**：先 git 核验（HEAD==origin、R2541 后零 .rs 行为 drift 确证 plateau 真实），再做 fresh probe——① css-conditional `@supports selector()` 核验 = **已完整实现+评估**（matcher/mod.rs:1341 evaluate_supports_condition + is_valid_selector_parse，非 lever）；font-tech/font-format niche（font-wall，非 lever）。② revert-layer + !important 核验 = **实现已正确处理 importance**（cascade.rs:376 cascade_tier_key 含 important 故 important/normal 分属不同 tier，revert-layer 正确同 importance 轴回退；R2388 kill-switch + 2 测试；残余 css-cascade fail = @layer 深 cascade gated 非 revert-layer lever）。③ **`counters()` = 真 gap**：grep 确认 ContentComputedValue 无 Counters 变体（仅 Counter）+ counter 模型 flat HashMap<String,i64>（无作用域栈，无法支持嵌套祖先链）+ **75 driving test**（css/CSS2/generated-content content-counters-001~018+ / content-021~032）。**实施**（clean lever，A/B 三态门禁）：(1) parse_extended.rs 加 `ContentValue::Counters` + `ContentListItem::Counters` 变体 + `parse_counters_call_args`（**关键：separator 为引号字符串可含逗号如 `counters(c, ", ")`，不可按逗号 split**——手写解析取首逗号前 name + 引号包裹 sep + 可选 style）；(2) types.rs `ContentComputedValue::Counters`；(3) apply_advanced.rs 映射；(4) **painter/mod.rs counter 模型 flat→作用域栈**（`HashMap<String,i64>`→`HashMap<String,Vec<i64>>`；update_counters push-on-reset / set-increment-last；get_counter 返 last；新增 get_counter_scopes + pop_counter_scopes；**paint_node 末尾弹本元素 reset 的作用域**——paint_node 单一出口[line 695 orphan-skip 早返在 update_counters 前无压栈] → 所有压栈路径必经 pop）；(5) text.rs Counters paint arm（取全部祖先作用域值按 sep 拼接，无作用域→单默认 0 同 counter() unwrap_or(0)）；(6) tests_5.rs parser 测试（counters/counters+style/逗号分隔符/空分隔符/大小写不敏感/缺参 None/多 item）+ content_objectfit.rs 4 处 test 改 `vec![v]`。**A/B 三态门禁全绿**：① `make test` **13190/0/74**（精确持平 R2563 baseline，零回归）；② `make reftest-upstream FILTER=css/CSS2/generated-content` **211/226 (93.4%)，0 content-counters-* 失败**（counters() 驱动测试 flip PASS；15 fail 全非 counters：before-after-* `display:table-*` 伪元素深布局 + content-NNN 1-2% font-wall）；③ `make product-smoke` welcome **17.03%（81737px 精确持平）** + 全 struct PASS。**零回归证明**：非嵌套 counter-reset（常见情形，含全部 15 fail 用例）old flat-map 与 new scope-stack 产出**相同 counter() 值**（每元素作用域独立 push/pop；旧 leaky 值总在下次 reset 被覆盖）→ 仅嵌套同名（旧 bug）+ counters()（新特性）行为差异。**门禁**：cargo fmt + `cargo clippy --workspace --all-targets -D warnings` 全绿；pre-commit guard PASS。**意义**：**clean-lever surface 经第 9 angle（候选清单残留 bounded feature 定向 code-reading）再证非穷尽**——8 angle 漏扫此 vein；**订正 R2542-R2571「纯 holding」框架**，后续轮应续做定向 probe 非只 plateau-guard；counters() 是 CSS2 核心生成内容特性，net 正 land。**下一轮**：续定向 code-reading probe（候选清单/残余 bounded feature）/ 低频 plateau-guard / 用户点名深结构。

> **📍 R2571（2026-08-04）🔧 doc-control 瘦身续：§最近轮次摘要 底部 14 轮（R2525–R2539）迁出至 [`archive/rounds-r2525-r2539-summary.md`](./archive/rounds-r2525-r2539-summary.md)（100% 逐字保留）；master.md 250→222 行，§最近轮次摘要 现 R2540–R2570（~28 轮健康 recent 窗口）；纯 .md 零 .rs 行为变更**。承接 R2570（跨文档一致性线索收官）。本 session R2562-R2570 累积 9 条 round-note 致 §最近轮次摘要 窗口达 42 轮（R2570→R2525），超 R2556 既定 ~28 健康目标。本轮按 R2555/R2556 sed 按行号低风险模式续 fifo archive。**实施**（先核边界）：① 边界核验 lines 69-71 / 97-99 + `grep -cv` 确认 71-97 全 blockquote/blank 无 must-stay 内容（0 非 note/blank）→ 抽取 lines 71-97（14 轮 R2539→R2525，全 blockquote 体）→ 新 `archive/rounds-r2525-r2539-summary.md`（6 行 header + 27 行原文 = 33 行，首行 R2539 / 末行 R2525 / 14 📍 / era min R2525 max R2539 核验，**100% 逐字保留未删减未重排**）；② master.md `sed -i '71,98d'`（删 28 行含 trailing blank）→ 250→222 行，boundary 核验 R2540(69)→blank(70)→`## 轮次详记归档`(71) 干净（单 blank）；③ §轮次详记归档 + §历史轮次归档索引 各加 R2525–R2539 era 条目。**验证全绿**：master.md 222 行；H2 结构 8 section 全在序；§最近轮次摘要 现 R2540–R2570 = 28 轮（健康 recent 窗口）；archive 文件 33 行含 R2525–R2539 全 14 轮。**意义**：doc-control 瘦身续（R2555 R2459-R2495 + R2556 R2496-R2524 + R2561 R2201-R2458 + R2571 R2525-R2539 = 累计迁出 337 轮）；§最近轮次摘要 从 42 轮恢复至 28 轮健康窗口，master.md 保持 navigable；本 session 9 条 round-note 累积既经 archive 吸收。**门禁**：纯 .md doc 重组零 .rs 行为变更，不跑昂贵 make test；pre-commit guard PASS。**下一轮**：长间隔 plateau-guard（R2563 刚绿，待 .rs drift 仍 zero）/ fresh probe（低概率，8 angle 既穷尽）/ 用户点名授权深结构（最高 value = Phase A IFC line-box-metric 统一，唯一推向 95% 路径）。
