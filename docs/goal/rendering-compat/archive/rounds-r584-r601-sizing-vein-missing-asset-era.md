# 历史轮次归档：R584–R601（sizing vein LANDED 收尾 + missing-asset 扫描/导入 + DC-14 honesty era）

> 从 `master.md` 迁出（内容 100% 保留）。综合结论见 master.md「综合裁决」+ R589（sizing 闭环）/ R601（导入 honesty）。新→旧排列（R601 在上）。

**R601（★ LANDED — code agent 复活执行 R597 turnkey missing-asset 导入：26 资产落 `wpt-data/`，5× stall 清零，DC-14 honesty 实证（false-pass→real divergence）；make test 0 fail / product-smoke 16.97% neutral）**：承 R600 诊断（code agent 自 R588 INACTIVE 是 stall 真因），本会话作为 code agent 执行 R597 turnkey handoff。**导入 26 资产**（writing-modes support 13 含 swatch-blue latent / css-fonts support 5 / css-flexbox support 3 / css-multicol support 1 / css/support 顶层共享 3 / images 1），全部经 `file` 校验为有效 PNG/CSS/SVG（非 HTML 404），上游 raw.githubusercontent 经 `~/use-proxy`(192.168.1.212:7078) 抓取。**两处对 R597 handoff 的实证修正**：① **red-rect.svg 目标 = `css/support/red-rect.svg`**（handoff 原文正确；align-items-007 的 `../support/` 经 URL 解析 `..` 从 css-flexbox 回退到 css/ 再入 support/ = 顶层共享，非 css-flexbox/support/，后者上游 404 经 GitHub contents API 确认不存在）；② **css/support/60x60-green.png + 60x60-red.png 实为缺失**（handoff 误信「已落」= R596 fetch-untracked 但从未 commit，现 gone），经 grep 确认 6 css-text 测试（line-breaking-030/031/032 + hyphens-none-014/015 + ref）+ 多 CSS2 backgrounds 案经 `../../support/` 引用，补齐。**验证（test-guard 包裹）**：① **make test 全绿 0 fail**（fixture-only 变更不改渲染代码）；② **product-smoke welcome 16.97%**（< 22 阈值 neutral，= R587 基线不动）；③ **scoped reftest 确认 DC-14 honesty 预测坐实**：`inline-replaced-vrl-004` **0.00% FALSE PASS → 5.13% REAL divergence**（test+ref 双侧 broken-image 同破→0% 假通过，导入后双侧有图→暴露真实 vertical-rl layout gap = selfsource PASS 数下降但更诚实，同 R546 动态）；`percent-margin-vlr-005` **20.91% → 14.87%**（blue-yellow 图渲染后 divergence 降低）。**oracle A/B 已做（2 案确认 + 全 writing-modes oracle 刷新）**：`capture-oracle-per-dir.mjs --category css/css-writing-modes --skip-existing` 实测重抓全 877 writing-modes oracle PNG（--skip-existing 未生效但无害 = 整 dir 刷新 post-import）；对 2 确认案 REFTEST_DUMP 落 ZeroWeb dump + cross-validate 得 **z_vs_chr**：`inline-replaced-vrl-004` = **6.04%**（pre-import 两侧 broken-image 同破→oracle 亦假一致；现双侧有图→暴露真实 vertical-rl layout gap，honesty 增） / `percent-margin-vlr-005` = **16.64%**（图渲染后残差 = vertical-rl margin/layout gap）。★ **结论 = handoff 预测坐实**：导入**无 clean oracle win**（2 案 z_vs_chr 均 >1%，残差为 writing-modes 轴结构性 vertical-rl clearance/margin gap，R114/R164 4 轮 ruled-out 簇），**价值 = DC-14 honesty**（false-pass/false-consistency 消除，selfsource + oracle 数字更诚实但通过数不升）。全量 writing-modes cross-validate（须 REFTEST_DUMP_PASS=1 全案 dump）留后续 round（2 案已定性，aggregate 数字精化边际递减）。★ **forward motion 解锁**：R590-R600 5× stall 真因（code agent INACTIVE）由本会话消除，missing-asset vein 从「handoff 反复 stall」变「已落位 + 实证」，DC-14 selfsource 数字将更诚实（false-pass 案转 real divergence）。详见 commit。

**R600（doc-maintenance 调研·零代码：★关键诊断修正——并行 code agent 自 R588(03e7ca80, 02:59) 起 INACTIVE（2h 内全仓 0 文件改动，R589-R599 全为 doc agent 提交），missing-asset 导入「6× stall」实为 code agent 未运行非任务卡住；飞书升级准确诊断：须重启/重分配 code agent 或用户 Phase A 决策）**：本轮诊断澄清「6× stall」真因。**全仓活动核查**（`find -newermt '2 hours ago'` 排除 target/node_modules/.git）= **0 文件改动**；**并行 code agent 末次 commit = `03e7ca80`（R588 root 百分比 height，2026-06-25 02:59:42）**；其後 11 commit（101cff78→4aa15097，R589-R599）**全为 doc agent 提交**。**裁决**：missing-asset 导入（R590 hand-off / R597 turnkey）从未被执行的真因 = **code agent 自 R588 后 INACTIVE**（非「任务 stall」/「路径错」/「摩擦」，而是 agent 未运行）；R592-R599 观察到的「stall」是 doc agent 对一个非运行 code agent 的观察。**forward motion 真实 gate**：须 (a) **重启/重分配 code agent**（执行 R597 turnkey 导入 + 续 code 工作）或 (b) 用户 Phase A IFC 多会话决策 或 (c) 授权 doc agent 跨界执行导入（fixtures 非源码，turnkey 低风险，无活跃 code agent 冲突）。doc 侧 role-exhaustion（R599）不变。**飞书升级**（第 3 次，准确诊断）：code agent INACTIVE 自 R588，须用户重启/重分配/决策。零代码变更（read-only 全仓活动诊断）。

**R599（doc-maintenance 调研·零代码：★doc/research role-exhaustion 正式认定（9 轮 R590-R598）——missing-asset 导入第 5 次 stall + turnkey handoff 已在 origin/main 可访问但并行 agent 仍不执行；forward motion 完全 code-agent/user-gated，doc 侧无新独立 lever 可挖，后续 doc 会话勿重扫已穷尽 8 角度）**：本轮仅状态复核（零新调查）。**missing-asset 导入第 5 次 stall 确认**：wpt-data/css/support/ 2 文件未 commit + writing-modes swatches（swatch-yellow/blue-yellow/test-*）全仍缺 + 60min 无 wpt-data 写入；**R597 turnkey handoff 经核已在 origin/main**（`git cat-file -e origin/main:docs/.../missing-asset-import-handoff.md` = YES，并行 agent 可访问）但仍未被执行。**doc/research role-exhaustion 正式认定**（9 轮 R590-R598）：单会话调查 **8 角度全穷尽**（① missing-ref R590 ② missing-asset handoff R590/R597 ③ non-CSS2 CSS-correctness R591 ④ converter audit R591 ⑤ product-fixture 资产 R593 ⑥ skip-list DC-14 R594 ⑦ inline-level box 机制 R595 ⑧ oracle 完整性 R598，**全 clean-negative 或 handoff，0 新独立 lever**）+ 多会话 handoff 全齐（R595 inline-level box 补齐 multicol R581/Phase A R577）+ 导入 turnkey 化（R597）+ 飞书 2 次（R594/R598）。**forward motion 完全 gate**：(a) code agent 执行 turnkey handoff 落导入——5× stall 表明此 agent 路径不可靠，须用户介入（重分配/手执行）或 (b) 用户 Phase A IFC 多会话决策（设计文档就绪）。**裁决**：doc 侧已尽单会话调查 + handoff + escalation 职责，无新独立 lever；**后续 doc 会话勿重扫 8 已穷尽角度**（皆 clean-negative/handoff，重扫零价值）；待 code-agent/用户动作解锁（导入落位 A/B 验证 / Phase A 决策）后继续。零代码变更（read-only 状态复核）。

**R598（doc-maintenance 调研·零代码：DC-14 oracle-shot 完整性审计 = 无 gap（10/10 目录 oracle≥test，over-count=无害 ref）；第 8 个测量角度 clean negative 闭环；★missing-asset 导入第 4 次 stall，飞书指引 turnkey handoff）**：审计 chr<1% oracle 分母是否有「test 缺 oracle PNG → 被排除出分母」缺口（类 R546 ref-denominator 但在 oracle 侧）。**cross-validate.py 逻辑**：遍历 `oracle-shots/*.png`，匹配 `{safe_id}-test.png` dump → oracle 分母 = 有 oracle PNG 且有 reftest dump 的 test。**逐目录 test 文件 vs oracle PNG 计数**：CSS2 6262/9239（oracle 多 2977=ref 亦抓）/ css-text 1302/1912（多 610=ref）/ css-position 103/105 / **css-flexbox 502/502、css-fonts 284/284、css-grid 50/50、css-multicol 455/455、css-tables 119/119、css-text-decor 246/246、css-writing-modes 787/787 全 exact-match**。**裁决**：**10/10 目录 oracle≥test，0 test 缺 oracle PNG**（over-count = ref 文件亦被 capture-oracle-per-dir.mjs 抓，cross-validate 找不到对应 `-test.png` dump 自动 skip，无害）→ chr<1% 分母未被「缺 oracle」收窄，DC-14 测量完整性成立。★ **第 8 个 DC-14 测量/lever 角度 clean negative 闭环**（① missing-ref R590 ② missing-asset R590/handoff ③ non-CSS2 CSS-correctness R591 ④ converter audit R591 ⑤ product-fixture 资产 R593 ⑥ skip-list R594 ⑦ inline-level box 机制 R595 ⑧ oracle 完整性 R598）——**doc/research 调查空间全面穷尽，0 新独立 lever**。**⚠️ missing-asset 导入第 4 次 stall**（R597 turnkey handoff 产出后并行 agent 仍未执行；css/support/ 2 文件未 commit + writing-modes swatches 仍缺）。**飞书已通知**：指引 code agent / 用户到 [`missing-asset-import-handoff.md`](./missing-asset-import-handoff.md)（turnkey，一条 curl loop 落 23 资产）。forward motion gate 不变（code-agent 按 turnkey handoff 落导入 / Phase A 决策）。零代码变更（read-only oracle 计数审计）。

**R597（doc-maintenance 调研·零代码：产出 missing-asset 导入 turnkey handoff doc——消除并行 agent 3× stall 的摩擦根因（session 1 误落路径），23 资产附精确上游 URL + wpt-data 目标路径 + 验证 + A/B 方法论，复制即可执行）**：承 R596 stall #2 + 本轮 stall #3，根因诊断 = 并行 code agent 反复 stall 非缺 spec 而缺 **turnkey 可执行性**（session 1 把文件误落 repo-root `css/` 非 wpt-data、session 2-3 仅落 css/support/ 2 文件即停）。**产出** [`missing-asset-import-handoff.md`](./missing-asset-import-handoff.md)：① **重扫实证当前 23 缺失资产**（排除 intentional broken-image + 已落盘 css/support/60x60-green/red.png），按目录分组（writing-modes 12 / css-fonts 5 / css-flexbox 3 / css-multicol 1 / css/support 1 / images 1）；② **每条附精确上游 URL**（`raw.githubusercontent.com/web-platform-tests/wpt/master/<rel>`）+ **wpt-data 目标路径**（`tests/wpt-runner/wpt-data/<rel>`，明示路径约束消除 session 1 误落）；③ **一条 curl loop 执行流程** + `file` 校验非 HTML 404；④ **验证**（scoped reftest 高 refcount 案，test-guard 包裹）；⑤ **A/B 方法论**（selfsource before/after 预期 false-pass 案变 real divergence=通过数下降=DC-14 honesty；⚠️ oracle PNG 须用 capture-oracle-per-dir.mjs 重抓因 chromium oracle 同样缺图）+ 门禁（make test + product-smoke）。**裁决**：turnkey handoff 把 R590 hand-off 从「需 agent 自行推导 URL/路径」降为「复制执行」，直接消除 stall 摩擦根因；code agent 或后续 session 可一条 loop 落位全 23 资产。doc 侧单会话调查仍 6 角度穷尽（R590-R594）+ 多会话 handoff 全齐（R595）+ 导入 turnkey 化（R597）；forward motion gate 不变（code-agent 按 handoff 落导入 / Phase A 决策）。零代码变更（read-only 重扫 + handoff doc 产出）。

**R596（doc-maintenance 调研·零代码：missing-asset 导入机制 proof-of-mechanism 验证 + 第 2 次 stall 确认——css/support/ 落盘后 ZeroWeb 渲染资产生效，但并行 agent 二度 stall，writing-modes swatches 仍未迁）**：核验并行 code agent 对 R590 hand-off 的恢复进度。**proof-of-mechanism 验证（scoped reftest，60x60-green.png 已落 wpt-data/css/support/）= 机制生效**：flex-minimum-width-flex-items-007.xht **0.00% PASS** / flexbox-min-width-auto-006.html **3.33% FAIL**——资产在盘上即被 ZeroWeb 正常渲染（非 "image not found" 退化），证 R590 hand-off 的资产导入路径正确，writing-modes swatches 同机制落位即可生效。**但导入第 2 次 stall**：wpt-data/css/support/ 仅 2 文件（60x60-green/red.png）**未 commit 且 30+min 无写入**；writing-modes swatches（swatch-yellow/blue-yellow/test-tl/swatch-blue，item ①）**仍缺**（wpt-data 仍 25 pre-import）；items ②-④（css-fonts/flexbox/text/multicol support）**未动**。**裁决**：导入机制经实证可行（资产落 wpt-data 即生效），但并行 agent 二度 stall（首次 abandoned 删 staging、二次仅落 css/support/ 2 文件即停），R590 hand-off 经此轨迹仍不会完整落位——须 code agent 稳定完成 ①-⑤ + commit，或重新分配。doc 侧已 3 轮 escalate（R592 de-risk / R594 abandoned flag / R596 mechanism proof + 2nd stall）+ 飞书通知；单会话调查 6 角度穷尽（R590-R594）+ 多会话 handoff 全齐（R595 inline-level box 补齐）。forward motion gate 不变（code-agent 稳定落导入 / Phase A 决策）。零代码变更（read-only scoped reftest + 状态复核）。

**R595（doc-maintenance 调研·零代码：inline-level box 簇（014 inline-table）精确根因 + 修复 seam 定位 = Phase-A-blocked 确证——填 inline-level box 无 dedicated handoff 之缺，区别 multicol R581/Phase A R577）**：承 R594 后 forward-motion 列表的 inline-level box 项（含 014 inline-table，R589 PIL 定性 defer 但未 trace 修复 seam）做函数级代码核查。**精确根因链**：① `convert_display`（converter/mod.rs:264）把 `InlineTable` 映射 `taffy::Display::Block`（同 `InlineBlock`:275）→ taffy 以 block 布局 inline-table；② `collect_inline_items`（inline/mod.rs:799）识别 inline-table 为 inline-level，从 `inline_block_sizes: HashMap<NodeId,(w,h)>` 读尺寸；③ **`ib_sizes` 构造**（inline_finalization.rs:650-690 / engine.rs）只取 inline-level 盒**自身显式** width/height，经 `resolve_inline_block_dimension`（text_metrics.rs:168）——**Auto/Percentage/MinContent 一律返回 0.0**（"暂不支持"），且 `if resolved_w>0||resolved_h>0` guard 跳过全 auto 盒；④ 014 inline-table `height:auto`（仅其 *cell* `height:1in`）→ 自身尺寸 auto→0→**不入 ib_sizes**→ IFC 见 inline-table height=0；⑤ R180/R368 `shrink_inline_blocks_to_content`（float_positioning.rs:111）**只算 content-based WIDTH，无 HEIGHT 对偶**→ inline-table 内容高（cell 1in=96px）永不传 IFC → 行盒留 line-height ~25px → cell 塌缩 + Y 偏 ~13px（block 定位 vs inline-level baseline 偏移）。**修复 seam**：content-based HEIGHT for inline-level boxes（`shrink_inline_blocks_to_content` 的 height 对偶，或 ib_sizes 构造补 content-height），把 inline-table/inline-block 内容高（经 table.rs/block 布局）传 IFC 行盒。**裁决 = Phase-A-blocked，非 narrow viable-slice**：content-based height 须 inline-level 盒**自身内容布局先定型**（inline-table 须经 table.rs 后处理得 cell 1in 高）**再喂父 IFC 行盒** = 两遍布局 ordering 死锁，同 R125/R198/R205（IFC line-height/font_size 两遍死锁）+ R109（inline-level 盒 ownership）谱系。★ **填 handoff 缺**：forward-motion 列表的 5 多会话项里 multicol 有 R581/R582 handoff、Phase A 有 R577 scoping、baseline-export/writing-modes 各有 ruled-out 记录，唯 **inline-level box（含 014）此前无 dedicated 机制 + seam 记录**——本轮补齐（seam = content-based height 对偶，阻塞 = 两遍 ordering 死锁），供 Phase A 决策时权衡。零代码变更（read-only 代码核查）。

**R594（doc-maintenance 调研·零代码：reftest skip-list DC-14 合规审计 = 全合规（24 specific skip + 全 category skip 均 legitimately out-of-scope）；doc/research 单会话调查第 6 角度穷尽；★飞书已通知用户 consolidated 卡点）**：审计 `tests/wpt-runner/reftest-skip-list.txt` 是否违反 DC-14（goal line 167「不允许跳过范围内但已知的困难 case 或预期会失败的 case」）。**category skip 全合规**：SVG/canvas/webgl/webgpu/offscreencanvas/animations/transitions/media/pointer/editing/dom 等 = goal 明确排除项（line 93/95）。**24 specific test skip 全合规**：逐案核 `<script>` + DOM markers——5 个名似静态的可疑案（column-rule-001 / auto-margins-001 / aspect-ratio-intrinsic-size-012/013 / abspos-containing-block-initial-001）**均含 `<script>` + getElementById/.style./document. markers**，余 19 个名含 dynamic-/change/scroll/insert/toggle/resize 显式 JS 动态；全部 = JS/DOM-API 依赖（goal line 93 排除 JS/DOM API 兼容 + line 51 注明 V8 sandbox 不提供 DOM API），**legitimately out-of-scope，非「困难 case 跳过」**。**裁决**：skip-list DC-14 合规，无范围内静态测试被跳过抬通过率。★ **doc/research 单会话调查第 6 角度穷尽**（① WPT missing-ref R590 ② missing-asset R590 hand-off ③ non-CSS2 CSS-correctness R591 ④ converter audit R591 ⑤ product-fixture 资产 R593 ⑥ skip-list DC-14 R594）——**6 角度全收敛「无单会话 lever」**。**飞书已通知用户**：CSS-correctness vein（R491-R588 ~30 win）已闭 + missing-asset 导入 stalled + Phase A 须用户决策。**⚠️ R594 末更新：missing-asset 导入经核 = ABANDONED（并行 agent 删除 repo-root `css/` staging 目录，无新 commit，wpt-data 仍 25 文件 pre-import，swatch-yellow/blue/blue-yellow/test-tl 全仍缺）**——R590 hand-off 经此轨迹不会落位，须 code agent 重新拾起或重新分配。forward motion gate = code-agent 导入重拾落位 / Phase A 决策。零代码变更（read-only skip-list 审计 + wpt-data 复核）。

**R593（doc-maintenance 调研·零代码：产品 fixture（DC-13）资产完整性扫描 = 无 gap——welcome 0 缺失 / wintertc static 全在（R318 已验）/ morning-work cc_unavailable.png = intentional broken-image 非缺口；资产完整性谱系（WPT + 产品 fixture）全清；★doc/research 单会话调查 5 角度全穷尽，forward motion 全 gate 在并行导入落位 / Phase A 用户决策）**：对 3 个产品 fixture（`apps/browser/assets/{welcome.html, wintertc/index.html, morning-work/article.html}`）做 R546/R527-style 资产完整性扫描（img src / link stylesheet / css url()）。**welcome.html = 0 缺失** ✓。**wintertc = `/static/` 资产全在**（logo.svg + 14 logos 在 `static/logos/`，R318 已验渲染正确；绝对路径 `/static/` 非 fs-root 缺口）。**morning-work = `images/cc_unavailable.png` 「缺失」实为 intentional broken-image**：文件名 `cc_unavailable` 本身表明是 Creative Commons 徽章外链录制时不可用（原作者 hotlink creativecommons.org 徽章，录制 fixture 时 404 命名为 cc_unavailable）→ 同 WPT `src="404"` intentional 类（R590 已正确排除），**非 gap，勿导入**（导入真 CC 徽章会背离原页实际渲染）。**裁决**：产品 fixture 资产完整性无 gap。★ **资产完整性谱系（WPT + 产品 fixture）全清**：WPT missing-ref（R590: 0）/ WPT missing-asset（R590 hand-off，导入 stalled）/ 产品 fixture（R593: 无 gap）。★ **doc/research 单会话调查 5 角度全穷尽**：① WPT missing-ref（R590 闭）② WPT missing-asset（R590 hand-off，并行导入 stalled 待落位）③ non-CSS2 CSS-correctness 单点（R591 闭）④ converter 层审计（R591 clean）⑤ 产品 fixture 资产（R593 闭）。**forward motion 全 gate**：(a) 并行 code agent missing-asset 导入——**stalled**（item ① fetch 后 03:50 起未 relocate/补 swatch-blue/做 ②-⑤/commit，多个 doc 轮无进展）或 (b) 多会话 Phase A IFC（R314 飞书用户决策 pending）。doc 侧已尽单会话调查空间，须 code agent 复活导入或用户决策 Phase A。零代码变更（read-only 资产扫描）。

**R592（doc-maintenance 调研·零代码：R590 missing-asset 导入 in-progress 状态 de-risk——并行 code agent 已 fetch R590 hand-off item ① writing-modes 53 support 文件，doc 侧只读核验资产有效性 + 标 2 blocker + 队列 ②-⑤ pending）**：核验并行 code agent 对 R590 hand-off（writing-modes swatch/quadrant 簇）的 in-progress 导入。**资产有效性核验（read-only `file`/`stat`，未触碰 code agent untracked 文件）= 52/53 合法 PNG**：R590 标记缺失的 swatch-yellow（15×15 1-bit）/ blue-yellow-206w-165h（206×165）/ test-{tl,tr,bl,br}（100×100 4-bit 象限图）均**已 fetch 且为有效 PNG**（非 404/HTML 错误页，无 < 100B 损坏——swatch-green/yellow 84B 是合法 15×15 小图）。**2 blocker（code agent 待解）**：① **路径错**——fetch 落 repo-root `css/css-writing-modes/support/`（53 文件），但 runner 读 `tests/wpt-runner/wpt-data/css/`（wpt-data 位置仍 25 文件 pre-import 状态）→ 若按当前位置 commit 导入**无效**，须 relocate 到 `tests/wpt-runner/wpt-data/css/css-writing-modes/support/`；② **swatch-blue.png 仍缺失**（即便在已 fetch 集内）须补 fetch。**队列 ②-⑤ 全 pending**：css-fonts（font-variant-features/variation-sequences.css + font-weight-*-ref.png，11 test）/ css-flexbox（test-style.css/flexbox.css，9 test）/ css-text+multicol（5 test）/ css/support 顶层共享目录（60x60-green.png 等）**均未 fetch**。**裁决**：导入 in-progress 有效但未落位（路径错 + swatch-blue 缺 + ②-⑤ pending），doc 侧已 de-risk（资产内容合法，无 404/损坏）；落位 + 补齐后须 A/B（selfsource + 重抓受影响 case oracle PNG）验证 R590 预测（false-pass 消除 / modest yield）。doc/research 单会话 lever 空间仍全穷尽（R589-R591），forward motion = code-agent 导入落位（in-progress）+ 多会话 Phase A IFC（R314 飞书用户决策 pending）。

**R591（doc-maintenance 调研·零代码：non-CSS2 CSS-correctness 单点 lever 扫描 = EXHAUSTED——converter 层审计 comprehensive 无 applied-but-wrong 值 bug + grid near-miss 2 案实证 brittle oracle 亚像素噪声非值 bug + scrollbar-width 0 footprint；R513 dead-value 后 (c) 谱系最后子分支闭环，doc/research 单会话 lever 全穷尽）**：承 R590 CONTINUE 续扫 non-CSS2 全量目录的 CSS-correctness 单点 lever（applied-but-wrong / unimplemented-clause 类，R513 仅扫 dead-value / R491–R511 仅覆盖 CSS2）。**converter 层审计**（`crates/layout-engine/src/converter/mod.rs` 1628 行 `computed_style_to_taffy` 主路径 + 全 convert_* 子函数）= **comprehensive 无 applied-but-wrong 值 bug**：无 TODO/FIXME、值路径无 `unwrap_or(0)`；仅 3 处 hardcoded 均合理——scrollbar-width（Auto=15/Thin=8/None=0 platform 默认）/ gap 简写回退（col/row==Length(0)→shorthand，正确 CSS）/ visibility:collapse flex-basis=Length(0) strut（R111/R145）。**grid near-miss 2 案 scoped reftest 实证 = brittle oracle 亚像素噪声非值 bug**：grid-item-percentage-quirk-001（quirks + grid + canvas height:200%，ref 在 wpt-data/quirks/reference/ 经 `../../` 正确解析，selfsource **0.00% PASS**，R560 报 1.21% 是 z_vs_chr oracle 亚像素发散非值 bug）/ grid-with-aspect-ratio-uses-content-box-height-for-track-sizing（selfsource **0.42% PASS**，R560 报 1.05% 同为 oracle 噪声）→ 两案 selfsource 均 PASS，>1% 全是 chromium-oracle 亚像素/字体噪声带（R560 「9 case 落 1.05%-1.43% brittle」结论坐实）。**scrollbar-width hardcoded = 0 WPT footprint**（全 10 目录 0 reftest test 用）→ 按 code-guidelines §2 latent 非 lever。**裁决**：non-CSS2 CSS-correctness 单点空间（applied-but-wrong / unimplemented-clause，区别 R513 dead-value）**EXHAUSTED**——converter clean + near-miss=噪声 + R513 dead-value 闭 + R519 code-side 5 区闭 + R568 两 lever complex/structural 闭。★ **(c) 谱系 doc/research 单会话 lever 全穷尽**：sizing（R589 闭）/ missing-ref（R590 闭）/ non-CSS2 CSS-correctness 单点（R591 闭）；唯一剩余 actionable = R590 missing-asset **code-agent** 导入工程（非 doc/research 渲染 lever）。★ **forward motion 全多会话架构**：Phase A IFC 统一（设计文档就绪，R314 飞书已通知用户决策）/ multicol fragmentation / baseline-export / writing-modes 轴 / inline-level box-model（含 014 inline-table）。零代码变更（read-only converter 审计 + scoped reftest）。

**R590（doc-maintenance 调研·零代码：R546-style 分母真实性双半扫描——missing-ref = 0/10232 彻底穷尽（clean negative）+ missing-asset = 63 test 文件 / 24 资产活簇 hand-off code agent；R589 sizing 子分支外的 (c) 谱系再扫，missing-ref 子分支亦闭环）**：承 R589 CONTINUE 做 R546-style 分母真实性系统复扫（runner `resolve_ref_path` 语义复刻 + 静态扫描全 10 目录 10232 个 rel=match / img src / link stylesheet href + scoped reftest 验证）。**发现 A（missing-ref half）= EXHAUSTED clean negative**：全 10232 非 URL rel=match href 用 runner 同款解析（绝对 `/css/...` → wpt-data base join、相对 → test parent join）后 **0 缺失**（0 test 含缺失 ref）→ R546（缺失共享 ref）+ R551（绝对路径解析）+ R552（尾随空格 + 3 ref）+ R553（about:blank）四件套**彻底闭环** missing-ref 分母缺口，R552「剩 about:blank 唯一 Warning」现 100% clean，**此 half 无剩余 lever**。**发现 B（missing-asset half）= R527/R529/R532 谱系活簇，hand to code agent**：排除 intentional broken-image（src="404"/"invalid.jpg"/"broken"）后，**63 个 test-side 文件（have rel=match 非 ref）引用 ≥1 缺失 support 资产**，按目录 css-writing-modes 38 / css-fonts 11 / css-flexbox 9 / css-text 4 / css-multicol 1；**24 unique 缺失资产**，≥3 referrers 高影响 11 个，writing-modes swatch/quadrant 簇主导（blue-yellow-206w-165h.png t18 / test-{tl,tr,bl,br}.png + swatch-yellow.png 各 t10）。**结构性根因 = support 目录部分导入（非解析错）**：css/support/ 顶层共享目录**整目录缺失**（60x60-green.png t6 等）+ css-writing-modes/support/ 部分导入（25 文件在，11 核心 swatch/quadrant 缺）+ css-fonts/support/ 仅 1 文件（font-variant-features.css/variation-sequences.css/font-weight-*-ref.png 缺）→ import 脚本分批抓 reftest pair 时未同步抓 support 资产（R527/R529/R532 曾补 img 但 css/stylesheet + writing-modes swatch 未覆盖）。**scoped reftest 验证（4 受影响 writing-modes 案）= 混合结果**：inline-replaced-vrl-004 + float-contiguous-vrl-002 **0.00% FALSE PASS**（test+ref 双侧同破无图→0% 差异，DC-14 可信度问题）/ central-baseline-alignment-002 3.94% 容差内过 / percent-margin-vlr-005 **20.91% REAL divergence**（缺图致发散）。**裁决**：missing-asset 杠杆**主要价值 = DC-14 metric credibility**（消除 false-pass：writing-modes selfsource 78.0% 含部分「双侧同破 0.00%」假通过；导入后变 real divergence，selfsource 数字会**下降**但更诚实，同 R546「149 PASS + 12 诚实暴露」动态）+ **次要价值 = modest potential yield**（real-divergence 案若 ZeroWeb layout 本正确则 clean win；但 writing-modes oracle 已 5.6% 残余归因 vertical-rl clearance+font/metric，多数缺图案导入后暴露更深 layout bug 而非翻 PASS）。**⚠️ oracle 关键约束**：oracle PNG（2026-06-21 抓）从同一 wpt-data 渲染→chromium oracle **同样**看到缺图；导入资产后须**重抓受影响 case oracle PNG**（capture-oracle-per-dir.mjs）才 A/B oracle 真变化。**hand-off 顺序**：① writing-modes swatch/quadrant 簇（11 资产/38 test，ROI 最高）→ ② css-fonts font-variant-features/variation-sequences.css + font-weight-*-ref.png（11 test）→ ③ css-flexbox test-style.css/flexbox.css（9 test）→ ④ css-text+multicol 残余（5 test）→ ⑤ css/support/ 顶层共享目录；每批 A/B（selfsource + 重抓 oracle）守全量+product-smoke。详见 [`evidence/r590-missing-asset-denominator-scan-2026-06-25.txt`](./evidence/r590-missing-asset-denominator-scan-2026-06-25.txt)。★ **与 R589 综合**：(c) 谱系 sizing 子分支（R589 闭）+ missing-ref 子分支（R590 闭）均穷尽；missing-asset 子分支开放但属资产导入工程（code agent）非 doc/research 渲染 lever；项目仍处 firm structural plateau。

**R589（doc-maintenance 调研·零代码：R583–R588 sizing vein LANDED 实证复核 + 014 inline-table 残余根因定性 = inline-level box 渲染（非 sizing 杠杆）；sizing/percentage 单会话 clean lever 全闭环穷尽；项目回 firm structural plateau）**：承并行 code agent 连续 LANDED R583–R588（6 个 table/percentage sizing clean win）做 doc-side 实证复核 + 残余定性。**实证 1（scoped reftest，test-guard，`display:table` 013 簇）= R583–R588 全 LANDED 确认**：min/max-width-013 + min/max-height-013 + width/height-013 **6/6 PASS 0.00%**（R584 table min-width 列同步 / R585 table min-height 行分配 / R586 table max-height 两部件 cap / R587 百分比 min-height / R588 根百分比 height 相对 ICB 全坐实）。**实证 2（残余 014 inline-table 簇定性）= inline-level box 渲染缺口，非 sizing 杠杆**：min/max-width-014 + min/max-height-014 + width/height-014 **8/8 FAIL 全同 1.39% / 6650px**（无论 min/max/width/height 哪个属性 diff 完全相同）→ 证明 inline-table 外层盒渲染本身错（与 sizing 属性值无关，table.rs sizing 对 `display:table` 已全对）。PIL bbox 精确定性双症状：① **min-height-014**：TEST 黑仅 2639px、cell 塌缩到 ~25px（一行 line-height）vs REF 10160px（96×96 方块）= **inline-table 高度塌缩到行盒高**（cell `height:1in` 未应用到 inline-table 外层盒）；② **min-width-014**：TEST 黑方块在但 **y 偏高 ~13px**（inline-table 作 inline-level 盒在父 IFC 中垂直定位偏移）。两症状均为 **inline-table 作 inline-level 盒的 shrink-to-fit/height/vertical-position** 行为。**裁决**：014 inline-table = **inline-level box-model 簇**（同 R180 shrink-to-fit / ifc-011 inline-block / R109 匿名盒 谱系），非单会话 clean sizing lever，defer 到 Phase A IFC / inline-level box 统一。**★ sizing/percentage 单会话 clean lever 全闭环**：R583（cell min/max-W）+ R584（table min-W）+ R585（table min-H）+ R586（table max-H）+ R587（百分比 min-H）+ R588（根百分比 H/ICB）= R548/R177b/R119 谱系的 **cell/table × W/H × min/max × percentage 全对称补全**；百分比高度三件套（R119 max-H + R587 min-H + R588 根链）完成 → 任意元素百分比 height/min-height/max-height 相对明确包含块（含根→视口）全解析。**R546/R549 next-lever 队列全清**（currentColor R550 done / applies-to cell+table R583–R586 done / 百分比 R587–R588 done / 014 inline-table=inline-level 非 sizing / blocks-017 + abspos-width-005 已判 structural）。**★ 项目回 firm structural plateau**：剩余前向全多会话架构（Phase A IFC 统一 ~2200 案 / multicol fragmentation / baseline-export / writing-modes 轴 / inline-level box-model 含 inline-table 014）。零代码变更（read-only + scoped reftest 验证）。

**R588（★ LANDED — 根元素百分比 height 相对 ICB（视口）解析；CSS §10；normal-flow 净 +1 / 0 回归跨 4 dir；R587 min-H 后根/ICB 链补全，完成百分比高度三件套）**：承 R587 CONTINUE 查 min-height-percentage-003（html,body height:100% + p min-height:100%）FAIL 96.62%。**根因**：`clamp_percentage_max_height` 根调用（engine.rs:341）传 `cb=None` → 根元素（如 `<html>`）的百分比 height/min-height/max-height **从不相对 ICB 解析** → html height:100% 不解析 → body/p 百分比高度链断裂（p 不填满视口，红背景露出 96.62%）。**修复**（1 行 + 注释）：根调用传 `Some(self.viewport_height)` 作包含块内容高度（CSS §10：根元素百分比高度相对 ICB=视口）；后代经既有 `my_definite_content_height` 链传播明确定高度。**A/B（test-guard stash + 多 dir）**：min-height-percentage-003 **FAIL 96.62%→PASS 0.00%**；normal-flow **645→646 净 +1**；css-tables 80→80 / css-position 64→64 / css-flexbox 368→368 **全 0 回归**；product-smoke welcome **16.97%**（<20% neutral）；make test 全绿。门禁：clippy −D warnings clean、fmt clean。★ **完成百分比高度解析三件套**：R119（百分比 max-H）+ R587（百分比 min-H）+ R588（根/ICB 链）= 任意元素百分比高度/min-height/max-height 相对明确包含块（含根→视口）全解析。★ sizing/percentage clean lever 连续第 6 win（R583-R588）。详见 commit。

**R587（★ LANDED — 百分比 min-height 解析；CSS §10.7；normal-flow 净 +3 / css-tables +1 / 0 回归；R119 百分比 max-height 的 min 对称补全）**：承 R586 CONTINUE 扫 normal-flow FAIL 簇定位 min-height-094/095（min-height:100% of 明确高度父）FAIL 2.00%。**根因**：`clamp_percentage_max_height`（engine.rs:1433）R119 已解析**百分比 max-height** 相对包含块明确高度，但**百分比 min-height 无对称解析** → min-height:100%（父 height:1in 明确）被忽略，div2 内容高 0 渲染（ref 96px 黑方缺失，darkpx 10174 vs ref 19774）。**修复**（engine.rs +18 行，1a block，对称于 R119 1) block）：百分比 min-height 相对 cb_content_height 解析为内容高度**下限**（floor content_height，box-sizing 一致），置于 max-height block 之后使 min 优先于 max（§10.4）。**A/B（test-guard stash）**：normal-flow **642→645 净 +3**（min-height-094/095、min-height-percentage-001/002 FAIL→PASS）/ css-tables **79→80 +1**（百分比 min-height 表案）/ 0 回归（Failed 104→101、35→35... 实测 css-tables 36→35）。门禁全过：make test 全绿、clippy −D warnings clean、product-smoke welcome **16.97%**（<20% neutral）、fmt clean。残余：min-height-094/095 0.24%（sub-px，过阈值）；min-height-percentage-003 96.62%（百分比 min-height 对**非明确**高度父或不同机制，独立 lever）。★ **sizing clean lever 连续第 5 win**（R583 cell W + R584 table W + R585 table min-H + R586 table max-H + R587 百分比 min-H = R548/R177b/R119 谱系跨 cell/table/W/H/percentage 全对称补全）。详见 commit。

**R586（★ LANDED — table 元素 max-height cap 两部件；CSS §10.4；normal-flow self-source 净 +1 PASS / 0 回归；R585 min-H 后 max-H cap 对称补全）**：承 R585 CONTINUE 攻 max-height on table（max-height-applies-to-013/014 FAIL 3.84%）。**根因（两部件，probe 实证）**：① engine.rs `clamp_percentage_max_height`（R168 table height 下限，line 1480）在 `apply_table_size_constraints` 已 cap table box 到 max-height(96) **之后**，用 `style.height`(288) 重新 floor 回 288 → **覆盖 max-height cap**（probe: apply_table_size_constraints 内 table_box.h=96，但渲染 288，因 R168 后跑覆盖）；② table.rs `row_extras`（R585 行高分配）只用 height 不 cap max-height → 行展开到 288 溢出 96 table box。**修复（两处）**：① engine.rs R168 floor 先 `spec.min(max_height_content)`（§10.4 max cap 优先于 height）；② table.rs row_extras 分配目标从 `max(height,min)` 扩为 `clamp(height, min, max)`（max cap、min floor、min 优先）。**A/B（test-guard stash）**：max-height-applies-to-013 **FAIL 3.84%→PASS 0.00%**；normal-flow **641→642 净 +1 / 0 回归**；css-tables 79→79（0 flip）；014（inline-table）3.83→1.39 改善但仍 FAIL（inline-level 残余，同 014 谱系）。门禁全过：make test 全绿、clippy −D warnings clean、product-smoke welcome **16.97%**（<20% neutral）、fmt clean。★ **table-sizing clean lever 连续第 4 win**（R583 cell min/max-W + R584 table min-W + R585 table min-H + R586 table max-H = R548/R177b col-W 的 W/H × min/max × cell/table 全对称补全）。★ 教训：max-height on table 涉及 layout 后处理（apply_table_size_constraints）+ engine 通用 clamp（R168）**两部件顺序耦合**——单修一处无效（apply cap 被 R168 覆盖 / 行未同步），须双修。详见 commit。

**R585（★ LANDED — table 元素 min-height 行高分配；CSS Tables §17.5.3 + §10；normal-flow self-source 净 +1 PASS / 0 回归；R584 width 后 height 对称补全）**：承 R584 CONTINUE 查 height 对称缺口（min-height/max-height on `display:table`/`inline-table`，013/014 全 FAIL）。**根因**：`apply_table_size_conditions`（table.rs:1972-1980）**已**把 table box 高度 floor 到 min-height，但 `position_cells` 的 `row_extras`（R89 行高分配，table.rs:1651-1662）只读 table `height` **不读 min-height** → 行高留在内容高（empty cell ~20px strut），table box 96px 但行/单元格 ~20px → min-height-applies-to-013 无 96px 黑方（cell height = `row_height.max(content)`，table.rs:1844，行不高则 cell 不高）。**修复**（row_extras 的 `target_content_h`，+18 行）：分配目标从 `height(Px)` 扩为 `max(height, min-height)`（均 Px，box-sizing 一致扣 padding/border），min-height > 内容时把缺口均分到行使 cell 填充。**A/B（test-guard stash）**：min-height-applies-to-013 **FAIL 1.52%→PASS 0.00%**；normal-flow **640→641 净 +1 / 0 回归**；css-tables 79→79（0 flip）；014（inline-table）2.31→1.39 改善但仍 FAIL（inline-level 残余，同 R584 014 谱系）；max-height-013/014 不受影响（cap 方向不同，独立 lever）。门禁全过：make test 全绿、clippy −D warnings clean、product-smoke welcome **16.97%**（<20% neutral）、fmt clean。★ **table-sizing clean lever 连续第 3 win**（R583 cell min/max-W + R584 table min-W + R585 table min-H = R548/R177b col-W 的 width/height × cell/table 对称补全），证 applies-to 残余 min/max-W/H on cell/table 多为逐级遗漏 clean 单点。详见 commit。

**R584（★ LANDED — table 元素 min-width 列宽同步；CSS Tables §17.5.2 + §10；normal-flow self-source 净 +1 PASS / 0 回归；R583 cell 级后 table 级对称补全）**：承 R583 CONTINUE 查 R546 残余 applies-to 013/014（min-width on `display:table`/`inline-table` 元素本身）。**根因**：`apply_table_size_conditions`（table.rs:1907-1928）**已**把 table box 宽度 floor 到 min-width，但 `compute_column_widths` 返回的**列宽未同步** → table box 96px 但列仍 = intrinsic（empty cell 9.6px，probe 实证非 0）→ cell 按 9.6 渲染无 96px 黑方。**修复**（compute_column_widths 末尾 +20 行）：读 table min-width（Px，box-sizing 一致于 apply_table_size_conditions），当 `min_cw > cur_total` 且**所有非折叠列均 auto**（无显式 width）时按比例 `*= min_cw/cur_total` 放大列到 min-width。仅 all-auto 整体放大避免触碰有显式 width 列的 auto-column 分布语义（须单独按 auto 列分配，留独立 lever）。**A/B（test-guard stash）**：min-width-applies-to-013 **FAIL 1.72%→PASS 0.00%**；normal-flow **639→640 净 +1 / 0 回归**；css-tables 79→79（0 flip）；014（inline-table）1.87→**1.39%** 改善但仍 FAIL（inline-table 残余 = inline-level 定位/方框渲染独立子问题，非本 lever）。门禁全过：make test 全绿、clippy −D warnings clean、product-smoke welcome **16.97%**（<20% neutral）、fmt clean。★ **table-sizing clean lever 连续第 2 win**（R583 cell min/max + R584 table min-width，R548 col/colgroup 的对称补全谱系），证 R546 applies-to 残余非全结构性——cell/table 级 min/max-width 是 R548/R177b col-width 的逐级遗漏，clean 单点。详见 commit。
