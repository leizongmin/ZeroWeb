# 归档：master.md header R351-R380 详记（2026-06-20，从 header 迁出）

> 本文件为 master.md header **最后更新**行中 R351-R380 轮的逐轮详记，于 R385（2026-06-21）header 治理时迁出，防 header 无限增长（goal doc 治理规则）。R381+ 详记保留在 master.md header；更早轮次见各 archive。

---

前轮 R380：**CSS App E painting-order sort-key 修复 net-negative，已回退——需完整 stacking-context 算法**。承接 R379（识别 painting-order 真 bug：`child_paint_sort_key` 把 z-index:auto 定位元素画在 floats 之前，违反 CSS App E），实测把 z-auto 分支 `(1,0)`→`(3,0)`（画在 floats 之后、与 z>=0 stacking context 同层）：self-source **443→442/490 净 -1**——回归 `flex-item-position-relative-001`（正是 c3543e8 `(1,0)` 近似为之而设的嵌套定位案），**0 改善**（当前无失败案是纯 positioned-over-float；float-006 还需 R379 bug ①）。**关键洞察**：sort-key 调参**内在冲突**——z-auto 在 floats 前=对嵌套定位正确但对 positioned-over-float 错；z-auto 在 floats 后=反之。真正修复须实现**完整 CSS App E stacking-context 树遍历**（把定位子树画进父 stacking context 的 step 6，定位后代按 tree-order 与其他 step-6 项交错），非扁平 child sort 可解。裁决：painting-order sort-key 修复加入 ruled-out（非当前 lever，同 Wall ③），fix 已回退。本轮零代码变更。前轮 R379：**float-006 深查——2 entangled bugs，piecemeal 修为 net-negative，已回退**。承接 R378「re-investigation 可纠正误诊」，再审 float-006（7.47%，floats-clear）。读 test+assert+ref 确证根因：WPT assert「零高度（margin-box）空浮动元素不缩短行盒，会滑入行盒顶部」+ ref 是单一 14em×10em 绿矩形。**两 bug 耦合**：① 18em 宽零高度浮动（`#zero-height-first-float`）错误地在水平方向排除红色浮动（推到 x=296），应不排除（红应在 x=8 绿色 abs 盒下）；② `child_paint_sort_key`（painter/mod.rs）把 z-index:auto 定位元素（绿色 abs 盒）画在 level 1（**先于** floats level 2），故红浮动盖住绿——违反 CSS App E（positioned step 6 应在 floats step 4 之后）。**修复 bug ①**（float_positioning.rs 给 `left_used_width/right_used_width` 累加加 `child_outer_height>0` 守卫）：实测红正确移到 x=8 绿下，但 float-006 **仍 FAIL**（bug ② 暴露：绿被红盖），且**回归 clear-float-002 + float-with-absolutely-positioned-child-with-static-inset**（self-source 443→441 净 **-2**，0 改善）。即便再修 bug ② 仍净 -1（2 回归是 float 定位非 paint 顺序）。**裁决**：float-006 非 clean slice（2 bug 耦合，piecemeal 修 net-negative），fix 已回退；加入 ruled-out。本轮零代码变更（fix 已回退）。前轮 R378：**empty-inline-002 诊断纠正——非 paint bug，是结构性形状失配**。R377 据 band 分析误判「span 绿色填充 y=215-550 缺失 = paint bug」；本轮加 PAINTDBG 插桩（`paint_node` 入口打印 node/display/abs/w/h/border/padding/bg/r109_split/frag_ids）+ 逐像素采样**纠正**：span（NodeId 43，display=Inline）**被 paint_node 访问且几何正确**（abs=(133,215) w=250 h=350 bt/bb/bl/br=25 pt/pb=100 bg=green，`skip_split_inline_deco=false`），像素 (250,220)/(250,300)/(250,400) 实测**全绿** (0,128,0)——span 渲染正确，**非 paint bug**。真 25.78% diff = test 与 ref **结构性形状失配**：test 用 div1 `margin-top:100` + div3 `position:relative;top:-150;z-index:-1` + div3>div `top:-125` 的复杂嵌套定位产出绿色形状（内容起 y=135），ref 用更简单结构（绿色起 y=35），两者形状/位置不同；relative offset 实测**已应用**（div3 box.y=250 含 -150 偏移）。属 large-font/嵌套定位结构性簇（R125），**非 clean slice**。本轮零代码变更（PAINTDBG 插桩已回退）。前轮 R377：**Phase A 设计勘误——Wall ③ 证伪为非 lever + Gate 现状澄清**。承接 R376（CONTINUE 误指 Wall ③），精读实际存储代码（`inline_finalization.rs:300-339`）发现 v1.2 设计文档的 Gate 2 描述（`lines.len()<=1 && is_pure_ahem`）**已过时**——R207 PHASEA_STORE_EXT + R355 多行放宽后实际已覆盖直接文本容器 + 纯 inline 叶文本容器。**关键证伪**：Wall ③（混合 inline+block 内容存储排除，line 308）**非当前 lever**——47 失败案无一依赖其移除（border-bottom-width-006=匿名块盒*生成*缺失 R361 非存储；余皆 multicol/baseline/WM/taffy/large-font 簇），移除仅重演 R206 回归（ifc-001/002/003）无 upside。empty-inline-002（25.78%）深查：空 span box 几何**正确**（LAYOUT_DUMP h=350=100 line-height+200 padding+50 border）+ paint `skip_split_inline_deco=false`（painter/mod.rs:385，非 R109-split）故 bg **应**绘制，但绿色填充 y=215-550 **缺失**（仅底部 y=550-575 渲染），根因未定位（paint-traversal 问题），非 clean slice。设计文档已加 **v1.3 勘误**（实际 Gate 现状 + Wall ③ 非 lever + empty-inline 诊断 + 真实失败分布：最大簇=multicol 碎片化 13）。本轮零代码变更（仅设计文档）。前轮 R376：**树清理——孤立 no-op WIP stash + baseline 纠正 442→443**。并行 agent 遗留 WIP（`adjust_inline_block_positions` 加 `DisplayValue::Inline => background_color != Transparent`，让带背景 inline 作原子行内盒参与 IFC）经独立验证为**零效果 no-op**：morning.work DC-13 16.41%（78752px）字节不变 / reftest-upstream 443/490 持平 / make test 绿（WIP 孤立 6.4h 无活动进程，已 `git stash`，recover `git stash pop`）。根因：morning `.item-tag` 徽章在与兄弟文本「全文约…」混合的 `<div id=remaining-time>` 内，对个别 inline 元素的事后 IFC 定位**无法与兄弟文本交错到同一列**——混合内容 block 需**统一 IFC**（Phase A 核心），非单点修复（R373 的 width-shrink 有效因只改宽度维度不碰行内流；行内流参与属更深一层）。empty-inline-002（25.78%）REFTEST_DUMP 复核：bbox x=[8,557] y=[35,599] 覆盖近全视窗 + max diff 255 = large-font 簇结构性（R109/R125），非 narrow slice。clean-HEAD 复验 **443/490（90.4%）**（R373 clear-inline-001 已升至 443，纠正旧标 442）。clean-win 面（self-source 近-miss R352/R354/R363/R365/R369b/R370 六轮 0/N 衰减 + product-fixture shrink 族 R368/R373 收尾）单会话确穷尽；forward motion = Phase A 统一 IFC（Wall ③ v_offset/baseline，R306 证伪 geometric-baseline 后 viable 方向 = 存 render glyph_y）多会话，或接受 plateau。本轮零代码变更（仅 stash 孤立 no-op + 文档）。前轮 R375：**advance-width DC-14 终局证伪——morning cascade 是字体不匹配非 advance 计算错误**。DC-14 实验：estimate_char_width 加 DejaVu 实测 advance 表（i=0.279/W=0.939 替代 0.55 一律，bilateral layout+paint+intrinsic），morning DC-13 **16.41%→19.14%（+2.7pp 退步）**已回退。机制：morning system-ui 字体 / chromium OS 系统 UI 字体 / ZeroWeb fontdue 捆绑字体 / DejaVu 表 = 四种字体，crude 0.55 启发式**碰巧接近 system-ui 平均字宽**故反而最优；advance-width 改善须用 chromium 实际字体 advance，ZeroWeb 无该字体→**不可修（字体加载问题非计算问题）**。**纠正 R374 advance-width-cascade 推断**（实为字体不匹配）；R224 回退**非 false-revert**（chr-Oracle 真退步，非 R209 式假阴性）；advance-width **definitive 关闭**：R375 hand-crafted 表 19.14% + R375b fontdue-actual advance 19.08% 双 variant 均使 morning 退步（vs R373 16.41%）——fontdue-actual 是最后未测变体（临时加 fontdue dep+缓存 DejaVuSans Font+metrics.advance_width），亦退步。机制：accurate DejaVuSans advance 使 ZeroWeb 换行偏离 chromium（chromium system-ui 字体≠DejaVuSans 或换行算法不同），crude 0.55 启发式碰巧更近。advance-width 非 morning cascade 根因。morning cascade 剩余 = fontdue line-metric/glyph 字体层（多会话），非 layout 杠杆。详见 [`evidence/r375-advance-width-dc14-deadend-2026-06-20.txt`](./evidence/r375-advance-width-dc14-deadend-2026-06-20.txt)。前轮 R373：**plateau 再破——inline+bg 元素 shrink-to-fit 三双赢**。承接 R371（welcome 复测发现已提交 fix 有未测 DC-13 收益）+ R372（wintertc/morning plateau 确认），深查 morning 主残余 = 3 个 `.item-tag` `<span>`（display:**inline** + background-color + padding）被 ZeroWeb 当 Block 拉到满宽色条（~55k px）。R368 shrink 仅处理 InlineBlock，**inline 未覆盖**——这是 R370「inline-flex 零杠杆」结论之外的未试机制（inline+bg 有装饰→有杠杆）。修复 `shrink_inline_blocks_to_content` 扩 `display:inline && background_color!=Transparent` 也按 intrinsic 内容宽收缩（仅 width 维度；元素仍 block 堆叠，完整 inline-box 模型属 Phase A，此为 partial width 修正）。验证（DC-14 chromium-Oracle + 双回归）：① **morning DC-13 28.72→16.41%（-12.3pp 真 chr 改善）**；② **reftest clear-inline-001 FAIL→PASS（self 5.86→0.45%，z_vs_chr 3.61→1.26% 真 chr 改善 -2.35pp）**，self-source **442→443/490 净 +1 零回归**；③ layout-engine 895/0（+1 单测）、workspace --exclude render-foundation 42/0、clippy/fmt 干净。**纠正 R370「单会话 lever 空间彻底穷尽」**——inline+bg 是漏网的 clean win（R370 只查 inline-flex/inline-grid，未查 inline+bg）。前轮 R372：**DC-13 wintertc/morning 复测——确认 plateau**（承接 R371 welcome 复测发现的「已提交 fix 可能有未测 DC-13 收益」假设）。product-smoke 子命令重渲染 vs chromium Oracle：wintertc **13.72%**（vs R318 13.70% / R227 13.59%，±0.13pp 噪声持平）、morning 800×600 **28.72%**（vs R175 28.72% 完全持平）。**R355~R368 对 wintertc/morning 零 DC-13 收益**——welcome 是唯一受益者（17.06→14.68%），因 welcome 用 `display:inline-block`（R368 修）；wintertc 残余=fontdue AA 噪声+flex-wrap logo 精度（Twind，无 inline-block）、morning 残余=`.item-tag` 纯 inline `<span>` 全宽堆叠（R109 匿名块盒，R368 不适用 inline span）。**DC-13 三 fixture 经 fresh 复测：welcome 14.68% / wintertc 13.72% / morning 28.72%，唯 welcome 改善，余 plateau**。3 个 evidence PNG 已更新为当前渲染。前轮 R371：**DC-13 welcome 复测——R368 未测量的 DC-13 收益兑现**。post-R368 welcome `z_vs_chr`（ZeroWeb CPU vs chromium Oracle）= **14.68%**，较 R227 的 17.06% 改善 ~2.4pp。归因 R368（commit 8db89df inline-block 文本 shrink-to-fit）修正 welcome 的 `.version` 徽章 + `.shortcut kbd`（两处 `display:inline-block; width:auto` 含文本，此前被 taffy 当 Block 拉到满宽灰条，现收缩到文本宽）。方法：welcome 包成临时 reftest + REFTEST_DUMP + cross-validate.py vs welcome-chromium.png（临时文件已清理，490 baseline 不动）。残余 14.68% = fontdue AA 噪声 + item-tag R109（Phase A 结构性）。welcome-zeroweb-cpu.png 已更新。**R368 现确证为 reftest(ifc-011 12.30→2.22% chr) + DC-13(welcome 17.06→14.68%) 双赢**。前轮 R370：R368 式机制缺口 hunt 收尾——probe 实证 inline-flex width:auto 同 inline-block 拉伸到满宽（真 bug），但**零杠杆**（全 48 失败案 + product-smoke 均不用 inline-flex/inline-grid width:auto），按 code-guidelines 不修；加入 ruled-out 列表。单会话 lever 空间经此彻底穷尽。前轮 R369b：DC-14 chromium-Oracle **全 48 失败案扫描**——找 self-source FAIL 但 z_vs_chr 低（false-negative：test 已≈chromium 仅 ref 发散）的易修 ref bug。5 候选（flex-abspos-inset-nested-001/002 chr 0.73/0.74%、baseline-multi-line-horiz-003/004、box-offsets-rel-pos-vlr-005）逐案证伪均结构性：flex-abspos 像素分析揭穿 ZW-test 与 chromium 均退化（非 200×200，z_vs_chr 低仅因两者主体皆白相似）；baseline-004 = baseline-export 聚类（卡点#4）。**false-negative 亦结构性，非易修 ref bug**——反向印证 plateau，self-source 非因易修 ref bug 人为偏低。DC-14 方法论正面价值 = 可信判据（R368 真胜/border-box 真负均经 z_vs_chr 确证），负面 = 不改 forward-motion 结论。勿再以「修 false-negative ref」为单会话杠杆。详见 [`evidence/r369b-dc14-scan-falseneg-structural-2026-06-20.txt`](./evidence/r369b-dc14-scan-falseneg-structural-2026-06-20.txt)。前轮 R369：border-box ib_sizes **DC-14 chromium-Oracle 终局证伪** + R368 DC-14 真值验证。弃守卫 hack 转 DC-14 交叉验证（REFTEST_DUMP + cross-validate.py vs oracle-shots）：ifc-011 **z_vs_chr pre-R368 12.30% → R368 2.22%（-10pp 真 chromium 大胜，R368 确证为真实非 self-source 假象）→ +border-box 2.50%（+0.28pp 真 chromium 退步）**；multicol-dynamic-change z_vs_chr 1.42→1.50%（亦退步）。机制：border-box 同时改 ib_sizes 的 w+h，span2 line-height:10px 的 content_h=10→border-box h=50 改变 IFC baseline（inline-block baseline=底=h）→ glyph 垂直位置发散，即 span2 **x 位置与 glyph baseline 耦合**不可单点修。border-box 三层证伪（R367 net-neg / R368 self-src -1 / R369 真 chr +0.28pp）彻底关闭。multicol-dynamic-change 实证为 self-source 假通过（0.97% self / 1.42% chr）。ifc-011 残余 2.22% z_vs_chr = 多会话 inline-block baseline/line-box 合成（Phase A）。docs-only，工作树净零代码变更。详见 [`evidence/r369-borderbox-dc14-refutation-2026-06-20.txt`](./evidence/r369-borderbox-dc14-refutation-2026-06-20.txt)。前轮 R368：R366（margin 归零）net-neutral + R367（border-box 尺寸）net-negative 均回退。残余 1.23% = span2 x 重叠 + glyph 度量 + height-grow（未试）= 多会话。详见 [`evidence/r368-inline-block-text-shrink-2026-06-20.txt`](./evidence/r368-inline-block-text-shrink-2026-06-20.txt)。前轮 R365：plateau 再确认（4 新角度全结构性/阻塞/spec 冲突，0 clean win）+ **系统性 REF-side blocker 洞察**——table-cell-width-0（REF fit-content-on-flex）、min-max-size-table（REF inline-block shrink）等多案 TEST 侧可修对但 self-source 因 ZeroWeb 自渲染 REF 错而**假阴性**，实证 DC-14「self-source pass rate 含系统性假阴性、chromium-Oracle 才是唯一可信指标」。新发现：min-height 表格 box-sizing 处理 height 路径不对称（min-width content-box 正确 / min-height border-box），但改 content-box 会回归 min-height-table（两 WPT 案 spec 冲突 csswg-drafts #5336）→ 非 clean win；inline-block shrink gap（R180 不完整）仅影响 1 失败案 REF 且双侧阻塞 → 零杠杆。clean-win 面经 R352/R354/R363/R365 四轮 0/N 衰减彻底穷尽。详见 [`evidence/r365-refside-blocker-insight-2026-06-20.txt`](./evidence/r365-refside-blocker-insight-2026-06-20.txt)。前轮 R364：table 显式 width 列冻结 + min-content floor——CSS 正确性修复（**net-neutral self-source 但改善 chromium-Oracle 方向**）。table-cell-width-0 诊断：definite-width 表（width:100%/100px）的显式 width 单元格列被按比例扩展撑大（td.big-positive 20px→529px），而非保持宽度。修复 `compute_column_widths` 扩展填满块：显式 width 列冻结（不吸收剩余），仅 auto 列吸收；+ `cell_used_width` 显式分支 floor 到 min-content（width:2px cell 内容需 9.6px → 列 9.6 非 2）。验证：table-cell-width-0 **TEST 侧列宽全修正**，ZeroWeb-TEST vs chromium-Oracle = **11.14%**（修复前 TEST 明显错）；self-source 仍 29.63% FAIL——因 **REF 侧 `width:fit-content` on flex container 渲染成满宽（taffy 0.7 blocker，R304 DEFERRED）**，TEST 正确但 REF 错 → 同源假阴性，非 TEST bug；self-source 全量 **442/490 净中性零回归**；layout-engine 893/0（+2 单测）、engine 1146/0、clippy/fmt 干净。保留：TEST 侧正确化，待 fit-content-on-flex 解锁即贡献 PASS。详见 [`evidence/r364-table-column-freeze-2026-06-20.txt`](./evidence/r364-table-column-freeze-2026-06-20.txt)。前轮 R363：上一轮 CONTINUE 标记的 3 个「未深查」候选 [flex-abspos-inset-nested / fixed-table-in-flex / multicol-fill·count] + 1 个 false-pass polluted 案 [multicol-contained-absolute] 精确诊断——**4 案全部结构性/复杂，0 clean win**，hit-rate 0/4 与 R352/R354 一致衰减；baseline 复验 self-source 442/490 零漂移。新增根因 pinpoint：flex-abspos=abspos §10.3.7 shrink-to-fit 缺失 + flex 替换元素固有比尺寸（2 互相依赖 bug）；fixed-table-in-flex=flex definite-size resolution（Mozilla bug 1469649）；**multicol-contained-absolute（chr 16.33% / self 0% 假通过）= multicol column-fill:balance 不平衡单子**（ZW 392x200 单列 vs chr 784x100 跨两列）。详见 [`evidence/r363-flagged-candidates-structural-2026-06-20.txt`](./evidence/r363-flagged-candidates-structural-2026-06-20.txt)。前轮 R362：**plateau 第四次突破 +1 clean win**——CSS float 侵入传播到后代 block IFC（`compute_final_inline_layouts` 加 `ancestor_floats` 参数，排除子节点自身），ifc-009 FAIL→PASS，self-source **441→442/490 净 +1 零回归**。R355：**Phase A 首个 chromium-Oracle 验证的 net-positive 进展**——放宽 Gate 2 多行存储（解 large-font 簇），float guard 解 R209/R213 死锁；**paint 侧 line.y 偏移补全**（text.rs:832，Gate 2 提交 ca14d05 的 load-bearing 配套）使 ifc-008 达 0.00% 完美匹配。chromium-Oracle 实测 ifc-008 -4.01% / ifc-009 -1.95% Z_vs_chromium（真一致性提升），multicol-fill-auto-001 9.15% 不变=非真回归（self-source 0.63% PASS 经 float guard 保留）；**self-source reftest 439→440/490 净 +1 零回归**（ifc-008 FAIL→PASS，stash 对照证 paint fix load-bearing：HEAD Gate-2-only ifc-008 4.17% FAIL → +fix 0.00% PASS）。关键洞察：R209 报告的 multicol-fill-auto-001「0.63→9.15 回归」经 Oracle 复测是 **self-source 假阴性**（ZW-ref 因多行存储变化发散，但 ZW-test vs chromium 不变）——R209 因仅看 self-source 误判为真回归而回退，错失 Phase A 进展。详见 [`evidence/r355-multiline-oracle-validated-2026-06-20.txt`](./evidence/r355-multiline-oracle-validated-2026-06-20.txt)。前轮 R354：fresh baseline 439/490 零漂移再确认 + 8 失败案独立 pixel-dump 根因——**每案均落 4 条多会话架构路径之一**，clean-win 单点修复面确已穷尽。详见 [`evidence/r354-plateau-revalidation-2026-06-20.txt`](./evidence/r354-plateau-revalidation-2026-06-20.txt)。前轮 R351：**plateau 首破——self-source 438→439/490（89.6%）**，首个 pass-rate 进展（R305 以来）。修复 `table-layout:fixed` 不尊重显式 width：CSS Tables §17.5.2.1 fixed 表 width 应由 width 属性决定，但 ZeroWeb 的 `compute_column_widths` 对所有表用内容固有列宽（col_max_widths），导致 fixed 表 `width:100px`+200px 内容渲染成 200px（内容撑宽）而非 100px（内容溢出 cell 由 overflow 裁剪）。根因 `table-cell-overflow-auto-scrolled`（2.17% FAIL）：cell 被撑到 200px，其 overflow:auto 无物可裁 → 与 100px div-ref 发散。**修复**（compute_column_widths）：fixed + 显式 Px width + 内容列宽和 > width 时按比例收缩列到 width（`fixed_capped` 标志），且仅当收缩实际触发时跳过「填满容器」扩展（未收缩的 fixed 表仍正常扩展——保 multicol-fill-001 reference 表）。验证：`table-cell-overflow-auto-scrolled` FAIL→PASS（0.00%）；**upstream reftest 438→439/490 零回归**（multicol-fill-001 + calc-percent-plus-0px-fixed + table_grid_size_col_colspan 三 fixed-layout 案全保）；make test 12265/0/72、clippy/fmt 干净、+1 单测 `test_fixed_layout_caps_columns_at_explicit_width_when_content_wider`。**启示**：plateau 并非绝对——逐案深查 self-source FAILURE（非 polluted）的近 misses 仍可发现单点 CSS 解析/布局 bug；table-layout:fixed width 尊重是 R305-R350 漏网的 clean win（仅 3 个 fixed-layout 案、爆炸半径小）。下一步：继续逐案审 self-source failure 近 misses（baseline-007/multicol-breaking-006/child-border-box-max-content 等 <1.5% 案）。