# 轮次详记归档：R2896–R2901（master.md §最近轮次摘要 era 迁出）

> 本档为 master.md §最近轮次摘要 的 era 归档（**2026-08-07 R2910 doc-control 瘦身迁出**）。原 §最近轮次摘要 底部 6 轮（R2901→R2896，全 blockquote 体，降序）已**逐字保留**，零删减、未重排。归档时 held baseline `make test` 13495/0/74；clean-lever surface definitively 穷尽（self-source 全 dir + oracle 8 dir + 高通过 dir 实测 + 11 验证角度〔10 property/value vein + 1 source-marker vein〕）。各轮结论已沉淀于 master.md 通过率快照 / 通过率详细快照 / 待用户决策清单 等结构化章节，迁出详记不丢失当前指引。

> **📍 R2901（2026-08-07）🔍 css-text-decor self-source 全 triage（98% 高通过 dir，本 session 首测）= 5 fail 全 precision/host-layer/R1043/IFC-adjacent，无 clean lever**。承接 R2900（post-drift plateau-guard 13495/0/74 既验）。无 .rs drift（R2900 后）+ cadence 未到（~R2907），续用 R2899 验证的「实测探针」新法扫本 session 未聚焦的高通过 dir。`make reftest-upstream FILTER=css-text-decor` = **245/250=98.0%（持平快照 R2592 零 drift），5 fail**：① **text-decoration-dotted-002 (6.53%)** = 唯一真实 feature-diff（dotted 跨 `<span>` inline 边界连续性）；②-④ **text-decoration-skip-ink-003/upright-002/vertical-002 (0.03-0.07%)** = near-pass 精度；⑤ **text-underline-offset-scroll-001 (0.06%)** = scroll host-layer。**逐 fail 深查**：① **skip-ink 全未实现**（grep `skip_ink` 全代码库零命中）但**非 lever**——skip-ink-003 测 `text-underline-offset:.5em` 把 underline 移到文本下方 → skip-ink 无关（无可 skip 的 glyph 交叉），0.03% diff 是 underline-offset 位置精度非 skip-ink 缺失；upright/vertical = **R1043 vertical-mode user-gated**。② **dotted-002 根因**：text-decoration 按 **text-fragment** 逐段绘（text.rs:1079/1454/1734 `paint_text_decoration_from_style(frag_base_x,...,text_width,...)`），`<span>` 拆 foo|bar|baz 3 fragment → render_dotted_line 每 fragment 从 d=0 重启点阵（stroke.rs:279）→ 跨边界 dot 相位错移（fragment 起点非 dot_spacing 整数倍）vs chromium decorating-box 单连续线。**裁决：dotted-002 = inline-fragment 装饰连续性 = IFC paint-model territory（须聚合同 decorating-box 同 line 的 fragment 为单连续装饰线 + dot 对齐全局 origin），属深结构 paint 架构，2026-08-04 降频守成 + IFC-adjacent 护栏下不自主开工**（moderate 改动面 + 245 既过 decoration 测试 A/B 风险 + ROI 仅 1 test）；记待决策清单等 IFC/装饰连续性点名。**结论**：css-text-decor self-source 5 fail 全 precision/host-layer/R1043/IFC-adjacent，**无 clean lever**——与 css-counter-styles(R2899)/css-content(R2888)/css-grid(R2889)/css-flexbox(R2896) 一致（全 deep/gated/系统性，无 clean lever）。**无 .rs land**——held baseline 13495/0/74（零 .rs drift，guard 窗口健康）。**下一步**：守 plateau-guard（零 .rs drift，cadence ~R2907）+ 等用户授权 Phase A IFC + font-stack coherence rebuild 整体（唯一 font-wall/Mission 65% oracle unlock；dotted-002 inline-fragment 装饰连续性亦 IFC paint-model territory 须 IFC 单一权威化一并解）+ R1043/R2174/multicol-Phase-2/form-control-native 点名切回；css-text-decor 角度经实测关闭勿重复。

> **📍 R2900（2026-08-07）🛡️ .rs drift 触发的 post-drift make test 复验：远端 cb83110c（父目标 perf wpt-runner·杠杆4：进程级 OnceLock 缓存默认 FontLoader——build_font_resolver/build_line_metric_map 取 &self 只读共享，无 @font-face case 零 re-parse 复用，@font-face case 走 fresh owned loader 保持原行为；+ per-case 计时 CaseTimer/REFTEST_TIME_LOG）rebase 入树后重跑全量 make test = 13495/0/74 EXIT=0 零 FAIL——drift 为 perf-only（FontLoader 缓存 + 计时），未改测试行为/计数，baseline drift 后仍精确持平，零回归**。承接 R2899（hiragana A/B 实测 non-lever revert）。R2899 push 时 rebase 拉入远端 cb83110c（父目标 wpt-runner perf .rs drift，触及测试 harness 字体加载路径——与 rendering-compat font fallback 相关），按 plateau-guard「父目标 .rs 变更触发复验」规则 post-drift 重验：全量 `make test`（wpt-runner 重编译，单案串行因 cb83110c 缓存已从 ~0.9s→~0.004s）= **passed=13495 failed=0 ignored=74，EXIT=0，0 FAIL/panic**——与 R2887/R2897/R2898 held baseline 精确持平，证 cb83110c（OnceLock 缓存默认 FontLoader + per-case 计时）perf-only 无测试行为/计数变化（缓存 FontLoader 确定性共享：bitmap_glyphs/fonts 表 keyed by (font_id,codepoint,size) 决定性，跨 case 复用零状态泄漏），rendering-compat baseline 零回归。**plateau-guard 价值再次实证**：连续两轮（R2898 catch 910f2e6f + R2900 catch cb83110c）catch 父目标 wpt-runner perf .rs drift → 复验 → 零回归放行，guard 设计目的兑现。**无 .rs land**——held baseline 13495/0/74（post-drift fresh 全量证）。**下一步**：守 plateau-guard（下次 cadence ~R2907 或下次 .rs drift 复跑）+ 等用户授权 Phase A IFC + font-stack coherence rebuild 整体（唯一 font-wall/Mission 65% oracle unlock；R2899 锁定 css-counter-styles marker `×0.85` 失败簇亦此 territory）+ R1043/R2174/multicol-Phase-2/form-control-native 点名切回。

> **📍 R2899（2026-08-07）🔍 hiragana/katakana/iroha counter-style 实测 A/B = 净 0 flip + diff 全幅微增（+0.1pp），经验证伪 R2890 留下的 marker-rendering 杠杆候选——css-counter-styles 失败根因经 A/B 锁定为 marker `font_size×0.85` 缩放 vs ref 全尺寸 stand-in（Phase A IFC territory），非 counter 值正确性问题**。承接 R2898（post-drift plateau-guard 13495/0/74 既验）。本轮打破 plateau「待命」循环，主动实测 R2890 标记但仅「预测」未实测的开放 lever：css-parser 核验 `hiragana`/`katakana`/`hiragana-iroha`/`katakana-iroha`（CSS Counter Styles 3 §6.1.5-6.1.8 预定义 alphabetic system）**全未实现**（parse 落 `_`→`Custom(name)`→注册表未命中→decimal fallback，parse_misc.rs:232 + types.rs ListStyleTypeValue 无此 4 变体）。**font-wall 核验（关键解阻）**：reftest runner `create_font_loader`（reftest_fonts.rs:56-68）将 NotoSansCJK-Regular.ttc 入 fallback_chain + CPU 渲染器 `rasterize_glyph_with_fallback`（cpu/mod.rs:506/610）→ hiragana 字形经 fallback 渲染**非 tofu**（同 cjk-decimal R2472 路径），R2890 隐含「tofu」担忧排除；advance 量度 `measure_advance`→`rasterize_glyph_with_fallback` 亦用 fallback 真实 advance。**实现 + 三态 A/B**（additive：4 enum 变体 + 4 parse arm + render dispatch arm + `to_alphabetic_counter` helper + 4 符号表 + getComputedStyle 序列化 + 单测；cargo check 全绿）：`make reftest-upstream FILTER=css-counter-styles` = **183/250=73.2% 与 baseline 精确持平零 flip**；scoped `FILTER=hiragana` 严格 A/B（git stash BEFORE vs AFTER）= **2/6 PASS 双向持平（032/035 单 `<li>` tiny-marker 既 before 已 PASS 非 flip），但 6/6 diff 全幅微增**：hiragana-030 1.45%→1.55% / 031 2.35%→2.57% / iroha-033 1.58%→1.70% / 034 2.51%→2.71% / 032 0.07%→0.09% / 035 0.04%→0.06%。**反直觉关键洞见**：渲染**正确** counter 值（hiragana 字形）比**错误** decimal fallback 产生**更大** diff——证 css-counter-styles 失败**完全在 marker 渲染**（`font_size×0.85` 缩放 marker vs ref 全尺寸 `<bdi>` stand-in，宽 CJK 字形在 0.85× 下与全尺寸 ref 偏离 > 窄 ASCII 数字），**非 counter 值正确性**（decimal vs hiragana 仅 ±0.1pp）。**裁决：净 0 flip + diff 微负 → 按 A/B 门禁「net 负回退」全 revert（git stash drop，零残留）不 land**。**意义**：R2890 的「hiragana 实现后不会干净翻转」预测经本轮**实测硬化为 measured fact**（非仅类比推理），并新维锁定 css-counter-styles 全簇失败 = marker `×0.85` 缩放（Phase A IFC / R834 strut 谱系 deep），非算法/parse 杠杆——clean-lever surface 经「实测 lever 候选」新法（非仅扫描）再确认 exhaust。**无 .rs land**（实测 revert）——held baseline 13495/0/74（零 .rs drift，guard 窗口健康）。**下一步**：守 plateau-guard（零 .rs drift，cadence 未到 R2907）+ 等用户授权 Phase A IFC + font-stack coherence rebuild 整体（R2867/R2869 唯一 font-wall/Mission 65% oracle unlock；css-counter-styles marker `×0.85` 失败簇亦此 territory，须 IFC strut/marker box-model 真实化一并解）+ R1043/R2174/multicol-Phase-2/form-control-native 点名切回；hiragana/katakana/iroha 角度经实测关闭勿重复。

> **📍 R2898（2026-08-07）🛡️ .rs drift 触发的 post-drift make test 复验：远端 910f2e6f（父目标 perf wpt-runner：移除 --gpu reftest jobs=1 footgun + GPU stub 文档，改 main.rs + reftest.rs）rebase 入树后重跑全量 make test = 13495/0/74 EXIT=0 零 FAIL——drift 为 perf-only（jobs 并行 + stub），未改测试行为/计数，baseline drift 后仍精确持平，零回归**。承接 R2897（cadence guard，pre-drift）。R2897 push 时发现远端 910f2e6f（父目标 wpt-runner perf .rs drift），rebase 后树含此 drift → 按 plateau-guard「父目标 .rs 变更触发复验」规则 post-drift 重验：全量 `make test`（重编译 wpt-runner ~10min）= **passed=13495 failed=0 ignored=74，EXIT=0，0 FAIL/panic**——与 R2897 pre-drift 精确持平，证 910f2e6f（jobs 并行化 + GPU stub 文档）perf-only 无测试行为/计数变化，rendering-compat baseline 零回归。**plateau-guard 价值实证**：本轮 .rs drift 触发的复验正是 guard 设计目的（catch 父目标 .rs 变更潜在回归），结果零回归（guard 成功放行）。**无 .rs land**——held baseline 13495/0/74（post-drift fresh 全量证）。**下一步**：守 plateau-guard（下次 cadence ~R2907 或下次 .rs drift 复跑）+ 等用户授权 Phase A IFC + font-stack coherence rebuild 整体（唯一 font-wall/Mission 65% oracle unlock）+ R1043/R2174/multicol-Phase-2/form-control-native 点名切回。

> **📍 R2897（2026-08-07）🛡️ plateau-guard make test cadence 复跑（R2887 + 10 轮到期）= 13495/0/74 精确持平，全量 fresh 证据证 baseline 跨 R2879→R2897 ~17 轮纯 doc/measurement plateau 零漂移**。承接 R2896。本轮按 plateau-guard cadence（R2887 后 10 轮到期）跑全量 `make test`（setsid 后台 + 轮询，~10min）：**passed=13495 failed=0 ignored=74，EXIT=0，0 FAIL/panic，无残留进程**——精确持平 R2887 held baseline，证 ~17 轮纯 doc/measurement（零 .rs drift，自 R2879 最后 .rs 代码获胜）+ 工具链/依赖/环境零漂移，baseline 稳（全量 fresh 证非断言）。**自主推进面状态再确认**：clean-lever surface 经本 session 9 轮（R2888-R2896：css-content/css-grid/css-counter-styles/css-flexbox self-source 全 triage + marker 0.85× 实验证伪 + lark-cli/use-proxy 环境核验 + marker-rendering is_ahem/advance 深查证伪 + DC-13 morning/wintertc oracle 审计）definitively 穷尽，**meta-洞见** = 残余跨布局模式渲染精度共性（marker/counter-styles/flexbox_flex-*/grid baseline 均系统性常量小 % 差）= Phase A IFC line-box-metric territory，非 per-mode clean lever；深结构 user-gated（2026-07-29+2026-08-04+R2025）+ slice 8 轮 net-negative 不可切片；无 in-loop 通知通道（lark-cli 缺）。**无 .rs land**——held baseline 13495/0/74（本轮全量 fresh 证）。**下一步**：守 plateau-guard（下次 cadence ~R2907 或 .rs drift 复跑）+ 等用户授权 Phase A IFC + font-stack coherence rebuild 整体（R2867/R2869 唯一 font-wall/Mission 65% oracle unlock，font-stack R2025 硬 block）+ R1043/R2174/multicol-Phase-2/form-control-native 点名切回。

> **📍 R2896（2026-08-07）🔍 css-flexbox self-source 全 triage（本 session 首测，156 fail）+ 关键发现：最大簇 flexbox_flex-*(50) + flexbox_flex-N-*(9) = 59 fail 全 <5% 且大量完全相同 4.27%（跨 flex:0/1/N/auto/percent 全值一致）→ flex 布局正确（grow/shrink/basis 全值正确解析），4.27% 是与 flex 值无关的 flex-vs-inline-block-ref 系统性渲染差（text 垂直定位/亚像素/stretch 高度 subtle，深非 clean lever）**。承接 R2895。本轮独立 triage css-flexbox self-source（R2219/R2607 既测但未本 session 逐 fail 分类）。`make reftest-upstream FILTER=css/css-flexbox` = **341/497 (68.6%, +0.8pp vs R2607 旧 67.8% 正向 drift)，156 fail**。**逐簇分类**：① **flexbox_flex-*(50) + flexbox_flex-N-*(9) = 59**：全 <5%，**大量完全相同 4.27%**（flex-N-0/1-N/1-1/1-0 系列全 4.27%，flex-0-N-Npercent 3.41% 等）——不同 flex 值却相同 diff → **flex 布局正确**（grow/shrink/basis 全值正确解析），4.27% 是 test(flex 容器) vs ref(inline-block width:10em/height:6em) 共同结构里的**系统性渲染差**（blockified flex-item text 垂直定位 vs inline-block baseline / 亚像素 / align-items:stretch 高度 subtle，深 flex-vs-block 语义，**类 R2890 marker 系统性差非 clean lever**）；② **writing-mode ~16**（srl/slr/vrl/vlr）= **R1043 user-gated**；③ **aspect-ratio-img(11) + aspect-ratio-intrinsic(4) = 15** = **R2174 replaced-element intrinsic-size territory**；④ **flex-minimum-width/height(7)** = min-size:auto flex 深；⑤ **baseline-synthesis(4)** = R311 深；⑥ misc 单点。**结论**：css-flexbox self-source 156 fail 全 R1043/R2174/baseline-deep/flex-stretch-deep/系统性渲染差，**无 clean lever**——flex 算法本身正确（4.27% 常量证），最大簇是 flex-vs-ref 系统性差非算法 bug。与 css-grid(R2889)/css-counter-styles(R2890)/css-content(R2888) 一致（全 deep/gated/系统性，无 clean lever）。**snapshot 纠偏**：flexbox self-source 67.8%→**68.6%**（+0.8pp 正向 drift）。**无 .rs land**——held baseline 13495/0/74（零 .rs drift）。**下一步**：守 plateau-guard + 等用户授权 Phase A IFC + font-stack 整体（唯一 unlock）；本 session 9 轮（css-content/css-grid/css-counter-styles/css-flexbox self-source 全 triage + marker 实验 + 环境核验 + marker 深查 + DC-13 审计）clean-lever surface definitively 穷尽。
