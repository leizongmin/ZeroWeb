# 归档：R689–R690（css-position static-inset clean lever + css-flexbox baseline 簇）

> 从 master.md 迁出的 R689–R690 逐轮详记（按时间倒序，内容 100% 保留）。R689 = position-static-001 clean lever（static 应用 inset bug，第 4 个 clean lever，详情亦见 pending-clean-levers.md Tier S）；R690 = flexbox-baseline-align-self-baseline-vert-001（全域最高未查发散实证为 R109/R255 inline-flex blockification 结构性）。

---

**R690（PIVOT→css-flexbox baseline 簇·flexbox-baseline-align-self-baseline-vert-001 = 全域最高未查发散 = R109/R255 inline-box blockification（inline-flex）·非 baseline bug·结构性·零源码）：承 R689 CONTINUE 攻全域最高未查发散（除 100% backdrop-filter）**flexbox-baseline-align-self-baseline-vert-001（32.93%）** LAYOUT_DUMP。方法：`LAYOUT_DUMP=1 make reftest-oracle DIR=flexbox-baseline-align-self-baseline-vert-001`（read-only；overflow=0/script=0→非 JS 驱动，不受 engine.rs:666 WIP 影响=main HEAD honest 数）+ product-smoke（`--base-dir` Ahem）+ PIL。**test**（css-flexbox §flex-baselines，Daniel Holbert）：body font:20px Ahem，3 个 `.flexContainer display:inline-flex;flex-direction:column;align-items:baseline`（各 3 item 混 smallFont 10px/默认 20px/hugeAndUnaligned 35px+stretch），前置文本「a」，assert「vertical(column) flex 容器，items 水平 baseline 不参与 main-axis baseline 对齐，align-self:baseline 无特殊效果；测容器自身 baseline 与「a」对齐」。**dump（裁决：inline-flex 被 blockify，baseline 焦点 moot）**：3 个 `.flexContainer` **w=800（满宽！）abs_y=20/85/150（垂直堆叠，block-level）**——应 `inline-flex` 水平流（与「a」同行）；hugeAndUnaligned item 亦 w=800（stretch 满宽容器）。**PIL**：ZW lightblue bbox **(10,798,20,214)**（3 容器满宽垂直堆，37871px）vs CHR lightblue bbox **(50,184,20,64)**（3 容器 inline 水平紧凑，420px）——ZW nonwhite (0,798,0,214) vs CHR (0,183,0,84)，**布局完全不同**。**裁决：R109/R255 inline-box blockification**——ZW 把 `inline-flex` 映射 taffy::Flex（converter/mod.rs:258），**taffy Flex/Block 均 block-level**，inline-level 行为丢失→ inline 容器变满宽垂直堆叠块（同 R687 inline-span→taffy Block）；**非 baseline bug**（容器根本未 inline，baseline 对齐无从谈起，test 名误导）。**与 R687 同根**（converter inline-* → block-level taffy）：R687 inline-span + R690 inline-flex（+ inline-block/inline-grid 同族）= **inline-box blockification 家族**，根因=ZW 无 inline-level box 布局（仅 IFC for text），inline BOX 被转 block-level taffy 节点。**裁决：STRUCTURAL（Phase A inline-as-IFC），非 clean lever**。**意义**：全域最高未查发散（32.93%）实证为 R109/R255 inline-box blockification（结构性），非隐藏 clean lever——css-flexbox baseline 簇（vert-001 32.93% + horiz-001 17.70%）推同 R109/R255 主导。**LAYOUT_DUMP 13 案跨 3 目录总结**（R678-R690）：css-tables 产 3 clean lever（R678/R679/R680）+ table-width 家族；css-flexbox（R685/R686/R687/R690）+ css-position（R688/R689）——仅 R689（static-inset）1 个非 css-tables clean lever，余多归 **inline-box-blockification（R109/R255 Phase A）/ image-intrinsic / form-control / table-width** 已识结构性家族；方法论价值在 css-tables 最高（标签多误），他目录标签多准（structural 多真 structural），但仍偶现 clean lever（R689）。零源码（LAYOUT_DUMP read-only + product-smoke read-only + PIL，未改任何文件）。

**R689（PIVOT→css-position 续·position-static-001 = ★REAL ISOLABLE BUG·position:static 元素被应用 inset 偏移（应忽略）·PIVOT 首个非 css-tables clean lever·零源码）：承 R688 CONTINUE 续 css-position 次高案 **position-static-001（17.53%）** LAYOUT_DUMP。方法：`LAYOUT_DUMP=1 make reftest-oracle DIR=position-static-001`（read-only；overflow=0→不受 engine.rs:666 WIP 影响=main HEAD honest 数）+ product-smoke + PIL。**test**（servo #39399，css-position §insets）：`#grid display:grid;width:200px;height:200px;background:red` 内 `#item width/height:200px; top:200px; left:200px; background:green`（**无 position 属性→static**），assert「**inset 属性不适用于 statically positioned grid item**」（green 应填 grid 覆 red，top/left:200 应被忽略）。**dump（裁决：BUG 确证）**：#grid abs_y=51 x=8 w/h=200；**#item abs_y=251 x=208 w/h=200**——item **被精确偏移 +200（top）+200（left）**（= grid 原点 51,8 + inset 200,200），即 **ZW 对 position:static 元素应用了 top/left inset**！应忽略（static 不受 inset 影响）。**PIL**：ZW green bbox (208,406)×(251,450)（item 偏移至右下）+ **red bbox (8,206)×(51,250)（grid 红背未覆，FAIL 可见红）** vs CHR green (8,206)×(50,249) 填 grid 位 **red=None**（red 被覆）——ZW 同时显红 grid + 偏移绿 item，CHR 仅绿覆红。**★ 根因（代码 read-only 精确界定）**：① `converter/mod.rs:71-74` 把 inset **无条件**传 taffy（`inset: Rect{left: convert(style.left), top: convert(style.top), ...}`）；② `converter/mod.rs:295` `PositionValue::Static => taffy::style::Position::Relative`（static 映射 taffy Relative）；③ taffy **Relative 把 inset 当位置偏移应用**→ static 元素被 top/left/right/bottom 偏移。**裁决：真 isolable layout bug**——ZW 对 position:static 应用 inset（违反 spec「insets only apply to non-static position」）。**fix scope（hand-off code-agent，clean lever）**：position==Static 时 **inset 归零**（converter/mod.rs:71-74 加 position 条件，Static 不传 top/left/right/bottom；或 Static 不映射 Relative-with-inset）→ 本案 17.53%→~0% PASS；**一行级条件修复**，spec-correct（static 不受 inset 是明确规范），blast radius 低（无 spec 测试期望 static 元素被 inset 偏移）。**yield**：position-static-001 17.53% + CSS2 position-static-001.xht 3.00%（同 bug，dump 第二块亦偏移）。**★ 意义（PIVOT 转折）**：css-position PIVOT **第二个案即现首个非 css-tables clean lever**——R688（form-control out-of-scope）后 R689（static-inset 真 bug）；方法论价值再证：R670 判 position-static-001 为「structural」（未特别标注），LAYOUT_DUMP 实证是 **clean isolable bug**（区别 css-flexbox 头两簇归已知 structural）。hand-off code-agent：static-inset 修复是当前 **css-position 最高价值 lever**（17.53%→PASS，低风险）。零源码（LAYOUT_DUMP read-only + product-smoke read-only + PIL + 代码 read-only 定位 converter/mod.rs:71-74/295，未改任何文件）。
