# R432–R437 — R428 flex min-size:auto probe 追踪归档

**归档自**: `master.md` header 状态段（2026-06-21 压缩，6 段→1 段 summary + 本归档）。
**主题**: R428 flex min-size:auto probe（`default_impl.rs` `min_width/min_height: Px(0.0)→Auto`）的验证 saga——从在途跟踪（R432）→ 机制闭合（R433）→ 全量回归被 reap（R434）→ per-dir 真数据确认 clean win（R435）→ multicol 中性 + 暂退验证（R436）→ unblock（R437）。
**结论（见 master.md summary）**: probe 经验证为 net-positive clean win（flex +14 / grid +1 / multicol 0 / 0 净回归），建议直接 commit；clean baseline 再测量既被 rally 轮次 reap 又冗余（无 engine 变更致 baseline 仍有效）。

以下是 R432–R437 逐轮 verbatim 记录。

---

**R432（R428 probe 在途跟踪·read-only）**：并行 agent 已尝试 R428 flex min-size:auto 修复——`default_impl.rs` `min_width/min_height: Px(0.0)→Auto`（**本轮仍 uncommitted**，跨轮评估中；改动自 08:35 起，css-flexbox reftest ~08:35-08:40 已跑但结果未持久化仅 stdout）。**converter 只读核查确认机制生效**：`convert_length_to_dimension`（converter/mod.rs:362）`Auto=>Dimension::Auto`，flex item 的 taffy 0.7 `resolved_minimum_main_size.unwrap_or(min-content floor)`（CSS Flexbox §4.5）将激活，预判 57 例 min-size:auto 簇改善。**两点跟踪**：① 改动为**全局 default**（非 R428 建议的 flex-scoped converter），block/grid 的 min-size 也翻 Auto，**commit 前须核验 block/grid/strict-baseline 回归**（css-flexbox reftest 仅验证 flex 上行；强制 `cargo test --workspace` pre-commit 覆盖单元/集成层，reftest 层 block/grid 须单独跑）；② 若并行 agent 上下文重置结果或丢，下轮代码侧须 re-validate 后 commit-or-revert（改动持久可重跑）。零代码变更（仅 doc 跟踪，未碰 default_impl.rs）。

**R433（闭合 R432 hedge：grid §6.6 机制确认·read-only taffy-local 追踪）**：R432「commit 前须核验 block/grid 回归」过笼统，本轮只读追踪三路径闭合该 hedge，结论=**全局 Auto 默认是 CSS-correct 方向，regression 唯一真实变化轴是 grid**：① **block 安全**——`compute/mod.rs:76` 与 `block.rs` 对 `min_size` 走 `maybe_clamp(min_size, max_size)`，Auto(None)→无下界 vs 旧 `Some(0.0)`→floor 0，但 block 尺寸本 ≥0 使 floor 冗余 →**无可观测变化**（block.rs:194 `min_size.height Some(h>0)` 判定在 Some(0.0) 与 None 下同为 false）；② **flex=预期修复**——`flexbox.rs:774` `resolved_minimum_main_size=style_min_main_size.unwrap_or(min-content floor)`，旧 `Some(0.0)` 短路 floor（违反 §4.5），Auto(None) 激活 §4.5 min-content floor（R428 目标，57 例簇预期改善）；③ **grid=真实变化轴但 spec-correct**——`grid/types/grid_item.rs:452 minimum_contribution` 解析链 `size.or_else(min_size).or_else(overflow).unwrap_or_else(§6.6)`：旧 `min_size=Some(0.0)` 命中第 2 步贡献=0 **抑制 §6.6**，Auto(None) 落入第 4 步对「span auto-min-track 且单轨或无 flex 轨」item 应用 `min_content_contribution`（CSS Grid §6.6 自动最小尺寸）。即旧 `Px(0.0)` 默认是 ZeroWeb **bug**（把 CSS initial 值当 0 而非 auto），R428 全局修正是 **spec-correct**。**预判**：flex 改善 + grid 混合（item 不再缩至 0，css-grid 62.5% self 双向偏移但向正确方向）+ block 不动。**裁决**：全局 Auto 默认**优于** flex-scoped（block 已安全 + taffy 0.7 已按 display-context 正确区分三路径），仅当 regression 实测 grid net-negative 才收敛到 flex+grid-scoped（排除 block/leaf-text）。并行 agent regression PID 53512（`test-guard -- reftest-upstream` 全目录 → `/tmp/all-after-minsize.txt`，07:32 wall @1297% CPU 健康，~10-15 min，跨多轮在途，per-dir 汇总跑完才打印）正经验验证此三轴。零代码变更（仅 doc 跟踪）。

**R434（R428 全量回归被 rally 轮次 reap → 并行 agent 转单目录验证·read-only 状态追踪）**：R433 标「PID 53512 在途」**已过时**——该全量回归（reftest-upstream 1924 例 → /tmp/all-after-minsize.txt）~17min wall 后**整树被回收、无结果**（无 exit 标记 + 末行中途截断「document is not def」紧跟「[reftest JS]」=SIGKILL 未冲刷缓冲）。死因核查：test-guard L1 阈值（单进程 RSS>6GB / 全树>16GB / >1800s，见 `docs/rally/oom-guard.md`）**均未触**（死时 RSS ~5.0GB<6GB、~17min<30min），系统无 OOM（40GB 可用、dmesg/journalctl 无 OOM 证据）→最可能是 **rally 轮次边界回收了超长运行的全量进程树**（1924 例 ~20+min 超出单 rally 轮）。即 R432「context reset 致结果丢失」担忧实化——但 **probe default_impl.rs 在磁盘安全跨轮持久**（仅运行结果丢失须重跑，非 probe 文件丢失）。**并行 agent 已适配**：现跑 `timeout 240 ./target/test-guard -- cargo run --bin zero-wpt-runner -- reftest-upstream css-multicol > /tmp/multicol-after.txt`（单目录+240s 上界，契合 rally 轮次），将逐目录扫。**R433 三轴预判（block 安全/flex §4.5 修复/grid §6.6 偏移）仍待经验验证**——代码追踪结论不依赖该次被 reap 的运行，成立。**操作启示**：全量 upstream 跑（~20+min）不契合 rally 轮次结构，单目录 240s-bounded 是后续验证策略。**perf 观察（代码确认，非 reap 归因——阈值未触）**：Auto 默认对每 flex/grid item 增加一次 min-content 测量（`flexbox.rs:774` unwrap_or 闭包执行 `measure_child_size` + `grid_item.rs:452` §6.6 回退调 `min_content_contribution`，`Some(0.0)` 时均短路跳过），是 commit-or-revert 决策须权衡的 correctness↑ vs 测量成本↑ 维度；单目录 240s 上界已兜底超时风险。default_impl.rs 仍 uncommitted（probe 在途，跨轮持久）。零代码变更（仅 doc 跟踪）。

**R435（R433 三轴预判经 per-dir 真数据确认·R428 probe = net-positive clean win，建议 commit）**：并行 agent 单目录 reftest（240s-bounded，`/tmp/flexbox-after-minsize.txt` 08:47 + `/tmp/grid-after-minsize.txt` 09:08 已完成；multicol in-flight）实测：**css-flexbox 260/496=52.4%（baseline R425 246/496=49.6%→+14 例 +2.8pp）/ css-grid 31/48=64.6%（baseline R401 30/48=62.5%→+1 例 +2.1pp）/ 零净回归**——**R433 预判（flex §4.5 修复 + grid §6.6 偏移非回归）经实证确认**，block 轴无对应回归用例亦与 R433「floor 冗余」一致。flexbox 残余 236 失败分布：`flexbox_flex` 简写 59（R429 独立）+ `flex-minimum-width/height-flex-items` 28 + `flexbox-min-width-auto` 3 = **~31 min-size 残余**（R428 主轴 floor 修了部分，~14 翻转来自此簇 45→31 fail，但 cross-axis/交互 case 仍败=**部分修复非完全**）+ aspect-ratio 34（taffy-blocked R304/R428）+ writing-mode 9。**裁决**：R428 全局 Auto 默认在两个已分析轴（flex/grid）是 **net-positive clean win**（improvement + zero downside），**建议并行 agent commit default_impl.rs**；min-size 修复为 partial（主轴 §4.5 生效，残余 cross-axis/交互须后续），不影响 commit 价值（增量正确性、零回归）。multicol 单目录结果待落（R433 未专项分析 multicol，若 multicol net-negative 才需收敛 scope，否则全量 commit）。零代码变更（仅 doc 跟踪；记录此验证以防并行 agent context 重置丢结论——R434 已证 rally 轮次边界会回收长运行）。

**R436（R435 之后：multicol 中性确认 + 并行 agent 暂退 probe 测 clean baseline·read-only 状态追踪，防 context-reset 误读）**：① **multicol 单目录结果落定**——`/tmp/multicol-after.txt`（probe 态，09:14）195/451=43.2% = R427 baseline（无 probe，commit 878ee8f）195/451=43.2% → **multicol 中性（0 变化）**，R435「multicol 若 net-negative 才收敛 scope」hedge **正向解除**（multicol 非负→全量 commit 安全，无需 flex+grid-scoped 收敛）。**完整 per-dir probe 图景**：flex +14（49.6→52.4%）/ grid +1（62.5→64.6%）/ multicol 0（43.2% 两态相同）/ block N/A（R433 代码分析=floor 冗余安全）——R433 三轴全确认 + multicol 中性。② **并行 agent 于 09:16 暂退 probe**（default_impl.rs 恢复 Px(0.0)，working tree 全清，HEAD 仍 ad3c00f 未 commit revert），现跑 `/tmp/multicol-clean.txt`（220s timeout）+ 已跑 `/tmp/multicol-reverted.txt`(09:16) 测当前 commit 的 **fresh clean baseline**——**严谨 before/after 隔离**（clean vs after = 纯 probe 效应；原 R425/R401/R427 baseline 在更早 commit 含其他代码差异），**非放弃**；clean 若确认 flex 246/grid 30/multicol 195 即坐实 +14/+1/0 delta，并行 agent 将重新 apply probe + commit。③ **R435「建议 commit」结论不变**——probe 经三目录实证为 net-positive clean win，暂退仅为隔离验证。**⚠️ 防 context-reset 误读**：clean working tree ≠ probe 被否决；若并行 agent context 重置见 clean 树（default_impl.rs 无改动），须据 R435/R436 知 **probe 已验证为 clean win（flex +14/grid +1/multicol 0/0 净回归）**，应**重新 apply default_impl.rs（min_width/min_height: Px(0.0)→Auto）+ commit**，而非丢弃（R432/R434 已证 rally 轮次边界会回收长运行致 context 重置风险真实）。零代码变更（仅 doc 跟踪）。

**R437（unblock 并行 agent：clean 再测量被 reap 且冗余——probe delta 已隔离，可直接 commit·read-only）**：核查发现并行 agent 的 clean baseline 隔离验证**未产出数据且不必要**：① **clean 运行被 reap**——`/tmp/multicol-clean.txt`(4484B) + `/tmp/multicol-reverted.txt`(4204B) 均中途截断于 JS 警告、**无 Total/Passed 汇总**（R434 风险再现：单目录 220s 也被 rally 轮次边界回收，clean 验证拿不到数据）；② **fresh clean 再测量冗余（纠正 R436「baseline 含其他代码差异」推断）**——`git log 878ee8f..HEAD -- crates/ apps/` **空**（R427→HEAD 无 engine 代码变更，仅 doc commit），R425 baseline 后唯一 engine 变更是 `4d2e93d`（multicol column-breaking panic 守卫，**不触 flex/grid 布局**）→ **R425 flexbox 246 / R401 grid 30 / R427 multicol 195 在 HEAD 仍是有效 clean baseline**（同 engine 状态，无 flex/grid 相关混入）。故 probe 的 **+14(flex)/+1(grid)/0(multicol) delta 已对有效 baseline 干净隔离**（probe-after vs 同-engine-无-probe-baseline），**无需 reap-prone 的 fresh 再测量**。**裁决（强化 R435/R436）**：并行 agent 可**跳过冗余 clean 验证、直接重新 apply `default_impl.rs`（min_width/min_height: `Px(0.0)`→`Auto`）+ commit**——delta 已坐实（flex 246→260 / grid 30→31 / multicol 195→195）、零净回归、无 engine 混入。probe 仍 temp-reverted（clean 树，git diff 空），并行 agent PID 2316 存活（轮次间，非崩溃）。零代码变更（仅 doc 跟踪）。

---

**后续补充（归档后观察，R437 之后）**: probe 截至 R437 之后多轮仍未 commit——并行 agent 继续在 reap-prone clean 验证循环（multicol-clean2 第 4 次亦被 reap 无汇总），probe 持续 temp-reverted。doc-agent 已 lark 升级通知本人（validated clean win 长时间未落地）。详见 master.md summary + 后续轮次记录。
