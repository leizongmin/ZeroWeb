# 归档：R694–R695（css-grid 收口 + CSS2/normal-flow pivot era）

> 从 master.md 迁出的 R694–R695 逐轮详记（按时间倒序，内容 100% 保留）。R695 = height-percentage-005（★CLEAN LEVER，§10.5 indefinite-CB %height→auto，**88.44% 全 lever 最高发散**——详情在 [`pending-clean-levers.md`](../pending-clean-levers.md) Tier A）/ R694 = grid-calc-margin（STRUCTURAL，taffy grid track-sizing；**次级发现 calc() in margin/padding→0，masked 待 harvest**——见 pending-clean-levers.md「masked/dormant」note）。
> R715 起 master.md 恢复 last-20（R696–R715）。

---

**R695（PIVOT→CSS2_normal-flow（fresh 第 5 采样目录）·height-percentage-005 = ★REAL ISOLABLE BUG·§10.5 indefinite-CB-height 下 %height 应 compute-to-auto（ZW 解析到 CB width 784）·LAYOUT_DUMP 脉络最高发散 88.44%（>R691 34%）·clean lever（ZW post-process，类 clamp_percentage_max_height）·★6 hand-off lever 全未实现·零源码）：R694 CONTINUE 原拟探 calc-margin（converter/mod.rs:468），但 corpus scan 证 calc-margin 在全语料 **masked**（12 calc-margin 测试全 dynamic/JS 或 text-metric/grid-masked，无 clean block-context 可 harvest）→ 转更高价值的 **fresh dir pivot**（4 布局目录 grid/position/tables/flexbox 已 triage 完）。**★ 战略发现（先查 lever 实现状态）**：grep 近 40 commit 的 6 hand-off lever（R678/R679/R680/R689/R691/R692）——**全 6 个 match 仅是 doc commit，零实现**；code-agent 当前在 scrollbar/overflow/multiprocess（55dbafd5/5c2f4692/8729f845 + engine.rs WIP），**未消费 LAYOUT_DUMP lever**。选 **CSS2_normal-flow（857 oracle shot，core box-model 非 font-wall/taffy-grid）**（同为 Ahem 的 css-tables 产 3 lever，CSS2 dir 同样可能）。`make reftest-oracle DIR=css/CSS2/normal-flow`（746 案，read-only；**74.3% pass**——高于 grid 32.7% 但仍 192 fail；不涉 table-cell→engine.rs WIP no-op = honest 数）取 top-15：**height-percentage-005 88.44%**（standout）/ max-width-107/105/110 29/28/15% / min-height-106 26% / **inline-replaced-width-007/008/009/012/013 + inline-block-replaced-width-007/008 12-22%（replaced-intrinsic-width 簇，似 R685/R686 territory）** / block-non-replaced-height-011 16% / replaced-intrinsic-001 12%。选 **height-percentage-005（88.44%，最高）** LAYOUT_DUMP。**dump 前预检**：无 `<script>`✓ / 无 `overflow:`（overflow=0；grandparent height:0+overflow visible 是 test 设计非 overflow 属性）✓ / 有 `<img src=support/black96x96.png>`（img-loading）→ **oracle validity 验证**：nonwhite=10525 ≈ 96×96 黑方(9216)+文本，**img 已加载 = oracle valid**（非 R692 broken-img race）。**test**（CSS §10.5「Image % sizing interoperability」）：`div#grandparent{height:0px}` > `div#parent`(auto) > `div#child{height:100%}` > `<img height:100% src=black96x96.png(96×96 intrinsic)>`，assert「%height 仅当 CB height 显式指定时才解析；div#parent auto → div#child height:100% **treated as auto** → img 按 intrinsic 96×96 渲染（grandparent height:0 + overflow:visible 故可见）」。**dump（裁决：BUG 确证）**：grandparent h=0（✓）/ **parent(auto) h=784 / child(height:100%) h=784 / img(height:100%) h=784 w=784**——全部 784（= body content width！）应 img intrinsic 96×96（child/parent 应 auto 内容高 ~96）。**PIL**：ZW nonwhite **432161 bbox(8,29)-(792,600)**（img 784×784 巨黑方块填满 viewport，clipped 至底）vs CHR nonwhite 10525 bbox(8,18)-(280,146)（96×96 黑方+文本）；**88.44%(424516px) = img 过大 784×784 vs 96×96**。**★ 根因（代码 read-only 精确界定）**：CSS §10.5——「%height 仅当 CB height **显式 specified**（非 auto/非依赖 content）时解析；否则 **compute to auto**」。本案 CB 链 grandparent(0,definite)→parent(auto,**indefinite**)→child(100%,**应 auto**)→img(100%,**应 auto→intrinsic 96**)。**ZW 违反 §10.5**：把 %height 传 taffy 作 Percent(1.0)（converter/mod.rs:1309 height:75%→Percent(0.75) 正确），taffy 0.7.7 在 indefinite-CB-height 时把 %height **fallback 到 CB width(784)** 解析（非 auto）；ZW **无 normal-flow %height 后处理**（clamp_percentage_max_height engine.rs:1404 **仅处理 max/min-height Percentage，不处理 height Percentage**；engine.rs:1285 height_auto 检查是 abspos-vrl 不相关）→ 784 全程泄漏。**裁决：真 isolable layout bug（spec §10.5）·clean lever**——**fix scope（hand-off code-agent）**：仿 clamp_percentage_max_height（ZW 已有的 taffy-override post-process 模式）加 §10.5 处理：遍历布局树，对 height:Percentage 元素，**若其 CB height indefinite（auto / percent-of-indefinite / height:0-definite-but-... 须界定）则把该 %height compute-to-auto**（→ block 取内容高 / replaced 取 intrinsic；本案 img→96, child/parent→auto）。**复杂度中等**（非一行）：须传播 CB-height-definiteness（parent %height-of-indefinite 本身 indefinite，cascade）；blast radius 须 A/B（definite-CB %height 案如 min-height-094/095 R587 须仍 pass）。**本案 88.44%→~0% PASS**（修后 img 96×96），**潜在多案 yield**（height-percentage 簇 + indefinite-CB %height reftest）。**★ 与 R691 同族不同机制**：R691 = grid-item %height 对 track（grid CB 语义）；R695 = normal-flow %height 对 indefinite CB（§10.5 compute-to-auto）；两 bug 独立。**★ 意义（双重）**：① **CSS2_normal-flow fresh pivot 首案即现 LAYOUT_DUMP 脉络最高发散 lever（88.44%，超 R691 34%、R692 64%）**——第 7 个 clean lever，第 5 个采样目录（css-tables/css-position/css-grid/css-flexbox/CSS2_normal-flow）首案即命中，再证 fresh-dir pivot 价值（CSS2 core box-model 与 css-tables 同高产）；② **战略 lever 实现审计**：6 hand-off lever 全未实现（code-agent 在 scrollbar/overflow 线），hand-off 队列堆积——doc-maintainer 继续找 lever 但 code-agent 消费滞后，后续可考虑优先级对齐。hand-off code-agent：**§10.5 indefinite-CB %height→auto 是当前最高价值 lever（88%→PASS，+潜在多案），ZW post-process 可行（类 clamp_percentage_max_height）**。零源码（reftest-oracle read-only + LAYOUT_DUMP read-only + product-smoke 渲 PNG read-only + PIL + 代码 read-only 定位 converter/mod.rs:1309 + engine.rs:1404/1285，未改任何文件）。

**R694（PIVOT→css-grid 续·grid-calc-margin = STRUCTURAL·grid 隐式 auto-column 不扩展填 definite 容器（items w=0）= taffy track-sizing·次级 calc() margin→0 bug（masked）·css-grid top 穷尽·零源码）：承 R693 CONTINUE 续 css-grid 次低 valid-oracle 案 **grid-calc-margin（4.17%，无 JS/overflow，valid oracle）** LAYOUT_DUMP（选它而非 stretch-grid-item-button-overflow 8.17%——后者疑 form-control 原生渲染 R688 out-of-scope）。**dump 前预检**：无 `<script>`✓ / 无 `overflow:`（overflow=0）✓ / 无 table cell→engine.rs WIP no-op = honest 数✓。**test**（webkit bug 301873，Sam Weinig）：`.cardStack display:grid;width:100px`（**无 grid-template-columns**→单隐式列）> `#card1 margin-top:calc(50% - 0px);bg:lightpink` + `#card2 bg:lightgreen`，两 `.card height:100px`，assert「calc(50%-0px) margin 解析」（百分比 margin 对 CB width=100px→margin-top 应 50px）。**dump（裁决：w=0 主导，非 margin）**：cardStack w=100（✓）/ **两 div.card 均 w=0**（应 100px stretch）h=100 mt=0。**PIL 实证**：ZW cardStack 区 **全白 0 nonwhite**（两卡 w=0 不可见）vs CHR pink y58-157 + green y158-257（两 100×100 卡，card1 mt=50 故 y58）；**4.17%(20k px) = 两张不可见 100×100 卡**。**★ 与 passing grid-item-non-auto-height-stretch-001 对比**：该案用 **explicit `grid-template:100%/100%`**（轨道显式定尺寸→item stretch 100px✓）；grid-calc-margin **无 grid-template-columns→隐式 auto 列**。**根因（代码 read-only）**：ZW **无 grid 布局文件**（grep 无 grid.rs；converter 仅映射 style，grid 几何全由 taffy 0.7.7 算）→ items w=0 是 **taffy grid track-sizing 输出**：taffy 把隐式 auto column 按内容 max-content（空 div=0）定尺寸，**未按 css-grid §12.6「justify-content:stretch（默认）扩展 auto track 填 definite 容器」扩展到 100px**。**裁决：STRUCTURAL（taffy grid track-sizing/content-distribution，R304 deferred territory）**——非 ZW 可单点修（须 taffy 升级或 ZW 侧 grid-item-stretch 后处理，后者 entangled 须逐 item 知 track 宽，非 clean）。**次级发现（real 但 masked）**：card1 `margin-top:calc(50%-0px)` dump mt=0——`converter/mod.rs:468 convert_length_to_lpa`（margin/padding LengthPercentageAuto 路径）`LengthValue::Calc(_) => length(0.0)`，即 **calc() 在 margin/padding 完全未支持（直接→0）**，对比 `convert_length_to_dimension`（width/height，line 374）有 `extract_calc_percentage`（calc(100%-6px)→Percent）。**两 bug 关系**：w=0（taffy）主导可见发散；calc-margin→0（ZW）masked（卡 w=0 不可见）；修 calc-margin alone yield 0（卡仍不可见），须先修 w=0（taffy）才能 harvest calc-margin。**裁决：grid-calc-margin 非 clean lever**（primary taffy structural 阻塞）。**★ css-grid top 穷尽**：R691（%height clean）+ R692（aspect-ratio clean）+ R693（oracle triage）+ R694（grid-calc-margin structural）；css-grid 余 top = baseline-synthesized（R266 skip）/ replaced-...-001 9%（R691 noted）/ stretch-button-overflow 8%（form-control R688）/ grid-calc-margin 4%（本轮 structural）——**2 clean lever 已 harvest，余多 structural/taffy/form-control，css-grid pivot 边际递减**。**hand-off code-agent（独立 lever 候选）**：① grid 隐式 auto-column 扩展（taffy territory，R304，多会话）；② **calc() in margin/padding（convert_length_to_lpa:468）= ZW-side 可修**——extend `extract_calc_percentage` 到 lpa 路径，但须找 calc-margin 为唯一 issue 的 block-context 测试 harvest（grid-calc-margin 被 w=0 masked 不可用）；潜在多案 yield（所有 calc() margin/padding reftest）。**意义**：css-grid pivot 收口——继 css-tables(3)/css-position(1)/css-grid(2) 共 6 clean lever 后，css-grid top 穷尽；次级 calc-margin→0 发现开启新 ZW-side lever 线索（calc 在 margin/padding 缺支持）。零源码（LAYOUT_DUMP read-only + product-smoke 渲 PNG read-only + PIL + 代码 read-only 定位 converter/mod.rs:468/374，未改任何文件）。
