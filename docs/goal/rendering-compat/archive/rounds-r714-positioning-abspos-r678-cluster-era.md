# 历史轮次归档 — R714（CSS2/positioning 续 · abspos-008 = R678 同根 · R678 float-width 簇 ≥5 案跨 3 目录 · 最高渗透 cluster era）

> 内容 100% 保留自主控文档 master.md（R714 块逐字迁出，仅作归档）。下文为该轮完整逐轮详记。

**R714（CSS2/positioning 续·abspos-008 = R678 同根·abspos §10.3.7 shrink-to-fit（.outer w=185）正确·.control red float w=784 满 width 非 shrink-to-fit 致红露出·R678 float-width 簇再 +1（≥5 案跨 3 目录，最高渗透 cluster）·abspos-float-with-inline-container = R109/R255 inline span blockification 结构性·CSS2/positioning top 全 triage 完（R711 inset 簇 + R678 + R109 + R165-confirmed，无新 lever）·零源码）：承 R713 CONTINUE 续 CSS2/positioning abspos sizing 簇。选 **abspos-008（17.64%，Ian Hickson §10.3.7 abspos shrink-to-fit，script=0/overflow=0/dynamic=0）** LAYOUT_DUMP。**test**：`.container(position:relative;font:100px)` > `.outer(abspos;top:0;left:0)` > `.inner(float:left;green)"X X0"` + `.control(float:left;red)"X X1"`，assert「green shrink-to-fit 覆盖 red 无红」。**dump（裁决：R678 同根，非新 lever）**：**.outer(abspos) w=185**（§10.3.7 shrink-to-fit **正确✓**，含 float 子 .inner 的 max-content ~185）+ .inner(float green) w=185 + **.control(float red) w=784**（**满宽！应 shrink-to-fit ~185**）——green(185) 不覆 red(784) → 红露出 = 17.64%。**ref 确证**：ref 是单个 green `float:left` "X X0" shrink-to-fit ~185，故 test 的 .control red float "X X1" 亦应 ~185。**★ 裁决：abspos-008 = R678 float shrink-to-fit**（.control 正常流 float 取满宽 784 非 shrink-to-fit，float_positioning.rs:16 taffy 当 float 作 in-flow block）；abspos §10.3.7 shrink-to-fit（.outer w=185）**工作**（补强 R712/R713 R165 abspos 覆盖确证——abspos width:auto shrink-to-fit + max-width clamp + margin:auto 三 facet 全工作）。**★ R678 float-width 簇累计 ≥5 案跨 3 目录**：css-fonts/font-size-zero-3（R678 pin）+ floats-clear/float-non-replaced-width-007（R704）+ floats-clear/floats-125（R706）+ floats-clear/floats-124（推）+ **CSS2/positioning/abspos-008（R714）**——R678 是当前最高渗透率 cluster lever（跨 css-fonts/css-floats/CSS2 三目录）。**续 abspos-float-with-inline-container（16.87%）LAYOUT_DUMP**：`span(inline;position:relative;padding-left:100px)` > `#float(float:left)` > `#abs(abspos;green;height:100)`，assert「green square 100×100」。**dump（裁决：R109/R255 结构性）**：**span w=784**（inline span 被当 block 满宽，应 inline shrink ~100）+ #float w=684 + **#abs w=684**（应 100×100 square）——inline span 含 block float 触发 §9.2.1.1，ZW 把 inline span blockification（converter inline→taffy::Block，R255/R109/R709 同根）→ span 满宽 → #abs CB 满宽 → 非 square。**★ CSS2/positioning top 全 triage 完**：① bottom/top-113/103/104/091/092（~8 案 15-47%）= R711 relative %inset（clean-ish，已在队列）；② abspos-008 = R678（已在队列，+1 案）；③ abspos-float-with-inline-container = R109 结构性；④ absolute-non-replaced-width-025/026 = R712/R713（R165 abspos 确证 + red-width quirk）。**CSS2/positioning 唯一新 clean lever = R711 inset 簇**（已入队列），余 R678/R109/R165-confirmed。**★ 意义**：① revisit 已采目录（R711 CSS2/positioning）次低案 triage 完，top 穷尽——印证 R709「12 目录采样」模式（clean layout lever 全来自 box-model sizing，positioning 的 inset/abspos sizing 已在 R711/R678 覆盖，余 structural）；② R678 渗透 3 目录（最高产 cluster），Tier A 优先级确证；③ abspos §10.3.7 shrink-to-fit 工作（R165 三 facet 全确证）。**hand-off**：R678 float-width 簇扩至 CSS2/positioning abspos-008（≥5 案），优先级维持 Tier A 最高；R711 inset 簇仍待 code-agent disambiguate。**下一步**：CSS2/positioning 收口，转他 revisit（css-position 次低案 / css-tables 余案待 engine.rs WIP / CSS2 normal-flow 次低）或 lever consolidation。零源码（reftest-oracle read-only + LAYOUT_DUMP read-only + 代码未触碰；engine.rs WIP 是 table-cell overflow 非 abspos，对本案 no-op）。
