# 归档：R697（CSS2/normal-flow · max-width 簇 = overflow-clip 负结果 era）

> 从 master.md 迁出的 R697 逐轮详记（内容 100% 保留）。R697 = CSS2_normal-flow max-width 簇（107/105/110）triage = **负结果**（max-width clamping 正常，发散=element overflow:auto 内容裁剪缺失 = paint-clip render feature gap，非 clean LAYOUT lever；110 = float+replaced defer）。max-width 本身 ZW 已正确实现。不入 clean-lever 队列。

---

**R697（CSS2_normal-flow 续·max-width 簇 = MIX·107/105 = overflow:auto 内容裁剪缺失（非 max-width bug，clamping 正常）·110 = float+replaced·非 clean LAYOUT lever·负结果·零源码）：承 R696 CONTINUE 探 CSS2_normal-flow top 的 max-width 簇（107/105/110，29/28/15%，3 案）。**cluster sizing**：簇 MIX——① **107/105（29/28%）**「max-width - height and overflow」= `div position:absolute; font:200px/1 Ahem; height:200px; max-width:200px; overflow:auto`（red "XX XX" 800px 内容 + green "X" 覆盖），assert「green 方块无红、green 无 scrollbar」；② **110（15%）**「max-width - float and inline replaced」= float + replaced shrink-to-fit（不同机制，defer）。选 **107（29.11%）** LAYOUT_DUMP。**预检**：无 script✓ / **overflow=1**（test 设计的 overflow:auto，非 table-cell→engine.rs WIP no-op = honest 数）✓ / 用 Ahem（font:200px/1 Ahem，/fonts/ahem.css）→ oracle validity 验证 nonwhite=47816≈合理（非 broken-img）。**dump（裁决：max-width clamping 正常，非 max-width bug）**：两 div 均 **w=200**（max-width:200px **正确 clamp**✓）h=200 abs_y=106——**max-width 完全工作**。**PIL 实证（裁决：overflow 内容未裁剪）**：ZW nonwhite **169850** bbox(8,29)-(775,506)（内容 ~3.5× CHR）vs CHR nonwhite 47816 bbox(8,18)-(760,302)；**ZW red 内容 x 延伸 8-406**（远超 200px box x=8-208）——overflowing red "XX XX"（200px Ahem wrap+溢出）**visible 渲染未裁剪**，应 overflow:auto 裁剪到 box + scrollbar（chromium 裁剪 → 仅 green "X" 可见）。**裁决：element overflow:auto 内容裁剪缺失（paint-stage clip gap）·非 clean LAYOUT lever**——box 尺寸正确（max-width ✓），缺陷在 paint 不对 overflow≠visible 的**非 viewport 元素**应用内容 clip rect（red 溢出可见）。**与 code-agent 活跃工作区分**：code-agent 近期做 **page-level scrollbar/viewport clip**（8729f845「Clip all WebView extra primitives to page viewport」= 页面视口裁剪 / 55dbafd5/5c2f4692 = 页面滚动条）；本案 = **element-level overflow:auto 裁剪**（div 内内容裁剪 + element scrollbar），**不同层级**，是独立 render feature gap。**裁决：非 clean isolable LAYOUT lever**——是 render/paint-clip feature（非布局 bug），且 element overflow:auto 隐含 scrollbar（>仅 clip），复杂度高于一行 layout fix；**不入 clean-lever hand-off 队列**（低优先级 render feature，区别 R695/R696 的 layout lever）。**★ max-width 簇负结果**：107/105 = overflow 裁剪（非 max-width）；110 = float+replaced（defer）——max-width 本身 ZW 已正确实现（clamping ✓），簇非 clean lever。**CSS2_normal-flow top 进度**：R695（height-percentage 88% clean）+ R696（inline-replaced-width unified clean）+ R697（max-width = overflow-clip 非布局 lever）；余 top = **min-height-106（26%）/ block-non-replaced-height-011（16%）** 未查。零源码（cluster sizing grep read-only + LAYOUT_DUMP read-only + product-smoke 渲 PNG read-only + PIL red-x-range 实证 + 代码未触碰，未改任何文件）。
