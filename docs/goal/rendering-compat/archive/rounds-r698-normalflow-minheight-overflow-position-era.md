# 归档：R698（CSS2/normal-flow · min-height-106 overflow/position entangled 负结果 era）

> 从 master.md 迁出的 R698 逐轮详记（内容 100% 保留）。R698 = CSS2_normal-flow min-height-106（26.11%）triage = **负结果**（min-height honored h=200✓；发散=overflow:auto 内容未裁剪（R697 element-overflow-clip gap 同根）+ green position:relative top:-200 未应用 entangled，非 clean 单点 lever）。与 R697 同「min/max - overflow」簇。

---

**R698（CSS2_normal-flow 续·min-height-106 = R697 同族 overflow/scrollbar 交互·min-height honored（h=200✓）·发散=overflow 内容未裁剪 + green position:relative top:-200 未应用·非 clean lever·「min/max - overflow」簇确认全 element-overflow-clip gap·负结果·零源码）：承 R697 CONTINUE 查 **min-height-106（26.11%）**。**预检**：无 script✓ / **overflow=1**（test 设计 overflow:auto，非 table-cell→engine.rs WIP no-op=honest 数）✓ / Ahem 字体 / oracle validity nonwhite=46293≈合理（非 broken）。**test**（Gérard Talbot「min-height - float and overflow」）：`div{font:100px/1 Ahem;overflow:auto;width:200px}` / `#test-red-overlapped{bg:red;float:left;min-height:200px}` content "XXX"(300px→横滚条) / `#control-green-overlapping{bg:green;clear:left;height:200px;position:relative;top:-200px}` content "XX"，assert「**有 horizontal scrollbar 时，scrollbar height 应从 CB 减去但 min-height 仍 honored**」「green 方块无红」。**dump（裁决：min-height honored，同 R697 max-width clamping 模式）**：red div **h=200（min-height:200px 正确 honored✓）** w=200 abs_y=106 / green div abs_y=**305.2**（= clear 位置 ~306，**top:-200px 未应用**——应 shift 到 ~106 覆盖 red）。**PIL**：ZW nonwhite **117951** bbox(8,29)-(775,506)（~2.5× CHR）vs CHR nonwhite 46293 bbox(8,18)-(760,302)——ZW 多出 red "XXX" 溢出内容（未裁剪，R697 pattern）+ green 未覆盖（position:relative 未生效）。**裁决：非 clean lever（overflow/scrollbar + position:relative entanglement）**——min-height 本身 honored（h=200✓，如 R697 max-width clamping ✓），发散是多因子 entangled：① overflow:auto 内容未裁剪（R697 element-overflow-clip gap 同根，red "XXX" 溢出可见）；② **green `position:relative;top:-200px` 未应用**（green 留在 clear-position 305 非 shift 到 106 覆盖 red）——疑似 position:relative offset 缺陷（但与 overflow entangled 不可单独隔离验证，taffy 0.7 comment 称 layout.location 含 relative inset，本案实证未生效，defer 独立验证）；③ scrollbar 未渲染（element-level，R697 同）。**★「min/max - overflow」簇确认**：CSS2_normal-flow 的 max-width-105/107（R697）+ min-height-106（R698）+ 推测 max-width-110 均为 **Gérard Talbot「min/max-width/height - overflow/float」系列**，统一测 **overflow:auto + scrollbar + min/max-size 交互**——**全 element-overflow-clip/scrollbar render feature gap**（min/max-size 本身 honored），**非布局 lever**。**裁决：min-height-106 非 clean lever**（min-height honored，发散 overflow+position entangled）。**CSS2_normal-flow top 进度**：R695（%height clean 88%）+ R696（replaced-sizing unified clean）+ R697（max-width = overflow-clip 负）+ R698（min-height = overflow-clip+position 负）；「min/max - overflow」簇（105/107/106/110）**统一判 element-overflow-clip gap 非 lever**，跳过余案；clean lever = R695/R696 两案。余唯一未查 top = **block-non-replaced-height-011（16%）**（block height 计算，§10.5 非 overflow，或干净）。零源码（预检 read-only + LAYOUT_DUMP read-only + product-smoke 渲 PNG read-only + PIL + 代码未触碰，未改任何文件）。
