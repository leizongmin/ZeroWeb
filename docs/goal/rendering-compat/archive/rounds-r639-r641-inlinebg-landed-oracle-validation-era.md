# 归档轮次 R639–R641 — per-fragment inline-bg owner-height 索引 LANDED + oracle-impact validation era

**归档日期**: 2026-06-26（doc-maintenance 轮迁出，R639–R641 超出最近 20 轮窗口）

**范围**: R639–R641。下列逐轮详记从 master.md 100% 原样迁出，未修改。综合结论见 master.md「综合裁决」节（R592–R645 段 per-fragment inline-painting vein + font-wall oracle blocker）与本归档顶部摘要。

---

**R641（R639 oracle 影响 validation·self-source +13 但 oracle 受字体墙限制·零源码）：承 R639（+13 self-source landed）验证 R639 对 **DC-14 真优化目标（chromium oracle 一致率）**的实际影响。cross-validate text-indent-wrap-001（R639 修复案之一）：post-R639 **z_vs_ref=0.00%（self-source PASS，R639 修对 test==ref bg）**，但 **z_vs_chr=13.91%（仍与 chromium 不一致，POLLUTED）**。**裁决**：R639 的 +13 是**self-source（reftest harness）正确性 win**（test 与 ref 现均正确按行片段绘 bg，消除「bg 覆盖 text-indent 区」bug），但对 **chromium oracle 一致率影响有限**——13.91% 差异主导是**字体/文本渲染（font-metric / 行盒度量，R633 字体墙）**非 bg（bg 占 ~0.33%，字体/文本占余下）。即 R639 修了真实渲染 bug 并提升 self-source 通过率，但 oracle 真指标仍被字体墙压住。**意义**：(1) 确认 R639 是合法 self-source 正确性修复（非假通过，test==ref 均正确）；(2) 校准期望——inline-fragment vein（bg）对 oracle 杠杆小，**真 oracle blocker 是字体墙（R633 Phase A IFC 接 FontLoader）**；(3) inline-fragment vein 已收口（bg landed，border/outline/shadow 无 driving test，见 R640）。**下一步方向**：oracle 真提升须攻字体墙（多 session Phase A IFC FontLoader 真实 ascent/advance 度量），或转其他 goal area（DC-8/9 blend·drop-shadow paint-isolation 完整性）。零源码变更（cross-validate.py + REFTEST_DUMP_PASS）。详见本轮 cross-validate 数据。

**R640（R639 后续：inline-border 扩展无 driving test 证伪 + R639 跨 dir win 扫描收口·零源码）：承 R639（per-fragment inline bg landed, +13）评估向 inline BORDER 扩展的可行性（同 pattern，复用 owner-height 索引）。**核查**：`paint_borders`（border.rs:40）绘 4 边作**单一 box-level rect**（用 box_node.width/height）——与 R639 前 bg 同 bug（多行 inline border 应按行片段：top/bottom 逐行 + left 首行 + right 末行）。**但** WPT 导入集内**无 driving 多行 inline-border 测试**：CSS2/borders 无 inline 相关文件；css-text/CSS2/text 抽样无 span+border 跨行失败案。**裁决**：per code-guidelines §2（不做无 driving test 的投机实现），inline-border 扩展**当前不实施**——须先发现一个具体失败的多行 inline-border reftest 才启动。R109 split inline（inline 含 block 子）的 border 边选择已有（r109.rs r109_first/last_fragment），但纯文本跨行 inline span（无 block 子）不在 R109 路径，其 border 仍 box-level（潜在 bug，无 driving test 暴露）。**R639 跨 dir win 扫描收口**：css-text +10（已记 R639）/ CSS2/text text-indent-wrap +2 / generated-content content-175 +1 = **+13 确认**；css-text-decor 245/250（vs R417 244/246，分母 +4，+1 不可归因，中性）/ css-tables 80/115 + css-position 64/97（vs R546m 分母漂移，不可归因）—— R639 win 集中在 text-heavy 目录（css-text 主），其余 dir 无明显 bg win。**结论**：R639 inline-fragment bg vein 已收口（+13 landed），inline-border 无 driving test 暂不扩展；下步须识别下一个有 driving test 的 per-fragment inline 属性（outline/box-shadow on 跨行 inline）或转其他 goal area。零源码变更（read-only 代码核查 + 跨 dir reftest 扫描）。

**R639（per-fragment inline bg owner-height 索引修复·LANDED commit c7ff730b·首个 landed Phase A inline-fragment narrow slice）：承 R638（锁定 blocker = 宏 box_node 是 IFC owner 非 inline 本身）实施精确修复。新增 `Painter.inline_heights: HashMap<NodeId, f32>`，paint() 开头预扫描布局树（`collect_box_heights` 递归）填充 NodeId→LayoutBox.height；render_fragment 宏改用 **owner inline 自身 height**（`self.inline_heights.get(&owner_id)`）门控 per-fragment bg，**非** box_node.height（IFC owner 的）。抑制（paint_node，inline 自身 box 上）仍用 box_node.height（inline 自身）。两处现均用 inline 自身 height → **一致，消除 R638 的 inline-ownership split 分歧**。per-fragment rect 用 frag_base_x（IFC 首行 current_x 已含 text_indent）故首行 bg 从缩进后起。**验证（default-on 全门禁通过）**：reftest self-source A/B vs HEAD——**A/B 4 dir 0 回归**（normal-flow 102→102 / generated-content +1: content-175 / block-in-inline 35→35 / floats-clear 88→88 全 0 回归，case-name diff 空）+ text-indent-wrap +2（001/002 5.92%→PASS）；**css-text 全量 1409/1576（vs R632 基线 1399 = +10，R632→R639 间唯一代码变更是 R639，故 +10 归 R639）**——R639 per-fragment inline bg 修 css-text 内 inline bg span 跨多行 case。**R639 真实 WPT 合计 ≈ +13 self-source / 0 回归**（2 CSS2/text + 1 generated-content + 10 css-text）；make test 全 workspace 绿（含 R638 失败的 test_pipeline_consecutive_different_renders 现 pass；+新单测 test_r639_multiline_inline_bg_painted_per_fragment）；product-smoke welcome 16.16% 持平；clippy/fmt clean。**门控**：display==Inline + bg + !abspos + height>1.5×fs + has_direct_paintable_text（单行/空/定位 inline 保留 box-level）。**意义**：首个 landed Phase A inline-fragment narrow slice（继 R355/R358/R362 谱系），R636/R638 两轮 revert 后用 owner-height 索引攻克 inline-ownership split。owner-height 索引（NodeId→box.height 预扫描）是**通用 inline-ownership 桥接**——后续 per-fragment inline border / 真实 font 度量等 Phase A 子项可复用此索引查 owner inline 自身 box geometry。详见 [`evidence/r639-inline-bg-owner-height-index-landed-2026-06-26.txt`](./evidence/r639-inline-bg-owner-height-index-landed-2026-06-26.txt)。
