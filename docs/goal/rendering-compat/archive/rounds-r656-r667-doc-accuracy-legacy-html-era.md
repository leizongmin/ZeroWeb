# 历史轮次归档：R656–R667（DC-13 legacy-html smoke + doc-accuracy/M7 arc era）

> 从 `master.md` 迁出的逐轮详细记录（内容 100% 保留）。该 era 覆盖：
> - **R656–R659**：DC-13 legacy-html smoke fixture 集（testpage-001..020 + 窄屏 viewport），`make product-smoke-legacy` 门禁落地。
> - **R660–R667**：doc-accuracy arc——M7 渲染器图元覆盖 stale 纠正（CPU/GPU/浏览器消费全 13 图元，DC-8 13/13 + DC-9 13/13 framebuffer rigor）+ DC-12 高级视觉 stale 纠正 + DC-14 anti-false-pass checklist 收口 + font-wall 彻底定性。
>
> 综合结论仍见 `master.md` 顶部「综合裁决」+ R668（font-wall rustybuzz refuted）/ R669（reftest-oracle LANDED，当前 LAYOUT_DUMP vein 的工具基础）/ R670（reftest-oracle validation）。

---


**R667（doc-accuracy arc 收口·DC-14 anti-false-pass 部分勾选 + goal doc 各 DC checklist 现大面积准确·零源码）：承 R660-R666 doc-accuracy 谱系（external-CSS / M7 / DC-12 / real-reftest / DC-11 stale 纠正），本轮审计 DC-14「真通过标准」并收口整个 arc。**DC-14 勾选 3 项（commit 8dfa612a）**：① 分母真实性（去子集化）✅ R484 完成 10/10 目录全量（~9967 reftest）+ audit-reftest-coverage.py 对账，纠正 stale「COUNT=60 子集」；② GPU 非 passthrough ✅ R660/R661 真实 wgpu+mesh 管线 + DC-9 13/13 framebuffer 测试，纠正 stale「DC-9 CPU 后处理对齐」；③ 内联 smoke 不计达标 ✅（685 inline smoke-only）。**DC-14 保留 open（真 anti-false-pass 缺口）**：独立 Oracle（reftest.rs:230-232 仍 ZeroWeb self-ref，chromium Oracle 仅独立 cross-validation 路径——**DC-14 核心未解项**；→ **R669 已解**：`reftest-oracle` 子命令 + `make reftest-oracle` 一等独立 Oracle 指标，doc-maintainer spot-check `DIR=css-grid`=16/49=32.7% 复现 R560）/ 严格容差复跑+三态分类（无三态实现）/ 非平凡性检查（仅 min_mismatch_ratio mismatch 方向）/ 容差锁定（policy）。**arc 收口结论**：goal rendering-compat.md 各 DC checklist 现大面积准确——DC-8/9 **13/13**（code-agent R664-R666 据 R661 审计补齐 framebuffer 测试）/ DC-10 item1 / DC-11 7 项（Margin/fixed/replaced/BFC/百分比/Auto-margin/min-max；Float/sticky/scroll 真 incomplete）/ DC-12 3+support-envelope（text-shadow/multi-bg/repeating ✅；clip-path/backdrop-filter/mask 真 incomplete）/ DC-13 7+ / DC-14 3+4open / external-CSS+real-reftest P1 ✅。**真实剩余工作**（非 doc）：(a) ~~DC-14 独立 Oracle~~ ✅ **R669 已落地**（`reftest-oracle` 子命令 + `make reftest-oracle`，原估「harness 改 chromium-ref code/infra」已由 code-agent 完成；非剩余工作）；(b) Phase A 字体度量 plateau（IFC↔FontLoader 统一，按 rally 跨会话续跑协议推进）；(c) 真 incomplete 特性（Float/sticky/scroll 布局 + clip-path/backdrop-filter/mask 渲染，code）。doc 侧 stale-claim 纠正穷尽，后续转支持 code-agent 或 Phase A 续跑。零源码（read-only 代码审计 + rendering-compat.md DC-14 勾选）。

**R666（M7 DC-8 rigor 闭环·CPU ClipPrimitive + TransformPrimitive framebuffer 像素断言单测·LANDED·DC-8 13/13）：承 R665（CPU DC-8 11/13），补最后 2 项 CPU clip/transform framebuffer 测试，DC-8 闭环 13/13。**实现**（`crates/render-foundation/src/cpu/tests.rs`）：① `cpu_full_scene_clip_rect_clears_outside`——黑 fill 全屏 + ClipPrimitive rect (8,8)-(24,24) → apply_clip 清区外白 → 断言区内 (16,16) 黑 / 区外 (2,2)+(6,16) 白（验证 apply_clip post-process 裁剪语义）；② `cpu_full_scene_transform_translates_content`——左半屏黑 fill + TransformPrimitive 纯平移 tx=8（a=1,b=0,c=0,d=1）rect 全屏 → apply_transform_post 反向采样 → 断言 (20,16) 黑（src=12 原黑区，内容右移）/ (4,16) 白（src=-4 越界清白）（验证仿射平移）。**验证**：render-foundation lib **502→504 测试全绿**（+2，0 回归）；clippy -D warnings 干净。**勾选**（rendering-compat.md DC-8）：TransformPrimitive + ClipPrimitive ✓——**DC-8 现 13/13 全 framebuffer-rigor 覆盖**（与 R664 GPU DC-9 13/13 对称，M7 验证 rigor 全闭环）。**意义**：M7 DC-8 + DC-9 framebuffer 像素断言测试全 13/13（CPU+GPU 双侧），R661 识别的 M7 rigor gap 彻底闭合——「每图元渲染能力有对应 framebuffer 像素断言单测」M7 验证 bar 达成。零既有行为变更（纯新增测试）。

**R665（M7 DC-8 rigor·CPU ImagePrimitive + Stroke + PathFill + PathStroke framebuffer 像素断言单测·LANDED·对称 R664 GPU）：承 R661 识别的 M7 rigor gap（CPU stroke/image/clip/transform/path 缺 framebuffer 测试），本轮补 CPU 侧 4 测（对称 R664 GPU 侧）。**实现**（`crates/render-foundation/src/cpu/tests.rs`，复用 `render_full_scene` + `fb.get_pixel` pattern）：① `cpu_full_scene_image_solid_red`——1×1 纯红 ImageData → ImageCache → ImagePrimitive rect 16×16 → `render_full_scene(..., Some(&mut image_cache), ...)` → 断言中心 RGBA≈红；② `cpu_full_scene_stroke_horizontal_line`——水平线 (0,16)-(31,16) 宽 4 → 断言中心黑 + 上方白；③ `cpu_full_scene_path_fill_black_rect`——矩形多边形 (4,4)-(28,28) → 断言中心黑 + 角白；④ `cpu_full_scene_path_stroke_closed_rect`——闭合矩形描边 (8,8)-(24,24) 宽 3 → 断言内部白 + 顶边带黑。**验证**：render-foundation lib **498→502 测试全绿**（+4，0 回归）；clippy -D warnings 干净。**勾选**（rendering-compat.md DC-8）：ImagePrimitive / StrokePrimitive / PathFillPrimitive / PathStrokePrimitive 4 项 ✓（DC-8 现 11/13 framebuffer-rigor，剩 clip/transform 为独立 gap，R661 标注）。**意义**：M7 DC-8 从「7/13」补到「11/13」framebuffer-tested，与 R664 GPU 13/13 对称覆盖主渲染图元；CPU clip/transform framebuffer 测试为残余 gap（独立，非阻塞）。零既有行为变更（纯新增测试 + ImagePrimitive import）。

**R664（M7 DC-9 rigor·GPU ImagePrimitive + PathFill/PathStroke framebuffer 像素断言单测·LANDED）：承 R661 识别的 M7 rigor gap（GPU ImagePrimitive 无测试 + PathFill/PathStroke 仅 mesh 顶点测试无 framebuffer readback），补 3 个 `test_gpu_full_scene_*` framebuffer 像素断言测试。**实现**（`crates/render-foundation/src/gpu/renderer/tests.rs`）：① `test_gpu_full_scene_image`——1×1 纯红 ImageData → ImageCache.insert → ImagePrimitive rect 16×16 → `render_full_scene_gpu(..., Some(&mut image_cache), ...)` → readback 断言中心 (8,8) RGBA≈(255,0,0,255)（验证 draw_image_pass 纹理采样路径）；② `test_gpu_full_scene_path_fill`——矩形多边形 (4,4)-(28,28) 黑填充 → 断言中心 (16,16) 黑 + 角 (1,1) 白（验证扫描线填充 + clear color）；③ `test_gpu_full_scene_path_stroke`——闭合矩形描边 (8,8)-(24,24) width=3 → 断言内部 (16,16) 白 + 顶边带 y=8/x∈[8,24] 至少一黑像素（验证描边绘制）。**验证**：render-foundation lib **495→498 测试全绿**（+3，0 回归）；clippy -D warnings 干净。**勾选**（rendering-compat.md DC-9）：ImagePrimitive / PathFillPrimitive / PathStrokePrimitive 3 项 ✓（DC-9 现 13/13 全 framebuffer-rigor 覆盖，除 mesh-only→framebuffer 升级；R661「DC-9 image/path 未勾」缺口闭合）。**意义**：M7 DC-9 从「10/13 framebuffer-tested」补全到「13/13」（剩 CPU DC-8 image/stroke/path/transform/clip framebuffer 测试为独立 gap，R661 标注，非本轮）。零既有行为变更（纯新增测试）。无 commit hash（本轮提交）。

**R663（doc-accuracy·DC-12 高级视觉效果 stale-claim 纠正·零源码）：承 R660/R661 M7 stale 纠正谱系，read-only 代码审计 goal rendering-compat.md「DC-12 高级视觉效果」+ Support Envelope「渐变/阴影/图片/线段/路径/变换/裁剪/滤镜/混合模式渲染」行的「未实现」声明。**实证已渲染**：① **text-shadow**——painter/text.rs:1067/1231/1451 `if has_text_shadow { push shadow glyph at (glyph_x+shadow_ox, glyph_y+shadow_oy, shadow_color) }` + `test_paint_text_shadow_basic`（原「paint 未生成 text-shadow 图元」过时）；② **多背景图层**——painter/effects.rs:134 `for layer in background_image.iter().rev()` 全图层叠加 + `test_paint_multiple_overlapping_backgrounds`（原「仅渲染第一个」过时）；③ **重复渐变**——cpu/gradient.rs:28 `if gradient.repeating`（原「paint 未实现」过时）；④ Support Envelope **线段/路径/变换/裁剪/滤镜/混合模式** 6 行 `XPrimitive 已定义但渲染器未实现` → 全 ✅ 已实现（M7，见 DC-8/9，R660/R661）。**纠正**（commit 6ac4f94d）：DC-12 checklist 勾选 text-shadow/多背景图层/重复渐变 3 项（附 test ref）；Support Envelope + known-gaps（双表）多背景图层/重复渐变 + 上述 6 行 → ✅ 已实现（M7）。**保留「未实现」**（已解析但效果未渲染，解析≠效果）：clip-path（css-parser 解析，shape 裁剪未实现，ClipPrimitive 仅 rect）/ backdrop-filter（computed_style 存属性，需 backdrop 捕获未渲染）/ CSS mask（MaskImage 解析，未渲染）/ border-image（registry 存属性）。**意义**：延续 M7 stale 纠正，goal doc「视觉效果」区从「大面积未实现」精确化到「text-shadow/multi-bg/repeating/全图元 ✅ 已渲染，clip-path/backdrop-filter/mask/border-image 效果仍待实现」；与 DC-8/9 granular rigor（R661）一致。下一步：clip-path/backdrop-filter/mask 渲染（code-agent）或继续 DC-11 layout stale 审计。零源码（read-only 代码审计 + rendering-compat.md 纠正）。

**R662（DC-13 line 321 zero-webview 直接渲染路径对比·WebView 层 byte-identical engine-direct·新增 --via-webview 模式·零既有行为变更）：DC-13 line 321 要求「同一输入经 zero-webview 直接渲染路径截图 vs Chromium，避免产品层↔WebView 层互相掩盖」，此前 product-smoke 只走 engine-direct（render_to_framebuffer_with_base → RenderPipeline）。**交付**：`tests/wpt-runner` 加 `zero-webview` 依赖 + `reftest::render_via_webview_to_framebuffer`（WebView::load_html 嵌入边界 → RenderPrimitives → 同源 create_font_loader + GlyphCache + render_full_scene）+ product-smoke `--via-webview` flag。**验证（welcome.html 800×600，自包含）**：via-webview **16.16%** vs Chromium，engine-direct **16.16%** vs Chromium，**WebView-vs-engine-direct = 0.00%（0 px，byte-identical）** ★。→ WebView 嵌入层对自包含页**透明**，产品层（ZeroBrowser 图元消费 DC-10）与 WebView 层**不互相掩盖**，DC-13 line 321 满足；残余 16.16% 仍是字体墙（R633 Phase A）非层间掩盖。范围=自包含 fixture（load_html 不做 base_dir，外链资源走 fetch_url HTTP 路径）。make test 全绿（1156+512+495+... 全 0 failed，59+10 ignored = 真实网站测试）+ clippy -D warnings 干净。证据 `evidence/product-static/welcome-zeroweb-via-webview.png` + [`evidence/r662-...txt`](./evidence/r662-webview-render-path-dc13-line321-2026-06-26.txt)。

**R661（doc-accuracy·M7 framebuffer-test rigor 审计 + DC-8/9 granular 勾选·零源码）：承 R660 deferred（granular DC-8/9 勾选须按 M7 bar 做「每图元 framebuffer 像素断言」审计），本轮 read-only 审计 CPU/GPU 各图元的**测试 rigor**（不只是函数存在）。**审计结论（rigor 不均匀）**：① **GPU（DC-9）9/13 图元有 framebuffer 像素断言测试**——`test_gpu_full_scene_*`（fills/rounded_rect/gradient/shadow/stroke/empty + filter 8 变体 opacity/brightness/contrast/grayscale/hue_rotate/invert/saturate/sepai + transform_translation）+ `test_gpu_renderer_clip_rect_limits_rendering` + `test_render_scene_with_alpha_blending`，均 `read_pixels`+assert（gradient 实测断言 left R≥252/right B≥252，**精确匹配 M7 spec**）；② **CPU（DC-8）gradient/shadow/filter/blend 有 `fb.get_pixel` 像素断言**（cpu/gradient.rs:155 / shadow.rs:96 / effects.rs:46-103）；③ **rigor gap（真实剩余 M7 验证工作）**：GPU **ImagePrimitive 无测试** + **PathFill/PathStroke 仅 mesh 顶点测试**（gpu/mesh.rs test_stroke/path_mesh_*）**无 framebuffer readback**；CPU **stroke/image/clip/transform/path 无确认 framebuffer 测试**。**勾选**（commit b76f9e8f）：DC-8 7/13（fill/rounded/glyph + gradient/shadow/filter/blend）+ DC-9 10/13（fill/glyph + rounded/gradient/shadow/stroke/transform/clip/filter/blend）；**未勾**：DC-8 image/stroke/path×2/transform/clip + DC-9 image/path×2（implemented 但无 framebuffer 像素断言测试）。**意义**：M7「图元覆盖」从 R660 的「summary ✅ 已实现」精确化到「**framebuffer-rigor 已验证 17/26 项（GPU 10 + CPU 7），剩 9 项缺 framebuffer 测试**」——image/path framebuffer 测试是真正剩余 M7 验证工作（非「M7 未起步」）。下一步：补 image/path framebuffer 单测（code-agent territory）或继续 DC-13/Phase A。零源码（read-only 测试审计 + rendering-compat.md DC-8/9 勾选）。

**R660（doc-accuracy·M7 图元覆盖/浏览器消费 P0 stale 纠正·零源码·reframe 项目状态）：read-only 代码审计发现 goal rendering-compat.md 仍描述 **pre-M7 起步态**（"CPU 仅 3 种图元 / GPU 仅 2 种 / append_webview_primitives 仅 fills+glyphs 丢弃 11 种 / P0-致命"），与当前代码**事实矛盾**。**代码实证**：① `append_webview_primitives()`（app_render.rs:1512）遍历 `RenderPrimitives` **全 13 字段**（line 1526-1840：fills/rounded_rects/shadows/gradients/images/strokes/path_fills/path_strokes/clips/transforms/filters/blend_modes/glyphs），无静默丢弃；② **CPU** 渲染器有 `render_gradient`（逐像素插值 cpu/gradient.rs）/ `render_shadow`（cpu/shadow.rs）/ `render_image`（cpu/mod.rs）/ `render_stroke`+`render_path_fill`+`render_path_stroke`（cpu/stroke.rs）/ `apply_filter`+`apply_blend_mode`（cpu/effects.rs）；③ **GPU** 渲染器有真实 wgpu+mesh 管线 `draw_gradient_pass`/`draw_image_pass`/`draw_rounded_rect_pass` + `collect_shadow/rounded/gradient/image/stroke/path_fill/path_stroke_vertices` + color/blur filter collector（gpu/renderer/mod.rs + gpu/mesh.rs），**非 CPU passthrough**，附 `test_gpu_full_scene_gradient/shadow/stroke` + mesh 单测。**纠正**（commit 5f547499）：rendering-compat.md **current-state 表全标 ✅ 已实现（M7）**——Support Envelope（渲染器覆盖/浏览器消费/渐变/阴影/图片 5 行）+ Current Proven Baseline（CPU/GPU/浏览器消费 3 行）+ known-gaps（P0-致命 渲染器覆盖 + 浏览器消费 2 行 → ✅）+ infra 注释（cpu.rs/gpu/ "仅支持" → M7 覆盖）；**DC-10 item 1 勾选**（全 13 字段消费，grep 实证）；**审计表（2026-06-07）保留作历史快照** + 加 header note（P0 图元/浏览器 → M7 解决；Margin/BFC → R323；验证 → R484）；line 426「3/13 种」stale 事实纠正。**deferred**：granular DC-8/9 逐项 checklist 勾选须按 M7 验证 bar 做**每图元 framebuffer 像素断言 rigor 审计**（函数+测试存在但每项 test-rigor 未逐项核），known-gaps 行文已注明。**意义（reframe）**：M7 此前被 goal doc 描述为「critical-path P0 致命未起步」的最大瓶颈，**实证已基本完成**（CPU+GPU+浏览器消费三端均支持全 13 图元）——渲染能力远强于 doc 所述，当前 reftest plateau 瓶颈是字体度量/布局结构性（Phase A）非图元覆盖。下一步：DC-8/9 granular rigor 审计（逐项 framebuffer 断言）+ 继续 DC-13/Phase A。零源码（read-only 代码审计 + rendering-compat.md 纠正）。

**R659（DC-13 line 322 产品 fixture 窄屏 viewport 覆盖·welcome/morning/wintertc·零源码）：承 R658 addendum（legacy 子集窄屏），补齐 3 个产品 fixture 的窄屏 375×667 覆盖，至此 DC-13 line 322「桌面+窄屏两 viewport」对全部产品 + legacy fixture 完整。**交付**：`evidence/product-static/narrow/{welcome,morning,wintertc}-{chromium,zeroweb}-375.png` + diff%。**窄屏 diff%**：welcome 16.16→**25.35%** / morning 18.15→**22.42%** / wintertc 13.59→**25.74%**（wintertc +12pp 升幅最大）。**逐行 ASCII 体检（wintertc 最可疑案）**：三条全宽 nav/section `+++` 带（y≈192-224）CHR 与 ZW **完全一致**→结构骨架 intact；hero logo/nav 按钮/参与方 logo grid 两侧均渲染，仅字形位置差异。**thick_core**：welcome 窄 33.5%（与其桌面 26% 同源卡片背景+字形漂移）/ morning 10.7% / wintertc 15.8%——morning/wintertc 低 thick 直接 = 字体/边噪声，welcome 经 ASCII 证伪（同 R658 §3 thick_core 被垂直字体漂移混淆）。★**裁决：3 产品 fixture 窄屏结构完整渲染，diff 升高纯字体墙放大**（窄宽→更多换行→更多字形像素+行度量漂移累积），无新响应式布局 bug。wintertc README 已知 flex-wrap logo 多 item 未布局 bug 在窄屏可视区（折叠线下）未新增暴露（仍待独立诊断，非本轮范围）。**意义**：DC-13 line 322 覆盖完成（全部产品+legacy 双 viewport）；全部 fixture 残余 diff 主导 = 字体墙（R633 Phase A 死锁），无新单 session clean lever。零源码（3 fixture × 2 PNG + evidence + master.md R659）。详见 [`evidence/r659-product-narrow-viewport-2026-06-26.txt`](./evidence/r659-product-narrow-viewport-2026-06-26.txt)。

**R658（DC-13 legacy-html smoke fixture 凑齐 ≥20 页·全量结构体检·新增 product-smoke-legacy 门禁·零源码）：承 R656（001）+ R657（002/003/004，a5a3e5fd 已提交）= 4/20，goal line 316 要求 legacy-html smoke「首批至少 20 页」。本轮补 005–020 凑齐 20 页全集。**交付**：`evidence/product-static/legacy-html/testpage-{005..020}.htm` + 全套 chromium oracle PNG + ZeroWeb CPU PNG + diff%——005 ol 编号变体(type/start/value) / 006 嵌套列表(disc/circle/square 缩进) / 007 h1–h6 级联 / 008 `<font>` size1-7±+color+face / 009 colspan/rowspan+thead/tbody/tfoot / 010 img align=top/middle/bottom+td valign / 011 blockquote 嵌套+address+small/cite/q/abbr / 012 `<hr>` 变体(默认/size/width/align/noshade) / 013 pre+code/kbd/samp/tt/var(white-space:pre) / 014 CSS1 bg-color+text-indent+text-decoration+font 简写+list-style / 015 CSS2 relative/absolute+两列 float 布局+clear / 016 CSS1 盒模型 margin 折叠+solid/dashed/dotted/double border / 017 table-layout:fixed+colgroup/col+caption(border-collapse) / 018 a:link/visited/hover+text-transform / 019 继承+line-height+justify / 020 HTML3.2 综合 90s 主页(TABLE 布局+TR/TD BGCOLOR+IMG ALIGN+FONT+UL/A+HR+CENTER，testpage.htm 风格)。**diff% 全量(800×600)**：001 16.70 / 002 6.80 / 003 7.65 / 004 7.72 / 005 5.09 / 006 3.80 / 007 3.35 / 008 3.08 / 009 5.31 / 010 3.13 / 011 6.29 / 012 4.70 / 013 2.70 / 014 4.53 / 015 7.04 / 016 9.90 / 017 4.26 / 018 2.70 / 019 4.77 / **020 20.87**（中位~5%，无崩溃/空白/结构错乱）。★**裁决：legacy 渲染结构性健康，无 major bug**——20 页逐像素/逐行 ASCII 体检：body/td/tr 背景色与文本色全精确匹配(020 像素采样 body #FFFFCC/nav #EEEEEE/content 白均对，**推翻视觉模型「色差」误判**=fontdue 锐利字形被误读)、表格 2 列并排+边框可见(017 ZW1940/CHR1617 border px)、colspan/rowspan、font 标签、float 文本环绕**双形态均工作**(003 img-align + 015 CSS block-float，015 ASCII 体检证绿色 float 盒左+正文右侧并排，早期「窄列」误判=把绿盒当 content)、列表/标题/blockquote/hr/pre、CSS1/2 盒模型+定位+继承全正确。**全部 diff% = 字体墙**（fontdue 行度量 vs chromium NotoSansCJK 垂直渐进漂移 + AA，020 内容整体下移~30px / 012 hr 行 ±1px 漂移），R633 多会话 plateau，非 legacy 特有、非单 session lever。**方法学沉淀**：thick_core%(5×5 腐蚀存活)高≠布局 bug——垂直字体漂移时 glyph 整体下移形成大块实心 diff 带→thick_core 虚高(020 51.7% 经 ASCII 证伪)，须配逐行 ASCII 结构对比交叉验证；勿信视觉模型对缩略图的灾难性口头判词(与像素数值矛盾时以像素为准，纠正 R657 同款误判)。**产品门禁**：新增 `make product-smoke-legacy`(trend-only 退出 0，跑 20 页报 diff%，因 diff 全字体墙非回归)纳入 product-smoke 路径(goal line 316)；工具 `run-all.sh`(cached-oracle 复用)+`diff-localize.py`(纯 PIL)。**意义**：DC-13「首批 ≥20 页」交付完成 20/20，「可读且结构不崩」acceptance(goal line 318)逐项通过；延续 R656/R657 结论 20× 强化。零源码(16 fixture+2 工具+Makefile target+evidence+master.md R658)。详见 [`evidence/r658-legacy-html-smoke-20fixtures-2026-06-26.txt`](./evidence/r658-legacy-html-smoke-20fixtures-2026-06-26.txt)。

**R657（DC-13 legacy-html smoke fixture 002/003/004·续 R656 扩 fixture 集 1→4/20·结构性像素验证·零代码）：承 R656（首批 1 页 + capture-legacy-oracle.mjs 流水线），goal line 316 要求 legacy-html smoke ≥20 页，本轮补 3 页覆盖 R656 未触的 legacy 模式。**交付**：`evidence/product-static/legacy-html/testpage-{002,003,004}.htm`（各含 chromium oracle PNG + ZeroWeb CPU PNG）——002 CSS1/2 内联 `<style>` + 类选择器（serif/sans-serif font-family 继承、h1 border-bottom、.box dashed border+padding+bg、.center/.small class）；003 IMG `align="left" hspace="8"` + 文本环绕 + `<br clear="left">` + `<hr size="3" noshade>`（经典 old-web 图片浮动+环绕）；004 嵌套 table（outer td 内嵌 bgcolor 表）+ `<dl>/<dt>/<dd>` + `<blockquote>` + `<pre>`。**像素对比（PIL ground-truth, 800×600）**：002 **6.80%** / 003 **7.65%** / 004 **7.72%**（001 基线 16.70% 校验方法一致）——三页均**显著低于** welcome ~17%（文本密度低于 001，font/AA 噪声面小）。**结构性验证（content bbox + 行带 diff 分布）**：vision diff 工具初判 003 报「灾难性破损：每字母独占行 / 图片缺失 / 30% 一致」**与 7.65% 像素数值矛盾**（真破碎应 30-60%+），PIL 像素分解**证伪该误判**——002 内容止于 y≈250 两侧同高（diff 均匀文本区，y≥250 全 0，纯 font/AA 噪声无结构破损）；003 chr bbox (8,13,791,244) / zw (8,39,766,245) 真实差异 = ZW 首行低 26px + 环绕文本窄 ~25px + diff 集中 y=150-199 浮动带（**图片存在**，float 环绕生效但几何偏差）；004 chr (8,25,680,473) / zw (8,39,749,480) ZW 低 14px + 某 block 宽 ~69px + diff 集中表格/dl 区，总高接近。**差异全映射已知部分实现特性（非新 legacy bug）**：003 float P2-partial（goal line 79/409 明确 float exclusion/clear 不完整）+ 首行 y 行盒度量；004 嵌套 table 宽度 + line-height 字体度量（R177/R656 table-width 谱系）。**意义**：DC-13 进度 1→4/20（覆盖 presentational-attr / CSS1/2 内联 / float+wrap / 嵌套结构 四类）；方法学沉淀——低 diff% 须 PIL bbox/行带分解确证「非空白假低」，勿信视觉模型对缩略图的灾难性口头判词（与像素数值矛盾时以像素为准）。下一步续扩 ≥20 + legacy-html 纳入 product-smoke 回归门禁。零代码（fixture+evidence，PNG 既有流水线）。详见 [`evidence/r657-legacy-html-smoke-fixture2-4-2026-06-26.txt`](./evidence/r657-legacy-html-smoke-fixture2-4-2026-06-26.txt)。

**R656（DC-13 legacy-html smoke fixture 001·首批 + legacy 渲染体检通过·零代码）：goal line 38-55/316-318 把 Legacy Static Web（HTML 3.2/4 + CSS1/2）提升为短期高优先级，要求 legacy-html smoke fixture 集（≥20 页含 testpage.htm 风格），此前完全缺失。**交付**：evidence/product-static/legacy-html/testpage-001.htm（合成最小代表页，覆盖 BODY BGCOLOR/TEXT/LINK/VLINK + TABLE BORDER/CELLPADDING/width + TR/TH/TD bgcolor/align/valign + IMG ALIGN + FONT size/color/face + UL/LI + A + b/i/strong/em + hr）+ tests/wpt-runner/scripts/capture-legacy-oracle.mjs（puppeteer-core + /usr/bin/chromium 截 file:// HTML→PNG，可复用其余 19 页）+ oracle/ZeroWeb PNG。**对比**：diff 16.70%（与 welcome ~17% 同量级）。**关键结论：legacy 渲染功能正常，无 major bug**——结构色双方一致（#DDDDDD TR bgcolor / #FFCCCC TD bgcolor / #FFFFEE body bg / #000080 navy text 均渲染），R502 presentational hints + R255 UA display 覆盖 legacy 核心。**diff 主因 = font/AA 平台噪声**（navy 文本 ZW 16324px fontdue 锐利 vs CHR 5147px Skia 反走样多灰阶，同 R174/R388 字体墙），非 legacy 特有。次要 minor（<1.5%，trend 指标非阻塞）：table width="80%" ZW 634 vs CHR 626（~8px，percentage/border-box）；TH 表头 y ZW ~175 vs CHR ~165（~10px，cell line-height）。**意义**：DC-13 缺失交付开始补齐；legacy 渲染体检通过（残余是通用字体墙非 legacy 特有，无独立 clean lever，同文本域 plateau）。下一步扩 fixture ≥20 页纳入 product-smoke 门禁。零代码（fixture+脚本+evidence）。详见 [`evidence/r656-legacy-html-smoke-fixture1-2026-06-26.txt`](./evidence/r656-legacy-html-smoke-fixture1-2026-06-26.txt)。
