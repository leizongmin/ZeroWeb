# 归档轮次 R602–R629 — DC-14 fresh-oracle 诚实测量 + control-chars/R101 font_size 8 轮死锁 + plateau 多角度再坐实 + harness ref_base_dir 修复 era

**归档日期**: 2026-06-26（R657 轮迁出，超出最近 20 轮窗口）

**范围**: R602–R629（R604 无记录）。下列逐轮详记从 master.md 100% 原样迁出，未修改。综合结论见 master.md「综合裁决」节与本归档顶部摘要。

---

**R629（text-decoration-thickness 单点证伪 + 反 thrash·零源码变更）：承 R628（text-decor 簇 font-metric-coupled 定论，thickness 单点预期 yield 0-1/3）。上 session 中断态 = computed_style.rs 加了 `text_decoration_thickness: LengthValue` 字段但未在 default_impl.rs 初始化 → **build 破**（E0063 missing field），apply.rs/effects.rs 未接 = 半成品。★**复核 R628 evidence 逐案**（非重测，R628 已测）：7 POLLUTED（length-rounding-001/002/min-val、fixed、calc、percent、underline）**z_vs_ref 全 0.00%** = test 与 ref 都渲染默认厚度（ZW 忽略该属性）→ 双方已互匹配的假通过；实现 thickness（带 floor-rounding）后双方同步变（test 2.3px→2px、ref 2px→2px）**仍互匹配 = self-source pass 数 0 增**，仅 z_vs_chr 诚实度改善（length-rounding 12.20%→更低，非 pass-rate lever）。4 self-fail（ink-skip-dilation 1.38% / scroll 2.97% / linethrough+overline 0.50% 边界）**非纯 thickness**（需 skip-ink/scroll/字体度量）→ thickness 单独不动其状态。5 self-pass（from-zero-sized-font/non-inherit/vertical-001/002）+ 7 POLLUTED = **11 案回归风险**（实现 rounding/positioning 错则破当前 pass）。**裁决**：thickness 单点 = **0 self-source yield + 11 案回归风险 vs ~3 案 oracle 诚实度** = net-negative/neutral 单 session lever，**不实现**（半成品字段已 revert，build 恢复 clean）。R628「0-1/3」预测由其自身 evidence（z_vs_ref=0.00%）坐实——重测已测之物 = thrash。**反 thrash 锚**：thickness/skip-ink/underline-offset/underline-position/font-metric-coupled 簇**勿再单点重试**（同 R164 vertical-rl 4 轮证伪谱系），真修复 = Phase A 字体度量统一（多会话）。**plateau 跨 font_size(R627) + text-decor(R628) 确认**，单 session clean lever 谱系穷尽（R100-R628），forward motion 全多会话架构（Phase A IFC 统一 + estimate-vs-fontdue 字体度量统一 / multicol fragmentation / baseline-export / font-features rustybuzz），**需用户决策：承诺 Phase A 多会话 or 接受 plateau**。零源码变更（read-only R628 evidence 复核 + build check）。

**R628（css-text-decor fresh pollution + font-metric-coupled 确认·零源码变更）：承 R627（font_size 线 font-metric-coupled 定论），测量 css-text-decor fresh pollution（oracle 非 stale—R601 未触 text-decor 资产，R417 oracle 仍有效）确认 text-decor 簇**亦 font-metric-coupled**。**fresh pollution**（[evidence](./evidence/r628-textdecor-pollution-fresh-2026-06-25.txt)）：**116/184 self-pass = 63% polluted**（vs R417 stale 70/242 chr<1%—fresh 口径更准）。**簇分解**（top）：text-emphasis-position-property **~50**（Phase-A line-box 阻塞 R392/R393，已实现+回退）/ text-decoration-inset **18**（underline-position 字体度量）/ text-decoration-skip-spaces **9**（per-segment 渲染特性）/ text-decoration-thickness ~5（**from-font + 自定义 variable font** 耦合，如 thickness-fixed 用 `@font-face underline-variable` + `text-underline-position: from-font`）/ text-decoration-line 5。**grep 证 text-decoration-thickness / skip-spaces / skip-ink / text-underline-offset / text-underline-position 全 0 hits = 完全未实现**。**裁决**：text-decor 簇**亦 font-metric-coupled**（同 font_size R627 谱系）——thickness/offset/position 的 auto/from-font 分支需字体度量（underline thickness/position 字体表），explicit-length 分支（如 thickness-length-rounding 2.3px→2px）虽确定但用 table + 5 styles（solid/double/dotted/dashed/wavy）致 table-cell decoration 定位 + 多 style 渲染复杂度，单点 thickness 实现预期 yield 低（0-1/3）。**plateau 跨 font_size + text-decor 两 major 簇均 font-metric-coupled 确认**——真修复 = Phase A **字体度量统一**（layout/paint 共用 fontdue underline/emphasis/ascent 度量，非 estimate_char_width / 0.06×font_size 启发式），多会话。text-emphasis ~50 案另需 line-box 定位（Phase A IFC）。零源码变更（read-only REFTEST_DUMP + cross-validate + grep + PIL）。

**R627（Phase A font_size 第 3 seam net-negative 确认 + R623 误诊纠正·零净源码）：★clean node-targeted probe 终结 R617-R624 的 font_size 诊断矛盾——实证 **layout IFC 全程正确算 font_size=64**（control-chars-080 div 26v1 的 ::after text 28v1：`collect_inline_items` 查 parent=div styles → font_size=64，probe `[IFCFONT] child=28v1 parent=26v1 style_present=true font_size=64`）。**纠正 R623/R622「layout IFC store=16」**——该结论来自 R622 head-截断 probe（漏关键 node），R623 未复验 store 值即继承。**真 loss = pure-Ahem inline_layout 存储门控**（`inline_finalization.rs:493 if !is_pure_ahem { return }`）：control-chars div 用默认字体非 Ahem → 门控 return 不存 `inline_layout` → paint `use_stored=false`（probe `[USESTORED] node=26v1 inline_layout=false`）→ paint 空-styles IFC 重跑 → font_size_overrides 为空（compute_final 此前不调 store_font_sizes_from_ifc，仅 remeasure 子集存）→ 默认 16。** attempted fix（第 3 seam）**：compute_final pure-Ahem 门控**之前**调 `store_font_sizes_from_ifc`（仅存 font_size 度量值，非完整 line-box；区别 R84/R355 inline_layout 门控放宽、R125/R198/R205 gate-entry 加宽两 net-negative seam）。**A/B（css-text clean WITH vs WITHOUT，test-guard release）= net -15**：WITH 195 fail / WITHOUT 180 fail；**16 新回归**（white-space/pre-wrap-align-{center,end,left,right,start}-001/002/003 ×15 + word-spacing-002）**1 新通过**。control-chars-080 自身 0.01%→0.18% 改善但仍 <0.5% mismatch 阈值未过（fontdue \0080 字形 64px 仍小，需配合 R617 control-box 渲染）。**net-negative 根因 = Phase A 双 IFC 路径发散**：存 font_size 使 paint-IFC 重跑（带 overrides）的 char-width 估算 ≠ layout-IFC 行断 → pre-wrap（宽度敏感）行断错位。fix 已 REVERT（inline_finalization.rs 回 HEAD，clean）。**裁决（终结 control-chars/large-font font_size 线）**：font_size 存储 patch（3 个 seam：inline_layout 门控 / gate-entry / font_size-value）**全 net-negative**——根因是 layout-IFC 与 paint-IFC **双路径**（paint 重跑带 overrides 必发散），**真修复 = Phase A IFC 统一**（单一 IFC 运行，paint 复用 layout 结果，非两套），多会话架构。R617-R627 11 轮 control-chars 诊断线收口：blocker 精确定位 = paint-IFC 重跑发散（非 font_size value 错、非 storage gate 单点），须 Phase A 统一。零净源码（fix 实现 + A/B + revert，3 探针全 revert）。

**R626d（DC-14 fresh-oracle chr<1% 精确化 + CSS2 staleness 低影响确认·零源码变更）：承 R626b/c（flexbox/fonts/multicol stale-oracle 修复），用 fresh oracle 精算 chr<1% 真一致率（master.md 旧数用 stale pre-R601 oracle，含假一致/假发散噪声）。**fresh-oracle chr<1%**（parse evidence z_vs_chr<1%）：**css-flexbox 237/497 = 47.7%**（master.md stale 50.6% R562 略乐观，fresh 更准）/ **css-fonts 97/282 = 34.4%**（≈ stale 34.8% R422，fonts 数本稳）/ **css-multicol 105/452 = 23.2%**（≈ stale 23.5% R427）。flexbox 50.6→47.7 的 -2.9pp 修正 = stale oracle 双向噪声（test-style.css 测试 stale-blank 致假发散，但另一些 stale-偶然一致被 fresh 揭真发散，net 略下）；fonts/multicol R601 资产影响 few 故微动。**CSS2 staleness 低影响确认**：CSS2 oracle（9239 PNG，R484 2026-06-21）早于 R601，但仅 ~13/6055 CSS2 测试引用 R601 顶层 css/support 资产（60x60-green/red + red-rect.svg），且多数用自身 subdir `support/`（如 `support/60x60-green.png` 解析到 CSS2/<subdir>/support/ 非顶层 css/support/）→ **CSS2 stale oracle 影响面 ~0.2%，不值得 9239-PNG 重抓**（vs flexbox test-style.css 全局影响 30 案）。**裁决**：non-CSS2 4 dir（flexbox/fonts/multicol/writing-modes）DC-14 数现 fresh 准确；CSS2 stale 低影响保留。fresh chr<1% 确认 plateau（flexbox 47.7% / fonts 34.4% / multicol 23.2% 全远低于 95% 目标，gap 全结构性）。**测量债清偿完毕**，forward motion 全结构性多会话（Phase A IFC font_size / multicol fragmentation / baseline-export / font-features rustybuzz 接入）。零源码变更（read-only evidence parse + CSS2 grep）。

**R626c（DC-14 stale-oracle 纠正续·css-fonts + css-multicol oracle 重抓 post-R601·零源码变更）：承 R626b（flexbox stale-oracle 修复）将其余受 R601 影响 dir 的 stale oracle 一并刷新——capture-oracle-per-dir.mjs --category css/css-fonts --category css/css-multicol 重抓（chromium HTTP 本地 wpt-data，284+455 ok / 0 fail）。**fresh-oracle cross-validate**（[evidence](./evidence/r626c-fonts-multicol-pollution-fresh-oracle-2026-06-25.txt)）：**css-fonts 151/249 self-pass = 61% polluted**（top: font-size-adjust-009/010/011 各 50%+、size-adjust-03、font-family-name-025、alternates-order、font-size-adjust-ic-height）；**css-multicol 41/124 = 33% polluted**（top: multicol-rule-nested-balancing-004 25%、subpixel-column-rule-width、multicol-contained-absolute、multicol-span-all-* 簇）。**fonts oracle-consistent ≈ 34.7%**（fresh，≈ R422 stale 34.8%——R601 fonts 资产 font-weight-ref.png 等对 oracle 影响微，fonts 数本就稳）。**裁决**：fonts/multicol fresh oracle 后污染仍结构性主导——fonts = font-size-adjust（grep 证 **完全未解析** 0 hits，font-metric-dependent = Phase A）+ font-features/variant（rustybuzz 未接生产 R330/R331/R332，~89 例仅 10% chr）；multicol = fragmentation/balance/rule（R113/R128/R131 结构死锁）。**与 flexbox（R626b 30 stale false-signal）不同**，fonts/multicol 的 R601 资产影响 fewer tests（font-weight-ref vs test-style.css 全局色），故 fresh oracle 未显著移动其数——但其 DC-14 数字现**新鲜准确**。**全 post-R601 stale-oracle 债清偿**：flexbox(R626b) + fonts + multicol + writing-modes(R602) 四 dir oracle 现 fresh；CSS2 oracle（9239 PNG，2026-06-22 R484）早于 R601 但 R601 的 css/support 资产影响 CSS2 测试较少（待证）。**plateau 以 fully-fresh oracle 数据再坐实**：fonts/multicol/flexbox polluted 全结构性，0 新 clean lever。零源码变更（read-only 重抓 + cross-validate + grep）。

**R626b（DC-14 stale-oracle 纠正·css-flexbox oracle 重抓 post-R601·纠正 R626 污染数）：★发现并修复 R626 fresh pollution 测量的 stale-oracle 污染——css-flexbox oracle PNG 是 **2026-06-21** 抓取，**早于 R601（2026-06-25 04:45）导入 test-style.css 等 26 support 资产**；故 chromium 经 HTTP 抓 oracle 时 support/test-style.css 缺失 → oracle 渲染 nearly-blank（如 align-content_space-between oracle 97.7% white，应有色块）。R602 仅刷新 writing-modes oracle，**css-flexbox / css-fonts / css-multicol oracle 仍 stale pre-R601**。**实证 ZW 正确**：ZW-test 正确加载 test-style.css（色块 red/#7FFF00/#00FFFF/#4169E1 与 css 一致），污染信号是 stale-oracle 假象（R388 stale-Ahem 谱系）。**修复**：capture-oracle-per-dir.mjs --category css/css-flexbox 重抓（chromium HTTP 本地 wpt-data，506 ok / 0 fail）。**fresh-oracle cross-validate**：污染 **104→74（32%→22.6%）**——30 案是 stale-oracle false-signal（ZW 本就正确）。**Top fresh polluted（74）仍全结构性**：flexbox-baseline-align-self-baseline（17.65%，baseline-export R266）/ flex-aspect-ratio-cross-size（17.45%，taffy R304）/ flex-abspos-inset-cross-size（17.45%，R363）/ flexbox-collapsed-item-horiz-001（15.04%，flex-collapse 布局非 painting，R626 控制实验已证 visibility:collapse painting 正确）/ flex-cross-size-border-box（14.28%，box-sizing+flex taffy）/ align-content cluster（5-7%，多行 flex 行分布，同色块位置偏移非 stale）。**裁决**：R626 的 104 polluted 含 30 stale-oracle 假信号，fresh 口径 74；flexbox 仍无 clean 单点 lever（top 全结构性），但 DC-14 数字现**准确**（honesty 增）。★ **flag**：css-fonts / css-multicol oracle 同 stale pre-R601 → 其 master.md DC-14 数（fonts 34.8% / multicol 23.5%）**系统性低估 ZW 正确性**（含 stale false-divergence），须同法重抓 oracle 得真数。**教训（方法论）**：cross-validate pollution 只与 oracle 新鲜度齐平；**任何 support 资产导入（R527/R529/R532/R601）后必须重抓受影响 dir 的 oracle**，否则污染数虚高。oracle-shots gitignored（本地可再生），故此为测量环境债非提交数据债。零源码变更（read-only 重抓 + cross-validate + PIL；flexbox oracle PNG 本地刷新）。

**R626（DC-14 fresh pollution measurement·css-flexbox 当前真候选扫描·零源码变更·plateau 以当前数据再坐实）：★替换 9 天 stale 的 analyze-pollution-2026-06-16 表（R603 标记须 fresh 重生成）——对 css-flexbox 497 案跑 fresh chromium-Oracle cross-validate（REFTEST_DUMP 全量 + cross-validate.py vs oracle-shots，R625 binary post-R525-R542 fixes）。**结果**：对比 497 / 同源通过 327（@0.5% strict）/ **污染（同源通过但 chromium 不一致）104 = 32%**（[evidence](./evidence/r625-flexbox-pollution-fresh-2026-06-25.txt)）。**Top polluted 全结构性**（PIL 像素 + 控制实验逐案）：① flexbox-baseline-align-self-baseline-horiz-001（17.65%）= baseline-export 簇（R266/R313/R316 ruled out）；② flex-aspect-ratio-cross-size-001（17.45%）= aspect-ratio taffy-blocked（R304/R325）；③ flex-abspos-inset-cross-size-001（17.45%）= abspos definite-size（R363/R97）；④ **flexbox-collapsed-item-horiz-001（15.04%）经控制实验澄清 = flex 布局结构性，非 painting**——minimal probe（`<div style=visibility:collapse;background:red>` 在 plain 与 flex 容器内）实证 ZeroWeb **visibility:collapse painting 正确**（0% red，painter mod.rs:380/495 hidden 检查生效）；该案污染真因为 collapsed flex item 的 flex-basis:0/cross-size-maintain/sibling-交互布局（R111 仅 zero flex_basis，未完全匹配 chromium §algo-visibility 折叠行为），taffy-coupled；⑤ flex-cross-size-border-box-001（14.28%）= box-sizing:border-box + flex stretch taffy 交互；⑥ align-content cluster（center/space-around/space-between/stretch 各 5-6%）= 多行 flex 行分布（taffy align-content 或换行 Phase A）。**裁决**：R525-R542 clean win 落地后 flexbox 当前 104 polluted 全结构性（baseline/aspect-ratio/collapse-layout/box-sizing-flex/align-content-multiline），0 新 clean 单点 lever——与 R426/R562 plateau 结论一致，但以**当前 post-fix 数据**坐实（非 06-16 stale）。**visibility:collapse painting 正确**结论 ruling out 一类假说（painting skip lever），collapsed-item 真因为 flex 折叠布局（多会话/taffy）。forward motion 不变（Phase A IFC font_size / multicol fragmentation / baseline-export）。零源码变更（read-only REFTEST_DUMP + cross-validate + PIL + 控制 probe，已清理 _tmp_visprobe）。**

**R625（harness fix LANDED·ref base_dir 分辨 + master.md 下一步 item 4 误诊纠正·净中性 pass-rate / DC-14 度量诚实性提升）：★发现并修复 reftest harness 一个真实 DC-14 度量 bug——参考页（ref HTML）的相对图片 URL 一直按**测试**文件目录解析（`wpt_file_loader.rs` 旧 `base_dir = test_path.parent()` + `reftest.rs` test/ref 共用同一 base_dir），导致参考文件位于不同目录（如 `reference/` 子目录）时其 `../support/*.png` 图片加载失败、ref 仅渲染边框无图。修复：`FileReftestCase` + `ReftestCase` 新增 `ref_base_dir`（参考文件自身父目录；about:blank 回落测试目录），渲染 ref 时用 `case.ref_base_dir.as_deref().or(base_dir)`（CPU + GPU 两路径）。**验证（REFTEST_DUMP + PIL）**：margin-collapse-vrl-022 的 ref 从「仅 teal 边框 + 白（0 orange/blue）」变为「teal 边框 + blue swatch 5000px + orange swatch 5000px」——4 个 25×100 swatch 现正确加载。**A/B（test-guard release self-source，4 dir）= net-neutral 零翻转**：writing-modes fail set WITH==WITHOUT（186/186 byte-identical）/ flexbox 368/497 / multicol 193/452 / fonts 283/287 全部 = 已知基线，0 PASS→FAIL 0 FAIL→PASS。机制：所有 image-ref 测试（vrl-022/vlr-023 等）因**真实**布局 bug 失败，swatch 加载与否均不翻 PASS。**价值 = DC-14 度量诚实性**（ref 渲染正确，与 chromium Oracle 对齐，消除 self-vs-oracle 图片缺口假差异），**非 pass-rate lever**（净中性）。★ **纠正 master.md「下一步 item 4」margin-collapse-vrl-022/vlr-023 误诊**：item 4 称其为「R323×writing-mode axis-swap 2 案 clean lever」，**PIL 像素分析证伪**——test 渲染 orange **38775px** + blue **16050px**（远大于 ref 的 25px swatch 条带 5000px/色），是真 **vertical-rl block-flow 布局 bug**（box 尺寸/堆叠方向错误致 box 远大于预期），**非 margin-collapse axis-swap 单点**；且该 2 案 ref 此前 swatch 未加载（base_dir bug）掩盖了真实 diff 量级。故 margin-collapse axis-swap **非 clean 2-案 lever**，勿再以 axis-swap 为入口（真因为 vertical-rl 布局结构性，多会话）。附 swatch-orange.png 导入（css-writing-modes/support/，上游本有此资产，import 脚本漏抓，R527/R529/R532/R601 谱系）。86 wpt-runner 单测 + cargo test --workspace 全过（test-guard exit 0），clippy/fmt 干净（注：style-system/lib.rs:592 一处 R502 遗留 fmt drift 预存于 HEAD，非本变更，未动 per code-guidelines §3）。零 pass-rate 回归。**

**R624（doc-maintenance 调研·零源码变更：doc-side 静态印证 R623（code agent `561c69d2` 终结性 probe）+ control-chars/R101 诊断线收口——静态事实 compute_element_style_internal 主路径 `return resolved`（lib.rs:546）结构上保 resolve 的 64（与 R623 实测 div 26v1 `COMPUTED_RETURN=Px(64.0)` 吻合）；R623 定论 = style-system 全程正确 64，loss 在 style-system→layout/paint font_size 传播 = memory R101b/c paint-box=storage-box（修复点 = 统一 layout IFC 与 paint IFC font_size 来源，painter/text.rs paint_text + engine.rs compute_final_inline_layouts，非 style-system）；★R622 教训 = head 截断 probe 输出误读（head-10 漏 div 26v1 → 假 computed=16）→ 须 node-targeted grep 非截断；诊断线 R617-R623 8 轮收口，plateau 确认，forward = Phase A）**：本轮 doc-side read-only 印证 R623（code agent 终结性 probe，style-system 正确 64 / loss 在 layout/paint）。**静态印证（lib.rs:277-547 compute_element_style_internal）**：主路径 `return resolved`（546），resolve_computed_style@507 返回后 quirks@518 + float/display@528 仅改 display 不改 font_size → **resolve 的 64 原样返回**，结构上坐实 style-system 保 64（与 R623 实测 div NodeId 26v1 `COMPUTED_RETURN=Px(64.0)` + ::after 继承 64 完全吻合）；early-return lib.rs:326（`ComputedStyle::default()` font_size=16）仅对**无规则伪元素**触发，control-chars `div::after` 有规则不命中（与 R623「store 实运行、style 正确」一致）。**R623 定论裁决（doc 印证）**：control-chars/large-font R101 簇 blocker **不在 style-system**（computed font_size 全程 64 正确），**在 style-system→layout/paint 的 font_size 传播**（layout `store_font_sizes_from_ifc` 据 computed 应得 64 但存 16 / paint IFC re-run 用默认 16）= **memory `r101-large-font-rootcause` R101b/c「paint-box≠storage-box」**——修复点 = **统一 layout IFC 与 paint IFC 的 font_size 来源**（painter/text.rs `paint_text` 调用 box + engine.rs `compute_final_inline_layouts` 存储 box），**非 style-system 改动**（R619/R620/R622 style-system 侧假设均 dead-end）。**★ R622 教训（方法论，防后续 session）**：R622 据 COMPUTED_RETURN **head -10 截断**输出（仅 node 3-20，漏 div 26v1）误判「div computed=16」→ 假 resolve-loss 结论；R623 用 **node-targeted grep**（div 26v1）纠正确 64。**probe 输出解读须 node-targeted grep，勿用 head 截断**（截断漏关键 node → 假归因）。**control-chars/R101 诊断线 R617-R623 收口**（8 轮：R617 box-render 症状 / R619 paint_content / R620 gate-skip[错] / R621 key-miss[superseded] / R622 resolve-loss[错,截断误读] / R623 定论 style 正确 loss 在 layout-paint / R618+R624 doc 印证）→ blocker 精确定位 layout-engine↔paint font_size 传播 = R101/Phase A 架构性，memory 明载 net-negative defer。**plateau 确认**：R588 sizing vein 后 control-chars/large-font 为最新「看似 clean」候选，8 轮实证全 R101/Phase-A-blocked → clean 单点 lever 谱系仍未破，R609 Phase A 升级立场不变。零源码变更（read-only lib.rs 结构核查 + R623 印证 + memory R101b/c 交叉）。

**R623（rendering-level DEFINITIVE probe·零净源码：★终结 R617-R622 多轮矛盾 probe 归因——clean node-targeted probe 实证 **style-system 全程正确**：div（NodeId 26v1）`COMPUTED_RETURN font_size=Px(64.0)`（::after 继承 64 亦对）；loss 在 **style-system(64)→layout/paint(16)** 传播，= R101/Phase A；**纠正 R620（has_text_children 错）+ R622（resolve-loss 错，head-10 截断误读漏 div）**，确认 R617/R618 原 R101 归因；勿再查 style-system 侧）**：承 R622 CONTINUE（probe 信号矛盾须 clean 消歧）做 node-targeted 核验。**关键修正**：R622 据 COMPUTED_RETURN head-10（仅 node 3-20，**不含 div 26v1**）误判「div computed=16」；本轮 grep **div 26v1 的 COMPUTED_RETURN = Px(64.0)**——**style-system 返回的 div computed font_size 正确为 64**。完整链（control-chars-080 div 26v1）：① `RESOLVE_INPUT Em(4.0) parent_fs=16` → `RESOLVE_OUTPUT Px(64.0)`；② `COMPUTED_RETURN node=26v1 font_size=Px(64.0)`（返回点正确）；③ ::after `RESOLVE_INPUT Px(64.0) parent_fs=64`（继承正确）→ `RESOLVE_OUTPUT Px(64.0)`。**但** layout `store_font_sizes_from_ifc` 存 ::after text=**16**（R622 probe），glyph 渲染 16（R617 [CTRLBOX]）。**裁决（终结矛盾）**：font_size 在 **style-system 全程正确（64）**，loss 在 **style-system→layout/paint 传播**（layout IFC 据 computed style 应得 64 但实得 16，或 paint IFC re-run 用默认 16）= **R101/Phase A propagation**（R617/R618 原归因正确）。**纠正**：R620「has_text_children gate-skip」**错**（gate 通过，store 实运行）；R622「resolve_computed_style→downstream computed resolve-loss」**错**（downstream computed 实为 64，非 16；R622 据截断 probe 误判）。**防后续 session 误靶**：control-chars/large-font blocker **不在 style-system**（computed font_size 正确 64），**在 layout-engine↔paint 的 font_size 传播**（IFC 据哪个 style map 取 font_size）——R101/Phase A 真修复点 = 统一 layout IFC 与 paint IFC 的 font_size 来源（memory R101b/c paint-box=storage-box），非 style-system 改动。3 探针已 revert（lib.rs 回 HEAD，clean）。零净源码变更（node-targeted probe + revert）。


**R622（rendering-level runtime probe·零净源码：★纠正 R620 机制误判 + 精确定位 font_size 丢失点；**supersedes 并行 R621（c6532b1e code-analysis 「store 存 div key / paint 取 text-node key → miss」假设）**——本轮 runtime probe 实证 text-node key NodeId(4v1) **被存**（值 16），故非 KEY miss 而是 **VALUE 错**（resolved 64 丢失存为 16）；R621/R622 结论一致（R620 has_text_children 错 + control-chars=R101/Phase-A blocked），机制以 R622 runtime 为准）——R620 称 blocker=`has_text_children` gate 不识别 ::after text，**实证 REFUTED**：`store_font_sizes_from_ifc` **实处理** ::after text（NodeId(4v1)），但存 font_size=**16**；`resolve_computed_style`（computed.rs:189-192）对 div `Em(4.0)` context=16 **正确解析 px=64**（::after 继承 `Px(64.0)`→64 亦正确）；但下游 `compute_styles_recursive` 的 `computed`（lib.rs:179 elem_style）及 layout `store_font_sizes` 均读到 **16** → **resolved 64 在 resolve_computed_style 与下游 computed style 间丢失**；= R101/Phase A propagation loss（非 has_text_children，非 storage-gate），R617/R618 R101 归因正确但 R620 机制错）**：承 R620（has_text_children 假设）做实证核验。**3 探针**（已 revert，工作树 clean）：① `store_font_sizes_from_ifc`（inline_finalization.rs:83）打印 `node_id/font_size` → ::after NodeId(4v1) **被处理**（证 has_text_children 通过），存 **16**（非 64）；② lib.rs:198 打印 `elem_style.font_size`/`after.font_size` → div=**Px(16)** after=**Px(16)**（下游 computed 均错）；③ computed.rs:192 打印 resolve 输入输出 → div `Em(4.0)` context=16 **→ px=64 正确**（+ `Px(64.0)`→64 = ::after 继承正确）。**矛盾裁决**：resolve_computed_style 返回 font_size=Px(64)，但 lib.rs 的 `computed` 与 layout store 读 16 → **resolved 64 未持久化到下游 computed style**（resolve 与下游 computed 间有覆写/未用 resolved 返回的路径）。**纠正 R620**：blocker **非** `has_text_children` gate（::after 实被处理）→ 实为 **resolve_computed_style 的 resolved font_size 未传到下游 computed**（可能是 compute_element_style_internal 未用 resolve 返回，或二次 resolve 覆写）。**对 R101 的精确化**：R101 memory「computed=100 correct 但 paint=16」= 本发现「resolve=64 correct 但下游 computed=16」**同一现象的 style-system 侧定位**——font_size 正确解析但未持久化到下游消费的 computed style，layout/paint 据此用 16。**裁决**：control-chars（+ large-font R101 簇）blocker = **resolved font_size 持久化缺口**（resolve_computed_style→downstream computed），**非** R620 has_text_children / 非 R617 box-render / 非 paint_content（R619）——三者均 dead-end。修复须查 compute_element_style_internal 是否调用并采用 resolve_computed_style 返回（潜在 narrow 切口：确保 resolved font_size 写入下游 computed），但触及 style-system→layout font_size 主链 = R101/Phase A 谱系，高风险。3 探针已 revert（computed.rs/lib.rs/inline_finalization.rs 回 HEAD）。零净源码变更。


**R621（doc-maintenance 调研·零源码变更：★纠正 R620 gate-skip pinpoint 经 read-only 代码核查 = imprecise——R620 称 control-chars div「无 real DOM Text child → 被 has_text_children gate 跳过」，但 inject_pseudo_text_nodes（pipeline.rs:549+558 `create_text_node`+`append_child`）把 ::after content 注入为 parent 的真实 `NodeKind::Text` DOM child → has_text_children（inline_finalization.rs:301-305 查 `doc.child_nodes` 的 `NodeKind::Text`）= **true** → div **未被跳过** → `store_font_sizes_from_ifc` **实运行**；compute_final（engine.rs:329 layout-engine）晚于 pipeline inject（架构：pipeline 建 doc/styles → layout-engine compute）故注入文本已在；R620 结论（R101/Phase-A blocked, plateau 不变）仍成立，仅 pinpoint 机制需修正；真机制疑 = R101b/c paint-box≠storage-box 存/取 key 错配；flag 勿据 R620 改 has_text_children gate）**：本轮 doc-side read-only 核查 R620（code agent `1a099de9` 提交的 paint_content probe 结果 +「real blocker = has_text_children gate」pinpoint）。**核查 3 点**：① `inject_pseudo_text_nodes`（pipeline.rs:513, 调用@304）对 `::after` string content 经 `doc.create_text_node(&text)`（行 549）+ `doc.append_child(parent, text_id)`（行 558）**注入为 parent 的真实 DOM Text child**（注释行 509「after 经 append_child 追加为末子节点」+ 单测 `inject_after_pseudo_as_last_child` 坐实）；② `has_text_children`（inline_finalization.rs:301-305）= `root.children.any(is_anonymous_text_item) || doc.child_nodes(node_id).any(... NodeKind::Text(_))`——**查 DOM child 的 NodeKind::Text**，注入的 ::after text 正是 NodeKind::Text → **has_text_children=true**；③ `compute_final_inline_layouts`（engine.rs:329，layout-engine）**晚于** pipeline 的 inject（pipeline 建 doc+styles → layout-engine compute 布局树），故 compute_final 时 div 已含注入 text child。**裁决（纠正 R620 pinpoint）**：R620「div 无 real DOM Text child → 被 gate 跳过 → store 不运行」前提**错误**（注入 ::after text 是真实 NodeKind::Text DOM child）→ **div 未被跳过，store_font_sizes_from_ifc 实运行**；故 font_size=16（R617 [CTRLBOX] size=16）**非 has_text_children gate-skip 所致**。**真机制（疑，须 runtime font_size-storage probe 确认）**：store 运行但存/取 key 错配——store 存 div（block 容器）font_size 于 div key，paint_text 重跑 IFC 按 **text-node** key 查 → miss → 默认 16；与 memory `r101-large-font-rootcause` R101b/c「paint-box≠storage-box：paint_text(#div1) 的 IFC 下钻收集内层 block div 文本当 inline，但 #div1 无 font_size 存储 → 默认 16」**一致**。**R620 结论仍成立**：control-chars/large-font = R101/Phase-A storage-gate blocked（font_size 存/取 box 不匹配，架构性），plateau 不变。**★ flag 防后续 session 误靶**：R620 pinpoint「has_text_children gate 不识别 ::after synthetic text」**imprecise**——gate 已识别（has_text_children=true）；据 R620 改 gate 认 ::after text **无效**（已认）；真修须统一 store_font_sizes_from_ifc 存 key 与 paint_text 取 key（R101b/c paint-box=storage-box 统一，memory 明载 net-negative 非 narrow slice）。零源码变更（read-only pipeline.rs/inline_finalization.rs/engine.rs 代码核查 + 调用序确认）。

**R620（rendering-level probe result·零净源码：★R619 等待的 paint_content probe 结果 = paint_content **未被调用**（control-chars `::after { content:"\0080" }` 走正常 layout IFC 非 paint_content）；paint_content 非 R101 修复点；真 blocker = `compute_final` 的 `has_text_children` gate（inline_finalization.rs:299-304）不识别 ::after synthetic text → div font_size 不存 → paint IFC re-run 默认 16；=R101/Phase-A storage-gate relaxation（multicol 耦合），非 narrow slice；probe 已 revert）**：承 R619（doc 监测 code agent 的 paint_content `[PAINTCONTENT]` probe，待结果）回报 probe 实测。**结果**：control-chars-080 reftest 跑后 **0 行 `[PAINTCONTENT]` 输出**——paint_content **完全未被调用**（::after `content:"string"` 不经 paint_content）。**机制追溯**：`::after { content:"..." }` 的 string 内容由 `pipeline.rs:522-548` **注入为 text node** 进 layout 树（注入后 line 548 `pseudo_style.content = Normal` 复位，防 paint_content 双绘），故走**正常 layout IFC + paint_text** 路径非 paint_content。**R619 假设（paint_content 是 R101 修复点）证伪**——paint_content 仅处理 counter/attr（line 511-525），string content 走 IFC。**真 blocker 定位**：control-chars 的 `<div style="font-size:4em">`（仅含 ::after 注入的 synthetic text，无 real DOM Text/element child）被 `compute_final_inline_layouts` 的 `has_text_children` gate（inline_finalization.rs:299-304，查 `NodeKind::Text` DOM child）**跳过** → `store_font_sizes_from_ifc` 不运行 → div 的 64px font_size 不存入 `text_node_font_sizes` → paint_text 重跑 IFC 用默认 16 → glyph 渲染 16px（R617 `[CTRLBOX] size=16` 实证）。**裁决**：control-chars font_size=16 的**精确 blocker = `has_text_children` gate 不识别 ::after synthetic text**（非 paint_content，非 fontdue）；修复须**放宽该 storage gate**（认 synthetic ::after text）= R101/Phase-A storage-gate relaxation = memory R101 明载 net-negative（multicol-fill-auto 回归）**非 narrow slice**。**probe 已 revert**（painter/text.rs 回 HEAD，工作树 clean）；paint_content lever（R619 预期）**dead-end 闭环**。code plateau 不变——control-chars/large-font = R101/Phase-A storage-gate blocked，唯一前向 = Phase A 统一（须用户决策）。零净源码变更（probe add+result+revert，painter/text.rs 回 HEAD）。


**R619（doc-maintenance 调研·零源码变更：★code agent 轨迹转向 R101 根因——新 in-flight 编辑 `painter/text.rs:531` `paint_content` 的 font_size（generated content `::after { content }` 渲染路径），正是 R101 memory「How to apply」点名的修复点；当前仅 eprintln 诊断 probe（`[PAINTCONTENT] font_size_px` / `NON-Px`）无 fix，building；追溯 R617 `[CTRLBOX] size=16` 从何来——`style.font_size` 在 paint_content 解析为 16 还是 64；★预 flag R101 架构风险：R101/R101b/c 先前 paint-box≠storage-box font_size 传播尝试均 net-negative（multicol-fill-auto 回归），memory 判「架构性非单点」）**：本轮 doc 监测发现 code agent control-chars revert 后**新开 lever**：`crates/engine/src/paint/painter/text.rs` uncommitted M + 2 build 进程在跑。**diff（read-only）= 仅诊断 probe**（paint_content 行 531-540 区域）：`match style.font_size { LengthValue::Px(s)=>s, _=>{eprintln!("[PAINTCONTENT] ... NON-Px (return)");return} }` + 正常路径 `eprintln!("[PAINTCONTENT] text={text:?} font_size_px={font_size}")`——**无 fix，纯 instrumentation**。**probe 目标 = paint_content（generated content 渲染器）读到的 `style.font_size`**：control-chars 测 `::after { content:"\0080" }` 在 `font-size:4em` div 内，R617 实证 glyph 抵 `size=16`（应 64）；本轮 code agent 追溯此 16 来源——paint_content 的 `style.font_size` 解析为 16（→ 问题在上游 style 未把 4em→64 传 generated content 盒）还是 64（→ 问题在下游传 rasterize 时被覆写）。**★ 轨迹意义**：从 control-chars 的**症状层**（font/loader.rs rasterize box）转向 **R101 根因层**（painter/text.rs paint_content font_size 传播）——此即 R101 memory `r101-large-font-rootcause`「How to apply: 修复点 `crates/engine/src/paint/painter/text.rs` paint_text 调用点 + `engine.rs` compute_final_inline_layouts」点名的**第一修复点**。**★ 预 flag R101 架构风险（memory 证）**：R101「paint IFC re-run font_size=16」+ R101b/c「paint-box≠storage-box，#div1 无 font_size 存储→默认 16」——memory 明载 R101/R101b/c 先前补救（relax 存储 + populate text_node_font_sizes）**均 net-negative**（multicol-fill-auto-001 PASS→FAIL 2.45% 回归未消），判「架构性非单点，搁置大字号集群」；修复须**统一 paint_text 调用 box 与 compute_final_inline_layouts 存储 box**。**裁决（doc 监测）**：code agent 现 attack R101 根因（高价值若 clean——可解锁 control-chars R617 + large-font 簇 008/009/011/font-051；高风险——R101 历史 net-negative，multicol 回归）。当前 probe 阶段无 fix/commit；doc-side 不跑 reftest（building + eprintln probe 会 flood）。**战略**：若 code agent 此轮破 R101（clean font_size 传播 fix），则 code plateau 真正打破（非 control-chars 假象），R609 Phase A 升级或大幅淡化；若 net-negative 回退（如 R101 历史），plateau 不变。监测待 fix+A/B+commit。零源码变更（read-only diff + memory R101 风险预 flag）。

**R618（doc-maintenance 调研·零源码变更：doc-side 独立印证 R617（code agent 自报 ae6bb52e）+ memory 交叉确证 R101 归因——control-chars pre-fix 基线独立实测 2/64（3.1%，post-revert pre-fix binary）；R617 的「font_size=16 抵 paint glyph」经 memory「R101 Large-Font Root Cause」精确印证（R101：「paint IFC re-run font_size=16（错！）」+ R101b/c「paint-box≠storage-box，默认 16」= R617 `[CTRLBOX]` size=16 实证完全吻合）；★纠正 R616 yield 假设（非 fontdue Cc 渲染 / 非 char-drop-before-raster——char 实达 rasterize 仅尺寸错）；control-chars 现归入 R101 deferred large-font 簇，code plateau 再坐实）**：本轮 doc-side 独立核查 control-chars lever 终态（font/loader.rs 已回 HEAD clean；code agent 自报 R617 `ae6bb52e` A/B=reverted）。**独立实测（post-revert pre-fix binary，`reftest-upstream control-chars`，test-guard release）= 2/64 PASS（3.1%）**：62 mismatch FAIL，代表案 control-chars-09F「59/480000 px differ（0.01%）< 1% 阈值」= 控制字符渲染 ~空（test≈ref→mismatch FAIL）；此 2/64 补 R617 缺的 pre-fix 聚合数（R617 给 post-fix per-case 0.05%）。**R101 归因 memory 交叉确证（核心价值）**：调 memory `r101-large-font-rootcause`——R101 明载「paint IFC re-run：font_size=16（错！）…paint IFC 无 overrides → 默认 DEFAULT_FONT_SIZE=16」+ R101b/c「paint-box≠storage-box 不匹配…paint_text(#div1) 的 IFC 下钻收集内层 block div 文本当 inline，但 #div1 无 font_size 存储 → 默认 16」——**与 R617 `[CTRLBOX]` probe 实证 `size=16` 抵 `rasterize_control_box`（应 4em=64）完全吻合**→ R617 的 R101 归因经 doc/memory 侧独立坐实，**control-chars 簇正式归入 R101 deferred large-font/pseudo-element font_size 簇**（同 008/009/011/font-051，「搁置大字号集群」适用，修复须统一 paint_text 调用 box 与 compute_final_inline_layouts 存储 box——架构性非单点）。**★ 纠正 R616 yield 假设**：R616 推测「pre-fix fontdue Cc 字符渲染（.notdef/tofu 可见→已 PASS / 空字形→FAIL）」或「char 在 rasterize 前被丢弃」；R617 实证 + R101 memory 双向纠正——**char 实达 rasterize（`[CTRLBOX]` 触发，非被丢弃）**，真 blocker = **paint 侧 font_size=16（R101 paint-box≠storage-box）**致 box 16×16 非 64×64→ 0.05% 仍 < 1% FAIL；box fix spec-correct 但在错误 font_size 下渲染→ no yield。**裁决（doc 印证 R617）**：control-chars（62-67 案，css-text 最大「看似 clean」候选）= **R101-blocked**，非独立 glyph-rendering lever；box fix REVERTED 正确。**code plateau 再度坐实**：R588 sizing vein 后 control-chars 为最新「看似 clean」候选，实测仍 R101-blocked（同 R589 014 inline-table / R101 large-font / R109 谱系）→ clean 单点 lever 谱系仍未破，R609 Phase A 升级立场不变。零源码变更（doc-side scoped reftest 独立实测 + memory 交叉确证）。

**R617（rendering-level A/B·code+revert：★control-chars box fix（R614-R616 监测的 font/loader.rs +36 行 lever）实测 = R101-blocked 非 clean win——`[CTRLBOX]` probe 实证 font_size=16 抵 `::after` glyph（应 4em=64），box 渲染 16×16 非 64×64→ 0.05% 仍 FAIL；css-text 1402→1402 net-0；make test 过；纠正 R616「yield 不确定性=fontdue Cc 渲染」=实为 font_size（R101 谱系）；REVERTED）**：承 R614-R616（doc 监测 code agent 的 control-chars box fix，reachability ~64-65/67 已证，yield 待 reftest 定夺）做实测 A/B（即 R614-R616 观察的 code agent 本人）。**实现**：`rasterize_glyph_with_fallback` 顶部加 `is_visible_control_char`（Cc 除 \t\n\r）→ `rasterize_control_box`（实心方块复用 Ahem 几何）。**A/B 结果（test-guard release）**：① control-chars-080 **仍 FAIL 0.05%**（pre 0.01%→post 0.05%，box 仅 +192px）；② PIL 量像素：box 渲染 **16×16=256px at y[64,79]**（应 4em=64→64×64=4096px）；③ `[CTRLBOX]` probe（临时 eprintln，已删）实证 **`size=16`** 抵 `rasterize_control_box`——**font_size=16 非 64**；④ css-text **1402/1576 net-0**（control-chars 微动被他处抵消）；⑤ make test 全绿。**根因 = R101 large-font/pseudo-element font_size 谱系**：`::after { content:"\0080" }` 在 `font-size:4em` div 内，paint 侧重跑 IFC 用默认 font_size=16（非 computed 64）→ box 按错误尺寸渲染→ 太小不过 mismatch 阈值。**★纠正 R616「yield 不确定性=pre-fix fontdue Cc 字符渲染」**：实非 fontdue Cc 渲染问题（.notdef/tofu）——**真 blocker = font_size 抵 glyph 错误（R101）**；box fix spec-correct 但在错误 font_size 下渲染→ no yield。**裁决**：control-chars 簇（62-67 案，css-text 最大「看似 clean」簇）= **R101-blocked**（同 large-font 谱系，font_size 不抵 paint 侧 glyph），**非 glyph-rendering lever**；box fix **REVERTED**（net-0 + blocked by deferred R101 + 引入 diverge 非净 win，违 code-guidelines §2/§4）。**code plateau 再度坐实**：R588 sizing vein 后 control-chars 是最新「看似 clean」候选，实测仍 R101-blocked；clean 单点 lever 谱系仍未破。零净源码变更（fix 实现+A/B+revert，loader.rs 回 HEAD 原状）。

**R616（doc-maintenance 调研·零源码变更：gate ③ read-only 闭环——css-parser `consume_escape`（tokenizer.rs:433-438）spec-compliant：`\0000`（codepoint 0）→ U+FFFD（`test_escape_zero_codepoint` 坐实），故 control-chars-000.html 注入 U+FFFD ∉ Cc 范围→ `rasterize_control_box` 不触发（此 1 测 lever 失活，靠 U+FFFD .notdef/tofu 渲染独立定胜负，与 fix 无关）；其余 ~65 测（0001-08/0B/0C/0E-1F/7F/80-9F）注入真实 Cc codepoint→ 在 is_visible_control_char 范围内→ lever 触发（CR/00D 正确排除为空白）；三 gate 全验证→ lever reachable ~64-65/67；★yield 残余不确定性=pre-fix fontdue 对 Cc 字符渲染（.notdef tofu 已可见→mismatch 已 PASS→fix no-op yield / 空字形不可见→test==ref→FAIL→fix flip PASS）；code agent 仍验证中 reftest css-text、font/loader.rs uncommitted）**：本轮 read-only 核查 R615 留的 gate ③（CSS `\0000` escape 是否正确转 U+0000 抵 rasterize_glyph）。**核查（css-parser consume_escape，tokenizer.rs:409-447）**：hex escape 解析后行 433-438 `if codepoint==0 || >0x10FFFF || (0xD800..=0xDFFF).contains → Some('\u{FFFD}')` else `char::from_u32(codepoint)`——**spec-compliant**（CSS Syntax §4.3.7：0 / 越界 / 代理对 → U+FFFD 替换字符）。单测 `test_escape_zero_codepoint`（tokenizer_coverage.rs:124-129，`\0`→替换字符）+ `test_escape_surrogate_codepoint`（`\00D800`→替换）坐实。**对 lever 的精确影响**（control-chars 测经 `content:"\00XX"` 注入，按 XX）：① **000（NUL）→ U+FFFD**（codepoint 0），U+FFFD ∉ `is_visible_control_char` Cc 范围 → `rasterize_control_box` **不触发**——此 1 测 lever **失活**，PASS/FAIL 由 U+FFFD .notdef/tofu 渲染独立决定（与 fix 无关）；② **0001-08/0B/0C/0E-1F/7F/80-9F → 真实 Cc codepoint**（非 0、非代理、非越界 → `char::from_u32` 原样返回）→ **在 is_visible_control_char 范围内 → rasterize_control_box 触发**；③ **00D（CR）**：codepoint 0x0D 非 0 经解析返回 `\r`，但 CR 在 `is_visible_control_char` **正确排除**（标准空白 \t\n\r，CSS Text-3 §white-space-processing 视作 segment break 非可见，spec-correct）。**三 reachability gate 全验证**：① mismatch runner（R615）✓ ② `content:"string"`→glyph（R615）✓ ③ escape 解析（本轮）✓ → **lever reachable for ~64-65/67 测**（NUL 失活 + CR 边缘）。**★ yield 残余不确定性**（read-only 无法定夺，须 reftest）：pre-fix fontdue 对 Cc 字符渲染——若 .notdef/tofu 字形已可见（即使「过浅」）→ test 已 != ref → mismatch **已 PASS** → fix **no-op yield**（仅 spec-correct 化渲染）；若 Cc 字符渲染为空/零宽 → test==ref → mismatch **FAIL** → fix 实心方块 → **flip PASS**（clean win）。code agent 注释「过浅 control-picture glyph」暗示字形可能已存在→偏 no-op 侧，但 reftest 定夺。**实时状态**：code agent 已过编译，**正在验证**（`test-guard ... reftest-upstream css-text`；cargo test 已完；font/loader.rs uncommitted pending validation）——doc-side 不自跑（test-guard 在跑，避争用+oom）。**裁决**：gate ③ 闭环（lever reachable ~64-65/67），yield 待 code agent css-text reftest 结果（flip=自 R588 后首个 clean win / no-op=spec-correct 但 0 yield）。零源码变更（read-only consume_escape 代码 + 单测核查）。

**R615（doc-maintenance 调研·零源码变更：★纠正 R614 lever 误判 + read-only yield-gate 核查——code agent in-flight `font/loader.rs` 真实 lever = **css-text white-space/control-chars**（67 mismatch 测，Cc 控制字符可见渲染为实心方块），**非** R614 推测的 css-fonts font-variant（R601 导入与此 lever 无关）；两 reachability gate read-only 确认通过（mismatch runner 支持 + `content:"string"` 生成 glyph），余 gate = CSS `\0000` escape→U+0000 抵 rasterize_glyph（code agent `[CTRLBOX]` eprintln probe 正验证）；潜在 clean win 若可达=自 R588 sizing vein 后首个，breaking code plateau）**：本轮 read-only 复核 R614 记录的 in-flight `font/loader.rs` +37 行 diff（uncommitted，cargo+rustc 自 05:46 编译 `zero-wpt-runner --release` 中）。**真实 lever 识别**（纠正 R614）：diff 加 `is_visible_control_char`（U+0000-08 / 0B / 0C / 0E-1F / 7F / 80-9F，排除标准空白 \t\n\r）+ `rasterize_control_box`（实心方块 side=font_size，复用 Ahem 方块几何 y_offset=-ascent），在主 rasterize 路径（cache 后 fallback 前）对可见 Cc 控制字符返回方块。**目标簇 = css/css-text/white-space/control-chars-0XX**（67 测，全 `rel=mismatch`，ref=`control-chars-000-ref.html` 仅含 `<p>` 无 div）：test 经 `div::after { content: "\00XX" }` 注入 Cc 控制字符，spec（CSS Text-3 §white-space-processing）要求控制字符须可见；test 有可见字符 vs ref 无 → 须 **differ** → mismatch PASS。**注**：`letter-spacing-control-chars-001`（rel=match）用 Cf 零宽格式符（U+200B/200C/200D/2060/FEFF）经 HTML 实体注入，**不在** `is_visible_control_char` 覆盖（非 Cc），不受此 lever 影响（零宽应 no-op letter-spacing，独立 concern）。**Reachability gate read-only 核查（2/3 通过）**：① mismatch runner 支持 ✓（`wpt_file_loader.rs:118`「比较模式 true=match false=mismatch」）；② `content:"string"` 生成 glyph ✓（`paint_content` + `test_paint_content_string_generates_glyphs` 单测；坐实 R486/R487 伪元素纯字符串内容渲染完成，R554 回退仅 counter()/content-list 非纯字符串）；③ **余 gate** = CSS parser 把 `\0000` escape 转 U+0000 char 抵 `rasterize_glyph(code_point)`——code agent 加 `eprintln!("[CTRLBOX] ...")` debug probe 正验证此路径是否命中。**裁决**：lever 机制 spec-aligned 且 likely reachable；若 gate ③ 通过则 67 mismatch 测 FAIL→PASS（当前控制字符若渲染为空/过浅→test==ref→mismatch FAIL；fix 后可见方块→differ→PASS）= **自 R588 sizing vein 后首个潜在 clean win**，breaking R588 起 ~2.5h code plateau（坐实 R614「plateau 或自动打破」预测，但 lever 性质误判已纠正）。**★ 回归 flag**：(a) `eprintln!("[CTRLBOX]")` debug 行 **commit 前必须删**（code-guidelines：用 tracing 非 println；且每个控制字符都喷 stderr）；(b) 实心方块覆盖所有可见 Cc 控制字符——真实页面文本含控制字符处现会渲染方块（罕见，product-smoke 须守，welcome/wintertc/morning-work 预计不含 Cc 控制字符）。**doc-side 行为**：不跑 reftest（cargo 自 05:46 编译中，避 build contention + oom-guard）→ 待 code agent commit 后 doc-side 验证（scoped `css/css-text/white-space/control-chars` reftest selfsource before/after + product-smoke）。**战略**：code agent 自主找到 R601-import-independent / Phase-A-independent 的 rendering-level 新 lever，R609「须 Phase A 用户决策」升级**进一步淡化**。零源码变更（read-only diff + wpt-data test/ref 核查 + runner/paint 代码 read-only grep）。

**R614（doc-maintenance 调研·零源码变更：★code agent 复活续 code 工作打破 R588 起 ~2.5h code plateau——in-flight 编辑 font/loader.rs（+36 行，uncommitted，cargo+rustc 编译中）；疑 R601 导入（css-fonts support CSS：font-variant-features/variation-sequences）启用的 font-feature 新 lever；doc-side 监测避 build contention 待 commit 验证；R609 Phase A 升级或因此 lever 自动解锁而淡化）**：R613 后状态核查发现 **code agent 复活续 code 工作**（自 R588 sizing vein 02:59 后 ~2.5h 首个源码改动）：`crates/render-foundation/src/font/loader.rs` **+36 insertions（in-flight，uncommitted，cargo+rustc 编译中）**。**推测 lever**：R601 导入了 css-fonts support（`font-variant-features.css` / `variation-sequences.css` / font-weight-ref.png）→ 启用 font-feature/variant 测试 → code agent 疑在 font/loader.rs 加 font-feature 加载支持（+36 行新增）。**doc-side 行为**：不触源码（code agent 工作）+ 不跑 make test/reftest（避 cargo/rustc build contention + oom-guard）→ 监测待 code agent commit 后 doc-side 验证（rendering-level scoped reftest + product-smoke）。★ **战略含义**：R609「clean lever 耗尽须 Phase A 用户决策」升级**可能淡化**——code agent 自主找到新 code lever（font-feature，R601-import-enabled），plateau 或自动打破，forward motion 无须用户 Phase A 决策即恢复。待 commit 确认 lever 真实性 + yield。零源码变更（read-only git diff --stat + 状态监测）。

**R613（doc-maintenance 调研·零源码变更：承 R611 baseline 更新 + R612 TODO——authoritative baseline（基线 § line 77，9-dir post-R484）post-import 落位：4 dir selfsource 数经 pattern-addressed sed 更新（writing-modes 613/786→602/788 / flexbox 368/496→360/497 / multicol 195/451→193/452 / fonts 282/284→283/287），pattern「布局类，runner 容差」唯一匹配 line 77 不伤 122/443 历史快照；line 77 4 dir 全 post-import ✓ 0 residual；R611 推荐闭环；122/443 = R427-era/partial 历史 era 快照保留 pre-import 正确）**：承 R611「baseline 应 post-import 更新」+ R612 flag。**authoritative baseline（基线 § line 77）post-import 落位**：pattern-addressed sed（`/布局类，runner 容差/{s#368/496=74.2%#360/497=72.4%#;s#195/451=43.2%#193/452=42.7%#;s#282/284=99.3%#283/287=98.6%#;s#613/786=78.0%#602/788=76.4%#}`，`布局类，runner 容差` 唯一匹配 line 77，避开 Edit 3-副本不可唯一匹配问题）→ 验证 **line 77 4 dir 全 post-import，0 pre-import residual；line 122（R427-era 7-dir，「见顶部主基线」）+ line 443（partial「非全量」）保留 pre-import**（历史 era 快照正确，非重复权威）。**裁决**：R611 baseline 更新**闭环**——authoritative baseline（line 77）现 post-import 准确；R612「3-副本 consolidation TODO」**澄清**：122/443 非「冗余权威副本」而是 era-specific 历史快照，line 77 更新即 sufficient（122/443 保留历史正确）。post-import selfsource 基线 doc-side 全落位（R611 数据 + R612 top 权威 + R613 line77 准确）。forward path 不变（R606/R609：clean lever 耗尽，主前向 Phase A 用户决策）。零源码变更（doc baseline 数更新 via pattern-addressed sed + awk 验证）。

**R612（doc-maintenance 调研·零代码：承 R611 baseline 更新建议——post-import selfsource 基线 doc-side 权威落位（writing-modes 602/788=76.4% / flexbox 360/497=72.4% / multicol 193/452=42.7% / fonts 283/287=98.6%，vs pre-import 613/786·368/496·195/451·282/284，~23 false-pass 消除）；★baseline selfsource 行存在 3 冗余副本（基线§/状态概览§/旧里程碑§）+ pre-import 数未批量改（replace_all 恐伤历史 R422/R512 条目，surgical 难唯一匹配），flag 为 consolidation TODO；当前权威 = R611/R612 top entry）**：承 R611「master.md baseline 应据 post-import 更新」做 doc-maintenance 落位。**post-import selfsource 基线（R610/R611 实测，权威）**：writing-modes **602/788=76.4%**（pre 613/786=78.0%，-11 false-pass）/ flexbox **360/497=72.4%**（pre 368/496=74.2%，-8）/ multicol **193/452=42.7%**（pre 195/451=43.2%，-2）/ fonts **283/287=98.6%**（pre 282/284=99.3%，分母 +3）；grid/position/tables/text-decor/css-text 不受 R601 影响（数不变）。**baseline 落位难点**：selfsource 基线行存在 **3 冗余副本**（基线 § ~line75 / 当前状态概览 § ~line120 / 旧里程碑 § ~line441，近同内容），pre-import 数（368/496·195/451·282/284）replace_all 会伤历史 R422/R512 条目（同字符串），surgical 改单副本难唯一匹配（3 副本近同）→ **flag 为 consolidation TODO**（统一为单一权威 baseline + post-import 更新，须理解 § 结构，独立 refactor）。**当前权威**：R611 entry（top）+ 本 R612 = post-import 权威；baseline § 3 副本暂留 pre-import（被 R611/R612 supersede，consolidation 留独立 refactor）。**裁决**：post-import 基线 doc-side 权威化（R611 数据 + R612）；3-副本 consolidation flag TODO。forward path 不变（R606/R609：clean lever 耗尽，主前向 Phase A 用户决策）。零代码变更（read-only 数据汇总 + 结构核查）。

**R611（rendering-level 测量·零代码：R601 导入 per-dir selfsource honesty 全画像完成——css-flexbox 368→360（-8）/ css-multicol 195→193（-2）/ css-fonts 282→283 pass 但分母 284→287（+3 案 font-weight-ref 启用，1 pass 2 real-divergence）+ writing-modes -11（R610）= **~23 false-pass 消除**，0 clean win（全暴露案=结构性真发散）；R601 导入价值 = DC-14 honesty 实测闭环，非 fixable lever）**：承 R610 CONTINUE 对 R601 其余受影响目录（release binary `reftest-upstream`，单 test-guard 调用各 <2min，证 R610 lighter 路径可行）。**per-dir post-import selfsource vs baseline**：① **css-flexbox 360/497=72.4%**（baseline 368/496=74.2%，**pass -8** 分母 +1）= test-style.css/flexbox.css/red-rect.svg 导入暴露 8 真 flex 布局发散；② **css-multicol 193/452=42.7%**（baseline 195/451=43.2%，**pass -2** 分母 +1）= swatch-lime.png 暴露 2 真 multicol 发散；③ **css-fonts 283/287=98.6%**（baseline 282/284=99.3%，pass +1 但**分母 +3**）= font-weight-bolder/lighter/normal-001-ref.png 启用 3 pre-import 缺 ref 案（1 pass / 2 font-weight 真发散）。**全画像汇总**（含 R610 writing-modes -11）：**~23 false-pass 消除**（flex 8 + multicol 2 + fonts 2 + writing-modes 11），pass rate 各 dir 微降（honesty 增），**0 clean win**——全暴露案为 flex/multicol/font-weight/writing-modes 轴结构性真发散（R109/R114/R164/taffy 簇）。**裁决**：R601 导入**价值闭环 = DC-14 honesty**（selfsource 数字更诚实，~23 假通过消除，同 R546 动态），**非 fixable lever**（无 clean win，残差全结构性）；R601 missing-asset vein 测量侧**完成**。master.md 各 dir selfsource baseline 应据 post-import 数更新（writing-modes 613→602 / flexbox 368→360 / multicol 195→193 / fonts 282→283）以反映诚实基线。**forward path 不变**（R606/R609/R610）：clean 单点 lever 谱系耗尽，主前向 = Phase A IFC 多会话（须用户决策）。零代码变更（read-only release-binary scoped reftest ×3 dir）。


**R610（rendering-level 测量·零代码：★实际跑成 writing-modes post-R601-import selfsource = 602/788=76.4%（release binary `reftest-upstream`，单 test-guard 调用完成 <2min）= 纠正 R609「全量 intractable」结论（R609 用 builtin 0.58s/case 推 8.5min，高估 ~4×；release binary reftest-upstream 实测 ~0.15s/case）+ 给出 R609 仅 extrapolate 的真数：pre-import 613/786=78.0%(R567)→**-11 false-pass** = R601 DC-14 honesty 实测落地）**：承 R609（writing-modes 测量 extrapolate 为 intractable）做**实际测量**而非 extrapolate。**实测**（release binary `./target/test-guard -- target/release/zero-wpt-runner reftest-upstream css-writing-modes`，无 REFTEST_DUMP）= **602/788 = 76.4%**，**单 test-guard 调用完成**（< 2min bash 窗口，788 案≈0.15s/case）。★ **纠正 R609**：R609 用 `reftest writing-modes`（50 builtin 案 28.85s=0.58s/case）extrapolate 877 WPT 案≈8.5min→判 intractable；**但 builtin(debug) timing ≠ release binary WPT timing**——release `reftest-upstream` 实测 ~0.15s/case（4× 快），788 案 <2min 单 cycle 可完成。**R609「code agent 36min 无 output=任务装不下」结论需修正**：全量 selfsource（无 dump）单 cycle 可完成；code agent stall 真因更可能是其跑 **oracle+dump 重路径**（capture-oracle-per-dir 877 PNG chromium 截图 + REFTEST_DUMP_PASS=1 dump）非纯 selfsource。**R601 DC-14 honesty 实测落地**：pre-import R567 **613/786=78.0%**→post-import **602/788=76.4%**=**-11 false-pass**（分母 786→788=2 pre-import missing-ref 案现加载）；机制（R601 2 案已证）= 13 swatch/quadrant 资产 pre-import 双侧 broken-image→0% 假通过，post-import 双侧有图→真 vertical-rl layout gap（R114/R164 ruled-out 簇）→翻 FAIL。**裁决**：(1) R601 导入价值 = DC-14 honesty 实测（-11 false-pass，selfsource 更诚实但通过数降，非新 fixable lever，同 R546 动态）；(2) **纠正 R609 intractable 结论**——纯 selfsource 单 cycle 可完成（<2min），intractable 的是 oracle+dump 重路径，建议后续测量分离 selfsource（轻，单 cycle）与 oracle-recapture（重，分批）；(3) R609 三选项 (a/b/c) 不变（incremental/Phase A/doc-measurement），但「测量 intractable」非阻塞理由。零代码变更（read-only release-binary scoped reftest 实测 + R609 extrapolation 纠正）。

**R609（doc-maintenance 调研·零代码：★empirical 确证 writing-modes 全量测量对 rally cycle 过重——scoped `reftest writing-modes`（test-guard 包裹，全 agent idle 无 contention）实测 50 builtin 案 28.85s（0.58s/case）→ 877 WPT 案 selfsource ≈ 8.5min（oracle+dump 更重）= code agent 5min cadence 无法单 cycle 完成（R607 诊断坐实，真因非 stall 而装不下）；builtin 50/50 100% PASS（smoke）；多 agent 交叉确证 clean lever 耗尽 + 测量 intractable → 须 incremental 分批 or Phase A 用户决策；飞书升级）**：承 R607 CONTINUE + R608（rendering-level css-tables scoped 复核，采 R607 lighter 路径，证 sizing-vein 渲染层亦 closed）。**doc-side 实测**（test-guard 包裹 `reftest writing-modes`，全 agent idle 无 contention）：50 builtin 案 **100% PASS（28.85s）**——但 `reftest` 子命令跑 **builtin 注册案非 WPT 导入案**（smoke，非 WPT selfsource 数，后者走 reftest-upstream/cross-validate）。**关键 extrapolation**：0.58s/case × 877 WPT 案 ≈ **8.5min selfsource**（oracle+dump 更重）→ **empirical 确证 R607「全量 cross-validate 对 rally 5min cadence 过重」**——code agent 自 R601 后 36min 无 measurement output 真因 = 单 cycle 装不下 ~8.5min+ 任务（非 agent stall/死）。**裁决**：writing-modes 全量测量 **rally-cadence intractable**，须 **incremental per-subdir 分批**（每批 ≤5min + commit）or 超-cycle 长跑。★ **多 agent 交叉确证 clean lever 耗尽**：R605 multicol Phase2a / R606 forward-path / R608 css-tables 渲染级 / 本轮 writing-modes 测量 intractable → 谱系趋尽一致。**战略（须用户决策）**：code agent 测量 intractable + clean lever 耗尽 → (a) 重启 code agent incremental 分批测量 (b) 授权 Phase A IFC 多会话 (c) 授权 doc-agent incremental 测量续 doc。**飞书升级**（准确诊断 + 3 选项）。零代码变更（read-only scoped reftest 实测 + extrapolation）。

**R608（rendering-level 调研·零代码：css-tables 低 diff FAIL 渲染级 scoped 复核（R607 推荐的 lighter scoped 路径实例）——最诱人的 table-intrinsic-size-001-004（2.08% 4 案簇）= R97 min-content→0 实证 .outer 塌缩 0 无绿；★converter/mod.rs:383-386 代码注释载 R181c 已试 Auto 映射→net -5→回退 length(0)，防后续 session 误靶此「看似 clean」簇；css-tables 低 diff 渲染级全 non-clean，坐实 R589/R606 谱系趋尽）**：承 R607「lighter scoped 替代 heavy 全量 cross-validate」做 css-tables scoped reftest（release binary + test-guard，正确 diff 过滤）渲染级根因，区别 R589（code/doc 级）。css-tables 80/115 PASS（69.6%）。**table-intrinsic-size-001-004（2.08% ×4 同 diff 簇，最诱人）渲染级根因 = R97 min-content→0**：`.outer{width:min-content}` 含 `display:table` 子（content 60px）期望 100×100 绿方；ZeroWeb 渲染**全无绿**（仅 `<p>` 文本，.outer 塌缩 0 area）——`computed.rs:67-68` + `converter/mod.rs:387` 均 `MinContent|MaxContent→0/length(0)`；ref 为干净 `ref-filled-green-100px-square-only`（非 false-pass，4 案真缺口）。★ **converter/mod.rs:383-386 代码注释载 R181c 历史**（master.md 未记）：「不能映射为 Auto——taffy 把 width:auto 块级容器拉伸填充违反 shrink-to-fit 语义（**R181c 实测 net -5**）」→ **Auto 映射已试 net -5 回退** length(0.0)；真修复须两趟固有宽度测量（converter+layout shrink-to-fit 扩展 R180 机制）= R97 多子系统 deferred 500+ 轮，**非 clean**。**box-shadow-001（1.08%）= inset box-shadow 渲染精度**（测 Chrome bug-fix，inset 60px green 起始位置，render-primitive 精度非布局单点）。**visibility-collapse-border-spacing-001（1.74%）= collapse 已实现**（table.rs:743-951）**但精度偏**：test red=3000 泄露+green=12340（超大）vs ref red=0/green=10000 = border-spacing+outline+collapse 交互精度，非 clean 单点。**裁决**：css-tables 低 diff FAIL 渲染级全 non-clean（R97/R181c + render-precision + border-spacing），从渲染层（pixel 计数 + R181c 代码注释证据）坐实 R589 sizing-vein-closed + R606 谱系趋尽。**★ 防后续 session 误靶**：table-intrinsic-size 4 案 2.08% 簇「看似 clean 实为 R97/R181c-hard（Auto 映射 net -5 已证）」，勿据 diff 低判定 clean。零代码变更（read-only scoped reftest dump + PIL pixel 计数 + converter 代码注释核查）。

**R607（doc-maintenance 调研·零代码：monitoring 状态——code agent writing-modes oracle cross-validate（R601 deferred 全量 REFTEST_DUMP 877 案）in-flight ~24min（04:52 起 multiple reftest bursts，至 05:15 无 commit）= heavy approach 疑对 rally cycle 不友好；doc-side 避 reftest 防 OOM-contention；master.md forward-path 经核查与 R605/R606 一致无 stale；若全量不完成建议 lighter scoped/sample 方案）**：承 R606 CONTINUE 监测 code agent 测量进度。**观察**：code agent 自 ~04:52 起 burst 跑 writing-modes reftest（04:53/04:57/05:03 多个 zero-wpt-runner，05:09/05:15 idle），至 05:15 **~24min 无 commit**（末次 code/measurement commit = R601 04:45/04:51）→ R601 deferred「全量 cross-validate REFTEST_DUMP_PASS=1 全 877 案」**疑对 rally 单 cycle 过重**（dump 877 PNG + cross-validate vs oracle = heavy；code agent 反复 burst 未达 commit 状态）。**doc-side 不自跑**（避与 code-agent cadence OOM-contention，oom-guard 杀进程树恐扰其测量）。**master.md 一致性核查**：综合裁决 multicol 卡片 + forward-path 经核查与 R605（Phase 2a closed）/R606（clean lever exhausted）**一致无 stale**（multicol 一般路径「layout 侧 column-aware IFC」仍 Phase-A-blocked，R605 仅闭 Phase 2a 存储 sub-path）。**建议**（若全量 cross-validate 持续不完成）：lighter 方案 = ① scoped selfsource reftest（无 dump，pass count 量化 false-pass→real-divergence）② 或 incremental per-subdir cross-validate（分批 dump，每批 commit）。**战略状态不变**（R606）：clean 单点 lever 谱系趋尽，主前向 = Phase A IFC 统一（须用户多会话决策）或 code-agent 测量 yield 新 lever。零代码变更（read-only 监测 + 一致性核查）。

**R606（doc-maintenance 调研·零代码：writing-modes oracle（R601 刷新 877 PNG）doc-side 有效性验证 = SOUND（全 valid，0 bad/0-byte/404，avg 8415B）→ deferred cross-validate infra 健全；★预测 post-import writing-modes chr<1% 自 5.9%(R567) 下移（false-consistent→real divergence，R601 2 案证机制，暴露案=vertical-rl 结构性 R114/R164 非新 lever）；承 R605 code-verify multicol Phase 2a net-negative 闭环 → forward path 更新：multicol 全 Phase-A-blocked，clean 单点 lever 谱系趋尽，主前向=Phase A 用户决策）**：承 R603 CONTINUE + R605（code-verify multicol Phase 2a 存储接线 net-negative 闭环）。**oracle 有效性验证（read-only `file`/`stat`，doc-level 测量 infra 质量检查，区别 R605 code-level）= SOUND**：`tests/wpt-runner/oracle-shots/` writing-modes **877 PNG 全 valid**（= R601 刷新全量），0 bad / 0-byte / HTML-404，最小 2814B（合法小测试）/ avg 8415B → deferred 全量 cross-validate 可信。code agent writing-modes 测量 idle（避 duplicate 不自跑）。★ **预测**：post-import writing-modes chr<1% 自 **5.9%（R567 46/784）下移**——pre-import 用缺失 swatch/quadrant 资产的案（R590：38 test）test+oracle 双侧 broken-image=false-consistent；post-import 双侧有图=真 comparison→暴露 vertical-rl layout gap（R601 2 案 6.04%/16.64% 已证=R114/R164 ruled-out 簇）→ **honesty 增但通过数降，非新 fixable lever**；code agent 测量落位 confirm/refute。★ **forward-path 更新（承 R605）**：R605 code-verify 闭环 multicol Phase 2a=net-negative（缺口实存但修复撞 R125/R198/R205 回归，empirically-established guard 非 reasoned net-0；纠正 R582 前提确认结论）→ **multicol 全 Phase-A-blocked**（Phase 2a ruled out + 2b/2c 需 R109 + paint-side 4 轮 ruled out）；**clean 单点 lever 谱系趋尽**（sizing R589 闭 / multicol R605 闭 / semi-replaced R603 闭 / parser-length R601 闭 / converter R591 闭 / 8 角度 R599 穷尽）→ 主前向 = Phase A IFC 统一（~2200 案，须用户多会话决策）。零代码变更（read-only oracle 审计 + 分析预测 + R605 结论集成）。

**R605（code-level 调研·零代码：multicol Phase 2a 存储接线从代码侧彻底闭环——R581 handoff「3 结局待 A/B」+ R582「reasoned net-0」复核：R582 推理前提经代码核查 INCORRECT，但其结论经更强机制 CONFIRMED = 显式 regression guard；纠正前提防后续 session 据错误前提重试）**：承 R581（multicol Phase 2a 存储接线 executable handoff，3 结局待 A/B）+ R582（H2 probe 推理 net-0，未 empirical A/B）决定是否值得实施。**代码核查 4 点**：① `inline_finalization.rs:284` compute_final `if root.is_multicol { return }` 显式跳过 multicol → `store_font_sizes_from_ifc`（行 801/935）**不对其运行**；② `painter/text.rs:807` `use_stored = multicol_info.is_none() && ...` → multicol（`multicol_info.is_some()`）`use_stored=false` → 行 934 `ctx.layout(doc, node_id, &HashMap::new())` **空 styles 重跑 IFC**；③ font_size override 来自 `box_node.text_node_font_sizes`（行 1257），由 `store_font_sizes_from_ifc` 填充——**multicol 多数为空**（仅 remeasure 子集 `height:Auto`+`content_height<1.0` 填充，行 894-902）；④ **故 R582「store_font_sizes_from_ifc 完整填充无 R84 缺口」推理对一般 multicol INCORRECT**（缺口实存：multicol paint 空-styles 重跑，override 多为空）。**但 R582 结论（不实施 Phase 2a）CONFIRMED via 更强机制**：行 284 显式 multicol skip（注释「多列在 paint 阶段按列分配 IFC 内容，不适合预存储」）+ 行 888 `content_height<1.0` 守卫注释明列 **multicol-fill-auto 为 R125/R198/R205 回归源**——**这些 guard 正因「为 multicol 存储 font_size 触发已知回归」而存在**。**裁决**：Phase 2a multicol 存储接线从代码侧**彻底闭环**——缺口实存但修复 = 已知 net-negative（regression-coupling，非「reasoned net-0」而是「empirically-established net-negative guard」）。**纠正 R582 推理前提**（防后续 session 据「overrides 已填充」错误前提重试 Phase 2a 浪费 effort）；**确认其结论**（不实施）。multicol 真阻塞仍是 Phase A R109（混合内容 inline→block）+ fragmentation，非 font_size 存储。零代码变更（read-only 代码核查 inline_finalization.rs / painter/text.rs / engine.rs 三文件）。

**R603（doc-maintenance 调研·零代码：position-absolute-semi-replaced-stretch 簇（stale 污染 #2，23%）scout = ALREADY HANDLED——R498-R500 abspos 全-inset stretch + R499 is_replaced guard 均在 stale 数据(2026-06-16)之后(06-22)，passing 单测直接引用该 WPT 案断言 input 拉伸 144×94；候选移出队列；★analyze-pollution-2026-06-16 整表 stale（先于 R491-R588 ~30 fix + R601 导入），多数 top 候选已修或结构性，须 fresh 重生成得当前真候选）**：承 R602 CONTINUE，code agent 活跃跑全 writing-modes cross-validate（zero-wpt-runner 04:52 起 5min+，避 reftest 争用），doc-side read-only scout 最高 ROI 的 stale 未修候选。**核查 semi-replaced-stretch 簇**（input/button/other 3 案 + 3 ref，csswg #6789：表单控件 semi-replaced abspos 全-inset+auto 尺寸应 stretch 填满 CB 非固有尺寸）：① `types/mod.rs:93 is_replaced` 注释列 replaced={img/video/iframe/embed/object/svg/canvas}**不含表单控件**→ input/button/select `is_replaced=false`→ 进 stretch 非跳过；② `engine.rs:1769` stretch gated「仅非替换」+ `tests_5.rs:1716 test_absolute_stretch_in_inline_block_container`（注释明引 position-absolute-semi-replaced-stretch-input.html）**断言 `<input>` abs 全-inset+border-box+auto 拉伸到 144×94 PASS**；③ git blame：该测试 2026-06-13 触及、**is_replaced guard = R499（f5737018, 2026-06-22）**——**均晚于 stale analyze-pollution(2026-06-16)**。**裁决**：semi-replaced-stretch 机制已由 R498-R500+R499 处理（post-stale-data），stale 23% 先于此；候选**移出 fix 队列**（残差若存=表单控件固有尺寸/UA appearance，complex 非单点）。★ **analyze-pollution-2026-06-16 整表 stale**：先于 R491-R588 ~30 clean fix + R601 资产导入；18 top 候选中 table-colspan(R177 结构)/backdrop-inherit(P3 未实现)/baseline-overflow(R180 修)/html-display-table(R165 修)/collapsed-item(R111 修)/table-grid-item-004(R168 修)/semi-replaced(R499 修) 等多已修或结构性→ **须 fresh 重生成**（code agent 全 dir cross-validate + analyze-pollution，writing-modes 正在跑）得当前真候选，stale 表勿再作优先级依据。零代码变更（read-only scout + git blame）。

**R602（doc-maintenance 调研·零代码：R601 code-agent 导入 doc-side 独立验证 = 全通过（26/26 资产 valid）；★R600「code agent INACTIVE」诊断 RESOLVED——code agent 04:45 复活执行 R597 导入 + 全 writing-modes oracle 刷新 + A/B，5× stall deadlock 打破，R599/R600「blocked 须用户介入」状态 OVER，forward motion 解锁；R601 结论（无 clean oracle win，价值=DC-14 honesty）独立符合 R590 预测）**：承 R600 CONTINUE 对 code agent R601（747c8c44 导入 26 资产 + f056f50c A/B + capture-oracle-per-dir 全 writing-modes 刷新）做 doc-side 独立验证（read-only `file`/`stat`，未重跑 reftest 避免与活跃 code agent 资源争用——其 zero-wpt-runner 04:53 仍在跑）。**资产完整性 26/26 = 全 valid**：writing-modes 12（swatch-yellow/blue 15×15、blue-yellow-206w-165h 206×165、test-{tl,tr,bl,br} 100×100、5 exp-res PNG）+ css-fonts 5（font-variant-features/variation-sequences.css + font-weight-{bolder,lighter,normal}-ref.png 671×186 等）+ css-flexbox 3 + css-multicol 1 + css/support 3（60x60-green/red + red-rect.svg）+ images 1（blue.png）**全正确类型/尺寸，0 HTML 404，84B swatches 合法小图**；R597 handoff 两修正（red-rect.svg 落 css/support/；60x60-green/red 实缺）独立确认准确（本 turn-1 亦见 css/support/ ABSENT）。**R601 结论独立交叉确认**：2 案 oracle z_vs_chr（inline-replaced-vrl-004 6.04% / percent-margin-vlr-005 16.64%）均 >1%，残差 = writing-modes 轴 vertical-rl clearance/margin 结构性 gap（R114/R164 4 轮 ruled-out 簇）→ **无 clean oracle win，价值 = DC-14 honesty**（false-pass 消除，数字更诚实但通过数不升），**符合 R590 预测**（real-divergence 案导入后暴露更深 layout bug 而非翻 PASS）。★ **R600 诊断 RESOLVED**：code agent 自 R588 INACTIVE 是 5× stall 真因，**04:45:13 复活（747c8c44）执行 turnkey 导入即消除**——R599「doc role-exhaustion」+ R600「blocked」**状态 OVER**，deadlock 打破。doc track 恢复「验证 + handoff + 下 lever 识别」正常角色。**下 lever（code agent 活跃）**：① sizing/CSS-correctness vein 续（R583-R588 谱系，R589 014 inline-table=inline-level defer）② multicol layout-side column-aware IFC（R581 可执行 handoff 蓝图）③ Phase A IFC 统一（~2200 案，须用户决策多会话）。零代码变更（read-only 资产审计 + commit 交叉确认）。
