# 历史轮次归档 — R713（R712 异常 minimal repro · width:100px+margin:auto 隔离正确 · R712 red w=784 abspos-sibling quirk · 负结果 era）

> 内容 100% 保留自主控文档 master.md（R713 块逐字迁出，仅作归档）。下文为该轮完整逐轮详记。

**R713（R712 异常 minimal repro·`width:100px+margin:auto` 隔离测试 = w=100 居中正确·R712 red w=784 是 abspos 兄弟上下文触发（narrow quirk）非通用 lever·负结果·证 R165 margin:auto 覆盖 normal block·零源码）：承 R712（red `width:100px` 在 abspos 兄弟前被忽略 w=784，root cause 未明）做 minimal repro：构造隔离 `<div style="width:100px;margin:auto">`（**无 abspos 兄弟**）+ 占位 oracle PNG（reftest-oracle 须有 oracle 才 render+dump）LAYOUT_DUMP。**dump（裁决：隔离正确）**：div **w=100 x=350 居中✓**（= (784-100)/2+8，**width:100px + margin:auto 在隔离态完全工作**）。**★ 裁决：R712 red w=784 非通用 width+margin:auto bug**——隔离态 w=100 正确，故 R712 异常是 **abspos 兄弟（green）在 red 前触发**的 narrow 上下文 quirk（block 跟在 abspos 后 width 计算被扰乱），非 crisp 通用 lever。**★ 补强 R165**：R165 margin:auto 居中**覆盖 normal block**（minrepro w=100 居中证，非仅 abspos/table）。**裁决：R712 降级为 narrow test-specific quirk**（abspos-sibling-disrupts-following-block-width），非通用 lever，不入 hand-off 队列（低频，须时再 dig）。**★ 方法论意义**：minimal repro（隔离简化案 + 占位 oracle 触发 render）是裁决「通用 lever vs test-specific quirk」的决定性工具——R712 疑似 lever 经 minrepro 证伪为 quirk，避免 code-agent 追假 lever（类 R388/R692 oracle 须验证、R164 推断须实验落地的教训）。**下一步**：续 CSS2/positioning abspos-008（17.64%）或转他 revisit。零源码（minimal repro temp 文件已删 + 占位 oracle 已删，git status 仅 engine.rs WIP，未改任何源码；LAYOUT_DUMP read-only）。
