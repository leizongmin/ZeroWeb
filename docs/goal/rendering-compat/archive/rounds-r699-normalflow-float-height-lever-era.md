# 归档：R699（CSS2/normal-flow · §10.5.1 float-height clean lever era）

> 从 master.md 迁出的 R699 逐轮详记（内容 100% 保留）。R699 = block-non-replaced-height-011（★CLEAN-ISH LEVER，§10.5.1 非-BFC 块父 height 应排除 float 子，float_positioning.rs:16 taffy 当 float 作 in-flow block 计入父高——**详情在 [`pending-clean-levers.md`](../pending-clean-levers.md) Tier B**）。

---

**R699（CSS2_normal-flow 续·block-non-replaced-height-011 = ★REAL ISOLABLE BUG·§10.5.1 非-BFC 块 height 应排除 float 子（ZW 计入→h=96 显红，应 h=0 无红）·clean-ish lever（ZW parent-height post-process）·第 9 lever·CSS2_normal-flow 收口 3 clean lever·零源码）：承 R698 CONTINUE 查 CSS2_normal-flow 最后未查 top **block-non-replaced-height-011（16.12%）**。**预检**：无 script✓ / **overflow=1**（child 的 overflow:visible 是 test 设计；非 table-cell→engine.rs WIP no-op=honest 数）✓ / 无 img（oracle validity nonwhite=905≈仅文本「no red」=valid，无 broken-img）。**test**（Microsoft，§10.5.1 normal-block）：`#div1{background:red;height:auto}` 内唯一子 `div div{float:left;height:1in;overflow:visible;width:1in}`（1in=96px float），assert「**floated child 不计入 block-level non-replaced 元素 height**（overflow:visible 时）」→ `#div1` 应 **h=0**（float ignored，无 in-flow 子）→ 无红可见。**dump（裁决：BUG 确证）**：`#div1` **h=96**（= 1in float 高，**计入 float 子！**）w=784 abs_y=51 / float 子 h=96 w=96——应 #div1 h=0。**PIL**：ZW red **75264**（= 784×96，#div1 红背显）bbox(8,29)-(792,147) vs CHR red=**0**（#div1 collapse 到 0 高，无红）；16.12%(77365px) = #div1 多出的红背。**★ 根因（代码 read-only 精确界定）**：`float_positioning.rs:16` 注释明言「**taffy 将 float 元素当作普通 block 处理（按正常流排列）**」——taffy 把 float 当 in-flow block，**计入父 content height**；ZW float_positioning.rs 后处理仅调 float **位置**（reposition 到左/右 + clear），**不修正父 height**→非-BFC 父 #div1（overflow:visible）继承了 taffy 的 float-inclusive height（96）。**CSS §10.5.1**：block-level non-replaced + overflow:visible + height:auto → height = 「topmost in-flow 子顶 到 bottommost in-flow 子底」距离，**floating box 与 abspos box 显式 ignored**；#div1 无 in-flow 子→h=0。**裁决：真 isolable layout bug（spec §10.5.1）·clean-ish lever**——**fix scope（hand-off code-agent）**：加 ZW parent-height post-process（类 clamp_percentage_max_height / R695 §10.5 模式）：对 **非-BFC** 块父（`establishes_bfc` margin_collapse.rs:33 / `is_flow_root` engine.rs:676 已有检测，但**注意 is_flow_root 仅查 FlowRoot|InlineBlock，未含 overflow:hidden BFC**——次级 issue），重算 height = 仅 in-flow 子的 bottom（**排除 float 子**）。**复杂度中等**：① 须识别 BFC vs 非-BFC 父（含 overflow:hidden/clip/auto/scroll 也 establish BFC，当前 is_flow_root 未覆盖——若一并修则 expand BFC 检测）；② 重算 height 须不破坏 float 自身渲染（float 位置由 float_positioning.rs 独立设，与父 height 解耦，故 zeroing 父 height 安全——float 仍 overflow:visible 渲在其位）；③ blast radius 须 A/B（float-heavy 测试：现 wrong-包含-float 行为若被某些 reftest 依赖则 regression，但 spec 上现行为错，修应净改善）。**本案 16.12%→~0% PASS**（#div1 h=0 无红），**潜在多案 yield**（非-BFC float-containing-parent reftest 簇 + clearfix 相关）。**★ 与已知 float 工作区分**：R108b（float flow_bottom 双计 margin）/ R145（flex/grid item float 归零）/ R247（multiline stacking）均 float 子**位置**；R699 是**父 height**（float 是否计入父高），新维度，独立 lever。**★ 意义**：① CSS2_normal-flow 第 3 clean lever（R695 %height 88% + R696 replaced-sizing unified + R699 §10.5.1 float-height）——fresh pivot（第 5 采样目录）产 3 lever（与 css-tables 3 持平，最高产目录之一），证实 CSS2 core box-model 高产；② LAYOUT_DUMP 脉络第 9 clean lever（R678/R679/R680/R689/R691/R692/R695/R696/R699）；③ **「taffy 把 float 当 in-flow block」是 ZW float 模型系统性特征**（float_positioning.rs:16 注释），非-BFC 父 height 排除 float 是该模型下须 ZW 后处理补的 spec 点。**CSS2_normal-flow top 收口**：3 clean（R695/R696/R699）+ 2 overflow-负（R697/R697 max-width/min-height = element-overflow-clip gap）+ inline-replaced-width sub-group-2（012/013 float-wrapping defer）——top 穷尽，下一轮 pivot 第 6 目录。hand-off code-agent：**§10.5.1 非-BFC 父 height 排除 float 是 clean-ish lever（16%→PASS，+潜在多案），ZW post-process 可行**。零源码（预检 read-only + LAYOUT_DUMP read-only + product-smoke 渲 PNG read-only + PIL red-count + 代码 read-only 定位 float_positioning.rs:16 + margin_collapse.rs:33 + engine.rs:676，未改任何文件）。
