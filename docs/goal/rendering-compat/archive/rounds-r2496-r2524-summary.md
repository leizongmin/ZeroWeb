# 轮次详记归档：R2496–R2524（自 master.md §最近轮次摘要 迁出）

> **迁出日**：2026-08-04（R2556，doc-control 策略「master.md 不允许无限增长」瘦身续 R2555）。
> **来源**：master.md §最近轮次摘要 底部 29 轮（R2524→R2496，全 blockquote 体，R2555 已清 plain-format 不一致）。
> **性质**：100% 原文保留，未删减、未重排、未改格式。索引见 master.md §历史轮次归档索引。

> **📍 R2524（2026-08-03）📋 sibling-not-implemented vein 系统性穷尽复核（registry-vs-corpus diff，强于 R2519-R2521 angle-based 钉案）— 零代码，确认 css-text-decor 同源（R2522/R2523）是真最后 cluster**。承接 R2523「sibling vein 续 → 转其他 CSS 模块」。**方法**（系统性，非猜测）：① 提取 `registry.rs::known_properties()` 全集 + shorthand 集；② grep `css-content/css-pseudo/css-lists/css-masking/css-overflow/css-backgrounds/css-color/css-ui/css-ruby/css-writing-modes` 测试文件的 CSS 声明；③ diff 出「测试用但未注册」属性，按 driving-test 数 + 实现难度排序（Explore agent + 人工核验双重）。**核验结论**：所有「未注册」候选经人工核验后**全落 5 类**：(a) **已实现**（agent 误报）——quotes（10 driving test PASS）/ mask-type·mask-mode / block-size·inline-size + min/max-block/inline-size 全逻辑尺寸 / outline-offset 含 inset keyword 全已 apply；(b) **parse-only no-op**（静态 reftest 不渲染）——caret-shape·caret-animation（光标渲染 = host-layer，静态截图无 caret）/ nav-up/down/left/right（spatial navigation = 交互，无视觉）= R2520-BiDi 谱「computed-value correctness（getComputedStyle）零渲染 net-0 非 lever」，code-guidelines §2 反对未请求灵活性，defer；(c) **无 driving test**——overflow-block·overflow-inline（仅 `overflow-inline-block-*` 是 inline-block 元素测试非属性）；(d) **moderate 稀疏**——background-blend-mode（须 per-layer 存储 + 背景 paint 集成，mix-blend-mode 引擎既有但 wire 风险，稀疏 test）/ marker-content（须 ::marker pseudo 生成 + content override，moderate）；(e) **deep**——ruby-align（43）/ ruby-overhang（41）/ shape-margin·shape-image-threshold（float 文字绕排 = inline 布局）/ text-spacing-trim（字间距 + font 度量）/ 垂直模式 logical（R1043 gated）。**结论**：sibling-not-implemented vein 经**系统性 registry-vs-corpus diff**（强证据，非 angle 猜测）穷尽性钉案——clean real-effect lever 真正尽；R2522 offset + R2523 color 是 css-text-decor 同源真最后 cluster。**rally 状态 = directive 07-29 mandated 稳定 plateau**（已 3 连 clean lever：R2517 text-decoration-line / R2522 offset / R2523 color，皆 fresh code-reading 破「exhausted」）； sanctioned 持守工作（doc-drift 高价值既清 / plateau-guard R2523 既稳）薄。**真正推向 95% 须用户授权深结构**（最高 value = **Phase A IFC** 解 css-text + welcome IFC 残余；次 = font-stack C-dep / R1043 vertical-mode / R2174 taffy border-box / ruby-layout / multicol-fragmentation），授权后 spec-rfc 出 RFC 多 session 推进。**下轮可接续**：① 周期 re-probe（systematic diff 已尽，新 clean lever 概率极低）；② 偶发 doc-drift；③ background-blend-mode / marker-content 作 moderate slice（须 A/B 量证，低 ROI）；④ 待用户点名深结构（最高价值解锁）。

> **📍 R2523（2026-08-03）🔧✅ `text-emphasis-color` 实现 LANDED（CSS Text Decor 3 §3.3；longhand+shorthand parse + 继承 computed + paint 显式色覆盖；net-0 reftest +1 unit，默认 currentColor 字节不变）— sibling-not-implemented vein 第 2 个 clean lever**。承接 R2522「sibling vein 续」。**probe**：text-emphasis-color **完全未存储**（registry/shorthand 显式注释「暂未实现，剥离 color token」），paint 标记固定用 owner.color（currentColor）。driving test text-emphasis-color-property-002（显式 green，标记应绿，currently 黑）。**bug**：`text-emphasis-color: green` 整条丢 → 彩色 CJK 着重点颜色错误。**fix**（2 crate，mirror text_decoration_color 模板）：① style-system computed_style 字段 ColorValue（默认 CurrentColor）+ default + **inherit 双路径**（inherited：clone + apply_initial + is_inherited=true）+ apply arm（parse_color_fn）+ registry initial/known + PropertyDecl 变体 + **text-emphasis 简写「剥离 color」→「展开为 color longhand」**（color_part Some 时 emit）+ 注释纠偏；② engine paint **双 IFC 路径**（主路径 owner_style + macro_rules render_fragment owner_style_opt）：每路径加 emphasis_color（显式覆盖 currentColor，默认 → unwrap_or(frag_color) 字节不变），标记 add_glyph color 从 frag_color 改 emphasis_color（仅 ● mark，文字 glyph 不动）。**driving 单测 2 个**（apply：默认/green=Rgba(0,128,0,255)/非法 false；shorthand：filled circle red → color+style 两条）；既有 strips_color 测试改 expands_color（行为反转）。**门禁（四重零回归）**：fmt + clippy `--workspace --all-targets -D warnings` 干净；全量 `make test` **13190/0/74 EXIT=0**（vs R2522 13189 = **+1 新单测**）；scoped `make reftest-upstream FILTER=css-text-decor` **245/250 net 0**（5 fail 全 pre-existing dotted/skip-ink/scroll 与 R2522 完全同集；**全 5 emphasis-color 测试 pass**，property-002 显式 green 0.21% 从黑→绿 spec-correct；R2394 谱：标记小故 diff 在 ≤0.5% 容差内 footprint 0）；`make product-smoke` EXIT=0 welcome **17.03%（81737/480000）字节不变** + 全 8 struct PASS。**意义**：CSS Text Decor 3 §3.3 落地（real-world 彩色 CJK 着重点正确）；sibling vein 续证（R2522 offset 后第 2 个 clean lever）；默认 currentColor 字节不变 = additive 安全。**下轮可接续**：① sibling vein 续（text-underline-position moderate font-wall-gated / text-justify·hanging-punctuation·hyphens 深）；② 转其他 CSS 模块 not-implemented-with-driving-tests probe（css-masking/css-ui 余项/css-lists）；③ 待授权深结构。

> **📍 R2522（2026-08-03）🔧✅ `text-underline-offset` 实现 LANDED（CSS Text Decoration 4 §2.5；auto|length|% parse + 继承 computed + paint 下划线下沉；net-0 reftest +4 unit，auto=0 字节不变）— fresh code-reading 第 5 角度（CSS-Text-Decor sibling not-implemented）破「exhausted」**。承接 R2521「周期 re-probe」。**probe**：fresh 角度（前 9 维穷尽漏此）——CSS-Text-Decor/Text **sibling 属性 not-implemented** vein：grep corpus = `text-underline-position` 32 / `text-underline-offset` 59 / `text-justify` 39 / `hanging-punctuation` 33 / `hyphens` 86 driving test 文件，全 zero impl。逐项 triage：hyphens=深（dictionary+shaping）；hanging-punctuation=深（inline 布局）；text-justify=深（justify 算法）；text-underline-position=moderate（`under`/`from-font` 须 font 度量 + 垂直模式）；**text-underline-offset=CLEAN**（mirror text-decoration-inset R1607 模式，paint-localized，auto=0 字节不变）。**bug**：属性完全未注册 → `text-underline-offset: 11px` 整条丢，下划线停默认位置（real-world 下沉/上抬链接下划线位置错误）。**fix**（3 crate，mirror inset R1607）：① css-parser `TextUnderlineOffsetValue{Auto,Length(LengthValue)}` + `parse_text_underline_offset`（auto/px·em·rem·ch·%/负值；from-font 等非法→None）；② style-system computed_style 字段 + default(Auto) + **inherit 双路径**（inherited：inherit_property parent→child + apply_initial_value + is_inherited=true）+ apply arm + registry initial/known + PropertyDecl variant；③ engine paint effects.rs underline y += offset_px（auto=0 字节不变；em/rem/% 按 font_size resolve），仅 underline 受影响。**driving 单测 4 个**（auto 大小写 / px 正负 / em·% / 非法 from-font·多token·空）。**门禁（四重零回归）**：fmt 干净 + clippy `--workspace --all-targets -D warnings` 干净；全量 `make test` **13189/0/74 EXIT=0**（vs R2517 13185 = **+4 新单测**）；scoped `make reftest-upstream FILTER=css-text-decor` **245/250 net 0**（5 fail 全 pre-existing skip-ink/dotted/scroll/host-layer；**11/12 underline-offset pass**，driver 002 等 0.00% exact；R2394 谱：spec-correct 但 diff 在 text-class ≤0.5% 容差内故 footprint 0）；`make product-smoke` EXIT=0 welcome **17.03%（81737/480000）字节不变** + 全 struct PASS。**意义**：CSS Text Decor 4 §2.5 落地；再证「exhausted」可破（第 5 角度 sibling-not-implemented）；auto=0 additive 安全。**下轮可接续**：① sibling vein 续（text-underline-position moderate 须 font 度量 / `from-font`；text-justify/hanging-punctuation 深）；② 周期 re-probe；③ 待授权深结构。

> **📍 R2521（2026-08-03）📋 clean-lever exhaustion **全维度收官**（presentational-attributes vein 续证 = 第 9 维度尽；零代码）— rally 进入稳定 plateau，深结构待授权**。承接 R2520「周期 re-probe」。**presentational-attributes vein（R1651-R1675/R1710-R1716 谱）probe**：① **`<ol start=N>` / `<li value=N>`** = **已实现**（R1701 `list_item_counter` text_list.rs:726 直接读 start/value attr，fixture 22 `<ol start=3 type=A>` → C/D/J/K；非 presentational hint 路径）；② **`hspace`/`vspace`**（img/applet margin）= **R1710 已 A/B 证 net-negative defer**（font-wall 文本宽 + inline 垂直 margin 发散 +168/+541px；lib.rs:1138 注释存档）；③ **`vlink`/`alink`**（body visited/active link color）须 `:visited`/`:active` pseudo-class 状态（host-layer），非 clean；④ nowrap/clear/type/border/align/valign/width/height/bgcolor/cellpadding/cellspacing/frame/noshade 全 handle。**结论（全维度收官）**：clean-lever surface 经 **9 大维度**穷尽性钉案——(1) reftest clean lever（4-dir oracle + 30-dir self-source，R2516/R2519）、(2) value-space multi-value/enum-drop（R2517-R2520）、(3) UA stylesheet 完备性（R2520）、(4) BiDi（parsed-not-applied，R2520）、(5) filter 多函数（Vec-handled，R2520）、(6) alignment safe|unsafe（无 driving test，R2519）、(7) presentational-attributes（R1701/R1710 既覆盖，本轮）、(8) simple keyword props（全注册，R2519）、(9) complex value forms + CSS functions（handled-or-deep，R2519）。**R2509-R2517 已 land**（5 §5 拆分 + text-decoration-line lever），全验证零回归。**rally 状态 = directive 07-29 mandated 稳定 plateau**：自主 clean-lever 真正穷尽（9 维度钉案）， sanctioned 持守工作（doc-drift 高价值既清 / plateau-guard R2517 既稳 / 周期 re-probe 低概率）薄；**真正推向 95% 须用户授权深结构**（最高 value = **Phase A IFC** 解 css-text + welcome IFC 残余；次 = font-stack C-dep / R1043 vertical-mode / R2174 taffy border-box / ruby-layout / multicol-fragmentation），授权后 spec-rfc 出 RFC 多 session 推进。**下轮可接续**：① 周期 re-probe（9 维度钉案，新 lever 概率极低）；② 偶发 doc-drift；③ 待用户点名深结构（最高价值解锁）。

> **📍 R2520（2026-08-03）📋 clean-lever exhaustion 续证（UA 完备性 + BiDi parsed-not-applied + filter Vec-handled + css-align 无 driving test）— 零代码，新维度钉案**。承接 R2519「周期 re-probe」。**转新维度 probe**（value-space 既尽，试 UA/presentational/BiDi/filter）：① **UA stylesheet 完备性**（lib.rs:561+ `ua_decl_inputs`）：常见 inline-semantic 全覆盖（mark/sub/sup/small/kbd/samp/var/cite/figure/figcaption/ins/del/s/tt/big/font/...）；缺失仅 `bdi`/`bdo`（UA default `unicode-bidi: isolate`/`bidi-override`）、`ruby`（ruby-layout 深）；`abbr`/`wbr`/`time` 无 Chrome default = 非 gap。② **BiDi parsed-not-applied 钉案**：`text_metrics.rs:449` 用 `unicode_bidi::BidiInfo` 仅处理**原始文本**，**不读 `style.unicode_bidi`**；唯一非 test 消费 = `painter/mod.rs:1501` debug indicator → CSS `unicode-bidi` 属性 parse-only（含 isolate/override/isolate-override/plaintext 全值）→ **bdi/bdo UA default 即使加也只是 computed-value correctness（getComputedStyle），零渲染效果，net-0 reftest**，非 lever（R2394 谱但无 visual 路径）。③ **filter 多函数链**：`computed_style.filter: Vec<FilterComputedValue>`（apply_advanced.rs:864 逐函数映射），**多函数链已 handle**，非 lever。④ **css-align 无 safe/unsafe driving test**（corpus grep `safe|unsafe` = 0，dir 未导入）→ alignment safe|unsafe 即使做也 net-0 reftest + moderate 改动面，code-guidelines §2 反对（无 driving test 不费力），**钉案 defer**。**结论（续证）**：clean-lever exhaustion 经 R2519 + 本轮 UA/BiDi/filter/css-align 多新维度**再次钉案**——自主 surface 真正穷尽。**rally 状态**：directive 07-29 mandated 持守模式（doc-drift/plateau-guard，深结构 gated）；近期无代码变更（R2518/R2519/R2520 皆 probe-only/doc），plateau-guard 上轮（R2517 legacy）已确认稳定，重复 guard 冗余。**真正推向 95% 须用户授权深结构**（最高 value = Phase A IFC 解 css-text + welcome IFC 残余 / font-stack C-dep / R1043 vertical-mode / R2174 taffy border-box），授权后 spec-rfc 出 RFC 多 session。**下轮可接续**：① 周期 re-probe（低概率，exhaustion 多维钉案）；② 偶发 doc-drift；③ 待用户点名深结构。

> **📍 R2519（2026-08-03）📋 clean-lever surface **穷尽性钉案**（alignment safe|unsafe = 48 处 prod 引用 = 离散重构非 clean；简单关键字属性全注册；复杂值形式 handled-or-deep）— 零代码，省后续 agent 重探**。承接 R2518「续 value-gap probe」。**alignment safe|unsafe 深度钉案**：taffy 0.12.1 **原生支持** overflow-position（`AlignItems`/`JustifyContent` 是 struct { position keyword, `AlignmentSafety` modifier }，有 SafeStart/SafeCenter 等 const）—— 但 ZW `AlignmentValue` 是扁平 enum，全 codebase（style-system + layout-engine）**48 处 prod 引用**（converter 4 函数 + 布局计算 match 散布，非仅 converter）。enum→struct 带 safety flag 改动 = 48 处 match 全破（离散）；并行加 4 computed_style 字段（align_items/self/content + justify_content 各一 safety）+ parser 签名改 + converter = ~15 处 moderate。**两者皆非 clean lever**（改动面大 / moderate，超 directive「低风险 改动面小」门槛）。`first|last baseline`：taffy AlignItemsKeyword 仅单 `Baseline`（无 first/last 区分）→ parse-only 误导（code-guidelines §2 反对），defer。**简单关键字属性全注册**：empty-cells / unicode-bidi / break-inside|before|after / table-layout / caption-side / border-collapse 全在 registry（初始值齐），非 lever。**复杂值形式 handled-or-deep**：grid-template-areas 存 raw string（消费 = taffy grid 深）、clip-path path() = paint 深（shapes 已 M9）、attr() 已 handle、border-image R2464。**结论（钉案）**：rendering-compat **autonomous clean-lever surface 经多轮多角度穷尽性实证完毕**——reftest 4-dir oracle（nesting/cascade/grid/tables）+ 30-dir self-source + code-reading 全维（color/values/cascade/selectors/logical/display/text-align/counter-style/backgrounds/transforms/font-face/CSS-Text-4/css-ui/multi-value/enum-drop/simple-keyword/complex-form/CSS-function）；R2509-R2513 §5 收口、R2517 text-decoration-line 是近期唯一 clean lever；残余全 **user-gated 深结构**（Phase A IFC / R1043 vertical-mode / R2174 taffy border-box / font-stack C-dep / ruby-layout / multicol-fragmentation）或 **moderate 候选**（alignment safe|unsafe ~15 处 / individual translate/rotate/scale R2368）。**rally 自主推进 = sanctioned 持守模式**（doc-drift + plateau-guard + 周期 re-probe），**真正推向 95% 须用户授权深结构**（07-29 护栏）。**下轮可接续**：① 周期 plateau-guard（product-smoke/legacy 回归守）；② doc-drift 残余 pre-existing `.rs:NNN`（低价值）；③ 偶发 re-probe（低概率出新 clean lever）；④ 待用户点名深结构 → spec-rfc 出 RFC 多 session 推进（最高 value：Phase A IFC 解 css-text+welcome IFC 残余 / font-stack C-dep）。

> **📍 R2518（2026-08-03）📋 value-gap probe 续扫（flex-basis/text-emphasis 已全 + alignment safe|unsafe/first|last baseline = moderate/deep gap）+ R2517 product-smoke-legacy 零回归确认 — 零代码**。承接 R2517「续 apply-enum-drop / value-gap probe」。**probe 结果**：① **flex-basis: content**（parse.rs:373）= **已实现**（FlexBasisValue::Content），非 lever；② **text-emphasis-style `<string>` + `[filled|open] [shape]`**（parse_misc.rs:465）= **已全**，非 lever；③ **align/justify `safe|unsafe <pos>` + `first|last baseline`**（parse_alignment parse_misc.rs:294 单关键字 match 丢这些 multi-token 形式）= **真 gap 但 moderate/deep**——`safe`/`unsafe` 须 overflow-layout fallback 处理（内容溢出容器时 safe 回退 start 防数据丢失，须布局层）、`first`/`last baseline` 须 taffy baseline（R… font-wall gated）；parse-only 储存而不 apply 会误导（code-guidelines §2 反对 unused 灵活性），**非 clean lever，记 moderate 候选待用户点名或深布局 slice**。**R2517 product-smoke-legacy 验证**（R2517 改 text-decoration-line 渲染，legacy HTML 有下划线链接）：42 fixture / **0 struct-check FAIL** / avg **2.36% 与 baseline 持平** → R2517 **四重验证零回归**（make test 13185/0/74 + css-text-decor reftest net-0 + product-smoke welcome 17.03% + product-smoke-legacy 2.36%）。**意义**：value-gap vein 经多轮多属性穷尽（flex-basis/text-emphasis 已全；alignment safe|unsafe/first|last baseline = moderate/deep；font-variant-系 font-wall 低值）——**clean high-value lever surface 再次趋薄**（R2517 text-decoration-line 是近期唯一真 lever）；alignment 多 token 值钉案（moderate，须 overflow/baseline 布局层，非单点 parse fix）。**下轮可接续**：① 续 value-gap probe 转其他属性组（border-image slice/width/outset、grid-template-areas `<string>`、clip-path `path()`、CSS 函数 `attr()`/`toggle()`）；② alignment safe|unsafe/first|last baseline 作 moderate slice（须布局层 A/B）；③ 机会性 plateau-guard；④ 待授权深结构（Phase A IFC / R1043 / R2174 / font-stack）。

> **📍 R2517（2026-08-03）🔧✅ `text-decoration-line` 多值组合 LANDED（CSS Text Decoration §3；enum→struct flags + 多关键字 parser + paint 多线循环 + shorthand 累加；net-0 reftest +1 unit，single-value 字节不变）— fresh code-reading probe 出 lever（再证「exhausted」可破）**。承接 R2516「下轮 fresh code-reading CSS 合规 probe」。**probe**（4 角度）：① value parser `starts_with` 位置敏感性 → 全函数前缀（rgb/calc/cubic-bezier），非 R2476 inset-anywhere 类；② background-repeat 2-value（space/round ×）→ enum 单轴建模 + paint 平铺精度（R2484 既核 paint-deep），非 clean；③ 缺失 shorthand → 全主要 shorthand 已处理（R2386 all/R2396 logical/R2462-65 wide-keyword）；④ **apply 层 enum-drop** → 命中 **text-decoration-line 多值**。**bug（双路径丢）**：① longhand `parse_text_decoration_line`（parse.rs:230）单关键字 match → `underline overline` 落 `_ => None` 整条丢；② shorthand `expand_text_decoration`（mod.rs:1917）`line = part.to_string()` **覆盖**致仅留最后一个 line 关键字。故 `text-decoration: underline line-through red`（常见：删除线+下划线链接）→ 仅 line-through，underline 丢。**fix**（CSS Text Decoration §3 多值组合，单 crate style-system + 2 engine/layout 消费点）：① `TextDecorationLineValue` enum→`struct { underline, overline, line_through }`（property/types.rs，`NONE` const + `has_any()` helper；obsolete `Blink` 删——历史 `Blink => return` 不渲染 = 全 false）；② parser 多关键字累加（`none` 重置全 false 取消其他、`blink` 接受不设 flag、任一非法→整值 None）；③ shorthand `line_toks: Vec` 累加 + `join(" ")`（旧 `line=part` 覆盖改累加）；④ paint `paint_text_decoration_from_style`（effects.rs:337）改 `has_any()` 早退 + 3 条件绘制（underline/overline/line-through 各 y_offset，共享 color/width/inset）——**single-value y_offset 与历史字节一致**；⑤ inline_finalization.rs:31 `!matches!(...None)`→`has_any()`；default/registry `None`→`NONE`。**css-parser 重复 enum（types.rs:534）test-only，不动**。**driving 单测** `test_parse_text_decoration_line_multi_value`（2/3 条组合 + 顺序无关 + 大小写 + 非法整值 None + none 取消）TDD red→green；既有 single-value 测试经 sed 机械改 variant→struct-literal（5 文件 24 处）。**门禁**：scoped `cargo clippy -p zero-style-system/engine/layout-engine --all-targets -D warnings` 干净；**全量 `make test` 13185/0/74**（vs R2516 13184 = **+1 新单测**，零 panic/OOM，零回归）；**workspace clippy --workspace --all-targets -D warnings 干净**；fmt 干净；**scoped css-text-decor reftest 245/250 = net 0**（5 fail 全 skip-ink/dotted/scroll unrelated；010-013 实为 `none`+font-wall 测试非多值，故多值 fix 零 reftest footprint——R2394 谱：正确算法不产 reftest 因 driving 案不在 corpus）；**product-smoke welcome 17.03% 字节不变 + 全 struct PASS**（single-value paint 字节一致实证）。**意义**：CSS Text Decoration §3 多值组合落地（`underline line-through` 等常见组合不再丢，real-world 删除线+下划线链接正确渲染）；再证「exhausted」可破——fresh code-reading probe 第 4 角度（apply enum-drop）出 lever（R2476/R2485/R2517 谱）；single-value 字节不变 = additive 安全。**下轮可接续**：① 续 apply-enum-drop probe（其他 enum 单变体属性可能藏多值缺口，如 text-emphasis / box-shadow 已多值 / list-style）；② 机会性 plateau-guard；③ 待授权深结构（Phase A IFC / R1043 / R2174 / font-stack）。

> **📍 R2516（2026-08-03）📋 plateau-guard：§5 拆分链（R2509-R2513）渲染零回归确认 + css-grid oracle 复核 clean-lever 穷尽 — 零代码**。承接 R2515。本轮两路验证：(1) **css-grid oracle 真一致率复核**（self-source 75.5% 之外首次 oracle，50 shot）= **51.0% credible**（self-source 75.5% 含 ~24.5pp 假通过）；21 fail 全分类非 clean——`table-grid-item-dynamic-*`（JS 动态 OOS）、`grid-container-baseline-synthesized-*`（baseline 合成 = taffy baseline 深）、`nested-grid-item-block-size`（嵌套 grid 深）、`stretch-grid-item-*-overflow`（form control = R2174 replaced border-box territory）、`firefox-bug-1881495`（FF 专属 OOS）、`whitespace-reattach`/`dynamic-grid-within-flexbox`/`grid-with-dynamic-img`/`img-src-changes`（全 JS 动态 OOS）。**css-grid 无 clean lever（oracle 真指标确认）**——累计 4 dir oracle（css-nesting R2509 + css-cascade R2509 + css-grid 本轮 + **css-tables 续测**：59.1% credible vs self-source 89.6%，36 fail 全 table-layout 深——border-collapse/sizing/margin-collapse + R1043 vertical-mode collapsed-border + R2174 border-box min-max-size，无 clean lever）+ 30 dir self-source 全证 clean-lever 穷尽。(2) **product-smoke plateau-guard**（验证 R2509-R2513 §5 拆分——touch css-parser/style-system/engine/dom/layout-engine——渲染零影响）：welcome **17.03% diff 与 baseline 完全持平**（81737/480000 px）+ 全 struct-check **PASS**（welcome card:4/shortcut:6/link-tile:4/footer:1/title:1/tagline:2、wintertc bg-orange-500:4/img-visible/text-justify≥2、morning-work article:1/item-tag:3/lang-bash:1，800/375/320 三宽度全 PASS，0 issues + sibling-overlap）→ **§5 拆分 byte-identical 渲染输出实证**（mechanical refactor 经 make test 13184/0/74 + product-smoke 双确认）。**追加 product-smoke-legacy 验证**（document.rs/dom 拆分按 run-rules 触发 legacy 门禁）：42 fixture / **0 struct-check FAIL** / avg diff **2.36% 与 baseline 持平**（font-wall 趋势数据不变）→ §5 拆分链**三重验证零回归**（make test + product-smoke 现代 + product-smoke-legacy HTML 3.2/4）。**意义**：§5 拆分链（5 文件 -1088 行）经产品 fixture oracle 回归门禁确认零渲染回归（run-rules「涉及渲染/布局变更建议跑 product-smoke」兑现）；css-grid oracle 钉案（join css-nesting/css-cascade，clean-lever 穷尽多 dir 多指标实证）。**自主高价值工作面现状**：clean-lever 穷尽（oracle 多 dir 确认）、§5 实质收口（R2514）、doc-drift 高价值项已清（R2515，残余 ~33 多为 pre-existing 经 R2482/R2502/R2504 既核）、深结构全 user-gated。**下轮可接续**：① doc-drift 续扫残余 pre-existing `.rs:NNN`（低价值，逐符号 git 考古）；② 机会性 plateau-guard（product-smoke-legacy 51 fixture）；③ 待用户授权深结构（Phase A IFC / R1043 vertical-mode / R2174 taffy border-box / font-stack）。

> **📍 R2515（2026-08-03）✅ doc-drift 续修 2 处 `.rs:NNN` 注释引用（R2509-R2513 §5 拆分所致行号漂移；纯注释零行为变更）**。承接 R2514「下轮 doc-drift 纠偏（R2482/R2502/R2504 vein 续）」。本轮定向修本会话链 §5 拆分**直接造成**的 2 处 stale 行号引用：① `crates/layout-engine/src/engine/postprocess.rs:844` `engine.rs:1909` → **engine.rs:1170**（R2513 抽出 sizing 组 745 行致 is_block_level 从 1909 上移到 1170）；② `crates/layout-engine/src/print_pagination.rs:31` `pipeline.rs:748` → **pipeline/mod.rs:751**（R2511 pipeline.rs→pipeline/ 目录化 + 加 `mod extract` 3 行致 layout_extent_y 748→751，且文件名 pipeline.rs→pipeline/mod.rs）。两处均经 git 考古（pre-split commit 的旧行内容）+ 当前符号位置双重核对。**门禁**：纯注释零行为，`cargo fmt --all --check` 干净（注释改动不经 make test）。**意义**：清理本会话链 §5 拆分的连带 doc-drift（负责任的「精准修改 §3：删除因自己修改而无用的引用」）；doc-drift vein 续（R2482/R2502/R2504/R2515），残余 ~35 处 `.rs:NNN` 代码注释引用多为 R2482/R2502/R2504 既核（pre-existing，非本链所致），下轮可继续定向核。**下轮可接续**：① doc-drift 续扫（残余 ~35 处逐符号核对，纯注释）；② 机会性 plateau-guard；③ 待授权深结构。

> **📍 R2514（2026-08-03）📋 §5 文件大小控制脉**收口评估**（flexbox.rs vendored 豁免确认 + text.rs borderline 容忍 + §5 实质工作收官）— 零代码，记判定省后续 agent 重探**。承接 R2513「下轮 text.rs / flexbox.rs / borderline」。**flexbox.rs（2553）vendored 豁免确认**：`crates/taffy-local/Cargo.toml` name=`taffy` version=`0.12.1`，lib.rs = 上游 Taffy 文档逐字，flexbox.rs = 上游 Taffy flexbox 算法文件（2553 行上游结构）→ **vendored fork，按上游结构保 re-sync 兼容，豁免 run-rules §5**（§5 管 ZeroWeb 自有代码，不管 vendored）。**text.rs（2025，+25）borderline 容忍**：唯一可抽 = 2 free 函数（format_counter_text 计数器格式化 + get_img_intrinsic_size 图像固有尺寸，弱 cohesion——「paint_text 支持助手」grab-bag）；且 format_counter_text 调 `format_counter_alpha`（sibling `text/text_list.rs` `pub(super)`）→ 移到 `text/support.rs` 须**拓宽 sibling helper 可见性**（pub(super)=父可见，sibling 不可见）= entangled。裁决：**+25 属 run-rules §5「一般不要超过……应考虑合理拆分」的「考虑」容忍区间**（只 25 行超、唯一拆分损 cohesion 且须 visibility 拓宽），不强拆，记 borderline-accepted。**borderline 文件**（`lib.rs` 2014 / `types.rs` 2003 / `app_render.rs` 2003，差 +3~+14）：同容忍区间，模块 hub / 类型定义 hub 强拆损 cohesion，不强拆。**§5 实质工作收官**：本会话链 5 个 >2000 文件 land（parser.rs 3047→1959 R2509 + shorthand/mod.rs 2260→1963 R2510 + pipeline.rs 2122→1795 R2511 + document.rs 2034→1835 R2512 + engine.rs 2163→1419 R2513），全 byte-identical 零回归（make test 13184/0/74 ×5）；残余全 vendored-exempt 或 borderline-tolerance。**§5 脉至此收口**。**下轮可接续**（§5 既收口，pivot 其他 sanctioned 非 gated 非网络工作）：① **doc-drift 纠偏**（R2482/R2502/R2504 vein 续——`.rs:NNN` 行号引用核对修，~41 处候选，纯注释零行为变更）；② 机会性 plateau-guard（product/legacy smoke 回归确认 + reftest dir 守）；③ 若用户授权深结构（Phase A IFC / R1043 vertical-mode / R2174 taffy border-box / font-stack）→ spec-rfc 出 RFC；④ 勿以 §5 为 lever（实质工作已尽，borderline/vendored 不动）。

> **📍 R2513（2026-08-03）🔧✅ `engine.rs` sizing-helpers 关联函数组抽出 LANDED（run-rules §5；2163→1419 跨 2 模块；mechanical refactor 零行为变更）— R2509-R2512 §5 文件拆分脉续，本会话链第 5 个 >2000 文件收官（含风险最高的布局计算 slice）**。承接 R2512「下轮 engine.rs 2163 / text.rs 2025 impl 跨文件拆分（大工程，R2509 模式）」。**engine.rs 结构核**：单一巨型 `impl LayoutEngine`（169-~2155，~2000 行）但含一组 **associated functions（关联函数，无 `&self`）**——sizing-helpers（apply_intrinsic_content_sizing / is_zeroish_len / apply_flex_aspect_ratio_item_size / apply_aspect_ratio_container_cross_size / apply_indefinite_percent_height_to_auto / gather_replaced_html_attr_intrinsic / resolve_percentage_padding，lines 1016-1760，745 行）。**关键发现 1**：这些方法经核查**零 `self.` 引用**（按参接收 taffy_tree/styles，非访问 LayoutEngine 字段）= 等效 free 函数，比 parser at-rules（&self 方法访问字段）**更易抽**。**关键发现 2**：engine.rs **早已有 `engine/` 子目录**（abspos.rs R342 / postprocess.rs R965，皆 free-函数 + glob import 模式）→ **零目录化**，直接加 `mod sizing;`。**关键发现 3**：关联函数经 `Self::method(...)` 由 compute 方法调用——inherent impl 块随 `mod sizing;` 激活即 crate-wide 可用，**无需 glob import**（区别于 abspos/postprocess 的 bare-name + glob）；移出的 7 方法 `pub(super)`（等价「engine 模块私有」，父 engine.rs 经 `Self::` 访问）。**关键发现 4**：sizing.rs `use super::*;` **继承 engine.rs 全部模块级 import**（pub + private `use` re-import）——Rust 子模块可访问父模块私有项，glob 据子视角可见性导入，故零显式 import 即编译首过通。**fix**（mechanical 抽取，零逻辑变更）：组整体移到 `crates/layout-engine/src/engine/sizing.rs`（760 行，`impl LayoutEngine { pub(super) fn ... }` 第二 impl 块）；engine.rs 删组（2163→**1419 < 2000 ✓**）+ 加 `mod sizing;`（无 glob）；extract_layout（1764，6 处外部调用）保持原位。**门禁**：scoped `cargo clippy -p zero-layout-engine --all-targets -D warnings` 干净（首过零修复）；**全量 `make test` 13184/0/74**（vs R2512 **13184 完全持平**，零 panic/OOM，零回归——**布局计算 byte-identical 实证**，本会话链风险最高 slice 亦稳）；**workspace `cargo clippy --workspace --all-targets -D warnings` 干净**；`cargo fmt --all --check` 干净。**意义**：engine.rs 2163 §5 违规收官（2163→1419 跨 2 高内聚模块 engine 主 1419 / sizing 固有尺寸修正 760）；证「associated-fn 组 + 第二 impl 块 + `Self::` 跨文件解析 + `use super::*` 继承父 import」= **既有 `engine/` 子目录的巨型 impl 文件**拆分可复现模式（零目录化、零 glob、零显式 import，最简 impl-split 变体）；**本会话链累计 5 个 >2000 文件收官**（parser.rs 3047→1959 R2509 + shorthand/mod.rs 2260→1963 R2510 + pipeline.rs 2122→1795 R2511 + document.rs 2034→1835 R2512 + engine.rs 2163→1419 R2513），全 byte-identical 零回归（含布局计算最高风险 slice）。**残余 §5 违规**（下轮可接续）：`flexbox.rs` 2553（taffy-local **vendored fork**，倾向豁免，须 read-only 判定）；`text.rs` 2025（engine paint，**单一巨型 `impl Painter`** ~1950 行，同 engine.rs/R2513 关联函数模式可抽——paint helper 关联函数组）；borderline `lib.rs` 2014/`types.rs` 2003/`app_render.rs` 2003（差 <20，文件大小 tolerance 边缘，强拆损 cohesion 低价值）。**下轮可接续**：① text.rs 2025 paint-helper 关联函数组抽（R2513 模式，需核 paint 方法是否 associated fn）；② flexbox.rs vendored 豁免 read-only 判定；③ borderline 文件评估（低优先）；④ 每 slice 跑 fmt+clippy+make test 守，net≥0 land；⑤ 机会性 plateau-guard / 文档纠偏。

> **📍 R2512（2026-08-03）🔧✅ `document.rs` TreeWalker/NodeIterator 抽出 LANDED（run-rules §5；2034→1835 跨 2 模块；mechanical refactor 零行为变更）— R2509-R2511 §5 文件拆分脉续，本会话链第 4 个 >2000 文件收官**。承接 R2511「下轮 document.rs 2034 / text.rs 2025 小幅 overshoot 抽」。**选 document.rs**：text.rs 是单一巨型 `impl Painter`（~1950 行，须 impl 跨文件拆分 = 大工程）；document.rs 尾部有 **`TreeWalker` + `NodeIterator`**（独立 DOM 遍历迭代器类型，struct+impl 连续块 lines 1805-2004，200 行）= 完美自包含抽取单元（DomError enum 2010-2034 保持原位）。**外部依赖核（最简）**：全 codebase `TreeWalker`/`NodeIterator` **零生产引用**——仅 dom 内 tests（tests_4/5/10）用 bare 名（`TreeWalker::new(...)`），经 lib.rs `pub use traversal::*` + 测试 `use crate::*` 解析；struct 字段仅 `NodeId`，impl 方法取 `&Document` 调 doc 导航方法 → traversal.rs 仅需 `use crate::{Document, NodeId};`（皆 crate root 经既有 re-export）。**fix**（mechanical 抽取，零逻辑变更）：块整体移到 sibling 模块 `crates/dom/src/traversal.rs`（207 行）；document.rs 删块（2034→**1835 < 2000 ✓**）；lib.rs 加 `mod traversal;` + `pub use traversal::*;`（保 `zero_dom::TreeWalker` 路径 + 测试 bare 名解析）。**门禁**：scoped `cargo clippy -p zero-dom --all-targets -D warnings` 干净（含 tests，首过零修复）；**全量 `make test` 13184/0/74**（vs R2511 **13184 完全持平**，零 panic/OOM，零回归——mechanical byte-identical 实证）；**workspace `cargo clippy --workspace --all-targets -D warnings` 干净**；`cargo fmt --all --check` 干净。**意义**：document.rs 2034 §5 违规收官（2034→1835 跨 2 高内聚模块 document 主 1835 / traversal 遍历迭代器 207）；证「零外部引用的独立类型族 + sibling 模块 + lib.rs re-export」= 最简 §5 拆分（无路径保全/跨 crate 调用方簿记，比 R2511 pipeline 8+ 跨 crate 调用方更简）；**本会话链累计 4 个 >2000 文件收官**（parser.rs 3047→1959 R2509 + shorthand/mod.rs 2260→1963 R2510 + pipeline.rs 2122→1795 R2511 + document.rs 2034→1835 R2512），全 byte-identical 零回归。**残余 §5 违规**（下轮可接续）：`flexbox.rs` 2553（taffy-local **vendored fork**，倾向豁免，须 read-only 判定）；`engine.rs` 2163（**单一巨型 `impl LayoutEngine`** ~2000 行 = 须 R2509 式 impl 跨文件拆分 + `pub(super)` + 目录化，大工程）；`text.rs` 2025（engine paint，**单一巨型 `impl Painter`** ~1950 行，同 engine.rs 大工程）；borderline `lib.rs` 2014/`types.rs` 2003/`app_render.rs` 2003（差 <20，文件大小 tolerance 边缘，强拆损 cohesion 低价值）。**下轮可接续**：① engine.rs 2163 或 text.rs 2025 impl 跨文件拆分（大工程，R2509 模式，须识别 cohesive 方法组如 table/flex/text layout）；② flexbox.rs vendored 豁免 read-only 判定；③ borderline 文件评估（低优先）；④ 每 slice 跑 fmt+clippy+make test 守，net≥0 land；⑤ 机会性 plateau-guard / 文档纠偏。

> **📍 R2511（2026-08-03）🔧✅ `pipeline.rs` extract_* 资源提取族抽出 LANDED（run-rules §5；2122→1795 跨 2 模块；mechanical refactor 零行为变更）— R2509/R2510 §5 文件拆分脉续，本会话链第 3 个 >2000 文件收官**。承接 R2510「下轮 engine.rs 2163 或 pipeline.rs 2122 free-函数抽取」。**选 pipeline.rs**：engine.rs 是单一巨型 `impl LayoutEngine`（~2000 行，须 impl 跨文件拆分 = R2509 复杂变体），pipeline.rs 有 free-函数 `extract_*` 资源提取簇（R2509/R2510 已证的更简模式）。**簇核**（lines 1000-1327，328 行）= `extract_stylesheet_hrefs`/`extract_page_scripts`/`extract_img_srcs`/`srcset_first_url`/`extract_img_resources`/`extract_font_faces`/`extract_import_urls`/`extract_css_image_urls`/`extract_html_style_text` + 私有 helper（`script_type_is_javascript`/`script_is_module`/`strip_cdata`/`strip_script_cdata`）+ `PageScript` enum / `ImgResource` struct（皆簇内定义）。**自包含性核**：簇全用全限定路径（`zero_dom::parse_html`、`crate::paint::image_resource_key`）+ 函数内 local `use`（随函数走）→ **extract.rs 零模块级 import**，编译首过即通。**外部依赖核查**：`strip_cdata` 是唯一被簇外（`mod.rs::inject_print_page_dividers:947`）调用的私有 fn → `pub`（其余 3 私有 helper 簇内 only 保持私有）；8 个 `extract_*` + `srcset_first_url` 跨 crate 调用方（webview async_load 等）经 engine `pub use pipeline::*`（lib.rs:54）走 `zero_engine::*` 路径 → `pub use extract::*;` 保路径不变（含 `PageScript`/`ImgResource` 类型）。**fix**（mechanical 抽取，零逻辑变更）：`pipeline.rs` → `pipeline/` 目录（`git mv`，模块路径不变）+ 子模块 `pipeline/extract.rs`（337 行）；mod.rs 加 `mod extract; pub use extract::*;`，删簇块（2122→**1795 < 2000 ✓**）。**门禁**：scoped `cargo clippy -p zero-engine -D warnings` 干净（首过零 import 修复）；**全量 `make test` 13184/0/74**（vs R2510 **13184 完全持平**，零 panic/OOM，零回归——mechanical byte-identical 实证）；**workspace `cargo clippy --workspace --all-targets -D warnings` 干净**；`cargo fmt --all --check` 干净。**意义**：pipeline.rs 2122 §5 违规收官（2122→1795 跨 2 高内聚模块 pipeline 主 1795 / extract 资源提取 337）；证「free-函数簇 + `pub use child::*` 路径保全 + 全限定路径自包含」= 跨 crate 调用方密集的 utility 簇拆分可复现模式（比 R2510 shorthand/background 单外部调用更广——8+ 跨 crate 调用方，靠 `pub use` 一行保全）；**本会话链累计 3 个 >2000 文件收官**（parser.rs 3047→1959 R2509 + shorthand/mod.rs 2260→1963 R2510 + pipeline.rs 2122→1795 R2511），全 byte-identical 零回归。**残余 §5 违规**（下轮可接续）：`flexbox.rs` 2553（taffy-local **vendored fork**，拆分权衡 re-vendoring 复杂度，倾向豁免，须 read-only 判定）；`engine.rs` 2163（layout-engine，**单一巨型 `impl LayoutEngine`** ~2000 行 = 须 R2509 式 impl 跨文件拆分 + `pub(super)` 方法 + 目录化，较大工程）；`document.rs` 2034（dom，+34）；`text.rs` 2025（engine paint，+25）；borderline `lib.rs` 2014/`types.rs` 2003/`app_render.rs` 2003（差 <20，文件大小 tolerance 边缘，强拆损 cohesion 低价值）。**下轮可接续**：① document.rs 2034 / text.rs 2025 小幅 overshoot 抽 free-函数/impl 子模块（小工程）；② engine.rs 2163 impl 跨文件拆分（大工程，R2509 模式）；③ flexbox.rs vendored 豁免 read-only 判定；④ 每 slice 跑 fmt+clippy+make test 守，net≥0 land；⑤ 机会性 plateau-guard / 文档纠偏。

> **📍 R2510（2026-08-03）🔧✅ `shorthand/mod.rs` background 族抽出 LANDED（run-rules §5；2260→1963 跨 2 模块；mechanical refactor 零行为变更）— R2509 §5 文件拆分脉续，本会话第 2 个 >2000 文件收官**。承接 R2509「下轮可接续 shorthand/mod.rs 拆分（2260，最高价值非 vendored 源文件）」。**shorthand/mod.rs 结构核**：43 个顶层自由函数（无 impl 块）= 完美契合 color.rs/parse_misc free-函数抽取模式（R2400-R2402），无 impl 跨文件隐私复杂度。**background 族（lines 1747-2045，299 行）**= 高内聚自包含单元：`expand_background`（+ 族内私有 `bg_append`/`split_bg_position_and_size`/`classify_bg_token`/`BgSlots` struct）。**外部依赖最小**：仅 `MatchingDecl` 类型别名 + `matches_css_wide_keyword` helper（族内 1 处用），`mk` 为每函数局部闭包（随函数走）。**fix**（mechanical 抽取，零逻辑变更）：族整体移到子模块 `crates/style-system/src/shorthand/background.rs`（307 行，`use super::{MatchingDecl, matches_css_wide_keyword};`）；`expand_background` 唯一外部调用方 = `expand_one`（mod.rs:401 `"background" => expand_background(...)`）→ `pub(super) fn`（族内其余私有）；mod.rs 加 `mod background; use background::expand_background;`，删族块（2260→**1963 < 2000 ✓**）。**门禁**：scoped `cargo clippy -p zero-style-system -D warnings` 干净；**全量 `make test` 13184/0/74**（vs R2509 **13184 完全持平**，零 panic/OOM，零回归——mechanical byte-identical 实证）；**workspace `cargo clippy --workspace --all-targets -D warnings` 干净**；`cargo fmt --all --check` 干净。**意义**：shorthand/mod.rs 2260 行 §5 违规收官（2260→1963 跨 2 高内聚模块）；证 free-函数族 + `pub(super)` 单入口 + `use child::entry` import = 简写展开器按族拆分的可复现模式（比 R2509 impl 跨文件拆分更简——无 impl 隐私簿记）；**本会话累计 2 个 >2000 文件收官**（parser.rs 3047→1959 R2509 + shorthand/mod.rs 2260→1963 R2510），均 byte-identical 零回归。**残余 §5 违规**（下轮可接续）：`flexbox.rs` 2553（taffy-local **vendored fork**，拆分权衡 re-vendoring 复杂度，倾向豁免）；`engine.rs` 2163（layout-engine）；`pipeline.rs` 2122（engine，有 extract_* free 函数易抽）；`document.rs` 2034（dom）；`text.rs` 2025（engine paint）；`tests_1.rs` 2128/`tests_4.rs` 2036/`visual.rs` 2092/`tests_10.rs` 2019（**测试文件**，run-rules §5 优先级低，纯测试集合拆分价值小）；`lib.rs` 2014/`types.rs` 2003/`app_render.rs` 2003（borderline 差 <20）。**下轮可接续**：① engine.rs 2163 或 pipeline.rs 2122 free-函数抽取（源文件，高价值）；② flexbox.rs 2553 vendored 豁免判定（read-only 核 re-vendoring 影响）；③ 每 slice 跑 fmt+clippy+make test 守，net≥0 land；④ 机会性 plateau-guard / 文档纠偏。

> **📍 R2509（2026-08-03）🔧✅ `parser.rs` 拆分 LANDED（run-rules §5 文件大小控制；3047→1959 跨 3 高内聚模块；mechanical refactor 零行为变更）— 推翻 R2508 BLOCK：autonomous 推进面未穷尽（§5 文件拆分是 sanctioned 非 gated 工作上轮漏判）**。承接 R2508 BLOCK。任务指令「跳过网络、从其他收益最大事项开始、勿把需决策当阻塞」→ 复核 R2508 BLOCK 三前提：① **oracle 复核**推翻「仅 self-source」隐含假设——`tests/wpt-runner/oracle-shots/` **本地存在 640 张 chromium oracle 截图**（css-fonts 284 / css-cascade 142 / css-tables 119 / css-grid 50 / css-nesting 45），`make reftest-oracle DIR=` **离线可用**；独立实测 css-nesting（71% credible，fail 全 Shadow DOM host-nesting `attachShadow` OOS + 低 diff font-wall）+ css-cascade（49% oracle-pass，fail 全 scope-*/Shadow DOM/revert-layer/@layer 深）= **两 parse-level dir 真一致率复核确认 R2508 clean-lever 穷尽判断成立**（fail 全 host-layer/深结构/font-wall，非 clean lever）。② corpus-expansion = 网络确认仍不可用（任务指定跳过）。③ **R2508 漏判的 sanctioned 工作面 = run-rules §5 文件大小控制**：14 个 `.rs` 文件 >2000 行，最大 `crates/css-parser/src/parser.rs` = **3047 行**（全仓最大单一违规），R2400-R2402 color.rs 拆分已证此模式安全可复现——R2508 应做此项却输出 BLOCK。**fix**（mechanical refactor，零逻辑变更，R2400-R2402 模式扩展到 `impl` 跨文件拆分）：`parser.rs` → `parser/` 目录（`git mv parser.rs parser/mod.rs`，模块路径 `parser` 不变，`lib.rs pub mod parser;` 零改动）+ 两子模块：① `parser/helpers.rs`（321 行）= 底部自由辅助函数（容器查询条件 / @counter-style 描述符 / @font-face src 提取 / @page 尺寸·外边距，原 `fn`→`pub(super) fn` 等价「parser 模块私有」语义）；② `parser/at_rules.rs`（791 行）= 13 个 @规则消费方法（consume_at_rule/font_face/page/property/counter_style/keyframes/layer/import/supports/container + collect_paren_content + parse_font_face_weight/style，`impl<'a> Parser<'a>` 拆分跨文件，子模块可访问父私有字段/方法，13 方法 `pub(super)` 供父 `mod.rs` 调用）。`mod.rs` 现 **1959 行 < 2000 ✓**（3047→1959，-1088/-36%）。**关键设计**：`pub use helpers::{resolve_page_size_px, resolve_page_margin_px};` 保 `crate::parser::resolve_page_*` 测试路径不变；at-rule 块经核查**零引用** mod.rs-local 自由函数（amp_selector/desugar_selectors 等），仅依赖 Parser 方法 + ast + helpers（`use super::helpers::*`）→ 自包含可干净抽。**门禁**：scoped `cargo check` + `cargo clippy -p zero-css-parser -D warnings` 干净；**全量 `make test` 13184/0/74**（vs R2500 baseline **13184 完全持平**，零 panic/OOM，零回归——mechanical byte-identical 实证）；**workspace `cargo clippy --workspace --all-targets -D warnings` 干净**；`cargo fmt --all --check` 干净（fmt 仅 reorder at_rules.rs import 顺序，已应用）。**意义**：全仓最大文件 parser.rs 3047 行 §5 违规**收官**（跨 3 高内聚模块 parser 主 1959 / helpers 自由辅助 321 / at_rules @规则 791）；证 `impl` 跨文件拆分（目录化 + `pub(super)` 子模块 + glob import）= run-rules §5 安全可复现模式（R2400-R2402 自由函数抽取模式的 impl 变体）；**推翻 R2508 BLOCK**——autonomous 推进面不止 reftest clean lever，§5 文件大小合规是 sanctioned、低风险、非 gated、非网络的持续工作面。**残余 §5 违规**（下轮可接续，同模式）：`flexbox.rs` 2553（taffy-local **vendored fork**，拆分须权衡 re-vendoring 复杂度，可能豁免）；`shorthand/mod.rs` 2260（style-system，按属性族抽 free 函数，类 color.rs 最干净）；`engine.rs` 2163（layout-engine）；`pipeline.rs` 2122（engine，有 extract_* free 函数易抽）；`document.rs` 2034（dom，5 impl 块）；另 7 文件 2003-2128（多 tests/ 文件 + borderline）。**下轮可接续**：① shorthand/mod.rs 拆分（2260，最高价值非 vendored 源文件）；② engine.rs / pipeline.rs free-函数抽取；③ 每 slice 跑 fmt+clippy+make test 守，net≥0 land；④ 机会性 plateau-guard / 文档纠偏。

> **📍 R2508（2026-08-02）🛑 BLOCK 判定：autonomous reftest 推进面穷尽 + corpus-expansion 网络阻塞 + deep-structural user-gated — 需用户决策解锁**。承接 R2507 全 dir triage 完成。本轮验证唯一剩余 sanctioned 实质推进路径（corpus-expansion R2221 模式 = 扩未导入 dir 如 css-display/css-masking/css-break）的可行性：① **网络不可用**——`git ls-remote github.com/web-platform-tests/wpt` + `zeroweb-wpt-data` 均 timeout（EXIT 143），且**本环境无 `~/use-proxy`**（任务指定代理配置不存在），corpus 扩展需 upstream WPT fetch，**无离线替代**；② lark-cli auth 仍不可用（飞书通报未发，run-rules 不阻塞）。**综合**：(a) autonomous clean-lever surface 穷尽性实证完毕（全 30 可达 dir + reftest-driven + 4 code-reading 维 + selectors + flexbox + transforms + legacy-alias + parser-compliance 全扫）；(b) corpus-expansion = 外部依赖（网络）不可用且无替代方案；(c) deep-structural（R2174/R1043/Phase A/font-stack/ruby-layout）全 07-29 护栏 user-gated。**rendering-compat goal 的 reftest 通过率实质推进已真正无法自主继续**（marginal doc-drift 余 ambiguous 引用须 git 考古，非 goal 推进）。**输出 BLOCK**，待用户：① 恢复网络（解锁 corpus 扩展）或 ② 点名授权某 deep-structural 方向。**近会话净进展回顾**（R2499-R2507）：R2499 css-logical cascade canonical +4 真 flip、R2500 overflow-clip-margin 特性 +1（已 land 推送）；R2501-R2507 plateau-guard（axis-clip 死胡同 revert + doc-drift 5 处 + product-smoke-legacy 0 struct FAIL 回归确认 + selectors/flexbox/transforms 全 dir fresh triage）。

> **📍 R2507（2026-08-02）📋 css-transforms 深扫 triage = 无 clean lever（transform % 已实现；transform3d 出范围 2D-only；余 compound/abspos 深）— 全可达 dir fresh re-triage 完成**。承接 R2506 plateau。本轮 fresh triage 最后一个大量 fail dir css-transforms（548/841=65.2%，293 fail）。**逐簇 triage**：① **transform3d-***（matrix3d/rotate3d/rotatex-perspective/rotatey 等，大量 0.00% fail）= **ZW 2D-only**（helpers.rs:142 RotateX/RotateY → identity matrix，3D 降级丢；goal 明确「CSS 2D transform」，3D 出范围）；② **transform-percent-001..007**（0.00% vs mismatch ref）= **% 解析+消费已实现**（TranslateMixed/TranslateXMixed/YM 解析 parse_transform.rs:403-433 + 消费 helpers.rs:102-119 按 rect.size.width/height 求 %），fail 根因为 compound transform（rotate+translatex%+skewx 矩阵复合）+ abspos 交互深路径非 clean；③ transform-table/generated（transform 应用到 table/generated 特定交互深）；④ animation/*（动效帧级出范围）。**结论**：css-transforms 无 clean lever。**至此 3 大高 fail dir（selectors R2503 + flexbox R2506 + transforms 本轮）+ 全 wpt-data 可达 dir（30 dir）fresh re-triage 全完成**——**autonomous clean-lever surface 彻底、穷尽性实证完毕**（reftest-driven 全 dir + 4 code-reading 维 + selectors + legacy-alias + parser-compliance + product-smoke 回归门禁）。**零 .rs 变更（read-only triage）**。**结论（最终）**：rendering-compat goal 的自主 reftest 推进面已 100% 穷尽；剩余全部 fail 分属 user-gated 深结构（R2174 taffy border-box / R1043 vertical-mode / Phase A IFC baseline / font-stack C-dep / ruby-layout / 3D-transforms 出范围 / 动效帧级出范围 / host-layer scroll·focus·popover·JS-dynamic）。**下一实质进展的唯一路径 = 用户点名授权某深结构方向**（taffy 迁移虽 07-16 预授权但已 vendored@0.12-local 完成，余 R2174 fork-internal/converter 仍 gated）。

> **📍 R2506（2026-08-02）📋 css-flexbox 深扫 triage = 无 clean lever（全 taffy flex-math / R2174 / Phase A / R1043）— flexbox 维度实证穷尽**。承接 R2505 plateau。本轮 fresh triage 最高 fail 量布局 dir css-flexbox（337/497=67.8%，160 fail），寻 low-diff 离散 lever。**逐簇 triage**：① `flex-aspect-ratio-img-*`（1.04-1.08%，~8案）= replaced-element + aspect-ratio（R2174/R976 territory）；② **`flexbox_flex-*-shrink` 簇（1.28%，~8案）= flex-shrink 计算**——**核验 flex-basis 解析正确**（`expand_flex` shorthand/mod.rs:912 正确把 `flex: 0 1 4em` 折为 flex-basis:Length(4em) 覆盖 width，computed.rs:376 resolve px），故 shrink diff 根因 = **taffy flex-math**（非 ZW clean fix，taffy 迁移 territory）；③ `dynamic-isize-change-*`（JS dynamic defer）；④ `baseline-synthesis-*`（Phase A baseline）；⑤ `css-flexbox-row.html`/`flexbox-writing-mode-*`（**writing-mode: vertical-rl** = R1043 gated，非基础 horizontal flex）；⑥ `flexbox-column-row-gap`/`flex-container-margin`（gap/margin，taffy 深案）。**结论**：css-flexbox 无 clean lever（全 taffy/R2174/Phase A/R1043/JS-dynamic）。**至此 2 个最高 fail dir（css-selectors R2503 + css-flexbox 本轮）fresh re-triage 均确认全维度 clean-lever 穷尽**。**零 .rs 变更（read-only triage）**。**下一轮**：autonomous plateau 工作面彻底穷尽；实质进展须用户授权深结构（含 taffy 迁移裁决——css-flexbox shrink 簇 + css-ui box-sizing R2174 同脉，或为 taffy→新版升级的高价值候选，但属 user-gated multi-session）。

> **📍 R2505（2026-08-02）📋 product-smoke-legacy 回归门禁复核（post-R2499/R2500）= 0 struct FAIL，零回归**。承接 R2504 plateau。R2499（logical sizing cascade canonical 化，动 canonical_property_name）+ R2500（overflow-clip-margin，动共享 clip rect `overflow_clip_rect`）两改均触及 cascade / clip 高风险面，run-rules 强调 product-smoke 防 hidden regression（R541 min-size:auto welcome +7.65pp 藏 14 轮先例）。本轮 `make product-smoke-legacy`（42 fixture vs chrome-127 oracle）= **struct-check failures: 0**（全 struct PASS，含 37-form-controls 3.85%）；avg diff 2.36%；worst 19-testpage 17.23% / 20-mixed 11.49% **与快照 byte-identical**（零像素回归）。**结论**：R2499/R2500 对 legacy HTML（HTML 3.2/4 + CSS1/2，UA 样式/表单/legacy 元素）**结构 + 像素双零回归**——cascade canonical 仅影响「同元素 logical+physical 共存」稀有情形、clip rect 默认 byte-identical 的设计实证成立。**未发现新离散 lever**（diff 全 font-wall plateau，struct 全 PASS）。**意义**：due-diligence 回归门禁通过；近期两改可信赖无 legacy 退化。**下一轮**：autonomous plateau 工作面（clean-lever + doc-drift）双穷尽；实质进展须用户授权深结构。

> **📍 R2504（2026-08-02）✅ doc-drift 续修 2 处 stale `.rs:NNN` 注释引用 LANDED（R2502 续，纯注释零行为变更）— doc-drift surface 亦近穷尽**。承接 R2503 plateau。本轮深核 R2502 余 ~15 处 uncertain 引用（逐符号核对 cited line 内容 vs 注释 intent）：**2 处确认 stale 修复**：① `webview.rs:154` `reftest.rs:553→568`（553=set_image_ratios 非 font-metric kill-switch；另 2 文件 text_metrics.rs:345/font loader.rs:432 已用 568，本次对齐）；② `inline_finalization.rs:187` `text.rs:744→737`（744=.with_vertical 非空白保真；737=.with_preserve_whitespace paint-path 空白保真）。**其余 ~13 处核对**：correct（`collect_items.rs:327`→tree.rs:597 eff_ratio / `image_cache.rs:596`→tree.rs:439 aspect_ratio / `multicol.rs:81`→927 position_multicol_children 等命中）或 **ambiguous 不盲改**（`engine.rs:371` tree.rs:358+428 双 citation 均漂移须全重写流程；`painter/text.rs:801`→853 container_is_ahem 模式 target 不明 802 vs 907；`painter/text.rs:1615`→1464 ellipsis cutoff target 不明——须 git blame 定原 commit，过度成本）。**门禁**：comment-only → `cargo build -p zero-webview -p zero-layout-engine` 通过；未跑全量 make test（零 .rs 行为变更）。**意义**：doc-drift surface（R2482 修 9 + R2502 修 3 + R2504 修 2 = 14 处）亦近穷尽，余皆 correct 或 ambiguous。**至此 autonomous plateau 工作面（clean-lever + doc-drift）双穷尽**——下一实质进展须用户授权深结构（Phase A IFC / R1043 vertical / font-stack C-dep / R2174 border-box / ruby-layout）或新维度灵感。

> **📍 R2503（2026-08-02）📋 plateau-guard：css/selectors 深扫 = 无 clean lever（全离散伪类已实现，fail 全 host-layer/JS/Shadow/OOS）— selectors 维度实证穷尽**。承接 R2502 plateau 续。本轮转 selectors 维度（187/224=83.5%，37 fail，R2221/R2223 曾修 specificity/nth-of-invalid）creative probe。**逐簇 triage（全已实现或 OOS）**：① `:focus-within-*`（10+案 4.90-5.02%）= **已 parse**（parser.rs:867）但需 focus 状态（reftest 靠 JS/autofocus 设焦，script-sandbox focus 路径 = host-layer defer）；② `:dir()` `dir-style-01a/01b` = **已 parse+match**（ast.rs:380 / matcher/mod.rs:322 matches_dir），残余为 `lang-pseudo-class-across-shadow-boundaries`（Shadow DOM OOS）+ auto-direction-change（JS dynamic）；③ `:nth-child of S`（`nth-child-of-universal-selector` 1.06%）= **已 parse**（NthChildOf selector.rs:55/213/233），残余 edge 非 clean；④ `featureless-*`（@scope/shadow OOS）；⑤ `popover-open-with-has`（popover API host-layer）；⑥ `invalidation/*` + `remove-hovered-element`（JS dynamic relayout defer）；⑦ `selection-image-001/002`（::selection + image，0% diff mismatch edge）。**结论**：selectors 无 clean lever（全离散伪类已实现，fail 全 host-layer/JS/Shadow/popover/dynamic — 均非自主可修）。**飞书通知**：尝试 lark-cli 通报 plateau + 待授权深结构 5 选 1，但本环境 lark-cli auth 不可用（SELF 空），通知未发（run-rules：通知仅为告知不阻塞）。**零 .rs 变更（read-only triage）**。**意义**：selectors 维度（css 最后一未深扫大类）clean-lever 实证穷尽；至此 **css 全可达维度 clean-lever surface 彻底穷尽**（reftest-driven + 4 code-reading 维 + selectors + legacy-alias + parser-compliance 全扫完）。**下一轮**：clean-lever 须待新维度灵感或用户授权深结构（Phase A IFC / R1043 vertical / font-stack C-dep / R2174 border-box / ruby-layout）；自主仅余 marginal doc-drift（R2502 余 21 uncertain 引用逐符号深核，低优先）。

> **📍 R2502（2026-08-02）✅ doc-drift 续修 3 处 stale `.rs:NNN` 注释引用 LANDED（R2482 模式续，纯注释零行为变更）**。承接 R2501 plateau（可达 clean-lever surface 穷尽，转 sanctioned 文档纠偏）。本轮定向核对剩余 24 处 `.rs:NNN` 注释引用（R2482 已修高流量 9 处），**3 处确认 stale 修复**：① `inline_finalization.rs:1050` `tree.rs:1540→1571`（`multi_inline_block_skip` 定义漂移 +31）；② `inline_finalization.rs:112` `text.rs:560→581`（`resolve_text_align_last` paint-path 调用漂移 +21）；③ `engine/postprocess.rs:844` `engine.rs:1888→1909`（`is_block_level` 定义漂移 +21，R2482 曾修到 1888，后续又漂移）。**其余 21 处核对正确**（`intrinsic_sizing.rs:29`/`painter/border.rs:590`/`helpers.rs:635`/`collect_items.rs:439`/`multicol.rs:122` 等命中，或 uncertain 须逐符号深核不盲改）。**门禁**：comment-only → `cargo build -p zero-layout-engine` 通过；未跑全量 make test（零 .rs 行为变更）。**意义**：续 R2202/R2482 doc-drift 治理；3 处 21-31 行漂移引用修正（防后续 agent 误导读错位置）；零风险 landable。**下一轮可接续**：① 余 21 处 uncertain 引用逐符号深核（marginal，低优先）；② clean-lever 须待新维度灵感（reftest-driven + 四面均穷尽）；③ 待用户点名深结构（Phase A / R1043 / font-stack C-dep / R2174 / ruby-layout）。

> **📍 R2501（2026-08-02）📋 plateau-guard：reftest-driven vein 续扫 3 dir 全墙 + clip-002 axis-specific clip 死胡同 revert（net −1）— 可达未扫 dir clean-lever surface 实证穷尽**。承接 R2500「续扫未深扫 dir」。本轮系统 triage 剩余可达未扫 dir，**结论 = 全部撞墙**：① **css-writing-modes 631/788=80.1%**（首测入档）——157 fail **几乎全 `vrl`/`vlr`/`slr`/`srl`**（vertical writing-mode = R1043 user-gated）；11 非 vertical fail 全 `inline-block/table-alignment-*`（6.94-16.12% = baseline/font-wall 深，非 clean lever）；② **css-ruby 107/141=75.9%**（首测入档）——34 fail 全 ruby 布局特化（ruby-overhang-spaces / ruby-intrinsic-isize / ruby-annotation / ruby-box-generation / ruby-layout-internal-boxes = ruby-layout RFC 已 defer territory）；③ **css-values 0 reftest**（parsing-only，无图测，非 reftest 来源）；④ **css-display 未导入**（wpt-data 无该 dir）。**clip-002..005 axis-specific clip 死胡同**（R2500 defer 项本轮攻）：实施 `overflow_clip_rect` 扩展——clip+visible 时 visible 轴按 CSS Overflow 3 §2 **不裁剪**（clip 不像 hidden/scroll 把对侧 visible 算成 auto）→ 用 HUGE 有限范围（1e7）实现 no-clip。**A/B net −1**：clip-002..005 + overflow-clip-[xy]-visible-*-svg **全无变化**（diff byte-identical，证这些 fail 根因非 axis-clip 而是**既有布局差异**，clip-002 diff 3.65%=17520px > 100×100 inner=10000px）；**回归 1**：`line-clamp/line-clamp-019.html ✓→✗`（HUGE 范围与 line-clamp ellipsis 裁剪路径交互致回归）。**故 net −1，全 revert 回 R2500 状态**（overflow_clip_rect 恢复，零代码残留，tree clean）。**门禁**：未提交（net 负不 land）。**意义（plateau-guard 价值）**：① 实证可达未扫 dir clean-lever surface **彻底穷尽**——css-writing-modes/css-ruby/css-values/css-display 均非 clean lever 来源（vertical/baseline/ruby-layout/未导入）；② clip-002 axis-clip 死胡同钉案（net −1 + 布局根因 + line-clamp 回归交互 → 勿再以 axis-clip 为 lever，须先拆单 rect 为 per-axis 双 rect + 解决 line-clamp ellipsis 交互，深）；③ 新增 css-writing-modes + css-ruby 数据点入档。**结论**：reftest-driven vein 在可达 dir 已穷尽；剩余 fail 全属 user-gated 深结构（R1043 vertical / Phase A IFC baseline / ruby-layout RFC / R2174 border-box / host-layer scroll）或既有布局差异（flex/abspos）。**下一轮可接续**：① 文档 vs 代码行号漂移纠偏（R2482 模式续扫，纯注释零风险）；② 待用户点名深结构方向（Phase A / R1043 / font-stack C-dep）；③ clean-lever 须待新维度灵感（current 四面+reftest-driven 均穷尽）。

> **📍 R2500（2026-08-02）✅ CSS overflow-clip-margin 特性 LANDED（CSS Overflow 3 §3，4-crate plumbing + paint 裁剪扩展；overflow-clip-margin-010 fail→pass，零回归；cluster 余员 fail 全布局原因非特性缺口）— reftest-driven vein 续扫 css-overflow**。承接 R2499「续扫未深扫 dir」。**css-overflow scoped self-source 首测**（`make reftest-upstream FILTER=css-overflow`，518 case，原不在快照）= 273/518=52.7%。**最大 fail 簇 = `overflow-clip-margin-*`（16+ case）+ `clip-002..005`**。**根因**：`overflow-clip-margin`（CSS Overflow 3 §3，`<visual-box> || <length>`，仅对 `overflow:clip` 生效——决定裁剪边基准盒 content/padding/border ± length 向外扩展）ZW **完全未实现**（0 grep）。`overflow:clip` 本身已 wired（OverflowValue::Clip → OverflowClip::Clip，painter `!=Visible` 裁剪），但裁剪恒到 padding-box 无 margin 扩展。**fix（4-crate plumbing）**：① css-parser `OverflowClipMarginBox`（Content/Padding/Border）+ `OverflowClipMarginValue{box_kind,length:LengthValue}` + `parse_overflow_clip_margin`（`||` 文法任意顺序/各至多一次/缺省 padding-box+0）；② style-system ComputedStyle.overflow_clip_margin + registry（known_properties + initial_value PaddingBox/0 + PropertyValue::OverflowClipMargin 变体）+ apply + default + inherit/initial；③ layout-engine LayoutBox 加 `overflow_clip_margin_box` + `overflow_clip_margin_length:f32`（engine.rs build 期按 font_size resolve em→px）；④ painter `overflow_clip_rect(box)` helper——clip 轴按基准盒±length 扩展，hidden/scroll/visible 轴恒 padding-box，**默认（PaddingBox/0）byte-identical 零回归**。**A/B**：css-overflow 273→274（+1：overflow-clip-margin-010 fail→pass **实证 end-to-end 正确**）；003/004/006/008/009 仍 PASS（零回归）；css-multicol 264→265（+1，0 回归，证共享 clip rect 改动安全）；**cluster 余员（001/013-022 等）仍 fail**：001 diff 4.50%=21600px **远大于** clip-margin 环 ~4400px → 证 fail 根因是**既有 flex/定位布局差异**（非 clip-margin 特性缺口，特性本身已实证正确 via 010 flip）。TDD 1 测（`test_parse_overflow_clip_margin` 12 assert 覆盖 length/box/`||`顺序/em/非法）。**门禁**：make test **13184/0/74**（+1 测；初版 `test_initial_value_completeness` fail 因 known_properties 加项未配 initial_value → 补 PropertyValue 变体 + initial_value entry 修复）；fmt/clippy 干净。**意义**：overflow-clip-margin 特性 spec-complete 落地（真页面 `overflow:clip`+margin 现正确扩展裁剪边）；4-crate plumbing 全链路实证；零回归 landable；master 快照新增 css-overflow 数据点。**局限**：cluster 多数 fail 为既有布局差异（flex/abspos），非本特性范围；单 rect 裁剪对 mixed-axis（x clip + y visible）仍裁 y（pre-existing clip-002 同脉，host-layer/IFC territory，defer）。**下一轮可接续**：① clip-002..005 axis-specific clip（per-axis 裁剪，须拆单 rect 为 x/y 双 rect，深）；② 续扫 css-ruby / css-writing-modes horizontal 子集 / css-values；③ 待用户点名深结构。

> **📍 R2499（2026-08-02）✅ CSS Logical Properties sizing 族 cascade canonical 化 LANDED（css-logical cascading-001 fail→pass + css-box +3 真 flip，零回归）— reftest-driven vein pivot 到未扫 dir css-logical**。承接 plateau（clean-lever 四面 R2488 穷尽 + font-matching 族 R2491-R2498 完结）。「**自主 clean-lever surface 穷尽**」结论**再受约束**：限定于**已扫维度**；未深扫 dir（css-logical 全 6 case 原不在快照）仍出离散 lever。**css-logical scoped self-source 扫描**（`make reftest-upstream FILTER=css-logical`，6 case，原不在快照）= 2/6=33.3%。**fail 簇 triage**：① `cascading-001`（2.81%）= **horizontal-tb** 真离散 lever；② `logical-values-float-clear-2/4/reftest`（1.4-2.4%）= **`writing-mode: vertical-rl`**（float/clear logical 值 horizontal 已 PASS=1/3，vertical 失败全部 R1043 vertical-mode-gated，defer）。**根因（cascading-001）**：`canonical_property_name`（cascade.rs:234）仅规范化 `word-wrap→overflow-wrap`，**未把 logical 尺寸属性（`inline-size`/`block-size`/`min|max-inline|block-size`）规范化到物理等价（width/height）**→ logical 与 physical 各占独立 canonical 槽位 → 都进 result HashMap → apply 时双双应用 → **specificity 丢失**（终值由 apply 顺序/HashMap 迭代序决定，非 spec 的 cascade 共解）。CSS Logical Props：logical+physical 共同声明的 computed value 须由 cascade 按 specificity 决定。**driving**：`<div class="horizontal-logical" id="vertical-physical">`（.class inline-size:100px vs #id width:10px）→ #id 须胜。**fix**（cascade.rs canonical_property_name 加 6 映射 arm：inline-size→width / block-size→height / min-inline-size→min-width / min-block-size→min-height / max-inline-size→max-width / max-block-size→max-height）。apply.rs inline-size/block-size arm **保留**（直接 apply 调用者 apply_coverage 测试仍用，非死代码）；垂直模式轴交换由 converter `swap_writing_mode_axes`（apply 后 width↔height 互换）处理，**不受 canonical 化影响**（logical 与 physical 均已 fold 进 style.width/height，converter 行为不变）。**A/B 净正**：① css-logical cascading-001 **fail→pass（2.81%→0.00%）**；② **css-box 38/67→41/67（+3 真 flip，56.7%→61.2%，0 回归）**——同 logical+physical cascade 冲突根因，canonical 化一并修复；③ **css-sizing 230/562=40.9% byte-identical（0 回归 562 case）**——sizing-heavy dir 零 delta 证改动仅影响「同元素 logical+physical 共存」稀有情形。TDD 2 测（`test_canonical_logical_size_aliases` + `test_cascade_logical_and_physical_size_share_slot`，类 `test_cascade_word_wrap_and_overflow_wrap_share_slot`）。**门禁**：make test **13183/0/74**（+2 测，零回归）；fmt/clippy 干净。**意义**：reftest-driven vein 在未扫 dir 仍可出 +4 真 flip（css-logical +1 / css-box +3）零回归净正；CSS Logical Properties sizing 族 cascade 正确化；master 快照新增 css-logical 数据点 + css-box 升档。**局限**：仅 sizing 族（margin-inline/padding-block/inset-block/border-inline ZW 未实现，不在范围）；css-logical 余 3 fail 全 vertical-mode R1043-gated。**下一轮可接续**：① 续扫未深扫 dir（css-values/css-overflow/css-ruby/css-writing-modes 非 vertical 子集）找离散 lever；② css-logical vertical float-clear（R1043 territory，等点名）；③ 待用户点名深结构。

> **📍 R2498（2026-08-02）🔧✅ synthetic italic slice 2-full LANDED（paint_text render_fragment 宏 per-fragment wiring + kill-switch；body 文本 italic 现合成 shear；A/B 全 fixture net 0；零回归）— 承接 R2497 slice 2-full**。承接 R2497「paint_text 宏 per-fragment wiring 待续」。**改动**：① `default_font_id` 解构 `(default_font_id, default_resolved_italic)` + 算 `container_want_italic`；② `text_node_font_ids` 构建旁开 parallel `text_node_font_italic: HashMap<NodeId,bool>`（每节点 resolved face 是否 italic）；③ **render_fragment 宏**（4 个 GlyphPrimitive literal，body 文本主路径）按 fragment 算 `frag_synthetic_italic`：Ahem 片段恒 false（测试字体保直立）/ 非 Ahem `resolved_italic = text_node_font_italic[frag_nid] ?? default_resolved_italic`，`want_it = owner_style_opt.font_style italic ?? container_want_italic`，`synthetic = want_it && !resolved_italic`（避 double-shear 真 italic face）；4 literal 改 `synthetic_italic: frag_synthetic_italic`。④ **kill-switch** `ZW_SYNTHETIC_ITALIC=0`（cpu/mod.rs blit_glyph_bitmap 入口 env 守卫，默认开，关则强制不 shear 回退 upright）。**A/B**（env toggle 开/关，无需 rebuild）：① welcome **17.03%（81737px）双侧 byte-identical**（welcome.html **无任何 italic** 用法 → 不触发）；② make test 13183/0（Ahem gated + 无 italic system 文本单测）；③ **fixtures/WPT 全 net 0**：welcome 无 italic；morning-work italic 用 JetBrainsMono @font-face（R2493 resolved italic → synthetic=false 不触发）；WPT reftest 全用 Ahem/控制字体无 italic system 文本。**故 synthetic italic 在现有测试面 net 0**（machinery proven + 正确 wired，但无 italic system 文本输入触发）。**wiring 正确性 trace**：`<p font-style:italic>` system 字体（无 italic @font-face）→ default_resolved_italic=false → frag_synthetic_italic=true（want_it=true&&!false）→ blit shear 生效（真页面 `<em>`/`<i>` system 文本将获正确 shear，对齐 chromium）。**未 wire（边缘，defer）**：paint_list_marker / paint_input_value / multicol 分支直接 literal（list marker + input value italic 极罕见；可后补同 paint_content 模式）。**门禁**：make test **13183/0/74**（零回归）；fmt/clippy 干净；product-smoke welcome byte-identical 全 struct PASS。**意义**：synthetic italic slice 2-full 完成——paint_text 宏 body 文本 per-fragment wiring + kill-switch；machinery 全链路 LIVE（paint_content R2497 + body 宏 R2498 + blit shear R2497）；R2496 RFC 三 slice 全兑现；零回归 landable（fixtures/WPT 无 italic system 文本 → net 0，真页面 value）。**局限**：paint-only（不 reflow，同 chromium）；oblique angle 忽略（按 ~14°）；GPU passthrough（TODO，reftest/smoke 走 CPU）；paint_list_marker/input 未 wire（边缘）。**下一轮可接续**：① wire paint_list_marker/paint_input_value（边缘补全，同 paint_content 模式）；② font-weight §5.2 精细化 RFC（R2494）；③ 待用户点名深结构（Phase A IFC / srcset-full / font-stack C-dep）。

> **📍 R2497（2026-08-02）🔧✅ synthetic italic slice 1+1.5+3 + slice 2-partial LANDED（infrastructure + paint_content wiring + blit shear + 单测；paint_text 宏路径 slice 2-full 待续；零回归）— 承接 R2496 RFC 实施**。**slice 1**：`GlyphPrimitive + synthetic_italic: bool`（primitive/mod.rs）+ 全 codebase **59 构造 site** 补默认 false（render-foundation tests 12 / canvas 2 / engine 48 跨 text/text_list/effects/controls/helpers+tests / browser 5；cargo E0063 引导 + sed `rotation:`/`rotation,` + 手修 GlyphDraw 误匹配 + .map 闭包 `.0` 位）。**slice 1.5**：`resolve_font_id` 返回 `(FontId, resolved_italic: bool)`（命中后缀含 "italic" = resolved face 本就 italic，避 double-shear）；11 caller 取 `.0`。**slice 3**：`blit_glyph_bitmap` 加 `synthetic_italic` 参 + 非旋转分支按行 `shear_dx=(row-height/2)*0.249(tan14°)` 水平偏移（锚 height/2 对称）。**slice 2-partial**：`paint_content` 解构 `(default_font_id, resolved_italic)` + 算 `synthetic_italic=matches!(font_style,Italic|Oblique)&&!resolved_italic` + 设 glyph（pseudo-content italic 现合成 shear，machinery LIVE）。**单测** `test_glyph_synthetic_italic_shear`：10 高位图 synthetic_italic=true 顶端左移(px=9)/锚中(px=10)/底端右移(px=11)，非斜体底端 px=10 不偏 → shear 算法实证。**未完 slice 2-full**：paint_text `render_fragment` 宏 per-fragment（frag_font_id 各异）须按 fragment 算 resolved_italic + 透传——宏 per-fragment 非单次 resolve，须平行跟踪或重算，**复杂 defer 下轮**。故现仅 paint_content + blit machinery LIVE，body 文本 italic 仍 upright。**门禁**：make test **13183/0/74**（+1 blit 单测，零回归）；product-smoke welcome **17.03%（81737px）byte-identical** + 全 struct PASS；clippy/fmt 干净。**意义**：synthetic italic 机制建成 + 实证（blit 单测证顶端左移/底端右移/锚中）；paint_content LIVE；infrastructure 为 slice 2-full 铺路；零回归 landable（fixtures 不用 italic pseudo-content → net 0）。**下一轮可接续**：① **slice 2-full：paint_text `render_fragment` 宏 per-fragment synthetic_italic wiring** → body 文本 italic 合成 shear + A/B（welcome/morning/wintertc 自抓 oracle net≥0）+ kill-switch `ZW_SYNTHETIC_ITALIC`；② wire paint_list_marker/paint_input_value 简单 path；③ 待用户点名深结构。

> **📍 R2496（2026-08-02）📋 synthetic italic RFC 落地（设计就绪，待实施）→ [`synthetic-italic-rfc-2026-08-02.md`](synthetic-italic-rfc-2026-08-02.md）；零代码**。承接 R2495「synthetic italic = rasterizer 深，须 RFC」。**RFC 内容**：① 问题（系统字体 `font-style:italic` 现渲染 upright，chromium 做 ~14° synthetic shear，~21 处 fixture italic 文本受影响）；② 方案 A（采纳）：`GlyphPrimitive + synthetic_italic: bool` 字段（59 构造 site 机械补默认 false）+ `resolve_font_id` 返回 `(FontId, resolved_italic)`（resolved_italic = 命中 `:700:italic`/`:italic` 后缀，避对真 italic face double-shear）+ caller 算 `synthetic_italic = want_italic && !resolved_italic` + CPU `blit_glyph_bitmap` 加 shear 分支（`ITALIC_SKEW=0.25`，锚 height/2，每 row 水平偏移）+ GPU passthrough TODO；方案 B（平行 bitmask）备选但同步风险更高；③ 3-slice 计划（slice 1 机械字段+签名零行为 / slice 2 blit shear + 信号接通 / slice 3 A/B + kill-switch `ZW_SYNTHETIC_ITALIC` + 锚点角度微调）；④ A/B 三态门禁（make test + product-smoke welcome/morning/wintertc 自抓 oracle + css-fonts italic dir，net≥0 land / net 负回退）；⑤ 风险缓解（shear 角度/锚点 A/B 可调、59-site cargo 引导、GPU passthrough、kill-switch）。**关键设计点**：paint-only shear（不改 layout/advance，同 chromium synthetic italic 不 reflow）；resolved_italic 返回避 double-shear。**门禁**：零 `.rs` 变更（设计文档），HEAD=R2495 make test 13182/0 + product-smoke welcome byte-identical 已验。**意义**：synthetic italic 设计就绪（方案钉案 + blit 算法 + 3-slice + kill-switch + A/B）——下轮可直接按 RFC 实施；证 R2495 去风险（GlyphPrimitive 无 skew）经设计可解（方案 A 字段 + resolve 返回 resolved_italic）；paint-only scope 限风险（不 reflow）。**下一轮可接续**：① **实施 synthetic italic per RFC**（slice 1 机械字段+签名 → slice 2 blit shear → slice 3 A/B + kill-switch，net≥0 land / 负回退）；② font-weight §5.2 RFC（R2494 已分析）；③ 待用户点名深结构（Phase A IFC / srcset-full / R1043 / taffy R2174 / font-stack C-dep）；④ doc-drift 续。
