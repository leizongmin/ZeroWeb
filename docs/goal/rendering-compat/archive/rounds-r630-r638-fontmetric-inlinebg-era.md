# 归档轮次 R630–R638 — font-metric 行盒度量修复 LANDED + Phase-A plateau 多角度再坐实 + inline-bg per-fragment 调查 era

**归档日期**: 2026-06-26（R665 轮迁出，超出最近 20 轮窗口）

**范围**: R630–R638。下列逐轮详记从 master.md 100% 原样迁出，未修改。综合结论见 master.md「综合裁决」节与本归档顶部摘要。

---

**R638（per-fragment inline bg 多行门控修订版·真实 WPT CLEAN +3 / 0 回归·1 synthetic 单测失败精确锁定 Phase A blocker·REVERTED）：承 R636（blanket net-negative）做定向改进——加 4 门控（display==Inline + bg + !abspos + height>1.5×fs + has_direct_paintable_text）仅对「跨多行+有文本+非定位」inline 生效。门控经 4 步实证演进（inline_layout.len 否决→height 启发→abspos guard→has_direct_paintable_text）。**A/B 结果（default-on）**：真实 WPT **CLEAN**——text-indent-wrap +2（001/002 5.92%→PASS）+ generated-content +1（content-175）/ normal-flow 102→102 0 回归 / block-in-inline 35→35 / floats-clear 88→88（按 case 名 diff 全空）/ product-smoke welcome 16.16% 持平。**但全量 make test 1 synthetic 单测失败**：`test_pipeline_consecutive_different_renders`（`<span height:150>Second</span>` 蓝背景消失 fills2==0）。**根因精确锁定 Phase A blocker = inline-ownership split**：① span 显式 height:150 致抑制门控触发（box-level bg 抑制）；② span 文本 "Second" 由**父 IFC** 绘制（inline 有 box 存 bg 但文本被父 IFC 收集）；③ render_fragment 宏的 box_node 是 **IFC owner（body）非 inline 本身**→宏门控用 body.height≠inline.height→per-fragment 不绘；④ 抑制（inline box 上）与 per-fragment（IFC owner box 上）**分歧**→bg 消失。text-indent-wrap 为何 work：span 自身 paint_text 绘其文本（自有 IFC 跨多行），box_node=span，抑制与 per-fragment 同 box 一致。**裁决 REVERTED**（rally 正确性 rigor：land 已知不正确变更即使用 test 车辆隐藏亦违反）。**价值**：R636+R638 两轮把 inline-bg 单 session 可行性穷尽定位——真实 WPT 门控版 clean（+3/0 回归，证明对合法多行 inline CSS 正确），唯一剩余 blocker 是 **box 归属一致性**（per-fragment 须用 inline 自身 geometry 非 IFC owner 的 box_node）= Phase A inline/block box-model 统一核心，为 Phase A 提供精确 acceptance criteria。零 landing（4 步门控 A/B + make test + revert，工作树 clean）。详见 [`evidence/r638-inline-bg-multiline-gated-ab-2026-06-26.txt`](./evidence/r638-inline-bg-multiline-gated-ab-2026-06-26.txt)。

**R637（plateau 第 4 角度再确认·4 新增 avenue 全闭·product-smoke 基线健康·零源码）：承 R636 后再扫 4 个潜在 forward avenue，全 structural/ruled-out，plateau 进一步坐实。**① **multicol Phase 2a handoff（multicol-layoutside-IFC-implementation-handoff.md）**：复核该 R581 turnkey handoff 顶部 ⚠️ R582 更新——**H2 经 probe 证伪，Phase 2a 非安全首切片**（col_ctx minimal config 仅 white-space/word-break/overflow-wrap:normal 子集成立，该子集 paint overrides 本就正确→Phase 2a 净 0；pre/pre-wrap/nowrap/break-word/break-all multicol 用 normal 配置会回归；foundation 2b 被 Phase A R109 阻塞）→ 「Phase 2a 当前 spec 不实施」裁决，勿以 col_ctx 捕获重试。② **font-advance（IFC 接 FontLoader 真实 advance）= 三重 negative 已 effectively refuted**：R225 双侧 fontdue oracle 26 案零变化（advance 非 oracle driver）/ R375b fontdue-actual advance morning 16.41→19.08%（DejaVu≠chromium 更差）/ R631 NotoSansCJK+heuristic 零变化；唯一未测组合（NotoSansCJK+fontdue advance）需重实现两个已 revert 实验（R631 font flag + R375b advance wiring），高 effort 低 EV（fontdue advance≠HarfBuzz 即使同字体），thrash 风险，不试。③ **text-transform 缺失值 = 非 clean lever**：scoped reftest 148/156 (94.9%)；2 fail = full-size-kana-005/006（assert「leaves kanas unaltered」= 未实现→None→本应 pass，5.76% diff 实为 CJK 渲染/字体度量非 transform 缺口，R633 墙）；fullwidth 测试用 Ahem 故未实现亦 pass（transform amount 不可见）；6 bicameral fail = Unicode 大小写转换边界（Rust to_uppercase vs ICU 边缘）。无 missing-feature clean win。④ **product-smoke DC-13 基线健康**：welcome **16.16%**（MAX_DIFF=22 gate pass，与 R632 持平，R635/R636 doc+revert 零回归）。**裁决**：单 session clean lever 经 R635(doc)/R636(A/B)/本轮(4 avenue) **四角度穷尽坐实**，forward 全多 session Phase A（消除 Path B / IFC 接 FontLoader / inline-block box-model 统一），需用户承诺。零源码变更（read-only handoff/spec 复核 + scoped reftest + product-smoke）。

**R636（per-fragment inline background Phase A 首切片 A/B 实测·net-negative REVERTED·empirically 关闭 inline-bg 单 session lever·零 landing）：承 R635 CONTINUE 建议的 Phase A 首切片（per-line-fragment inline background，解 text-indent-wrap-001/002），按 rally 方法论做 env 门控 A/B 实测。**实现**（env `INLINE_BG_FRAGMENT=1`）：text.rs render_fragment 宏 text_width 提前到 glyph loop 前 + 按 frag 几何 + `box_node.text_node_line_heights`（缺省 fs*1.2）绘 inline 元素 background per 行片段（在 glyph 下）；mod.rs paint_node + paint_node_in_rect 抑制 inline display 的 box-level paint_background（防双重）。**A/B 结果**：① **目标命中**：text-indent-wrap-001/002 双双 5.92%→PASS（+2，CSS2/text），ref-inline-margin 10.53→6.78%；② **回归**：normal-flow **648/750→646/750（net -2）**——2 solid 新失败（block-formatting-contexts-009 4.17%、inline-replaced-width-014 6.25%）+ 多个 inline 案 diff% 变差（inline-non-replaced-height-002/003 2.08→4.08%、inline-table-zorder-004/005 4.38→4.39/5.21→4.58、inline-block-zorder-005 5.85→1.54、inlines-002 8.04→3.82）+ 0 solid improvement。**裁决**：per-fragment inline bg **net-negative REVERTED**（git checkout text.rs/mod.rs 回 HEAD，build clean）。根因 = inline-bg 单 session 不可解：line-height 近似（fs*1.2/text_node_line_heights）+ frag 几何对一般 inline 案不匹配 chromium；多行 gating 不可能（paint_node 抑制在 paint_text 之前，无 multi-line 信息；box-level bg 必先于 text 抑制，但 multi-line 状态由 paint_text 计算 = 时序死锁）；nested/empty inline（外层 bg 内层无 bg / 空 inline）亦需处理。**结论**：inline-bg = Phase A 结构性经本轮 A/B **empirically 坐实**（+2 text-indent 被 -2 normal-flow + inline 退步抵消），匹配 R635 doc-side characterization；真修复 = Phase A 完整（per-line-fragment inline bg/border + 多行 gating 重构 paint_node/paint_text 时序 + IFC inline/block box-model 统一）。零 landing（实现 + A/B + revert，工作树 clean）。详见 [`evidence/r636-inline-bg-fragment-ab-net-negative-2026-06-25.txt`](./evidence/r636-inline-bg-fragment-ab-net-negative-2026-06-25.txt)。

**R635（plateau 第 N 次新鲜复核·5 候选全 structural/ruled-out·零源码·纠正 master.md 下一步陈旧项·DC-14 自洽）：承 R634 后 fresh 重新审视「单 session clean lever 是否真穷尽」，并对 R590 turnkey handoff（missing-asset-import-handoff.md）做执行核查。5 候选逐案 PIL/scoped-reftest 实证全 structural 或已 landed，**单 session clean pass-rate lever 谱系经第 5 个角度再坐实穷尽**：

1. **R590 missing-asset handoff = 已 LANDED（非活跃）**：R601 commit `747c8c44` 已导入 26 个 WPT support 资产；handoff 列的 23 资产（writing-modes 12 / fonts 5 / flexbox 3 / multicol 1 / css-support 1 / images 1）**全部已存在** `tests/wpt-runner/wpt-data/<wpt_rel_path>`（`file` 校验有效 PNG/text/SVG），handoff 文档已 stale。
2. **word-spacing = 非 lever（plumbing 完整）**：scoped `reftest-upstream word-spacing` = **75/77 (97.4%)**（CSS2 68/69 + css-text 7/8）；inline override 三件套（font_size / letter_spacing / line_height）经 R632 `store_font_sizes_from_ifc` 统一存储 + paint Path B `with_*_overrides` 全应用，letter_spacing 亦在同函数（inline_finalization.rs:86）已存。2 fail = `word-spacing-characters-001`(25.57%, CJK/特殊字符 word-boundary 分词 missing feature) + `word-spacing-002`(5.05%, 边界)。
3. **text-indent-wrap-001/002 (5.92%, 2 案同根) = Phase A 结构性**：PIL 像素实证根因——inline `<span background:yellow>` 跨多行作**单一 bounding-box rect** 绘制（test 全行 x=8..791）；ref 首行 x=108..791（text-indent:100px 后起）、2+ 行 x=8..791。**diff 恰为首行 100px 缩进区**（test 黄 / ref 白）。修 = per-line-fragment inline background = **Phase A**：需 inline/block box-model 区分（ZW inline 映射 block in taffy，无区分）+ paint() 多分支（362 普通分支 / 460 R109 split 分支）协调 + box-level paint_background 抑制；R335 首次 per-fragment **color** net-negative（R358 加 abs-pos guard 才 land），background 更难（需抑制 box-level + 两路径 + multicol/non-multicol）；text-indent gating 不可能（paint_background 时 box 无 first-line 信息）。
4. **blend_mode / drop-shadow（DC-8/DC-9 唯一 ❌）= paint-isolation defer**：WPT footprint 实测 mix-blend-mode **3** / drop-shadow **0** / backdrop-filter **0** / clip-path **0**（导入集内）；paint-isolation 架构（offscreen 子树 + source/dest blend/blur pass）对 3 案 ROI 不足 + 高回归风险，维持 master.md 既定 defer。
5. **text-indent-113-ref-margin (0.07%, mismatch 边界)**：`p{margin-left:100px}` vs 无样式 ref 应大幅差异却 0.07%（mismatch 应 !=），疑 margin 应用或 mismatch 比较边界，1 案孤立非 lever。

**裁决**：R590/R549/R550/applies-to(R583-588) 全 LANDED，master.md「下一步」当前活跃 code lever 段**全 stale**（见下方该段已加 closed 标注）。单 session forward motion 确认全多 session Phase A 架构：①per-line-fragment inline bg/border（本轮 text-indent-wrap 候选）+ ②IFC 接 FontLoader 真实 ascent/advance 度量（R633 字体度量墙）= Phase A IFC 统一核心。零源码变更（read-only scoped reftest + PIL 像素分析 + git/asset 核查）。详见本轮 5 候选 PIL/reftest 数据见正文。

**R634（Gate 2 放宽墙②证伪·Phase A 完整仍被 multicol column 分配阻塞·零 landing）：承 /loop 第 5 轮 + R633（留「R209 墙②可能被 R630/R632 吸收」假设）。A/B（env PHASEA_GATE2_RELAX=1 放宽 Gate 2 存所有容器）：product-smoke **零变化**（morning 18.15→18.13% / welcome 16.16% 持平 = Path A/B 行盒度量已趋同信号）；reftest css-multicol 195→**185 (-10)** = **墙②没被吸收**。**裁决**：墙②非行盒度量分歧（R630/R632 已修行盒度量，product-smoke 零变化证实趋同），是更深的 **multicol column 分配**（Path A stored 不跑 column 重分配 vs Path B 重跑跑 column；test 真 multicol vs ref float 模拟列切不同路径致几何分歧）= Phase 2 column-aware IFC（multicol-fragmentation-design.md 独立 RFC，多会话），R630/R632 无法触及。**R633 死锁分界进一步确认**：R630-R632 修了 Path B 行盒度量（不依赖 column 分配部分，net +29），但**没消灭 Path B**；Phase A 完整（消灭 Path B，Gate 2 放宽）需先解墙②column-aware（多会话）+ 字体度量统一（IFC 接 FontLoader，多会话）。R633 留的「R209 墙②可能过期」假设由 R634 证伪。代码 git checkout 回退，零 landing。详见 [`evidence/r634-gate2-wall2-persists-2026-06-25.txt`](./evidence/r634-gate2-wall2-persists-2026-06-25.txt)。

**R633（字体度量死锁分界诊断·零代码）：承 /loop 第 4 轮 + R630/R631/R632 三轮。诊断剩余字体相关残余根因分界。**结论**：(1) morning 中文 modest 退步（+0.99pp）非行间距错（ZW vs CHR 正文行间距均 ~21-29px），是分行后 frag.height 字体自然行高诚实暴露，<1pp 无单 session clean fix；(2) baseline v_offset=font_size 已是较好近似（vs strut_ascent 0.8 启发式更偏离），非残余主因；(3) 真实 ascent/advance 需 IFC 接 FontLoader（layout-engine 已依赖 render-foundation，但 IFC 内部用 estimate/0.8 启发式不调 fontdue）= Phase A IFC 统一核心，多会话。**死锁分界**：✅已修（不依赖字体度量，启发式可得）= R630 多行 y + R632 line-height CSS 响应（net +29 reftest）；⛔未修（依赖字体度量，需 IFC 接 FontLoader）= baseline 真实 ascent / advance-width（R225 可能过期但高风险）/ 中文 frag.height / font-features。**字体四层全 characterized**：加粗(R229c)/选择(R631)/光栅化(R388)三证伪 + 度量(R633 Phase A 死锁)。「字体问题」单 session 优化目标**已穷尽**——R630-R632 把不依赖字体度量的行盒度量修复全落地，剩余需承诺 Phase A IFC 统一（IFC 接 FontLoader，layout+paint 同源 fontdue 度量）多会话架构。详见 [`evidence/r633-font-metric-deadlock-boundary-2026-06-25.txt`](./evidence/r633-font-metric-deadlock-boundary-2026-06-25.txt)。

**R632（compute_final 存 font/line-height overrides 修复 paint Path B 行间距 LANDED·commit 0911a2ac）：承 R630（多行 y 分行）+ R631（字体归因证伪→转行盒度量）。**根因确证**：store_font_sizes_from_ifc（inline_finalization.rs:80，存 text_node_line_heights）4 调用点**全在 remeasure 路径**，compute_final_inline_layouts（non-pure-Ahem 容器走此）**从不调** → text_node_line_heights 空 → paint Path B IFC override 空 → mod.rs:707 fallback line_height 19.2 (16×1.2) 忽略 CSS。决定性实验：line-height 1.5 与 2.0 产出**完全相同**行位置（固定 19px）。**修复**：compute_final 在 layout 后 Gate 2 return 前调 store_font_sizes_from_ifc，override 命中。修复后行间距正确响应 CSS（1.5→24px, 2.0→32px）。**A/B**：reftest **net +5**（css-text 1399→1405 **+6** / css-text-decor +1 / normal-flow -1 / generated-content -1 / positioning 0）—— **R627 pre-wrap -15 被 R630 with_line_y 吸收反成 +6**；welcome product-smoke 17.27→16.16% **-1.11pp**；morning 17.16→18.15% **+0.99pp**（中文 NotoSansCJK frag.height 行高 ≠ chromium，R374 字体度量谱系，独立）。zero-engine 1149 测试全过、clippy 干净。★纠正 R627 死锁结论：R630 吸收了 font_size override 的行断回归，post-R630 存 override 为 net +5（同 R246/R630 谱系，单点 net-negative 结论随基础设施过期）。**连续 R630（多行 y）+ R632（line-height）两步行盒度量实质进展**——确证行盒度量（非字体 R631 三证）是 morning/welcome 17% 真因。详见 [`evidence/r632-line-height-override-fix-2026-06-25.txt`](./evidence/r632-line-height-override-fix-2026-06-25.txt)。

**R631（字体选择/匹配 A/B 证伪·R374「字体不匹配」归因推翻·零 landing）：承 /loop「彻底修复字体问题」+ R229c（font-weight 加粗证伪）。**假设**：R374 归因 morning 17% diff =「字体不匹配」（ZW DejaVu ≠ chromium system-ui）。**调查**：product-smoke（reftest.rs:create_font_loader）主字体 DejaVu（default_id=0），system-ui/sans-serif 经 build_font_resolver→resolve_generic_family([DejaVu Sans,...]) 解析到 DejaVu；browser（app_platform.rs:load_system_fonts）优先 NotoSans-Regular；fontconfig（chromium 经此）`fc-match sans-serif/system-ui` → 均解析到 **NotoSansCJK**（最强匹配，覆盖拉丁+CJK）。**A/B**（env ZERO_FONT_PRIMARY_CJK=1，跳过 DejaVu/Liberation 强制 sans-serif→NotoSansCJK default）：morning 17.16→17.15%（-0.01pp）、welcome 17.27→17.20%（-0.07pp）= **几乎零变化**，即使 ZW 拉丁用 chromium 同款 NotoSansCJK。**裁决：R374「字体不匹配」归因证伪**。结合 R229c（加粗证伪）+ R388（fontdue 光栅化≈chromium W0.1%/i3.0%）+ R631（选择对齐零变化），**「字体问题」三角度全证伪为 diff 主因**——morning/welcome 17% 真因 = **布局/行盒度量**（line-height/baseline/行间距/多行结构），与 DC-14 注释（rendering-compat.md line 311）一致。★纠正 /loop「字体问题」框架：肉眼「文字问题」（堆叠/间距/行高）实际是**布局/行盒度量**，非字体；字体方向（加粗/选择/光栅化）是死路，真正 lever = Phase A 行盒度量统一（R630 已修多行 y，残余 wrap 50px→72px 缺口 = line-height/baseline）。代码 git checkout 回退（reftest.rs clean），零 landing。详见 [`evidence/r631-font-match-refuted-2026-06-25.txt`](./evidence/r631-font-match-refuted-2026-06-25.txt)。

**R630（paint Path B 多行 y 堆叠 bug 修复 LANDED·commit d31cf03a·用户痛点直接解决）：承用户反馈「有些文字堆叠在一起看不清」+ /loop「彻底修复字体问题」。**根因确证**：paint Path B（未存储容器重跑 IFC）对 white-space normal/nowrap 用 `all_fragments()`（y 恒 0，行内相对）而非 `all_fragments_with_line_y()`（y = run.y + line.y）→ **layout 算对多行 h，paint 把多行画在同一 y → 堆叠**。最小用例确证（16px line-height:1.5，排除字体/large-font 干扰）：wrap y 跨度 13→50px、`<br>` 14→53px 恢复多行；LAYOUT_DUMP div h=92px（layout 对）但 paint 堆叠 16px。R246 当初把 `with_line_y` 限定在 preserve_whitespace（pre 族），因 auto-wrap 多行块 test==ref 堆叠同错、正确修复致同源 -11。**R630 发现该 -11 已被 R502/R504/R525 吸收**——统一应用现为同源 **normal-flow +6 / positioning +19 / borders +1 / generated-content -2 = net +24 clean win**；css-text 1399/1576、css-text-decor 244/250、zero-engine 1149 测试全过、clippy 干净。**product-smoke morning 16.40→17.16% / welcome 16.98→17.27%（+0.76/+0.29pp）= 分行正确后行级字体/line-height 度量差异（R374 独立）显现的诚实化**（堆叠时被重叠掩盖，分行后暴露），非结构退步。★ **这是「文字堆叠看不清」的直接修复**——layout 早已算对，paint 只需正确用 line.y；不依赖 Phase A 字体度量墙（16px 字体一致纯多行 y 逻辑 bug），是 R525-R546「plateau 可被打破」又一实例（R246 单点穷尽结论随基础设施过期）。详见 [`evidence/r630-paint-pathb-multiline-y-fix-2026-06-25.txt`](./evidence/r630-paint-pathb-multiline-y-fix-2026-06-25.txt)。★纠正「字体是死路」泛化结论：font-weight 加粗（R229c 证伪）+ advance-width（R225）确实是字体墙死路，但**多行 y 堆叠不是字体问题**（16px 一致），是 paint 逻辑 bug——「真实网站没法看」是**多个独立 bug 叠加**，逐个定位修复比笼统归「字体架构」更有效。
