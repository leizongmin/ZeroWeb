# 归档：R703（CSS2/margin-padding-clear · padding-percentage-inherit entangled 负结果 era）

> 从 master.md 迁出的 R703 逐轮详记（内容 100% 保留）。R703 = padding-percentage-inherit-001（12.85%）= **ENTANGLED 负结果**（`inherit` 已实现且 padding-left=30 正确；失败=复合 block-flow/padding-top 异常非 inherit 缺陷；padding/em-inherit 簇均复合，margin-padding-clear clean lever 仍= R702）。

---

**R703（CSS2/margin-padding-clear 续·padding-percentage-inherit-001 = ENTANGLED·非 clean lever·`inherit` 已实现（inheritance.rs:44）且 padding-left=30 正确·失败=复合 block-flow/padding-top 异常非 inherit 缺陷·padding/em-inherit 簇均复合·负结果·零源码）：承 R702 CONTINUE 续 margin-padding-clear 次高 padding/em-inherit 簇（padding-percentage-inherit-001 12.85% / padding-em-inherit-001 11.17% / margin-em-inherit-001 11.25%）。选 **padding-percentage-inherit-001（12.85%）** LAYOUT_DUMP。**test**（Gérard Talbot，CSS21 §8.4 + §6.2.1 inherit）：`#grand-parent{width:300}` > `#parent{padding:20%;width:150;height:150}` > `#child{padding:inherit}`（应继承 computed 20%→对 child CB[parent content 150] 解析=**30px 各侧**）+ `#abs-pos-overlapping-green{left:98,top:162,position:absolute}`（=8 body+60 parent-pad+30 child-pad，img 须落 98,162 覆盖 green）。**预检**：script=0/overflow=0/dynamic=0✓。**dump（裁决：ENTANGLED 非 clean lever）**：① **`inherit` 已实现**——`crates/style-system/src/inheritance.rs:44 KeywordResolution::Inherit`（+ :115/:139 parse），`padding:inherit` 路径存在；② **padding-left=30 ✓**（img x=92 = #child x=62 + pad-left 30，**inherit + 百分比对 child CB 重解析正确**）；③ **但 padding-top=6 ✗**（应 30，左右不对称）+ **#parent abs_y=16 = p 同位**（应 below p ~56+）→ img 落 (92,76) 非 (98,162)，green 露出 = 12.85%。**裁决：ENTANGLED**——inherit 机制本身工作（pad-left 30 证），失败是**复合**：a) #parent 未被推到 p 之下（block-flow/margin-collapse 异常，或 dump abs_y 对此类盒不可信如 R682 storage≠paint）；b) padding-top≠padding-left（百分比 top/bottom 解析或 collapse 干扰）。**非单点 inherit lever**（inherit 已实现非缺口，区别「补 inherit」假想 lever）。**★ padding/em-inherit 簇裁决**：三案（percentage/em/margin-inherit）均「inherit + 单位解析 + margin-collapse + abspos」复合 test 设计，非 clean 单点；inherit 已实现故无「补 inherit」lever。**margin-padding-clear clean lever 仍= R702 margin-collapse**（本 subdir 唯一 clean lever）。**下一步**：转 fresh CSS2 subdir **floats-clear**（249 test，R699 §10.5.1 float-height + R108b flow_bottom float 脉络，float 模型 ZW 已知 rich vein）或 box-display（139）。零源码（LAYOUT_DUMP read-only + 代码 read-only 定位 inheritance.rs:44/115/139，未改任何文件）。
