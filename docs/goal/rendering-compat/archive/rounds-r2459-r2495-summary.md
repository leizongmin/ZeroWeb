# 轮次详记归档：R2459–R2495（自 master.md §最近轮次摘要 迁出）

> **迁出日**：2026-08-04（R2555，doc-control 策略「master.md 不允许无限增长 — 过时内容须迁移到 archive」）。
> **来源**：master.md §最近轮次摘要 底部 36 轮（R2495→R2459，含 R2486–R2495 旧 plain-「承接」format 体 + R2459–R2485 blockquote 体）。
> **性质**：100% 原文保留，未删减、未重排、未改格式。索引见 master.md §历史轮次归档索引。

承接 R2494「font-matching 续」。**R2493 回归根因**：R2493 给 resolve_font_id 加 italic fallback 链时，把既有 `if want_bold { sans-serif:700 }` bold fallback **误窄化为 `want_bold && !want_italic`**——致 bold+italic 文本在「无匹配 face」（family list 全 miss 后缀链）时回落 **FontId(0)** 而非 bold sans（丢失 bold 权重，注释却仍写「既有行为」= 不一致）。**fix**（mod.rs 1 行，删 `&& !want_italic`）：恢复对所有 bold 文本（含 bold+italic）的 sans-serif:700 fallback——bold+italic 无 face 时取 bold-sans-upright（bold 正确，italic 无 face 本就无解）优于 FontId(0) fallback 链（regular 权重）。**scope 论证**（零回归）：fix 仅影响「bold+italic + family list 全无 face（含 plain）」边角（unknown family）；welcome/morning 用已注册的已知字体，bold+italic 文本经后缀链（`:700:italic`→`:700`→`:italic`→plain）在 family 内解析，**不达此 fallback** → 必 byte-identical。**验证**：live_fontface 4/0（matching 不变）；全量 `make test` 13182/0（零回归）；product-smoke welcome **17.03%（81737px）byte-identical** + 全 struct PASS（实证 scope 论证）。**synthetic italic 去风险**（本轮探）：现 ZW 对系统字体 `font-style:italic`（无 italic @font-face）渲染为 **upright**（painter 仅 R2493 选 @font-face italic face，系统字体 italic 无 skew）——chromium 做 synthetic italic（~14° shear）。**核查 `GlyphPrimitive`**（render-foundation/primitive/mod.rs:264）：仅有 `rotation`（vertical-rl/lr 90° 旋转）字段，**无 skew/shear 字段** → synthetic italic 须 (a) 新增 skew 字段（触全 ~16 构造 site + CPU blit + GPU 着色）+ (b) blit 期水平 shear 重映射 = **rasterizer 多 site 深**，非 clean 单点。**裁决 defer**：synthetic italic = rasterizer 深（新字段 + CPU/GPU blit skew），须 RFC + A/B（broad smoke——所有系统字体 italic 文本受影响），不自主抢做。**门禁**：1 行 fix（mod.rs）+ fmt/clippy 干净；全量 make test 13182/0；product-smoke welcome byte-identical + 全 struct PASS。**意义**：修 R2493 自引入的 bold fallback 窄化回归（zero-risk，scope 论证 + A/B 双证）；synthetic italic 去风险钉案（GlyphPrimitive 无 skew 字段 = rasterizer 深，省后续重探）。**下一轮可接续**：① font-weight §5.2 精细化 RFC（R2494 已分析）；② synthetic italic RFC（新 GlyphPrimitive.skew 字段 + CPU/GPU blit，须 A/B broad smoke）；③ 待用户点名深结构（Phase A IFC / srcset-full R2421 / R1043 vertical-mode / taffy R2174 / font-stack C-dep）；④ doc-drift 续。

承接 R2493「font-weight 精细化是下一项」+ R2418「DC-5 收益量未测」。**css-fonts oracle**：`capture-oracle-per-dir`（284 shot）+ `make reftest-oracle DIR=css/css-fonts` → **105/282（37%）oracle-pass**。**fail 簇全 font-stack 深**：① `font-size-adjust-*`（11.58%/11.37%/10.44%/9.91%×3）= font-size-adjust 属性（CSS Fonts 5 §9，按 ex/ic aspect ratio 调 font-size）**未实现**（须 ex/ic metric，font-stack-gated）；② `font-feature-resolution-001`/`alternates-order`/`font-variant-*`（0% 退化可疑 pass）= **OpenType GSUB/GPOS 字形特性**（font-feature-settings/caps/ligatures/numeric，须 OpenType shaping，fontdue 不支持=font-stack 深）；③ `standard-font-family-{2,7}`（10.97%/9.76%）= 标准字体族匹配（font-stack）；④ `font-weight-bolder-001`（9.16%）= **bolder/lighter 计算期解析**（CSS Fonts §5.2）。**font-weight 精细化 + bolder/lighter 互锁钉案**（关键）：R2417 现 weight≥600→`:700`（粗）；bolder/lighter parsed 为 `Bolder`/`Lighter` variant 但 **compute 期未解析为绝对 weight**（painter 把 Bolder 当 want_bold→`:700`）。**关键事实**：parent-400 + bolder→700 = 现状（同），故 bolder 解析对常见 case **net 0**；仅 parent-700+bolder→900 才异，但 900 经 R2417 仍映射 `:700`→ 渲染 Bold = **bolder/lighter 解析单独无可见效果**，须配合精确 weight matching（`{f}:900` key + §5.2 nearest-weight 算法）才生效。**故 font-weight 精细化（§5.2 nearest-weight + bolder/lighter compute 解析）是 entangled 深 work**——须 (a) compute 期 bolder/lighter→绝对 weight（§5.2 表）+ (b) 注册精确 weight key（`:400/:500/:700/:900`）+ (c) resolve_font_id §5.2 nearest-weight 选用算法（W<400 降序/400-500 升序/>500 升序规则），多 site + smoke 风险（morning 用 5 weight：300/350/500/600/900，500→Medium、900→ExtraBold 会改渲染）。**裁决 defer**：entangled §5.2 深（非 R2493 式 zero-smoke 单 lever），须 RFC + 严格 A/B（同 font-metric R2393 谱，可能 net 负），不自主抢做。**门禁**：零 `.rs` 变更（read-only oracle 量化），HEAD=R2493 make test 13182/0 + product-smoke 全 struct PASS + morning net 0 已验。**意义**：R2418 DC-5 量化闭环——css-fonts 37%（R2417/R2418/R2493 font-matching 投资的 reftest 价值受 font-wall 限：fail 非 matching gap 而是 font-size-adjust/OpenType/standard-family font-stack 深）；钉案 font-weight 精细化 + bolder/lighter entangled 深（§5.2 nearest-weight 算法 + compute 解析 + 注册，须 RFC），省后续单点重探。**下一轮可接续**：① font-weight §5.2 精细化 RFC（compute bolder/lighter + 精确 weight key + nearest-weight 算法，须 A/B）；② synthetic italic（rasterizer skew，moderate-deep）；③ 待用户点名深结构（Phase A IFC / srcset-full R2421 / R1043 vertical-mode / taffy R2174 / font-stack C-dep）；④ doc-drift 续。

承接 R2492「实施 font-style matching」。**实施**（R2492 scope 全兑现，~7 文件）：① css-parser `FontFaceRule` 加 `style: Option<FontStyleValue>`（ast.rs）+ `consume_font_face_rule` parse `font-style` 描述符（normal→Normal/italic→Italic/oblique[+angle]→Oblique(None)，parse_font_face_style helper）；② engine `extract_font_faces` 返回 `(family, sources, weight, is_italic)`（在 engine/css-parser 边界把 FontStyleValue 折为 bool，避 webview 跨 crate 引入 css-parser 依赖）；③ webview `async_load` font_pending/font_loaded/drain 全程带 is_italic；④ renderer main.rs + tab_worker drain 按 (weight, is_italic) 构注册键；⑤ **painter `resolve_font_id` 加 `font_style` 参 + 候选键序列 fallback**（bold+italic→`{f}:700:italic`→`:700`→`:italic`→plain；bold→`:700`→plain；italic→`:italic`→plain；regular→plain；缺 italic/bold face 回落 plain = 零回归下限）；⑥ **11 caller 传 `&style.font_style`**（text.rs ×6 含 Ahem/per-text-node 路径、text_list.rs、effects.rs、controls.rs）；⑦ harness `load_font_faces_into` style-aware（镜像生产 drain）。**A/B**（product-smoke + chromium oracle 实证）：① **welcome 17.03% byte-identical** + 全 struct PASS（welcome 无 italic @font-face）；② **morning-work diff = 13.33% 双侧相同 net 0**（stash 开/关两轮，自抓 chromium oracle /tmp/morning-oracle.png）——morning-work 声明 italic JetBrainsMono/github face 但 **article.html 不渲染 italic JetBrainsMono 文本**（code 块全 upright），故 `:italic` 键注册但无 italic 文本查询 → 零渲染变化；③ 全 struct PASS（morning/wintertc/welcome 桌面+375+320）。**故 R2492「非零 smoke」预判在本 fixture 实证为 net 0**（italic face 声明 ≠ 被使用；真使用 italic @font-face 的页才触发，此时 spec-correct 选 italic face）。**driving test** `live_fontface_italic_style_uses_italic_face`（镜像 R2417 bold test：regular+italic 同族 @font-face，`<p font-style:italic>` glyph 用 italic face id ≠ regular）+ engine `extract_font_faces_returns_weight_and_style`（italic/oblique→is_italic=true、normal/缺失→false）TDD red→green。**门禁**：scoped css-parser 8/0 + engine 4/0（+1）+ webview live_fontface 4/0（+1）；**全量 `make test` 13182/0/74**（vs R2492 13180 = +2 新测，零 panic/OOM，零回归）；**workspace clippy `-D warnings` 干净**；`cargo fmt --all --check` 干净。**意义**：font-style @font-face matching 落地（R2417 weight + R2493 style 双轴完备；italic 文本不再静默渲染为 upright-regular）；R2492 去风险 + 本轮实施闭环；证 R2417 谱系可复用（同模式 multi-site + zero-smoke-if-unused landing）；A/B 诚实（R2492 预判非零 smoke 经实证为 net 0，fixture 未用 italic face）。**局限**（接受）：仅 italic/oblique→italic 键（oblique angle 忽略，匹配视为 italic）；synthetic italic（系统字体无 italic face 时的 skew）未加——仅 @font-face italic face 选用；harness 与生产一致（italic 仅 @font-face 路径）。**下一轮可接续**：① synthetic italic（系统字体 font-style:italic 的 skew 渲染，rasterizer 层，moderate）；② font-weight 精细化（600/800/900 精确 weight key，现全映射 :700）；③ 待用户点名深结构（Phase A IFC / srcset-full R2421 / R1043 vertical-mode / taffy R2174 / font-stack C-dep）；TDD/A-B + 全量门禁 net≥0 land。

承接 R2491「真进展须深结构；R2417 font-weight matching 是唯一已半铺路零 smoke 自主 landed 深项，查同族下一项 font-style」。**现状 ground truth**（read-only 核验）：① `ComputedStyle.font_style: FontStyleValue`（Normal/Italic/Oblique，types.rs:611）已 parse+computed；② **painter 完全不读 font_style**——`resolve_font_id(font_family, font_weight)`（painter/mod.rs:295）签名无 font_style，**11 个 caller 全 `(&style.font_family, &style.font_weight)` 不传 font_style**（text.rs ×6、text_list.rs、effects.rs、controls.rs 等）；③ `FontFaceRule`（css-parser ast.rs:85）有 weight（R2417）**无 style 字段**；④ `FontDesc.italic: bool`（render-foundation/font/mod.rs:21）load 期基建在（系统字体可 italic 注册）但 resolver 无 `:italic` key 选用机制。**故 italic 现状 = 声明 `font-style:italic` 的文本渲染为 upright regular（italic 被静默忽略）**——真 gap（非「已近似」）。**去风险 = smoke 非零**：smoke fixtures 有 **21 处 italic**（morning-work `JetBrainsMono.css`/`github.css`/`fira_code.css` 含 regular+italic @font-face + wintertc 用 italic）→ font-style matching 会把 morning-work italic 文本从 upright-regular 翻为真 italic glyph，**改 morning diff**（同 R2393 font-metric 谱，非 R2417 零 smoke）——**预期 net-positive**（修 upright-italic gap → 真 italic glyph 更近 chromium）但**须严格 A/B 守 net-negative 回退**（如 italic face 加载/渲染瑕疵致 diff 恶化）。**实施 scope（R2417-equivalent，~7 文件）**：① css-parser `FontFaceRule + style: Option<FontStyleValue>` + `consume_font_face_rule` parse `font-style` 描述符（normal/italic/oblique + 0-45deg oblique angle≈italic）；② engine `extract_font_faces` 返回 (family,sources,weight,**style**)；③ webview `async_load` font_pending/font_loaded/drain 带 style + `drain_loaded_fonts` 注册 italic face；④ renderer main.rs + tab_worker drain 按 style 构 key 注册；⑤ **painter `resolve_font_id` 加 `font_style` 参 + italic key 选用 + fallback 链**（`{f}:700:italic` → `{f}:700` → `{f}:italic` → `{f}`）；⑥ **11 caller 传 `&style.font_style`**；⑦ harness `load_font_faces_into` style-aware（R2418 模式）。**关键设计**：italic key 方案 = plain family（regular）+ `:700`（bold，R2417）+ `:italic`（italic）+ `:700:italic`（bold-italic），resolve 按 want_bold×want_italic 组合查 + 逐级 fallback（缺 italic face 时回落 plain = 现状行为，保零回归下限）。**门禁**：零 `.rs` 变更（read-only 去风险），HEAD=R2491 fmt+clippy 干净已验。**意义**：font-style matching 去风险完成——scope 全 map（7 文件 + 11 caller + key/fallback 设计）+ smoke 非零但预期正向钉案；下轮/ RFC 作者无须重探；证 italic 现状真 gap（静默忽略非近似）是 R2417 谱系下一可实施 lever（须 A/B 守 net-negative）。**下一轮可接续**：① **实施 font-style @font-face matching**（本 entry scope；kill-switch `ZW_LIVE_FONTFACE_STYLE=0` + 三态 A/B：self-source reftest + product-smoke〔welcome/morning/wintertc〕+ 相关 dir oracle；net≥0 land / net 负回退记 evidence）；② 或续 doc-drift / 等用户点名其他深结构（Phase A IFC / srcset-full / R1043 / taffy R2174）。

承接 R2490「oracle 扫 css-tables（table 专属子系统，最可能含离散 table-cell/border-collapse/caption lever）」。**css-tables oracle**：`capture-oracle-per-dir`（119 shot）+ `make reftest-oracle DIR=css/css-tables` → **79/115 pass（69%）**。**fail 全 table 自动布局算法边角**（CSS2 §17.5 + CSS Tables 3）：① `row-group-order`（5.44%，row/row-group 排序）；② `collapsed-border-paint-phase-002`（4.81%）+ `border-conflict-resolution`（3.32%）+ `collapsed-border-{vertical-lr/sideways-rl/vertical}-rtl-overflow`（3.24-3.29%）= **border-collapse 边缘 paint/conflict + vertical-mode**（R1043）；③ `table-cell-width-0`（4.39%，cell `width:0` 应取 intrinsic content size，§17.5.2.2 auto layout）；④ `baseline-vertical`（4.06%，R1043 vertical baseline）；⑤ `table-cell-overflow-explicit-height-001/002`（3.94%×2，cell overflow+显式 height 交互）；⑥ `min-max-size-table-content-box`（3.47%，box-sizing content-box on table min/max）；⑦ `col-definite-max-size-001`（3.43%，`<col>` `max-width:0` clamp `width:100px`——ZW `<col>` 读 `width`〔table_grid.rs:519〕但 col min/max-width 经 col-definite-max-size 实测未充分约束，table auto-layout 算法深）；⑧ `anonymous-table-cell-margin-collapsing`（3.40%，匿名 table cell margin 折叠——CSS2 §17.5 cell margin 不折叠，匿名 wrapper 规则深）；⑨ `visibility-collapse-colspan-003`（3.02%）。**结论**：css-tables 无 clean discrete lever——fail 全 table auto-layout/border-collapse/anonymous-box 边角，属 table 布局子系统深区（非单点 parse/apply gap）；自主 clean-lever surface **第七证**（shorthand+value+feature+apply+grid+nesting+tables-oracle 七面穷尽）。**门禁**：零 `.rs` 变更（read-only oracle triage），HEAD=R2490 fmt+clippy 干净已验。**意义**：css-tables oracle triage 入档（首测 69%，fail 全 table-algorithm 深边角）；第七面 plateau 确认——经 7 个 dir/维度 oracle + grep 系统验证，**自主 clean-lever surface 可信地彻底穷尽**，残余真 reftest 进展须深结构（非任何 parse/apply 单点修能解锁）。**下一轮可接续**：① 勿再盲目 oracle 扫 dir（css-multicol/css-ruby/css-position 预期同 plateau，R2430 已知深）——除非用户点名某 dir；② doc-drift 续扫（engine.rs:369 canvas 含糊注释 + 其他 stale 行号）；③ **真进展须用户从待决策清单点名一深结构**（自主 surface 七面既尽）：建议优先级 font-weight matching R2405（已半铺路、零 smoke 风险、可单 session）/ Phase A IFC 单一权威化（最高 value，解 css-text+welcome 残余，多 session）/ srcset-full R2421（已去风险）/ font-stack C-dep（解 font-wall 全 dir 受益，R2025 user-blocked 须重授权）/ R1043 vertical-mode / taffy R2174 replaced border-box。

承接 R2489「oracle 扫更冷门 dir（css-nesting 53 case 可能有离散 lever）」。**css-nesting oracle**：`capture-oracle-per-dir`（45 shot）+ `make reftest-oracle DIR=css/css-nesting` → **12/17 credible pass（71%）/ 真通过-strict 0 / 近似通过 12（70.6%）/ 5 inconsistent**。**5 fail 全 `host-nesting-001/002/003/004/005`（各 2.57%）= Shadow DOM `:host` nesting**（`host.attachShadow({mode:"open"})` + `:host{...}` 嵌套）——ZW 不实现 Shadow DOM（attachShadow/customElements OOS，R2475 已钉案），**非 nesting 解析 gap**。**其余全 <1%**（nesting-basic 0.97%、conditional-rules 0.73%、contextually-invalid-selectors 0.53%×3、nesting-type-selector 0.53%、supports-rule 0.41%、implicit-nesting 0.37% 等）= **font-wall/sub-pixel 近似通过**（nesting-basic 非 Ahem 用真文本 = DejaVu vs chromium 字形差），非离散 bug。**结论**：css-nesting 核心 nesting 选择器解析与级联**全工作**（implicit nesting / nested type selector / conditional @supports/conditional-properties / forgiving selector 全近 0% 差），无 clean discrete lever——自主 clean-lever surface **第六证**（shorthand+value+feature+apply+grid-oracle+nesting-oracle 六面穷尽）。**门禁**：零 `.rs` 变更（read-only oracle triage），HEAD=R2489 fmt+clippy 干净已验。**意义**：css-nesting oracle triage 入档（首测 71%，core 全工作，fail 全 Shadow DOM OOS）；证 ZW CSS Nesting 实现 solid；第六面 plateau 确认。**下一轮可接续**：① oracle 扫剩余冷门 dir（css-ruby 298=需 ruby 布局深 / css-tables 180=可能 table-specific / css-multicol 664=已 R2430 知深）——预期同 plateau，或转其他；② doc-drift 续扫；③ **真进展须用户从待决策清单点名一深结构**（自主 surface 六面既尽）：建议优先级 font-weight matching R2405（已半铺路、零 smoke 风险）/ Phase A IFC 单一权威化（最高 value，解 css-text+welcome 残余）/ srcset-full R2421（已去风险）/ R1043 vertical-mode / taffy R2174 replaced border-box / font-stack C-dep（解 font-wall 全 dir 受益）。

承接 R2488「自主 surface 四面既尽，oracle failure analysis 是剩余 lever 发现路径（R2423 precedent）」。**css-grid oracle**：`node capture-oracle-per-dir.mjs --category css/css-grid`（50 shot）+ `make reftest-oracle DIR=css/css-grid` → **28/49 oracle-pass（57%）/ 真通过 11（22.4%）/ 21 inconsistent**。**21 fail 全深/JS**：① `table-grid-item-dynamic-*`（25.76%/9.22%/1.64%）+ `whitespace-reattach`/`dynamic-grid-within-flexbox`/`grid-with-dynamic-img`/`img-src-changes` = **JS-driven**（ZW 不执行 JS）；② `grid-container-baseline-synthesized-*`（16.56%/14.52%×3）= **grid baseline 合成**（R109/IFC 深）；③ `stretch-grid-item-{button,text-input,checkbox,radio}-overflow` = **form-control replaced stretch**（R2174 replaced border-box territory）；④ `nested-grid-item-block-size-001`（13.76%，nested grid + img `block-size:55vw aspect-ratio:2/1` + viewport overflow = aspect-ratio+grid replaced sizing 深）；⑤ `firefox-bug-1881495`（3.26%，auto-block-size grid content-based block-size 计 line-wrapping 深）。**结论**：css-grid 无 clean discrete lever（全 structural/JS/font-wall-gated）——自主 clean-lever surface **第五证**（shorthand+value+feature+apply+grid-oracle 五面穷尽）。**doc-drift 续扫**（R2482 后高 churn 文件行号漂移）：grep 代码注释 `.rs:NNN` → 逐条核符号现位置 → 修 7 处确认 stale（pure 行号漂移，零逻辑变更）：`print_pagination.rs:31` pipeline.rs layout_extent_y **668→748**；`image_cache.rs:604` tree.rs no_ratio 分支 **413→543**；`collect_items.rs:327` tree.rs eff_ratio **436→597**；`inline_finalization.rs:112` text.rs text_align_last **949→560**；`computed.rs:443` painter/border.rs paint_outline **554→590**；`computed.rs:444` helpers.rs length_to_f32 **583→635**；`tree.rs:19` inline/mod.rs InlineItem::Br **741→1122**；`lib.rs:705` inline/mod.rs vertical-align baseline **2173→1189**。**未修**：`engine.rs:369` canvas-with-attrs 引用（tree.rs:358+line 428）——注释逻辑本身含糊（canvas 经 tag-allow-list 不早返），非纯行号漂移，遵 code-guidelines §3 不擅改含糊注释。**门禁**：本轮 7 处纯注释改动（零行为变更）；`cargo fmt --all --check` 干净；`cargo clippy -p zero-style-system -p zero-layout-engine -p zero-render-foundation -p zero-engine --all-targets -D warnings` 干净（注释-only 免全量 make test，R2480/R2482 precedent）。**意义**：css-grid oracle triage 入档（首测 57%，fail 全深/JS，省后续重扫）；doc-drift 续收敛（7 处高 churn 文件行号导航准确↑，含 lib.rs inline/mod.rs 2173 超 file 长度 1593 的明显失效）；证 oracle failure analysis 在已穷尽 dir 仍主要确认 plateau（R2423 css-cascade 出 lever 是小 dir 例外）。**下一轮可接续**：① oracle 扫更小/未扫 dir（css-multicol/css-tables/css-position 已知深，预期同 plateau；或 css-ruby/css-nesting 冷门 dir 或有离散 lever）；② doc-drift 续扫（残余 stale 行号 + 含糊注释如 engine.rs:369 canvas）；③ 真进展须用户点名深结构（待决策清单）。

承接 R2487「下一轮②转向 apply 层 parsed-but-not-applied grep」。**方法**：枚举 `computed_style.rs` ComputedStyle 字段 → 逐字段 grep layout-engine/engine/render-foundation 读消费 → 找「仅 style-system 出现（apply/inherit/default/test）下游零读」字段。**结果**：3 候选 = ① **mask_mode**（registry+apply 解析存储，下游零读）——**Explore agent 判 trivial（mask_image 已消费）但实证错**：`apply_mask_image`（effects.rs:968）仅处理 **Gradient** 蒙版（alpha 衰减），**`Url` 蒙版「暂不实现」需图像加载基建**（line 1003），mask_mode 完全不读 → wiring mask_mode 须 (a) mask URL 图像加载（unimplemented）+ (b) luminance→alpha 逐像素合成（mask-mode: luminance 真合成 op）= **深，非 clean**；② **border_image_width**（registry:297 存储下游零读）+ ③ **border_image_outset**（:307 同）——border-image paint 基建部分在（painter/border.rs 用 source/slice/repeat）但 width/outset 须扩展 border-image 切片/溢出区计算 = **R2174/paint-deep territory**。**其余 ComputedStyle 字段经核全被 layout/paint 消费**（覆盖度remarkably good）。**结论**：apply 层 parsed-but-not-applied surface **穷尽**——残余 mask_mode（URL 图像加载 + luminance 合成）/ border_image_{width,outset}（paint 深切片）全深结构；与 R2416/R2433/R2475 shorthand/value/feature-probing 穷尽结论**第四证**（自主 clean-lever surface 跨 shorthand+value+feature+apply 四面确证耗尽）。**门禁**：零 `.rs` 变更（read-only grep + 核验），HEAD=R2487 全量 `make test` 13180/0 + product-smoke welcome 17.03% + legacy 0 struct FAIL/avg 2.36% 已验。**意义**：闭合 R2486/R2487 标注的「apply 层新反模式面」——经系统 grep 确证亦穷尽（残余深结构）；钉案 mask_mode 非 trivial（纠正潜在误判：mask URL 图像加载未实现 + luminance 须逐像素合成）。**下一轮可接续**：① 真进展须用户从待决策清单点名一深结构（建议优先级：font-weight 精细化 R2405/R2417 已半铺路 / Phase A IFC 单一权威化 [解 css-text+welcome 残余 最高 value]/ srcset-full R2421 已去风险 / R1043 vertical-mode / taffy R2174 replaced border-box / font-stack C-dep 解 font-wall）；② 自主侧仅余 plateau-guard + 文档纠偏（surface 四面既尽）；③ 勿再以 mask_mode/border_image_width/border_image_outset 为 clean lever（已钉案深）。

承接 R2486「续用未探 sibling/shorthand 复查法探剩余简写（list-style/border-image/grid-template/animation·transition）」。先核 animation/transition（R2307 已 paren-aware comma + `split_outside_parens` 全就绪，非 lever）+ border-image（`/` 切片已 `split_outside_parens`，仅 repeat 2 值取首=niche defer）+ grid-template（`find('/')` 路径对 track 函数无常见 `/`，非 lever）→ **list-style = 唯一真缺口（3 处）**：① 旧 `is_type` 硬编码枚举**漏 R2445-R2450 预定义计数器样式**（lower-greek/georgian/hebrew/arabic-indic/cjk-decimal）与**全部自定义 @counter-style 名**（`Custom-Style`/`my-circle`）→ 这些 token 落「其他值暂不处理」→ **type 退回默认 disc**（WPT `list-style: Custom-Style inside`/`georgian inside` 等真案）；② **`list-style-image`（url()/image()）从不展开**（image longhand 全链路就绪却无 shorthand 触发）；③ **简写未复位 image**（`list-style: disc` 不复位既有 `list-style-image:url`，latent 简写重置 bug）。**fix**（mod.rs `expand_list_style` 重写，CSS Lists 3 §5.1 `||` 文法）：`split_outside_parens` 替 `split_whitespace`（paren-aware url）；token 分类 = position（inside/outside，大小写不敏感归一小写）→ position 槽；`url(`/`image(`/`image-set(` → image 槽；**其余 token（含 `none` + 任意计数器样式名/自定义 ident，保留大小写——计数器样式名大小写敏感）→ type 槽**；`none` 双语义（同时置 type+image 默认 none，「other values assigned to property they fit」）；**恒发 3 longhand**（type/position/image）保简写复位语义；wide_keyword 路径补 `list-style-image`（旧漏）。**9 driving 新单测**（custom-style、predefined counter style×5、image url、paren-aware url 含空格、none+square+url 双语义、image-only type=disc、type 大小写保留、position 大小写不敏感、wide-keyword 含 image）+ 更新 6 codifying-bug 旧测（grid.rs 5 + error_cases.rs 1，断言 len 2→3）TDD red→green。**A/B**（css/CSS2/lists scoped self-source 156 案，stash 开/关两轮）：**154/156 双侧相同 net 0**——2 fail（`list-style-position-023/024` nested inside marker ltr/rtl）用 `list-style-position: inside` **longhand**（非简写）+ 属深 marker/IFC 定位，非本修范围，pre-existing；CSS2 corpus 简写仅用 builtin 类型故无 flip（自定义/现代预定义样式 lever 价值在 css/css-lists，本地无 corpus）。**门禁**：scoped style-system **2117/0**（+9 新测）；**全量 `make test` 13180/0/74**（vs R2486 13171 = +9 新测，零 panic/OOM，零回归）；**workspace clippy `-D warnings` 干净**；`cargo fmt --all --check` 干净；**product-smoke** welcome **17.03% byte-identical** + 全 struct PASS；**product-smoke-legacy 0 struct FAIL avg 2.36% 不变**（产品/legacy fixture 不用自定义 @counter-style list-style 简写 → 零影响）。**意义**：list-style 简写 3 缺口闭合（自定义/预设计数器样式 type 不再退 disc + image 现展开 + 简写复位 image）；第 N 个 shorthand 解析 clean lever（R2476-R2486 谱系续）；证「未探 sibling/shorthand 复查」法在 list-style 出 lever（animation/transition/border-image/grid-template 本轮 probe 全已就绪或 niche defer）。**下一轮可接续**：① 续查其他未深扫简写（border-image repeat 2 值 niche / `font-variant` 简写 / `mask` 简写 / `place-*` 已 done）；② 转向 style-system apply 层「parsed-but-not-applied」系统 grep（R2486 标注新反模式面）；③ 待用户点名深结构（srcset-full R2421 / font-weight 精细化 / R1043 vertical-mode / taffy R2174 / Phase A IFC 单一权威化）；TDD/A-B + 全量门禁 net≥0 land。

承接 R2485「同族/同域未探 sibling 复查」法。`expand_font` 仅处理 attached `16px/1.5`（`part.contains('/')`），但 CSS 允许 `/` 两侧空白 → **`font: 16px / 1.5 serif`（spaced）被 split_whitespace 拆成独立 `/` token → 落 family_parts 致 family="/ 1.5 serif" 垃圾、line_height=normal 未设**。**fix**（mod.rs expand_font 循环加 `expect_line_height` 旗）：size_found 后遇 `/` token → 下一 token 归 line-height（不进 family）；attached 路径（contains('/')）不变。**3 driving 单测**（spaced-/ size+line-height+family、attached 回归、spaced-/ + style+weight 前置）TDD red→green。**验证**：wpt corpus 的 font:../. 全用 attached 形（grep spaced `/ ` = 0 命中）→ 无 reftest flip，net 0；css-fonts scoped **281/287=97.9% 不变**（零回归）；门禁全绿：make test **13171/0/74**（+3），clippy/fmt clean，product-smoke welcome **17.03% byte-identical** 无 struct FAIL，legacy 42 avg 2.36% 无 FAIL。**意义**：font 简写 `/` 分隔符两形（attached+spaced）完备；证 shorthand 解析复查仍有 net-0 spec-correct lever。**本 session 续证**：transform（parse_css_number 全单位就绪）、outline（split_paren_aware_tokens）、bg-position calc/minmax（R2313 已支持 + paint 期 min/max 求值 nuance = 深）均 clean/深——css-parser 反模式（bare-parse / split_whitespace 破 paren）经 R2485+R2486 收敛。

> **📍 R2485（2026-08-02）✅ drop-shadow() px-length + rgb 解析修复 LANDED（CSS Filter Effects；shadow 函数族 R2476-R2477 同根续查）— 推翻「plateau 绝对」假设**。挑战上 session「自主 clean-lever 彻底穷尽」结论，定向复查 shadow 函数族中**未探的 `drop-shadow()` 滤镜函数**（R2477 修了 text/box-shadow，漏了 filter drop-shadow）。**bug 严重**：`parse_drop_shadow` 用裸 `parts[0].parse::<f32>()` 对 `"2px"` 失败 → **所有 px 长度的 `filter: drop-shadow(2px 2px red)` 整条丢**（仅 unitless 数字能过——但非零 unitless 是非法 `<length>`）；又 `split_whitespace` 拆散 `rgb(0, 0, 0)` 含空格色；2 值（无 color）被拒（应默认 currentcolor）。**既有 5 处 codifying-bug 测试**用 unitless（`drop-shadow(1 2 3 black)` 等）固化该 bug，甚至一注释明写「参数应为纯数值而非 2px」。**fix**（复用 box-shadow R2477 模式重写 parse_drop_shadow）：paren-aware `split_top_level_whitespace`（rgb 保持一体）→ 抽首个 color（任意位置，缺省 currentcolor）→ 剩余必须全为长度（`parse_filter_length_px` 接受 px + unitless-0、拒非零 unitless）恰 2-3 个 → ox/oy(/blur)。**纠正 5 处 codifying 测试**（unitless → px：tests_9 basic/no_blur/filter_list、advanced test_parse_filter_drop_shadow、engine visual_inline、style-system apply_coverage×2）。**6 driving 新单测**（px-not-dropped、2-值 currentcolor、color-anywhere、rgb-含空格、unitless-非零拒绝、too-few-lengths）TDD red→green。**验证**：无 css-filters wpt corpus → 无 reftest A/B（unit-test 驱动），self-source net 0；门禁全绿：make test **13168/0/74**（+6），clippy/fmt clean，product-smoke welcome **17.03% byte-identical** 无 struct FAIL，legacy 42 avg 2.36% 无 FAIL。**意义**：① shadow 函数族 parsed-but-dropped/codifying-bug 收官（text/box/drop 三函数）；② **「plateau 绝对」结论过强**——定向复查同族未探 sibling 仍出 severe lever（drop-shadow px 全丢，比 R2477 更严重），后续勿轻言穷尽，多从「同族未探 sibling」角度查。

> **📍 R2483（2026-08-02）📋 多 dir clean-lever 重扫 = plateau 再确认（css-text-decor + css-borders 全 fail 深）+ css-borders 首测数据入档**。承接 plateau-guard。本轮定向扫两个未深扫 dir 找离散 fail：(1) **css-text-decor 245/250=98.0%**（与快照一致），5 fail 全深——text-decoration-dotted-002（6.53% dotted 线渲精度 paint）、skip-ink-003（0.03% paint 精度）、skip-ink-upright/vertical-002#1（**mismatch 测试 0.00% fail = ZW vertical-rl skip-ink 不生效 = R1043 vertical-mode 深**）、text-underline-offset-scroll（host-layer scroll）；(2) **css-borders 43/83=51.8%（R2483 首测，原「待测」）**，fail 簇 = **corner-shape/border-shape（bevel/scoop/notch/superellipse 角几何 paint 层深，~2% diff）**+ border-image/sub-pixel。**结论**：两 dir 无 clean discrete lever（全 paint/host-layer/vertical-mode 深结构）。结合 R2479-R2482（counter/shorthand/text-decoration/background/font-metric-drift 全 plateau）+ MQ L4 已实现 + bg-size paint 已消费，**自主 clean-lever 面 经 6+ dir/feature 实证彻底穷尽**。本轮零代码 land（snapshot 数据更新 + triage 记录）。**下一轮**：续 plateau-guard 或等用户点名深结构方向（Phase A IFC / R2174 taffy border-box / font-stack / paint-layer corner-shape·opacity·blend·bg-size intrinsic / vertical-mode R1043）；勿自主开工深结构、勿盲扫已证 plateau dir。

> **📍 R2482（2026-08-02）✅ 代码注释 stale 行号引用批修 LANDED（plateau-guard 文档纠偏，纯注释 9 处）**。承接 R2480 后续「行号漂移续扫」。grep 代码注释中 `file.rs:NNN` 引用 → 逐条核对目标符号现位置，修 9 处确认 stale（高流量文件漂移最重）：`effects.rs:69→100`（canvas_propagated_node 跳过检查，painter/mod.rs + paint/tests/basic.rs）、`engine.rs:1293→1888`（is_block_level 定义，anonymous_flex_item_tests + postprocess，**漂移 ~600 行**）、`engine.rs:1419→1905`（Relative|Sticky inset 应用，r1982 诊断测试）、`converter/mod.rs:286→434`（Sticky→taffy Relative 映射，coverage 测试）、`converter/mod.rs:241→312`（visibility:collapse strut，intrinsic_sizing）、`multicol.rs:797→927`（position_multicol_children 调用，自引）、`tree.rs:1415→1540`（multi_inline_block_skip eligible，inline_finalization）。**未修**：`intrinsic_sizing.rs:29`（核验仍准确，min-content 未实现注释仍在该行）。**验证**：fmt clean，clippy `--workspace --all-targets -D warnings` clean（注释-only，零行为变更）。**意义**：导航准确性↑（engine.rs 两处 ~600 行漂移最误导），R2480 高价值行为类 + 本轮 cosmetic 行号类共同收敛代码注释 doc-drift。**下一轮**：自主 clean-lever 面 plateau 确认（MQ L4 已实现、bg-size paint 已消费、css-logical=vertical-mode 深）；续 plateau-guard 或等用户点名深结构方向。

> **📍 R2481（2026-08-02）✅ background 简写 `/size` 解析 LANDED（net 0，spec-correct parse 修复，paint 层瓶颈故零 reftest flip）；`<box>` 累积经 A/B 证 net −3 → 回退保持 drop**。承接 R2479「box + /size 打包切片」。**bug**（css-parser/style-system）：`background: url(x) top left / 100% auto no-repeat` 的 `/size` 此前**整体丢**（split_whitespace 把 `/` 当 token，`100%` 误累进 position、`auto`/`contain`/`cover` 落 bg_color 垃圾、size 恒 auto）。**fix**（`expand_background` 重构 + 新 `BgSlots`/`split_bg_position_and_size`/`classify_bg_token`）：① paren-aware depth-0 `/` split（`url(support/x.png)` 内的路径 `/` 在 depth≥1 被排除）→ pos_part / size_part；② classify 加 `size_side` 旗——`/` 后的 length/percent/auto 归 **size** 而非 position，`contain`/`cover` 归 size（改前误落 bg_color），repeat/attachment/box/color 仍正常分类（可在 `/` 后出现）；③ vec emission 用 slots。**`<box>` 决策**：box 累积（origin/clip）A/B 实测 net −3（attachment-local-clipping-color-3/4 + image-3 false-pass unmasks，host-layer JS-scroll deferred，同 R2479），故**保持 drop**（box arm `return;`），仅 land /size。**验证**：A/B（box+/size = 360/710 net −3；/size-only box-drop = **363/710 net 0 = R2478 baseline**，background-334 仍 4.38% fail——**paint 层 background-size 渲染**是瓶颈非 parse）；5 单测（/size keyword、两值+repeat-after、url 内 `/` 不分割、box-drop+size-parsed、no-box 默认）TDD red→green。门禁全绿：make test **13162/0/74**（+5），clippy/fmt clean，product-smoke welcome **17.03% byte-identical** struct 全 PASS，product-smoke-legacy 42 fixture avg 2.36% 0 struct FAIL。**意义**：/size parse 正确化（contain/cover/auto 不再误赋 bg_color、size 不再丢、position 不再被 size token 干扰）——net 0 landable（master.md net≥0），为未来 paint 层 background-size 渲染就绪后铺路；box parse 单修确认无 ROI（paint + host-layer 双瓶颈）。**下一轮**：background 简写 parse 侧（box+/size）已尽，残余失败 = paint 层 size/clip 渲染 + attachment:local host-layer scroll（深）；pivot 到其他 vein 或 doc-drift 续扫。

> **📍 R2480（2026-08-02）✅ font-metric 注释 doc-vs-code 漂移纠偏 LANDED（doc-drift 首切片，纯注释零行为变更）**。承接 R2479「pivot 到 doc-vs-code 纠偏」。R2202 实证本 goal 注释滞后误导 agent；本轮定向核对 font-metric 注释簇（master.md 待决策清单已标 font-metric 激活 = R2393 net 负完结，但**代码注释仍称「待 env 激活 / 见待决策清单」**= 双重 stale）。**fix**（2 文件纯注释）：① `render-foundation/font/loader.rs:429-437` + `layout-engine/inline/text_metrics.rs:344-348` 的「接通状态」block 改写——`reftest.rs:557` → `reftest.rs:568`（行号漂移 +11，set_font_metric_map 现位置实测确认）+ 状态从「R2202 dormant 待激活见待决策清单」翻为「**R2393 实证生产激活 = net 负保持 dormant（welcome 英文 +0.44pp 恶化、morning 中文零变化——CJK lever 假设证伪）→ 勿再以 font-metric 生产激活为 lever**」+ 指向 `evidence/font-metric-activation-ab-2026-08-01.md`。**防误导价值**：避免后续 agent 读旧注释误把已完结 net 负项当 pending lever 重推（正是 R2202 根因模式）。**验证**：fmt clean，clippy `--workspace --all-targets -D warnings` clean（注释-only，零行为变更，免全量 make test）。master.md:69 的 `reftest.rs:557` 系 R2202 历史轮次记录（当时正确），archive 性质不动。**下一轮**：续 doc-drift 扫描（代码注释中其他 `xxx.rs:NNN` 行号引用漂移 + 过时行为描述），或其他 vein。

> **📍 R2479（2026-08-02）📋 background 简写 `<box>` drop 修复 → 净负 REVERT（spec-correct 但零 reftest ROI，bottleneck = /size + host-layer scroll）**。承接 R2478 probe 鉴定的 background 简写 parsed-but-dropped 簇。**实施**（已写+TDD 4 单测 green 后 A/B 证负回退）：`classify_bg_token_owned` 加 `bg_boxes: &mut Vec<String>` 累积槽（box token 不再 `return;` 丢弃）+ `expand_background` vec emission 按 CSS §3.10/§3.11 消费（0=默认、1=origin&clip 同值、2=origin/clip）；4 单测（单 box origin&clip、双 box origin/clip、无 box 默认、padding-box）red→green。**A/B**（css-backgrounds 全量 710，stash 开/关两轮）：scoped self-source **363→360（net −3，+0 gain）= 3 false-pass unmask（attachment-local-clipping-color-3/4、image-3）**：其 test `background: green local content-box`+JS scrollTop vs ref `background: green content-box`，改前 content-box 两边都丢→都默认 border-box 假通过；改后 content-box 正确应用但 test 仍需 attachment:local+JS scroll（host-layer deferred）→ 真不一致揭露（同 R2478 attachment-local 模式）。**+0 gain 根因**：box-in-shorthand 失败案（background-334/335/336 等）**真正缺的是 `/size` 解析**（`background: #CCC url() top left / 100% auto` 的 `/size` 被整体丢，非 box 问题）+ attachment:local host-layer scroll；box parse 单修无 reftest ROI。**裁决 = 按 master.md「net 负 → 回退」回退**（stash drop，工作树回 R2478 HEAD de89f3a8）；spec-correct 但需 **bundled `/size`** 才能出 gain（background-334/335/336 `/size` flip 可抵消 unmasks → 净 0 landable）。**下一轮**：box + `/size` 打包切片（paren-aware `/` split〔url 内 `/` 须排除〕+ size 分类 + contain/cover→size 不落 bg_color）；或转其他 vein。本轮零代码 land（实现 revert + 证据记入）。**同 session 续探 text-decoration 多行 shorthand**（`text-decoration: underline overline line-through`）：`expand_text_decoration`（mod.rs:2128）同样 `line = part.to_string()` 覆盖而非累积（只留末个 line 关键字），但 **`TextDecorationLineValue`（types.rs:130）是单值 enum（None/Underline/Overline/LineThrough/Blink），不支持多行组合** → 修 shorthand 须同步把 computed type 改 bitflags + apply + paint（borderline-deep，非 clean 单点），driving `text-decoration-line.html` .all-decorations 现仅渲 line-through（oracle 错，self-source test+ref 同 shorthand 故假通过 net-0）。**结论：多组件 shorthand value-级 vein 真 plateau**——border/text-decoration/flex 已 order-independent 或 spec-positional；background box/text-decoration-多行 均 entangled paint/type 深层。下轮勿再扫此 vein，pivot 到 doc-vs-code 纠偏 或其他。

> **📍 R2478（2026-08-02）✅ background-position 3/4 值语法 LANDED（CSS Backgrounds §3.6）— R2477 同族「parsed-but-dropped / value 位置敏感性」续查破局**。承接 R2477「下一轮可接续 ① `background-position` keyword+length 混合」。**bug**：`<position>` 3/4 值语法（一个或两个「边缘+偏移」对，如 `right 25px top 75%`、`left 50px center`、`top -15px left 0`）改前 `parse_background_position` 仅处理 1/2 值，`parts.len()∈{3,4}` 落单值分支 → None → **整条声明丢**（含 `background:` 简写内联位置）。**WPT 真案 driving**：css-backgrounds/background-position-three-four-values（改前 12.76% FAIL）、background-position-xy-three-four-values-passthru。**fix**（css-parser + style-system + engine 三层纯加性）：① parser 加 `BackgroundEdge` enum + `EdgeOffset{side,offset}` variant + `parse_position_three_four`（左→右扫 token：边缘关键字 + 后随 lp 结成 EdgeOffset 对，按关键字轴 left/right=水平 top/bottom=垂直 分配 h/v，center 补缺轴，同轴重复 / 裸 lp → None 非法；3 值缺一轴补 center）；② computed 加 `EdgeOffset(BackgroundEdge, Box<…>)` + 递归 `position_value_to_computed`；③ engine `resolve_position_component` 加 EdgeOffset 分支（left/top = offset 本身；right/bottom = (container-image) − offset 翻转，与 bare right/bottom 关键字一致 right 0%≡right）—— **Rust 穷举 match 在编译期守住 parsed-but-dropped**（缺该 arm 直接 E0004 编译失败，非静默丢；本 session 接手时 working tree 即停在「parser+computed 已写、engine 缺 arm 编译失败」半成品态，补 arm 后绿）。**5 driving 新单测**（3 值 keyword+offset、4 值两对、bottom 边、顺序无关、同轴非法 None）TDD red→green。**验证**：A/B（parser 开/关两轮全量 css-backgrounds 710 案 + 隔离 6 案双跑）scoped self-source **365→363（net −2）= +2 真 flip（three-four-values 12.76%→0.00%、xy-passthru fail→0.00%）− 4 false-pass unmask**（attachment-local-clipping-image-1/2/4/5 PASS→FAIL：其 **ref** 用 `background: … top -15px left 0` 4 值位置模拟 JS `scrollTop=15` 滚动后的 attachment:local 状态；改前 ref 位置被丢 → ref 与 test〔ZW 不应用 JS 滚动〕都落 (0,0) 假通过；改后 ref 正确定位 (0,−15) 而 test 仍 (0,0) → 真不一致被揭露 = 同 R2477 box-shadow-border-radius-001 模式，需 host-layer JS-scroll + attachment:local〔deferred，不自主开工〕）；ref 解析手验正确（top -15px→y=−15、left 0→x=0）。**意义**：position 3/4 值收官 + parsed-but-dropped 同族连三破局（R2476 inset / R2477 color / R2478 3-4-value）；DC-14 测量诚实度↑（4 假通过揭露，self-source −2 全为 deferred host-layer 假通过非真回退）。门禁全绿：make test **13157/0/74**（+5 vs R2477 13152），clippy/fmt clean，product-smoke welcome **17.03% byte-identical** struct 全 PASS，product-smoke-legacy 42 fixture avg 2.36% 0 struct FAIL。**下一轮可接续**：① 续审其他简写/值 keyword 位置敏感性（R2477 遗留 `border`/`flex`/`grid` 简写 keyword 顺序）——**R2478 收尾 probe 已证伪**：`parse_border_shorthand`（mod.rs:700）早已 order-independent（按 token 类型 width/style/color 累积，与位置无关）；`expand_flex`（:912）位置匹配 spec 文法（flex 文法本就 positional `<grow> <shrink>? <basis>?`，非 bug）；`expand_grid_axis`（:1328）按 `/` 分 start/end 正确。**故 border/flex/grid 非 lever，勿再扫**。**R2478 follow-up 真正新 lever = `background` 简写 parsed-but-dropped 簇**（`expand_background`+`classify_bg_token_owned` mod.rs:1751/分类函数）：① **`<box>` 值静默丢**（border-box/padding-box/content-box → `return;` 不设 origin/clip；spec 单 box 设 origin+clip、双 box 设 origin/clip）；② **`contain`/`cover` 落 bg_color 误赋**（无 size 分类臂 → 默认 `*bg_color=token`）；③ **`/size` 完全不解析**。driving ~22 box-in-shorthand + ~11 /size 案（css-backgrounds）。**但 moderate 非 clean 单点**（box+size+comma-layer 交织；现 parser 单层 split_whitespace 破坏 `url(a),url(b)` 多层）→ 须切片设计（box 单切片先：加 box 累积槽 + 单/双 box 语义 + vec emission，~20 行加性，A/B 验），勿一把梭；② attachment:local scroll 渲染（host-layer JS-scroll，深，等点名）；TDD/A-B + 全量门禁 net≥0（含 false-pass unmask 记 evidence）land。

> **📍 R2477（2026-08-02）✅ text/box-shadow 颜色位置敏感性修复 LANDED（CSS Text Decoration §3 + Backgrounds §7.1 `&&` 文法）— R2476 inset-anywhere 同族「parsed-but-dropped / parse 位置敏感」续查破局**。承接 R2476「续查同族 parsed-but-dropped / parse 位置敏感」。**bug**：`<shadow>` 文法 `<length>{2,3|4} && <color>?`（text/box）的 `&&` 允许颜色任意位置，但 `parse_text_shadow`/`parse_box_shadow` 按**固定下标**取 parts[0..1]=length、parts[2/3]=blur/color → ① 颜色在前（`red 2px 2px`）/在中（`2px red 2px`）→ `parse_length(parts[0|1])` 失败 → **整条丢 None**；② `2px 2px red 4px` → red 占 blur 槽（`unwrap_or(0)`）、4px **静默丢**（blur=0）。**WPT 真案 driving**：css-backgrounds/box-shadow-005 `rgba(0,255,0,1) 10px 10px`、css-pseudo/marker-text-shadow `#0f0 1px 2px 3px`、selectors/focus-within `text-shadow: black 0px 0px 0px`（**均颜色在前，改前被丢**）。**fix**（css-parser 单 crate，纯加性重构两函数）：统一算法 = `split_paren_aware_tokens` → 抽 inset（box，扫全部 token）+ 抽**首个**颜色（`parse_color` 命中即移出 length 槽）→ 剩余必须**全为长度**并按序映射 ox/oy/blur/spread（spec：至多一色一 inset；第二色会 `parse_length` 失败被拒）；blur/spread 改严格 `?` 替旧 `unwrap_or(0)`（拒绝非长度垃圾，更 spec-correct；全既有测均良构不受影响）。**纠正 2 个 codifying-bug 旧测**（`test_edge_parse_text_shadow_color_first`、`test_parse_box_shadow_color_at_start_returns_none` 均断言「color-first→None」，现翻为 Some + 正确字段断言）+ **1 coverage 旧测**（comma-list 进 singular parser 旧 lenient Some，现正确 None，改测单阴影）+ **1 误导注释**（coverage.rs「不支持颜色在前」改 accurate）。**7 driving 新单测**（color-first keyword/hex、color-middle、blur-after-color-not-dropped、box color-first/middle+spread/inset-anywhere）TDD red→green。**验证**：scoped box-shadow self-source **19→19 net 0**——box-shadow-029 `green 50px 50px 0px 50px`（color-first）**FAIL→PASS +1**；box-shadow-border-radius-001#1 **PASS→FAIL −1 = false-pass 被 unmask**（改前阴影被丢、mismatch 靠 2px 盒形差通过；改后阴影渲染但 ZW outset-shadow **忽略 border-radius**〔渲矩形〕致 test(circle)/notref(rect) 阴影同形 0.00%→mismatch fail；该 border-radius shaping = paint-deep + spec-ambiguous〔csswg #7103〕，同 R2476 deferred `inset-without-border-radius` 族，**不自主开工**）；**DC-14 真通过 +2**（box-shadow-005 false→true 0.58%→0.00%；box-shadow-029 fail→true-pass）；css-pseudo marker-text-shadow 仍 PASS（0.07%→0.11%，现 truthful）；text-shadow 路径零回归。门禁全绿：make test **13152/0**（+7 vs R2476 13145），clippy/fmt clean，product-smoke welcome **17.03% byte-identical** struct 全 PASS，product-smoke-legacy **42 fixture 0 struct FAIL avg 2.36% 不变**。**意义**：shadow parse 颜色位置敏感性收官（R2476 inset-anywhere + R2477 color-anywhere）；证 plateau 后「parsed-but-dropped / parse 位置敏感」复查仍出 lever（box-shadow-005/029/marker 均 WPT 真案 color-first，非臆造）。**下一轮可接续**：① 续审其他 CSS 函数/值 keyword 位置敏感性（`border` 简写 keyword、`background-position` keyword+length 混合、`flex`/`grid` 简写 keyword 顺序）；② outset-shadow border-radius shaping（paint-deep + csswg #7103 spec-ambiguous，须 RFC/A-B，可解锁 border-radius-001#0/#1 + box-shadow-outset-without-border-radius-001）；TDD/A-B + 全量门禁 net≥0 land。

> **📍 R2476（2026-08-02）✅ box-shadow inset 渲染 LANDED（CSS Backgrounds §7.1）— box-shadow 12→19（+7 真 flip 零回归），plateau 后破局**。`box-shadow: inset` 此前**parse 漏识别**（`parse_box_shadow` 仅 `starts_with("inset")`，但 CSS 允许 inset 在值**任意位置**；`black 10px 10px 0px 0px inset`〔inset 在末尾，最常见语法〕被当 outset）。**修复链**：① parse_box_shadow 改扫全部 token 提取 inset（移出 parts），前/中/后位均识别；② ShadowPrimitive 加 `inset: bool` 字段（全 16 构造 site 更新）；③ paint_box_shadow 对 inset 用 **padding-box** rect（perimeter = padding edge，border 内；outset 仍 border-box）；④ render_shadow CPU 加 inset 分支（frame 形状 = box 减 offset+spread 收缩的洞，box_blur_inset 正确 2D-clamp blur——既有 box_blur 滑动窗口 init 对非均匀 frame mask 有 double-count 残差泄漏〔对 outset 均匀矩形 mask 良性故未暴露〕，inset 须独立正确 blur）。**验证**：1 unit test（inset 盒内暗化/盒外白/中心淡出）；scoped box-shadow **12→19（+7，零回归）**，inset-box-shadow-scroll 4.94%FAIL→0.66%PASS；box-shadow-inset-without-border-radius 残余 3.35%（border+padding+spread blur=0 硬阴影几何 nuance，defer）。GPU 路径暂渲 inset 作 outset（TODO；reftest/product-smoke 用 CPU 不受影响）。门禁全绿：make test 13145/0/74（+1 inset 单测），clippy/fmt clean，product-smoke welcome 17.03%（outset 零影响）struct 全 PASS，legacy 51/51 struct PASS。

> **📍 R2475（2026-08-02）📋 全 dir clean-lever 穷尽综合 triage（plateau 确认）**。承接 R2474。本轮补 triage 两个未深扫 dir：① **css-content quotes 簇（8 fail, 1-6%）**——render 诊断证 `<q>` 注入引号（resolve_q_quotes before/after pseudo）与 literal 引号**glyph 列表完全一致**（同位置同字形），paint_quotes skip A/B 零 delta；residual 1-6% = 全文档结构 nuance（句中 inline `<q>` 流 / body margin / lang），非单点 clean bug，font-wall/sub-pixel territory，defer；② **css-color（287/308=93.2%, 23 fail）**——tagged-images（color profile）/ opacity-* compositing（paint 层深）/ animation（OOS）/ system-color-highlights（selection host-layer）/ color-mix-basic-001（**JS-driven** `<script>` TEST_CASES，19.41%）全非 clean。**结论：经 R2467-R2475 全 session 扫描，带 wpt-data 的全 dir（css-sizing/backgrounds/pseudo/nesting/transforms/counter-styles/cascade/color/content(ui/position/box/contain/images/text/text-decor/fonts/variables/conditional/grid/flexbox/multicol/tables/CSS2)）clean-lever surface 真 plateau**——残余 fail 全 structural（R2174 taffy border-box / Phase A IFC / font-wall）、paint-deep（opacity/bg-image canvas tiling/3D）、advanced/host-layer（Highlight API/slider/selection/Shadow DOM/@layer）、OOS（animation）或 JS-driven。**显著推进需用户授权一项 structural 待决项**（master.md 裁决均「等点名」勿自主开工；建议优先 R2174 footprint 最大，或 font-wall 全 dir 受益）。本轮零代码 land（render 诊断 revert + triage 记录）。

> **📍 R2474（2026-08-02）📋 simp-chinese-informal 实现尝试 → 负结果 revert（spec-exact 递归分组未 nailed，defer）**。承接 R2473 i64 widening unlock。实现 `to_simp_chinese_informal`（multiplicative-additive：万/亿/万亿 4 位分组 + 内零 + per-group 10-19 informal 省 一 + ≥10^16 digit-〇 fallback + 负数），**算法对 < 10^12 全 64 值 ground-truth 正确**（含负数、亿组、万亿小系数），但 **≥ 10^12 大系数（9999999999999999 等）的 万亿 分组规则非简单 4-chunk**：spec 用递归 亿(10^8) 剖分，万亿 marker 由递归 high 渲染涌现（9999999999999999 = 99999999 亿 99999999 → 渲 "九千九百九十九万九千七百九十九万亿九千七百九十九万九千七百九十九"，非 chunk3=9999 的 "九千九百九十九万亿..."）。手推递归规则未收敛（15-digit 万亿=10^12 vs 16-digit 涌现 万亿 矛盾），须取 CSS Counter Styles 3 §limited-chinese 权威 algorithm（spec 文本 / chromium 实测对比）。**ROI 评估**：单 reftest flip（counter-simp-chinese-informal，非 false-pass 真 fail）须 ~150 行 spec-exact 递归算法 + 5 CJK 同族各一份 → **defer 整簇**（unlock 已落地 R2473 widening 保留，CJK formal/informal 待 spec algorithm 或降优先级）。本轮零代码 land（实现 revert 到 R2473 HEAD）。

> **📍 R2473（2026-08-02）✅ counter value type i32→i64 全链路 widening（基建，unlock CJK formal/informal）**。CSS 计数器值规范上无界，旧 `parse_counter_action`/`CounterActionValue.value`/`painter.counters`/`get_counter`/`format_counter_text`/`counter_style_body`/`format_counter_alpha/roman` 全 i32 → CJK counter 测试用 **10^16 量级 counter-reset**（simp/trad-chinese 等）静默溢出丢弃声明。widening 8 site 全 i64（覆盖 ~9.2×10^18），`Fixed(first)` cast i64 适配。**验证**：unit test 加 i64 大值断言（`n 9999999999999` → Some(9999999999999)，`n 10000000000000000` → Some(10^16)，原 i32 溢出 None）；scoped arabic-indic 2/1 零回归。**net-0 self-source**（无 CJK algorithm 前），spec-correct 基建，**unlock 后续 CJK formal/informal（6+ flip 候选）须配合各 ~100 行 multiplicative-additive 算法**。门禁全绿：make test 13145/0/74，clippy/fmt clean，product-smoke welcome 17.03% struct 全 PASS，legacy 51/51 struct PASS。**旁证 dead-end**：css-content quotes A/B（paint_quotes 跳过）= **零 delta**（paint_quotes 非 quotes fail 因因，double-render 假说证伪，已 revert）；css-cascade 实测 **26/49=53.1%**（stale 快照 32.7% 已订正），fail 全 @layer/revert-layer（深 cascade）+ scope-*（Shadow DOM OOS）。

> **📍 R2472（2026-08-02）✅ cjk-decimal + cambodian 计数器样式 LANDED（CSS Counter Styles 3 §6.1）+ CJK-counter 深度 triage**。续 R2471 numeric counter 批次。**cjk-decimal** = numeric system 但 digit **非连续码点**（0=〇 U+3007，1-9=U+4E00/U+4E8C/U+4E09/U+56DB/U+4E94/U+516D/U+4E03/U+516B/U+4E5D）→ 须 lookup table（`to_cjk_decimal`，同 to_persian 模式）；**cambodian** ≡ khmer 别名（同 U+17E0+ digits，1-line parse alias）。enum + parse_misc + match arm 接线。验证：1 unit test（cjk-decimal ground-truth 10=一〇/101=一〇一/1002=一〇〇二）；scoped cjk-decimal 3/4 PASS（004 残余结构性小差，改前即 fail）；cjk-decimal/cambodian 改前非 builtin → Custom → decimal fallback，现按 Chromium 渲脚本字形（**oracle 真增益**）。门禁全绿：make test 13145/0/74，clippy/fmt clean，product-smoke welcome 17.03% struct 全 PASS，legacy 51/51 struct PASS。**CJK-counter 深度 triage**：simp/trad-chinese、japanese、korean formal/informal = 复杂 multiplicative-additive 算法（含 十百千万/亿/万亿 分组 + 内零处理 + 10-19 informal 规则 + ≥10^16 digit-〇 fallback + 负数 负 前缀），且 driving test 用 **10^16 量级 counter-reset 值，ZW `parse_counter_action` parse::<i32> 溢出 → 须先 i32→i64 counter 全链路 widening（~8 site）才能加载这些值 → 每 style ~100 行算法 + widening，单 flip ROI 低，defer。

> **📍 R2471（2026-08-02）✅ 11 numeric 预定义计数器样式 LANDED（CSS Counter Styles 3 §6.1）— oracle 真增益（self-source net-0）**。css-counter-styles triage 后续：devanagari/bengali/gujarati/gurmukhi/kannada/malayalam/tamil/telugu/lao/khmer/myanmar 全 numeric system = 十进制位数字替换（同 arabic-indic R2451 算法，仅 digit 字形块不同）。**实现**：① ListStyleTypeValue 加 11 变体；② parse_misc.rs 加 11 parse case（此前非 builtin → Custom → registry 未命中 → decimal fallback）；③ `to_digit_script(value, base)` 泛型 helper（base = 脚本 DIGIT ZERO 码点 U+0966/09E6/0AE6/0A66/0CE6/0D66/0BE6/0C66/0ED0/17E0/1040）+ text_list.rs match arm 接线。**验证**：1 unit test（devanagari/bengali/tamil/myanmar ground-truth digit + 与 arabic-indic 同算法一致性）；scoped `make reftest-upstream FILTER=css-counter-styles` = **183/250=73.2% net-0 self-source**（DC-14 false-pass：test+ref 均 `list-style-type: devanagari` 经 ZW 渲同算法 → 改前都 decimal / 改后都 script digits → 恒匹配；**oracle 真增益**：ZW 现按 Chromium 渲脚本字形，原 decimal fallback 消除）。门禁全绿：make test 13144/0/74，clippy/fmt clean，product-smoke welcome 17.03% struct 全 PASS，legacy 51/51 struct PASS。**注**：CJK formal/informal（simp/trad-chinese、japanese、korean）= 复杂 multiplicative-additive 算法（含 十百千万 标记 + 内零处理），残余 67 fail 多为此类，moderate（须逐风格实现 algorithm，driving 用 literal-ref 非 false-pass，真 flip 候选但工作量高）。

> **📍 R2470（2026-08-02）📋 plateau-guard：多 dir clean-lever 重扫负结果（防下轮重探）**。承接 R2469 css-backgrounds +12 后续探。逐 dir triage（均无新 clean lever）：① **css-pseudo（186/261=71.3%）** 76 fail = slider-*(7, form-control 伪元素 host-layer) + Highlight API（active-selection 8 / highlight-cascade 7 / highlight-painting 4 = CSS Highlight `::highlight()`/`::selection`/`::spelling-error` 高级伪元素 host-layer/advanced）+ first-line-line-height(3 = `::first-line` per-line 样式，**ZW 零消费**——css-parser 解析 `::first-line` 选择器但 style-system/engine 无应用路径，须 layout 识别首格式行 + 应用 per-line 样式含 font-size 改行度量，moderate-to-deep，defer)；② **css-nesting（12/17=70.6%）** 5 fail 全 `host-nesting-*` = Shadow DOM `:host` 嵌套（OOS，非本 goal）；③ **background-root 残余**（008/009 1.15% 等）= bg-image 画布传播 + repeat-y 全页高 tiling（paint 层，R1428 anchor 已就位，残余 sub-pixel/tiling-extent 精度，moderate）；④ **body-propagation-010**（3.09%）= `body::before{display:block;background:green}` 块渲染精度（::before 注入为 block 子，画布传播已不涉，残余 = 块几何精度，moderate）；⑤ 复核 logical-property 族（margin/padding/inset/border/place-* 全就位）、`overflow` 简写（1/2 值全就位）、counters() plural（须 counter scope stack，深 defer）全 done/deep。**结论**：可达 clean-lever surface 真 plateau（R2467-R2469 三连后），残余全 structural（R2174/IFC/font-wall 待决）或 paint-deep/advanced/host-layer。下轮方向：① 继续定向 code-reading probe 新 property value-级 gap（R2204 模式，~50 属性已扫但新角度可能出 lever）；② 或评估是否对某 structural 待决项（如 ::first-line per-line styling、bg-image canvas tiling）做可回退切片设计（须先 RFC，勿自主开工深结构）。

> **📍 R2469（2026-08-02）✅ body 无 principal box 时背景不传播到画布（CSS §9.2.4/§14.2）LANDED + css-backgrounds 全量 triage**。reftest-driven vein 转 css-backgrounds（wpt-data 710 案，原 347/710=48.9%）。**clean lever**：`background-color-body-propagation-004`（`body{display:none}`）/ `-007`（`body{display:contents}`）100% FAIL → 0.00% PASS。根因：画布背景传播（painter/mod.rs `canvas_propagated_node` 机制，R491/R507/R2369）body-fallback 分支只查 `contain_blocks(body)`，**未查 body 是否生成 principal box** → body{display:none}（不生成盒）/ display:contents（无主盒）仍把 body 红 bg 传播到画布。修复：加 `no_principal_box` 闭包（`display` ∈ {None, Contents}），body-fallback 增 `&& !no_principal_box(body_style)` 守卫（display:none 路径 body 完全不生成盒→无自身 bg 填充；display:contents 路径 body 仍生成盒但 converter 映射 Contents→Block 致空 body 盒 0 高→不可见，画布传播被阻→reftest 0.00% PASS）。**css-backgrounds triage**（351 fail）：background-size 132（bg-image 缩放 = paint + intrinsic sizing）、background-clip 34（text/border/padding/content clip = paint 层深）、animations 30（OOS）、origin/repeat/attachment 小簇。css-backgrounds net **347→359（+12，零回归，failed −12）**——body-propagation 004/007 + 其他 background-root-* body/html display:none canvas 填充纠正。残余 006（JS-dynamic html display:none，须 JS+relayout，defer）/010（3.09% 独立小案）。display:contents 完整布局（converter 现 maps Contents→Block）= 独立深 gap，本修复的传播守卫对其 defensive 正确。门禁全绿：make test 13143/0/74，clippy/fmt clean，product-smoke welcome 17.03% struct 全 PASS，legacy 51/51 struct PASS。

> **📍 R2468（2026-08-02）✅ contain-intrinsic-{inline,block}-size logical longhands LANDED（CSS Sizing 4 §intrinsic-size-override）+ css-sizing 全量 triage（plateau-guard）**。reftest-driven vein 转 css-sizing（wpt-data 861 案，原不在快照）。**clean lever**：`contain-intrinsic-inline-size`/`block-size`（logical longhands）ZW **完全未实现**（grep 0 命中）→ apply/registry/inherit 三处补（inline→width、block→height，垂直轴交换由 converter swap_writing_mode_axes 负责，同 inline-size/block-size），`parse_cis_longhand` 复用（剥可选 `auto` 前缀）。driving test `contain-intrinsic-size-logical-001` 0.62% PASS（fail→pass）。**css-sizing triage**（333 fail 全分类）：aspect-ratio/*（~139，flex/grid/replaced/block + box-sizing:border-box + padding = **R2174 taffy border-box 同脉**，待决）、intrinsic-percent-replaced（26，replaced sizing）、contain-intrinsic-size（14，parse 全就位 / `contain:size` R2239+R2256 消费就位，残余 fail 主为父 `width:max-content` 的 intrinsic two-pass）、fit-content()（11，intrinsic two-pass）。**结论：css-sizing clean lever 仅 logical longhands 这一处**（已 land），余全 structural（R2174 / intrinsic-sizing two-pass / taffy 待决），下轮勿重扫。门禁全绿：make test 13142/0/74，clippy/fmt clean，scoped css-sizing 230/562=40.9%（+1，零回归），product-smoke welcome 17.03% struct 全 PASS，legacy 51/51 struct PASS。

> **📍 R2467（2026-08-02）✅ line-clamp slice 2 ellipsis LANDED（CSS Overflow 4；painter text.rs vertical-triggered ellipsis + LayoutBox.line_clamp_clamped 信号）— 承接 R2466 识别的最高价值 lever**。承接 R2466「下一最高价值 lever = line-clamp slice 2（ready RFC `line-clamp-rfc-2026-08-02.md`，slice 1 R2431 已 land）」。**根因深挖**：slice 1（R2431 IFC cap）+ 旧 6f1919fc paint-side glyph-truncation ellipsis 两套独立 cap **冲突**——pure-Ahem 容器走 stored 路径（inline_finalization.rs:946 gate），inline_layout 被 R2431 cap 到 N 行后，paint 旧 `line_ys.len() > max` 判定**永不成立**（已 ≤ N）→ ellipsis 漏渲。**修复**：① LayoutBox 加 `line_clamp_clamped: bool` 字段（types/mod.rs，layout 期 IFC `clamped` 镜像，inline_finalization.rs:925 后置）；② painter text.rs line-clamp 后处理增第二触发源 `box_node.line_clamp_clamped`（stored 路径据此补 ellipsis），省略号从「3 个 ASCII '.'」改为**单 U+2026 glyph**（与 WPT line-clamp refs 一致：refs 用 `…` 字面量），定位**自适应**（末行末尾文本后；末行满则 cutoff + 右对齐 content_right，镜像 text-overflow cutoff）。**验证**：unit test 2 个（stored 路径 U+2026 出现 / 内容不足 N 无 ellipsis）；scoped `make reftest-upstream FILTER=line-clamp` = **141/274=51.5%（+1 pass）但 strict-credible 21→40（+19，省略号大量精确化）**，canonical line-clamp-001 0.00% PASS，零回归（fail 134→133，无类别倒退）；webkit-line-clamp-* fail = legacy `-webkit-box` 布局（深，非 ellipsis 问题，defer）。**门禁全绿**：make test 13141/0/74，clippy clean，fmt clean，product-smoke welcome 17.03%（无 fixture 用 line-clamp → 零影响，diff 为 AA 噪声）/ struct 全 PASS，product-smoke-legacy 51/51 struct PASS / 0 fail。

> **📍 R2466（2026-08-02）📋 css-overflow scoped 扫描 + line-clamp 高价值 lever 重浮 + longhand-completeness 审计负结果**：系统审计 vein 转「shorthand longhand 完备性」维度 → **负结果**：`font` 简写仅 emit 5 longhand（漏 reset font-variant/font-stretch），但 ZW **根本未 track font-variant/font-stretch**（registry/field/apply 全空，仅 font-variant-numeric tracked）→ reset 是 no-op，与未 track 属性 entangled；font-variant-numeric reset niche 且 css-fonts 已 97.9% → **非 clean lever**。同期 css-overflow scoped 扫描 triage：fail 簇 = **line-clamp/*（~30 案，最大簇）+ webkit-line-clamp/block-ellipsis（CSS Overflow 4 line-clamp feature，parsed+computed 就位 R2430，~130 driving reftest）+ clip-002~005（4 案 overflow:clip paint）+ scroll-into-view/dynamic（deferred）**。**关键重浮**：`line-clamp-rfc-2026-08-02.md` 现成 RFC（lint Fail=0 自主放行，R2407 先例）——**slice 1（IFC cap + height）R2431 已 LANDED**（inline/mod.rs:638-658 `apply_line_clamp_cap` + clamped 字段 + kill-switch ZW_LINE_CLAMP，horizontal-tb only），line-clamp reftest 现 fail 因 **slice 2（第 N 行末 ellipsis）缺**。slice 2 = 真 reftest 收益（~130 案），painter text.rs 加 vertical-triggered ellipsis（复用 text-overflow 的 measure_char_for_paint/add_glyph，区别：触发=clamped 末行 vs 横向溢出，定位=末行末尾 vs content_right）。**下一最高价值 lever = line-clamp slice 2**（ready RFC，非 user-gated，高 footprint）。

> **📍 R2465（2026-08-02）fix shorthand+wide-keyword gap class 续：font/outline/text-decoration/list-style/columns/grid-area/grid-column/grid-row（8 个）**：R2464 后批量审计 `expand_one` 全部简写 → 发现另 8 个 token-classifying 简写仍缺 wide-keyword guard（`font: inherit`→[] 整条丢；其余 token 分类失败取默认）。逐个加 guard（font/outline/text-decoration/list-style/columns/grid-area 用 `wide_keyword_to_longhands`；grid-column/grid-row 经参数化 `expand_grid_axis` inline `vec![mk(start_prop,value),mk(end_prop,value)]`）。批量审计测试 `test_r2464_audit_other_shorthands_wide_keyword`（收集全部 fail）red→green。net≥0 by construction。`make test` 13139/0/74。**shorthand+CSS-wide keyword gap class 全闭环**（R2464 6 + R2465 8 = 14 简写；pass-through 类 + border 助手 R2354 + background + text-emphasis R2422 本就 OK）。

> **📍 R2464（2026-08-02）fix shorthand + CSS-wide keyword gap class（flex-flow/flex/transition/column-rule/animation/border-image）**：系统审计 vein 第 3 个 gap class——token-classifying 简写的 expander 把 `inherit`/`unset`/`revert`/`revert-layer` 当未知 token 分类失败 → 各 longhand 取默认值（如 `flex-flow: inherit`→row/nowrap、`transition: inherit`→property:inherit+其余默认）。TDD 7 red→green（+7 测，`background` 已正确无需修），加 `wide_keyword_to_longhands` 助手 + 6 expander 顶部 guard（镜像 R2354 border 助手）；flex 保留显式 `initial`（spec 0/1/auto，test_flex_initial_keyword 守）。net≥0 by construction（仅改 wide-keyword 路径 broken→correct，token 分类正常值不变）。`make test` 13138/0/74。reftest A/B 免：gap class 已由 box-shadow R2462 钉案。pass-through 类简写（margin/padding/border-w/s/c/inset/overflow/overscroll/gap/axis_logical/border-radius/background）audit 全 OK（split_whitespace/parse_rect_values 天然透传单 token）。

> **📍 R2463（2026-08-02）fix `contain-intrinsic-width/height: inherit` + 系统审计闭环 inherit gap class**：R2462 box-shadow inherit 修复后做系统审计——对所有非继承 known property 检查 inherit_property 是否有 case（`prop: inherit` 须对任意属性生效）→ 仅 **contain-intrinsic-width / -height 两长手缺失**（有 value/initial 路径，独缺 inherit）。TDD red→green（+1 测 test_contain_intrinsic_longhands_explicit_inherit_keyword），加 2 case。**再审计 = 0 missing**：inherit gap class 全闭环（box-shadow R2462 + contain-intrinsic R2463）。net≥0 by construction（同 gap class，仅改显式 inherit 路径）。`make test` 13131/0/74。reftest A/B 免：gap class 已由 box-shadow-currentcolor 13.79%→0% 钉案（R2462）；contain-intrinsic-computed 为 JS parsing 测试非 reftest。

> **📍 R2462（2026-08-02）fix `box-shadow: inherit` 关键字静默失效（CSS wide keyword gap）**：reftest-driven vein 转「按 master.md 残余清单点名 probe」产出真 lever——`inherit_property`（inherit.rs）有 text-shadow 但**缺 box-shadow case** → `box-shadow: inherit` 经 inheritance.rs Inherit arm 调 inherit_property 返回 false → 子元素 box_shadow 留默认 none（应继承父值）。TDD red→green（+1 测 test_box_shadow_explicit_inherit_keyword）；**A/B 钉案**：css-backgrounds box-shadow-currentcolor.html 修复前 13.79% FAIL → 修复后 0.00% PASS（真 reftest flip，非理论修）。net≥0 by construction（仅改 box-shadow:inherit 路径 broken→correct，默认非继承行为不变，test_box_shadow_not_inherited 仍绿）。`make test` 13130/0/74。**意义**：R2460/R2461「clean-lever era 终结」结论**过强**——reftest-driven vein 在「按残余清单点名 probe specific named gap」模式下仍有产出（区别于盲扫全 dir）；后续可续按各 dir 残余清单逐点名。

> **📍 R2461（2026-08-02）📋 reftest-driven vein：css-position scoped 扫描 + 全 fail triage**：css-position self-source **83/97=85.6%**（与快照一致）；14 fail 全分类——hypothetical-dynamic-change-002/003 + position-absolute-dynamic-relayout-004/005/006（5 案）= **JS-driven dynamic relayout**（script-sandbox 执行+重排，deferred 深路径）；position-absolute-in-inline-003~006（4 案）= **IFC 内 abspos 静态位置**（Phase A territory）；position-relative-003/004/005（3 案，1 静态+2 动态）= 百分比 inset，**`convert_length_to_lpa` 已正确保 `Percentage→LengthPercentageAuto::percent`**（converter/mod.rs:615），残余 1.15% diff = taffy 0.7 relative 百分比 inset 解析（taffy-migration territory）/sub-pixel；replaced-object-backdrop（100%）= backdrop+replaced paint-isolation（深）；position-absolute-center-002（1）= abspos centering 单案。**结论：css-position 无 clean discrete lever**。**双 dir（css-ui R2460 + css-position R2461）收敛钉案**：reftest-driven vein 残余 fail 全为 JS-dynamic / IFC(Phase A) / taffy-internal / R2174 / host-layer 深结构，clean-lever era 经 2 dir 实证彻底终结。

> **📍 R2460（2026-08-02）📋 reftest-driven vein：css-ui scoped 扫描 + 全 fail triage**：css-ui **926/986=93.9% self-source**（新数据点，原不在 oracle 快照）；60 fail 全分类为 **深结构**——box-sizing-007~025（~20 案）= **R2174 replaced-element border-box territory**（`<img>` intrinsic+padding+min/max，user-gated）；caret-*/caret-shape-*/caret-eol-*（~15）= host-layer 光标渲染（深）；outline-*（~10）= 多为 sub-pixel/font-wall（0.2-2%）+ outline-013 负 offset clamp 单案（fiddly re-center 几何，低 ROI）；text-overflow-*（~6）= IFC baseline/relative-pos + ellipsis glyph font-wall（Phase A）；widgets（accent-color-checkbox/radio、compute-kind-revert、appearance）= 深。**结论：css-ui 无 clean discrete lever**。同期核验 value-level 已饱和：tab-size/empty-cells/accent-color/caret-color/contain-intrinsic-size/text-indent 全 parse+消费已实现；lh/rlh 非快修（须 line-height 度量 thread 进 `resolve_length` 签名 = borderline-deep）。clean-lever era 经实证终结。

> **📍 R2459（2026-08-02）feat CSS Values 4 §7.3 `<resolution>` 全单位**：`parse_dpi_value` 仅剥 `dpi` → 现支持 `dpi`/`dpcm`(×2.54)/`dppx`(×96)/`x`(=dppx 别名，×96)，大小写不敏感。TDD red→green（4 新 fail→全过），`make test` 13129 passed/0 failed/74 ignored 净正；media-query `resolution` 媒体特性 value-级合规补全（dpi/bare-number 行为零变更，strictly more-accepting → net≥0 by construction）。
