# 归档：R686–R688（css-flexbox aspect/direction 簇收口 + css-position pivot 起步）

> 从 master.md 迁出的 R686–R688 逐轮详记（按时间倒序，内容 100% 保留）。R686 定位 aspect-ratio-intrinsic-size-007 SVG intrinsic-size bug 代码（image_cache.rs:419，cluster 非 unified）；R687 确认 flex-direction:row-reverse 正确、发散归 R255 inline-span→taffy::Block（Phase A）；R688 开启 css-position PIVOT（semi-replaced stretch 已正确，发散是原生 form-control out-of-scope）。

---

**R688（PIVOT→css-position·position-absolute-semi-replaced-stretch-input = ★stretch 已正确（R499 确证）·发散=原生 form-control 渲染·非 layout bug·refute「stretch broken」假设·零源码）：承 R687 CONTINUE 第 3 目录 PIVOT 到 css-position。先 `make reftest-oracle DIR=css-position`（97 案，read-only；position 不涉 table-cell overflow→不受 engine.rs:666 WIP 影响=main HEAD honest 数；aggregate 39/97=40.2% chr<1%，与 R559 41.2% 一致）取 top-15：replaced-object-backdrop 100% / backdrop-inherit-rendered 47.54%（backdrop-filter 未实现 R670，skip）/ **position-absolute-semi-replaced-stretch-input 21.19%** / position-static-001 17.53% / **semi-replaced-stretch-other 13.66%** / abspos-dynamic-relayout-005 11.84%…。★ 瞄准 **semi-replaced-stretch 两案**（21.19%/13.66%）——据 memory R603「semi-replaced-stretch 已 R499 修」，但**仍 13-21% 发散**，疑「fixed 标签错或不全」（典型方法论目标）。选 **input（21.19%，overflow=0）** LAYOUT_DUMP。方法：`LAYOUT_DUMP=1 make reftest-oracle DIR=position-absolute-semi-replaced-stretch-input` + product-smoke + PIL。**test**（csswg #6789）：9 个 `.cb`(position:relative,height:100px,width:150/350px,border:3px,inline-block) 各含 abspos `<input>` 表单控件（button/submit/reset/color/text/password/date/file/range，class abs：`top/right/bottom/left:3px` 全 inset + `width/height:auto` + `outline:2px solid lime`），assert「semi-replaced 元素 auto main size + explicit insets 应 stretch 填 CB」。**dump（裁决：stretch 正确）**：input.abs **w=144 h=94**（= CB 150×100 padding-box − 2×3 inset = 144×94，精确）——**abspos stretch 对 semi-replaced input 工作**✓（R499 确证，refute「stretch broken」假设）；9 input 均正确 stretch。**PIL（发散 = 原生 form-control 渲染，非 stretch）**：lime outline 两侧均 1496px（stretch+outline 一致✓）；**但 cb 内部 ZW=(255,255,255) 白盒 vs CHR=(239,239,239) 原生灰 widget**——CHR top colors 含 (239,239,239) 灰背 4853px + (118,118,118) bevel 573px + (0,117,255) 蓝 accent 123px（date/file/range 原生控件），**ZW 全白盒无 form-control 外观**。**裁决**：position-absolute-semi-replaced-stretch-input **非 layout bug**（stretch 正确）——21.19% 发散 = **原生 form-control（`<input>`）渲染差异**：ZW 把 input 当纯白矩形，CHR 渲原生平台 widget（灰背+bevel+accent），**非 clean lever**（原生表单控件 platform-specific，非 spec-rendering scope，每浏览器/平台不同，不可复刻）。**-other（13.66%）推同机制**（object/embed 等其他 semi-replaced 原生外观）。**意义**：css-position PIVOT 首案——refute「semi-replaced-stretch broken」假设（R499 stretch 确工作），实证发散是原生 form-control 渲染（out-of-scope）；与 css-flexbox 经验一致（头部「structural/fixed」标签纠偏后多归已知结构性/out-of-scope 家族，非 css-tables 式隐藏 clean lever）。**css-position top 余案**（position-static-001 17.53% / abspos-dynamic-relayout-005 11.84% / position-absolute-in-inline-006 5.10%）待续轮。零源码（LAYOUT_DUMP read-only + product-smoke read-only + PIL，未改任何文件）。

**R687（PIVOT→css-flexbox 续·flex-direction 簇·flexbox_direction-row-reverse = row-reverse 顺序正确·发散=inline span 在 flex-item 内垂直堆叠（R255/R109 inline→taffy::Block）·非 flex-direction bug·零源码）：承 R686 CONTINUE 测 css-flexbox「flex-direction 簇」（flex-flow-001 23% / flex-flow-002 23% / direction-row-reverse 21.53% / flexbox_display 24.85%）是否藏可隔离 bug（如缺 row-reverse 逆序）。选 **flexbox_direction-row-reverse（21.53%）** LAYOUT_DUMP。方法：`LAYOUT_DUMP=1 make reftest-oracle DIR=flexbox_direction-row-reverse`（read-only；overflow=0→不受 engine.rs:666 WIP 影响=main HEAD honest 数）+ product-smoke（`--base-dir`）+ PIL。**test**：`#container font:40px Ahem;width:13em`(=520px) > `ul display:flex;flex-direction:row-reverse` > 3×`li width:4em`(=160px)，各 li 含 4 个彩色 Ahem 字形 `<span>`（IJKL/ABCD/EFGH），assert「row-reverse 正确排序」（li 应逆序：EFGH 左 / ABCD 中 / IJKL 右）。**dump（裁决：row-reverse 正确，span 堆叠才是发散）**：3 li x 位置 **li[1](IJKL) x=368 / li[2](ABCD) x=208 / li[3](EFGH) x=48**——**逆序正确**（li[3] 最左，li[1] 最右）✓ flex-direction:row-reverse **工作**；**但每个 li 内 4 个 `<span>` 垂直堆叠**（li[1] 内 span 全 x=368，y=40/88/136/184，各 w=160 全宽）——应水平流（I J K L 单行），**ul h=192（4 span×48）vs 应 h≈48（单行）**。**PIL**：diff 21.53%；ZW nonwhite-y 32-231（~200px，4 行堆叠）vs CHR 10-105（~95px 单行）。**根因（代码 read-only 确认）**：`crates/layout-engine/src/converter/mod.rs:273-275`——`DisplayValue::Inline | InlineBlock | ... => taffy::style::Display::Block`，即 **inline 盒（span）映射为 taffy Block**（全宽），在 **flex-item（li 被 blockify）上下文内垂直堆叠**而非 IFC 水平流（= **R255 issue**，旧记 line 265，code drift 现 273-275；R255 morning.work .item-tag span 同机制）。**裁决**：flexbox_direction-row-reverse **非 flex-direction bug**（逆序正确）——发散 = **inline span 在 flex-item 内 inline→taffy::Block 垂直堆叠**（R255/R109 inline-as-IFC Phase A 结构性），**非 clean lever**。**★ flex-direction 簇推断**：flex-flow-001/002 / direction-row-reverse / flexbox_display 均「flex-item 内含 inline span/text」，发散**很可能同被 R255 inline-span-堆叠主导**（非 flex-direction 本身）——勿期「修 flex-direction 解簇」，须逐案 dump 确认（部分或纯 flex-direction 但 row-reverse 已证正确）。**hand-off code-agent**：inline-box 参与父 IFC 水平流（非 taffy Block 堆积）= R109/R255 Phase A（inline-as-IFC），多会话结构性，非单 session。**意义**：css-flexbox PIVOT 三轮（R685/R686/R687）——aspect 簇 mix（007 SVG-intrinsic 孤立）/ flex-direction 簇（row-reverse 正确，发散是 R255 inline-span 堆叠）；两簇均**非 flex-box 本身 bug**而是 **replaced-intrinsic（image path）+ inline-as-IFC（Phase A）**；css-flexbox 头部发散（除 baseline R266 / abspos-inset）多归已识结构性家族，**未现 css-tables 式隐藏 clean lever**（R678/R680/R679 在 css-tables 被纠偏出，css-flexbox 头部两簇纠偏后均归已知 structural）。零源码（LAYOUT_DUMP read-only + product-smoke read-only + PIL + 代码 read-only converter/mod.rs:273-275，未改任何文件）。

**R686（PIVOT→css-flexbox 续·★pin 007 SVG intrinsic-size 代码位置 + aspect cluster sizing 证非统一·负结果·零源码）：承 R685（007 = SVG intrinsic-size bug）做两件 read-only 收口：① **cluster sizing**（grep aspect-ratio 簇 test setup）：aspect-ratio-intrinsic-size-001（inline-flex，div-based，无 img）/ 003（显式 `aspect-ratio:1/1`）/ 006（img 60×60-green.png 定尺寸 raster）/ flex-aspect-ratio-cross-size-001（显式 `aspect-ratio:2/1` + 1×1 PNG）/ 013（img 300×150-green.png）——**007 是唯一用 `100%/100% viewBox` SVG 的案**，余案用显式 CSS aspect-ratio 或定尺寸 raster PNG。**裁决（负结果）**：css-flexbox「aspect-ratio cluster」**非统一簇**（unlike css-tables table-width 簇 R679/R681/R682/R684 一根因）——007 = 孤立 SVG-intrinsic bug（修仅 yield 007）/ 013 = aspect+min-size taffy-blocked（R670）/ 003/cross-size-001 = 真 CSS aspect-ratio / 001/006 = flex transferred/intrinsic sizing；**无 cluster-level clean lever**，code-agent 勿期「修一处解 aspect 簇」。② **pin 007 bug 代码位置**：`crates/render-foundation/src/image_cache.rs:416-430` `decode_svg_bytes`——**line 419 `let size = tree.size()`** 取 usvg tree 内在尺寸，usvg 对 `<svg width="100%" height="100%" viewBox="0 0 7500 3750">` 把 `tree.size()` 解析为 **viewBox 像素维 7500×3750**（百分比 w/h 回落 viewBox），line 421-422 `w=7500 h=3750` → raster 7500×3750 pixmap → 成 img intrinsic px。**fix scope（hand-off code-agent）**：`decode_svg_bytes` 须区分 SVG 有定 px w/h（用之）vs 百分比/auto（**非** viewBox px，应 ratio-only 无 intrinsic px，viewBox 仅供 ratio），并把「ratio-only 无 intrinsic px」语义传到 layout（replaced 元素按 ratio+容器 sizing，非 7500×3750 定尺寸）；非一行修（须语义穿线 image→layout），但**根因精确界定**（image_cache.rs:419）。**与 R683 合「replaced-element intrinsic-size 谱系」**：R683 canvas（1×1 intrinsic 被忽略→满宽）+ R685/686 SVG（viewBox 当 intrinsic→过大），两反向 facet，均 image/intrinsic-sizing path。**意义**：css-flexbox PIVOT 两轮（R685/R686）收口——aspect 簇非统一（负结果，区别 css-tables 正结果）+ 007 根因代码定位完成（code-agent 可直接动手 image_cache.rs:419）；方法论：cluster sizing 用 grep test setup（快）区分统一簇 vs mix，勿假设 cluster 单根因。零源码（grep test setup read-only + 代码 read-only 定位 image_cache.rs:419，未改任何文件）。
