# 归档：R691–R693（css-grid pivot era）

> 从 master.md 迁出的 R691–R693 逐轮详记（按时间倒序，内容 100% 保留）。R691 = grid-item %height 对 track 解析 clean lever（第 5 个）；R692 = replaced img CSS aspect-ratio 被固有比覆盖 clean-ish lever（oracle 损坏须先 regen）；R693 = oracle 可信度全语料 triage（broken-image race 孤立，排名可信）。lever 详情亦见 pending-clean-levers.md。

---

**R693（oracle 可信度全语料 triage·css-grid 49/50 valid + 全语料 60 wide-sparse 候选实证非系统性·broken-image 抓取 race 孤立（仅 nested-grid）·density 启发式不足须 regenerate 裁决·零源码）：承 R692 CONTINUE「triage suspect-broken oracle shots」——R692 证 nested-grid-item-block-size-001 committed oracle 是 blank broken-img placeholder（真发散 64.15% 非 20.41%），疑 reftest-oracle 排名被 broken oracle 污染（R388 机制）。本轮 read-only 系统排查。**① css-grid 全 50 oracle shot PIL triage**（nonwhite + bbox + density）：density<0.10 且 bbox-area>20k 启发式标 4 个 wide-low-dens 嫌疑——nested-grid-item-block-size-001（nw=2240/792×440/d=0.006，**R692 已证 broken**）/ min-size-auto-overflow-clip（1668/784×52/d=0.041）/ anonymous-grid-items-002（3259/784×74/d=0.056）/ stretch-grid-item-text-input-overflow（11082/643×208/d=0.083）。**puppeteer regenerate（显式 networkidle0+500ms）裁决**：后 3 案 regen 与 committed **逐像素 match**（1668/3259/11082 全一致）→ **均 valid**（min-size = full-width border-only outline 784×52 周长≈1672 / anonymous-grid = sparse 文本字形 / stretch-input = 原生 form-control widget 渲染 R688 territory）；**仅 nested-grid regen=348480 vs committed=2240（155×）= 唯一 broken**。**css-grid oracle set 49/50 valid**。**② 全语料扫描**（13791 oracle shot，C-speed ImageChops）：wide(ba>150k)+sparse(dens<0.03,nw<8k) 签中 **60 候选**，但**签名过度命中**：~25 是 **CSS2/pagination paged-text**（page-break/orphans 测试分页文本，无 img/bgurl→结构上必 valid sparse）+ text-heavy（css-text/hyphens/text-align/font-variant 文本字形）+ border-outline（tables/positioning）。抽查最稀疏非 img 候选 **background-bg-pos-204/206（dens=0.0058，比 nested-grid 更稀疏，含 background-image:url）** regenerate → **逐像素 match（2522/769×567）valid**（background-position 定位到角落小区域）。**裁决**：broken-image 抓取 race **孤立（仅 nested-grid），非系统性**——须 `<img>` 或 `background-image:url` + 大 image-box + 抓取 race 三者同时（nested-grid 的 nested-grid>inline-block>grid>tiny 8×16 img+overflow 结构触发异常慢 layout/decode 错过 networkidle0+80ms 窗口，罕见）。**③ refined 方法论**：**density alone 不能区分 broken-image vs valid sparse**（text/pagination/border 均合法 sparse）——**regenerate-with-img-complete-wait 是唯一裁决性测试**；density 启发式仅作「候选筛选」，不替 regenerate。**fresh css-grid ranking**（`make reftest-oracle DIR=css-grid`）：nested-grid 现 **20.41%（rank 5）但真 64.15%**——broken oracle **低估** severity 3×（非 phantom-high，是 metric 不可信）；其余 top（replaced-...-002 34% R691 / -001 9% / stretch-button-overflow 8.17% / grid-calc-margin 4.17%）均 valid oracle。**hand-off**：① regenerate nested-grid 本地 oracle（oracle-shots gitignored 可再生）+ **建议 chromium-oracle-shot.mjs 加 `img.complete`/`Page.evaluate` img-load 等待**防 race 复发（tooling 改动，非 browser 源码，hand-off code-agent）；② next valid css-grid clean-lever 候选 = **stretch-grid-item-button-overflow 8.17%（疑 form-control）/ grid-calc-margin 4.17%**（baseline/dynamic/broken 已排除）。**★ 意义**：R692「oracle 可能被污染」担忧实证为**孤立非系统性**（全语料仅 nested-grid 1 案 broken，其余 wide-sparse 均合法 sparse 渲染）——reftest-oracle 排名整体可信，方法论再精化（density 筛选 + regenerate 裁决）。零源码（PIL triage read-only + puppeteer regenerate 7 案 read-only + `make reftest-oracle DIR=css-grid` read-only + 代码未触碰；临时 regen 脚本已删）。

**R692（PIVOT→css-grid 续·nested-grid-item-block-size-001 = ★REAL ISOLABLE BUG·replaced img 显式 block-size 下 width 按固有比推导忽略 CSS aspect-ratio·★但 committed chromium oracle 损坏（blank broken-img placeholder）·oracle 真发散 64.15%（非 20.41%）·clean lever 但 oracle 失效须先修·零源码）：承 R691 CONTINUE 续 css-grid 次高非 baseline/非 dynamic 案 **nested-grid-item-block-size-001（committed oracle 20.41%）** LAYOUT_DUMP。**dump 前预检（R691 hand-off 要求）**：test 无 `<script>`（非 JS✓）/ 无 `overflow:` CSS（overflow count=0✓）/ 无 table cell→engine.rs:666 WIP 对本案完全 no-op = main HEAD honest 数✓。方法：`LAYOUT_DUMP=1 make reftest-oracle DIR=nested-grid-item-block-size-001`（1 案，read-only）+ product-smoke 渲 ZW PNG（`--base-dir` 解 support/colors-8x16.png）+ PIL。**test**（Sean Feng/Mozilla，css-grid §nested-grid-item-sizing）：`ul.container display:grid;grid-auto-flow:column` > li > `div inline-block` > `div grid` > `<img block-size:55vw;aspect-ratio:2/1 src=colors-8x16.png(8×16 intrinsic)>`，assert「nested grid item (img) 同非 grid 一样按 CSS sizing——block-size 55vw + aspect-ratio 2/1 → img 应 880×440（110vw×55vw，溢出 viewport）」（ref 为 img 直接在 li 内、ul block，无嵌套）。**ZW dump（viewport=800，55vw=440）**：img **h=440（55vw✓ 正确）w=220（= 440 × 8/16 = height × 固有比 0.5！）**——应 w=880（= 440 × 2 = height × CSS aspect-ratio 2/1）；li/两层 div 均 w=220（shrink-to-fit 包裹 img）。**PIL（裁决：BUG 确证 + oracle 损坏双重发现）**：① **ZW** nonwhite bbox **(8,8)-(227,447)=220×440**（img 内容正确加载：上半 Blue/Black + 下半 Pink/Green 四象限，但比例 1:2 而非 2:1）；② **committed CHR oracle** nonwhite **仅 2240px**，bbox (8,8)-(799,447) 跨 792×440 但内容稀疏——`colors-8x16.png` 无白色像素，正确渲染必满色 ~350k px，仅 2240px = **broken-image placeholder（grey 192,192,192 边框 + 31 green 破图图标）**！即 **committed oracle 是 blank 损坏抓取**（img 抓取时未加载）。**★ decisive 验证（regenerate oracle）**：直接 puppeteer 重抓 + `page.evaluate(img.complete/naturalWidth)` 探针——chromium `img.complete:true naturalWidth:8 naturalHeight:16 getBoundingClientRect=880×440`，重抓 PNG nonwhite=**348480（=792×440 满色，valid）**。**ZW vs 重抓 CHR 真 diff = 64.15%**（非 committed 20.41%——committed oracle 把真发散低估 3 倍）。**★ 根因（代码 read-only 精确界定）**：`crates/layout-engine/src/tree.rs:276-282`（replaced img，height 显式 width auto 分支）——`taffy_style.size.width = Length(ch * w / h)` 用**固有 w/h(8/16)** 推导 width，**完全忽略 `computed.aspect_ratio=Some(2.0)`**（converter/mod.rs:89 已正确把 CSS aspect_ratio 传 taffy，但本行用 Length(220) 显式覆盖，致 taffy aspect-ratio 失效）；aspect-ratio parse（apply.rs:665-691）与 plumbing（converter/mod.rs:89）均正确，**唯一缺陷在 tree.rs:281 用固有比而非 CSS aspect_ratio 推 auto 侧**。**裁决：真 isolable layout bug（spec-correct）**——CSS Box Sizing §aspect-ratio：replaced 元素有显式 size + CSS aspect-ratio 时，**CSS aspect-ratio 胜过固有比**（本案 width 应 height×2=880，非固有 height×0.5=220）；tree.rs:260/213 的 `is_none()` 守卫已正确跳过固有比设置，但 **276-282 显式 Length 推导旁路了 CSS aspect_ratio**，是 R325 replaced-sizing 逻辑遗漏 CSS aspect-ratio 的 bug。**fix scope（hand-off code-agent，clean lever）**：tree.rs:276-282 + 镜像 width 显式 height auto 分支（270-275）——当 `computed.aspect_ratio.is_some()` 时，auto 侧应按 **CSS aspect_ratio** 推导（本案 width=height×2=880）而非固有比；一行级条件分支（aspect_ratio Some/None 分流），spec-correct，blast radius 低（仅 replaced 元素一显式一auto 且设 CSS aspect-ratio 的子集）；**本案真 64.15%→~0% PASS**（oracle 修复后），潜在多案 yield（其他 img/aspect-ratio definite-size reftest）。**★ 但本 case 非 code-agent 可直接 harvest**：committed oracle 损坏须先修——**oracle 抓取 race**（chromium-oracle-shot.mjs `networkidle0`+80ms 对本案 8×16 tiny img 不足，img 未加载就截图），须对该 case 重抓（已验证重抓 valid）；hand-off code-agent：① 先 regenerate 本案 oracle（或加 img-load 等待），② 再做 aspect-ratio 修复 + A/B。**与 R691 同 cluster（replaced-element in grid sizing）正交**：R691 = grid-item %height 对 track 解析（percentage path）；R692 = replaced img 一侧显式一侧 auto 时 CSS aspect-ratio 优先于固有比（intrinsic-ratio path）；两 bug 独立，code-agent 可分别修。**★ 方法论意义（双重）**：① **LAYOUT_DUMP 再证 clean lever 分布广**——css-grid 第 2 案又现 clean isolable bug（R691 %height + R692 aspect-ratio），R670「css-grid top 全 baseline 结构性」泛判再被纠偏；② **oracle 失效须 regenerate 验证**——committed oracle 20.41% 是 blank 抓取假低估（真 64.15%），与 R388 Ahem oracle 损坏同机制（抓取 race），**对 image-loading reftest 的 oracle 须 generate 时显式等 img.complete**（chromium-oracle-shot.mjs `networkidle0`+80ms 不足）；本案是 css-grid 12/50 oracle shot <2% nonwhite 之一（抽样 <2% 非必坏，R691 -001/-002 真小内容 nw=1574 compact bbox 273×48 valid，本案 nw=2240 wide bbox 792×440 = 坏）。零源码（LAYOUT_DUMP read-only + product-smoke 渲 PNG read-only + PIL + puppeteer regenerate oracle read-only + 代码 read-only 定位 tree.rs:276-282，未改任何文件；regen 脚本临时文件已删）。

**R691（PIVOT→css-grid·replaced-element-percentage-height-in-grid-nested-in-flex-002 = ★REAL ISOLABLE BUG·grid-item %height 对 grid 容器而非 track 解析·css-grid clean lever·零源码）：承 R690 CONTINUE 第 4 目录 PIVOT 到 css-grid。先 `make reftest-oracle DIR=css-grid`（49 案，read-only；grid 不涉 table-cell overflow→不受 engine.rs:666 WIP 影响=main HEAD honest 数；aggregate 16/49=32.7% chr<1%，与 R560 一致）取 top-15：grid-container-baseline-synthesized-001/003 **78.69%**（R266 baseline-export，skip）/ **replaced-element-percentage-height-in-grid-nested-in-flex-002 34.00%** / table-grid-item-dynamic-003 23.81%（dynamic/JS）/ nested-grid-item-block-size-001 20.41% / baseline-synthesized-002/004 17.35%（skip）。选最高非 baseline **002（34.00%，overflow=0/script=0）** LAYOUT_DUMP。方法：`LAYOUT_DUMP=1 make reftest-oracle DIR=replaced-element-percentage-height-in-grid-nested-in-flex-002` + product-smoke（`--base-dir`）+ PIL。**test**（css-grid-2 §grid-item-sizing）：`.flex display:flex;flex-direction:column;height:400px;width:100px` > `.grid height:100%(=400);display:grid;grid-template-rows:1fr 3fr` > `<img src="100x100-green.png" style="height:100%">`，assert「100×100 绿方块」（img 在 1fr 行=100px，height:100%→100px，width auto→1:1 ratio→100px）。**dump（裁决：BUG 确证）**：flex h=400 / grid h=400（100% of flex✓）/ **img h=400 w=400**——img `height:100%` 解析到 **400（= grid 容器高）而非其 1fr track（100px）**！应 img h=100（track 1fr=400×1/(1+3)=100）。**PIL**：ZW green bbox **(8,406,51,450)**（img ~400×400，覆满 flex 列）vs CHR green bbox **(10,22,60,64)**（~100×100 绿方块）——img 过大 4×。**裁决：真 isolable layout bug**——**ZW 对 grid-item 的 `height:%` 解析到 grid 容器高度（400）而非 grid item 所在 row track 的高度（1fr=100）**，违反 css-grid §11（grid item %size 应对 its grid area/track 解析，definite 时如 1fr/定长算出后）。**fix scope（hand-off code-agent，clean lever）**：grid-item 百分比 size 解析须对 **item 的 grid track（row height for %height / column width for %width）** 而非 grid 容器；定位 grid item sizing 代码（layout grid-item containing-block 解析，类似 R119 CB 语义但 grid track 维度）→ 本案 34%→~0% PASS；潜在多案 yield（replaced-element-...-001 9.00% + 其他 grid-item %size 案）。**★ 意义**：css-grid PIVOT 首案即现 clean lever（R691 grid-item %height，34%）——继 R689（css-position static-inset）后**第 2 个非 css-tables clean lever**，且 css-grid 是第 4 采样目录；方法论再证：R670 判 css-grid top 为 baseline-synthesized（R266）结构性，但**次高非 baseline 案（34%）实证为 clean isolable bug**（grid track CB 解析缺陷），非「css-grid 全结构性」。hand-off code-agent：grid-item %size 对 track 解析是 css-grid 当前最高价值 lever（34%→PASS，+潜在多案）。零源码（LAYOUT_DUMP read-only + product-smoke read-only + PIL，未改任何文件）。
