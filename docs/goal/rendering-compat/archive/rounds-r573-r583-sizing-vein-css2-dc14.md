# 归档：R573–R583（sizing vein 收尾 + multicol/Phase-A 诊断 + CSS2 子目录 DC-14 重跑 era）

> 从 master.md 迁出（2026-06-25 R604 archiving），内容 100% 保留。综合结论见 master.md「综合裁决」point 2 + R589（sizing vein 闭环）/ R580（CSS2 aggregate 46.0%）。

**R583（★ LANDED — table-cell min-width/max-width 列宽约束；CSS Tables §17.5.3；normal-flow self-source 净 +1 PASS / 0 回归；R553 后首个 clean code yield）**：R582 CONTINUE 后查 R546 暴露/R548 残余 applies-to 队列。**根因**：`compute_column_widths` 的 `cell_used_width`（table.rs:1091-1126）读 cell `width` 但**完全不读 cell `min-width`/`max-width`**——R548 只给 col/colgroup 加了 min/max（resolve_col_min/max），cell 级遗漏。故 `min-width:1in` on empty `display:table-cell`（min-width-applies-to-007）→ 列宽取 intrinsic=0 → 空白无 96px 黑方（ref darkpx 9905 vs ZeroWeb 1649 仅描述文字）。**修复**（table.rs cell_used_width 末尾 +14 行）：读 cell min/max-width（Px），按 §10.4 顺序 `w.min(max).max(min)`（cap 后 floor，min 优先于 max），gated on Px（同 resolve_col_min/max）→ 仅影响显式声明 min/max-width 的单元格。**A/B（test-guard）**：min-width-applies-to-007 **FAIL 1.72%→PASS 0.00%**；normal-flow **638→639 净 +1 / 0 回归**（css-tables 79→79 不变，blast radius 极小——gated 仅 Px min/max-width 单元格）。门禁全过：make test 全绿、clippy −D warnings clean（layout-engine）、product-smoke welcome **16.97%**（<20% neutral，welcome 无 cell min/max-width）。残余：min-width-applies-to-008/013/014 仍 FAIL（display:inline/table-row-group 等，min-width 不适用或不同机制——008 inline 按 §10.2 不应生效，013/014 table·inline-table position 偏移，独立 lever）。★ **clean code lever 首次破 plateau**（R554-R582 净 yield=0 谱系后），证 R546「applies-to 残余非全结构性」——cell 级 min/max-width 是 R548 col/colgroup 的对称遗漏，clean 单点。详见 commit。

**R582（Phase 2a H2 probe 证伪 + blocks-017 结构性 = clean 单会话 lever 第 8 次确证穷尽；零代码 yield，纯 probe 负结果）**：承 R581 CONTINUE「实施 Phase 2a」按 handoff §6 先 probe 假设 H1/H2。**H2 证伪**：`inline_finalization.rs:944-948` col_ctx 仅设 vertical/text_align/inline_block_sizes（**无 white-space/word-break/overflow-wrap/float_exclusions**），而 `inline/mod.rs:collect_inline_items`（691-741）从真实 styles 读 per-element font 度量但 white-space/word-break/overflow-wrap 是 config 级（默认 normal）→ **H2 仅对 white-space:normal+word-break:normal+overflow-wrap:normal 子集成立，而该子集 paint 空-styles+overrides 本就正确（overrides 由 store_font_sizes_from_ifc 完整填充无 R84 缺口）→ Phase 2a 净 0**（纠正 R581 §1 发现 2「font 一致性 yield」乐观推断）；对 pre/break-word/break-all multicol blanket store+consume **回归**。进一步 col_ctx minimal config **不可复用于 Phase 2b**（混合内容须 rich config）→ R581「复用已算 col_ctx」成本节省前提不成立，Phase 2a 退化为净 0 speculative scaffolding 且 2b 被 Phase A 阻塞。**裁决：Phase 2a 当前 spec 不实施**（按 handoff §6「违反 H 即回滚」+ code-guidelines §2 不投机；重启须前置 ① 具体 multicol font-度量驱动 yield 案 或 ② Phase A 先行）。详见 [`multicol-layoutside-IFC-implementation-handoff.md`](./multicol-layoutside-IFC-implementation-handoff.md) §R582。**另查 R549 next-lever 队列 blocks-017（margin-collapse-with-border）= 结构性**：reftest 2.13% FAIL，像素分析 test 4 黑条 y=145/185/212/252（兄弟间距 40/27/40 跨 table↔table/table↔p/p↔p **不一致**）+ table 子 15px / p 子 19px 高（均应 20px）+ ref 为 table 基布局（border-spacing 均匀 20px）= margin-collapse + table intrinsic sizing + height 多系统交互（同 R568 abspos-width-005 谱系，R177 table 盒/R304 taffy DEFER 范畴），非单点。R549 队列（currentColor R550 done / blocks-017 结构性 / abspos-width-005 R568 结构性）全清。★ **clean 单会话 lever 第 8 次确证穷尽**（R305-R558 + R568×2 + R576 + R577 Phase A + R578 multicol + R579 代码级 + R581 handoff + R582 probe）。目标维持 firm structural plateau；剩余前向全多 session 架构（Phase A IFC 统一 → 解锁 multicol Phase 2b；flex-baseline 须 taffy 升级 R304；writing-modes 轴 R114/R164 4 轮证否）。

**R581（multicol layout 侧 column-aware IFC 函数级实施 handoff 产出：R579 代码级发现修正两个 R381 spec 的 Phase 2a 设计；零代码风险 spec = 多 session 架构首个可执行蓝图；★揭示 col_ctx 已算好被丢弃 + multicol paint 永远空-styles 重跑）**：承 R580 CONTINUE「产 multicol layout-side column-aware IFC 非交互式函数级实施 handoff」。spec-rfc skill 需用户 Q&A（与「不要向用户提问」冲突）+ 两份 R381 spec（column-aware-IFC-spec v1.0 Phase 1 A1-gate 已证伪关闭 / multicol-phase2-unified-column-flow-spec v1.0 Phase 2 混合内容目标案被 Phase A R109 阻塞）已存，故本轮产出**代码级修订版 handoff**（[`multicol-layoutside-IFC-implementation-handoff.md`](./multicol-layoutside-IFC-implementation-handoff.md)），核心 = 两个 R381 spec 均**未捕获**的 R579 代码级事实。**发现 1**：`inline_finalization.rs:942-956`（remeasure_inline_only_containers）balance 模式已用 `balance_column_geometry`（live，multicol.rs:120）的列宽 `cw` 跑 `col_ctx` IFC（**真实 styles**）算 balance 高度，**随后丢弃 col_ctx.lines**——pure-inline balance 的「列宽行盒」是已算好资产被丢弃，非缺口。**发现 2**：paint `text.rs:807` `use_stored = multicol_info.is_none() && ...` → multicol 容器**永远不走 stored 路径**，必落 `ctx.layout(doc, node_id, &HashMap::new())`（空 styles），且 `compute_final_inline_layouts` `inline_finalization.rs:284-286` 的 `if root.is_multicol { return }` gate **主动跳过** multicol → multicol 永远享受不到 R84 stored-font_size 修正。**意义**：两 R381 spec 假设 Phase 2a 须「新建 column-aware IFC / 统一 flow」；R579 事实把它**缩小为「存储接线」**（捕获 col_ctx.lines → 存 LayoutBox 新字段 `inline_multicol_lines`（复用现有 InlineLayoutLine 不新建类型）→ paint use_stored 消费）。★ **纠正 R579「Phase 2a = 净 0」**：R579 判 net-0 基于「paint even-split 已正确 R200」，未发现发现 2（空-styles 重跑），故 Phase 2a 真实结果区间 = {net-0 / 小正收益（font 一致性首获 R84 修正）/ 小回归（multicol-fill-auto-001 font_size 耦合，R198/R209 谱系）}，须 A/B 裁定而非预设净 0 → **有可测 yield 假设**（区别 R579「无 immediate-yield」）。**handoff 给出**：Phase 2a 精确函数级改动（4 文件 Step1-4，env `MULTICOL_STORED_LINES` 门控，sentinel multicol-fill-auto-001，三结局 A/B 判定，首版列分配与 paint even-split byte-identical 隔离 font 变量）+ Phase 2b（混合内容，前置 Phase A R383 R109 entanglement）+ Phase 2c（嵌套/breaking，paint 4 轮 ruled out 须 layout）+ 3 假设 H1/H2/H3（首步 probe 验证，违反即回滚）+ 代码变更边界（禁改 text.rs:713 门控 / taffy-local / multicol.rs 列分配算法；禁新建 ColumnFragmentationContext 类型）。★ clean 单会话 lever 仍第 7 次穷尽（本轮零代码，纯 handoff）；★ 这是多 session 架构首个**可执行**蓝图（区别前 7 轮「无 slice 诊断」）——下一会话可据 §2 直接实施 Phase 2a 并 A/B 裁定三结局。详见 [`multicol-layoutside-IFC-implementation-handoff.md`](./multicol-layoutside-IFC-implementation-handoff.md)。

**R580（CSS2 DC-14 aggregate 精确化 94% 覆盖：chr<1% 44.1%→46.0% +1.9pp；10-dir DC-14 headline ~36.2%→~37.1% +0.9pp；21 子目录 12 真 win；★CSS2 LANLED fix 真改善聚合真目标；零代码变更）**：spec-rfc skill 需用户 Q&A（与任务「不要向用户提问」冲突）+ 高层设计 doc 已存，故转具体零风险：CSS2 21/47 子目录（94% 分母 5694/6055）post-LANLED 重跑精确化。**结果**：lists +8.5pp（R546 counter-ref 解封 +73 case）/ abspos +18.5pp / values +12.5pp（R547/R544 ex）/ normal-flow +4.4pp / backgrounds +3.2pp / cascade +3.1pp / positioning +2.2pp / mpc +1.8pp / borders +1.8pp / linebox +1.5pp / text +0.7pp / selectors +0.6pp（12 真 win）；syntax/floats/colors/ui flat；tables/gc/fonts/box-display/floats-clear jitter-flat（text-family near-miss）。**CSS2 aggregate = 2863/6228 = 46.0%**（R484 44.13% → **+1.84pp/+191 case**；R569-R575 估 45.2% 上修，lists 等新子目录上推）。**10-dir DC-14 headline = (998 非 CSS2 + 2863 CSS2)/(4168+6228) = 3861/10396 = ~37.1%**（R484 36.2% → **+0.9pp**）。★ CSS2 LANLED fix（ex/inherit/border-w/currentColor/presentational/col-w/canvas-bg/bg-pos/counter-ref）跨子目录协同真 yield，sizing/border 真 win vs text jitter 两极分化在 CSS2 全量再证。详见 [`evidence/r580-css2-aggregate-finalized-2026-06-24.txt`](./evidence/r580-css2-aggregate-finalized-2026-06-24.txt)。

**R579（multicol layout-side column-aware IFC 代码级调查：确认 multi-session——layout_multicol 仅操作 block 子元素，简单 text-multicol paint even-split 已正确 R200，layout-side Phase 2a = 净 0 foundation 非 immediate-yield slice；clean lever 第 7 证穷尽；下一步 = spec-rfc 详细实施 spec）**：承 R578 读 multicol.rs/inline/ 代码确认。**代码级事实**：①`layout_multicol`（multicol.rs:244）仅操作 `container.children`（block 子）→ `assign_children_to_columns_*` 分列，**不触及 inline 行盒**；②简单 text-multicol（column-count:N + 直接文本）paint even-split `target_h=total/col_count` 已正确（R200 证 = chromium §8 顺序填充），类 A 残余是精度非算法；③失败类全结构性（B 混合 has_in_flow_children 门控 / C 嵌套 breaking / D baseline R316 ruled out）；④layout-side column-aware IFC = 跨 4 子系统协调（IFC 接 ColumnFragmentationContext / multicol 喂列几何 / 存 inline_column_lines / paint 消费删重算）= 多 session。★ **Phase 2a（简单 text-multicol 分布）= 净 0 foundation 非 immediate-yield**（简单 case paint 本就正确，layout-side 替换净 0，仅 complex case foundation）。★ **clean 单会话 lever 第 7 次确证穷尽**（设计 doc + 代码级双层评审）。★ **下一步 = spec-rfc 详细实施 spec**（设计 doc §R317 明示「多会话 spec-rfc」；现有 §2 高层架构缺函数级实施 spec）= 多 session 架构首个具体可交付物（零代码风险），区别前 7 轮诊断。详见 [`evidence/r579-multicol-layout-code-investigation-2026-06-24.txt`](./evidence/r579-multicol-layout-code-investigation-2026-06-24.txt)。

**R578（multicol fragmentation 设计 doc v0.4 评审：paint-side 4 轮 ruled out（R157/R198/R203/R317），layout-side column-aware IFC = 多 session，Phase 1 死字段属投机 scaffolding 勿过早加；与 R577 Phase A 共证全 forward path 无 clean 单会话 slice；★目标达 firm structural plateau；零代码 yield）**：承 R577 CONTINUE 读 multicol-fragmentation-design.md scope layout-side viable-slice。**结论 = 与 Phase A 一致无 clean slice**：①Round 1' baseline-export = 后处理侧 ruled out（R316 四轮，须 taffy 升级 R304 DEFER）②Round 2' breaking = paint-side 4 轮 ruled out（R317 放宽门控净 -5 回归），真修复须 **layout-side column-aware IFC**（设计 doc §R317 明示「多会话 spec-rfc」）③Round 3' column-rule 非单点安全 + 精度 = advance-width R225 死路。碎片化算法 `assign_children_to_columns_*` 已存在（R201），缺口 = layout-side 接线到 inline 列分配。★ **Phase 1「死字段 + 测量」勿过早加**（code-guidelines §2 投机性 scaffolding：测量 R201 已做，死字段未消费应在 Phase 2 按需加非现在空加）。★ **clean 单会话 lever 第 6 次确证穷尽**（R305-R558 + R568×2 + R576 + R577 Phase A + R578 multicol）——**全部 master forward path（Phase A/multicol/flex-baseline/writing-modes/taffy/R177/伪元素）经设计 doc 评审无一有 clean 单会话 slice，全多 session 架构**。★ **目标达 firm structural plateau**：DC-14 测量全面（9 非 CSS2 + 7 CSS2 子目录 56%），clean lever 6 证穷尽；剩余推进 = 多 session 架构承诺，优先 multicol layout-side column-aware IFC。详见 [`evidence/r578-multicol-fragmentation-design-review-2026-06-24.txt`](./evidence/r578-multicol-fragmentation-design-review-2026-06-24.txt)。

**R577（Phase A IFC 统一 viable-slice scoping：读设计 doc v1.2/v1.3 确认无 clean slice——存储已 R207/R355 放宽、offset 非阻塞 R306 证伪、真阻塞 = multicol fragmentation + 换行精度非 IFC 统一、墙③非 lever、M2 撞 R125/R198/R205 耦合；clean lever 第 5 证穷尽；零代码 yield）**：承 R576 CONTINUE 按 R355/R358/R362 先例 scoping Phase A 最窄 viable-slice。**结论 = 设计 doc 自身（v1.2/v1.3 post-R306/R378）已确认无 clean slice**：①存储已 R207/R355 放宽（Gate 2 过时，inline_finalization.rs:300-339 覆盖直接文本 block + 纯 inline 叶容器）②offset/baseline 非阻塞（R306 证伪几何基线 font-051 16.67%FAIL vs 默认 offset 0.00%PASS，当前 offset load-bearing 正确）③**真硬阻塞 = 墙② multicol fragmentation + 换行精度**（非 IFC 统一）④墙③混合内容移除非 lever（47 失败案无一依赖，移除重演 R206 回归）⑤M2 multicol Path B 真实 font_size 撞 R125/R198/R205 font_size+line-height 耦合 net-negative。★ **clean 单会话 lever 第 5 次确证穷尽**（R305-R558 + R568×2 + R576 + R577）。★ **剩余前向全多 session 架构且互相阻塞**：multicol column-aware IFC（Phase A 真阻塞，master 记 R157/R198/R203/R317/R122 五轮 paint-side 死路须 layout 侧；multicol-fragmentation-design.md）/ flex-baseline-export（R266 ruled out）/ writing-modes 轴（R114/R164 4 轮证否）/ taffy intrinsic sizing（R304 DEFER）/ R177 表盒宽度 / 伪元素 marker inline box（R554/R576）。47 self-source 失败 = multicol 13 / flex-baseline+abspos 5 / writing-modes 5 / taffy / large-font 2，全各架构簇无单点。详见 [`evidence/r577-phasea-viable-slice-scoping-2026-06-24.txt`](./evidence/r577-phasea-viable-slice-scoping-2026-06-24.txt)。

**R576（POLLUTED gross 簇挖掘 + list-style-position 调查：无 clean 簇，list-style-position = paint-side marker 启发式 R554 谱系 entangled；clean 单会话 lever 第 4 次确证穷尽；零代码 yield）**：改用**簇挖掘**（区别 R568 单点 edge-case）扫 R484 per-case raw POLLUTED gross（self 假通过 + chr>5%）。**35 case 分散多子目录，无 ≥3 同名簇**（除 list-style-position）。唯一簇 list-style-position（-018/-023/-024 chr 9.84/13.15/13.17% + applies-to-008/009/010 3.48%）：测试嵌套 `list-style-position:inside`，marker "1." 应为 li 首个 inline 子内容（嵌套 ol 后流动）。**ZeroWeb 实现 = paint-side 启发式**（painter/text.rs:346-348 `marker_x ± marker_size*N` 固定偏移，非 layout inline box）→ 无法表达 inside marker 作「首个 inline 子内容」语义 = **R554「伪元素 generated content 需 layout 独立 box」同架构 entangled**，非 clean（Outside 多过因 fixed 左偏移近似 OK，lists 81.9%；Inside 需 layout 集成）。★ **clean 单会话 lever 第 4 次确证穷尽**（R305-R558 + R568×2 + R576）。★ **剩余前向路径全多 session 架构**：①Phase A IFC 统一（~2200 案，narrow viable-slice + 充分 A/B，R247 高风险）/ ②marker/伪元素作 inline layout box（R554/R576 同谱系）/ ③R177 width:auto 表盒宽度 / ④multicol fragmentation。详见 [`evidence/r576-liststyleposition-polluted-mining-2026-06-24.txt`](./evidence/r576-liststyleposition-polluted-mining-2026-06-24.txt)。

**R573-R575（CSS2 子目录 DC-14 重跑第二批：margin-padding-clear/backgrounds/generated-content；2 真 win + 1 jitter，+17 case；7 子目录累计净 +79，CSS2 聚合估 ~45.2% +1.1pp；零代码变更）**：承 R569-R572 续 CSS2 第二批。**margin-padding-clear 272/682=39.9%（+1.8pp，R545/R549/R546）/ backgrounds 199/339=58.7%（+3.2pp，R491 canvas-bg/R508 bg-pos/R172）/ generated-content 75/226=33.2%（-6.1pp jitter，145/226=64% near-miss + >5% 仅 6 = 非回归）**。本批净 +17（+14/+11/-8）。★ **CSS2 7 子目录累计（R569-R575）净 +79 case**（borders+9/normal-flow+42/positioning+12/tables-1/mpc+14/bg+11/gc-8）。★ **CSS2 聚合估计 ~45.2%**（7 子目录重跑 3380/6055=56% + 余用 R484 基线 → 2751/6089）vs R484 44.1% = **+1.1pp**。★ **强化 DC-14 finding**：CSS2 sizing/border/layout 子目录 5/5 真 win（borders/normal-flow/positioning/mpc/backgrounds），唯一 text-family 子目录 generated-content jitter-flat——LANLED fix yield 按 fix-family 两极分化在 CSS2 内部再证。剩 ~31 子目录多 flat/text-family，分批续跑边际递减。详见 [`evidence/r573-r575-css2-subdirs-batch2-dc14-2026-06-24.txt`](./evidence/r573-r575-css2-subdirs-batch2-dc14-2026-06-24.txt)。
