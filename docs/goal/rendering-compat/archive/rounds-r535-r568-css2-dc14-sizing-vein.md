# 归档：R535–R568 逐轮详细记录（DC-14 oracle 重跑 + multicol/Phase-A 诊断 + CSS-correctness + sizing vein era）

**归档日期**：2026-06-25（R589 doc-maintenance 轮，超出「最近 20 轮」窗口迁出，内容 100% 保留）。
**范围**：R568（多证 clean lever 穷尽）→ R567/R566/R563-R565/R560-R562（DC-14 oracle 重跑 grid/tables/flexbox/multicol/text-decor/fonts/writing-modes）→ R559（css-position oracle +3.3pp 真 win）→ R558-R554（伪元素/R109 anon-box 多 session 诊断 + 回退）→ R553-R551（reftest 分母真实性：about:blank / trailing-space / abs-path ref 三波，+20 self PASS）→ R550/R549（currentColor / border-width medium LANDED）→ R502-reopen/R548-R544（presentational hints / col min-max / ex / inherit clean wins）→ R543-R535（product-smoke 门禁 / welcome 回归 / tokenizer hash / flexbox_flex 双 bug）。
**综合结论**：见 master.md 顶部「综合裁决」表（R305–R430 杠杆穷尽 + 两阶段 plateau）+ R589（sizing 子分支全闭环）/ R569–R588 条目。R527–R534、R491–R526、R484–R490、R342c–R480 各自另归。

---

**R568（2 sizing/border 候选 lever 调查 = 均复杂/结构性，clean 单会话 lever 再次确证穷尽；零代码 yield）**：承 R567「下一 lever 优先 sizing/border-family」指引，攻 2 候选均经像素+规范诊断判非 clean。① **abspos-width-005**：ZW-test abspos 盒 x=42-123 但**文本完全未渲染**（0 black px）vs ref x=25 文本溢出 x=426；两症状（静态位置左偏 ~17px + 长不可换行词未渲染）= abspos 静态位置+shrink-to-fit+overflow 多系统交互，非单点（R98/R498-R500 结构性残余）。② **table-cell-overflow-explicit-height-001/002**（R561 POLLUTED 41.60% self-false-pass 簇）：像素诊断主发散=**表宽**——ZW 无 width 表撑到 x=789 满宽 vs chromium shrink-to-fit x=159 = **R177 width:auto 表扩展（line1675 load-bearing 已 defer）entangled**；td-overflow:20px 非主发散（cell height=min 长到 300px R168 已正确）。★ **再次确证 master「clean 单会话 lever 穷尽」**（R305-R558 + R568 多证）。★ **下一前向路径**（均多 session 或零风险测量）：① Phase A IFC 统一（~2200 案，须充分 A/B 避 R554 net-negative，R247 高风险）/ ② **CSS2 目录 oracle 重跑**（最大分母 6055，R484 44.1%，未 post-LANLED 重跑；逐子目录分批避 OOM；零风险完成 DC-14 主导分母快照）/ ③ 伪元素独立 box 架构（R554 前置）。详见 [`evidence/r568-two-levers-complex-structural-2026-06-24.txt`](./evidence/r568-two-levers-complex-structural-2026-06-24.txt)。

**R567（css-writing-modes DC-14 oracle 重跑 + ★DC-14 oracle 重跑 9/9 非 CSS2 目录全部完成里程碑；chr<1% 5.6%→5.9% FLAT；★最大 self-vs-chr gap 72pp；零代码变更）**：writing-modes（最后非 CSS2 目录，788 case 430.85s）。chr<1% **46/784=5.9%（R457 基线 44/784=5.6%，+0.3pp FLAT）**，96 tight-band，self-source 615/788=78.0%。>5% 带 199 case = vertical-rl/R109/orthogonal 结构性（R114/R142/R164 多 session）。★ **writing-modes self 78.0% vs chr<1% 5.9% = 72.1pp gap（迄今最大）**。★ **★ DC-14 oracle 重跑 9/9 非 CSS2 目录全部完成（R559-R567，2 session）★**——综合：①LANLED fix yield 两极分化，**sizing/border 类**（position +3.3pp / tables +2.3pp）真 moved chr<1%，**text/inline 类**（grid -6.9pp / flexbox -2.1pp / text +1.1pp）self-source win 但 chr<1% jitter-flat；②self-source 对 text/font/writing-mode 目录高估真实正确性 64-72pp（writing-modes 72pp / css-text 70pp / text-decor 68.7pp / fonts 63.8pp）；③DC-14 是唯一可信达标指标。★ **下一 lever 可操作指引**：优先 sizing/border-family（proven 真 yield），避免 text/inline-family（proven self-false）；R549/R550 next-lever 队列 abspos-width-with-border / margin-collapse-with-border（sizing/border）= 高 yield 候选。详见 [`evidence/r567-cswritingmodes-dc14-oracle-revalidate-2026-06-24.txt`](./evidence/r567-cswritingmodes-dc14-oracle-revalidate-2026-06-24.txt)（含 9 目录全景表）。

**R566（css-text DC-14 oracle 重跑 post-R537/R542：chr<1% 18.8%→19.9% +1.1pp modest jitter-influenced；★最大 self-source 假通过目录 self 90.1% vs chr<1% 19.9%=70pp gap；零代码变更，复用 R471 1912 oracle）**：css-text 是余下非 CSS2 目录中唯一有 LANLED 影响（R537 nowrap + R542 min-content 影响 text 布局）+ 最大分母（1650 case 比较）。后台运行（1826 loaded，994s，**peak RSS 4956MB** 接近 test-guard 6GB 阈值 = OOM-prone 名副其实但安全完成）。**chr<1% 329/1650=19.9%（R471 基线 263/1402=18.8%，+1.1pp modest）**，326 tight-band，self-source 1646/1826=90.1%。★ R537/R542 LANLED text fix 真目标影响**有限**（与 text-decor/fonts FLAT 一致）。★ **css-text = 最大 self-source 假通过目录**：self 90.1% vs chr<1% 19.9% = **70.2pp gap**（1086 case 1-5% near-miss text 布局/度量噪声）——若看 self 会误判「接近达标」。归因非 fontdue 光栅化（R388 已证一致）是布局/度量。**★ DC-14 oracle 重跑 8/9 非 CSS2 目录完成**（剩 writing-modes R457 5.6% 结构性低信息留后续）。★ **综合结论**：LANLED fix 两极分化——sizing/border 类（position/tables）真 moved chr<1%（+3.3/+2.3pp），text/inline-layout 类（grid/flexbox/text）self-source win 但 chr<1% jitter-flat；self-source 对 text/font 目录高估真实正确性 64-70pp，DC-14 是唯一可信达标指标。详见 [`evidence/r566-csstext-dc14-oracle-revalidate-2026-06-24.txt`](./evidence/r566-csstext-dc14-oracle-revalidate-2026-06-24.txt)。

**R563/R564/R565（DC-14 oracle 重跑批量：multicol/text-decor/fonts 三目录，全 FLAT 无回归；★揭示 text/font 目录 ~64-69pp self-source 假通过 gap；零代码变更）**：承 R559-R562 继续 DC-14 重跑至余下非 CSS2 目录。3 目录均无 LANLED fix 自各自基线 → 预期 FLAT + 测 R549/R550 跨目录回归。**结果全 FLAT**：css-multicol 105/452=23.2%（基线 23.5%，-1 jitter，tight 47）/ css-text-decor 70/242=28.9%（基线 28.9%，+0 EXACT）/ css-fonts 98/282=34.8%（基线 34.8%，+0 EXACT）。★ **裁决 1 无回归**：R549/R550 border fix 未回归 multicol/text-decor/fonts。★ **裁决 2 严重 self-source 假通过（DC-14 最有力实证）**：css-text-decor self-source **97.6%** vs chr<1% **28.9%** = **68.7pp gap**（157 case 1-5% near-miss，69 tight）；css-fonts self-source **98.6%** vs chr<1% **34.8%** = **63.8pp gap**（139 near-miss）——self-source 通过率严重高估 text/font 渲染正确性，若看 self 会误判「已达 95%」。归因 = 布局/度量（line-height/text-decor 位置厚度/font metric sub-px/多行结构）**非 fontdue 光栅化**（R388/AA-baseline 已证一致）。★ **DC-14 oracle 重跑 7/9 非 CSS2 目录完成**（position/tables 真 win，grid/flexbox jitter-flat，multicol/text-decor/fonts FLAT；剩 css-text 18.8% OOM-prone + writing-modes 5.6% 结构性低信息留后续）。详见 [`evidence/r563-r565-dc14-multicol-textdecor-fonts-flat-2026-06-24.txt`](./evidence/r563-r565-dc14-multicol-textdecor-fonts-flat-2026-06-24.txt)。

**R560/R561/R562（DC-14 oracle 重跑批量：grid/tables/flexbox 三目录，复用 R559 工作流；2 真 win + 2 jitter-flat；零代码变更）**：承 R559（css-position +3.3pp 真 win 确立可复用工作流 = 复用现有 oracle PNG + REFTEST_DUMP + cross-validate.py），对 R559 列出的其余 LANLED 目录批量重跑 DC-14 真目标。**横向对比**：| 目录 | LANDED fix | baseline | post-fix | delta | 真目标 yield |——css-position(R559) R498-500/R508/R549/R551/R552 36/95=37.9%→40/97=41.2% **+3.3pp ✅真win**；css-tables(R561) R177b/R168/R548/R549 49/112=43.8%→**53/115=46.1% +2.3pp ✅真win**；css-grid(R560) R143/R435/R536/R537/R542 19/48=39.6%→16/49=32.7% -6.9pp ⚠️jitter-flat；css-flexbox(R562) R435/R536/R537/R429 251/496=50.6%→241/497=48.5% -2.1pp ⚠️jitter-flat。★ **核心 DC-14 anti-false-pass 实证**：position/tables LANDED fix 真 moved chromium 真目标（+3.3pp/+2.3pp），grid/flexbox LANDED fix 是 self-source win（test/ref 同向变化，self match baseline 67.3%/74.0%）但 chr<1% 在 near-miss 带 jitter 内 flat-to-down——**self-source 改善≠真目标改善**。grid -3 / flexbox -10 全由 brittle 1% 阈值带 jitter 解释（grid 9 case + flexbox **51 case** 落 1.0-1.5% 紧邻阈值带，多 exactly 1.00-1.02% POLLUTED；真实回归会显 >5% 带增长，此处 >5% 带全已知结构性簇），非代码回归。oracle PNG 全 2026-06-21 稳定 ground truth。★ **可复用工作流定型**：①`rm -rf target/reftest-dump` ②`REFTEST_DUMP=1 REFTEST_DUMP_PASS=1 ./target/test-guard -- cargo run --bin zero-wpt-runner -- reftest-upstream <dir>` ③`cross-validate.py --dump target/reftest-dump --oracle oracle-shots` → 3-bucket（chr<1% / 1-5% / >5%）+ jitter 诊断（tight 1.0-1.5% 带计数）。master 顶部基线 3 目录 chr<1% 已更新（grid→16/49、tables→53/115、flexbox→241/497）。下一步 DC-14 = 余下 LANDED 目录（css-text R471 18.8% / css-multicol R427 23.5% / writing-modes R457 5.6% / fonts/text-decor）oracle 重跑，或据本批 yield 区分（position/tables 类 sizing/border 真修 vs grid/flexbox 类 self-win 假改善）定向下一 code lever。详见 [`evidence/r560-cssgrid-dc14-oracle-revalidate-2026-06-24.txt`](./evidence/r560-cssgrid-dc14-oracle-revalidate-2026-06-24.txt) + [`r561-csstables-...`](./evidence/r561-csstables-dc14-oracle-revalidate-2026-06-24.txt) + [`r562-cssflexbox-...`](./evidence/r562-cssflexbox-dc14-oracle-revalidate-2026-06-24.txt)。

**R559（css-position DC-14 oracle 重跑 post-R549/R551/R552：chr<1% 37.9%→41.2% +3.3pp/+4 case；实证 landed fix 改善真目标；零代码变更，复用 R404 105 oracle PNG）**：R404（2026-06-22）css-position 全量 oracle 基线 36/95=37.9% chr<1%，其后 R493/R495/R498/R500（abspos stretch）+R508（bg-position）+R549（border-width medium）+R551/R552（ref 解析）LANDED 但 oracle 未重跑（master 标 stale）。R559 复用现有 105 oracle PNG + REFTEST_DUMP ZeroWeb test（97 case）+ cross-validate.py 产出 post-fix 数据：**chr<1% 真一致 40/97=41.2%（+3.3pp/+4 case vs R404），chr<5% broad 90/97=92.8%**，self-pass 53/97，污染 14（26.4%）。★ **证明 R549/R551/R552 LANDED fix 真改善 DC-14 真目标**（css-position chr<1% +4 case），非仅 self-source 通过率——R549 解 abspos border-width 计算缺口（css-position 多 abspos+border case），R551/R552 让此前被排除 case 入分母（97 vs 95）部分 chr<1%。master 顶部基线 css-position 36/95=37.9% 更新为 **40/97=41.2%**。剩余 57/97 chr>1% = abspos shrink-to-fit/百分比 inset/stacking/writing-mode 结构性（master 已归因）。下一步 DC-14 = 其它 LANDED 目录（tables R177b/R168/R364、flexbox R435/R536/R537、grid R143、borders R549/R550）oracle 重跑量化各自真目标改善。详见 [`evidence/r559-cssposition-dc14-oracle-revalidate-2026-06-24.txt`](./evidence/r559-cssposition-dc14-oracle-revalidate-2026-06-24.txt)。

**R558（⛔ REVERTED — R109 case#2 block 容器直接文本子匿名块盒；net-negative：content-047 -0.10pp 仍 FAIL + normal-flow -6 回归；零代码落地）**：承 R557b（content-047 真根因 = R109 匿名块盒）攻最小切片。tree.rs:640-649 block 容器（非 flex/grid）else 分支当前**仅收集 Element 子**（line 645），跳过直接文本子 → inject 注入的 body:before 文本被丢弃。R558 改 else 分支对非空直接文本子创建匿名 block taffy leaf（仿 flex/grid tree.rs:530-543）。编译通过。**A/B net-negative**：content-047 1.29%→**1.19%**（仅 -0.10pp 仍 FAIL）+ **css/CSS2/normal-flow baseline ~639→633 = -6 回归**。→ **REVERTED**（git checkout，工作树=R557b HEAD 6727db9b）。**根因深化**：block 容器直接文本当前通过 **paint IFC Path B**（paint_text 重跑 IFC 读 DOM 文本）渲染（不依赖 build_subtree taffy 子），故 css-text 89% PASS；R558 让文本进 build_subtree 匿名 leaf（layout 高度）+ paint IFC 仍渲染 = 双重/几何不一致 → normal-flow -6 回归；content-047 1.29% 主要来自 paint IFC Path B 对混合内容文本定位（非 build_subtree 跳过），layout 匿名 leaf 触及不到 → 仅 -0.10pp。★ **真解 R109 case#2 须协调 layout 匿名块盒 + paint IFC Path A/B 统一（Phase A IFC 统一前置），非 build_subtree 单点**——与 R554（伪元素独立 box）/ Phase A（墙② multicol）同属多 session 架构，勿再以 build_subtree 匿名 leaf 单点重试（R361 高风险 + R558 实证）。本会话 R554-R558 五轮净代码 yield=0（R554/R558 全回退 + 诊断），全面确认单会话 clean lever 穷尽 + R109 case#2/伪元素/Phase A 全多 session 架构（单点均 net-negative，R554/R558 双证）。详见 [`evidence/r558-r109-case2-block-text-anonbox-reverted-2026-06-24.txt`](./evidence/r558-r109-case2-block-text-anonbox-reverted-2026-06-24.txt)。

**R557（generate/cascade near-miss 扫描 = 落伪元素 generated-content 架构 R554 谱系 + R109 匿名块盒；零代码变更）**：R556（backgrounds）后扫 generate（205/226）+ cascade（38/40）。generate 低 diff 失败聚类全落**伪元素 generated-content / R109 匿名块盒 / 定位交互**：content-047（1.29%）= `body:before { content: attr(background) }`；before-after-positioned-002/003/004（2.08%×3）= 伪元素 position:fixed；content-174/171/175/113/122（1.58-1.84%）= generated content 簇。cascade 2 FAIL = html-precedence-004（级联优先级）/ cascade-import-001（嵌套@import+data:URI，R551 已判结构性）。0 独立单点 clean lever。**★ R557b 修正（INJDBG 插桩实证）**：content-047 **不是**伪元素 inject 缺口——inject **成功**为 body:before 注入文本节点 "PASS PASS"（`inserted=true`），但 LAYOUT_DUMP 该节点**不在 layout 树**。真根因 = body 现含 **[inline text 注入] + [p block]** 混合 inline+block 内容，ZeroWeb **不生成匿名块盒**（CSS §9.2.1.1，R109 谱系，同 border-bottom-width-006 R361 / R255 ua_default_display）→ 注入的 inline text 被忽略。REF 用显式 `<div class=test>` block 无混合内容。**修正初判**：content-047 = **R109 匿名块盒**（非 R554 伪元素；R554 content:counter/list 仍是伪元素 inject 跳过）。两个不同缺口勿混淆。R109 匿名块盒生成是多 session 架构（单会话高风险）。本会话 R554-R557 四轮诊断净代码 yield=0，全面确认单会话 clean lever 穷尽 + 剩余 forward motion 全多 session 架构（伪元素独立 box R554 / R109 匿名块盒 / Phase A IFC 墙② multicol / multicol fragmentation / baseline-export）或慢 OOM-prone oracle。详见 [`evidence/r557-generate-cascade-pseudo-arch-2026-06-24.txt`](./evidence/r557-generate-cascade-pseudo-arch-2026-06-24.txt)。

**R556（backgrounds near-miss 单点扫描 = 0 clean win，全特性/结构性；印证 near-miss clean-win 穷尽；零代码变更）**：R555（轨道1 import 穷尽）后转 CSS-correctness 单点扫描（R351 方法）。跑 css/CSS2/backgrounds self-source（339 cases = 287 PASS / 52 FAIL），低 diff 失败聚类 REFTEST_BBOX 逐案定位全特性/结构性：① **background-attachment-applies-to-001~014（2.40%×14）**=display:table-row-group 等 table-internal 的 background-attachment:**fixed**+repeat-x，bbox x=[8,103] y=[16,159] 整个 table 区域 background 整体差（taffy 0.7 table 模型对 table-row-group box/background，R177 table 谱系结构性）；② **background-*-applies-to-014（1.39%簇）**=display:**inline-table** background（同结构性）；③ **background-intrinsic-004（1.37%）**=intrinsic-ratio-only SVG（无固有尺寸按比例 cover bg positioning area）background sizing 特性缺口。0 单点 clean lever，印证 master「near-miss clean-win frontier 穷尽」（R352/R354/R363/R365/R369b 多轮 0/N 衰减；R525-R546 clean win 来自 CSS-correctness 单点+缺失资产，非 near-miss）。**本会话 R554-R556 净代码 yield=0**（R554 全回退 + 纯诊断），诊断全面确认**单会话 clean lever（CSS-correctness 单点 + near-miss + 分母 import）已穷尽**，剩余 forward motion 全是多 session 架构（Phase A IFC 统一 ~2200 案 / 伪元素独立 box / multicol fragmentation / baseline-export）或慢/OOM-prone（chromium oracle）。下一步 = Phase A IFC 统一下一个 narrow viable-slice（设计文档 phase-a-IFC-unification-design.md；R355/R358/R362 三 slice 先例，须充分 A/B 避 R554 net-negative）或 chromium oracle 重跑（DC-14 真目标，R471 css-text 基线 18.8%，覆盖 R551/R552 新导入）。详见 [`evidence/r556-backgrounds-nearmiss-zero-clean-2026-06-24.txt`](./evidence/r556-backgrounds-nearmiss-zero-clean-2026-06-24.txt)。

**R555（轨道1 DC-14 分母去子集化诊断：css-text 全量确认 DONE / CSS2 多会话级 / R551 exposure 全结构性；零代码变更）**：R554 回退后转轨道1（R395 唯一非纯架构前向路径）。① **css-text 分母去子集化 = DONE**：`discover --category css/css-text --import`（幂等）=「发现 1302 个权威 reftest 对 / 0 文件新下载」→ 上游 1302 权威 reftest 本地全部已导入，0 缺失。runner 加载 css-text 1826 cases > 1302 是 ref-as-test 噪声（非分母缺口）。css-text 全量 self-source reftest 慢（1826 cases，结果末尾批量输出，全量跑易超时/OOM-prone per master 记录），本轮未跑完整 baseline（R463 已测 1402/1572=89.2%）。② **CSS2 全量 = 多会话级（一次性 discover 超时）但子目录分批 discover = 全部已全量（0 gap）**：`discover --category css/CSS2` dry-run 280s 超时（exit 124，~5000-7000，git/trees recursive + fetch raw 数千文件单次超时）；但逐子目录 `discover --import` 实测 cascade 32 / syntax 275 / colors 19 / visuren 34 **全 0 文件新下载**（已全量，R546 +14 共享 ref + 历史 import 覆盖）。「CSS2 全量 discover 超时」是工具限制非真实分母 gap。★ **轨道1 import yield 基本穷尽**：css-text（1302/1302）+ CSS2 子目录（0 gap）分母均已全量，剩余 DC-14 价值 = chromium oracle（真目标验证，慢/OOM-prone），非 import。③ **R551 暴露 7 self-FAIL 全结构性**（逐案确认非单点 clean lever）：cascade-import-001=嵌套@import+data:URI / padding-applies-to-016/017=inline span padding in table(IFC/Phase-A) / flexbox-align-self-baseline=baseline-export(R266 ruled out) / multicol-on-broken-image-alt-text=multicol+broken image / replaced-object-backdrop=dialog::backdrop DOM/JS OOS(R349) / dynamic-table-cell-height=JS-dynamic(reftest-wait+script)。7/7 结构性，0 单点，印证「单会话 clean lever 趋尽」。**本会话净代码 yield=0**（R554 全回退 + css-text 0 import gap + 纯诊断），诊断有价值：css-text DC-14 分母完成（下轮直接 oracle 不需再导）、CSS2 全量须分批、R551 exposure 非下轮 lever。下一步 = 轨道1 css-text chromium oracle（DC-14 真目标，capture-oracle-per-dir.mjs 慢/OOM-prone 须分批/限量）+ CSS2 分批导入；或渲染架构多 session（Phase A IFC 统一 / 伪元素独立 box R554 前置 / multicol fragmentation）。详见 [`evidence/r555-csstext-full-css2-multisession-2026-06-24.txt`](./evidence/r555-csstext-full-css2-multisession-2026-06-24.txt)。

**R554（⛔ REVERTED — content-list 多 token 拼接 + counter none style + 伪元素渲染；net-negative：generated-content self-source -1 回归 + 驱动用例 content-counter-001 未解，多 session 架构 lever，勿再以单点重试）**：承 R553 揭示的 content:counter() 缺口攻 `content: counter(c,none) "z"`（content-counter-001）。**实现**（6 文件跨 parse/computed/paint/pipeline）：①parse_content 加多 token content-list（split 顶层 token→List 变体）；②ContentValue/ContentComputedValue 加 List + apply 映射；③paint_content 提取 content_to_text helper（List 递归拼接 + counter `none` style→空串，CSS Counter Styles）；④lib.rs 伪元素存储条件放宽（Counter/List 也存 before_pseudo/after_pseudo）；⑤inject_pseudo_text_nodes 对 Counter/List 注入占位空文本节点（保留 content，paint_content 在 paint 时用 increment 后计数器渲染）。**单测全过**（css-parser 3 content-list + engine 3 List/none-style，paint_content List 拼接 + none style 单元验证逻辑正确）。**但 A/B reftest net-negative**：①**驱动用例 content-counter-001 未解**——self-source 仍 0.00% PASS（假通过），因伪元素（::before）的占位文本节点被 **layout 合并入 inline flow**（span 是 inline，文本子节点不生成独立 LayoutBox），paint_content 从不对伪元素 content 调用（box.style.content = 宿主 Normal，非伪元素 List）。占位 box 不独立 = paint_content 触发不到。②**generated-content self-source 205→204 = -1 回归**（content-173.xht newly-FAIL，`:before`/`:after` content 多行单引号字符串含 `\A` 转义，split-priority parse 误处理）；lists 156→156 不变。**裁决：REVERTED**（stash drop）。根因=**伪元素 generated content 渲染需 layout 层为伪元素生成独立 LayoutBox + paint 协调**（当前伪元素 content 经 inject 转文本节点合入宿主 inline flow，无独立 box 承载 content），非单点 parse/paint 修复可解；counters() 函数（87 用例）+ counter 值链（嵌套作用域）同属此架构 lever。**勿再以单点「content-list parse + counter none + 伪元素存储条件」重试**——已实证伪元素 box 架构是前置阻塞。下轮若攻此 lever 须先设计伪元素独立 box（layout 侧）。零代码落地（全回退，工作树 = R553 HEAD 612d0671）。

**R553（★ LANDED commit — reftest ref about:blank 支持；DC-14 分母真实性，R551/R552 谱系第三波；**加载器 Warnings 7→0 全清里程碑**；揭示 content:counter() 缺口）**：承 R552（加载器剩 1 Warning）攻最后一个分母缺口。**缺口**：`content-counter-001.xht` `rel="match" href="about:blank"`——about:blank 是 WPT reftest 特殊参考（空白文档，常用于 match「应渲染空白」），不是文件路径，`read_to_string` 报 No such file→case 被排除。**修复**：加载循环 read_to_string 前 +1 分支短路（`raw_ref=="about:blank"`→空 HTML，否则走 R551/R552 resolve 路径不变，~9 行）。**加载器 Warnings 7→0，分母缺口全清**（R551 绝对路径 + R552 trailing-space/缺失 ref + R553 about:blank 三波清理完成）。**A/B + DC-14 cross-validate（关键）**：content-counter-001 with-fix loaded=1 **self-source PASS 0.00%**，但 chromium headless 直接渲染 test（dark-pixel 计数）= **2.869% dark px（"zzz" 可见）vs ZeroWeb 空白** → **self-source PASS 是假通过**（DC-14）。测试 `content: counter(c, none) "z"` ×12 span：chromium `counter(c,none)`=""（none list-style）拼"z"="z"→可见；ZeroWeb 渲染空白 = `content: counter() "字符串"` 多 token 拼接未渲染（paint_content text.rs:509-526 单值枚举 String|Counter，不支持拼接；解析侧多 token 可能整体失败→Normal→空）。**裁决**：R553 真实价值=分母真实性（content-counter-001 从排除→进真分母可 cross-validate）+ 揭示 **content:counter() 多 token 拼接缺口 = 下一轮 R554 候选 lever**（可能解锁 generated-content counter 簇，提 chromium Oracle 一致率）。self-source +1 PASS 不计达标（DC-14 假通过）。零回归 by construction。门禁全过：make test 45 套 ok/0 fail、clippy −D warnings clean、rustfmt clean、product-smoke welcome 16.97%（<20% neutral，零渲染改动）。详见 [`evidence/r553-about-blank-ref-content-counter-gap-2026-06-24.txt`](./evidence/r553-about-blank-ref-content-counter-gap-2026-06-24.txt)。

**R552（★ LANDED commit — reftest ref trailing-space trim + 3 缺失相对 ref 导入；DC-14 分母真实性，R551 谱系第二波；self-source 净 +1 PASS / 4 诚实暴露 / 0 回归；加载器 Warnings 7→1）**：承 R551（加载器 Warnings 清单=分母缺口免费诊断），攻其中 6 个（第 7 个 about:blank 留后续）。**两类缺口**：① **trailing-space ref href**（1 case）：`border-collapse-005.html` 的 `href="border-collapse-005-ref.html "` 带尾随空格（上游 WPT 原始写法，python 全 wpt-data 扫描确认仅此 1 处）；浏览器按 URL 解析语义 strip 首尾空白，ZeroWeb `Path::join` 不 trim → 拼带空格文件名→No such file→case 被排除。**修复**：R551 的 `resolve_ref_path` 入口加 `let ref_path = ref_path.trim();`（1 行，符合浏览器 URL strip 语义，绝对/相对分支都受益，防回归）。② **缺失相对 ref 文件**（3 文件解 5 case，R546 模式）：`font-language-override-02-notref.html` / `pass_if_filler_text_match.xht`（被 bold/smallcaps 引用）/ `pass_if_filler_text_underlined-notref.html` 经 ~/use-proxy 代理从上游 raw.githubusercontent fetch 导入（零运行时网络依赖，一次性离线资产）。**A/B（test-guard reftest-upstream 逐案 stem 过滤）**：border-collapse-005 baseline loaded=0→with-fix +1 PASS(0.00%)；font-language-override-02 含 match+mismatch 双 link，mismatch-case(#1) baseline 被排除→with-fix +1 honest exposure(0.00% mismatch 假)；pass_if_filler_text_{smallcaps,bold,underlined} 各 baseline 被排除→with-fix 各 +1 honest exposure（0.00% mismatch 假，4 个 css/reference/「pass-if」描述式 mismatch 页面，测样式变体应与 notref 不同，ZeroWeb 渲染相同=暴露样式差异缺口，非 harness 伪影，mismatch 模式经 grep 确认）。**净 +1 PASS / +4 honest exposure / 0 回归**（by construction：baseline 被排除者无 PASS→FAIL）。**加载器 Warnings 7→1**（仅剩 content-counter-001.xht→about:blank 特殊 scheme，runner 需支持 about:blank，独立特性留后续）。门禁全过：make test 45 套 ok/0 fail、clippy −D warnings clean、rustfmt 我的文件 clean、product-smoke welcome 16.97%（<20% neutral，零渲染改动 welcome 字节相同）。clean win（基础设施分母真实性，提 DC-14 真通过率分母真实性）。详见 [`evidence/r552-trailing-space-and-missing-refs-2026-06-24.txt`](./evidence/r552-trailing-space-and-missing-refs-2026-06-24.txt)。

**R551（★ LANDED commit — reftest 绝对 WPT 路径参考文件解析；DC-14 分母真实性，R546 谱系；self-source 净 +20 PASS / 7 诚实暴露 / 0 回归）**：承接 R546（缺失共享 ref 文件）后扫 DC-14 分母真实性第二处缺口——**ref 文件已存在但路径解析错误**。**根因**：很多上游 WPT reftest 用绝对 WPT 路径引用共享参考（`<link rel="match" href="/css/reference/ref-filled-green-100px-square.xht">`），而 runner 加载器 `wpt_file_loader.rs` 用 `Path::join` 把 ref 拼到测试父目录上——但 Rust `Path::join` 对**绝对路径参数**（`/` 开头）会丢弃 base 从文件系统根查找 → `read_to_string("/css/reference/green.html")` 报 `No such file` → 加载器 `continue` 跳过**整条 reftest case**（不进 cases 向量）→ 该测试**被静默排除出分母**（DC-14 分母静默缩小，与 R546 缺文件同源但机制不同：R546 缺文件、R551 解析错）。**修复**（wpt_file_loader.rs +15/-1，单一 helper）：新增 `resolve_ref_path(wpt_data_dir, test_path, ref_path)`——绝对路径（`/`开头）相对 wpt-data 根解析（手动 `trim_start_matches('/')` 避免 join 丢 base 语义陷阱），相对路径相对测试父目录（行为不变）。**A/B（test-guard 包裹 reftest-upstream，逐案 stem 过滤 27 受影响测试）**：BASELINE 27/27 全 `loaded=0`（加载器 `continue` 排除→filter 集空，实证 bug）；WITH-FIX 27/27 `loaded=1` = **20 PASS + 7 诚实暴露 / 0 回归**。20 PASS（渲染本就正确，缺的只是 ref 可达性）：font-size-119 / 5 个 counter-*（lists counter 簇）/ margin-collapse-clear-001/-007 / anonymous-block-change / 3 first-line / grid-with-aspect-ratio / position-absolute-multicol-001 / caption-relative-positioning / percent-height-table-cell-child / 4 hanging-whitespace。7 exposure（baseline 从未运行，各为独立 future lever 或 out-of-scope DOM/JS gap）：cascade-import-001(3.20%) / padding-applies-to-016/017(12.69%/11.72%) / flexbox-align-self-baseline-compatability(2.08%) / multicol-on-broken-image-alt-text(10.42%) / **replaced-object-backdrop(100%=dialog::backdrop 伪元素 DOM/JS gap，R349 已判 out of scope，ref /css/reference/green.html 正确加载)** / dynamic-table-cell-height(7.12%)。**0 回归 by construction**：受影响测试 baseline 下全被排除（loaded=0），无 case 由 PASS→FAIL。与 R546 同模式（资产/ref 解锁分母，+149 self 的姊妹 lever）。门禁：make test 全绿（45 套 ok/0 fail）、clippy −D warnings clean、fmt 我的文件 clean（未一并提交 pre-existing lib.rs:592 R502 drift）、product-smoke welcome 16.97%（<20% neutral，runner 加载器变更零渲染改动）。clean win（基础设施分母真实性，提 DC-14 真通过率分母真实性=真优化目标）。详见 [`evidence/r551-absref-path-resolver-2026-06-24.txt`](./evidence/r551-absref-path-resolver-2026-06-24.txt)。

**R550（★ LANDED commit af9b59bc — border-color currentColor 解析为元素 color；self-source 净 +4 PASS / 0 回归）**：承接 R549 的 5 诚实暴露，攻其 currentColor 子集（border-color-011 + shand-border-000，另带 2 bonus）。**根因**：ZeroWeb border-color 既不正确初始（initial=黑，违反 CSS §8.5.1 initial=currentColor）也不解析 currentColor（`color_value_to_render(CurrentColor)`=>黑，paint/color.rs 无元素上下文）→ `border: solid`（无显式色）在彩色文本上渲染黑边框（应取元素文本色），且 `border-color: inherit` 继承已解析黑值而非 currentColor 关键字（border-color-011 FAIL）。**3 部件 fix（4 文件 +47/-5）**：①style-system default_impl+registry border-*-color initial→`ColorValue::CurrentColor`（§8.5.1，默认元素仍无边框因 border-style=none）；②engine/paint/painter/border.rs `paint_borders` 经新 helper `resolve_border_current_color` 把 4 边框颜色 currentColor→`style.color` 再交 paint_border_edge（collapsed border 路径同覆盖）；③color.rs 文档注明 currentColor 无上下文回落黑、需解析的调用点先替换。**A/B（test-guard，17 目录）= borders +4 PASS（border-color-011 / border-shorthands-002 / border-shorthands-003 / shand-border-000）/ 0 回归**（normal-flow/positioning/backgrounds/colors/box/box-display/floats/floats-clear/generate/generated-content/cascade/css-tables/css-flexbox/css-position 全 0/0）。**残余**：border-color-012（inline `<span>` 边框，与 011 仅差 block vs inline）仍 FAIL = inline-border 独立渲染缺口（currentColor 已正确解析），非本 lever，入 next-lever。门禁全过：make test 12308/0（+1 新单测 resolve_border_current_color）、clippy −D warnings clean、fmt 我的文件 clean（唯一 drift = pre-existing lib.rs:592 R502 代码）、product-smoke welcome 16.97%（<20% neutral）。clean win（CSS-correctness 单点 lever 族，同 R491–R511），提升 chromium Oracle 一致率（真优化目标）。详见 [`evidence/r550-currentcolor-border-color-2026-06-24.txt`](./evidence/r550-currentcolor-border-color-2026-06-24.txt)。

**R549（★ LANDED commit ebff2595 — block C RESOLVED，3 部件 border-width `medium` 修复 reftest 净 +14，本 session 独立 A/B 复核确认）**：承接 R502-reopen 解 block A 后，absolute-replaced-width-006 唯一剩余阻塞 = block C（margin-collapse-through-border，archive 曾标「R490-entangled 死路」）。**根因（探针二分实证）**：`border: solid`（隐式 width=medium）经活 `parse_length`（types.rs:991）**漏 thin/medium/thick 关键字**（死代码 parse_basic.rs 有此处理，R544 死代码陷阱同谱系复发）→ border-width 回落 initial=0 → **border 不绘制 → 不阻断 margin 折叠**（margin 穿透，非 margin-collapse 算法 bug）。**3 部件协调 fix（实验 A/B 实证 RESOLVES block C）**：①fix1 parse_length 加 thin/medium/thick→Px(1/3/5)；②fix2 converter/mod.rs `border_lp` 闭包按 border-style=None/Hidden 抑制 width 为 0（CSS §8.5.3，修 `border:none` 布局偏移）；③fix3 default_impl.rs+registry.rs border-*-width initial 0→3px(=medium, §8.5.1)。三者缺一即回归。**A/B（test-guard，4 目录 FAIL-list diff）= reftest 净 +14**：positioning 396→**414（net +18：+19 新过含 absolute-replaced-width-006 长期死路破解 / -1 abspos-width-005 暴露）**、borders 432→429(-3)、normal-flow 639→638(-1)、backgrounds 0。**5 PASS→FAIL = DC-14 honest exposures**（非回归，是 `border:<隐式宽度>` 假通过被正确 border 渲染揭穿）：border-color-011/012 + shand-border-000 = **border-color `currentColor` 解析 bug**——★ **doc-side 根因定位（本轮 read committed HEAD）**：简写解析器 `parse_border_shorthand`（shorthand/mod.rs:489）**正确**省略色→`currentcolor`（排除简写层）；真 bug = (a) `ColorValue::CurrentColor` 在 computed 管线**从不解析为元素 `color`**（computed.rs HEAD 无此解析）+ (b) border-color initial = `Rgba(0,0,0,255)` 黑（registry.rs:39）**违反 §8.5.1 initial=currentColor** → `border-color:inherit` 继承已解析黑值而非 currentColor 关键字（border-color-011 meta assert「currentColor 作为关键字继承」正中此）；blocks-017 = margin-collapse-with-border 3px 偏移；abspos-width-005 = `thin`=1px border 触发 abspos-width-with-border 计算差异。★ **doc-side read-only 逐案核查（本轮读 5 测试源码）确认 5 个均为真·假通过揭穿**——修前 test/ref 同 border=0 偶然匹配；修后 border 正确渲染暴露底层 color/inheritance/layout 独立 bug（各为独立 future lever，非 border-width 缺陷）。**8 单测涟漪**（fix2 抑制 style=none，旧测试设 border-width 不设 style 依赖 buggy 行为，机械修各加 border-style=Solid）。**裁决建议：clean-land 据 DC-14 接受 5 暴露为 honest exposure（option b）**——+14 净（block C 解死路）是迄今最高价值单 lever；5 暴露是 self-source false-pass（不计达标），同 R525/R532 accepted-newly-FAIL 先例；其底层 bug（currentColor/inheritance/border-layout/abspos-width-with-border）应入 next-lever 队列提 chromium Oracle 一致率（真优化目标）。**★ 实测 code agent 走 option (b)（接受 5 暴露为 honest exposure，与 doc-agent 建议一致）**：working-tree diff 实证（本轮）14 文件 = fix1/2/3 + **10 单测涟漪**（border-width 断言 `Px(0.0)`→`Px(3.0)` + converter/engine/style-system 测试加 `border-style=Solid`）；`git diff | grep currentcolor` = **零新增 currentColor 逻辑**、`registry.rs:39` border-color **仍黑** `Rgba(0,0,0,255)`（非 currentColor）→ code agent **未修暴露**，是接受 +14 净（5 暴露留作 next-lever，见上文 (a)(b) currentColor 根因）。★ working-tree 14 文件状态在多轮观察间**正常振荡**（modified→clean→modified）= code agent 跑 test-guard A/B（`git stash` baseline / `pop` fix）的正常现象，非卡顿。**★ 本 session LANDED（commit ebff2595，已 push origin/main，前序 10+ 轮 stall = 529/429 API 限流杀掉 code agent，非 fix 问题）**：上一轮 529 overloaded 失败后，本会话核对工作树（fix 内容与前序会话完全一致、跨轮未变），独立跑 4 目录 A/B（git stash baseline / pop fix）复核确认 **净 +14**：positioning 396→414（+18，absolute-replaced-width-006 baseline FAIL 3.52%→fix PASS 0.24% = block C 死路破解实证）/ borders 74→77（−3）/ normal-flow 107→108（−1）/ backgrounds 52→52（0）。5 暴露经逐案核查确认为诚实 DC-14 揭穿（修前 test/ref 同 border=0 假一致；修后正确 border 暴露 currentColor/inheritance/border-layout 独立 bug），接受为 option b（同 R525/R532 accepted-newly-FAIL 先例）。全门禁通过：make test 12307/0、clippy −D warnings clean、product-smoke welcome **16.97%**（<20%，doc-side 预核 neutral 实测确认——welcome 唯一显式 border = `border:1px solid` 显式宽，fix3 不动；余 border-color 无 style→fix2 抑制 width=0→无边框）。next-lever 队列（提 chromium Oracle 一致率，真优化目标）：currentColor 解析（修后 border-color-011/012 + shand-border-000 三暴露可翻回 PASS）/ margin-collapse-with-border（blocks-017）/ abspos-width-with-border（abspos-width-005）。详见 [`evidence/r549-landed-ab-verified-2026-06-24.txt`](./evidence/r549-landed-ab-verified-2026-06-24.txt)（本 session landed A/B 复核）+ [`evidence/r549-block-c-border-width-medium-rootcause-2026-06-24.txt`](./evidence/r549-block-c-border-width-medium-rootcause-2026-06-24.txt) + [`evidence/r549-land-attempt-verified-block-c-resolved-2026-06-24.txt`](./evidence/r549-land-attempt-verified-block-c-resolved-2026-06-24.txt)。

**R502-reopen（code-side CSS correctness LANDED）CSS2 Appendix D presentational hints 重投（block A 已被 R504 解除）；4 目录 A/B net +7 PASS / 0 回归**：doc-agent e91e7924 评估「R504 global positioned-descendant deferral（Appendix E step 6, commit ea08de51）可能已解禁 R502 block A（draw_order 覆写 abspos img）」。本轮 code-side 重投 `collect_presentational_hints`（lib.rs 步 1.5a：img width/height 属性 → Author origin + specificity (0,0,0) + 最早位置 + 裸数字→px/%透传）+ LAYOUT_DUMP 实证 **block A 确已解除**：absolute-replaced-width-006 img 计算尺寸 15×15→**96×96**（hint CORRECT，同 R502 实证）且现可见（diff 2.24%→3.52% = img 可见但 block C margin-collapse 80px 垂直偏差，非覆写 blue_px=0）。**A/B（test-guard，git stash baseline / pop fix，4 目录全量 FAIL-list diff）**：normal-flow +3（block/inline-block/inline-replaced-width-006）/ positioning +0（absolute-replaced-width 簇 block C 仍阻塞）/ backgrounds +3（background-image-cover-002/-004/-cover-attachment-001）/ borders +1（border-color-005）= **net +7 / 0 回归**（全 4 目录 newly-FAIL=0）。★ doc-agent「285 文件 broad +N」乐观——实测 block C + 各案独立 blocker 限制到 +7（positioning 0、每目录 1-3），但 block A 解除是关键发现（R504 paint-order 修复对 abspos replaced 元素生效）。门禁：make test 全绿，clippy clean，product-smoke welcome 16.97%（welcome 无 img w/h 属性 neutral），fmt clean。详见 [`evidence/r502-presentational-hints-reopen-landed-2026-06-24.txt`](./evidence/r502-presentational-hints-reopen-landed-2026-06-24.txt) + item 2（lever 候选）doc-agent 详析。

**R548（code-side layout correctness LANDED）table col/colgroup min-width/max-width 列约束（CSS Tables §17.5）；normal-flow net +4 PASS / 0 回归**：承接 R546 暴露的 10 个 applies-to 中 col/colgroup 子集（doc commit 5180f681 已定位 seam = table.rs Pass 0 resolve_col_width 仅读 width）。**根因**：compute_column_widths Pass 0（table.rs:1128）遍历 `<col>`/`<colgroup>` 读显式 `width`(Px) 设 `col_max_widths[idx].max(w)`，但**完全不读 min_width/max_width**（CSS §17.5/§10 规定 col/colgroup min-width=列下限、max-width=列上限）→ `min-width:1in`/`max-width:1in` on colgroup/column 被忽略，cell 塌缩或溢出。**修复**（table.rs ~30 行）：resolve_col_width 旁加 resolve_col_min/resolve_col_max（仅读 Px），Pass 0 三分支改取 style 一次解析 width+min+max，循环内 `col_max_widths[idx]` 应用 `.max(w)`+`.max(mn)`+`.min(mx)`。min/max gated on 属性声明（Px）→ **blast radius 极小**（仅影响显式声明 min/max-width on col/colgroup 的表）。**A/B（test-guard）**：normal-flow applies-to col/colgroup 子集 **net +4**——min-width-applies-to-005（colgroup）/-006（column）/max-width-applies-to-005/-006 全 FAIL→**PASS 0.00%**；normal-flow **632/746→636/746**（failed 114→110，精确 +4 / 0 回归）。**0 回归**：css-tables 77/112→77/112 byte-identical；CSS2/tables grep 证 0 个 col/colgroup+min/max-width 测试→零影响。残余 6 applies-to（007 cell/008 inline/013-014 table·inline-table）= 不同 display/机制（cell 级 min-width / 列不填满 table 宽 / inline-table 位置偏移 / min-height 未传播），独立 lever。门禁：make test 全绿，clippy clean，fmt clean，product-smoke welcome 16.97%（不变）。★ R177 col-width 谱系：本轮仅**新增** min/max 读取（不改既有 width 路径）+ gated → 不触 R177「line1675 load-bearing」回归路径（css-tables 实测确认）。详见 [`evidence/r548-col-colgroup-minmax-width-2026-06-24.txt`](./evidence/r548-col-colgroup-minmax-width-2026-06-24.txt)。

**R547（code-side CSS parser correctness LANDED）CSS `ex` 单位比例修正 0.5em→0.8em（Ahem 实测 x-height）；values net +1 PASS / 0 回归**：R546 后扫 CSS2/values，R544 的 ex 单位支持用 0.5em 近似（"规范允许近似"），但 values/units-002~004 + numbers-units-012 仍 FAIL。**实证反推 Ahem x-height**：units-004 `.ex{12.5ex}` 须覆盖 `.em{10em}`=200px（20px font）→ 1ex=16px=**0.8em**；numbers-units-012 `#div1{6ex}`（父 20px）须使 `#test{1em}`=`#reference{1in=96px}` → 6ex=96px=0.8em。两测试独立推导一致：Ahem x-height=0.8em，非 R544 的 0.5em。**修复**（types.rs parse_length 1 行）：`"ex" => Em(num*0.8)`（ZeroWeb 未接 fontdue 度量管线，按 Ahem=WPT 测试字体实测值取常数，同 R544 常数策略仅修数值）。**A/B（test-guard）**：values 20/26→**21/26 net +1**（units-004 FAIL→PASS 0.00%，.ex/.em「no red」覆盖测试正确覆盖）；normal-flow 632/746→**632/746 = 0 flip / 0 回归**（ex 用例 max-width-080~084/089 两值都过，证不依赖精确比例）。★ numbers-units-012 diff 3.06%→3.84%**非 ex 比例错**：0.8em 后 #div1=96px 正确、#test/#reference=96×96 与 ref 一致；残余=fontdue 渲染 Ahem「X」字形（96px）不填满 em-box vs ref 96×96 实心块=字形光栅化（ruled-out 谱系）；0.8em 把 diff 来源从「#test 尺寸错位」转移到「X 字形度量」，仍 FAIL 但 ex 已 CSS-correct。门禁：make test 全绿（更新 1 旧单测 `test_parse_length_ex_unit_to_em_ahem_xheight`：Em(0.5)→Em(0.8)，6ex 用 `6.0*0.8` 规避 f64 精度），clippy clean，product-smoke welcome 16.97%（不变，welcome 不用 ex 单位）。★ 0.8em 是 Ahem 真实 x-height（units-004 覆盖铁证），修正后 ZeroWeb ex 解析更接近 chromium（用真实 Ahem 度量），利 DC-14 Oracle 一致率。详见 [`evidence/r547-ex-unit-ahem-xheight-0pt8em-2026-06-24.txt`](./evidence/r547-ex-unit-ahem-xheight-0pt8em-2026-06-24.txt)。

**R546（code-side asset import LANDED，迄今最大单轮净增）缺失共享 WPT 参考文件导入（14 个，零代码变更）；self-source net +149 PASS / 0 回归 / 12 诚实暴露**：R545 后扫 normal-flow 残余，上轮建议「内联 SVG 渲染」**经核对 goal doc line 99 判为范围外**（"SVG 文档/内联 SVG 渲染：不在本目标范围"），不可行。改扫其它缺口，定位**缺失共享参考文件**：很多 CSS2.1 reftest 引用 `css/reference/` 下共享 ref（`pass_if_number_5.xht` 等），但文件**未随 WPT 导入**→runner 加载 `No such file or directory`→该测试**被排除出分母**（从未运行）= DC-14「分母真实性」缺口（分母被静默缩小抬高通过率）。grep 全 wpt-data 发现 **14 个唯一缺失 ref**（11 个 css/reference/ pass_if_* 描述式 ref + single_square_list_marker + 1 css-writing-modes + 2 css-fonts）影响 **161 测试**。**实证**：baseline `reftest-upstream counter-increment-003`→**Total:0**（ref 缺失被排除）；导入 2 个 counter ref 后 `counter-`→**97/97 (100%)**——ZeroWeb 的 CSS counter 支持（painter/text.rs get_counter + content:counter()）本就正确，缺的只是 ref。**修复**：从上游 WPT（raw.githubusercontent，via ~/use-proxy 代理）fetch 14 文件放到 wpt-data 对应路径，**零代码变更**。**A/B（test-guard 全量 7 子目录）**：import refs 只新增此前被排除测试、绝不改变已运行测试→**0 回归 by construction**。新解封 161 测试交叉核对（新解封清单 ∩ 各子目录 FAIL 列表）：CSS2/lists 68 全过 / CSS2/text 33 全过 / CSS2/fonts 29 全过 / CSS2/generated-content 14 全过 / css-fonts 2 全过 / CSS2/normal-flow 13→3 过 10 暴露 / css-writing-modes 2→0 过 2 暴露 = **149 PASS + 12 诚实暴露**。子目录 after：lists 151/151(100%)/text 375/408(91.9%)/fonts 152/159(95.6%)/generated-content 207/225(92%)/normal-flow 632/746(84.7%)/css-fonts 284/286(99.3%)/writing-modes 613/788(77.8%)。★ **12 暴露 = DC-14 anti-false-pass 正面实例**：此前 ref 缺失排除（假性「不运行=不拖累通过率」），现真实运行暴露缺口（10 个 min/max-width/height 对 table-internal 元素不生效=R177 col-width 谱系 + 2 个 vertical-rl margin-collapse=R323×writing-mode）=下一步 lever 候选。门禁：代码 byte-identical to R545 HEAD（git diff 仅 14 个 ref 文件），make test 12307/0，clippy/fmt 不受影响（无 .rs 变更）。★ 与 R502/R524/R531「masked lever 零 yield revert」区别：本变更**零代码**，yield 来自测试可运行性恢复（渲染本就正确，缺的是 ref），无「fix 无效」问题。详见 [`evidence/r546-missing-shared-refs-import-2026-06-24.txt`](./evidence/r546-missing-shared-refs-import-2026-06-24.txt)。

**R545（code-side clean win LANDED）CSS `inherit` 关键字补全盒模型尺寸属性 = inherit_property 加 width/height/min-width/min-height/max-width/max-height 六 arm；CSS2 normal-flow net +3 PASS / 0 回归**：R544 后扫 normal-flow 残余，inline-replaced-width（8）= inline `<svg:svg>`（ZeroWeb 不渲染内联 SVG，大 feature，非本轮）；另查 `inherit` 关键字——`max-width-104`/`max-height-104`/`height-inherit-001` FAIL。**根因**：inherit.rs `inherit_property`（line 8-265，处理 `inherit` 关键字的 field 拷贝）只覆盖 color/font-*/margin/padding/background/border/visibility 等默认继承属性 + 部分非继承属性，**缺 width/height/min-*/max-***（这六个在 `apply_initial_value`，另一函数）→ `max-width:inherit` 走 inherit_property 无 arm 返回 false → 静默失败。最小复现：`max-width:inherit`（父 50px）子元素 200px（应 50px）。**修复**（inherit.rs，~27 行）：inherit_property 末尾加六 arm `child.X = parent.X.clone()`（CSS `inherit` 对任意属性生效，复制父计算值；margin/padding 已在，补齐尺寸六属性）。**A/B（test-guard）**：CSS2 normal-flow 626 → **629 = net +3**（max-width-104/max-height-104/height-inherit-001）；css-flexbox 368→368、css-position 63→63、css-tables 77→77、css-text 1646→1646 = 全 0 flip。门禁：clippy clean，make test **12307/0**（更新 2 个旧断言 `test_inherit_property_returns_false_for_non_inheritable` / `test_inherit_non_inherited_explicit`——它们断言旧的「width 不可 inherit」错误行为，改为反映修正后 width:inherit 复制父值），fmt clean。★ 教训：`inherit_property`（inherit 关键字）与 `apply_initial_value`（initial/unset）是两个独立 match，新增属性须两边同步；盒模型尺寸此前只在 initial 侧，inherit 侧遗漏。

**R544（code-side clean win LANDED）CSS `ex` 单位支持 = parse_length 加 `ex`→Em(num*0.5)（x-height≈0.5em，同 ch 策略）；CSS2 normal-flow net +31 PASS / 0 回归**：CSS2 normal-flow 扫描定位 max-width-079~084（6 案）= `max-width:` 用 `ex` 单位。实证 ZeroWeb **不支持 `ex` 单位**——parse_length（values/types.rs:991）单位 match 无 `ex` 致 `max-width:0ex`/`width:6ex` 解析失败被丢弃（min-width/width/height/min-height/max-height 的 ex-unit 测试全 FAIL）。最小复现：`<div style="width:10ex">` 渲染满宽 784px（应 80px），`width:10em` 正确 160px。**修复**（types.rs parse_length，3 行）：`"ex" => Some(LengthValue::Em(num * 0.5))`（ex 相对元素 font-size 同 em，0.5em 近似 x-height；resolve_length 已正确处理 Em）。★ **陷阱**：仓库有**两个** `parse_length`——parse_basic.rs（**死代码**，未被 values/mod.rs 导入，不编译）与 types.rs（活代码，`pub use types::*`）。首版误改 parse_basic.rs 零效果，复测 `width:10ex` 仍 784px 才发现；改 types.rs 后 `width:10ex`→80px、max-width-079 PASS。**A/B（test-guard）**：CSS2 normal-flow 595 → **626 = net +31 / 0 回归**（ex-unit 跨 width/height/min/max 各 sizing 属性，一个 parse 修复全收）；css-flexbox 368→368、css-text 1646→1646、css-writing-modes 613→613、css-fonts 282→282、CSS2/positioning 394→394 = 全 0 flip（ex 此前被丢弃，现解析为 Em(0.5)，仅 ex 用例变化）。门禁：clippy clean，make test **12307/0**（R543 12306 + 1 新单测 `test_parse_length_ex_unit_to_em_half`），fmt clean。★ 教训：仓库存在 parse_basic.rs/types.rs 双 `parse_length` 死代码重复，改长度解析须确认 `values::parse_length` 实际指向（mod.rs `pub use types::*`）。

**R543（infra 进程加固 LANDED）`make product-smoke` 回归门禁 = product-smoke 加 `--max-diff <pct>` 阈值退出 + Makefile target（welcome vs chromium Oracle，diff>20% 退出 2）**：承接 R541 进程缺口（welcome +7.65pp 回归藏了 14 轮，因 product-smoke 不在每轮验证）。**实现**：① product-smoke 命令加 `--max-diff <pct>` 参数（main.rs cmd_product_smoke）：diff 超阈值 `eprintln! REGRESSION + exit(2)`（退出码 2 区别于参数错误 1）；USAGE 同步更新。② Makefile 加 `product-smoke` target（test-guard 包裹 `cargo run --release --bin zero-wpt-runner -- product-smoke welcome.html --oracle welcome-chromium.png --max-diff 20`），`MAX_DIFF` 可调，加入 .PHONY。③ run-rules.md 加一条：渲染/布局变更建议额外跑 `make product-smoke`。**验证**：`make product-smoke`（阈值 20）→ welcome 16.97% → exit 0；`make product-smoke MAX_DIFF=10` → 16.97%>10% → REGRESSION + exit 2（test-guard 正确传播退出码）。门禁：make test 12306/0，clippy clean，fmt clean。★ 此后每轮渲染/布局变更可用 `make product-smoke` 单命令捕获 DC-13 产品可见回归，防 R428 类复发（welcome baseline ~17%，阈值 20% 留 3pp headroom；若 baseline 合法漂移可 `MAX_DIFF=N` 调整）。

**R542（code-side clean win LANDED）welcome 回归修复 = measure_text_content 的 MinContent 宽度此前误用 INFINITY（=max-content）；welcome 24.63%→16.97%（恢复 R371 baseline）+ css-flexbox net +2 PASS / 0 回归**：承接 R541（welcome +7.65pp 回归 bisect 到 R428 min-size:auto，min-width:0 实验证 content-min 偏大）。**根因精确定位**（inline_finalization.rs `measure_text_content` inline-content 分支，line 609）：taffy 测 MinContent 宽时 available_space.width=MinContent（无定值），`width = known.or(available.into_option()).unwrap_or(INFINITY)` = **INFINITY** → IFC 用无限宽布局 → 全部单词排一行 → `measured_width` = **max-content（整行宽）而非 min-content（最宽词）**。R428 前 min-width 默认 Px(0) mask 此 bug（content-min floor=0）；R428 后 min-width=Auto 启用 floor=偏大 max-content → grid/flex item 过宽（welcome 卡片级联推下半页）。**修复**（~10 行）：MinContent 分支 `width=0.0` 强制每个不可拆单元（单词/原子盒）独占一行 → 最宽行 = min-content（最宽词）；MaxContent/Definite 路径不变。**A/B（test-guard）**：welcome（vs fresh chromium Oracle）**24.63% → 16.97%**（= R371 baseline，回归完全消除）；css-flexbox 366 → **368 = net +2**（min-content 修正后部分 flex item 不再过宽，bonus 改善）；css-grid 32→32、css-tables 77→77、css-position 63→63、css-multicol 194→194 = **全 0 flip**。门禁：clippy clean，make test **12306/0**（R541 12305 + 1 新单测 `test_measure_text_content_min_content_is_widest_word`：MinContent<MaxContent，修复前两者相等），fmt clean。已更新 evidence/product-static/welcome-zeroweb-cpu.png 为修复后渲染。★ welcome DC-13 baseline 恢复 16.97%（R227 padding 双计修复后的水平）；残余 ~17% 经 DC-14 AA baseline 纠正 = 布局/度量（R109 item-tag span→block 结构性 + line-height），非字体噪声、非 min-size。

**R541（doc-side 诊断 + 实验证实，零代码变更）DC-13 product-smoke 发现 welcome.html 回归 +7.65pp（16.98%→24.63%）；git bisect 定位 + min-width:0 实验证实根因 = R428 min-size:auto 默认致 taffy 0.7 grid/flex item content-min 过大**：DC-13 product-smoke（welcome vs fresh chromium Oracle，chromium 与缓存 Oracle 一致）实测 welcome = **24.63%**，较 R371 记录的 16.98% 回归 +7.65pp。**git bisect（good=9c5c2d4f R371 / bad=f8b1dab3 R535，测试=渲染 welcome vs R370 缓存 diff<8%=good）定位首个坏提交 = 574db507（R428 min-width/min-height 初始值 Px(0)→Auto，CSS §4.5/§6.6）**；R535（R536/R537 前）已 24.63% = R536/R537 非因。**根因证实实验**：welcome 注入 `* { min-width:0; min-height:0 }` 重渲 → **24.63%→16.98%（精确回 R370 baseline）**，证明回归 100% 来自 min-size:auto 的 content-min floor。机制：welcome 用 grid cards（`grid-template-columns:1fr 1fr`）+ flex，R428 后这些 grid/flex item 的 min-size:auto=content-min，**taffy 0.7 算出的 content-min 大于 chromium** → 卡片过宽/过高，级联推下半页（首个发散行 y=267 x≈500，diff 集中 y=250-599）。★ **R428 不可 revert**：CSS 正确（min-size:auto 是规范默认）+ R428/R530 修了真 flex-shrink bug（item 不收缩）；revert 破坏 CSS 正确性 + 重引入 flex-shrink bug。**真修复 = taffy content-min 测量对齐 chromium**（grid/flex item min-content sizing，R304 deferred taffy 升级 / R530 flex measure_text_content 谱系），多会话级，非单会话 clean lever。★ **进程缺口**：product-smoke 不在每轮验证 → 此回归藏了 ~14 轮（R428→R540）未被发现（make test + scoped reftest 都不覆盖 welcome fixture）。建议把 welcome product-smoke 加入每轮验证（~5s 渲染+diff）防回归复发。下一步 = 攻 taffy/ZeroWeb grid item content-min 测量（min-width:0 实验已证 lever 有效）。

**R540（doc-side 状态修正，零代码变更）M7 渲染器图元覆盖实测已基本完成——goal doc（2026-06-06 baseline）过时；R539「转向 M7」判断基于过时信息，撤销**：R539 据 goal doc「CPU 仅 3/13、GPU 仅 2/13、browser 仅 2/13」判定 M7 为 critical-unfinished。**本轮逐文件核查代码，实证 M7 三部件均已基本完成**：① **CPU 渲染器（cpu/）13/13 全派发**——render_typed_buckets + render_draw_order 均处理 shadows/fills/rounded_rects/gradients/images/strokes/path_fills/path_strokes/glyphs/clips/transforms/filters/blend_modes，各图元有真实实现文件（gradient.rs 逐像素+repeating、shadow.rs、stroke.rs、effects.rs filter/blend），cpu/tests.rs 1043 行单测，make test 全绿。② **browser append_webview_primitives（app_render.rs:1515）13/13 全字段**——shadows/rounded_rects/gradients/images/strokes/path_fills/path_strokes/clips/transforms/filters/blend_modes 都 clone+sacle/offset/clip 应用。③ **GPU 渲染器（gpu/renderer/mod.rs render_full_scene_gpu）11/13**——WGSL 真实 pipeline（非 CPU passthrough，符合 DC-9）：shadows/fills/strokes/path_fills/path_strokes/glyphs 走 fill pipeline（push_*_mesh 专用顶点）、rounded_rects/gradients/images 各独立 pipeline、filters（opacity/brightness/contrast ping-pong + blur 2-pass H+V）+ transforms（2D 仿射逆采样）走 GPU 后处理 pass。**缺口仅 clips + blend_modes 未在 GPU 派发**，但 paint 阶段**几乎不生成 ClipPrimitive/BlendModePrimitive**（裁剪走 `clip_all_primitives_to_rect` 坐标裁剪 helper，非 ClipPrimitive）→ **理论覆盖缺口非实际渲染 bug**。★ **撤销 R539「转向 M7」**：M7 不是 critical-unfinished。goal doc 的 DC-8/9/10 + Current Proven Baseline（line 358/414/709「CPU 仅 3/GPU 仅 2/browser 仅 2」）严重过时（2026-06-06 后大量图元工作落地但未回写 doc）。**真正剩余高 EV 工作**（重新评估）：(1) DC-13 产品静态页 smoke（welcome/morning-work/wintertc vs chromium Oracle）——验证 M7 图元在真实页端到端，捕获 WPT 子集未覆盖的产品可见退化（welcome ~26% diff 残余，R174 判为字体噪声但需 Oracle 复核）；(2) DC-14 chromium Oracle 一致率（优化真目标，self-source 46.5% 假通过）；(3) 95% reftest 通过率（需多会话架构：R109 inline→IFC / multicol fragmentation / flex-direction-aware 替换元素分轴）。下一步 = DC-13 product-smoke（welcome.html 本地渲染 + chromium-oracle HTTP server 对比，捕获真实图元/布局 bug）。

**R539（doc-side 诊断，零代码变更）css-position/css-tables 残余失败诊断 = clean lever 穷尽；战略转向 M7 渲染器图元覆盖（goal 指定 Critical Path）**：R538 确认 css-flexbox/grid clean wins 穷尽后，本轮扫 css-position（63/95）+ css-tables（77/112），**同样全部确认为复杂/JS/taffy-domain**：① **css-position position-relative-001~005/014（6 案）= 百分比 inset（top:100%/left:100%）在 position:relative 未解析**——PIL 实证 test 2500 红+1300 绿（应 2500 全绿）；span 含 block 子元素（R109 split），r109_split 时 ZeroWeb 跳过 relative offset（engine.rs:1358 `if !root.is_r109_split`，防 inline-box-002 双计），交 taffy 处理，但 taffy 0.7 对该路径**不解析百分比**（converter 已正确传 Percent，convert_length_to_lpa Percentage→Percent）。修复须：(a) resolve_relative_inset 加百分比（Px-only→加 CB 解析，须传 nearest-block-ancestor content size 进 apply_relative_offsets_inline 递归）+ (b) r109_split 仅百分比时应用（Px 仍交 taffy）。**R109 territory 脆弱（R247 Phase-A deadlock 谱系），双计回归风险，6 案 yield 不抵风险，DEFER**。② css-position 残余多为 `*-dynamic-*`（JS 动态：hypothetical-dynamic-change / position-absolute-dynamic-relayout×6 / dynamic-auto-overflow 等）、position-absolute-root/fixed-root-element-{flex,grid}（root abspos in flex/grid）、replaced-intrinsic-size（替换元素+abspos）、fit-content（R97）。③ **css-tables**：absolute-tables（6 案，006=JS 动态 innerHTML；008-011=abspos table shrink-to-fit sizing 复杂）、table-intrinsic-size（4 案，R97 max-content taffy-blocked）、percent-height-replaced-in-percent-cell（替换元素+百分比高度嵌套）、fixup-dynamic-anonymous-*（JS）、visibility-collapse-border-spacing（2）。★ **结论（R538+R539 双轮诊断）**：布局 reftest（flex/grid/position/tables）clean 单会话 lever 全面穷尽，残余需多会话架构（R109 分轴百分比 / abspos table shrink-to-fit / taffy 升级 / JS 动态布局 / max-content sizing）。**战略转向 M7 渲染器图元覆盖（goal §Single Active Milestone 指定 Critical Path）**——CPU 渲染器现仅 fills+rounded_rects+glyphs（3/13 图元），渐变/阴影/图片/线段/路径/变换/裁剪/滤镜/混合模式全丢弃，是「视觉输出最大瓶颈」（远比磨已穷尽的布局 reftest 高 EV）。下一步 = M7 CPU 渲染器首个图元（优先 GradientPrimitive 或 ImagePrimitive，逐图元 + 单测 + framebuffer 像素断言）。

**R538（doc-side 诊断，零代码变更）css-flexbox/grid 残余失败聚类诊断 = clean lever 穷尽（与 R118 记忆一致）**：R537 收尾 flexbox_flex cluster 后扫残余 css-flexbox 130 失败 + css-grid 16 失败，逐聚类诊断，**全部确认为复杂/taffy-domain/JS-dynamic，非 clean 单会话 lever**：① **flex-minimum（13 案）heterogeneous**：height（022/024/026/028 等，2.08%）= **img 替换元素在 flex 列容器未 cross-axis 拉伸**——PIL 实证 test 100×100 方格区 9702 白+99 绿（应 9801 全绿），99 绿=1px 宽 img 的 99px border-bottom（img 停在 intrinsic 1px 宽未拉伸到容器 100px）。**根因定位**（tree.rs:267-269 apply_replaced_element_sizing both-auto 分支）：无显式尺寸 img 把 `size.width=Length(1.0)`+`size.height=Length(1.0)` 设为**definite**，taffy align-items:stretch 见 definite cross size 即不拉伸。**修复须 flex-direction 感知**（main 轴用 intrinsic、cross 轴留 auto 让 taffy 拉伸），但会动 R318/R325 调好的替换元素 sizing，且**高风险打破 R532 新过的 21 个 aspect-ratio-img**（它们 both-auto 依赖 intrinsic sizing）。**★ 实验已跑（本会话，已 REVERT）**：tree.rs apply_replaced_element_sizing both-auto 分支加 `is_flex_grid_item` 守卫（flex/grid 项不设 definite size，留 aspect_ratio 交 taffy stretch cross）。**实测 css-flexbox 366→363 = 净 -3 / 回归**——taffy 0.7 对 flex/grid img item 留 auto cross size 时**按 aspect_ratio 推主轴**（非 intrinsic），regress flex-aspect-ratio-img-{column,row} 多案（column 003/004/009/010/013/014/015/018、row 003/006/008/009/010 等，原 R532 新过）。**结论：img-stretch 非 clean lever，关闭**——简单 both-auto 跳过不行（taffy 用 aspect_ratio 推主轴致回归），真正修复须传 flex-direction 进 apply_replaced_element_sizing 分轴设主（intrinsic）/副（auto stretch）尺寸，多会话级，勿再以 both-auto 简单跳过重试。width（001/003，2.08%）= Ahem 文本 min-content（taffy-domain，R530 谱系残余）。② **flexbox-flex-basis-content（4 案）**=`flex-basis: content` 关键字（taffy 0.7 domain）。③ **flexbox-min-width-auto-004/005/006（1.5-3.3%）**= overflow-x/y ↔ min-width:auto 交互（overflow-x 非 visible 时 min-width:auto→0，CSS-overflow-3），复杂。④ **flexbox-writing-mode（9）**= vertical-rl + flex（R109 结构性）。⑤ **css-grid 16 案**：dynamic-*（dynamic-grid-with-auto-fill / grid-with-content-dynamic-display / grid-with-dynamic-img / img-src-changes / whitespace-reattach，x=[8,107] y=[51,150] 空方格 = JS 动态布局未生效）/ grid-container-baseline-synthesized（合成 baseline）/ child-border-box-and-max-content（max-content R97 taffy-blocked）/ firefox-bug-1881495（grid inline-grid R143）/ replaced-element-percentage-height-in-grid-nested-in-flex（嵌套替换元素）。★ **结论**：css-flexbox（366/496=73.8%）+ css-grid（32/48=66.7%）clean 单会话 lever 穷尽，残余需多会话架构（替换元素 flex cross-axis 拉伸 / flex-basis:content taffy 升级 / JS 动态布局 / max-content sizing）。下一步 lever 候选：转其它目录（css-tables 77/112、css-position 63/95 有 ZeroWeb-side 缺口？）或 img-stretch 专用实验轮。

**R537（code-side clean win LANDED）flexbox_flex-*-shrink 变体修复 = white-space:nowrap 不阻止 inline-block 换行；css-flexbox net +9 PASS / 0 回归，flexbox_flex cluster 全过**：承接 R536（+52）残余 9 个 -shrink 变体。REFTEST_BBOX 9 案均 y=[30,256]（test 止于 y=145=div 底，ref 延伸 y=256）。PIL 逐行扫描定位：TEST(flex) 4 inline-block 单行溢出正确；REF(block) line1 仅 3 盒、line2 span4 被换行——**容器设了 white-space:nowrap 却换行了**。**根因**：`adjust_inline_block_positions`（engine.rs:930）构造 IFC 定位 block 容器的原子行内级子元素，但**未把容器 white-space 传给 IFC**（no_wrap 恒 false）；IFC 的 inline-block 换行判定（inline/mod.rs:1328）虽有 `!self.no_wrap` 守卫但 no_wrap 从未被设 true。三条 IFC 构造路径（文本主布局 inline_finalization.rs:359 / paint text.rs:915 / inline-block 定位 engine.rs）只有前两条传 no_wrap，第三条漏。**修复**（engine.rs ~6 行）：`let no_wrap = styles.get(&container_node_id).is_some_and(|s| matches!(s.white_space, WhiteSpaceValue::Pre | WhiteSpaceValue::Nowrap))` + IFC `.with_no_wrap(no_wrap)`。精准只设 no_wrap（nowrap 下 preserve_whitespace 默认 false 即正确）。**A/B（test-guard）**：css-flexbox 357 → **366 = net +9 / 0 回归**（flexbox_flex-{0,1,N}-0-{N,Npercent,auto}-shrink 全过；flexbox_flex cluster 59 案现已全过）。**跨目录回归**：css-position 63→63、css-tables 77→77、css-text 1646→1646、css-writing-modes 613→613、css-multicol 194→194 全 0 flip（fix 仅 no_wrap=true 时改变行为）。门禁：clippy clean，make test **12305/0**（R536 12304 + 1 新单测 test_nowrap_inline_blocks_do_not_wrap），fmt clean。★ 教训：同一属性（white-space）在多个 IFC 构造路径须一致传播；新增 IFC 构造点核对 no_wrap/preserve_whitespace/word_break 三件套。详见 [`evidence/r537-nowrap-inline-block-nowrap-landed-2026-06-24.txt`](./evidence/r537-nowrap-inline-block-nowrap-landed-2026-06-24.txt)。

**R536（code-side clean win LANDED）flexbox_flex-* 双 bug 配对修复 = flex-basis em 解析 + inline-block 垂直 margin IFC 应用；css-flexbox net +52 PASS / 0 回归**：承接 R531 双 bug 互锁定位（双 bug 任一单独零 yield）。**修复 1**（style-system/computed.rs）：`resolve_length_field` 列表不含 flex_basis（FlexBasisValue 包装 LengthValue）→ `flex:0 0 4em` 的 em 在 converter 当裸数字（4em→4px 应 64）；补 `if let FlexBasisValue::Length(ref mut lv) = resolved.flex_basis { resolve_length_field(...) }`（仅 Length 分支，Percentage/Auto/Content 不改写）。**修复 2**（layout-engine/inline/mod.rs）：InlineBlockBox 新增 margin_top/right/bottom/left 四字段 + TextFragment 新增 margin_top；layout_inline 把 margin 计入水平推进 + 行盒高度（+margin_top+margin_bottom）；**apply_vertical_alignment 补 `run.y += run.margin_top`**（★ 前会话记录字段但漏了偏移步——本会话补上，否则几何不一致：行盒撑高但盒未下移）；垂直模式镜像。**互锁机制**：flexbox_flex-0-0-N 的 test=flex 受 bug1（flex-basis 宽错）、ref=block+inline-block 受 bug2（margin Y 错），self-source 须两侧一致才 PASS → 任一单独修仍 FAIL，配对修两侧正确→PASS。**A/B（test-guard）**：css-flexbox 305 → **357 = net +52 / 0 回归**（flexbox_flex-0-0-N/1-N-N/N-0-N 等 50 案目标聚类，精确匹配 R531「~50 yield」预测；仍 FAIL = *-shrink 变体，独立子 bug）。**跨目录回归**（bug2 影响 ALL inline-block+margin）：css-position 63→63、css-grid 32→32、css-tables 77→77、css-text **stash A/B 1646→1646** 全 0 flip。门禁：clippy clean，make test **12304/0**（R535 12301 + 3 新单测），fmt clean。★ 验证 R531 双 bug 互锁假设成立，R530（+9）后本方向最大单轮净增。详见 [`evidence/r536-flexbox-flex-doublebug-landed-2026-06-23.txt`](./evidence/r536-flexbox-flex-doublebug-landed-2026-06-23.txt)。

**R535（code-side CSS parser correctness LANDED）tokenizer hash token 允许首字符数字（修复 hex 颜色 #00FFFF/#4169E1 被破坏）；self-source 0-yield（颜色对称）但真实 correctness 修复**：承接 R534「flex style bug」调查。STYLEDBG 插桩 compute_element_style_internal 实证 test02 background="#0FFFF"（应 #00FFFF）、test03="#41690"（应 #4169E1）——**根因 = CSS tokenizer**：Hash 分支（`#` 后）用 `consume_ident()` 读 name，而 consume_ident 要求 ident_start（排除数字），CSS hash token name 却允许首字符为数字（name code point）。故 `#00FFFF`→consume_ident 返空→Hash("")，剩 "00FFFF" 被 Number 解析（"00"→0 去前导零→"#0FFFF"；"4169E1"→科学计数法 41690→"#41690"）→ 非法 hex→Transparent。**修复**（tokenizer.rs ~20 行）：新增 `consume_hash_name()`（允许数字首字符），Hash 分支用它替代 consume_ident。**验证**：align-content_center fills 2→**4**（test02 cyan + test03 royalblue 背景正确渲染）；新单测 `test_hash_color_leading_digit_preserved`；修正旧测 `test_hash_followed_by_non_ident`（原断言 #1→Hash("") 即 bug，改 #1→Hash("1")）。**A/B**：css-flexbox 305→305、css-tables 77→77 = **0 flip / 0 回归**（self-source 0-yield：hex 颜色 bug 对称，test/ref 都破坏→假一致，修复两侧正确→diff 不变）。门禁：clippy clean，make test **12301/0**，fmt clean。★ **保留非 revert**：与 R502/R524/R531（masked 布局 lever revert）不同，R535 是 **CSS parser 根因 bug**（token 破坏值，非投机 lever），单测锁定 + 0 回归 + 真实 correctness（chromium 不破坏 hex；产品页用 hex 颜色改善）。self-source 0-yield 是颜色对称度量假象。详见 [`evidence/r535-css-hash-tokenizer-fix-2026-06-23.txt`](./evidence/r535-css-hash-tokenizer-fix-2026-06-23.txt)。

---

（以上为 R568→R535 逐轮详记；R534 及更早已归 [`rounds-r527-r534-flexbox-img-asset-era.md`](./rounds-r527-r534-flexbox-img-asset-era.md) 等。）
