# 历史轮次归档 — R707（CSS2/box-display · top 全 dynamic-JS + root-box-003 oracle 损坏 · 负结果 era）

> 内容 100% 保留自主控文档 master.md（R707 块逐字迁出，仅作归档）。下文为该轮完整逐轮详记。

**R707（CSS2/box-display（fresh 第 10 采样目录，第 3 个 CSS2 subdir）·top 全 dynamic-JS（insert-inline-in-blocks 簇）+ root-box-003 oracle 损坏·非 clean static lever·负结果·零源码）：承 R706 CONTINUE 转**第 3 个 CSS2 subdir box-display**（139 test→120 oracle）。`make reftest-oracle DIR=CSS2/box-display`（120 案，read-only；**25.0% pass**）取 top-15：**root-box-003 100%**（standout）/ insert-inline-in-blocks-n-inlines-middle-003 36.23% / -middle-001 35.16% / -begin-003/001/002 32-33% / -end-001/003/002 32% / insert-block-in-inlines-* 19-25%。**① root-box-003（100%）= oracle 损坏**：`html{display:none;background:green}`（title 期望「big green expanse」= 根背景传 canvas），但 **CHR oracle nonwhite=0 全白**（display:none 根致空白帧抓取，R388/R692 broken-oracle class）→ 100% 不可信，skip。**② insert-inline-in-blocks 簇（9 案 29-36%）= DYNAMIC-JS**：top insert-...-middle-003 `[script]=1` + `class="reftest-wait"` + JS `document.createElement("span")+insertBefore`（DOM mutation）+ `<meta flags="ahem dom">`（dom=JS）——**全簇动态 DOM 插入 reftest**（insert-inline / insert-block 系列），测 §9.2.1.1 匿名块 + DOM 重排，**非 clean static-layout lever**（ZW JS 有限，oracle 捕 post-JS 态 ZW 无法 match，R694 dynamic-masked class）。**裁决：box-display top 全 dynamic-JS（insert 簇）+ broken oracle（root-box-003），clean static lever EV 低**——dir top 非 lever 来源。**hand-off**：box-display insert 簇须 ZW JS/DOM-bridge 成熟（code-agent 正做 page-scripts 线，近期 commit c9779aeb/50ec2d04），暂 defer。**下一步**：余 CSS2 subdir = linebox（246，IFC R109 structural 但或有 clean facet 如 R680/R702）/ borders（763，render-feature R172 同族）/ backgrounds（625 render-feature）；或 revisit top-level 已采目录次低案。零源码（reftest-oracle read-only + PIL oracle validity read-only + 代码未触碰，未改任何文件）。
