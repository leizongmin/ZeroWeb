# 历史轮次归档 — R717（visuren #3 abspos-%inset 确认工作（R98）+ fresh CSS2/visudet 采样 · replaced-elements-all-auto = SVG intrinsic-size viewBox-as-intrinsic CLEAN-ISH LEVER（第 13）era）

> 内容 100% 保留自主控文档 master.md（R717 块逐字迁出，仅作归档）。下文为该轮完整逐轮详记。

**R717（visuren #3 abspos-%inset 确认工作（R98）+ fresh CSS2/visudet 采样（第 7 个 CSS2 subdir，§10 sizing）·replaced-elements-all-auto = ★NEW CLEAN-ISH LEVER（第 13）·SVG intrinsic-size 用 viewBox 当固有尺寸（image_cache.rs:421-422）+ 默认 100 非 300×150·cluster replaced-elements-* 5 案 30-32%·归档 R697 恢复 last-20·零源码）：承 R716 CONTINUE 续 visuren 次高 + 转 fresh visudet。**① visuren #3 position-absolute-percentage-inherit-001（9.05%）LAYOUT_DUMP**：`#rel-pos-grand-parent{relative;300×400}` > `#abs-pos-parent{abspos;top:30%;bottom:10%;left:15%;right:20%}` > `#abs-pos-child-red{position:inherit;insets:inherit}`。**dump（裁决：parent abspos %inset 工作，child nested-inherit entangled）**：`#abs-pos-parent abs_y=106 h=180 w=260 x=68`——**全部正确**（top:30%×300=90→106, h=300-90-30=180, left:15%×400=60→68, w=400-60-80=260）✓ **abspos %inset + stretch 工作**（R98 adjust_absolute_pct_to_viewport 覆盖）；`#abs-pos-child-red` h=108 w=169（=inherit 对 parent 180×260 解析正确✓）但 **abs_y=216（应 160=parent 106+top:30%×180=54，+56 错）**——nested abspos + inherit 定位 entangled。**★ 裁决：entangled，非 clean lever**；但 **★确证 abspos %inset 工作（R98）**，与 R711 relative %inset 不工作（taffy 0.7 限制）形成对比——abspos 走 ZW adjust_absolute_pct_to_viewport（engine.rs:1857）覆盖 %inset，relative 依赖 taffy（不覆盖 %），故 abspos %inset 工作 / relative %inset 不工作。visuren 全 triage 完（R716 clean + anon-blocks structural + abspos-inherit entangled）。**② PIVOT→fresh CSS2/visudet（§10 sizing details，第 7 个 CSS2 subdir）**。`make reftest-oracle DIR=CSS2/visudet`（38 案，read-only；**39.5% pass**）取 top-15：**replaced-elements-* 簇主导（9/15 案，5 top 案 30-32%）**——replaced-elements-all-auto / min-height-20/40 / min-width-40/80 + inline-block-baseline-015 13.38% + replaced-elements-height/max-*/width-* 9% + content-height 簇 3-5%。选 **replaced-elements-all-auto（31.66%，簇 top，L. David Baron §10.3.2/§10.6.2 replaced sizing）** LAYOUT_DUMP。**test**：`div{width:200}` 内 7 个 `<img src=*.svg>`（height-25-width-50 / height-25-ratio-2 / width-50-ratio-2 / height-25-no-ratio / width-50-no-ratio / ratio-2 / no-ratio），测 img 按 SVG 内在尺寸/ratio 的 §10.3.2 sizing。**dump + SVG 文件 read-only（裁决：★CLEAN-ISH LEVER——SVG intrinsic-size viewBox-as-intrinsic + 默认错）**：① height-25-width-50（width:50 height:25 attr）→ img 50×25 ✓；② **height-25-ratio-2（height:25,viewBox 1000×500 无 width）→ img w=1000**（应 width=height×ratio=25×2=50；**ZW 把 viewBox width 1000 当固有 width！**）；③ **width-50-ratio-2（width:50,viewBox 1000×500 无 height）→ img h=500**（应 height=width/ratio=25；**viewBox height 500 当固有 height**）；⑥ **ratio-2（仅 viewBox 1000×500）→ img 1000×500**（应默认 300×150；**viewBox 当固有 size**）；④⑤⑦ no-ratio 缺维 → img 缺维=**100**（应 300 width / 150 height 默认）。**★ 根因（代码 read-only 精确）**：`image_cache.rs:421-422` `let w = size.width()…/h = size.height()…`（注释「按 SVG 内在尺寸（width/height 属性或 viewBox）栅格化」）——**usvg 的 size.width()/height() 在 width/height attr 缺失时回落到 viewBox 维度**，ZW 直接用作固有 size，**混淆 viewBox（定义 ratio）与 width/height attr（定义固有 size）**。CSS/SVG 规范：SVG 固有 size = width/height attr；viewBox 仅定义 intrinsic ratio；无 attr 无 viewBox → 默认 300×150。**裁决：clean-ish lever（R685/R686 image_cache.rs:419 vein，但本案 viewBox-as-intrinsic 子 facet clean 且 high-yield）**。**fix scope（hand-off code-agent）**：image_cache.rs:421-422 SVG intrinsic-size 提取须 ① 仅 width/height attr 作固有 size（usvg 须区分 attr-size vs viewBox-fallback）；② 无 attr 时报「无固有 size」让 tree.rs §10.3.2 默认/ratio 逻辑处理（传 viewBox ratio）；③ 默认 100→**300×150**（§10.3.2）。**复杂度中等**（usvg API 区分 attr vs viewBox + tree.rs §10.3.2 ratio/default 接线，2 sub-bug：viewBox-as-intrinsic + wrong-default）。**cluster yield**：replaced-elements-all-auto/min-height-20/40/min-width-40/80（**5 visudet 案 30-32%**，同 7 SVG + min 约束变体，同根因）+ 推 css-flexbox aspect-ratio-intrinsic（R685/R686 vein）。**★ 与 R696 正交**：R696 = tree.rs:186 非-img 替换 TAG（svg/canvas）无 sizing；R717 = image_cache.rs:421 `<img src=*.svg>` SVG 源固有 size 提取错。两「replaced sizing」bug 不同 code path 不同 facet。**★ 意义（双重）**：① **fresh CSS2/visudet 采样首日即命中 high-yield cluster（5 案 30%，第 13 clean lever）**——§10 sizing subdir 与 normal-flow 同高产 vein；② **replaced-sizing 谱系扩 2 facet**（R696 tag-level + R717 source-intrinsic），code-agent 修 R696 后 R717 是接续 lever。**★ batch 归档**：R697（max-width overflow-clip 负结果）迁出 → archive/rounds-r697-...，master 恢复 last-20（R698-R717）。**hand-off code-agent**：**SVG intrinsic-size viewBox-as-intrinsic（image_cache.rs:421-422）是 high-yield clean-ish lever（第 13，5 案 30%，R685/R686 vein），与 R696 正交可独立修**。**下一步**：续 visudet 次高（inline-block-baseline-015 13.38% / replaced-elements-height-20 9%）或转他 fresh subdir。零源码（reftest-oracle read-only + LAYOUT_DUMP read-only + SVG 文件 read-only + 代码 read-only 定位 image_cache.rs:421-422 + engine.rs:1857，未改任何文件；engine.rs WIP 是 table-cell overflow，对 SVG sizing no-op）。
