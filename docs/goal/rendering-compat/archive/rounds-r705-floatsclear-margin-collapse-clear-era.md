# 历史轮次归档 — R705（CSS2/floats-clear · margin-collapse-clear-015 = CLEAN-ISH 但 COMPLEX era）

> 内容 100% 保留自主控文档 master.md（R705 块逐字迁出，仅作归档）。下文为该轮完整逐轮详记。

**R705（CSS2/floats-clear 续·margin-collapse-clear-015 = CLEAN-ISH 但 COMPLEX·clearance+margin-collapse 算术（#next-yellow 低 ~110px）·cluster head（015/014/012/013）·与 R702 collapse 同族不同 facet（clearance 交互）·第 11 clean-ish lever 候选（complexity caveat）·零源码）：承 R704 CONTINUE 续 floats-clear top **margin-collapse-clear-015（33.66%）**。**预检**：script=0/overflow=0/dynamic=0✓。**test**（Talbot/Fassino，§8.3.1+§9.5.1）：`#parent-lime{border-top:1px;width:50%}` 内 `#float-left-blue{float:left;100×100}` + `#clear-left{clear:left} > div{aqua;height:60;margin-top:140}` + `#next-yellow{height:100}`，assert「clear 元素与首 in-flow 子的 adjoining top margin 应正常 collapse（clearance 不打断）」。**dump**：ZW #clear-left abs_y=**193**（float 底 153 下，clear✓）+ #clear-left 子 abs_y=193（mt=140，**与父同位=collapse✓**）+ **#next-yellow abs_y=352**。**PIL（chromium oracle，裁决：clearance/collapse 算术偏差）**：CHR lime y40-200 / blue float y40-160 / **aqua y160-240** / **yellow y240-350**；ZW aqua(clear-left) y193-253 / **yellow y352-452**。**★ 裁决：ZW #next-yellow 低 ~110px（352 vs CHR ~240）**——ZW 做 clear（clear-left 在 float 下）+ collapse（父子同位），但 **clearance + collapse-through-clearance 算术多出 ~110px 空间**（子 mt=140 未完全 collapse 进 clearance，或 clearance 计算未扣已 collapse margin），致 yellow 下移。**clean-ish 但 COMPLEX**——真 isolable bug（yellow 位置错 ~110px，可见），cluster head（015/014/012/013 clearance+collapse），但 medium-high 复杂（clearance 多步算术 + §8.3.1 collapse-through-clearance 交互，非一行修）。**与 R702 同族不同 facet**：R702 = collapse-through 丢 inline height；R705 = clearance 不打断 collapse 的算术。**hand-off code-agent**：margin-collapse-clear-015 是 clean-ish 但复杂 lever（33%→PASS 须修 clearance+collapse 算术），与 R702 margin-collapse 同 area 可一并评估；cluster yield（015/014/012/013）。**第 11 clean-ish lever 候选**（带 complexity caveat）。**下一步**：floats-clear 余案（floats-124/125 28% 或 R678 float-width 簇收口）或转第 3 个 CSS2 subdir（box-display 139 / linebox 246）；★master.md 现 24 轮（R681-R705），下轮首务 batch 归档恢复 last-20。零源码（LAYOUT_DUMP read-only + PIL oracle read-only + 代码未触碰，未改任何文件）。
