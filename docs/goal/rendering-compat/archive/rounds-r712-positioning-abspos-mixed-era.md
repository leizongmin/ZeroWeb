# 历史轮次归档 — R712（CSS2/positioning 续 · absolute-non-replaced-width-025 = MIXED era）

> 内容 100% 保留自主控文档 master.md（R712 块逐字迁出，仅作归档）。下文为该轮完整逐轮详记。

**R712（CSS2/positioning 续·absolute-non-replaced-width-025 = MIXED·abspos §10.3.7 max-width clamp 正确（R165 延伸确证）+ red 参考块 width:100px 被忽略（w=784）的独立异常·非 crisp lever，须 root-cause·零源码）：承 R711 CONTINUE 续 CSS2/positioning abspos sizing 簇（absolute-non-replaced-width-025/026 16.5%）。选 **025（16.51%，§10.3.7+§10.4，script=0/overflow=0/dynamic=0）** LAYOUT_DUMP。**test**（Talbot，§10.3.7）：`#abs-pos-overlapping-green{position:absolute;left:8;right:8;width:auto;max-width:100;margin:auto}`（应 §10.3.7 算 width→max-width:100 clamp→margin:auto 居中=100px 方块）+ `#overlapped-red{width:100px;margin:auto}`（参考红 100px 居中，被 green 覆盖），assert「green 方块居中无红」。**dump（裁决：MIXED——abspos 正确，red 异常）**：**green(abspos) w=100 x=350 居中✓**（§10.3.7 max-width:100 clamp + margin:auto 居中**工作**，R165 延伸确证）+ **red(in-flow) w=784 x=8**（**width:100px 被忽略！满宽 784 非 100 居中**）——green(100 居中)不覆 red(784 满宽)→red 两侧露出 = 16.51%。**★ 裁决：MIXED**——① **abspos §10.3.7 max-width clamp 正确**（R165 的 abspos margin:auto 居中 + max-width 收束都工作，good news，确证 R165 覆盖 abspos，排除「abspos max-width 缺陷」假设）；② **red 参考 `width:100px + margin:auto` 异常**（normal in-flow block，width:100px 被忽略→w=784）——**独立 bug，非 test 的 abspos 焦点**。**red 异常 root cause 待查**：width:100px on normal block 忽略——疑 ① R165 margin:auto 仅覆盖 abspos/table 未覆盖 normal block 致 width+auto-margin 交互坏，或 ② abspos 兄弟（green 在 red 前）扰乱 red 布局，或 ③ 基础 width 转换缺陷。**非 crisp lever**（root cause 未明，须 minimal repro + 代码 dig）。**★ 意义**：① R165 abspos max-width clamp 确证工作（025 green 正确）；② **发现 normal-block width:100px 忽略异常**（margin:auto 上下文）是新 lever 线索但须 root-cause 确认（可能 R165 regression 或独立 width bug）。**hand-off**：red width:100px-ignored 是 investigation item（须 minimal repro 确认是否 margin:auto 触发 / 是否 R165 regression），非直接 crisp lever。**下一步**：minimal repro red width-ignore（构造 `width:100+margin:auto` normal block 简化案确认 root cause）或续 abspos-008（17.64%）。零源码（LAYOUT_DUMP read-only + PIL oracle read-only + 代码未触碰，未改任何文件）。
