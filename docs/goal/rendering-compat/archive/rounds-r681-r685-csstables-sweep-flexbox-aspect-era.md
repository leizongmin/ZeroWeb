# 归档：R681–R685（css-tables top sweep 收口 + css-flexbox aspect-ratio pivot 起步）

> 从 master.md 迁出的 R681–R685 逐轮详记（按时间倒序，内容 100% 保留）。该区间完成 css-tables top 发散逐案 LAYOUT_DUMP pin：R681（anonymous-cell structural，table-row 满宽）、R682-R684（table-width cluster 横+竖 facet，unify R679/R681/R682 为「table-display 盒满 inline-size」单根因）；并开启 css-flexbox PIVOT（R685 SVG intrinsic-size bug，aspect-ratio cluster 是 mix）。

---

**R685（PIVOT→css-flexbox·LAYOUT_DUMP 第 8 案·aspect-ratio-intrinsic-size-007 = ★SVG img intrinsic-size bug·refute「aspect-ratio cluster」标签·cluster 是 mix·非 aspect-ratio 非 flex·image-path·零源码）：承 R684 css-tables top sweep 完成，按方法论（「structural/cluster」标签常掩盖可隔离 bug，如 css-tables R678/R680/R679）PIVOT 到 css-flexbox。先 `make reftest-oracle DIR=css-flexbox`（497 案，read-only；flexbox 不涉 table-cell overflow→不受 engine.rs:666 WIP 影响=main HEAD honest 数；aggregate 266/497=53.5% chr<1%，与 R670 一致）取 top-15：flex-minimum-width-flex-items-013 82.16% / aspect-ratio-intrinsic-size-007 33.65% / baseline-align-self-baseline-vert-001 32.93% / flexbox_display 24.85% / flex-flow-001 23.13% / direction-row-reverse 21.53% / aspect-ratio cross-size/intrinsic 多案 15-17%——两大簇：aspect-ratio（5+ 案）+ baseline（R266）。read-only 确认 **aspect-ratio 已实现**（apply.rs:665 解析 + tree.rs:209-214 R325 传 taffy，非 font-size-adjust 式未实现 feature）。选次高 aspect 案 **007（33.65%）** LAYOUT_DUMP。方法：`LAYOUT_DUMP=1 make reftest-oracle DIR=aspect-ratio-intrinsic-size-007` + product-smoke 渲 ZW PNG（★**须 `--base-dir`** 解析相对 SVG，否则 img 不加载全白误判）+ PIL。**test**（css-flexbox transferred-size-suggestion）：`display:flex;flex-direction:column` 内含 `<img src="support/large-green-rectangle.svg">`（SVG = `<svg width="100%" height="100%" viewBox="0 0 7500 3750">`，2:1 ratio），assert「automatic preferred physical width considered definite when computing transferred size suggestion」，ref=`<svg viewBox="0 0 1000 500" style="background:green">`（2:1 绿矩形满宽）。**dump**：div(flex column) w=784 h=3750 / **img w=7500 h=3750（=viewBox 像素维！）**。**PIL**：ZW green bbox **x8-798 / y8-599（满至 viewport 底，溢出）** vs CHR green bbox **x8-790 / y8-399（2:1 矩形 ~392px 高，fit 容器）**——两侧 (400,300) 均绿，**唯一发散 = green 高（ZW 满 599 vs CHR 399）**= SVG img 过大（7500×3750）溢出，可见部填满 viewport。**裁决（refute cluster 标签）**：007 **非** aspect-ratio（已实现且非本案机制）**非** flex（flex 仅布局错误的 intrinsic size）——是 **replaced-element SVG img intrinsic-size bug**：ZW **把 SVG viewBox 像素维（7500×3750）当作 img intrinsic size**，忽略 `width="100%" height="100%"`（应 scale-to-container，viewBox 仅供 ratio 2:1）。正确：SVG 100% 尺寸无 intrinsic px，flex 应据 ratio + 容器宽得 ~784×392（CHR 行为）。**★「aspect-ratio cluster」标签对 007 误导**——css-flexbox aspect-ratio 簇实为 **mix**：007 = SVG intrinsic-size（image path）/ 013 = aspect-ratio+min-size（taffy-blocked R670）/ 余案或真 aspect-ratio 或 transferred-size，须逐案 LAYOUT_DUMP 区分（勿一刀切判「aspect-ratio 簇 structural」）。**hand-off code-agent**：SVG intrinsic-size 修复（viewBox 不作 intrinsic px，honour width/height 属性，viewBox 仅 ratio）在 image 栈（render-foundation），或可隔离但须评 SVG 测试 blast radius；与 R683 canvas intrinsic-sizing（反向：canvas intrinsic 被忽略→满宽）合为「replaced-element intrinsic-size 谱系」候选。**意义**：css-flexbox PIVOT 首案即证方法论价值——「aspect-ratio cluster」泛判被 007 实证为 SVG intrinsic-size bug（image path），cluster 是 mix；与 css-tables 经验一致（structural/cluster 标签须逐案 dump 纠偏）。零源码（LAYOUT_DUMP read-only + product-smoke 渲 PNG read-only **须 --base-dir** + PIL + 代码 read-only，未改任何文件）。

**R684（LAYOUT_DUMP 第 7 案·baseline-vertical = ★R679/R681/R682「table-display 盒满 inline-size」cluster 的 VERTICAL-WRITING-MODE 表现·axis-agnostic 确证·算术精确吻合·css-tables top sweep 完成·零源码）：承 R683 CONTINUE 对 css-tables 末个 top 干净案 baseline-vertical（12.45%）做 LAYOUT_DUMP，完成 css-tables top 扫描。方法：`LAYOUT_DUMP=1 make reftest-oracle DIR=baseline-vertical`（read-only；test overflow=0→不受 engine.rs:666 WIP 影响=main HEAD honest 数）+ product-smoke 渲 ZW PNG + PIL 定位。**test**（css-tables-3 §table-cell-baseline）：两个 `writing-mode:vertical-lr`/`vertical-rl` 的 table，各 1 row × 2 td（`vertical-align:baseline; background:red; background-clip:content-box`，border 1/2/3/4 + padding 5/6/7/8），内含 `div width:50 height:100 background:green`，assert「vertical-mode + baseline + tall 元素不应产生 excess space」（green 应覆 td，无红）。**dump**：table1 **h=780 w=148 pt=5** / tbody h=764 / tr h=764 / **td h=764 w=64**（两 td 并排 x20/x84，vertical-lr block flow 水平✓）/ div **h=100 w=50**（仅 100px 内容！）。**PIL**：ZW red bbox **x32-138 / y20-599（满至 viewport 底！）**（td 红背溢出）+ green div 仅顶部 y26-125；CHR green 全覆 **red=None**（无 excess），non-white y8-503。**裁决（算术精确确证 cluster）**：**table screen-高 780 = body 内容宽 784 − 垂直 border(1+3=4) = 780 精确吻合**——ZW 把 **table 的 inline-size 当作 containing-block 可用宽（784）**，在 **vertical writing mode 下未旋转**→inline 轴（屏幕垂直）被填到 780 而非 shrink-to-fit 内容（div ~50/100）。即 **R679/R681/R682「table-display 盒取满容器 inline-size（784）而非 shrink-to-fit」cluster 在 vertical writing mode 的表现**（旋转轴→table 屏高≈784），**cluster 现 axis-agnostic**（R679/R681/R682 horizontal + R684 vertical）。**"baseline" 非主导**：vertical-align:baseline 不致 excess red（红满至 y599 是 inline-size 填充，非 baseline 偏移；CHR 无红），test 名误导（真因 table inline-size sizing 非 baseline）。**裁决**：STRUCTURAL——vertical-mode + table-sizing 交叉（table-width cluster vertical 表现 + R114/R142 writing-mode 轴处理），**非 clean 单点 lever**；但**精确归因 cluster**（算术吻合），hand-off code-agent：table inline-size sizing 须 writing-mode-aware（rotated 轴下用 shrink-to-fit 而非 unrotated available-width），与 R679/R681/R682 同修。**★ css-tables top sweep 完成**：头部发散逐案 pin——empty-table-height 30.74%(R679) / table-cell-width-0 20.09%(R680) / anonymous-cell-margin 16.53%(R681) / percent-height-replaced-003 14.95%(R683) / quirks-mode 14.95%(R682) / baseline-vertical 12.45%(R684)；**仅余 percent-height-overflow-auto 17.40%（overflow→engine.rs:666 WIP 影响，待 WIP 落地后测）**。**css-tables top 归类**：R679/R681/R682/R684 = 「table-display 盒满 inline-size」cluster（box-level，horizontal+vertical）；R683 = inverse content-level；R680 = br-height（独立真 bug）；R678 = float shrink-to-fit（独立真 bug，css-fonts）。**意义**：LAYOUT_DUMP 7 案——3 clean-lever 候选（R678/R679/R680）+ 4 structural（R681/R682/R683/R684 归 table-width sizing 谱系）；css-tables 头部结构性发散**统一为单一 table-width-sizing 缺陷家族**（box/content/horizontal/vertical 四 facet），code-agent 一个彻底的 table-width 修复（writing-mode-aware shrink-to-fit + 子节点约束）或解 4+ 案。零源码（LAYOUT_DUMP read-only + product-smoke 渲 PNG read-only + PIL + 代码 read-only，未改任何文件）。

**R683（LAYOUT_DUMP 第 6 案·percent-height-replaced-in-percent-cell-003 = canvas %height 正确·table 盒已正确 shrink·发散=cell 子节点（canvas+div）满宽溢出·★R679/R681/R682 cluster 的 INVERSE facet·零源码）：承 R682 CONTINUE 对 css-tables 次头部发散 percent-height-replaced-in-percent-cell-003（14.95%）做 LAYOUT_DUMP（同簇 002/004=2.74% / tentative=0.70%）。方法：`LAYOUT_DUMP=1 make reftest-oracle DIR=percent-height-replaced-in-percent-cell`（read-only；test overflow=0→不受 engine.rs:666 WIP 影响=main HEAD honest 数）+ product-smoke 渲 ZW PNG + PIL 定位。**test**（css-tables-3）：`display:table` > cell1(`height:50px`，含空 div + `<canvas width=1 height=1 style="height:200%;background:green">`) + cell2（含 `div height:100px`），assert「replaced 元素 %height 解析 + intrinsic ratio → 100×100 绿方块」（canvas height:200%→100px，width auto→1:1 ratio→100px）。**dump**：table(div) **w=19.2 h=100（已正确 shrink！）** / cell1 w=9.6 h=100（从 50px 长到 100✓，cell height 作 min） / cell2 w=9.6 h=100 / **canvas h=100（200%×50=100 ✓ 正确）w=784（满宽！）** / cell2 内 `div height:100px` w=784（满宽）。**PIL**：ZW green bbox **x8-790（783 宽）/ y51-150** vs CHR green bbox **x8-106（99 宽）/ y50-149**——canvas 高度正确（两侧 100px），**唯一发散 = canvas 宽 783 vs CHR 99**。**裁决**：① **canvas `height:200%` 解析正确**（h=100）+ **table 盒正确 shrink-to-fit（19.2）+ cell height 作 min-height 正确（50→100）**——**refute %height 假设**（%height 工作）；② **14.95% 发散 = cell 的子节点（canvas replaced + block div）渲染满容器宽 783 而非约束到 shrink 后的 cell 内容宽（~9.6）**：canvas 应按 intrinsic ratio（height 100px × 1:1）得 100px 宽，ZW 给满宽 784（replaced 元素当 block 填可用宽，未做 intrinsic sizing）。**★ 本案是 R679/R681/R682「table-display 盒满宽」cluster 的 INVERSE facet**——R679/R681/R682 是 **table 盒本身满宽（应 shrink）**；R683 是 **table 盒已正确 shrink（19.2）但 cell 子节点满宽溢出**（box 收缩 vs 子节点不约束，方向相反）。**共同主题**：ZW table 宽度 sizing 的「cell 内容宽」与「子节点布局宽」脱节——available-width(784) 在错的层级泄漏（R679/R681/R682 泄漏到 box 本身，R683 泄漏到 cell 子节点）。**裁决**：distinct facet，likely structural/entangled（table 内容宽约束 + replaced intrinsic sizing，R177/R364/R499 territory），**非 clean 单点 lever**，但与 cluster 同 area（一个彻底的 table 宽修复须同时处理 box-sizing + 子节点约束两层级）。**hand-off code-agent**：R679/R681/R682/R683 合为「table 宽度 sizing」广义谱系——box-level（cluster，3 案满宽）+ content-level（R683，子节点满宽溢出 shrink cell）+ replaced intrinsic sizing（canvas 未按 ratio）；修 table 宽须两层级都顾。**意义**：LAYOUT_DUMP 6 案——R678/R679/R680 真 bug（3 clean-lever 候选）+ R681/R682/R683 structural（R681/R682=cluster box 满宽 / R683=inverse content 满宽，3 案归「table 宽 sizing」广义谱系）；R683 再证 pixel ground truth（canvas 高对宽错）+ dump width 可信。零源码（LAYOUT_DUMP read-only + product-smoke 渲 PNG read-only + PIL + 代码 read-only，未改任何文件）。

**R682（LAYOUT_DUMP 第 5 案·percentages-grandchildren-quirks-mode-001/002 = ★refute %height-quirk 假设·实属 R679/R681「table-display 盒→满容器宽」cluster·3 案统一根因·零源码）：承 R681 CONTINUE 对 css-tables 次头部发散 percentages-grandchildren-quirks-mode-001/002（各 14.95%）做 LAYOUT_DUMP。方法：`LAYOUT_DUMP=1 make reftest-oracle DIR=percentages-grandchildren-quirks-mode`（read-only；两 test 均 overflow=0→不受 engine.rs:666 WIP 影响=main HEAD honest 数）+ product-smoke 渲 ZW PNG + PIL 定位。**test**（两案无 `<!DOCTYPE>`→**quirks mode**，css-tables-3 %height）：001 `display:table-cell;height:100px;background:green` > `div width:100px`(auto) > `div height:100%;background:red`，assert「cell 子节点不施 %height quirk」（red 应 0→绿）；002 cell bg:red > `div width:100px;height:100%` > `div height:100%;background:green`，assert「cell 子节点传正确 % 解析尺寸」（green 应 100 覆盖红）。两案均期望 100×100 绿方块。**dump + PIL（pixel 为 ground truth）**：① **001**：ZW green bbox **x8-790（满宽 782）/ y51-150**，**red=None（无红！）** vs CHR green x8-106——ZW **%height 正确**（red=0 绿显，符合期望），**唯一发散 = table-cell 满宽**；② **002**：ZW green bbox **x8-106（100×100，正确！）** + **red bbox x108-790（满宽余部）/ y51-150** vs CHR green x8-106 only——ZW **%height 正确**（green=100 覆盖），**唯一发散 = table-cell（red 背）满宽 784**。**裁决（refute test-name 假设）**：发散 **非** %height quirk——ZW 两案 %height 解析在 paint 均**正确**（001 red=0 / 002 green=100），test 名误导（quirk 实已工作）；**~94% 发散 = `display:table-cell`（无 table 父→应生成匿名 table 包装盒）渲染满容器宽 784 而非 shrink-to-fit 到内容宽 ~100**（001 多出 green 682×99≈67.7k / 002 多出 red 682×99≈67.5k，各占总发散 71.8k 的 ~94%）。**★ UNIFIES R679+R681+R682 为单一根因簇**：「table-display 盒（`display:table` R679 / `table-row` R681 / `table-cell` R682）width:auto 取满容器宽 784 而非 shrink-to-fit 内容宽」——css-tables 头部发散 empty-table-height(30.74%)+anonymous-cell-margin(16.53%)+quirks-mode(14.95%) **3 案同根**，合计 ~62% css-tables 头部发散，**一个修复机制（table-display 盒 shrink-to-fit 宽度）或解多案**。**★ 注意 dump 高度不可靠**：002 dump 显示 `%height` div `h=0`（middle+green 均 0）但 paint 渲 100——**storage height≠paint height**（R101/R617 paint-box≠storage-box 谱系），dump height 字段对 %height 解析盒**不可信**；但 **width 字段可信**（cell w=784 与 pixel green/red bbox 满宽一致），本案分类据 width+pixel。**hand-off code-agent**：R679/R681/R682 统一为「table-display 盒 shrink-to-fit 宽」单一缺陷（修 scope=table-box width sizing，R177/R364 territory，entangled 但根因已精确界定，3 案 yield）；与 R678（float 0-content shrink-to-fit，独立）/ R680（br 行盒高度，独立）正交。**意义**：LAYOUT_DUMP 5 案——R678/R679/R680 真 bug（3 clean-lever 候选）+ R681/R682 structural（均归「table-display 盒满宽」簇，R682 同时 refute %height-quirk 假设并 unify 3 案）；方法论再证：pixel ground truth 纠正 dump-height 误读 + test-name 误导，unify 散落结构性案为可操作根因簇。零源码（LAYOUT_DUMP read-only + product-smoke 渲 PNG read-only + PIL + 代码 read-only，未改任何文件）。

**R681（LAYOUT_DUMP 第 4 案·anonymous-table-cell-margin-collapsing = ★STRUCTURAL 首案·green table-row 取满容器宽（784）而非 table shrink-to-fit（~100）+ margin 穿透 table·非 clean lever·打破 3/3 连中·零源码）：承 R680 CONTINUE 对 css-tables 第 3 大发散 anonymous-table-cell-margin-collapsing（16.53%）做 LAYOUT_DUMP。方法：`LAYOUT_DUMP=1 make reftest-oracle DIR=anonymous-table-cell-margin-collapsing`（read-only，test-guard 包裹；test overflow 计数=0→不受 engine.rs:666 WIP 影响=main HEAD honest 数）+ product-smoke 渲 ZW test PNG + PIL 定位。**test**（§9.4.1 BFC）：`display:table;height:100px;background:red` 内含 3 个 `display:table-row`（前 2 个 bg:green，含 `<div margin:50px 0>` 子节点，div 非 table-cell→生成匿名 table-cell），assert「100×100 绿方块无红」。**dump**：table(div) **w=109.6 h=100 abs_y=85 mt=50**（mt=50 来自内部 `margin:50px 0` **穿透 table 泄漏**——table/匿名 cell 未建立 BFC），其内 **table-row w=784（满容器宽！）** ×3（row1 abs_y=85 h=50 / row2 abs_y=135 h=50 / row3 h=0），匿名 cell 内容（`width:100px` div w=100，空 div w=9.6）。**PIL 实证**：ZW green bbox **x8-790（782px 宽，满宽）/ y85-184** vs CHR green bbox **x8-106（~100px 方块）/ y50-149**——ZW green 满宽 782 vs CHR 100，**green 面积差 ~67.7k px ≈ 85% 总发散（79343px）**= 主导。**裁决：STRUCTURAL**——① **主导（85%）= table-row 背景取满容器宽 784 而非 table shrink-to-fit 宽（~100）**：table box shrink-to-fit 到 109.6（大致对，含空 cell 9.6）**但内部 row 未被约束到 table 宽**（row 仍 784=可用宽），green 行背景填满 784→这是 table 内部宽度传播缺陷（table box 收缩未传到 row group/row），与 R177/R364 table.rs build_grid + R679 shrink-to-fit 谱系 entangled；② **次要 = margin 穿透 table**（table mt=50 泄漏，y85 vs CHR y50）= table/匿名 cell 未建立 BFC（§9.4.1，R82/R83 BFC-004 territory），单独修不 yield（主导 85% 仍在）。**★ 打破 R678/R679/R680 的 3/3 连中**——本案是 LAYOUT_DUMP vein **首个真 STRUCTURAL 案**（table 内部 row 宽传播 + BFC 建立，多机制 entangled，非单点），证实方法论**非 cherry-pick**（honest 分类：遇 structural 如实判 structural）。**refine R670「anonymous-table-cell-margin-collapsing 16.53%」为 structural**（table-row-width 传播主导，非可单 session 修）。**hand-off code-agent**：若攻 table 内部，row 须约束到 table shrink-to-fit 宽（修后 green 782→~100，主导发散消，残余 margin 穿透 + 空cell 宽独立）；但属 R177 table.rs entangled territory（5 部件耦合，见 R177 memory），非 clean lever，须 A/B 评回归。**意义**：LAYOUT_DUMP 4 案汇总——R678 float 真 bug / R679 table-width 真 bug / R680 br-height 真 bug（3 clean-lever 候选）+ R681 structural（table-row-width 传播，非 lever）；css-tables 头部发散逐案 pin 完成（empty-table-height R679 / table-cell-width-0 R680 / anonymous-cell-margin R681），下一案可转他目录或续 css-tables 次头部。零源码（LAYOUT_DUMP read-only + product-smoke 渲 PNG read-only + PIL + 代码 read-only，未改任何文件）。
