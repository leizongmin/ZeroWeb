# 轮次详记归档 — R2902–R2909（8 轮，2026-08-07）

> 从 `master.md` §最近轮次摘要 逐字迁出（R2921 doc-control 瘦身，零删减/未重排，保留 master.md 文件序＝逆时序 R2909→R2902）。
> 归档时 held baseline `make test` 13495/0/74；clean-lever surface 11 角度 definitively 穷尽（self-source 全 dir + oracle 8 dir + 高通过 dir 实测 + source-marker 角度）。
> 各轮结论已沉淀于 master.md 状态锚 + 通过率详细快照 + 待用户决策清单，迁出详记不丢失当前指引。

> **📍 R2909（2026-08-07）🔍 fresh-angle 源码 partial-impl marker 扫描（第 11 验证角度）+ lark-cli/use-proxy 环境再核验 = clean/absent——自主推进面第 11 角度源码层独立佐证 definitively 穷尽 + 通知通道确认仍不可用（真待命轮）**。承接 R2908（off-cadence make test 13495/0/74 既证）。**无触发条件**（远程 `git fetch` = 0 behind + 本地工作树 clean + cadence ~R2918 未到 + product-smoke R2907 + make test R2908 双近），本轮做**有界低成本验证**（非重跑昂贵测试）。**① 第 11 验证角度——源码 partial-impl marker 扫描**（正交于前 10 vein 的 property-table/value-level 审计，本次 source-marker 级）：`grep -rE "unimplemented!|todo!\(|TODO|FIXME|未实现|暂未|待实现|stub"` 全 css-parser + style-system src（排除 test）= **仅 1 命中**（cascade.rs:271 历史注释「未实现前 all...」描述 R2386 已 land 的 `all` 简写过去态，stale 注释非 gap）→ **CSS 解析/样式层零 partial-impl gap marker**，从源码层独立佐证 clean-lever surface 穷尽（前 10 vein 从 property/value 层证，本角度从 source 层证，双向闭环；后续轮勿再扫 marker 角度）。**② lark-cli / `~/use-proxy` 环境再核验**（R2891 后 18 轮，环境或变）：`command -v lark-cli` = NOT FOUND + 无 lark 二进制 + `~/use-proxy` 不存在 = **通知通道 + 代理仍不可用**（与 R2891 一致），卡点仅能经 master.md + rally 输出呈现，无法经飞书主动告知用户待授权事项。**结论**：自主推进面经 11 角度（10 property/value vein + 1 source-marker vein）双向穷尽闭环；唯一 unlock（Phase A IFC line-box-metric 统一 + font-stack coherence rebuild 整体）user-gated（2026-07-29 护栏 + 2026-08-04 降频守成 + R2025 不自主开工），通知通道不可用。**无 .rs land**——held baseline 13495/0/74（R2908 全量 fresh 证）。**下一步**：守 plateau-guard（下次 cadence ~R2918 或父目标 .rs drift 复跑；rendering 维度 product-smoke R2907 welcome 17.03% 既绿）+ 等用户授权 Phase A IFC + font-stack rebuild 整体（唯一 font-wall/Mission 65% oracle unlock；设计就绪 unified-font-stack-design.md v0.2.3）+ R1043 vertical-mode / R2174 taffy border-box / multicol-Phase-2 / form-control-native / srcset 任一深结构点名即切回主动推进；clean-lever 11 角度穷尽勿重复扫描（含 source-marker 角度）。

> **📍 R2908（2026-08-07）🛡️ plateau-guard make test fresh 复跑 = 13495/0/74 EXIT=0 零 FAIL——off-cadence 证稳，确认 held baseline 跨 15+ 轮纯 doc/standby plateau（自 R2879 最后 .rs 代码获胜）零漂移**。承接 R2907（product-smoke 渲染 plateau-guard welcome 17.03% 既绿）。**无触发条件但主动复验**：远程 `git fetch origin main` = **0 behind**（父目标 zero-web 无新 .rs drift 入树）+ 本地工作树 clean（R2879 后零 .rs land，R2900-R2907 全 doc/measurement）+ cadence ~R2915 未到 + product-smoke R2907 刚做——严格按运行规则本轮无 mandatory 复验触发。**但按 rally 任务「确保所有测试都通过」+ 距上次全量 make test（R2905）已 3 轮 + 15+ 轮纯 doc/standby 需 fresh 全量证**，本轮跑全量 `make test`（test-guard 包裹，~6min，release 二进制零重编 cached）：**passed=13495 failed=0 ignored=74，EXIT=0，0 FAIL/panic**——精确持平 R2897/R2898/R2900/R2905 held baseline，证跨 15+ 轮纯 doc/standby + 工具链/依赖/环境零漂移，baseline 稳（全量 fresh 证非断言；74 ignored 仍 = real_website_compat 网络用例）。**cadence 时钟重置**：本次 off-cadence 全量绿复跑等效刷新「距上次验证」计数 → 下次 cadence ~R2918 或父目标 .rs drift 触发。**自主推进面状态再确认**：clean-lever surface 经 self-source 全 dir + oracle 8 dir + 本 session 高通过 dir（css-counter-styles/css-text-decor/css-fonts/css-text/css-flexbox/css-grid/css-content）全 triage 实测 = **definitively 穷尽**；残余全 font-wall/Phase A font-metric/IFC/R1043/R2174/host-layer 深（user-gated，2026-07-29 护栏 + 2026-08-04 降频守成 + R2025 不自主开工）。**无 .rs land**——held baseline 13495/0/74（本轮全量 fresh 证）。**下一步**：守 plateau-guard（下次 cadence ~R2918 或父目标 .rs drift 复跑；rendering 维度 product-smoke R2907 既绿）+ 等用户授权 Phase A IFC line-box-metric 统一 + font-stack coherence rebuild 整体（唯一 font-wall/Mission 65% oracle unlock；welcome 17.03% 残余 = 中文字体度量 font-wall，非布局 bug）+ R1043 vertical-mode / R2174 taffy border-box / multicol-Phase-2 / form-control-native / srcset 任一深结构点名即切回主动推进。

> **📍 R2907（2026-08-07）🛡️ product-smoke 渲染 plateau-guard（distinct 维度）= welcome diff **17.03%**（81737/480000px）精确持平 R2544 baseline，EXIT=0 全 struct-check PASS——证父目标 engine .rs（R2704-R2862 getComputedStyle/computed-value）零产品渲染回归**。承接 R2906（状态锚刷新）。无 .rs drift（R2900 后）+ make test cadence 未到（~R2915），本轮补 **rendering 维度 plateau-guard**（make test 单元测试不覆盖产品渲染；R541 教训：welcome +7.65pp 回归藏 14 轮）。`make product-smoke`：welcome @800 vs chromium Oracle（welcome-chromium.png，--max-diff 20）= **diff 17.03%（81737/480000px）EXIT=0**——精确持平 R2544 re-baseline（17.03%），证父目标 R2704-R2862 系列 engine .rs（getComputedStyle 序列化 + atob/btoa/crypto/TextEncoder/URLSearchParams/structuredClone/queueMicrotask/DOMException/AbortController/URL/EventTarget/MessageChannel/BroadcastChannel/location/CSS.escape/document.cookie 等 +291 测试推进）**零产品渲染回归**（getComputedStyle 读侧 JS bridge 不改 layout/paint，welcome 渲染几何未变）。**struct-check 全 PASS**：welcome @800/@375/@320（card:4/shortcut:6/link-tile:4/footer:1/title:1/tagline:2 + sibling-overlap + img-visible）+ morning-work article @320（article:1）。**结论**：rendering 维度 plateau-guard 绿——产品渲染面（welcome 17.03%）+ 结构面（全 struct PASS）跨父目标 engine 工作零回归，与 make test（R2905 13495/0/74）单元面双维证稳。**无 .rs land**——held baseline 13495/0/74（零 .rs drift；welcome 17.03% fresh 渲染证）。**下一步**：守 plateau-guard（rendering 维度 product-smoke + 单元维 make test 双维既绿；下次 cadence ~R2915 或父目标 .rs drift 复跑）+ 等用户授权 Phase A IFC + font-stack coherence rebuild 整体（唯一 font-wall/Mission 65% oracle unlock；welcome 17.03% 残余 = 中文字体度量 font-wall，非布局 bug）+ R1043/R2174/multicol-Phase-2/form-control-native/srcset 点名切回。

> **📍 R2906（2026-08-07）🔧 doc-control：顶部「当前 rally 进度态」状态锚 R2873→R2906 刷新（32 轮 stale 纠偏）**。承接 R2905（cadence make test 13495/0/74 fresh 证稳）。无 .rs drift + cadence 刚重置（~R2915），本轮转 **doc governance**（治理原则 §2 自洽）。**根因**：顶部「📍 当前 rally 进度态」状态锚停在 **R2873**（32 轮前），引用旧 13193 baseline + R2572-R2638 era 9-vein 审计细节，与当前（baseline 13495/0/74 + 本 session R2899-R2905 实测穷尽 + R2903 era 归档）严重不一致——状态锚职责是「当前态速览」，stale 致误导。**刷新**：awk 行替换（line 13）为简洁当前态——R2906 守成形态 + held baseline 13495/0/74 + Mission 分阶段里程碑（65/80/95）+ 自主推进面 definitively 穷尽（含 R2899 marker ×0.85 meta-洞见）+ 唯一 unlock（Phase A IFC + font-stack，user-gated）+ 待点名深结构清单 + lark-cli/use-proxy 环境约束 + 导航锚（§最近轮次摘要 R2896-R2905 + era 归档）；历史 R2572-R2638 细节已 100% 在 era 归档 `rounds-r2628-r2893-summary.md`，状态锚不再背负。**注**：line 11（2026-08-04 决策）保留「13192」为历史决策时基线（历史文本不动），当前 13495 在刷新后的状态锚（line 13）。**held baseline 13495/0/74**（零 .rs drift，纯 doc 零回归）。**下一步**：守 plateau-guard（零 .rs drift，cadence ~R2915 或父目标 .rs drift 复跑）+ 等用户授权 Phase A IFC + font-stack coherence rebuild 整体（唯一 font-wall/Mission 65% oracle unlock）+ R1043/R2174/multicol-Phase-2/form-control-native/srcset 点名切回。

> **📍 R2905（2026-08-07）🛡️ plateau-guard make test cadence 复跑 = 13495/0/74 EXIT=0 零 FAIL，全量 fresh 证据证 baseline 跨 R2900→R2905 五轮纯 doc/measurement plateau 零漂移**。承接 R2904（WOFF2/css-text 实测关闭）。无 .rs drift（R2900 后零 .rs land，R2901-R2904 全 doc/measurement），距上次全量 make test（R2900 drift-triggered）已 5 轮。本轮跑全量 `make test`（release 二进制零重编 cached，~5min）：**passed=13495 failed=0 ignored=74，EXIT=0，0 FAIL/panic**——精确持平 R2897/R2898/R2900 held baseline，证 5 轮纯 doc/measurement + 工具链/依赖/环境零漂移，baseline 稳（全量 fresh 证非断言）。**cadence 时钟重置**：下次 cadence ~R2915 或父目标 .rs drift 触发。**自主推进面状态确认**：clean-lever surface 经 self-source 全 dir + oracle 8 dir + 本 session 高通过 dir（css-counter-styles/css-text-decor/css-fonts/css-text）实测 + WOFF2/font-language-override/font-size-adjust 两值/hiragana 全实测关闭 = **definitively 穷尽**；残余全 font-wall/Phase A font-metric/IFC/R1043/host-layer 深（user-gated）。**无 .rs land**——held baseline 13495/0/74（本轮全量 fresh 证）。**下一步**：守 plateau-guard（下次 cadence ~R2915 或父目标 .rs drift 复跑）+ 等用户授权 Phase A IFC + font-stack coherence rebuild 整体（唯一 font-wall/Mission 65% oracle unlock，font-stack R2025 硬 block）+ R1043/R2174/multicol-Phase-2/form-control-native 点名切回；clean-lever/lever 候选全经实测关闭勿重复扫描。

> **📍 R2904（2026-08-07）🔍 WOFF2 self-source 影响经实测关闭（css-text 84 fail 仅 2 案涉 .woff2 → 多 false-pass，WOFF2 非 self-source lever）+ css-text 84 fail 全簇分类（white-space 17 散落边缘/CSS Text 4，无 clean lever）**。承接 R2903（WOFF2 记为「潜在 enabler，self-source 多 false-pass」**未实测**的开放断言）。无 .rs drift + cadence 未到（~R2907），本轮 **实测关闭 R2903 留下的 WOFF2 开放问题**。`make reftest-upstream FILTER=css-text` = 1742/1826=95.4%（持平 R2456 零 drift，84 fail）。**WOFF2 self-source 实测**：84 fail 中仅 **2 案**（hyphens-shaping-002 / shaping-arabic-diacritics-002）引用 `.woff2`——证 css-text 108 个 `.woff2`-referencing 测**绝大多数 self-source false-pass**（test+ref 同失 .woff2 字体 → 一致系统字体回退 → 0% PASS），WOFF2 实装 self-source 最多 flip 2（且 2 案 fail 可能另有 shaping 根因非纯字体加载）→ **WOFF2 非 self-source lever 确认**（R2903 defer 实测硬化）；oracle 侧 fontdue 光栅 font-wall 仍主导故 oracle ROI 亦低。**css-text 84 fail 全簇分类**（subdir 计）：white-space(17) / text-group-align(10, CSS Text 4) / hyphens(10, 连字符字典+测字体深) / text-align(8) / hanging-punctuation(6, CSS Text 3/4 未实现) / text-fit(5, CSS Text 4 极新) / letter-spacing(5) / text-autospace(4, CSS Text 4) / text-transform(3) / line-breaking(3) / word-space-transform(2) / word-break(2) / text-stroke-width(1) / shaping(1) / boundary-shaping(1)。**white-space 17 fail 散落**（control-chars×4 / object-replacement×2 / pre-wrap+align×4 / text-wrap:balance·nowrap×3〔CSS Text 4〕/ pre-line 边缘 / intrinsic-size / trailing-spaces），**非单未实现值**（break-spaces 已实现 types.rs:300，pre-line 已实现 :298）→ 散落边缘 + CSS Text 4 + IFC 交互，非 clean lever。**结论**：css-text 84 fail 全深（hyphens 字典/shaping/IFC/font-wall）或 CSS Text 4 未实现特性（text-group-align/hanging-punctuation/text-fit/text-autospace/text-wrap:balance——皆 bleeding-edge 非核心，effort 大 ROI 低非自主 clean lever）或散落边缘，**无 clean lever**；WOFF2 经实测关闭。**无 .rs land**——held baseline 13495/0/74（零 .rs drift，guard 窗口健康）。**下一步**：守 plateau-guard（零 .rs drift，cadence ~R2907 或父目标 .rs drift 复跑）+ WOFF2/white-space/css-text 全经实测关闭勿重复 + 唯一推进 Mission 65% oracle = 用户授权 Phase A IFC + font-stack coherence rebuild 整体（唯一 font-wall unlock）+ R1043/R2174/multicol-Phase-2/form-control-native 点名切回。

> **📍 R2903（2026-08-07）🔧 doc-control 瘦身：§最近轮次摘要 内联累积 107 轮详记（R2628–R2893）era 合并归档 → master.md 422→216 行；附 font-language-override / WOFF2 杠杆候选核验（皆深，非 lever）**。承接 R2902（高通过 dir 探针完结，clean-lever surface definitively 穷尽）。无 .rs drift + cadence 未到（~R2907），本轮转 **doc governance**（goal 强制「master.md 不允许无限增长」）。**根因**：R2627 后 doc-control 8 轮批归档停更，R2628+ 逐轮 blockquote 累积膨胀至 **§最近轮次摘要 114 内联条目 / 222 行**（master.md 总 422 行，详记占 >半）。**era 合并归档**：awk 抽取 lines 31-235（R2893–R2628，107 条逐字保留）→ `archive/rounds-r2628-r2893-summary.md`（209 行，零删减/未重排，R2894/R2895 无独立条目注）+ sed 删 lines 31-236（git 兜底）→ §最近轮次摘要 现仅留 R2896–R2902（7 轮健康 recent 窗口），**master.md 422→216 行**（-206 行）；轮次详记归档区 + 历史索引区两处加 R2628–R2893 era 条目。**附杠杆候选核验（顺带，皆 deep 非 lever）**：① **font-language-override-02 (css-fonts 0.00% FAIL)** = OpenType language system tag（须 rustybuzz shaping 时应用 lang 特定替换，shaping 层深）+ LinLibertine .woff font（WOFF1 已支持非阻塞）；② **WOFF2（`wOF2` brotli）未支持**——css-text 108 + css-fonts 4 测用 `.woff2`；`brotli` **已是既有传递依赖**（Cargo.lock 确认，非新 dep），但 WOFF2 解码须 brotli 解压 + glyf/loca/hmtx 变换逆转 ~300-500 行；且 self-source `.woff2` 测多 false-pass（test+ref 同失字体一致回退），oracle 侧 fontdue 光栅 font-wall 仍主导 → **ROI 不确定且 effort 大，非 clean lever，记潜在 enabler 不自主开工**。**结论**：本轮无 .rs land；doc governance 瘦身是高价值轻量活（goal 强制 + 改善未来轮 doc 可读性）。**held baseline 13495/0/74**（零 .rs drift，guard 窗口健康；纯 doc 零回归）。**下一步**：守 plateau-guard（零 .rs drift，cadence ~R2907 或父目标 .rs drift 复跑）+ 高通过 dir 探针 + WOFF2/font-language-override 杠杆候选皆 deep 已关闭勿重复 + 唯一推进 Mission 65% oracle = 用户授权 Phase A IFC + font-stack coherence rebuild 整体（唯一 font-wall unlock）+ R1043/R2174/multicol-Phase-2/form-control-native 点名切回。

> **📍 R2902（2026-08-07）🔍 css-fonts self-source 全 triage（97.9% 高通过 dir，本 session 首测）= 6 fail 全 CSS Fonts 4 两值语法 / font-metric Phase A / OpenType-lang / @font-face 深特性，无 clean lever——高通过 dir 探针完结**。承接 R2901（css-text-decor 无 lever）。无 .rs drift（R2900 后）+ cadence 未到（~R2907），续探最后一个未聚焦高通过 dir。`make reftest-upstream FILTER=css-fonts` = **281/287=97.9%（持平快照 R2607 零 drift），6 fail**：① **font-size-adjust-009/010/011 (一致 5.93%) + font-size-adjust-units-001 (5.52%)** = 4 案一致簇；② **size-adjust.tentative (6.90%)**；③ **font-language-override-02 (0.00% FAIL)**。**逐 fail 深查**：① **font-size-adjust 两值语法未实现**——`FontSizeAdjustValue` 枚举仅 `None`/`Number(f64)`（单值 ex-height 基础，types.rs:85），CSS Fonts 4 两值 `<metric> <number>`（cap-height/ex-height/ch-width/ic-width/ic-height/from-font）**未解析**；实测 font-size-adjust-009 用 `font-size-adjust: cap-height calc(1462/2048)` + noto-sans webfont（cap-height=1462/2048），须 **(a) 两值语法解析（6 metric 关键字）+ (b) per-metric 字体表访问（OS/2 sCapHeight/sxHeight、ch/ic 字形 advance）+ (c) per-metric apply 公式** = Phase A font-metric 架构 gap（types.rs:83 注释明列「font-x-height 访问同 Phase A 字体度量架构 gap」+ text_metrics.rs:364-379 apply 注释「非 Ahem 字体 aspect 未知 → Slice 3+」）；现有 R1192 is_ahem-gated narrow apply（Ahem aspect 硬编码 0.8）非 Ahem 不 apply。② **size-adjust** = @font-face `size-adjust` 描述符（按百分比缩放字形，须 font-face 加载 + 字形矩阵缩放，深）。③ **font-language-override** = OpenType language system tag（须 shaping 时 rustybuzz 应用 lang 特定替换，shaping 层深）。**结论**：css-fonts 6 fail 全 Phase A font-metric / CSS Fonts 4 两值 / OpenType-lang / @font-face 深特性，**无 clean lever**。**高通过 dir 探针完结**：css-counter-styles(R2899 marker 残差)/css-text-decor(R2901 IFC 连续性)/css-fonts(本轮 font-metric) 三大 ≥97% dir 全 confirmed 无 clean lever——clean-lever surface 经 self-source 全 dir + oracle 8 dir + 本 session 3 高通过 dir 实测 **definitively 穷尽**，残余全 font-wall/Phase A font-metric/IFC/R1043/host-layer 深（user-gated）。**无 .rs land**——held baseline 13495/0/74（零 .rs drift，guard 窗口健康）。**下一步**：守 plateau-guard（零 .rs drift，cadence ~R2907 或父目标 .rs drift 复跑）+ 高通过 dir 探针已完结勿机械重复（残余低通过 dir 既探皆深）；唯一推进 Mission 65% oracle = 用户授权 Phase A IFC + font-stack coherence rebuild 整体（唯一 font-wall unlock；css-fonts font-size-adjust 两值/font-metric 亦此 territory）+ R1043/R2174/multicol-Phase-2/form-control-native 点名切回。
