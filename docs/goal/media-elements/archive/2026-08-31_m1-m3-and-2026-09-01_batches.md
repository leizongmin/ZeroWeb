# media-elements 里程碑归档 — M1~M3 与六轮扩批（2026-08-31 ~ 2026-09-01）

> 归档自 master.md「当前状态」过程节选（控制面只保留最新态；过程细节在此只追加不修改）。
> 基线演进：46.5% → 73.1% → 79.6% → 84.0% → 88.3% → 89.1% → 90.0% → **90.1%**
> （Fail 77→0，Timeout 13→0，PF 41 恒定非 bug）。

## M1 — WPT 基线建立 + 摸底（2026-08-31）

- `make testharness-media` 基础设施落地（fetch-media-subset.sh + runner 子命令 +
  Makefile 目标）；首批 30 用例 / 245 subtest 基线 **46.5%**（114P/77F/13T/41PF）；
  失败聚类 F1~F6 成文（evidence/2026-08-31-media-baseline.md）。
- **切片 3（F2/F6）**：媒体元数据 IDL 面（currentTime/duration NaN/playbackRate/
  volume clamp/seeking/paused/preload/crossOrigin 枚举反射 + has-trap 白名单）+
  HTMLTrackElement 反射（kind/label/srclang/default/src + R122 实例层同步）+
  `<a href="">` 空串解析修复 → **73.1%**。单测 `test_media_metadata_idl_face_r388`。

## M2 — 状态机与事件序列（2026-08-31，F4）

- `_zwSettleResourceKey` handle/sel 双身份泛化 + headless 加载序列（loadstart→progress→
  durationchange+loadedmetadata→loadeddata→canplay→canplaythrough，networkState
  LOADING→IDLE）+ 动态 `.src=` setTimeout 模拟（精确空串仅 loadstart）+ play/pause
  事件面 + seeking/volumechange/ratechange + on* 兜底派发 → **79.6%**（13T→2T）。
  单测 `test_media_load_event_sequence_r389`；FR-009 集成测试契约更新（error 路径
  断言零改动通过 = A/B 佐证）。

## M3 — API 语义 + track 面（2026-08-31，F1/F3）

- TextTrack/TextTrackCueList/TextTrackList/TextTrackCue 接口声明（Illegal constructor +
  工厂原型链）+ `addTextTrack`（枚举精确校验 + WebIDL DOMString 转换）+ `textTracks`
  （same-object + 增量同步）+ `track.track`（身份缓存 + default→showing）→ **84.0%**
  （Fail 清零）。单测 `test_text_track_family_r390`。

## 扩批 I — event_* 族（2026-09-01，下一步计划 #1）

- 25 个 event_* 上游用例接入（autoplay + noautoplay + order_* 五件）→ **88.3%**。
  支撑语义：`play()` 返 pending Promise（resolve 于播放推进；pause() reject 未决
  promise AbortError）；`_zwMediaLoadSequence` 增 tag（video 派 resize：durationchange
  后 loadedmetadata 前）；autoplay/play() 路径补派 timeupdate。
  单测 `test_audio_constructor_and_media_methods_r2835` 扩展。

## 扩批 II — source-child 资源选择 + error 码（2026-09-01，余账 2T 清零）

- **source-child 资源选择触发**（appendChild 钩子，part04）：source 子插入 audio/video
  父 → 调度 headless 加载序列（WPT currentSrc「adding source element」族解锁）。
- **空 src 错误码**（`_zwSettleResourceKey` 增 errorCode 参数）：error 事件 +
  `MEDIA_ERR_SRC_NOT_SUPPORTED(4)` + MediaError 实例 + currentSrc 恒空
  （WPT error-codes 空串族解锁）。
- **source.src URL 绝对化**（get trap SOURCE 分支，同 track.src 模式）→ **89.1%**
  （Timeout 清零）。单测 `test_media_source_child_and_error_code_r391`。

## 扩批 III — volume/muted setter + Audio 构造器（2026-09-01）

- volume：非有限 TypeError；同值短路；volumechange 改 **queued** 派发（media element
  task source）；`pendingVc` 标记 + load() 清除（spec dom-media-load「pending events
  丢弃」）。
- volume getter `!= null` 守卫（_mediaState entry 被 muted setter 先建时防 undefined）。
- muted IDL setter + getter（dirty 优先/attr presence 回落）；has-trap 补列。
- Audio 构造器：preload='auto' + 无 new TypeError；HTMLAudioElement Illegal constructor
  + prototype 链补接（audio_constructor 11 subtest 全绿）；R2835 旧断言随 spec 纠正。
- 导入 volume_nonfinite / event_volumechange / audio_constructor → **90.0%**
  （+35 新通过 0 回归）。单测 `test_media_volume_muted_semantics_r392`。

## 扩批 IV — controlsList + the-video-element 起步（2026-09-01）

- `_classListProxy` 增 supportedTokens 参数——`supports(token)` 四值表精确匹配
  （nodownload/nofullscreen/noplaybackrate/noremoteplayback；未传表恒 false）。
- get trap 增 controlsList 分支（gate：HTML ns audio/video；gate-miss → undefined）。
- 导入 controlsList.tentative.html + video-tabindex.html → **90.1%**。
  单测 `test_media_controls_list_r393`。
- 决策记录：audio_volume_check/video_volume_check **不导入**（越界 IndexSizeError
  是旧 spec 断言，与现行 clamp 语义及 volume_nonfinite 冲突）。

## 扩批 V — playbackRate 非有限 TypeError（2026-09-01）

- `playbackRate`/`defaultPlaybackRate` setter：非有限 → TypeError（spec
  dom-media-playbackrate / dom-media-defaultplaybackrate 步 2；旧静默回落 1）。
  上游 playbackRate.html 断言面不含非有限值——纯 spec 对齐，无用例可导入。
  单测 `test_media_playback_rate_non_finite_r394`。

## 扩批 VI — preload setter 补缺 + sweep 收口（2026-09-01）

- `preload` IDL setter：enumerated 反射（写 attr 原样值、getter 归一 'metadata' 分离；
  DOMString 非 nullable，null→'null' 串）。旧落 expando 吞 → round-trip 断。
- **全 IDL setter sweep 巡检**（探针实证全部 round-trip 正确）：语义面 setter 缺口
  清零，巡检收口。单测 `test_media_preload_setter_roundtrip_r395`。

## 排除用例决策记录（不导入清单）

| 用例/族 | 原因 | 归属 |
|---|---|---|
| `*manual.html`（controls/volume 交互族） | 需人工交互 | 非自动化面 |
| `autoplay-*-permissions-policy*.html` | HTTPS + Permissions-Policy header 环境 | 基础设施域 |
| `error-sequence.html` | MSE 依赖 | Tier 3 远期 |
| `media_fragment_seek` / `played-loop` / `preserves-pitch` / `src_object_blob` / `resize-during-playback` / `video_size_preserved_after_ended` / `video_timeupdate_on_seek` | 真解码/真 seek/真播放驱动 | media-playback（解码层） |
| `video_initially_paused` / `intrinsic_sizes` / `video_dynamic_poster_*` / `video-transparent-controls` / `video-poster-*` | reftest，依赖真帧渲染 | media-playback M1 后解锁 |
| `video_crash_empty_src.html` | `src="about:blank"` 子测需真资源失败判定（headless 无媒体 fetch 通路，会整页 Timeout 连坐） | 随资源失败判定切片重评 |
| `audio_volume_check` / `video_volume_check` | 越界 IndexSizeError 是旧 spec 断言，与现行 clamp 语义及 volume_nonfinite 冲突 | 永久排除（上游用例过时） |
| `video-loading-*` 族 | lazy-load/viewport 语义（loading IDL 面 + 交叉观察器） | 渲染/视口域后续切片 |
