# JS/DOM 原生化 — 主控面板（master.md）

**入口文档**: [../js-dom.md](../js-dom.md)（长期 Mission / Done Criteria / 执行协议 / 文档治理规则）
**关联 RFC**: [../../specs/p1b-v8-native-bindings-rfc.md](../../specs/p1b-v8-native-bindings-rfc.md) + [../../specs/p1b-l2d3-unified-matcher-identity-bridge-rfc.md](../../specs/p1b-l2d3-unified-matcher-identity-bridge-rfc.md)（R166 新增——L2-d3 统一匹配器 + identity 桥设计）
**创建日期**: 2026-08-13（goal 拆分 bootstrap）
**本轮**: R305 — **M4/L2：tree-order 4F 诊断轮（wrapper identity 域界定；wpt-data 注入四上下文首分歧点，无代码 land）**：ParentNode-querySelector-All 的 tree-order 断言（`querySelectorAll("*")` 逐位 === `traverse(root)` 产物 identity）注入探针实证：**Document** idx0 `trav=HTML/res=META`（`*` 走 `_queryTreeByCompound` 的 body 内容树路径——html/head 结构元素恒缺席；试过裸 `*` 转 JSON 往返——host 侧返 1 元素须先修 host `*` 查询，已回退）；**Detached** idx1 / **Fragment** idx0 / **In-document** idx306 全为 identity 断（顺序对、对象异——traverse 读 `element.cloneNode(true)` 工厂克隆树 vs 查询走 handle registry / JSON wrapper 两套对象）。**域界定**：四上下文全构造在 iframe contentDocument 工厂域——Document 上下文是 R296 结构桥的 iframe 缺口（低成本低风险）；Detached/Fragment/In-doc 是 R291 定性的 wrapper→视图归一深结构（R171 902F 依赖须专用评估）。**R306 候选**：① host `__zw_parse_html_query` 的 `*` 修复 + 结构桥 iframe 接入（Document 上下文低成本路径）；② cloneNode 树与 handle registry identity 归一（深结构立项）；③ M1 L2-read-only 主线首切片。本轮 selector 全族 2050P/5F 基线验证无扰动（实验全回退，工作树 clean）。commit：见 evidence**
**上轮**: R304 — **M4→M1 L2：sel-based innerHTML 同 turn 读视图（R220 视图桥第一步 land；MutationObserver-inner-outer 1P/1F→2P/0F 100%，MO 全族恰 -1F）**：R303 归因的直接修复——三件：① innerHTML 的 `_ihAdded` wrapper 打挂父槽 `_zwSelPendingParent`（整体替换 = 尾部追加语义）；② `_zwOverlayPendingChildNodes` 反链查找放宽第三分支（无 sel 但有挂父槽的 wrapper）；③ R56 的基底缓存 `delete`→`set(sel, [])`（innerHTML 整体替换——stale 剔除恒 miss 的根因绕过；host apply 时 `_zwChildBaseInvalidateAll` 换代生命周期不变）。探针前后：`fc=old text` → **`fcEqAd0=true|lcEqAd1=true`**（同 turn firstChild/lastChild 与 addedNodes identity 全等）。**A/B**：MO 全族恰 -1F；ParentNode 2126P/6F / Range-mutations 342P/5F / nodes 全量 **Fail set-diff 恒等**（差 4P 为崩溃测试 Timeout flaky 双向噪声）/ traversal/ChildNode 全持平；engine 2442 绿 + fmt/clippy 干净 + make test 1F 环境项。**意义**：「JS 写 sel 容器后同 turn 读可见」从 innerHTML 形态打通——R220 族剩余 = handle-append 融合（tree-order/mixed-case）+ 工厂节点可观察 id（cross-realm）+ parse-time MO（document 3F）。**教训**：双跑 A/B 禁止前后台各自 stash 同一工作树（本轮险情——单进程顺序或 worktree 隔离）。commit：`0bbf362bc`**
**上轮**: R303 — **M4：MO disconnect record 清空（disconnect 套件 2P/0F 100%；inner-outer 归因 R220；nodes 轻量修复面宣告收尾）**：① disconnect 补 `this._records = []`（spec `dom-mutationobserver-disconnect` 步骤 2「empty the record queue」——旧版只清 targets 收 4 条 record，WPT 三段 observe/disconnect 序列期望 1 条；一行修复 + engine 回归单测）；② inner-outer 1F 探针归因（sandbox 微任务时点实证 `fc=old text`——sel-based innerHTML 后 firstChild 读 stale 快照、addedNodes 是无 handle 的 wrapper 不入 pending 融合，identity 断言必败）→ 与 R302 工厂节点不可观察、R299 host 不见 append-in 子树同归 **R220 live-view 视图桥域**（L2 第一批消费用例）。MO 全族 116P/5F（恰 -1F）；takeRecords 邻居持平；engine 2442 绿（+回归 +归因探针）+ fmt/clippy 干净 + make test 1F 环境项。**MO 剩余 5F 终局归属**：cross-realm 1F + inner-outer 1F + document 3F 全部归深结构域（R220 / parse-time / R188 元素域可观察 id）——nodes 域 selector+MO 簇轻量修复面**已尽**，后续转 R220 统一方案或 M1 L2 主线。commit：`7071f8f0e`**
**上轮**: R302 — **M4：cross-realm MO 机制三件（iframe win 补 MutationObserver + Function 绑定构造器 + MO 回调异常上报；cross-realm 1F 形态深化推进）**：WPT `new frames[0].MutationObserver(new frames[1].Function('throw …'))` 期望回调异常上报到 frame1 的 onerror。三件机制修复（engine 单测 `disp:error|onerrorHit` 全链验证）：① `_zwMakeIframeWin` 补 MutationObserver 转发（旧 "not a constructor" 第一关即挂）；② **Function 绑定构造器**（R187 Object/Proxy.revocable 同款——产物印记 `_zwRealmWin` + 入 `__zwRealmOf` 注册表）；③ **MO flush 回调异常的「report the exception」**（旧 `catch(_e){}` 静默吞——改 `_zwReportListenerError` 按 callback 关联 realm 定向派发）。**真实文件注入探针归因收窄**：机制件全通（fnIs/reg/onerrorSet ✓）后最终断点 = **iframe 工厂 body 不可观察**——工厂 append 走直插无 `_mo_notify` + 工厂节点无 handle/sel 使 `_mo_id` null → observe 静默丢弃（R188 'doc' 专用 id 同款问题的元素域）；归 R220 深结构域（工厂节点稳定 id 贯通 observe/notify）。MO 全族 114P/6F→115P/6F（+1P 子测试解锁）；EventListener/realm 族全持平；engine 2440 绿 + fmt/clippy 干净 + make test 1F 环境项。**教训**：fetch-dom-subset.sh 会重拉覆盖——注入必须 inject→make→restore 顺序（本轮两轮探针落空）。commit：`a0ab20da6`**
**上轮**: R301 — **M4：MO move-record 兄弟字段（childList Range 双套件全解，MutationObserver-childList 23P/2F→25P/0F 100%）**：① **appendChild move 路径的旧父 record 补 prev/next**（part04 trap move 分支——extractContents 中段子 move 经此，旧 record 无兄弟字段；R294 removeChild 同款融合 childNodes 定位 + identity/handle·data 内容键回退，**move 路径通用修复**）；② **surroundContents 快照的顺序移除语义**（_rmSnap 一次取全部移除前兄弟使 record2.pv 停留已移除的 s1——改 prev 沿链跳过本移除集内节点，next 保持原值）。**首版回退教训**：extract 循环内显式 notify 与既有触发器双发 record（探针 rmId 双 NNN 实证）——修触发源优于调用方叠加。MO 全族 112P/8F→114P/6F（恰 -2）；Range-mutations 342P/5F / insertNode 1841P / surround 1840P 全持平；engine 2439 绿（r301 单测）+ fmt/clippy 干净 + make test 1F 环境项。MO 剩余 4F 归属：cross-realm 1F（iframe 构造器面 R295 家族）+ disconnect 1F + document 3F（parse-time 深结构）+ inner-outer 1F（wrapper identity）。commit：`601064cde`**
**上轮**: R300 — **M4：Node-insertBefore 全文件收口（40P/0F 100%，32P/8F→+8P；探针连环挖出三层根因）**：原计划只做顺序断言族 ×3，sandbox 探针逐层暴露：① **泛型/trap 的 step-3 NotFound 晚于类型 HRE**（双违形态报 HRE(3) 非 NotFound(8)）——修：泛型 sel 域限定（detached 容器才查，blank-loading 域整体排除）+ parentNode identity 判据 + trap 既有检查纯前移；② **R296 的 step-6 是死代码**——winning fn 的 `_r296hre` 从未定义，ReferenceError 被 `catch` 的 name 过滤静默吞掉（「写了但从未生效」藏两轮）——修：函数内定义复活 + **ownerDocument 门移除**（豁免了同文档全部元素，应精确到 identity）+ doctype 唯一性补全（任何位）；③ **trap 版完全没有 step-6**（createHTMLDocument 产物经 wrapper proxy 走 trap 非 winning fn——`String(doc.insertBefore).slice(0,60)` introspect 暴露）——修：按插入位方向补 element/doctype 冲突 + fragment 形状 + text 禁入。**R296 回归哨兵全绿**：insertNode 1841P / surround 1840P / NodeIterator-removal 29P / delete 129P / extract 192P / clone 191P 全 0F（sel 域限定实证未波及 loading lenient）；replaceChild/appendChild/removeChild/CharacterData/insert-adjacent/Node-cloneNode/Range-mutations/mutations 全持平。engine 2438 绿（r300 单测三段）+ fmt/clippy 干净 + make test 1F 环境项。**教训**：catch-and-filter 吞 ReferenceError（异常工厂须同点定义）；同语义多实现点须逐点探针验证；豁免条件须精确到 identity。commit：`cbce3865a`**
**上轮**: R299 — **M4：属性选择器 `i`/`s` 大小写标志（JS 匹配器 land + mixed-case 归因收窄转深结构，selector 小簇收尾）**：WPT querySelector-mixed-case 首断言 `[testAttr="alpha" s]`（detached）0 命中——JS `_parseAttrInner` 的 `.*?` 把值尾 `" s` 吞进值全 miss。修：值尾 `(.*?)\s+([iIsS])$` 标志剥离（空白分隔——裸 `i` 值非标志，与 dom crate R200 `strip_attr_case_flag` 同源）+ `_matchAttrOf` 六运算符 `i` 双侧 ASCII 小写。探针复验 `s=1|sUP=0|i=2|inc=2|plain=1|bareI=1`。**真实文件注入归因（关键发现）**：该文件是双阶段测试（detached→indoc），detached 41 断言全过后 indoc 阶段 `container.querySelectorAll("*")` **恒 0——宿主 sel-based 查询看不见 append 进 document 的 handle 建树**（pending mutations 未桥接 handle-append 形态，基线同败实证预存）——mixed-case 文件级 1F 转入 R220 深结构清单（handle-append 的 sel-side 视图桥，L2 域）。**A/B**：selector 全族 2049P/5F 持平（Fail 集合不变；-1 为 All-content flaky）+ case-insensitive/matches/closest/Attr 全持平；engine 2437 绿（part24 +1）+ fmt/clippy 干净 + make test 1F 为 XOpenDisplayFailed 环境项（fetch_proxy 一过性失败为并行 cargo 竞态复跑消失）。commit：`204508ea5`**
**上轮**: R298 — **M4：querySelector :scope 子树作用域（引擎修复 land，scope 套件 2P/2F→4P/0F 100%，selector 全族 2050P/5F set-diff 零回归）**：WPT `div.querySelector(":scope > p")` 返 null——spec 要求元素子树查询的 `:scope` = 调用元素自身（selectors-4 §6.4），dom crate 静态实现是文档根 html（样式表语义，`query.rs:135` follow-up 注记早已记档此缺口）。修：**R153 closest 的 `replace_scope_tokens` 模式复用**——`resolve_subtree_scope` helper 把独立 `:scope` token 文本替换为 root 唯一选择器，接入 `query_match/all_in_subtree_doc` 两入口（querySelector/querySelectorAll 对称）。`:scope > p` → `div:nth-child(1) > p` 命中直接子；`:scope > span`（孙层）恒空 ✓。**深结构备档**：文本替换对 `:is(:scope)` 参数内层等极端形态未覆盖，scope NodeId 贯穿 matches 链（dom crate 正解）归 L2/选择器深结构域。**A/B**：closest 29P/0F + matches 675P/0F + shadow 12P/2F 全持平（stash 双跑）；engine 2436 绿（part24 +1 单测）+ fmt/clippy 干净 + make test 1F 为 XOpenDisplayFailed 环境项。commit：`85d853b3f`**
**上轮**: R297 — **M4：querySelector escapes 孤立代理 never-match（JS 原值 id 缓存 land，escapes 66P/2F→68P/0F 100%，dom 全量 sweep +5P/-2F set-diff 零回归）**：根因 = V8→Rust 跨界的 `to_rust_string_lossy` 把 id 里 lone surrogate 换损 U+FFFD，与选择器 `\d83d` 的 spec 规定解码（surrogate→U+FFFD）**碰巧同值**误命中（真浏览器 id 保留 UTF-16 code unit，U+FFFD ≠ \ud83d → null）。修：`_zwRawIds` per-key JS 原值缓存（part01；值含 surrogate 才入表零开销快路径）+ id getter 原值优先（part03）+ `.id=`/`setAttribute('id')`/`removeAttribute('id')` 三写点同步（part04）——JS 客户端匹配域全解。**对照组同测**：U+FFFD 本体 id 仍命中（testMatched 族不回退，单测断言）。**教训**：① Rust String 边界即 lossy 边界，「id 读回 ≠ id 写入」是通用检测器；② 两侧同解码≠等价——选择器侧 FFFD（spec replacement）与存储侧 FFFD（marshalling 副作用）是不同语义域的碰巧同值，正确语义恰要求不等。**已知限制记档**：host 侧（doc 树/id_map/Rust matcher）id 仍 lossy——sel-based 的 Rust `#id` 匹配对 lone-surrogate 仍会相等（WPT 只测 detached 容器形态，全覆盖）；host 无损化归 L2 live Document 深结构域。**A/B**：dom 全量双跑（TIME_LIMIT=2400）54101P/94F/25T→54106P/92F/22T，Fail set-diff 恰 -2；Timeout 差 5 例双向（All-content 基线 4 跑 2 Timeout 实证 flaky 环境噪声）。engine 2435 绿（**part24.rs 新测试段**启用——part23 4565 行超 2000 上限）+ fmt/clippy 干净 + make test 1F 为 XOpenDisplayFailed 环境项。commit：`9e6601b98`**
**上轮**: R296 — **M4：Node-insertBefore pre-insertion 校验族（引擎修复 land，文件级 apply 崩→32P/8F，dom 全量 54070P→54100P +30）**：旧版对非 child refNode 直接推 host mutation → apply 崩整文件。按 spec 步骤序四处实现点（trap/泛型/_zwMEl/detached-doc winning fn）补齐：WebIDL 参数序 TypeError + 步骤 1-2 HRE + 步骤 4 类型 HRE + 步骤 6 doc-parent（含 ref 位方向）。**三处首版回归当场抓回**（教训）：① NotFound 严格化打破 Range-insertNode/surround 1840F×2——R117 早已注明的内部 lenient 依赖，四处全回退；② doctype ref 方向反了（元素前插入是合法位）；③ doctype 尾部冲突过宽。tree-order 4F 归因深结构（traverse 不对称 313 vs 1——R220 defer 域记档）。NodeIterator-removal/Range 族全程守稳。engine 2434 全绿（+1 r296 单测）+ fmt/clippy 干净。commit：`8f7fd8704`**
**上轮**: R295 — **M4：iframe realm 的 Text/Comment 构造器 ownerDocument（引擎修复 land，Text-constructor + Comment-constructor 双 16P/0F 100%，dom 全量 89F→87F）**：`_zwMakeIframeWin` 旧直接转发主构造器使 `new iframe.contentWindow.Text().ownerDocument` 恒主 document（WPT across globals「expected Document with 1 child got 2」2F）——修：per-realm 包装类（实例经主构造器建保完整方法面 + ownerDocument defineProperty 覆写闭包 doc + prototype 直连主构造器 prototype 跨 realm instanceof 保持）。**首版教训**：对象字面量内闭包捕获 `win` 变量在字面量求值时未赋值（得 undefined）——改捕获函数参数 `doc`。MO-document parser 3F 归因 parse-time record 基建（深结构记档）。set-diff 恰 -2 零新增。engine 2433 全绿（+1 r295 单测）+ fmt/clippy 干净。commit：`a1a4fa7d6`**
**上轮**: **本轮**: R294 — **M4：MutationObserver record 语义两件（引擎修复 land，childList Range.deleteContents + inner-outer innerHTML mutation 各 -1F，dom 全量 54066P→54070P / 91F→89F）**：① removeChild 的 handle-子 record 补 previousSibling/nextSibling（移除前从父融合 childNodes 定位——identity 优先 + handle/data 内容键回退，registry 剔除前读）；② innerHTML 纯文本 addedNodes 用注册表同一 textEl 节点（消费方 el.firstChild 读同一注册表——旧用 _zwFragmentAdded 独立 wrapper「expected Text node got Text node」）。**教训（试加后回退）**：R129 sel-parent 分支补 record 被 r259 单测当场抓回（_mo_notify 的 _zwNodeParent 清链是 surroundContents HRE 回滚的隐式依赖）——extractContents 形态的真 record 来源在别处，记 R295。set-diff 恰 -2 零新增。engine 2432 全绿（+1 r294 单测）+ fmt/clippy 干净。commit：`5282b8ff9`**
**上轮**: R293 — **M4：insertAdjacent 家族全解（引擎修复 land，Element-insertAdjacentText + insert-adjacent 两文件整文件崩→6P/14P 全 100%，dom 全量 54045P→54066P / 93F→91F）——四层修复**：① insertAdjacentText 重写对齐 R133（position SyntaxError + doc 根 HRE + **同轮可见性**——proxy insertBefore/appendChild 同步子视图 + 兄弟缓存失效；旧只推异步 host mutation 使同步断言读 stale 快照 + 非法 position 在 apply 崩整文件）；② Element.prototype 补 insertAdjacentElement/Text（plain 链——createHTMLDocument().documentElement 旧 TypeError）+ 工厂 docEl 双方法；③ 参数校验收紧 nodeType===1（DocumentType 旧落 sel 分支推 host mutation → apply "no child match for undefined" **崩整文件**）；④ handle 子 pending bucket 按**实际插入父**记账（beforebegin/afterend 落 target 父——旧按 target sel 记账使 overlay 盲）。set-diff 恰消失 2 文件级 F 零新增 + 解锁 20 subtest。engine 2431 全绿（+1 r293 单测）+ fmt/clippy 干净。commit：`a76d07b8c`**
**上轮**: R292 — **M4：querySelector 结构元素身份归一（引擎修复 land，Element-matches + webkitMatchesSelector 双套件 100%，dom 全量 95F→93F）——R291 归因收窄的靶簇收口**：detached-doc 的 `queryOne`/`queryAll`（iframe 子文档共用族）对 body/head/html 的 tag/`#id` 形态直返 **doc 视图对象**（动态读 `doc.body`/`doc.head`/`doc.documentElement` 槽——R221 fresh-doc 重绑兼容；旧返 `_zwParseEl` wrapper 使 `q === doc.body` false + wrapper.matches 恒 false）。**首版教训**：归一后工厂 docEl 缺 `getAttribute` 使消费方 TypeError（+2F 当场暴露）——补 docEl 属性反射最小面（惰性解析 `_r159HtmlAttrs`）。A/B：matches 双套件 675P/669P 全 0F、querySelector-All 1971P/4F 持平（tree-order 4F 是内容树 wrapper 另簇）、closest/contains/createElement(-NS)/getElementById/escapes/scope/mixed-case 全持平；set-diff 恰消失 2F 零新增。engine 2430 全绿（+1 r292 单测）+ fmt/clippy 干净。commit：`9656235a0`**
**上轮**: R291 — **M4：body type selector 匹配域部分进展 + 归因收窄（引擎增量 land，dom 全量 set-diff 0/0 零回归）**——三件增量：① `_zwMOuterHtml` 对 Document 根合成 `<html>{head}{body}</html>`（旧 nodeType 9 恒返 '' 使查询源空）；② 轻量视图无 attributes 数组时反射 id/class 进序列化；③ matches 键比较改 tag+id 主键 + outer 决胜 + documentElement tag-only。sandbox 直测 `iframeDoc.body/head.matches` false→true，但 **WPT 套件数字不变**——归因收窄：测试经 `querySelector('#body')` 取元素，iframe 子文档 querySelector 返 `_zwParseEl` wrapper（probe：q===doc.body false、parentNode null），wrapper 上 matches 无根上下文恒 false；ParentNode-querySelector-All tree-order 4F 同源（结果数组 identity 与视图节点不一致）。**wrapper→视图归一定性为深结构域**（R158/R171/R173 系列，R171 曾实测 902F 依赖）——转 R292 靶点立项。engine 2429 全绿 + fmt/clippy 干净 + closest/contains/escapes/scope/MO 全持平。commit：`7e125f487`**
**上轮**: R290 — **M4：nodes 域首批三簇（引擎修复 land，Element-remove 2F + Node-constants 2F + Document-constructor 2F，dom 全量 54041P→54045P / 101F→95F -6）——六根因**：① Element-remove 的 handle-only 父子形态：remove() 后 `_parentNodeFor` 反链残留 + `_handleChildren` registry 不剔（修：通知**后**清理——迭代器 pred/succ 读移除前树位，首版顺序错被 r88 双单测当场抓回）；② Node-constants：R184 has-trap 正则 `[A-Z][A-Z0-9]+_NODE` 漏内部下划线名（CDATA_SECTION_NODE 等 4 个）+ get-trap 补常量直答（proxy target 无 Node 原型链）；③ WebIDL 接口原型 constructor 自反属性全缺（三基座 + 64 HTML + 37 SVG 子类——`el.constructor` 恒落 Object）；④ XML doc createElement 产物原型回落 Element.prototype（`_zwMEl` R125 无条件挂 HTML 子类）。A/B 全量 set-diff：nodes +6/-6（三套件全 100%）、ranges/traversal/collections 持平、events -2 为 timeout 噪声（12→14 环境波动）。engine 2429 全绿（+3 r290 单测）+ fmt/clippy 干净。commit：`8babd21a7`**
**上轮**: R289 — **M4：ranges 域最后三簇收口（引擎修复 land，Range-constructor 1F + Range-selectNode 整文件崩→304P/0F 100% + intersectsNode-2 1F，dom 全量 53733P→54041P +308）——四根因**：① `new Range()` 初始边界 (document,0)（R183 只落 createRange，构造器漏同步——startContainer 恒 null）；② **两个叶子工厂缺 childNodes 字段**（part05 `_wrapNodeEntry` parsed 文本/注释 + part06 `_zwRegisterTextEl` textEl 包装——common.js nodeLength/testTree 对每节点读 `childNodes.length`，undefined 使 Range-selectNode 整文件 setup 崩；补齐后**四工厂叶子字段面统一**）；③ selectNodeContents 的 endOffset = node length（spec `concept-node-length`：CharacterData = data.length——旧恒读 childNodes.length 使 text/comment/PI 144F「expected N got 0」）+ doctype 抛 InvalidNodeTypeError（12F）；④ intersectsNode 严格不等（`(parent,i)` 严格 before end 且 `(parent,i+1)` 严格 after start——边界相接不交，crbug 822510 形态）。**连带解锁**：delete/extract/clone +4/+5/+4（叶子 childNodes 字段）。ranges 域**真实失败面归零**（剩余 = in-shadow 2F variant 基建缺失 + mutations-data 5F 超时族 + 本地探针）。engine 2426 全绿（+3 r289 单测）+ fmt/clippy 干净 + quickjs 矩阵绿。commit：`aa5a9db02`**
**上轮**: R288 — **M4：Range 三大域 956F 一轮全解（引擎修复 land，compareBoundaryPoints 592F→0F + Range-set 240F→0F + comparePoint 124F→0F，三套件全 100%）——四个独立根因**：① comparePoint 步骤序（root 检查 WrongDocumentError 先于 doctype 检查——旧序 cross-root doctype 报错类型错）；② compareBoundaryPoints 边界点对选取重写（how=2 END_TO_END 旧版落入 START_TO_START 分支 + 跨容器 cDP 位无祖先 offset-vs-childIndex 比较——修为 spec 表选取 + 复用 R203 `_zwRangeBpAfter` 深度感知双 climb）；③ setStart/setEnd 元素容器超长 offset 恢复精确校验（R286 registry 事实源使 handle childNodes 长度可判定，旧 `nodeType!==1` 放宽过时）；④ iframe load 事件改 `_defer` 微任务延迟派发（spec 异步完成语义——旧同步派发使 onload 在 `.src=...` 赋值语句序列中间执行，R255 时代 16,x 时序深项收口）。**dom 全量单跑**：52778P→**53733P（+955）**，ranges 38841P；A/B（stash 前后同命令）证实剩余 ranges 10F 全部 pre-existing（constructor/selectNode/intersectsNode-2/in-shadow 3F + mutations-data 5F 超时族）。engine 2423 全绿（+3 r288 单测）+ fmt/clippy 干净 + integration/wpt-runner/quickjs 矩阵全绿。commit：`e411dfd76`**
**上轮**: R287 — **M4：clone 侧 doc 守卫 + 九轮全量 A/B 复核（引擎修复 land，cloneContents 185P/2F→187P/0F 100%——**ranges 域六套件全部 100% 收口**）——R282 修的「doc parentNode 恒 null 合法」只落了 extract 侧，cloneContents 仍持旧守卫使 doc-sc 路径从未执行（sandbox probe frag=0）；对称移植 scParOk 后 HTML 深克隆 + comment 头克隆全对齐。**dom 全量 A/B（R278-R287 九轮累计 vs R279 基线双跑）**：52717P→52778P（+61，set-diff 0 回归 / 62 fail 消失）+ nodes/events/traversal/collections 持平（-1 为已知 flaky）。教训：双侧同构的 spec 修正须双侧同步落，extract/clone 修正清单互为 checklist。engine 2420 全绿（+1 r287 单测）+ fmt/clippy 干净。commit：`480d6f96f`**
**上轮**: R286 — **M4：容器 handle innerHTML 空态（引擎修复 land，Range-deleteContents-in-ShadowRoot 3P/1F→4P/0F 100%）——sandbox 复现：shadow `{<span>ABC</span>}` 全删后 childNodes=0 但 innerHTML 仍显示旧 span。根因：shadow/fragment handle 的内容只存 JS registry（host 无对应 mutation 域），innerHTML getter 融合视图空时回落 `__zw_get_inner_html_handle` 的缓存序列化——删除后空态被旧缓存掩盖。修：容器以 registry 为唯一事实源，空 registry 返 ''（_isContainerHandle 分支）。**Range.detach() 连带翻绿**（R281 时代记录的 1F 实测 Pass——同族空态掩盖）。A/B 全 ranges sweep：ShadowRoot +1 + 五套件 100%（delete/delete-ShadowRoot/insert/surround/extract）+ clone 185P/2F 持平 + set-diff **0 回归**；engine 2419 全绿（+1 r286 单测）+ fmt/clippy 干净。ranges 域现状：五套件 100% + clone 最后 2F（29/31,x docEl clone 域）。commit：`475205df8`**
**上轮**: R285 — **M4：firstPartial clone 引导（引擎修复 land，extractContents 达 100%——187P/0F；clone 184P→185P）——R284 的「comparator walk 域」归因被推翻**：dual-walk probe 实证 E（oracle）是正确 spec 输出——sc 是 cac 直接子且 partially-contained 时 frag 以 sc 的空 clone 开头（first partially contained child 分支；53,x so=1 越过唯一 text 子 → P#d 空壳引导），引擎缺引导使 A 侧全部错位一位（walk 读成「expected Element got Text」）。修：extract/clone 双侧引导（限定 sc.parentNode===cac——**首版教训**：cac===sc 全形态引导使 48,x -3 回归，容器自身非 firstPartial）。**教训**：DOM/frag 两断言是两轮独立提取，probe 须注入目标断言同一轮（第一 probe 在 DOM 轮得出错误结论）。A/B 全 ranges sweep：extract +1（100%）+ clone +1 + deleteContents/insertNode/surround 100% 保持 + set-diff **0 回归**；engine 2418 全绿 + fmt/clippy 干净。ranges 域现状：四套件 100%（delete/extract/insert/surround）+ clone 185P/2F（29/31,x docEl clone 域小簇）。commit：`3c3fe2014`**
**上轮**: R284 — **M4：doc 容器 covered children + frag 域归一（引擎修复 land，extract 183P/4F→186P/1F + clone 180P/7F→184P/3F 双修 +7P）——①51,x：R194 的 `_coveredChildren` 容器白名单漏 Document（9）——同容器 doc 区间的 contained 子语义与元素一致，旧版返 null 使 extract/delete/clone 三侧同容器 doc 形态全空转；修后 html 子 move + 塌缩 (doc,1)。②53,x frag：extract/clone 的 frag 恒主 document 域——跨域 append 对 iframe 域子被 flat；修为 frag 归 start 节点 ownerDocument（spec 步骤 1）。**probe 先行归因**：53,x 残余非 frag 构造（probe 实证 P#e 已包裹 nodeType 1）而是 comparator walk 域（R276 家族，独立切片）。A/B 全 ranges sweep：extract +3 + clone +4 + deleteContents/insertNode/surround 100% 保持 + set-diff **0 回归**；engine 2418 全绿（+1 r284 单测）+ fmt/clippy 干净。残余 = comparator walk 域簇 4F + Range.detach() 预存。commit：`51818851f`**
**上轮**: R283 — **M4：element-sc 一层 + sibling 提取形态（引擎修复 land，extract 178P/9F→183P/4F +5P）——R280 限流器的精确放宽**：识别两种可精确表达的 element-sc 形态（更深递归仍 defer）——形态 A（48,x：ec 父是 sc 直接子——fully-contained 尾段子 move + ec 路径子留树由 ④' lastPartial 组树承载）+ 形态 B（53,x：ec 父===sc 父 sibling 方向——sc 尾段越过路径子时本体全保 + cac 中段 sibling move + ec 头段削）+ sc-is-cac 尾段对称段。sandbox 双形态断言全对齐 oracle。A/B 全 ranges sweep：extract +5 + deleteContents/insertNode/surround 100% 保持 + clone 180P 持平 + set-diff **0 回归**；engine 2417 全绿（+1 r283 单测；4 假红为 fixture comment 加长连带——已批量更新）。残余 4F = 51,x 同节点 doc 提取域 + 53,x fragment append-flat 域（两个已复现的域簇）。commit：`71caaddb5`**
**上轮**: R282 — **M4：extract doctype 抛移植 + doc-sc 父守卫修正（引擎修复 land，extract 168P/19F→178P/9F +10P）——①25/26,x：R281b 的 cloneContents doctype 抛对称移植（同容器 [so,eo) / 跨容器 [so,ecPathIdx) 尾段查 nodeType 10 抛 HRE）；②29/31,x：R280 doc-sc 尾段是**死代码**——sandbox 单测复现（frag=0 树未动但 collapse 却发生 + ⓪ debug 标记 unset）定位根因 = `!sc.parentNode` 守卫对 Document 恒真（doc 无父是合法形态非 detached 信号）——修为 sc 非 doc 时才要求有父，修后 HTML 本体 move 入 frag + comment 头段切片 + 塌缩 (doc,so) 全对齐 oracle。A/B 全 ranges sweep：extract +10 + deleteContents/insertNode/surround 100% 保持 + clone 180P 持平 + set-diff **0 回归 / 10 消失**；engine 2416 全绿（+1 r282 doc-sc 单测）+ fmt/clippy 干净。残余 9F = 48/51/53,x element-sc 递归组树簇。commit：`fdf9d23e6`**
**上轮**: R281 — **M4：cloneContents 路径克隆分支（引擎修复 land，clone 158P/29F→180P/7F +22P）——R280 路径克隆组树的纯 clone 移植（无 move 无删源）**：跨容器 frag 三段组树（firstPartial.clone(sc 侧切片) + 中段 deep clone + lastPartial.clone(ec 侧)）+ 同节点 CD 切片（27/35-39,x comment/PI 簇——旧 toString 文本回落）+ doctype contained 抛 HRE（25/26,x）+ **空切片守卫**（首版教训：无守卫时空 #text 克隆使 collapsed 文本族 0/4/8/56-59,x +8 翻红——spec 返空 frag）。A/B 全 ranges sweep：clone +22 + deleteContents 125P/0F 保持 + insertNode/surround 100% 保持 + extract 168P 持平 + set-diff **0 回归 / 22 消失**；engine 2415 全绿（+1 r281 四形态单测）+ fmt/clippy 干净。残余 7F = 29/31/51,x doc 容器 frag 域簇 + 53,x sc-el 递归 + 54/55,x collapsed foreign/xml 域克隆 + Range.detach() 预存。commit：`b06e9d8a1`**
**上轮**: R280 — **M4：跨容器 extractContents 分支（引擎修复 land，extract 156P/31F→168P/19F +12P，R279 后重聚类验证无 false-pass 翻真）——R280 分支覆盖 sc CharData/Document + ec CharData/元素跨容器形态（R211 同父 / R236/R242 直接子之外的全部空转簇）。核心结构 = spec frag 三段**路径克隆组树**（firstPartial.clone 包 sc 侧子树 + contained 中段本体 move + lastPartial.clone 包 ec 侧子树——两轮首版教训：flat 裸 #text 与端点层多余空壳 clone）；moveIn 带 R241 第三例兜底（proxy fragment 的 append 对 plain 子**不摘原件**——探针实证 pd 双份在两树）；形态限流（element sc 让位 R242/R236——首版泛化抢跑 +9 回归的教训）；doc-sc 尾段（nodeType 9）已接但 29/31,x comment 克隆域（[object Object]）另案。A/B 全 ranges sweep：extract +12 + deleteContents 125P/0F 保持 + insertNode/surround 100% 保持 + clone 持平 + set-diff **0 回归 / 12 消失**；engine 2414 全绿（+1 r280 单测）+ fmt/clippy 干净。commit：`2a3c355a5`**
**上轮**: R279 — **M4：sc=element 跨容器 deleteContents 分支（引擎修复 land，deleteContents 117P/8F→125P/0F **100%**，R266 起十三轮累计 80P→125P +45）——R278 翻真的 sc 元素缺口收口：新 R279 分支（插 R268/R278 之间）骨架复用 R268（cac + 双侧爬升 + 中段开区间），三个关键差异均为首版跑红后修正：①sc 元素尾部 **[so, ecPathIdx)**（止于 ec 路径子——首版 [so,end) 把 ec 本体与后置兄弟一并删，24,x got 2 vs expected 5）；②**同树位守卫**（sc.childNodes[so]===ec 且 eo=0 → 同一树位零删除，塌缩 (sc,so)——首版塌 (ec,0) 使 49/50,x startContainer 断言挂）；③**塌缩序分叉**（cac===sc 时 (sc,so)，R268 式 sIdx+1 对该形态爬过 cac 得 0）。A/B 双全量 sweep（vs R278 同日基线）：dom 52717P（+10）+ set-diff **0 回归 / 8 fail 消失** + nodes 12663（flaky 回归）+ extract/clone/insertNode/surround/events/traversal/collections 全持平；engine 2413 全绿（+1 r279 三形态单测）+ fmt/clippy 干净。预存遗留：ShadowRoot 一例 + mutations 环境超时族（均非本切片面）。commit：`75ae63c21`**
**上轮**: R278 — **M4：iframe 域元素兄弟 getter + deleteContents sc-CharData/ec-element 分支（引擎修复 land，deleteContents 112P/13F→117P/8F +5P）——R277「需多轮克隆轮转累积」假设推翻：真实文件注入 + **identity 标记 probe**（range 端点 PRE 打 tag，POST 双导航面读回）一步复现——①actual 侧引擎 deleteContents 对 `[text,3,element,1]` 全分支 miss 整体 no-op（probe 实证 nvE=full、P#a:kids=1）；②expected 侧 oracle walk 饿死（nodesToRemove=0——克隆域**每层 nextSibling=null**（P#a/DIV/BODY/HTML），nextNode(sc)=null=stop 使 `node!=stop` 立即 false）；③R276「平行 wrapper data 源」结论修正：firstChildSame=true（identity 单一，数据完整只因 delete 从未发生）——R274 的 nextSibling 断链诊断才是真根因。修三处：`_zwIframeCreateElement` 补 `_zwMDefineSiblings` 动态 getter（R273 CDATA 尾簇补元素域——expected 侧 oracle 遍历复活）；deleteContents 新 R278 分支（sc CharData + ec 元素的 partially-contained 语义：ec 本体保留仅 [0,eo) 子删，R268 爬升复用）；insertBefore 自插入重定向（spec pre-insert 步骤 2「ref is child → nextSibling」——A/B 抓到 insertNode 28,0 回归后修复，1840P/0F 持平）。A/B 全量 dom sweep（patched vs baseline 同日双跑）：deleteContents +5（22/28/52,x 全解）+ insertNode/extract/clone/surround/mutations/nodes/events/traversal 全持平；**24,x DOM 翻真非回归**（baseline 靠双侧一致空转 false-pass，oracle 复活暴露 sc=element 跨容器缺口记 R279）；engine 2412 全绿（+1 r278 单测）+ fmt/clippy 干净。commit：`c050908d0`**
**上轮**: R277 — **M4：text wrapper 单一对象源复现域收窄（诊断轮，无代码 land）——三轮递进 probe 复现 R276「比较遍历完整 vs childNodes 修剪」分歧：顶层文档域 / 单 iframe 域 / 单轮克隆轮转域（mimic restoreIframe：referenceDoc 克隆 + doc strip + appendChild + setupRangeTests）——**全部 same=true 无分歧**（deleteData 对所有遍历面可见）。结论：分歧需 **多轮克隆轮转累积**（真实 harness 每 subtest 执行 restoreIframe，54 轮 referenceDoc 再克隆——R220「跨轮残留」线索同源）或 **双 iframe 交错轮转**（actual/expected 交替的全局 registry 串扰）。R278 复现方向：probe 循环 N 轮 restoreIframe 二分找首分歧轮次 N*，复现后按 R276 修复面落地（融合视图 text 子从 textEl/_zwMText 注册表取同一对象）。commit：见 evidence**
**上轮**: R276 — **M4：22,x 比较遍历 wrapper data 源分歧定位（诊断轮，无代码 land——R275 假设推翻 + R272 churn 实体确认）——R275「扁平 body」是 **post-delete 伪影**（dump 发生在 oracle 塌缩后）：本轮 PRE dump 实证双侧 doc 完全同构（DIV kids=7、scE.parentNode=P 正确）——双 iframe body 绑定无缺口。**教训：dump range 状态必须在 mutation 前**。真根因（isEqualNode 比较循环起点注入 + 双侧同步 nextNode walk dump nodeValue/data）：A 侧 P#a-text 在**比较遍历**里 = 完整数据，而 childNodes 遍历 = 修剪后——**两个遍历面拿到 data 源不同的平行 wrapper**（range 端点的对象被 deleteData 修剪；firstChild 导航返回读 host 源的新 wrapper——_regWrite 对无 sel 的 handle 父是纯本地写，host 不更新）。即 R272「wrapper identity churn」的实体形态：不是 identity 不同是 **data 源不同**。R277 修复方向：text wrapper 单一对象源（导航与 range 端点共享同一 textEl/_zwMText 对象——融合视图 text 子包装从注册表取）。commit：见 evidence**
**上轮**: R275 — **M4：expected iframe 扁平 body 定位（诊断轮，无代码 land——R274 假设再修正）——注入 probe 检查 expected 侧 sc 真实父链：`sc.parentNode = BODY（kids=1，唯一子 = sc 本身的扁平文本）`——**expected iframe 的 body 从未建成结构化树**（无 DIV#test/paras，内容为扁平文本 blob）；「expected Äb」由 BODY-text 尾段 deleteData(3) 得出（与 actual 的 paras[0].firstChild 修剪值巧合一致掩盖树分歧）。R272 的 contentWindow.paras=undefined 线索同源——expected iframe 的 setupRangeTests append 系列静默 no-op。根因定性：restoreIframe **双 iframe 轮转**的 body 绑定时序（actual iframe 绑定成功、expected（后建）失败——R221 fresh-doc 重绑对每次 appendChild 到不同 iframe doc 的轮转形态覆盖缺口）。**三轮假设演进教训**：identity churn → CDATA 兄弟槽（真修复）→ 元素 nextSibling → 扁平 body——deleteContents 断言失败先 dump 双侧 sc 父链三件（nodeType/tag/kids），比 walk 追踪更早暴露树不同构。R276 靶点：restoreIframe 双 iframe body 绑定修复（22/48/52/53,x 的对齐基础）。commit：见 evidence**
**上轮**: R274 — **M4：22,x oracle walk 提前终止归因（诊断轮，无代码 land——克隆域元素 nextSibling 断链定位）——dual-iframe probe 复刻 22,x 时 POST-A===POST-E（两侧一致空转中段保留）→ 与真实测试分歧 → **真实文件注入法**（gitignored wpt-data 直接改 Range-deleteContents.html 比较点 + assert-dump + 跑完 restore，R222 模式复兴）：文档树 dump 双侧仍一致——断言失败在 **detached 孤儿集段**（actualAllNodes 逐节点 isEqualNode）；nextNode 链追踪 `DIV->P`（✓）→ `P->#text`（✓）→ **`#text->NULL`**——P#a.nextSibling 在克隆域返回 falsy→ 爬升断链 → walk 终止 nodesToRemove=0（中段/头段全不发生）。根因 = part04 元素 proxy 的 sibling trap对克隆域 testDiv 的 paras 子解析缺口（R273 CDATA 兄弟 getter 同族但元素域）。方法论沉淀：真实文件注入（环境差异一步消除）+ detached 段归因意识（文档树一致时下一步 dump 孤儿集）。R275 靶点：克隆域元素 nextSibling 修复（22/48/52/53,x 共同前置）。commit：见 evidence**
**上轮**: R273 — **M4：CDATA 兄弟导航 getter（deleteContents 113P/16F→115P/14F +2P，R266-R273 七轮累计 +35P）——R272「wrapper identity churn」假设修正：oracle 相位追踪探针（globalThis 相位数组经 iframe window 读回）实证 AFTER-WALK-n=0 + 手动 isContained 全对 + walk 逐节点 w[0] 后终止 + **sc.nextSibling=undefined**（双侧一致）——根因 = createCDATASection 字面量无 nextSibling/previousSibling 槽（common.js nextNode 爬升对 undefined 视为无兄弟上行到根遍历终止；_zwMText/_zwMComment 工厂有 _zwMDefineSiblings 而 CDATA 字面量漏配）。修：补动态 getter（parentNode.childNodes.indexOf 现算，detached/边界 null）。方法论沉淀：oracle 相位追踪（比整体 dump 定位快一个量级；parent globalThis 读不到 iframe oracle 的日志——首轮空日志教训）。A/B：deleteContents +2（6,x）+ 全套件持平 + engine 2411 全绿（+1 r273 单测）+ fmt/clippy 干净。R274 靶点（残余 14F）：element 端点跨容器（22/48/52/53,x）+ 28,x 深形态 + 49/50,x cursor-only + extract/clone 重聚类。commit：见 evidence**
**上轮**: R272 — **M4：6,x expected 侧归因收口（诊断轮，无代码 land——wrapper identity churn 定位到 API 层）——dual-iframe probe 复刻（iframe src=Range-test-iframe.html + onload 链 + restoreIframe 克隆重建 + oracle 内联）dump 双侧：PRE-A===PRE-E（克隆保真无差异）；POST-A=[CDATA(12),text:] **actual 侧完全正确**（R271 在真实测试上下文生效）；POST-E 中段 CDATA 未删 + ec 头段未削（oracle aborted）。**关键探针**：oracle 前直接调 pE.removeChild(midE) 成功且此后 POST-E===POST-A——根因 = oracle 树遍历（nextNode）产生的 wrapper ≠ childNodes 视图 wrapper（identity churn，R252 家族），identity-based removeChild miss。方法论沉淀：dual-iframe probe 模板 + expected 侧归因三步序（PRE 双 dump → POST 双 dump → oracle 内部步骤直调定位）+ wpt-data gitignored 可自由 probe。R273 靶点：6,x wrapper 单一对象源（identity 标记探针定位 churn 域）+ element 端点跨容器（22/48/52/53,x）。commit：见 evidence**
**上轮**: R271 — **M4：removeChild plain 子 registry 剔除放宽（引擎语义修复 land，WPT 净 0——6,x 归因 expected 侧克隆域）——6,x 探针链三轮：① deleteContents POST 中段 CDATA 未移除（首尾 deleteData 对）；② 直接 removeChild(mid CDATA) 探针 no-h + post=3 静默穿透；③ 根因 = R195 plain-子分支限定容器父（`_isContainerHandle`）而普通元素父承载的 CDATA 轻量包装 miss。修：容器限定放宽（child 在 _handleChildren 即剔除）。验证 probe：POST=[CDATA:12,text:空] + 塌缩 (p5,1)——**6,x actual 侧算法完全正确**。WPT 仍 113P/16F 归因 **expected 侧**（restoreIframe 克隆树的 CDATA 保真域，R221 fresh-doc 系列）记 R272。A/B：全套件 100%/基线全持平 + engine 2410 全绿（+1 r271 单测）+ fmt/clippy 干净。land 依据：引擎 removeChild 语义 bug 真实（probe 实证），为后续形态扫清 actual 侧。R272 靶点：6,x expected 侧克隆树 CDATA dump + element 端点跨容器（22/48/52/53,x）。commit：见 evidence**
**上轮**: R270 — **M4：foreignDoc title 域 cDP/contains 补齐（108P/21F→113P/16F +5P，R266-R270 五轮累计 +33P）——17,x「nodeB.compareDocumentPosition is not a function」定位链：全树走查探针实证 missing = R130 的 title 元素+文本子（其余 foreignDoc 节点 R243 已补齐）；common.js getPosition 对 oracle 遍历到的任意节点调 cDP——myDeleteContents 碰 title 子树即抛 → expected 回退未触碰树。修：title 两节点补 contains/cDP（委托 _zwNodeContains/_zwCompareDocumentPosition）。**连带解锁 24,x 族**（expected 侧不再回退——oracle 树遍历修复的杠杆效应）。A/B：deleteContents +5 + 全套件持平 + engine 2409 全绿（+1 r270 单测：全树方法面走查 + cDP 活调用）+ fmt/clippy 干净。R271 靶点（残余 16F）：element 端点跨容器（22/48/52/53,x）+ 6,x CDATA 族 + 28,x 深形态 + 49/50,x cursor-only。commit：见 evidence**
**上轮**: R269 — **M4：deleteContents document 容器分支（102P/27F→108P/21F +6P，R266-R269 四轮累计 +28P）——25/26,x 族 `[document,0,document,1/2]`：doc 的 contained 子（doctype/html）旧 _coveredChildren 融合视图对主文档 proxy 恒空无移除。修：同容器 Document 限定分支（contained 子 [so,eo) 逆序 rm + 塌缩 (doc,so)），**限定 nodeType 9**（元素/fragment 已被回落正确处理防漂移——R268 收敛模式复用）；连带 51,x 同族 +2。**element 端点族定性（未修）**：probe 实证 24,x 的「expected 7」= **oracle 侧抛异常后回退的未触碰树**——myDeleteContents 在我方引擎的树遍历（nextNode/isContained 对 CDATA 子树 + foreignDoc 域 compareDocumentPosition 方法缺）上失败，非 delete 算法缺口而是 oracle 遍历原语的 shim 缺口（17/24/6,x 同源）。A/B：deleteContents +6 + 全套件持平 + engine 2408 全绿（+1 r269 单测）+ fmt/clippy 干净。R270 靶点：foreignDoc 域 compareDocumentPosition 补齐（17,x 直修 + 24/6,x oracle 回退连带）+ element 端点跨容器（22/48/52/53,x）。commit：见 evidence**
**上轮**: R268 — **M4：deleteContents 跨容器 CharData 分支（98P/31F→102P/27F +4P，R266-R268 三轮累计 +22P）——20/21,x 族（sc=textA/ec=textB 跨段、cac 级中段子移除）旧落 _coveredChildren 空转。实现四段（对齐 myDeleteContents 树序算法）：sc 尾段 deleteData + sc 侧爬升右侧兄弟移除（cac 级跳过防误删 ec 路径）+ ec 侧爬升左侧兄弟移除（同跳过）+ cac 级 (sIdx,eIdx) 中段统一移除 + ec 头段 + 塌缩 (cac, sc路径子 idx+1)。**首版教训（范围收敛）**：泛化到 element 端点使 24,x 大幅过度删除（DIV 7→2——sc 元素尾部规则把 partially-contained 的 ec 子树整体删掉）→ 收敛到已验证形态：双侧 CharData + 子树不相交；element 端点/ancestor 方向走既有回落记 R269（需方向分支 contained 递归）。A/B：deleteContents +4 + 全套件 100%/基线持平 + engine 2407 全绿（+1 r268 单测）+ fmt/clippy 干净。R269 靶点（残余 27F）：element 端点跨容器（22/24/48/52/53,x）+ document 容器族（17/25/26/49-51,x）+ 6,x CDATA 族 + 28/29,x 深形态。commit：见 evidence**
**上轮**: R267 — **M4：deleteContents ancestor 分支（92P/37F→98P/31F +6P，R266+R267 累计 +18P）——23,x 族（sc 是 ec 的父元素、ec 是直接 CharData 子）旧落 _coveredChildren 空转使 text 头部残留。R236 extract ancestor 分支的 delete 侧对称缺口，实现三段：ec.deleteData(0,eo) + contained 中段子移除（逆序）+ 塌缩 (sc,so)，handled 标志短路防二次处理。**教训**：contained 子移除首版只调 remove() 被单测场景二立即抓到（探针 pre=p2/fn=function/post=p2——泛型 remove 对该域形态静默无效）；修 = remove 后校验 parentNode 非 null 则 removeChild 直调兜底。A/B：deleteContents +6 + extract/clone 持平 + engine 2406 全绿（+1 r267 单测）+ fmt/clippy 干净。R268 靶点（残余 31F）：跨容器 CharData 族（20-22/24/52/53,x——cac 级 contained 泛化，myDeleteContents 树序遍历）+ document 容器族（17/25/26/49-51,x）。commit：见 evidence**
**上轮**: R266 — **M4：deleteContents 同节点 CharData（detached）放宽（deleteContents 80P/49F→92P/37F +12P）——重聚类发现 12F 簇共享签名 `[detachedTextNode,0,…,8]` 族（32-37,x 六形态：detached text/comment × 3 文档域）「differing nodeValue expected "" got "Uvwxyzab"」——deleteContents 整体空转。根因：R213 同节点分支的 parentNode 门（异节点中段 remove 的遗留）使 detached 节点（parentNode=null）整族跳过；extractContents 的 R228 已放宽同款而 delete 侧漏对齐。修两处：外层 guard `sc===ec || 双父非空相等`（同节点无需父——spec replace-data 段不依赖 parent）+ 内层 si>=0 门同节点恒进（kids 空时同节点分支只做 deleteData+collapse 无中段兄弟）。A/B：deleteContents +12 + extract/clone/surround 100%/基线全持平 + engine 2405 全绿（+1 r266 单测：full/mid/comment 三断言）+ fmt/clippy 干净。R267 靶点（残余 37F 聚类）：ancestor-element 族（23,x——R236 extract 的 ancestor 分支 delete 侧对应缺口，优先）+ document 容器族（17/25/26/49-51,x）+ 跨容器族。commit：见 evidence**
**上轮**: R265 — **M4：textEl ref 的 registry 插入（mutations-insertBefore 0P/超时→76P/0F 100%，mutations 八套件全 100%）——R264 定位的死循环根因修复：part04 insertBefore 新增两分支——① textEl 注册表形态（物化 + 前插：textEl 包装从注册表域移入 _handleChildren（融合序保序）+ 注销注册（单源化防双计）+ newNode splice 到 text 位前；注销安全性 = handle-only 形态下 node 的 data/appendData 族是自身闭包不查注册表 + 融合视图返同一 node identity 保持）；② 物化后二次插入形态（refNode 已在 _handleChildren 但无 handle——registry 内位次直接 splice）。**首版教训**：只做分支①时回归单测场景④（物化后二次 insert）立即抓到位次错——物化是跨域搬运，所有消费该域形态的分支都要补位。A/B：insertBefore 76P/0F + removeChild/appendChild/replaceChild/splitText/insertNode/surround 全 100% 持平 + delete/extract/clone 预存不变 + engine 2404 全绿（+1 r265 单测）+ fmt/clippy 干净。**mutations 域终态：八套件全 100%**（removeChild 20/appendChild 70/replaceChild 60/insertBefore 76/deleteData 564/insertData 382/appendData 384/splitText 116）。R266 靶点：deleteContents 49F/extractContents 32F/cloneContents 29F 重聚类 + 全域 set-diff 复核。commit：见 evidence**
**上轮**: R264 — **M4：insertBefore 两段接线 + 超时根因定位（诊断轮——mutations 非超时套件保持全 100%）——part04 insertBefore 补 remove+insert 两段钩子（appendChild 同款）；insertBefore 超时族五轮探针定位到**单点死循环**：表格二分（38 条逐条，0-6 条 32ms、第 7 条 90s 挂起）→ clean-HEAD 复现（预存非 R262/R263 引入）→ PRE 视图探针（全正确）→ 引擎直调（0ms）→ 根因 = **refNode 是 textEl 包装（无 sel/handle）+ handle 父时三个 wire 分支全不命中 → 插入静默不入 registry → 调用方 common.js indexOf（无终止 while）在融合视图 miss 上自旋**。修复方向（R265）：text ref 的 registry 插入（textEl 注册表反查父 handle + 融合位次 splice）。A/B：appendChild/replaceChild/removeChild 100% 全保持 + engine 2403 全绿 + fmt/clippy 干净。R265 靶点：text-ref registry 插入解锁 insertBefore 76 subtest + delete/extract/clone 重聚类。commit：见 evidence**
**上轮**: R263 — **M4：insert 侧 live-range 边界调整（mutations-appendChild 42P/28F→70P/0F + replaceChild 30P/30F→60P/0F 双 100%，+58P）——WPT modifyForInsert 逐字引证：边界在 new parent 且 offset > new index → +1（**插入后调用**——与 R262 移除前快照对偶）。移动语义 = remove 调整 + insert 调整两段串联（testAppendChild = remove+insert；testReplaceChild = remove(old)+remove(new)+insert(new)）。实现 `_zwAdjustRangesForInsert`（part03 globalThis 路由，父三键匹配）+ 九站点：part04 appendChild 移动路径两段 + replaceChild 三路径/replace-with-self 短路（树不动边界照 WPT 序迁移）/无 wire 兜底 + part03 doc 级 own replaceChild + Node.prototype 通用路径。**诊断方法论**（foreignDoc 残余 8F 四轮 probe 定位）：对照探针（removeChild 单独 Pass）→ 直调探针（adjust 函数 Pass）→ **源码指纹探针**（`fd.replaceChild.toString()` 发现 `_makeDetachedDocument` 在 7840 行有 own replaceChild——通用路径修复不达 own 方法，detached doc 域方法解析须逐方法验证 own-vs-prototype）。A/B：appendChild/replaceChild 100% + removeChild/splitText/surround/insertNode 100% 持平 + delete/extract/clone 预存持平 + engine 2403 全绿（+1 r263 单测）+ fmt/clippy 干净。**mutations 域非超时套件全 100%**（removeChild 20/appendChild 70/replaceChild 60/deleteData 564/insertData 382/appendData 384/splitText 116）。R264 靶点：insertBefore 超时族接线验证（同 appendChild 模式）+ extract/clone/delete 重聚类。commit：见 evidence**
**上轮**: R262 — **M4：removeChild live-range 边界迁移（mutations-removeChild 2P/18F→20P/0F 100%）——WPT modifyForRemove 逐字引证 spec `concept-node-pre-remove` 末段两规则：① 边界在被移除子树内 → (old parent, old index)；② 边界在 old parent 且 offset > old index → −1。**调用时机契约 = 移除前快照**。实现 `_zwAdjustRangesForRemove`（三键身份匹配 + parentNode 上行子孙判定 + 重写清 _mode）+ 六站点接线。**R257 单测场景三语义翻转（非回归）**：19,9 detachedDiv 父 newParent 旧「成功 wrap」是因清子 removeChild 不迁移边界使 ancestor 检查 miss；spec 正确 = 边界迁 (dd,0) 后自检查命中 → HRE（chromium 同抛，surround 1840P/0F 保持是契约成立证据）。A/B：removeChild 100% + 全关键套件 100% 持平 + 预存簇 stash A/B 逐条一致 + engine 2402 全绿。commit：c192abcf1**
**上轮**: R261 — **M4：splitText live-range retarget（mutations-splitText 96P/20F→116P/0F 100%，ranges 全量 +20 F2P / 0 P2F）——WPT testSplitText 逐字引证 spec `dom-text-splittext` 末段两段语义：① replace-data 段（无条件——split 等价 replaceData(o, len−o, '')）：o < off ≤ len 的边界收到 o；② split 段（**仅 parent 非空**）：**原始** off > o → (尾节点, off−o）。两个中间红态教训：首版无条件迁移漏 detached 分支（12F 反向——spec 的 parent 门是硬边界）；次版两段链式用收缩后 off 判定（8F——WPT 算法用 originalOffset 判定与迁移，两段非链式）。实现 part03（p=n.parentNode 门）+ part04（_r196PH 门 + handle 匹配）。附带 R261(a) replaceData 超时归因收口：**预存项**（R255 时代日志同样超时，非 R260 引入）；切片二分（200/400/430 过、437 全量超、尾部单独跑过）定位为累积型慢非死循环；setup 成本实测排除（iframe 域 100 轮 13ms）——低 ROI 后续。A/B：splitText 100% + ranges set-diff +20 F2P / 0 P2F + engine 2401 全绿（+1 回归单测）+ fmt/clippy 干净。R262 靶点：removeChild 的 pre-remove 边界迁移（2P/18F）+ extract/clone 残余重聚类。commit：ef1f75fcd**
**上轮**: R260 — **M4：CharacterData 变更的 live-range 边界调整（mutations 三套件 100%，ranges 全量 set-diff +196 F2P / 0 P2F）——重聚类发现 insertData 122F「Wrong end offset」簇；harness PRE/POST 探针实证 sim 侧 (2→8) 折到 (2→2) 的机制 = 真浏览器 deleteData 的 spec `concept-node-replace-data` 末段 live-range 调整（shim 无此机制双侧恒不折叠）。实施：`_makeRange` 注册表（globalThis.__zwLiveRanges 环形 8192）+ `_zwAdjustRangesForData`（三键跨域身份匹配：identity/handle/父 sel-handle+childIndex——probe 实证 harness paras 域 pSel=N 需父 handle 键）+ 四域接线（part03 factory/part04 proxy/part05 parsed/part06 textEl）。spec 公式（WPT 引用原文）：被替换区间内边界**收到 offset**（首版 offset+insertLen 误——实测纠正）；区间后 + insertLen−count。过程坑：part06 闭包对 part03 函数直接引用不可靠（entry=0 实证）——全站经 globalThis 路由。A/B：insertData 260P/122F→**382P/0F** + deleteData **564P/0F** + appendData **384P/0F**（三套件 100%）+ extract 141P/51F→160P/32F（+19）+ surround/insertNode 100% 持平 + ranges 全量 **+196 F2P / 0 P2F** + engine 2400 全绿（r231 期望 2,8→2,2 更新为 spec 语义 + r260 回归单测）+ fmt/clippy 干净。R261 靶点：replaceData 套件超时（adjust 环路疑似）+ splitText/remove 调整未接线 + extract/clone 残余重聚类。commit：9f40ba074**
**上轮**: R259 — **M4：leaf-HRE 空覆盖子 extract-first——16,x 全簇 +11P，Range-surroundContents 达 1840P/0F（100%，R210 起二十六轮收敛）——形态：`[document.body,4,document.body,5]` × Text/Comment/PI newParent（11F）「startOffset expected 2 got 4」DOM 断言全过。探针四连：PRE 双侧输入完全一致（BODY N=1[DIV] so=4 eo=5——ZeroWeb iframe body 只 testDiv 一子，两侧同形 → 分歧纯算法）；E 侧 POST (2,2) + HRE vs A 侧 (5,5)——**机制解码**：sim 步骤 3 无条件 myExtractContents（setStart/setEnd 折叠对 (body,4→4)）→ 步骤 4 myInsertNode 尾步 setEnd(body,2) 经 shim R203 crossing 重设自然拉平双边界 (2,2)；host kids===0 分支只 insertNode（range 保持 (4,5) 未折叠使 R219 的 start===end 守卫跳过）→ 边界漂移 (5,5)。**首版教训**：抛前镜像 start←end 使 12–14,x leaf 簇回归 +87F（机制错——跨容器形态不经 crossing）→ 正确修复 = 按 sim 序 kids===0 也先 extract（R219 生效 + crossing 重设自然产出）。A/B：surround 1829P/11F→**1840P/0F** + ranges 上游 set-diff **+11 F2P / 0 P2F** + engine 2399 全绿（+1 回归单测）+ fmt/clippy 干净。R255「异步 fetch-rebuild 时序」假设本轮推翻（真因是 leaf 路径算法序）。R260 靶点：insertNode/extract/clone 套件残余重聚类（四轮行为面变化后取样）。commit：29b8fe636**
**上轮**: R258 — **M4：selectNode 落位 sc 回退——28,0 + 30,4/30,11 残余簇全解（+3P，set-diff 0 回归）——重聚类（R256/R257 行为面变化后取样）：三连共享新签名 `endOffset expected 1 but got 0` 且 **DOM 断言全过**（树正确仅边界缺写）。trace 探针实证根因：**newParent 自身是 covered 子**的跨容器形态（`[testDiv,0,comment,5]` / `[foreignDoc.body,0,foreignTextNode,36]`）——extract 把 newParent 本体移进 frag2（insertNode 前 p1.parentNode=#document-fragment），工厂 body 的 `_tree.appendChild` 事后父链修复对 frag 旧链 miss（parentNode 恒指 fragment），R212/R236/R242 三 selectNode 尾部读 `newParent.parentNode`（=frag）的 indexOf 全 miss → 落位整体跳过（trace 全程仅 extract 的 collapse 一对 sS:BODY:0,sE:BODY:0）；而 fb.childNodes 树视图正确含 newParent（数组视图与 parentNode 链分裂——落位读错源）。修：三尾部统一在 parentNode 查找 miss 时**回退 this.startContainer.childNodes**（树视图权威）取父与索引，既有命中路径零变化。A/B：surround 1826P/14F→1829P/11F + ranges 上游 set-diff **+3 F2P / 0 P2F** + engine 2398 全绿（+1 回归单测 30,x 形态 so=0/eo=1）+ fmt/clippy 干净。R259 靶点：**16,x 11F（最后一簇）**——异步 fetch-rebuild 时序深项。commit：94c1b59c3**
**上轮**: R257 — **M4：self-surround HRE——清子后 inclusive-ancestor 检查（18,0/19,6 簇全解 +4P，set-diff 0 回归）——形态：`[paras[0],0,paras[0],1]`+paras[0] / `[detachedPara1,0,…]`+detachedPara1（newParent===range 容器），spec 步骤 4 insertNode 的 pre-insertion validity「inclusive ancestor → HRE」必抛；host 元素主路径（R237 clone 循环+直接 insertBefore）不经 insertNode 无拦截：清 newParent 子（误删自身内容）+ R248 remove（摘出旧父）后静默成功。**时序关键（首版教训）**：检查须在**清子循环之后**——sim 序步骤 3 extract → 步骤 2 清 newParent 子 → 步骤 4 校验；range 容器是 newParent 的**子**时（19,9 detachedDiv 族）清子已断 sc→newParent 父链，ancestor 上行 walk 不再命中，wrap 合法成功（首版检查前置使 19,9 误抛翻绿成 fail，重定位后消除）。抛出前先移出 covered 子 + range 塌缩（先变更后抛对齐 sim 中间态）。A/B：surround 1822P/18F→1826P/14F + ranges 上游 set-diff **+4 F2P / 0 P2F** + engine 2397 全绿（+1 三场景回归单测：18,0 HRE / 19,6 HRE / 19,9 wrap 成功）+ fmt/clippy 干净。R258 靶点：16,x 11F（异步 fetch-rebuild 深项）/ 28,0 1F / 30,x 2F。commit：702d6d80c**
**上轮**: R256 — **M4：factory docEl mutation 兄弟 getter 重接线 + title ns 对齐——17,x 簇 12F 全解（surround 1810P/30F→1822P/18F，+13P，set-diff 0 回归）——诊断链三轮：① engine 级 twin-env 复刻（独立 iframe 建 foreignDoc+paras[0]，sim vs host 树 dump）顶层形态完全一致（R247 结论复证：分歧只在 harness 全环境）；② harness 内嵌 childNodes 递归 dump（探针经 assert 强制失败把树放进 Fail 消息，R248 方法论变体）A/E 双树 IDENTICAL 但 isEqualNode 仍 false；③ **nextNode getter 链 dump 暴露首差**——A 侧 walk `HTML→P→HEAD→TITLE→text→#comment`（BODY 子树整段被跳过）vs E 侧正常经 BODY；再扩展 ns/prefix 比较定位第二差 `/1/0/0/0`（TITLE）NS 分歧（clone=XHTML vs 原件=undefined）。**根因三层**：① factory docEl（_makeDetachedDocument 产物）insertBefore/appendChild 只改 childNodes 数组+裸赋 parentNode，**从不重接线兄弟 getter**——surround 插 paras[0] 后 nextNode oracle 的 getter 链视图与数组视图分裂，BODY 整段从遍历消失（17,x 主根因）；② 裸赋 `c.parentNode = docEl` 对 wrapper 域 proxy 是静默 no-op（权威父链在 _zwNodeParent[handle] 注册表）且 part04 sibling trap 父解析只认 parentSel/parentHandle **不认 plainParent**；③ factory `_r130TitleEl` 无 namespaceURI 字段——isEqualNode ns 归一比较判空而 _zwDeepCloneEl 克隆副本缺省 XHTML（sim 移原件 vs host 克隆循环的 ns 分歧）。**修**：docEl 三 mutation 后调 `_r130WireSiblings(docEl.childNodes)` + 裸赋改 `_r223SetParent(c, docEl)` + part04 trap 补 plainParent 分支 + title 补 ns/prefix（headEl/docEl 同款）。A/B：surround +12（17,0/4/6/9/11/13 六形态 DOM+position 全翻绿）+ ranges 上游 set-diff **+13 F2P / 0 P2F**（含 Range-extractContents 17 连带）+ nodes 57F 逐条一致 + events/collections/traversal 持平 + engine 2396 全绿（+1 回归单测）+ fmt/clippy 干净。StaticRange 17P2F 假回归核实：全量 sweep 未执行该文件（0 行输出），单独跑 17/17 Pass——harness 枚举伪影。R257 靶点：16,x 11F（异步 fetch-rebuild 时序，R255 已定位）/ 18,0+19,6 4F / 28,0 1F / 30,x 2F。commit：4ed3dc9c6**
**上轮**: R255 — **M4：iframe docEl own cloneNode 保真 + body 内容标准化提取（16,x 诊断基建轮，WPT 净 0，ranges set-diff 0/0）——16,x `[document.body,4,document.body,5]` 探针链定位两层基建缺陷：① 无显式 `<html>` 对的 HTML 子文档 bodyInner = 全 markup（含 doctype/html/head 段），ensureTree 的 `'<body>'+全文+'</body>'` 重解析让 host parser 对 body 上下文里的 html/head 标签 foster/剥离，**body 视图丢 `<script>` 元素**（真浏览器 body 含 script 使 offset 4 合法）；修：有 `<body …>` 开标签取其后全文，无 body 标签去 doctype/html/head 段取剩余。② docEl **own cloneNode(deep)**——运行时 docEl.childNodes 是工厂空数组（R220 教训：运行时链 head/body 曾 -158P），`documentElement.cloneNode(true)`（restoreIframe 的 referenceDoc 链）经 _zwDeepCloneEl 只产空壳；修：**只改克隆产物不改运行时结构**——克隆树按 spec 解析产物形态 [head, body] 递归克隆两视图（覆盖 mEl 提取与 R177 合成两条 docEl 路径）。A/B：surround 1810P/30F 持平 + ranges 全量 set-diff 0/0 + engine 2395 全绿（+1 克隆链回归测试）。16,x 本体仍阻塞在 iframe contentDocument 的**异步 fetch-rebuild 时序**（harness onload 时序 vs 同步 rebuild 分歧，深项延后）。commit：92d3e1fbb**
**上轮**: R254 — **M4：幽灵烘焙点锁定 + 修复 land——clone 循环中间态是幽灵真源，13/14,x 全解（+4P，set-diff 0 回归，详见 evidence/2026-08-25-r254-clone-loop-ghost-fix.md）——R254-probe 四连（代际读数排除缓存域→幽灵深 dump 是第三对象→上移本尊 dump 正确→根因锁定）：surround 主路径 clone 循环（正序 clone→逆序 remove）在克隆 covered children 时 newParent 仍挂 BODY>div#test 内，BODY 深克隆把「半完成 newParent」烘进 BODY-clone。修：part06 clone 循环前先 `newParent.remove()`（幂等）。A/B：surround 1806P/34F→1810P/30F（13,0/14,0 四连翻绿）+ ranges 全量 set-diff 0/0 + native 同值 + engine 2394 全绿。附带：r210 单测场景②期望修正（R239 改序后对齐）。commit：503e234e2 + f3806f6d0**
**上轮**: R253 — **M4：detached-build + 缓存失效实验负结果（R252 假设证伪，已回退，基线零漂移）——按 R252 方向实现：surround clone 循环 detached 数组攒齐克隆后一次 append（中途不产生半完成 newParent 形态）+ append 后统一失效 _zwQWrapCache。结果：surround 1806P/34F 净 0（subtest diff=0）+ ranges 全量 set-diff 0/0——13/14,x 失败形态与消息逐字不变，[object Object] 首差前后一致。结论：R252「clone 循环中途烘焙」假设被证伪——烘焙点不在 clone 循环。处置：按最少代码准则回退实验改动（part06 diff 0 行，基线复核一致）。R254 靶点（烘焙点重定位）：候选重排 removal 循环（kids[k].remove() 期间序列化读取）/ _rmSnap 构造期兄弟链 getter 触发 / domTests 前置断言在 surround 前烘焙且 surround 后未失效；方法 = R249 同款 in-window wrap 加 _zwQWrapGen 代际读数探针（surround 各步骤前后 gen + cache size），定位「surround 后未失效」的确切窗口；次选：16,x startOffset 11F**
**上轮**: R252 — **M4：幽灵定性收口——wrapperChurn 实证，幽灵 = 序列化缓存中间态快照（探针轮，无代码 land，基线 1806P/34F 零漂移）——两连否定 + 一定性：① run() 期 wrap testDiv.appendChild/insertBefore（stack 捕获）surround 期零调用——幽灵「插入」不经过 testDiv 变异方法；② childNodes 数组标记实验读到 REPLACED；③ window.__r252td 中继 + walk 身份对比 = wrapperChurn——positionTests 由 actualRoots walk 到的 div#test 与 run() 时对象**不同**。结论：幽灵不在活树（R248/R249 后活树已正确），而在 wrapper/序列化缓存域——positionTests 命中重建 wrapper，内容来自 clone 循环**中途**（P|a 只含首个 HEAD-clone 时）烘焙的缓存快照，之后未失效；R251 的 gp=DIV/无标签/非 paras[0] 全部由此解释。R253 修复方向： 克隆 build 到 detached 容器完成后一次 append（不在中途触碰可触发序列化的 getter）； surround 收尾统一失效 _zwQWrapCache/序列化源（clone 循环期间 suppress _zwQWrapGen 清理时机补强）。R253 首选：按方向 实现后 walk dump 幽灵消失 + 13/14,x 翻绿验证；次选：16,x startOffset 11F**
**上轮**: R251 — **M4：幽灵对象溯源——真插入的 P|a{HEAD-only}（gp=DIV），R252 栈捕获靶点（探针轮，无代码 land，基线 1806P/34F 零漂移）——R251-iframe run() 期标记 + probe walk dump 四连：① 标记实验：paras/docEl 子/testDiv 打 __r251tag 后 dump 全 NONE（testDiv 现子列表对象均非 setup 期标记对象或标记被克隆丢失）；② 身份实验（window.__r251p0 中继）：isLive=false——window.paras[0] 不在 testDiv（真 P|a 已正确上移，R248/R249 生效），幽灵是另一对象；③ 唯一性：全树仅一个 div#test（n=7=6 合法+1 幽灵）——排除 referenceDoc 克隆残留双 DIV；④ 父链：gp=DIV——幽灵 parentNode 就是 testDiv，非数组残留（R249 单向断链形态）而是**被真实插入**。签名 P|a{kids=1,k0=HEAD} 与 clone 循环首迭代（P|a 只含 HEAD-clone 时）快照形态完全一致。候选（R252 验证）：_mo_notify wrapper 重建/回放 / in-window 双调用中间态交叉 / clone 循环意外 append。R252 首选：in-window wrap testDiv.appendChild/insertBefore + Error().stack（R249 同款技术）一步定位插入者；次选：16,x startOffset 11F**
**上轮**: R250 — **M4：R249 修复后双树重 dump——主幽灵 P 已消，残余新签名（探针轮，无代码 land，基线 1806P/34F 零漂移）——R250-probe（深度 3 到 5）：① 深度 3 双侧 ROOT 树完全一致（13,0 的 HTML{P|a{HEAD{TITLE},BODY{DIV}}}、14,0 的 HTML{HEAD{TITLE},P|a{BODY{DIV}}}）——R246 时代大形态分歧（BODY 缺失/P 缺失）全部消失，R245/R248/R249 三轮修复树级生效；② 深度 5 发现残余：A 的 DIV|test=7 子（[P|a{HEAD{}},P|b..P|e,P{cdata},comment]）vs E=6 子——新签名：残留 P|a{HEAD{}} 是**无 TITLE 的 HEAD** 单子形态（真 P|a 已正确上移含完整 HEAD{TITLE}+BODY 双子，R249 splice 生效）——残留是独立幽灵对象非视图残留，factory headEl 字面量特征（TITLE 为 build 期 push，克隆时序差异产生无 title 变体）。R251 首选：幽灵创建点定位——候选 surround clone 循环 kids[0] 误取 factory headEl（_coveredChildren 的 sc.childNodes 视图在 iframe-doc 域混取）或 _rmSnap 通知路径包装重建；对象 id 标记（Object.assign(el,{__r250:1})）一步溯源；次选：16,x startOffset 11F**
**上轮**: R249 — **M4：幽灵 P 写入者栈捕获 + iframe 工厂元素 own removeChild land（WPT 净 0）——R249-iframe 内联探针（run() 时点捕获初始值 + Error().stack）：① p0.p@run=DIV/tdKids=7/p0InTd=true——p0 父链 setup 期健康（推翻 R248「已 null」猜想）；② surround 期唯一写入 pSET=null 的栈 = set(parentNode) <- removeChild(part03:1740 数组分支) <- Node.prototype.remove <- surroundContents——写入者是 Node.prototype.removeChild 的数组分支；③ 机制：testDiv（iframe 工厂元素）无 own removeChild，proto 数组分支 splice 落在 childNodes **视图副本**（getter 每次返新数组）而 child.parentNode=null 持久——**单向断链**（父链空、子列表残留 = 幽灵 P）。修：part05 _zwIframeCreateElement 产物补 own removeChild（单次读列表 + identity indexOf + 就地 splice + 父链置空 + NotFound/TypeError 校验，_zwMEl 同款）——修复后栈实证 remove 走新 own 实现（part05:1310）。A/B：surround 1806P/34F 净 0（13/14,x 仍 Fail——splice 已修，该形态另有残余分歧待 R250 重 dump）+ ranges 全量 set-diff 0/0 + insertNode 1841P 保持 + nodes 57=57 + native 1806P 同值；engine 2393 全绿（新增 r249 单测五断言）；fmt/clippy 干净；make test 1F 为 XOpenDisplayFailed 环境项；wpt-data 探针全部还原。R250 首选：13/14,x 残余（own removeChild 生效后重新 dump 双树首差）；次选：17,x "[object Object]"（isEqualNode 对工厂对象深层比较）**
**上轮**: R248 — **M4：harness 内联探针定位 17/13,x「幽灵 P」+ 显式摘除守卫 land（WPT 净 0）——Range-test-iframe run() 内联探针 12 轮迭代：① 对象域标签（run() 创建点）A/E 双侧完全对称（#1:HTML/cnOWN/pOWN）——对象域分裂假设排除；② 双树 dump（深度 3）POST sc 子树双侧一致但 ROOT 树发现「幽灵 P」——13,0 的 A 侧 DIV=6 P vs E 侧 5 P（sim 正确移出 paras[0]，host DIV 残留第六个 P）；③ p0 父链探针 p0pA/p0pE 均 null（对象真值一致）但 A 的 DIV childNodes 仍含 p0（单向断链）；④ run() 内联 op 包装（testDiv.removeChild/p0.parentNode setter 陷阱）surround 期零调用零写入而 p0.parentNode 终值 null——**父链置空走 defineProperty(value:) 覆盖路径**（R243/R245 同款）绕过 accessor 与 removeChild，某处对 p0 defineProperty 覆盖未同步 testDiv.childNodes。修（land）：part06 surround R237 插入前显式 newParent.remove()（Node.prototype.remove R238 泛型，wrapper/handle/plain 三域通吃）+ try/catch——spec concept-node-pre-insert 的 adopt 步显式化（防御性正确；本形态下幽灵 P 真正写入者是 defineProperty 覆盖路径，留 R249）。A/B：surround 1806P/34F 净 0（逐 subtest diff=0）+ ranges 全量 set-diff 0/0 + insertNode 1841P 保持 + nodes 57=57；fmt/clippy 干净；make test 1F 为 XOpenDisplayFailed 环境项；wpt-data 探针全部还原。R249 首选：grep surround 链所有 defineProperty('parentNode',{value:}) 写点，对「值变更时旧父 childNodes 未 splice」加同步摘除（幽灵 P 确切机制）；次选：17,x "[object Object]" 同源排查**
**上轮**: R247 — **M4：引擎级复现证伪「iframe-window factory no-op」假设（诊断修正轮，无代码 land，基线 1806P/34F 零漂移）——r247_tmp_repro（已清理）逐步复现 17,0 精确形态：顶层形态（implementation.createHTMLDocument + main-doc paras[0]）scDocEl=[P(1),BODY(2)] 全链工作；iframe-window 形态初见 de2=[P(1),BODY(0)] 疑 BODY 丢子——随后定位为复现构造 bug（variant 2 从未把 paras append 到 fd2.body，fp1/fp2 属 R245 测试的另一个 fd）；精确序列隔离 fdZ（iframe 委托 factory + text-child paras + body appends + createRange + setStart/setEnd）Zbl:2,2 完全正常，append-only 探针 Xbl/Ybl 全部正常。修正结论：R246 的「no-op」是 dump 伪影（positionTests 阶段读 documentElement getter，restoreIframe 每轮重建——getter 新树与 walk 的 sc 旧引用是不同对象，R246 记录的「对象域分裂」本身即伪影来源）；引擎级 17,0/13,0 序列全部通过，真实分歧在 harness 全环境（referenceDoc 逐轮克隆 + window.eval(testRangeInput) + iframe common.js setupRangeTests 交织），单测沙箱无法重现。R248 方法论修正：放弃 engine 单测复现路线，改 R222 式 win.run() 内联探针——插桩注入 Range-test-iframe.html 的 run()（range 创建点），dump sc/newParent 创建时对象形态（factory vs 克隆 vs wrapper 域）+ domTests 后双树 walk 差分，在 harness 真实环境内定位 sim/host 分歧的对象域断点**
**上轮**: R246 — **M4：17,x / 13–14,x sim 树等价深诊断（探针轮，无代码 land，基线 1806P/34F 零漂移）——R246-probe（positionTests 阶段 post-harness，无预调用污染）对 i∈{17,13,14},j=0 dump双侧 nextNode walk 逐 hop 对照 + 首差节点 + documentElement/#test 子名。三发现：① 17,0 hop8 分歧 A=foreignComment vs E=BODY（host html 子树缺 BODY），但 dump 时刻 deA=[HEAD,BODY] P 缺失——host surround 在 iframe-window factory docEl 容器整体 no-op（顶层 implementation factory 形态 R245 单测已通，iframe-window 形态未通——两 factory 路径行为不一致）；② 13/14,0 hop10 分歧 DIV#test 内 A=P(id=a) vs E=P(id=b)——host newParent 原件留在 testDiv（克隆/引用错位，spec 要求移动本体）；③ R234 动态 documentElement getter「首个元素子」语义被插入的 P 干扰 + walk 用 sc 固定引用——两 docEl 对象域分裂（诊断工具层，probe dump 须统一经 sc 引用）。R247 修复切片按依赖序：iframe-window factory docEl no-op 断点定位（两 factory 路径差分 dump）→ newParent 移动本体语义 → probe 工具修正**
**上轮**: R245 — **M4：factory doc parentNode getter-only 移动语义修复（17,x 深诊断轮，引擎 bug 落地，WPT 净 0）——17,x（[foreignDoc.documentElement,0,…,1] 元素 newParent 族）五轮探针链（identity 排除 → sim 内部日志 → micro-probe 步进隔离 → 运行时标记）定位真根因两层：① factory headEl/body 的 parentNode 是 getter-only accessor（R130 {get:()=>docEl}），fragment appendChild 的 c.parentNode=this 裸赋值被吞（HEAD.parentNode 恒指 docEl）；② _zwMEl.appendChild 的「从旧父摘除」c.parentNode.removeChild(c) 无 try/catch——HEAD 已摘出 docEl.childNodes，factory docEl.removeChild identity 找不到 → NotFoundError 直接传播到 sim catch → NOT_FOUND_ERR（WPT 17,4/11/13 期望异常来源）。修两站点（spec concept-node-pre-insert 的 adopt 摘除幂等语义）：_zwMEl.appendChild 摘除 try/catch + 入树父链 defineProperty 强写；factory fragment appendChild 父链强写。micro-probe 验证：p1.appendChild(frag-with-HEAD) THREW NotFoundError → OK(n:1)。A/B：surround 1806P/34F 净 0（17,4/11/13 失败形态迁移：NOT_FOUND assert → sim ret:ok + 树 walk TypeError——sim 成功后比较走到更深处，全解需后续 sim 树等价工作）；ranges 全量 set-diff 0/0；insertNode 1841P 100% 保持；nodes 57=57 逐条一致；engine 2392 全绿（新增 r245 单测三段断言）；fmt/clippy 干净；make test 1F 为 XOpenDisplayFailed 环境项 run-rules §10。land 依据：WPT 净 0 但引擎 move 语义 bug 真实（五轮链实证），为 17,x 下一步扫清 NOT_FOUND 噪声。R246 首选：17,x sim 树等价（sim ret:ok 后 assertNodesEqual 的 expected 树含无 nodeType 对象——probe dump mySurroundContents 成功后 expectedRoots 首差节点形态）；次选：16,x startOffset 11F（harness-iframe index 算术）**
**上轮**: R244 — **M4：Document 容器 doctype contained surround HRE（25/26,x 12F 簇全解，+12P 纯增）——WPT Range-surroundContents 25/26,x `[document,0,document,1/2]` × 元素 newParent（j=0,4,6,9,11,13）期望 HRE 而 host NO_THROW。R244-probe 探针（restoreIframe 后 sim/host 结果 + doc 子形态 dump）24 行实证：iframe doc=[doctype,html]，range 的 contained children 含 doctype——sim（common.js myExtractContents）步骤 9「contained children 含 DocumentType → HRE 树不变」（spec dom-range-extract-contents 同款，surround 步骤 3 调 extract 原样上抛）；host 对元素 newParent 无任何拦截（partial 不命中 + nodeType 白名单过 → 实际变更树/空转）。修：part06 surroundContents 入口新增 _r244DoctypeCheck（仅 cac===Document 时扫描——doctype 只能挂 doc 下零成本门；contained 判定同容器区间算术 + 跨容器 sideIdx 落点；命中抛 'A DocumentType node cannot be extracted.' HRE 树不变）。A/B：surround 1794P→1806P（+12 纯增，F2P=12/P2F=0/P→F=0）+ ranges 全量 set-diff 12 Fail→Pass/0 Pass→Fail；insertNode 1841P 100% 保持；nodes 失败集 57=57 逐条一致；events/collections/traversal 基线值不变；native 1806P 同值（diff exit 0）；engine 2391 全绿（新增 r244 单测 A/B 形态 HRE 树不变 + C 正常 wrap 零误伤 + D 跨容器零误伤）；fmt/clippy 干净；make test 1F 为 XOpenDisplayFailed 环境项 run-rules §10。R245 首选：17,x position/DOM 12F（cDP 解锁后新前沿——expected "[object Object]" 形态，sim walk 产物含无 nodeType 对象）；次选：16,x startOffset 11F（harness-iframe index 算术）**
**上轮**: R243 — **M4：cDP 重评转正（R235 负结果的翻转面已消失，净 +26 / ranges 全量净 +110）——R235 首测 -28 回退的实验在 R236–R242 十一个全序分支后复测：fixed 34 / new 8（净 +26）。方法论印证：负结果会过期——host 行为面大改后旧实验必须复测。修：_makeDetachedDocument 内部 docEl/headEl/body 附 contains/cDP own-prop（委托 _zwNodeContains/_zwCompareDocumentPosition，与 _zwMEl 同源 R79）——sim 深入 foreignDoc/iframe doc 合成树不再 TypeError。A/B：surround 1768→1794P（+26 净）+ extract 132→140P（+8）+ clone 155→162P（+7）+ ranges 全量 set-diff 118 Fail→Pass/8 Pass→Fail（净 +110）；insertNode 100% 保持；delete 零位移；nodes 失败集逐条一致（57=57 零扰动）；native 1768→1794P 同值；engine 2389 全绿（新增 r243 单测）；fmt/clippy 干净。commit `75c87456c`。十轮累计 surround 1385→1794P（+409）。R244 首选：25/26,x Document 容器 HRE 12F（[document,0,…] insertNode doc 级位序）；次选：17,x position 9F（cDP 解锁后新前沿）**
**上轮**: R242 — **M4：元素 ec 祖先形态 extract/surround 全序（24,x 全解 +38P）——24,x = `[testDiv,2,paras[4],1]`（sc 元素祖先、ec 元素直接子、双侧 clean 边界）。修三件（part06）：① extractContents R242 分支（contained [so,ecIdx) 子本体移入 frag 复用 R241 move 兜底 + ec shallow clone 承接 [0,eo) 子 + 塌缩 (sc,so)——对齐 sim myExtractContents ancestor 分支 last-partially-element 语义）；② surround 元素 newParent 同形态全序（extract→清子→按位插→appendChild→selectNode）；③ leaf newParent 同形态先 extract 后 HRE。单测边界注记：engine 沙箱树完好时 spec 正确行为是 partial 检查先抛 InvalidStateError——WPT 成功期望来自 harness 克隆树遍历盲区，单测覆盖 extract standalone。A/B：surround 1734→1768P（+34 0 新失败 24,x 全解）+ extract 128→132P（+4）+ ranges 全量 set-diff 38 Fail→Pass/0 反向；insertNode 100% 保持；delete/clone 零位移；native 1734→1768P 同值；engine 2388 全绿（新增 r242 单测）；fmt/clippy 干净。commit `d8ce01d1c`。九轮累计 surround 1385→1768P（+383）。R243 首选：cDP 40 重评（R235 负结果复测——当时 -28 的 sim 翻转面在 R236–R242 十一个新分支后可能已消失）；次选：25/26,x Document 容器 HRE 12F**
**上轮**: R241 — **M4：wrapper 域 move 语义兜底（28,x 簇 +33P，双份树根因）——R241-probe 全树递归 dump（含属性/文本/CDATA）实证 host 树双份：DIV=[P#a[拷贝…], P#a[拷贝], 原件…] vs sim DIV=[P#a[提取内容], comment-remainder]。根因：WPT iframe 的 covered 子在 querySelector wrapper 域（R240 实证），对 fragment 的 appendChild 是 **clone 语义**——原件残留 + 拷贝进 frag；R240 单测通过是因 engine 沙箱子是主文档 _zwMEl 产物（move）——两域 append 语义不同。修：contained-move 循环 append 后强制摘除（原件仍在 sc.childNodes 则 removeChild——move 兜底，对 move 域无害 no-op）。A/B：surround 1701→1734P（+33 0 新失败 28,x 簇主解）+ extract 126→128P（+2）+ ranges 全量 set-diff 36 Fail→Pass/0 反向（vs R239）；insertNode 100% 保持；delete/clone 零位移；native 1701→1734P 同值；engine 2387 全绿（新增 r241 单测）；fmt/clippy 干净。commit `dcd812416`。八轮累计 surround 1385→1734P（+349）。R242 首选：24,x assert_unreached 32F（[testDiv,2,paras[4],1] 与 28,x 同族但 ec 是元素内 text——祖先分支二级后代扩展 + contained-move 复用）；次选：25/26,x Document 容器 HRE 12F**
**上轮**: R240 — **M4：祖先 extract 的 contained 中段子移动（extract +1P，28,x 深诊断推进）——28,x = `[testDiv,0,comment,5]`（R236 祖先分支直接形态）。三轮探针实证：① sim 的 partial 检查在此形态不触发（nextNode trace 仅 DIV→P→text→null——克隆树 paras 未挂进 testDiv 克隆 childNodes，querySelector 返 wrapper 域使 append 落 wrapper 列表，P.nextSibling null 爬升终止）；② host 对称不触发——分歧在 surround 主体：sim generic extract 移动中段子+削头，host R236 分支只削头。修：R236 祖先分支补 contained 中段子移动（spec containedChildren），过程坑 = 按下标迭代时 appendChild 收缩滑位把 ec 本体拖进 frag（快照后移动修正）。A/B：extract 125→126P（+1）；surround 1701P 持平（28,0 从 walk 级 First-differing 推进到深层 isEqualNode 分歧——需 R241 generic cross-container surround 全序）；insertNode 100% 保持；delete/clone 零位移；native extract 同值 126P；engine 2386 全绿（新增 r240 单测）；fmt/clippy 干净。commit `36c54d135`。R241 首选：28,x generic cross-container surround 全序；次选 25/26,x Document 容器 HRE 12F；深项新增：克隆树 wrapper-domain 挂链（querySelector wrapper 域断链——assert_unreached 族共同根因候选）**
**上轮**: R239 — **M4：部分包含检查序 + nextNode 遍历（INVALID_STATE 30F 全解）——根因：20,x `[t0,0,t1,0]` + Document/Doctype newParent 时 host 的 R209 nodeType 检查先于 R210 partial 检查执行，sim 是 partial 在前（WPT 期望 INVALID_STATE_ERR）。修两件（part06 入口）：① 检查序对调（过程坑：拼接丢闭合大括号——shim 编译错当场暴露）；② DFS→nextNode 序遍历（过程坑：首版 DFS 比 sim 更完备，24,x 反向翻转 12F——DFS 扫到 sim 遍历漏掉的 partially-contained 节点，WPT 期望 INVALID_NODE_TYPE；改 sim 同款遍历原语后盲区对齐 fixed=30/new=0）。A/B：surround 1671→1701P（+30 0 新失败 INVALID_STATE 簇全解）+ ranges 全量 set-diff 30 Fail→Pass/0 反向；insertNode 100% 保持；extract/delete/clone 零位移；events 失败集一致；native 1671→1701P 同值；engine 2385 全绿（新增 r239 单测）；fmt/clippy 干净。commit `3ddd77312`。六轮累计 surround 1385→1701P（+316）。R240 首选：28,x differing 28F（`[foreignDoc.body,0,foreignTextNode,36]`）；次选：25/26,x HRE（Document 容器）**
**上轮**: R238 — **M4：Node.prototype.remove 泛型（assert_unreached 76F 簇主根因，+59P 纯增）——R237 后 228F 重评：assert_unreached 76（18/19/24,x）。递归探针对 19,1 dump 双引擎树实证：host detachedPara1=[Ä,Op]（原文本残留）vs sim [Ä]——根因 = extract/surround 路径 4 的 `typeof kids[j].remove === 'function'` 守卫遇到 iframe 工厂文本（无 remove 方法，Text.prototype 占位链亦无）静默跳过移除。修：Node.prototype.remove 泛型（spec dom-child-remove，own-property 优先）。过程坑：首版补 _zwMText/_zwMComment 工厂实测 0 变化（失败节点来自 iframe createTextNode 工厂）——泛型原型一次覆盖全部形态。A/B：surround 1612→1671P（+59 0 新失败）+ extract 121→125P（+4）+ nodes 12661→12663P（+2）+ events 失败集逐条一致（7=7）；insertNode 100% 保持；delete/clone 零位移；native 1612→1671P 同值；engine 2385 全绿（新增 r238 单测）；fmt/clippy 干净。commit `2a18b40a2`。R239 首选：INVALID_STATE 30（20–22,x 跨 text 边界部分包含检查）；次选：HRE 24,x 跨子区间**
**上轮**: R237 — **M4：surround 路径 4 全序对齐（12–14,x 全解，净 +69P 纯增）——R237-probe 探针对 12,0 `[documentElement,0,documentElement,1]` 双引擎 dump 实证三分歧：host docEl=[BODY,P]（P 恒末尾 appendChild + P{2} 原文本残留 + 边界不更新）vs sim [P{head},BODY]（P 按位 + 清子后含 head + selectNode）。修三件（part06 路径 4）：① 清 newParent 既有子（sim 步骤 2，**先清再 clone**——首版清在 clone 后把 covered 克隆一并误删，单测 cleared:0 抓回）；② 按位插入 insertBefore(ref=childNodes[startOffset])（步骤 4）；③ selectNode 边界 (父,idx)-(父,idx+1)（步骤 6）。A/B：surround 1543→1612P（+69 0 新失败——12–14,x 36F + 清-序连带 +33）+ ranges 全量 set-diff 69 Fail→Pass/0 反向；insertNode 100% 保持；extract/delete/clone 零位移；native 1543→1612P 同值；engine 2384 全绿（新增 r237 单测）；fmt/clippy 干净。commit `1729088ea`。R238 首选：HRE ~36 重评（R236/R237 全序落地后 host 行为面已变，旧归因可能失效）；次选：19,x detached differing ~28F**
**上轮**: R236 — **M4：祖先元素区间 extract/surround 全序（23,x 32F 全解，净 +37P 纯增）——形态：sc 是 ec 的元素祖先容器且 ec 为直接 CharData 子（`[paras[0],0,paras[0].firstChild,7]`）。host 全链路空转（_coveredChildren 对 sc≠ec 恒 null；extractContents 落 R234 else-branch 仅 collapse；surround leaf 直接抛/元素 defer）。修三件（part06）：① extractContents 祖先分支（对齐 sim myExtractContents：clone ec 头切片 [0,eo) 入 frag + deleteData 削头 remainder 留树 + 塌缩 (sc,so)）；② surround leaf-newParent 同形态先 extract 再 insert 后抛 HRE；③ surround 元素 newParent 全序（extract → 清 newParent 既有子（sim 步骤 2，首版漏此步 wrap 残留原文本）→ insertNode（newParent===sc 自身时 R215 inclusive-ancestor 在树变更后抛 HRE 对齐 23,0）→ appendChild(frag) → selectNode）。A/B：surround 1509→1543P（+34 0 新失败 23,x 全解）+ extract 118→121P（+3 恰为 range 23 三 subtest）+ ranges 全量 set-diff 37 Fail→Pass/0 反向；insertNode 100% 保持；delete/clone 零位移；native 1509→1543P 同值；engine 2383 全绿（新增 r236 单测）；fmt/clippy 干净。commit `78d0664fd`。R237 首选：12–14,x 残余 differing ~36F（R234 动态 getter 后 docEl 容器 extract 首差形态）；次选：19,x detached ~28F**
**上轮**: R235 — **M4：leaf-newParent extract-first 扩展两形态（+88P 纯增）+ cDP own-prop 负结果——R235(a) 给 _makeDetachedDocument 内部 docEl/headEl/body 附 contains/cDP own-prop（R234 同类诊断：foreignDoc = createHTMLDocument 路径的 docEl/body 是 plain object 无 cDP，17,x nodeB.cDP 17F + 30,x node2.cDP 23F）实测 fixed 6 / new 34（净 -28）——与 R227/R232 同现象（解锁方法面使 sim 完成度变化，position 翻转面 > DOM 修复面），回退记深项：17,x/30,x 的 40F 绑定 host 对 foreignDoc.docEl 容器的 surround 全序缺口，不可单独补方法面。R235(b) land：surroundContents leaf-newParent 路径只覆盖 R230 同节点 Text/CDATA 形态，补两缺失形态的「extract 先行」——① 异节点同父 CharData 区间（6,x 46F assert_unreached 簇：extract 削首尾切片→insertNode→HRE）；② 元素容器含覆盖子（18,x differing 簇：extract 移出 covered 子→insertNode 留 newParent→HRE）。过程坑：首版第二分支未排除同节点（sc.parentNode===ec.parentNode 对同节点恒真）劫持 R229/R231 PI 流 → 39,x 翻红 104，加 sc!==ec 守卫后 fixed=88/new=0。A/B：surround 1421→1509P（+88 0 新失败）+ ranges 全量 set-diff 88 Fail→Pass/0 Pass→Fail；insertNode 100% 保持；extract/delete/clone 零位移；native 1421→1509P 同值 +88；engine 2382 全绿（新增 r235 单测）；fmt/clippy 干净。commit `625e67e62`。R236 首选：23,x node-与-后代区间 32F（host _coveredChildren 对 sc≠ec 恒 null 使 extract 空转，spec 需 splitText 后代边界）；次选：12–14,x 残余 differing**
**上轮**: R234 — **M4：fresh-doc 深项落地——动态 documentElement getter（cDP 108F 簇正面修复，R233 归因修正）**——R234-probe 探针 dump 920 轮 restoreIframe 三站点（PRE/清理循环后/POST）：doc 级形态 round 1 起 920 轮完全一致（distinct POST 形态数 = 1），**「跨轮残留累积」假设在 doc 级被证伪**。真根因：_zwMakeIframeDoc 的 documentElement 是固定闭包 getter——restoreIframe 摘除 factory docEl + appendChild(referenceDoc 克隆) 后仍返回**已脱离的空壳**（无 cDP/contains 方法面）——rows 12–14,x `[documentElement,…]` 的 startContainer 即该空壳，sim isAncestorContainer 调 docEl.cDP → TypeError（108F 簇）。修三件：① part05 动态 getter（doc.childNodes 首个元素子，回落 factory docEl——spec「首个元素子」语义，restoreIframe 后读到克隆子树经 _zwDeepCloneEl→_zwMEl 方法面全配）；② part06 extract 记账两件（plain 子摘除后 parentNode 记到 fragment——move 语义无根判定；跨容器路径 collapse(true)——harness「containers must be same」断言）；③ part23 r234 单测（dyn/cdp/same/rooted 四断言）。**A/B：surround 1385P→1421P（+36，12/13/14,x 全解锁 0 新失败）+ ranges 目录全量 39571→39640P（set-diff fixed 39/new 3——3 个 new 为 49,x 旧空壳空转 vacuous pass 翻转，行为暴露非回归）；insertNode 1841P 100% 保持；native 同值 1421P；engine 2381 全绿；fmt/clippy 干净**；make test 1F 为 XOpenDisplayFailed 环境项（run-rules §10）。commit `3bafaec06`。R233 三簇同根假设修正：cDP 簇根因是 stale 指针非 R219×fresh-doc 耦合——rows 12–14,x（69F）已解，17,x/30,x foreignDoc 侧 39F 待同款诊断。R235 首选：foreignDoc cDP 残余 39F；次选：419F 重聚类（HRE 37 / INVALID_STATE 30 / assert_unreached 133 独立性确认）**
**上轮**: R233 — **M4：sim 全源注入复刻（负结果轮，关键归因突破）**——R231 方法论复用：注入 case-local mySurroundContents + common.js 依赖链 13 函数到沙箱，对 24,x 复刻形态直接执行。**实证：复刻形态上 sim（INVALID_STATE_ERR）与 host（InvalidStateError）行为一致**——但 WPT 24,x 期望 HRE。推断真实分歧在 sim partial-check 的 nextNode 遍历爬出 testDiv 后进入 body 兄弟（harness iframe）再上合成 docEl——爬升链 cDP 缺失（R219 关）使 sim 中途 TypeError 被 catch，走不到 INVALID_STATE 分支，后续 validity 产出 HRE。**结论：HRE 37 / INVALID_STATE 30 / cDP 108 三簇同根（R219 × fresh-doc），单独解任一簇被另两簇钳制（R227/R232 两次 -28P 实测）**。零代码 land（探针清理，engine 2380 全绿）。R234 首选转向 fresh-doc 深项本体（restoreIframe 残留 dump——解锁 ~250F 的钥匙），次选 startOffset 11F 独立小簇**
**上轮**: R232 — **M4：cDP-only 开关拆解 + HRE 簇 sim 考古（负结果轮）**——455F 聚类：assert_unreached 133 / cDP 108 / HRE 37 / INVALID_STATE 30 / startOffset(16,x) 11 / differing ~130。两实验均回退：① cDP-only 单开（R227 三开 -28P 归因）实测同样 -28P（1385→1357）——有害轴就是 cDP 本身（解锁 sim 深入合成树后跨轮残留分歧），R219 族整体保持关；② leaf 分支 kids>0 extract-first 实现 0 变化（_coveredChildren 对该形态返 null——分支不可达），按最小改动回退。HRE 37F 考古：24,x 期望 HRE 但 R210 partial 检查应先抛 INVALID_STATE——推断 sim 的 partial 检查 nextNode 遍历提前终止（部分包含子未被扫到）；16,x startOffset=2 涉及 body 内 harness iframe 的 index 算术。零代码 land。R233：mySurroundContents 全源注入探针（R231 方法论复用——dump sim partial-check 终止点与 validity 触发位，逐行对齐 host）+ assert_unreached 133 深项评估**
**上轮**: R231 — **M4：同节点区间 extract 不塌缩——方法论突破：把 common.js myExtractContents 源 + 5 helper 注入沙箱直跑，实证 [t,2,8] 终态 {data:"Op", so:2, eo:8}——deleteData 执行但 range 不塌缩（sim 的 CharacterData first/last 子路径中段早返回，尾部 setStart/setEnd 不执行；前几轮按 spec 推断塌缩语义两度证伪）。修：R211 同节点分支移除 setStart/setEnd（异节点同父保持 (父, si+1)）——surround 1269P→**1385P/455F（+116，diff 116 fixed / 0 new 纯增）**；insertNode 1840P/0F 保持 100%、extract/delete 稳；全量 ranges 38070→**38186（净 ≈ +116P）**；engine 2380 单测全绿（回归测试扩展 r231:2,8 断言：同节点 surround 抛 HRE 后边界保持 + data 已削）；fmt/clippy 干净。R232 靶点（455F）：assert_unreached ~110 / cDP 108（fresh-doc 深项）/ HRE+INVALID_STATE ~67 / 其他 differing ~170 重聚类 + 深项**
**上轮**: R230 — **M4：Text 容器 leaf-newParent 的 extract 先行——650F 重聚类 Text differing ~180 簇首片：9,x `[detachedPara1.firstChild,2,8]` + Text newParent 族根因 = sim 序步骤 3 extract（源 text 削前缀 "Op"）→ 4 insertNode → 5 appendChild(frag) 抛 HRE；host 的 leaf 分支（R229 只补了 7/8 容器）对 3/4 容器仍只 insertNode，源 text 保留原文（got "qrstuv" 族）。修：3/4 同节点路径补 extractContents 于 insertNode 前——surround 1190P→**1269P/571F（+79，diff 79 fixed / 0 new 纯增）**；insertNode 1840P/0F 保持 100%、extract 稳；全量 ranges 37991→**38070（净 ≈ +75P）**；engine 2380 单测全绿（回归测试扩展 r230 断言：源削前缀 + HRE）；fmt/clippy 干净。R231 靶点（571F 聚类）：assert_unreached 133 / cDP 108（fresh-doc 深项）/ HRE+INVALID_STATE 67 / **endOffset expected 8/9 got 2 ~93（最大可动簇——sim 的 extract 对工厂 text 的塌缩行为与 spec 不同，需逐行对齐 sim early-return 形态）** + 深项**
**上轮**: R229 — **M4：surround 残余 sim 全序复刻三步——897F 重聚类出三个可轻量修复簇：① leaf-newParent（Text/Comment 型）对 comment/PI 容器 sim 序 = 先 extractContents（变更 data）再抛 HRE（旧直接抛使 data 不变，35,1–2/36,x 的 Stuwxyz 残余 22F）→ +55P；② detached 全区间 extract **不塌缩**（sim 塌缩走 parent_ 定位，无父不重设边界；旧 R228 分支强 collapse 使 endOffset 8→0，32,x 18F）→ +69P；③ 同节点区间 **self-collapse**（sim 塌缩首分支 isAncestorContainer(start,end) 对 self 命中 → (容器, startOffset)，非 (父, si+1)——else 分支才走父定位；39,x 17F）→ 累计 +117P。surround 943P→**1190P/650F（+247，失败集 diff 247 fixed / 0 new 纯增）**；insertNode 1840P/0F 保持 100%、extract +4、delete/clone 稳；全量 ranges 37740→**37991（净 ≈ +254P）**；engine 2380 单测全绿（R228 回归测试扩展三断言）；fmt/clippy 干净。R230：surround 剩 650F（assert_unreached ~253 / cDP 108 fresh-doc 深项 / Text differing ~180 extract 边界精度）+ 深项**
**上轮**: R228 — **M4：detached 同节点 CharData 区间 surround——R227 聚类的 comment 分裂族四环根因：① extractContents R211 分支 parentNode 守卫把 detached 节点（parentNode null）整体拒之门外（中段切片/deleteData 空转，「Stuwxyz got Stuvwxyz」）；② surroundContents _r212 门同款守卫；③ detached 分支内 _r211p.childNodes/ownerDocument/indexOf 三处 null 崩（TypeError 代 HRE）；④ insertNode 的 HRE 被 try/catch 吞（sim 序：extract 变更树后抛，common.js mySurroundContents 原样上抛）。修：_r228sameNode（sc===ec && isCd）放宽两处守卫 + detached 子分支（切片→deleteData→collapse 到 (容器, startOffset)）+ null 三守卫 + HRE 不再吞——surround 893P→**943P（+50，失败集 diff fixed 50 / new 0 纯增）**、extract 99→111P（+12 连带）、insertNode 1840P/0F 保持 100%、delete 稳；全量 ranges 37678→**37740（净 ≈ +60P）**；engine 2380 单测全绿（新增 r228_detached_chardata_interval_surround：data 切片 + HRE 上抛双断言）；fmt/clippy 干净。R229：surround 剩 897F 重聚类（assert_unreached 282 / cDP 108 fresh-doc 深项 / HRE+INVALID_STATE ~73 sim 全序残余）+ 深项**
**上轮**: R227 — **M4：surround 947F 重聚类（负结果轮）**——聚类：assert_unreached 282 / cDP 108 / HRE 93 / extract 边界 ~220（comment 分裂族 84）/ INVALID_STATE 30 / startOffset ~30。两个 A/B 实验均负已回退：① R219 原型兜底 kill-switch 在 R222/R225/R226 后树形态下复测仍 -28P（893→865），保持关——108F cDP 簇绑定 fresh-doc 深项；② 单侧祖先 HRE 预检 -2P（sim 的 ensure 在 extract 塌缩后的 parent_ 判定，单侧必翻转）——正解是 CharData 路径复刻 sim 全序。零代码 land，聚类数据与决策依据入 evidence。R228：comment/PI 区间 surround（~84F，R212 路径扩 7/8 容器）+ sim 全序复刻（HRE/INVALID_STATE/startOffset ~153F）+ fresh-doc 残余深项**
**上轮**: R226 — **M4：`_zwMEl` insertBefore 的 **ref 位先取后摘**——R225 回退件（30,4+28,0 净 0）的根因收口：旧版先 removeChild 再 indexOf(ref)，`c === ref` 自引用形态（WPT 30,4 的 foreignDoc.body[0]===node）detach 后 ref miss → push 尾部（探针实证 body:[fp2,ftn,fp1]，期望 [fp1,fp2,ftn]）；spec `concept-node-pre-insert` 的 referenceNode index 在 adopt 摘除**之前**固定——修：`_r226RefIdx` 先取位、后摘除、按固定位 splice，自引用净效果 = 原位重插 no-op（R225 的单侧 advance 不再需要）。**Range-insertNode 1838P→1841P/0F 整文件 100%**（30,4/28,0 全 4 断言转绿），nodes +1、events/collections/traversal/surround 全稳（净 ≈ +3P）；engine 2379 单测全绿（新增 r226_self_ref_insert_before_keeps_position：fp1 保持首位 + p0 不动双断言）；fmt/clippy 干净。R227：surround ~350F（893P 基线）重聚类——insertNode 100% 后 ranges 族最大剩余簇 + 深项**
**上轮**: R225 — **M4：insertNode 残余 23F 三根因修复——探针复刻 setupRangeTests xmlDoc/foreignDoc 段实证：① fragment 不展平（`_zwMEl`/工厂元素 insertBefore 把空 docfrag 本体塞 childNodes 占位，endOffset expected 1 got 2）；② syncEnd209 identity miss（父 childNodes 是 `_wrapHandle` 包装，与 raw 节点 `===` 恒 false → end 永不同步）；③ doc 级 insertBefore 不摘除原父（spec pre-insert adopt 步骤缺失 → PI/comment 移位重复入列，探针 `[10,7,1,7,8]` 双 PI）。修：三站点 fragment 展平（part03 `_zwMEl` append/insert + part05 工厂 insertBefore）+ `_r225Same` handle-aware 同一判定 + fragment newOffset 语义 + doc 级 insertBefore 入口先 removeChild（part03 7568）——insertNode 1817P→**1838P（+21）**、文件级 23F→**2F**（仅 30,4），nodes/events/collections/traversal/surround 全稳（净 ≈ +21P）；「node-is-referenceNode 前移」步骤实测净 0（30,4 +2 / 28,0 -2，sel 容器 proxy trap 语义不连贯）已回退，30,4+28,0 成对记 R226；engine 2378 单测全绿（新增 r225_insert_node_doc_order_and_fragment_flatten）；fmt/clippy 干净。R226：30,4+28,0 成对（insertNode refNode-advance 与 proxy trap detach/advance 连贯）+ surround ~350F 重聚类 + 深项**
**上轮**: R224 — **M4：insertNode 的 node 自身类型合法性检查——148F 重聚类定位 HRE ~70 主簇根因：Document 型 node（nt=9，xmlDoc/foreignDoc/document 作被插 node）插入静默成功——sim（common.js ensurePreInsertionValidity）返 HRE 而 host 不抛（WPT「A HIERARCHY_REQUEST_ERR must be thrown」71F 簇：foreignDoc 27 / xmlDoc 31 / document 13）。根因：R215 引入的 `_r215Validate` 只实现 parent 类型/环/Text-入-Document/Doctype 位序四查，缺 spec `concept-node-ensure-pre-insertion-validity` 的「node 非 DocumentFragment/DocumentType/Element/CharacterData → HRE」第四查。修：白名单 11/10/1/3/4/7/8，其余（含 9/2）抛 `'Nodes of type <nt> cannot be inserted.'` HRE——insertNode 1693P→**1817P（+124）**、文件级 148F→**23F**，nodes/events/collections/traversal/surround 全稳（净 ≈ +124P）；engine 2377 单测全绿（新增 r224_insert_node_rejects_document_type_node：Document 型抛 HRE + Element 型照常插入）；fmt/clippy 干净；make test 1F 为 XOpenDisplayFailed 环境项 run-rules §10。R225：insertNode 剩 23F（25/26/29/31 的 doc 级 PI/comment 位序 + 30,4 foreignPara1 + docfrag 作 node 的 range position 5F）+ surround ~350F 重聚类**
**上轮**: R223 — **M4：doc 级子 parentNode 健壮写入——row 29 foreignDoc 族 22F 的 null.nodeType 根因修复：三层探针（step 包装栈捕获 → myInsertNode 包装 dump → 属性描述符探针）定位 foreignDoc 的 html/comment 子 parentNode 恒 null——html 子的 parentNode 是 getter-only accessor（赋值抛 which has only a getter）、handle comment 经 get trap 读 _zwNodeParent 注册表，doc 级 `c.parentNode = this` 对两形态分别抛/静默 no-op 从未生效；sim ensurePreInsertionValidity 读 parent_.nodeType 崩。另修正 R222 的「foreignDoc.childNodes 空」为探针伪影（probe 读 contentWindow.foreignDoc 是 undefined，守卫跳过后打印空列表误导）。修：_r223SetParent helper（handle 形态写 _zwNodeParent 注册表 R180 plainParent 槽；其余恒 defineProperty own 数据属性遮蔽继承 getter）落 doc 级 appendChild×1 + insertBefore×3 站点——insertNode 1669P→**1693P（+24）**、traversal 1595→**1602P（+7）**、mutations -2、其余全稳（净 ≈ +29P）；engine 2376 单测全绿（新增 r223_doc_append_child_parent_node_sticks）；fmt/clippy 干净；make test 1F 为 XOpenDisplayFailed 环境项 run-rules §10。R224：insertNode 剩 148F（HRE ~70 跨容器族 + 散布 78）+ surround ~350F 重聚类**
**上轮**: R222 — **M4：iframe doc doctype 入 childNodes（R216 回退件重试成功）——204F 重聚类定位 doctype 累积根因：R222-res 探针逐轮 dump doc.childNodes 发现轮 0 无 doctype 入树 → restoreIframe 清空 doc → 兜底 createDocumentType 触发 → 轮 1 起 [dt,dt,HTML] 双 doctype 稳定累积（[document,0,document,N] 语义基准错位，rows 25/26 整行 22F）。修：_zwMakeIframeDoc 把 _r209Dt unshift 进 childNodes 首位（R216 时净 -55 的扰动面已被 R221 fresh-doc 吸收——初始 [dt,docEl] 使清理循环不清空 doc，兜底不触发）——insertNode 1637P→**1669P（+32）**、surround 865P→**893P（+28）**、nodes +2、events +2、其余全稳（净 ≈ +64P）；engine 2368 单测全绿（新增退化路径稳定性断言）；fmt/clippy 干净；make test 1F 为 XOpenDisplayFailed 环境项 run-rules §10。R223：insertNode 剩 172F（HRE 73 跨容器族 + row 29 foreignDoc.childNodes 空——预存问题与 R222 无关）+ surround ~350F 重聚类**
**上轮**: R221 — **M4：fresh-doc 落地——iframe doc 的 body/head 重绑（appendChild docEl 时）——R220 定位的平行树根因正面修复：restoreIframe 语义（清 doc 首末子 + appendChild(referenceDoc.docEl.cloneNode(true)) + setupRangeTests）下 doc.body/head 仍指 factory 原字面量，setup 内容与 range 的 docEl 克隆树分裂。修：iframe doc（_zwMarkup 印章）的 doc 级 appendChild 收到 HTML 元素时 doc.body/doc.head 经 defineProperty 重绑到克隆子树 BODY/HEAD 子——两树归一；非 restoreIframe 路径克隆无 head/body 子时保持原值零扰动——**Range-insertNode 1094P→1637P（+543）**、**Range-mutations 0P→1338P（整族解锁）**、surround 854→865P、delete 56→68P、extract 100→103P、clone 153→156P、其余 ranges 文件 + nodes/events/traversal/collections 全平（**ranges 族净增 ≈ +1910P**）；kill-switch 复测（fresh-doc 后重试 _r219ProtoMethods=true）：surround -28P → **仍保持关**（sim 的 mySurroundContents 经其它缝隙泄漏）；engine 2367 单测全绿（新增 r221_iframe_doc_body_rebind 三断言）；fmt/clippy 干净；make test 1F 为 XOpenDisplayFailed 环境项 run-rules §10。R222：insertNode 剩 204F 重聚类（rows 25/26/29 跨容器族 + HRE 69 + null-nodeType 66）+ surround 452F 重聚类**
**上轮**: R220 — **M4：deep-clone CDATA/PI 保型 + 跨轮残留结构根因定位——R220(a) insertNode 747F 聚类（assert_unreached 516 / HRE 86 / null-nodeType 66）：抽样 16,0/25,0/27,0 单独跑全 Pass、累积跑才 Fail → 残余主体是跨轮残留非 per-subtest 语义。R220-drift 探针定位结构根因：referenceDoc 克隆链拿到 HTML(0) 空壳（iframe docEl childNodes 恒空——head/body 是 detached doc 字面量不在 docEl 下）→ 每轮 restoreIframe 克隆回空壳、内容逐步丢失。（b）成对实验：head/body 链入 docEl.childNodes → rows 12–16 -158P，R221-probe 树签名定位**平行树**（restoreIframe 用克隆替换 doc 后 document.head/body 仍指 factory 原字面量——paras 在字面量 body 树、rangeFromEndpoints 在克隆 docEl 树，两树分裂）→ 正确修法 = fresh-doc 级整体重建（R208 家族深项实证），linkage 已回退记 R221。本轮 land：_zwDeepCloneEl CDATA/PI 子分支（旧 return null 丢子；CDATA 经 createCDATASection / PI 经 _zwMPiFromBogus 保型重建）——WPT 零位移（1094/854/100/153 全不变）、engine 2366 单测全绿（新增 r220_deep_clone 三形态保型断言）、fmt/clippy 干净。R221：fresh-doc 深项实施（iframe contentDocument 每轮整体重建）**
**上轮**: R219 — **M4：insertBefore 防递归 + insertNode endOffset 同步 + deep-clone 工厂化——R218 遗留 12,x–15,x Maximum call stack 72F 页面插桩定位（R219-sur-probe 的 A/E 树签名 dump）：restoreIframe 的 refDoc documentElement 克隆（合成 docEl，原型链达 Node.prototype）作容器调 insertBefore，旧 `typeof this.insertBefore` 命中原型方法自身 → 自递归（depth 4198）。修三件：① own-property 防递归判定（R126 同款）+ proxy 委托修正（`__zwHandle` 经 get trap 可读 → 直调 trap 版本；own 判定单独绕过 host 桥实测 surround -350P）+ 本地 splice 插入语义整体回退（绕过 detached doc `_tree` 查询视图 → sim 跨轮残留 rows 12–24 分歧）；② Range.insertNode element 容器 collapsed 插入的 endOffset 同步（spec 末步 setEnd(parent, newIndex+1)——15,x「expected 2 got 1」簇）；③ `_zwDeepCloneEl` Text/Comment 经 `_zwMText`/`_zwMComment` 工厂重建（旧裸对象缺方法面使 nextNode oracle 崩「hasChildNodes is not a function」）。三方法原型兜底（contains/cDP/hasChildNodes）已实现但 `_r219ProtoMethods=false` 暂缓——启用使 sim 深入共享 iframe doc 产生跨轮残留（surround -350 实测），待 fresh-doc 深项随同启用——insertNode 952P→**1094P（+142，12–15,x 溢出全消）**、clone 149→153P、extract 98→100P、surround 873→854P（-19 跨轮残留已定位）、delete 56 不变、nodes/events/traversal/collections ~平（净约 +126P）、engine 2360 单测全绿（新增 r219_insert_before_anti_recursion_and_fallback 三层断言）、fmt/clippy 干净；make test 1F 为 XOpenDisplayFailed 环境项 run-rules §10。R220：insertNode ~740F 重聚类（HRE 86 + assert_unreached 516 + null-nodeType 66）+ fresh-doc 深项评估**
**上轮**: R218 — **M4：CDATA splitText + detached fragment insertBefore——916P 基线聚类方法面两簇：① splitText nt=4 补入（spec CDATASection : Text 经继承可达——6,x 34F）+ CDATA split 尾节点保型（旧恒 createTextNode 使尾变 Text，改经源 doc createCDATASection）；② detached doc fragment 的 insertBefore（38,x 17F——ref=null 尾插 + ref 前插 + fragment 展平）。12,x 栈溢出 72F 三种探针复刻全 clean（真 harness sim 流程未复现——R219 页面插桩已定位：refDoc docEl 克隆容器 insertBefore 自递归）——insertNode 916P→952P（+36）、surround 865→873P（CDATA splitText 连带 +8）、全量 51589P/3449F/21T→51631P/3405F/23T 净 +42P（F2P 57 / P2F 13 形态重分布）、native 51634P（flips 仅 3 既存 flaky Timeout 家族）、engine 2358 单测全绿、fmt/clippy 干净；make test 1F 为 XOpenDisplayFailed 环境项 run-rules §10。R219：12,x 页面插桩 + HRE 86F 残余重聚类**
**上轮**: R217 — **M4：insertNode 的 Document 子位置规则——R215 校验四件缺 spec `dom-node-pre-insert` 的「If parent is a Document」四分支（25,0 探针：host 对 element 入已有 element 子 docEl 的 Document 不抛，sim 返 HRE）——补 Fragment（多 element 子/Text 子/单 element 子 vs 既有 element/doctype 位序）、Element（vs 既有 element/插入点 doctype/后有 doctype）、DocumentType（vs 既有 doctype/前有 element/无插入点有 element）三组规则——insertNode 902P→916P（+14）、全量 51576P/3463F/20T→51589P/3449F/21T 净 +13P **P2F=0 纯增**（F2P 14 全在 insertNode）、native 51589P 逐计数一致（flips 仅 2 既存 flaky）、surround/delete/extract/clone 零扰动、engine 2357 单测全绿、fmt/clippy 干净；make test 1F 为 XOpenDisplayFailed 环境项 run-rules §10。R218：12,x 新溢出点 + insertNode ~925F 重新聚类**
**上轮**: R216 — **M4：iframe doc 的 docEl 入 doc 树（25,x setup 前置解锁）——R216(a) insertNode 902P 基线聚类（Maximum call stack 72F / null-nodeType 66F / HRE 94F）：25,x 根因 = iframe doc 的 childNodes 为空使 setEnd(document,1) 抛 IndexSizeError（setup unexpectedException 整簇）；12,x = docEl.parentNode null 使 upwalk 链断（furthestAncestor 错位）。修：docEl.parentNode=doc + childNodes.push + children 补（doctype 入首位实测 -55 回退——保持 R209 getter-only）；全量 51575P→51576P 净 +1P 中性但结构正确（F2P 1 / P2F 1 既存 flake 家族）、surround/delete/extract/clone 全不变零扰动、native 51575P（flips 仅 1 既存 flaky）、engine 2355 单测全绿、fmt/clippy 干净；make test 1F 为 XOpenDisplayFailed 环境项 run-rules §10。R217：25,x DOM 比较层（Document 子位置规则）+ 12,x 新溢出点**
**上轮**: R215 — **M4：insertNode 的 ensure-pre-insertion validity 前置——R214 的「restoreIframe 累积循环」假设被 300 轮循环复刻推翻（clean），真根因是** parentNode 环**：8,9 形态 host 旧版 splitText 路径无校验直接 insertBefore，把插入目标自身的祖先（detachedDiv）插进其后代（para）成 P→DIV→P→DIV 环（upwalk 探针 101 hops + chain dump 实证），后续 sim 的 isInclusiveAncestor 上行 walk 栈溢出——「Maximum call stack」的真相。修：insertNode 校验四件（parent 类型 / **inclusive ancestor 环检测** / Text 入 Document / Doctype 入非 Document，splitText 变更前执行）+ surroundContents 叶子路径经 _r215NoValidate 帧标志保 R212 先变更后抛序——insertNode 628P→**902P（+274）**、surround 829→865、extract 86→98、clone 137→149、全量 51302P/3737F/20T→**51575P/3463F/21T 净 +273P P2F=0 纯增**、native 51576P（flips 仅 1 既存 flaky）、engine 2354 单测全绿、fmt/clippy 干净；make test 1F 为 XOpenDisplayFailed 环境项 run-rules §10**
**上轮**: R214 — **M4：iframe documentElement 结构修复两件——① docEl 的 ownerDocument 指源 doc（mEl 解析产物 + R177 合成 html 两形态都缺；common.js rangeFromEndpoints 经 ownerDocument(docEl).createRange() 建域内 Range——insertNode 12,x 138F 根因）② 无显式 `<html>` 标签时不再落通用配对 regex（首个「开-闭对」= `<title>…</title>` 使 docEl=TITLE、refDoc 克隆链跟着 TITLE 化——真浏览器对无显式 html 的 HTML 文档合成 `<html>` 根；HTML/XHTML kind 落 R177 合成路径、XML kind 保持通用 regex）——全量 51243P/3795F/21T→51302P/3737F/20T 净 +59P（surround +36 / clone +12 / extract +12 / delete +6-8 形态重分布）、native 51301P（flips 仅 1 既存 flaky）、engine 2353 单测全绿、fmt/clippy 干净；make test 1F 为 XOpenDisplayFailed 环境项 run-rules §10。R215 靶点：insertNode restoreIframe 累积循环（~290 轮 Maximum call stack）+ delete 8 P2F 复查 + insertNode HRE 336F 簇**
**上轮**: R213 — **M4：extract 收缩偏移修正 + deleteContents CharData 删侧分支——6,x positionTests 残余根因（忠实复刻 testharness 探针：common.js 真源 + mySurroundContents 真函数注入，offsets A=0,1 E=1,2 一行暴露——host extract 收缩到 (p5, si) 而 spec「Set new offset to one plus the index of reference node」= (p5, si+1)，后续 insertNode 落位前/后一位）+ deleteContents 删侧三段（start 尾段 deleteData + contained 子逆序 remove + end 头段 deleteData + collapse 到 (parent, si+1)）——surround 817P→829P（+12 P2F=0）+ delete 45P→58P（+13）+ extract 85→86、全量 51216P/3820F/21T→51243P/3795F/21T 净 +27P **P2F=0 纯增**、native 51244P（flips 仅 1 既存 flaky）、engine 2352 单测全绿（R211/R212 旧断言按 spec 纠正）、fmt/clippy 干净；make test 1F 为 XOpenDisplayFailed 环境项 run-rules §10。方法论：手工模拟与 host 一致但真 harness 分歧时，注入真源函数跑完整流程**
**上轮**: R212 — **M4：surroundContents CharData 路径闭合——R211 首版 -48 的三根因定位修复（① 步骤 1 漏检 nodeType 11：docfrag newParent 旧版走到 CharData 路径实际变更树 → ,20 族 48F；② 工厂元素 appendChild 无 fragment 展平：frag 子全丢（spec dom-node-append-child「append fragment children then clear」）；③ 步骤 3「While newParent has children, remove its first child」缺失：wrapped 元素残留原文本）+ CDATA cloneNode nt=4 分支成对 land——探针双 iframe 对照实证树逐字节一致（isEqualNode=true + walk-identical）；surround 823P→817P（,20 全转绿 + 6,x DOM 转绿，剩 6,x positionTests 深比较 R213）、cloneContents 127→137、extractContents 81→85、全量 51208P/3828F/21T→51216P/3820F/21T 净 +8P、native 51216P 逐计数一致（flips 仅 2 既存 flaky）、engine 2351 单测全绿、fmt/clippy 干净；make test 1F 为 XOpenDisplayFailed 环境项 run-rules §10**
**上轮**: R211 — **M4：extractContents 的 CharacterData 区间分支（spec `dom-range-extract-contents` 三段算法——start 容器尾切片克隆 + contained 子本体移动 + end 容器头切片克隆，原树 deleteData；同节点取中段；range 收缩到 (parent, si)）——extractContents 61P→81P + cloneContents 127P→137P 连带、全量 51188P/3847F/20T→51208P/3828F/21T 净 +20P（F2P 24 全在 extractContents）、native 51208P 逐计数一致（flips 仅 2 既存 flaky）、engine 2350 单测全绿、fmt/clippy 干净；make test 1F 为 XOpenDisplayFailed 环境项 run-rules §10。评估回退两件记 R212（两侧对称性第三例）：CDATA cloneNode（单独正确但 host surround CharData 路径未完 → 6,x -34）、surround CharData 路径首版（docfrag 误伤 + frag 末子丢失 -48）**
**上轮**: R210 — **M4：surroundContents 步骤 1/2 校验序（spec `dom-range-surroundcontents` 步骤 2 的非 Text 部分包含 → InvalidStateError——cac 子树 DFS「start 容器祖先 XOR end 容器祖先」判定；步骤 1 的 newParent Doc/Doctype → InvalidNodeTypeError 前移到函数头恢复 spec 校验序）——surround 733P/1107F→823P/1017F（P2F=0/F2P=90 纯增，20,x 跨容器 115F 簇收口）、全量 51096P/3937F/22T→51188P/3847F/20T 净 +92P/-90F、native 51187P/3847F/21T flips 仅 1 既存 flaky、engine 2349 单测全绿、fmt/clippy 干净；make test 1F 为 XOpenDisplayFailed 环境项 run-rules §10。评估回退两件记 R211：head/docEl/body 三站点方法面（-9 非纯增）、CDATA cloneNode（单独正确但 host 缺跨容器 extract 致两侧树分歧 -34——与 extract 同切片）**
**上轮**: R209 — **M4：iframe/detached 工厂节点方法面 + surround/insert 叶子 newParent 异常链（探针直驱 win.run() 实测 22 testNode 形态——iframe createTextNode/createElement 产物缺 cDP/hCN/cloneNode/substringData/splitText/insertBefore、fragment 缺 cloneNode/isEqualNode、PI 缺 CharacterData 面、iframe doc 无 doctype；host surroundContents 对叶子 newParent 吞错不抛 HRE/insertNode 无 Text splitText 路径；DOMException 实例缺 legacy code 常量使 getDomExceptionName 崩）——七件修复（part05 工厂双面+doctype/parse 子统一经工厂、part03 fragment+PI+叶子 mutation HRE 抛（removeChild 先 NotFound 校验）、part06 surround 叶子先变更后抛+insertNode split 路径+自引用 HRE、part01b+native dom_exception.rs 实例挂单个 legacy 常量）——surround 645P/1195F→733P/1107F + insert 487P→628P、全量 50796P/4238F/21T→51096P/3937F/22T 净 +300P/-301F、native 51098P fail 集逐条一致（2 flips 为既存 flaky Timeout）、engine 2348 单测全绿、fmt/clippy 干净；make test 1F 为 XOpenDisplayFailed 环境项 run-rules §10（R203-R208 同款）**
**上轮**: R208 — **M4：iframe doc 查询缓存的同键失效（restoreIframe r1+ 轮 919F 簇根因——`_zwQWrapCache` 键 tag\x1fid\x1fouterHTML 撞车，移除后缓存残留使同键查询返已移除节点；三处 removeChild 失效点）——surround 0P/1840F→645P/1195F + insert 1P/1839F→486P/1354F、全量 49541P/5004F/22T→50796P/4238F/21T 净 +1255P/-766F 零真回归（逐 subtest 转移比对 Pass→Fail=0，364 新失败行全为新暴露 subtest——mega-case 旧 r0 崩只报 3-4 行现跑深轮 cloneContents 4→187）、native fail 集逐条一致、engine 2347 单测全绿、fmt/clippy 干净；make test 3F 为 SW 流已知红灯（stash 实证干净 main 同败）+ 1F XOpenDisplayFailed 环境项 run-rules §10**
**上轮**: R207 — **M4：iframe 子文档层四件（工厂元素 append/prepend/replaceChildren + textContent replace-all accessor + docEl 根优先 `<html>`（修 `<title>` 误命中）+ docEl 可变面）——restoreIframe 首轮端到端打通（window.testRange 产出）、全量 49534P/5005F→49543P/5004F 净 +9P 零新增失败行、native 逐行相同、engine 2346 单测全绿、fmt/clippy 干净；make test 1F 为 XOpenDisplayFailed 环境既存 run-rules §10）**：R206 通道暴露的层——① iframe 工厂元素缺 append 族（impl 路径有、工厂只有 appendChild——common.js `paras[5].append` 崩）；② textContent 旧 plain 字段不建 Text 子→ firstChild 恒 null → eval 喂 null 给 ownerDocument；③ 根 regex 首命中 `<title>` 使 documentElement.tagName=TITLE（html 优先 + 组序归一）；④ docEl 无 cloneNode/appendChild 面。**两轮回归全量逐行 diff 抓回**（新节点形态缺 hasChildNodes → cloneContents walk 崩 8 行；再缺 isEqualNode → cursor 断言）——land 前补齐。**R208 精确靶点已定位**：restoreIframe 第二轮起 setupRangeTests 的 `querySelector('#test')` 服务于 stale markup parse 树（live 组装树已前进，返回 parentNode=null 元素）。commit `5140516e2` 已 push。
**上轮**: R206 — **M4 能力面：iframe 子文档脚本执行通道（`_zwRunIframeScripts`——外链/inline 提取 + 合并作用域单 Function + per-part try/catch 隔离 + 顶层声明导出到 win + body onload）——套件计数持平 49534P/5005F（同文件内失败形态重分布、逐行 diff 零新增 vs R205）、engine 2345 单测全绿、fmt/clippy 干净**：src iframe 旧只建 doc/win 不跑脚本——Range-surroundContents/insertNode 的 `contentWindow.setupRangeTests is not a function` ×920 双簇根因。验证链（探针逐步）：单元 stub fetch 探针实证 `setupRangeTests`/`run` 成为 contentWindow 函数属性；Runner 侧 is-not-a-function 簇**消除**——失败下移一层（restoreIframe contentDocument 失同步 ×919 + detached doc 元素 append/cloneNode 缺口——R207 输入）。过程坑：fetch wire 分隔符（Rust 字面量 `\x1f` 非转义）+ wire 布局（无空字段）+ 合并 Function 顶层 throw 截断导出后缀（per-part try/catch 解决）。commit `7f36dbbe0` 已 push。
**上轮**: R205 — **M4：Range 点查询三方法边界点比较重写（isPointInRange 跨容器树序 + intersectsNode (parent,i) 区间交 + comparePoint -1/0/1 + doctype 抛错）——全量 47049P/7489F→49534P/5005F 净 +2485P 零新增失败文件 + intersectsNode/shadow 双文件全绿、native 49534P fail 集逐行相同、engine 2345 单测全绿、fmt/clippy 干净；make test 1F 为 XOpenDisplayFailed 环境既存 run-rules §10）**：三方法统一复用 R203 `_zwRangeBpAfter`——isPointInRange 旧跨容器 best-effort true→树序判定（830→34F）；intersectsNode 旧 node 父索引直接比 range offset（坐标系错位）→node 占据 [(parent,i),(parent,i+1)] 的区间交判定（186→1F）；comparePoint 旧 cDP 方向位反→树序-1/0/1（496→124F）。**两个 spec 纠正**：① intersectsNode 的 collapsed 前置 false 移除（现行 spec 无此步——collapsed range 仍与边界点所在节点相交，R178 首版自加步骤与 WPT 186F 全冲突）；② doctype 抛错步骤序（root 不同先返 false、同 root 才抛 InvalidNodeTypeError——首版 throw 前置使 foreign doctype 也抛，214F 回归当轮抓回重排）。**剩余记档**：surroundContents/insertNode 各 1840F（iframe.contentWindow.setupRangeTests 缺——srcdoc iframe 子文档域，R206 输入）。commit `0242f8ffa` 已 push。
**上轮**: R204 — **M4 大切片：compareBoundaryPoints 四件（WebIDL how 转换前置 + Range 常量 + 方向位反转 + detached createRange 递归）——该簇 9313F→601F（文件 1P→8713P）、全量 35453P/19085F→47049P/7489F 净 +11596P 零新增失败文件 + 4 文件转绿（Range-cloneRange/collapse/commonAncestorContainer/mutations-appendData）、native 47050P fail 集逐行相同、engine 2344 单测全绿、fmt/clippy 干净；make test 1F 为 XOpenDisplayFailed 环境既存 run-rules §10）**：① how 的 WebIDL unsigned short 转换前置（参数序先于 range 类型检查；ToNumber→NaN/±0/±∞→+0，mod 2^16 负回绕；非 0-3 抛 NotSupportedError——旧 `|0` 截断 NaN 误合法/-1 不回绕 + 参数序颠倒）；② **Range how 常量缺失**（START_TO_START..END_TO_START 整组没有——WPT 以四常量判合法性使所有 how 含合法 0-3 被期望抛错，4728F 簇）；③ 跨容器方向位反转修正（cDP 位以接收者为参照：&4 FOLLOWING→-1 / &2 PRECEDING→+1，旧两支写反）；④ **detached doc 的 createRange 自递归**——`_makeDetachedDocument` 的 doc 对象无 own createRange（body 对象有同名），查找落 Document.prototype R179 转发器，`this[name].apply` 再解析到自身 → 无限递归 Maximum call stack（'Creating context/argument range threw' 4041F 整簇根因）。探针链：how 形态对照全对排除 how → subtest 名锁定foreign range 建立 → 单步复现 → stash-call + own:false 定位转发器自递归。commit `441e97a24` 已 push。
**上轮**: R203 — **M4 大切片：Range setStart/setEnd crossing 重设 + 边界点序比较（Range-set 主簇 6767F 语义根因修复、全量 30144P/24395F→35453P/19085F 净 +5309P/-5310F 零新增失败文件、native 35454P fail 集逐字节相同、engine 2343 单测 + lit/vue e2e 全绿、fmt/clippy 干净；make test 1F 为 XOpenDisplayFailed 环境既存 run-rules §10）**：spec range-set-start 步骤 3 / set-end 镜像——新 start 在当前 end 之后（边界点比较含跨文档）时 end 一并重设，setEnd 前穿对称；旧版只写自身侧。新 `_zwRangeBpAfter` 三段判定（同容器 offset / 祖先-后代 child-索引对 offset / 深度感知双 climb 到共同父定序；跨文档恒 after）。**算法坑单测当场抓回**：首版逐父同步 walk 在深浅不一时（t1 in p0 深 2 层 vs p2 深 1 层）isAncestor 短路误序——修正深度对齐后共同 climb，11 规范形态 standalone 全绿。Range-set 4153→9297P + collapse +54P。**剩余簇记档**：compareBoundaryPoints 9313F（how 参数 WebIDL enum 抛错 5272 + context range 建立异常 4041）、foreign-tree Maximum-call-stack ~1009（Range-selectNode 整文件死于此）。identity 纯形态已大幅收缩（主因是 crossing 语义而非 wrapper 基建）。commit `9ebf34754` 已 push。
**上轮**: R202 — **M4 轻量：foreign/detached doc 文本节点 CharacterData 方法面（五方法 + data/nodeValue 可写 accessor 三接线点；subtest 计数持平 30144P/24395F/20T 双路径 fail 集逐字节相同零新增、engine 2342 单测全绿、fmt/clippy 干净；make test 1F 为 XOpenDisplayFailed 环境既存 run-rules §10）**：R201 解锁的 24kF 中 foreign-doc 簇的能力面——`foreignDoc.createTextNode` 产物旧 "insertData is not a function" 立即死亡，现五方法（appendData/insertData/deleteData/replaceData/substringData）+ setter 全通（探针验证）。**本地变更语义**（R48 no-parentSel 快照分支同款——foreign 树 JS 侧持有）。**两坑当轮抓回**：① setter 自递归（首版 _write 赋刚转 accessor 的 data/nodeValue 字段 → Maximum call stack；改只写 __nv/textContent）；② detached doc 的 createComment 是独立内联对象非 _zwMComment 工厂（单测抓回同款 attach + CDATA 同块）。**计数持平语义**：解锁子测试从立即死亡前移到下一断言（identity/offset）再失败——mega 簇主阻塞仍是 identity 域（R203 输入），本切片是其前置能力面。commit `d7b6c4967` 已 push。
**上轮**: R201 — **M4 里程碑轮：strict var accessor 导出 + V8 脚本看门狗——dom/ranges mega-case 家族解锁（12 文件 "xxxTests is not defined" 整族 0-subtest → 真实混合结果、基线大迁移 9767P/100F→30142P/24395F（native 30144P fail 清单逐行 IDENTICAL ±2 边缘 Timeout）、fail 文件集 diff 零新增 + 5 文件转绿、engine 2338 单测 + lit/vue e2e 全绿、fmt/clippy 双矩阵干净；make test 1F 为 XOpenDisplayFailed 环境问题 clean HEAD 复现 run-rules §10）**：① **var accessor 转发导出**（script_gen，R147/R198 第三族）——strict eval 困住顶层 var（Range-mutations.js 12 张测试表 + common.js 七行多声明 var）；与 const/let 值快照不同，var 有跨脚本再赋值流（setupRangeTests harness 回调赋值→后续脚本读）→ 每名 defineProperty accessor get/set 闭包双向转发 eval 绑定；多行 var 续行收集 + 末名剥分号（首版漏尾名单测抓回）。**strict 判定门（lit 回归教训）**：非 strict eval 的 var 本就泄漏 globalThis，accessor 重定义使 getter 解析到自身 → 无限递归 Maximum call stack（lit e2e 当场炸，探针 bisect 到 createElement 行）→ 仅源首'use strict' 指令启用，lit 六测试恢复全绿。② **V8 脚本看门狗**——解锁后 insertBefore 在 JS 层死循环（common.js indexOf 的 childNodes 视图失同步自旋，R51b 家族 latent bug；pre-mutation 视图探针一致、自旋在中段 mutation 后）；case 级 CASE_TIMEOUT 拦不到同步脚本自旋（3600s guard 整轮杀掉实证）→ WebViewConfig.script_timeout_ms（默认 0 生产不变）经 set_timeout_ms 接 V8 terminate_execution；runner 设 90s——自旋用例单用例 Fail 收场；Makefile native 入口 TIME_LIMIT 透传。**基线语义**：+20kP = 解锁 mega-case 真实 subtest；+24kF = 真实缺口两大簇——identity 域（Range-set 6767F / compareBoundaryPoints 9313F 的 expected-var-对象 vs got-wrapper identity 形态）+ foreign-doc CharacterData 方法面；全部落在既存失败文件内（零新增红文件）。commit `c4cbea15c` 已 push。
**上轮**: R200 — **M4 轻件：属性选择器大小写标志 `[attr=value i]`（case-insensitive 双 subtest 转绿、全量 9767P/102F→9767P/100F 零新增、native 逐文件 IDENTICAL、make test 全绿、fmt/clippy 双矩阵干净）**：CSS Selectors L4 属性值尾标志（空白 + i/I=不敏感 / s/S=显式敏感）——旧版 flag 混进值本体（`[name*=user i]` 的值解析成 "user i"）恒 miss。修三件：`AttributeSelector` 增 `ci` 字段 + `strip_attr_case_flag`（去引号/unescape 后剥尾标志——裸 i 值无分隔空白不算标志）+ 六运算符（Exact/Includes/Prefix/Suffix/Substring/DashMatch）ci 时双侧 ASCII 小写。**过程坑**：首版 `len-1` 字节切分在多字节值（é）上 panic——全量跑当场抓回，改 char 边界安全剥尾。**本轮评估搁置**：MO-document 3F（parser insertion 记录需 live parse stream——html-compat 深域）/ MO-inner-outer 2F（addedNodes wrapper identity——L2 identity 统一域家族）。commit `7f524865c` 已 push。
**上轮**: R199 — **M4 轻件：选择器输入预处理 NUL→U+FFFD（css-syntax §5.3.3，escapes NUL 双 subtest 转绿、全量 9765P/104F→9767P/102F 净 +2P/-2F 零新增、native 逐文件 IDENTICAL、make test 全绿（含上轮 SW flake 本轮亦绿）、fmt/clippy 双矩阵干净）**：WPT `testMatched("\u{fffd}", "#\u{0}")` 的裸 NUL 选择器旧版三层各自吞——① host 词法（NUL 非 ident 首字符 → 整串判非法抛 SyntaxError）；② shim handle 查询（`_readCompoundToken` 把 NUL 当普通 ident 字符读出，比对 FFFD id 恒 miss）；③ 工厂匹配器（R181 正则 `[a-zA-Z_][\w-]*` 把 FFFD 非 ASCII 拒掉）。修三件同步归一：zero_dom `preprocess_selector` 前置三解析入口（全消费方继承）+ part05 `_parseSelectorListOf` 输入归一 + R181 ident 域放宽到 CSS ident 域。探针方法论：逐步 probe 排除 id 侧序列化假设（fffdres:child vs nulres:null 对比锁定选择器侧）。**深项记档两条**：escapes 剩 2F lone-surrogate id 在 HTML 序列化 wire 塌缩为 FFFD（需 surrogate-preserving wire，超出序列化域）；ParentNode-querySelector-scope 2F 的 `:scope` query-root 语义需 scope NodeId 贯穿 matches 链（lang_dir.rs 注释明记 follow-up，中等结构独立切片）。commit `991e17f5d` 已 push。
**上轮**: R198 — **M4 轻件：NodeList length 劫持语义 + strict classic const/let 跨脚本导出（NodeList-static-length-getter-tampered 六用例 0P/6F→6P/6 全 100% + NodeList-Iterable own-keys 连带、全量 9758P/111F→9765P/104F 净 +7P/-7F 零新增、native 9765P/104F per-file 零差异、fmt/clippy 双矩阵干净；make test 1F 为 SW 流既存 flake run-rules §10 clean HEAD 同败实证）**：① **fixture 层**——fetch_dir_html 只列子目录顶层不递归 support/，六用例引用的 support/NodeList-static-length-tampered.js 缺失整簇 script fetch failed；fetch 脚本补显式抓取。② **strict classic 顶层 const/let 跨 `<script>` 不可见**（script_gen，R147 function 导出的 const/let 扩展）——两个坑：等号前空白（`const x = ...` 首版 `starts_with('=')` 全 miss，真实 WPT 源 `=` 前有空格）；**minified bundle 的 IIFE 内零缩进 const**（lit bundle `var ns=(function(){` 换行后零缩进 `const t=globalThis,...`——行首锚定无法区分真顶层与 IIFE 作用域，裸 `globalThis.t=t` 在 eval 顶层 ReferenceError **中止整个 eval**，lit e2e 六测试 NO-REPORT/EXEC-ERR 由 make test 当场抓回）→ 每名 `try{}catch{}` 独立包裹（作用域外名字静默跳过）。③ **静态 NodeList length 劫持三形态**（真浏览器 length 是原型属性——own defineProperty / setPrototypeOf 换原型 / NodeList.prototype 全局篡改都改读取结果；Array 承载 own+non-configurable length ① 抛 ②③ 被遮蔽）——_zwMakeCollection(htmlCollection=false) 升级 **Proxy 承载 plain target**：length 解析序 expando→原型链 getter→计数，原型链接线 NodeList.prototype→Array.prototype（indexOf/map/for-of 经 receiver 全兼容）；ownKeys 滤内部印记（NodeList-Iterable Object.keys 回归同轮修）；Array.isArray false 由 NodeList Symbol.hasInstance 补认 `__zwNL` 印记补偿（R159 instanceof 断言保持）。commit `0c2ba1f9a` 已 push（rebase 吸收并行 SW 流两提交后落位）。
**上轮**: R197 — **M4 轻件：两簇轻修（Range-stringifier 3P/2F→5P/5 + DocumentFragment-constructor 0P/2F→2P/2 双簇全 100%、全量 9753P/115F→9757P/111F 净 +4P/-4F 零新增、native 逐文件一致（两处 diff 均为既存 harness flake 互换）、make test 66 套件全绿零失败、fmt/clippy 双矩阵干净）**：① **Range.toString 跨容器 spec 精确化**（`range-stringification`）——旧版取 commonAncestor 整子树文本把端点外兄弟（含注入 script 文本）拼进结果；新实现按 spec 三段式：start 容器 Text 起始切片 + (start,end) 区间 contained Text 全收（CAC 子树 DFS + 边界 path 过滤）+ end 容器 Text 尾切片。**关键坑**：路径比较首版用 `join(',')` 字典序——`"10,0" < "2"` 字典序 true（'1'<'2'）使 ≥10 子索引的大索引子树被误收（WPT got 含 pIdx=10 注入 script 文本实证）；改数值逐段比较 `_r197PathCmp` + `_r197HasPrefix`（后代包含判定）。probe 方法论：replicate 用例状态序列最小复现（首版简化 probe 竟 Pass——setStart/setEnd 历史影响后续形态）+ texts pIdx dump 定位误收节点。② **`new DocumentFragment()` 实例化**（spec `dom-documentfragment-documentfragment`）——旧占位空函数返裸 `{}`（ownerDocument undefined + 无 appendChild）；改委托 `document.createDocumentFragment()`（handle 容器全 mutation/遍历面）。commit `ebdf58c50` 已 push（rebase 吸收并行流 `ec983df81` 后落位）。
**上轮**: R196 — **M4 轻件：三簇轻修（Text-splitText 4P/2F→6P/6 + StaticRange-constructor 15P/2F→17P/17 + append/prepend-on-Document 6P/4F→10P/10 三簇全 100%、全量 9746P/123F→9753P/115F 净 +7P/-8F、native 逐文件一致、fmt/clippy 双矩阵干净；make test 1F×3 轮均为既存 SW/net flake run-rules §10）**：① **Text.splitText spec 化**（`dom-text-splittext`）——offset>length 抛 IndexSizeError（旧 clamp 不抛）+ parent 非空时新节点插到原节点 nextSibling 位（旧返 detached 节点使 `new_text.parentNode === parent` 断言 fail；host mutation 走 InsertBeforeByHandleHandle + registry 记账 + MO childList record）；② **StaticRange 构造器校验**（`dom-staticrange-staticrange`）——DocumentType/Attr 容器（nodeType 10/2）抛 InvalidNodeTypeError + 必填成员缺失（undefined 容器/offset）抛 TypeError（WebIDL required 成员——`| 0` 归 0 会静默吞掉 offset 缺失）；③ **append/prepend 整批校验**（`dom-parentnode-append` 的 convert nodes into a node 语义）——字符串参数（变 Text 插 doc）与多 element 参数（frag 多 element）在 doc 目标上抛 HierarchyRequestError（旧逐参校验对字符串 continue 跳过 + 多参数 frag 形态不可见）；part05 `_zwValidatePreInsert`（proxy 路径）+ part03 `_r117Install`（detached doc 路径）双面同补。**流程注记**：insertBefore-iframe-crash 全量跑 1 次 Timeout（单跑 3/3 绿 + native 侧对偶 flake ParentNode-querySelector-All-content 单跑绿——testdriver Actions 时序类 harness flake 非回归）。commit `1e673accd` 已 push（rebase 吸收并行 SW 流 `b0aff9ede` 后落位）。
**上轮**: R195 — **M4 轻件：容器 registry 摘除两件（Range-delete/extractContents-in-ShadowRoot 单跑各 0F/3F→3/3、全量 9742P/127F→9745P/123F 净 +3P/-4F 零新增、native 逐文件一致、fmt/clippy 干净；make test SW 1F 既存 run-rules §10）**：① **removeChild 的 plain 子分支**——shadow/fragment 容器 innerHTML 解析产物 `_zwMEl`（无 handle 无 sel）旧两分支均不命中静默穿透——补容器 registry 剔除 + parentNode null + childList record；② **`_unrecordHandleChild` 改引用匹配**——旧 early-return 要求 `__zwHandle` + filter 按 handle 比对使 plain 子（handle undefined）剔除恒 no-op；改对象引用优先 + handle 兜底（proxy 形态引用比对更精确）。**残留 2F**（delete/extract 各 1——混合套件下 container/offset 交互形态）记深项。commit `c3c58ea2f` 已 push。
**上轮**: R194 —  **M4 轻件：shadow DOM 面三簇（svg-template 1P/2F→3P/3 + attach-shadow-realm 2P/3F→5P/5 + Range-cloneContents-shadow 连带 3F→0、全量 9734P/135F→9742P/127F 净 +8P/-8F 零新增、native 逐文件一致、make test 66 套件 exit=0、fmt/clippy 干净）**：① template.content.querySelector（子树 DFS 简单选择器）；② **ShadowRoot 身份三形态**——构造器占位（DocumentFragment 子链）+ proxy getPrototypeOf shadow handle 分支 + R179 工厂 shadow setPrototypeOf（nodeName 对齐）+ **Element.prototype.attachShadow 真方法**（defineProperty 不可枚举——`inner.Element.prototype.attachShadow.call(host)` 第三变体；实例读仍走 get trap 优先零行为变化）；③ Range `_coveredChildren` 放行 fragment/shadow 容器（nodeType 11——cloneContents 连带转绿）。**两次即时回归**：win.Element 子类替换致 createElement 78F（回退改 prototype 安全路径）；可枚举原型方法污染 expando 枚举单测（改 defineProperty 不可枚举）——**替换转发构造器是高危操作**的实证。**R195 深项**：delete/extract 对容器 registry 的摘除（shadow innerHTML 子 remove() 不摘 `_handleChildren`）。commit `724f32a04` 已 push。
**上轮**: R193 —  **M4 轻件：两簇轻修（DocumentFragment-getElementById 2P/3F→5P/5 + createProcessingInstruction 9P/3F→12P/12 全 100%、全量 9727P/141F→9734P/135F 净 +7P/-6F 零新增、native 逐文件一致、make test 66 套件 exit=0、fmt/clippy 干净）**：① **DocumentFragment.prototype.getElementById**（spec dom-nonelementparentnode——DFS 首命中 + 空串 id 恒 null；handle 容器分支补空串 null + fragment proxy get trap 转发 + template.content plain 视图 delegate——三形态全覆盖）；② **PI target 的 XML Name 严格产生式**（NameStartChar/NameChar 全集排除 ×/÷、· 中位合法——WPT ·A/×A/A× 抛 + valid A·A 不误伤；createElement 的 HTML 宽容名不动）。**配套单测修正**：part20 r135 单测的 `A\x0B` PI 期望 valid → THROW:InvalidCharacterError（\x0B 非 XML NameChar——浏览器同抛；旧期望过宽）。commit `bd38533a0` 已 push。
**上轮**: R192 —  **M4 轻件：Top 簇轻修四件（DocumentType-remove 0P/4F→4P/4 + ParentNode-append 21P/4F→25P/25 + prepend -1F + Document-adoptNode 0P/4F→4P/4、全量 9715P/154F→9727P/141F 净 +12P/-13F 零新增、native 逐文件一致、fmt/clippy 干净；make test SW 2F 为 service-workers 流既存 run-rules §10）**：① doctype.remove()（ChildNode mixin——摘除 + parentNode 置 null）；② append/prepend 参数内重复节点后位胜去重（`_appendVariadic` lastIndex 表——spec insert 收集语义，append(x,y,x) → [y,x]）；③ doc 收 doctype 的 pre-insert step 6 校验（`_zwValidatePreInsert` + `_r117Validate` 双校验器同补——doc 已有 doctype/element 时 HRE）；④ adoptNode spec 语义化（主文档 + detached doc——Document 参数 NotSupportedError；摘除（doctype 的 parentNode defineProperty 兜底——getter-only accessor 赋值静默 no-op）；子树 ownerDocument 传播扩 sel-based（`__zwAdoptDocBySel` 注册表 + ownerDocument trap sel 分支——WPT "x<" 解析产物形态））。commit `0ced7f327` 已 push。
**上轮**: R191 —  **M4 轻件：cross-realm adopt 传播三层（Range-adopt-test 3P/1F→4P/4 + node-realm-mixed "Element parsed into an adopted container" 转绿 13→14P + Node-replaceChild 58P 保持（中途回归即时修复）、全量 9712P/156F→9715P/154F 净 +3P/-2F 零新增、native 9715P/154F 逐文件一致、fmt/clippy 干净；make test SW 1F 为 service-workers 流既存 run-rules §10）**：① **range adopt-collapse**——selectNode 快照 `_mode.ownerDoc`，start/endOffset getter 比对 tracked node 当前 ownerDocument，变更即 collapse 0（spec live range retarget）；② **doc 级/iframe body appendChild 的 adopt 子树传播**——`__zwAdoptDocByHandle` 全局注册表（handle proxy 属性读写经 trap 实例 defineProperty 不可达）+ plain/`_zwMEl` 递归 defineProperty getter（spec concept-node-adopt 子树 ownerDocument 重指）；③ **doc replaceChild adopt 改 defineProperty**——旧赋值对 accessor 形态 ownerDocument 静默 no-op（doctype2 停留 doc2 探针实证）。**node-realm 剩 2F**：customElements 单 registry 近似（R181 已记深域）+ frame-removal 可达性。commit `31f8aa8b0` 已 push。
**上轮**: R190 —  **M4 轻件：NS 属性元数据域三层收口（Node-cloneNode-svg 1P/3F→4P/4 + Attr-prefix 4P/2F→6P/6 双 100%、全量 9708P/161F→9712P/156F 净 +4P/-5F 零回归、native 9713P/156F 逐文件一致、make test 66 套件 exit=0、fmt/clippy 干净）**：三层——① **host 限定名还原**（zero-dom node.rs）——html5ever 解析 `xmlns:xlink`/`xlink:href` 拆 prefix+local，旧 `attribute_names()` 只返 local 丢前缀且 get/set/has/remove 只比 local——新增 `attr_full_name_eq` 双形态匹配 + `attribute_names` 输出 prefix:local；② **deep clone 非 HTML ns 子树修正**（part04 cloneNode）——re-parse 产物是 HTML 语义（大写 + XHTML ns），`_r190FixNs` 递归补源 ns + tagName 小写 + prefix/localName 字段 + 带前缀属性 NS 印记（`_zwAttrsRaw` 访问口 + `_zwMAttrNS` 透传）；③ **解析产物带前缀属性 NS 推导**（共享 `_zwDeriveAttrNS`）——已知 prefix 映射 + 祖先链非 HTML ns 判定（svg/math 子继承根 ns；HTML 元素 "xml:lang" 整 local 不拆）；attrObj 与 getAttributeNodeNS fallback 两处消费；**局部推导专用**不进通用 getter（R185 教训）。**流程教训**：`make testharness-dom-native` 与 `make test` 并行竞态互删 target/test-guard 链接产物（rust-lld cannot open rcgu.o 假性全崩）——须串行。commit `33beff37d` 已 push。
**上轮**: R189 — **M4 轻件两切片：DOMParser 可变面 + textContent setter MO 记录（Node-normalize 3P/1F→4P/4 + StaticRange CDATA 容器连带 + MutationObserver-textContent 2P/2F+Timeout→4P/4 全 100%、全量 9703P/165F→9708P/161F 净 +5P/-4F 零回归、native 逐文件一致、make test 66 套件 exit=0、fmt/clippy 干净）**：**切片一（DOMParser 可变面）**——① `_zwParsedDoc` 补 createTextNode/createComment/createCDATASection 三工厂（轻量节点 + CDATA spec 校验：HTML 文档 NotSupportedError / ']]>' InvalidCharacterError）；② `_zwParseEl.prototype.normalize`（plain 容器版合并——exclusive-Text 兜接 + 空 Text 移除 + 递归）；③ **isText 判定修正**：PI 视图（nodeType 7 带 __zwIsText 印记）非 Text 不并（首版误合并 childNodes 9→7 探针实证）。**切片二（textContent MO 记录三面）**——① **同值 set 发 record**（handle 元素）——spec string replace all 恒重建；触发条件融合 `_handleChildren` + `_zwLocalChildNodes`（非 fragment 只入后者旧漏）；重建先 unregister 再 register；sel-based 快照元素保持无 record（childList n12 形态）；② **异值 removed 快照融合**——`_childNodeList` 对 handle 元素（sel null）恒返 []，补 registry + 本地注册子；③ **DOMParser 轻量元素可变容器面**（`_zwParsedDoc.createElement` 产物）——appendChild/removeChild/firstChild/lastChild + textContent getter（**含 CDATA data**——descendant text content 收 Text+CDATA+PI，normalize 的 exclusive-Text 才排除）+ setter（replace-all + `__r189:` 键独立派发 record + `__zw_mo_flush_lite` 导出调度 flush）；observe 入口对轻量元素落同款键。**调试方法论**：同值分支崩溃的阶段性插桩定位（stage marker）抓到清理 debug 时误删循环 guard 的回归——插桩清理须逐条 diff 复核。commit `44db2b358` + `124017497` 已 push。
**上轮**: R187 — **M4 轻件：跨 realm 异常上报定向（EventListener-handleEvent-cross-realm 0P/5F→5P/5 + TreeWalker-acceptNode-cross-realm 连带 0P/5F→5P/5 双 100% 双路径、全量 9698P/172F→9703P/165F 净 +5P/-7F 零回归（含删 zz-r186 探针残留 2F）、native 9703P/165F 逐文件一致、fmt/clippy 干净）**：spec「report the exception」按 callback 关联 realm 定向（WebIDL invoke）——error 事件派发到 callback 所属 realm 的 window + TypeError 构造器 identity 对齐。五件——① **realm 印记体系**：iframe win 的 Object 改绑定构造器（BoundObject——new 产物 `_zwRealmWin` 印记）+ Proxy.revocable 包装（proxy 印记 + `__zwRealmOf` Map 注册——revoked 后属性 Get 抛，Map.get 不触 trap 反查创建 realm）。② **report 定向**：`_zwReportListenerError(err, realmWin)` 新增 realmWin 参数——有印记 win 时 ErrorEvent(error=err) 派发到该 win 的 _et_listeners（不走主 window）。③ **realm TypeError 构造**：`_zwRealmError(msg, win)`——按印记 win 构造。④ **call-throw 重包裹**：`_zwWrapCallError`——listener call 抛的 TypeError 按 listener realm 重构造（WPT #3/#5 revoked-callable 场景）。⑤ **TypeError 转发还原（关键回归修复）**：首版 BoundTypeError 子类打破 `assert_throws_js(global.TypeError)` 的 constructor identity 契约（ParentNode-querySelector-All/Element-matches 簇净 +12F 实证）——TypeError 保持转发本体，identity 统一由 report 侧主构造满足。part05 EventTarget dispatch + part03 `_dispatchToListeners` 双路径同步（stamp 读取一律 try——revoked get 抛）。**make test 归因**：zero-webview SW 2F（update_permissions clean HEAD 单跑亦败 + message_import 全量并行 flake）——service-workers 流域非本切片（run-rules §10，R182 同款先例）。**调试方法论沉淀**：include_str! 嵌入的 shim 改动须 `touch js_dom_bridge.rs` 强制重编（cargo 不跟踪 include 依赖）——上轮 R186 探针结果失真根因之一。commit `dce8e88c6` 已 push。
**上轮**: R186 — **M4 轻件：tagName 跨文档大小写重算三变体收口（Element-tagName 3P/3F→6P/6 全 100% 双路径、全量 9694P/173F→9698P/172F 净 +4P/-1F 零回归、native 9698P/172F 逐文件一致、make test 66 套件全绿）**：spec `dom-element-tagname`——tagName = HTML-uppercased qualified name，随 ownerDocument 的 HTML-ness 重算。三件——① **importNode adopt 大写化**（part06）：adoptAll 递归中对 HTML ns（null/XHTML）元素 tagName/nodeName ASCII 大写（XML 小写 div → import 后 DIV）；非 HTML ns（SVG/MathML/自定义）保持大小写敏感。② **clone 快照限定名**（part03）：preserveCase 形态取 nodeName（含 prefix——`foo:div`）而非 localName——旧丢 prefix 使 createDocument("xhtml","foo:div") 变体克隆后 tagName 变 'div'（期望 FOO:DIV）。③ **DOMParser XML 真根**（part02）：XML mimeType 下 documentElement 探测源串真根（strip 注释/PI/doctype 后首个标签，支持 prefix）——旧恒 querySelector('html') 命中 HTML 解析合成包装根得 'HTML'（期望 'div'）；tagName 保源大小写（_zwParseEl 构造恒 toUpperCase 是 HTML 语义，XML 大小写敏感）+ 默认 xmlns 声明读根标签设 namespaceURI。**接续注记**：上轮 429 rate-limit 中断留下 part03/06 未提交 WIP，本轮验证有效（3F→1F）后补第三件（DOMParser XML 真根）收口至 6/6 全绿；依赖面核实零回归（Document-createElement-namespace 25P 不变 / StaticRange 3F 为 HEAD 既存同簇 / cloneNode-doctype DOMParser 变体保持绿）。fmt/clippy 干净。commit `9758153e3` 已 push。
**上轮**: R185 — **M4 轻件：isSameNode 三对象面 + 空类名分词 + cloneNode ns 保留（isSameNode 6P/3F→9P/9 全 100% + empty-set 0P/3F→3P/3 全 100% + cloneNode-svg +1P + Attr-prefix +2P、全量 9688P/180F→9694P/173F 净 +6P/-7F 零回归、native 9694P/173F 逐文件一致、make test 66 套件全绿）**：三件——① isSameNode 补挂三对象（doctype/document/Attr——spec 引用比较）；② getElementsByClassName 空类名 ASCII 分词（空/全空白 → 空 collection 不抛）；③ cloneNode ns 保留（host `__zw_get_ns` 纯快照回调 + sel 源 ns 探测只在 cloneNode 显式调用 + prefix 属性 `_attrNSMeta` 登记）。**关键教训（两轮 A/B 抓回）**：ns 探测进通用 namespaceURI getter 会使 live-collection 过滤行为漂移（getElementsByTagName 的 matches 读 ns → isHtml 小写折叠对 SVG 大写 tagName 永不命中）；而把过滤改双向小写又回归 15F（WPT 实证旧规则正确）——收口 = 探测只在 cloneNode 一次性调用，通用 getter 保持 XHTML 回退。剩深项：cloneNode-svg 3F（NS 属性元数据 + 深度子 ns——host 属性扁平存储的 ns 解析域）/ Attr-prefix 2F 同域。fmt/clippy 双矩阵干净。commit `c1d546198` 已 push。
**上轮**: R184 — **M4 轻件：Node 族三簇收口（constants 2P/4F→6P/6 + normalize 1P/4F→3P/4 + textContent 77P/4F→81P/81 全 100%、全量 9679P/189F→9688P/180F 净 +9P/-9F 零回归、native 9687P/180F 逐文件一致、make test 66 套件全绿一次过）**：三件——① **Node 常量 `in` 可见性**：get trap 返回值正确但 has trap 白名单漏列使 `prop in proxy` 恒 false——补 `*_NODE`/`DOCUMENT_POSITION_*` 模式白名单（值经 Node.prototype 验证）。② **normalize() 实化**（R2853 no-op → 真实现）：exclusive-Text 兜接 + 空 Text 移除 + 递归；**关键教训**：直接写 live childNodes 缓存会被 refresh 覆盖（首版实证）——权威存储是 `_handleChildren` registry，写后 `__zwRefresh` 刷 live 视图；document.normalize() 补挂。③ **textContent spec 纠正**：父元素拼接只认 Text（nodeType 3）——R81 原把 PI/CDATA data 计入与 WPT 期望矛盾（normalize/textContent 的 exclusive-Text 同口径）。剩 1F（normalize #4）需 DOMParser 可变文档面——DOMParser 只读模型深项记档。fmt/clippy 双矩阵干净。commit `7f9ceb88b` 已 push。
**上轮**: R183 — **M4 轻件：Range offset/commonAncestor 活性 getter（CAC-2 2P/4F→6P/6 全 100% + adopt-test 1P/4F→3P/4 + comparePoint-2/attributes 连带转绿、全量 9670P/198F→9679P/189F 净 +9P/-9F 零回归、native 9679P/189F 逐文件一致、make test 66 套件全绿一次过）**：三件——① **offset 活性 getter**（spec range 边界点随树变更更新）：selectNode 形态按追踪节点在容器 childNodes 的**当前** indexOf 现算，移除后 endOffset 收敛到 startOffset 位（WPT adopt-test #1/#3/#4 collapse-on-remove 断言；旧静态数据恒 1）；写路径转 base 槽。② **commonAncestorContainer 活性 getter**——startContainer 祖先链集合，endContainer 上行首个命中（spec 最近共同祖先；旧 _recalc 的 best-effort 直返 startContainer 使跨容器形态全错）。③ **createRange() 初始边界 (document, 0)**（spec dom-document-createrange；CAC-2 "Detached Range" 断言 CAC===document）。剩 1F（adopt-test #2 跨文档 adopt 后立即断言 endOffset===0）为深项——element 未被移除仍 childNodes[0]，真实引擎语义未定，记 cross-realm/adopt 深结构域（与 node-realm 剩 3F 同族）。fmt/clippy 双矩阵干净。commit `a152ae2c9` 已 push。
**上轮**: R182 — **M4 轻件：sel 子形态 insertAdjacentElement 三件收口（1P/5F→6P/6 全 100%、全量 9665P/203F→9670P/198F 净 +5P/-5F 零回归、native 9670P/198F 逐文件一致、engine 2330P + integration 781P 全绿）**：R133 曾记深结构（sel 子 pending overlay 首版并行集漂移）——本轮以既有基建收口三件：① host 新 `DomMutation::InsertAdjacentSelElement`（child 经 find_by_selector——静态页面元素无 handle；insert_nodes_at_position 自带 reparent）+ `__zw_insert_adjacent_sel_element` 回调；② JS 同步可见性——`_zwSelPendingParent` 槽（_zwNodeParent 反链的 sel 对偶）+ 经 _mo_notify 入 _zwPendBucket（对冲复用）+ `_zwSiblingBaseInvalidateAll` 兄弟缓存失效；③ `_zwOverlayPendingChildNodes` 三处扩展——sel 子分支 + **重定位语义**（sel 子跳过 seen 检查：快照内节点同父移动删旧位插新位）+ ref 匹配按**节点 identity**（text ref 形态）。afterend ref = target 旧 nextSibling（自指跳过）。**归因纪律**：make test 1 失败 = zero-webview SW 测试 repeated_registration_changes（clean main 6 跑 5 败实证 flake，20s 超时）——归 service-workers 流非本切片（run-rules §10）。fmt/clippy 双矩阵干净。commit `492992892` 已 push。
**上轮**: R181 — **M4 轻件：工厂元素 innerHTML setter + 创建域/adoption 传播（node-realm 四文件 0P/16F→13P/16、全量 9649P/215F→9665P/203F 净 +16P/-12F 零回归、native 9665P/203F 逐计数一致、make test 66 套件全绿）**：① **根因四层（探针实证）**——iframe/detached 工厂元素与 `_zwMEl` 均无 innerHTML **setter**（readonly getter 赋值静默 no-op → firstChild undefined）；`_zwMText/_zwMComment` 无 ownerDocument 属性；iframe win 缺 HTML 接口构造器（instanceof 报 right-hand not an object）；adoption 只改容器自身不传播子树。② **修五件**——(a) `_zwMEl` + 工厂 el 补 innerHTML setter（解析复 `_zwMBuildBodyTree` + 子 ownerDocument 继承创建域）+ 工厂 el 补 firstChild/lastChild/querySelector(All)；(b) 工厂 el 按 tag 接原型（`__zwHtmlTagIface` 表——instanceof inner.HTMLSpanElement 簇）；(c) iframe win 构造器全表转发（值集 + 基础面 + customElements）；(d) R177 两分支补 **adopt 子树传播**（递归 descendants ownerDocument 重指 + 清创建域印章，spec `concept-node-adopt`）；(e) detached createElement 补 `_zwCreatorDoc` 印章。③ **剩 3F 深项**（cross-realm 基建域记档）——iframe doc body appendChild 的跨 realm adopt / removed-iframe 后主文档 getElementById 可达 / customElements 单 registry 近似（同名 define 冲突）。fmt/clippy 双矩阵干净。commit `e6de62961` 已 push。
**上轮**: R180 — **M4 轻件：plain 父容器 appendChild 的 handle 子反链（single-activation 14F→0F 132P/132 全 100%、全量 9635P/229F/21T→9649P/215F/21T 净 +14P/-14F 零回归、native 9648P/215F 逐计数一致、make test 66 套件全绿）**：① **根因链（探针五轮实证）**——template.content.children 的 cloneNode 产出 **handle 元素**（`__zw_create_element`），用例把它们 append 进 **plain INPUT/FORM 容器**（`_zwMEl` markup 解析树）；`_zwMEl.appendChild` 只设 `c.parentNode` 数据属性**不记 `_zwNodeParent` 反链**（handle 父的 `_recordHandleChild` 才记）→ handle checkbox 的 parentNode getter（proxy get trap 经 `_parentNodeFor` 查反链）返 null、`_zwClickActivationConnected` 判 false → pre-click 激活翻转了 checked 但 post-activation input/change **不派发**（spec input activation behavior 末段 fire input/change 的 connected 前提）→ `oninput="this.checked ? activated(this)"` 永不执行，14 组 FORM/DETAILS/LABEL 父形态 got []。② **修三件（part03）**——`_zwMEl.appendChild` 对 handle 子记 `{ plainParent: node }` 槽（对偶 `_recordHandleChild`；只在本无链时记，移动语义已先摘旧父）+ `_parentNodeFor` 反链命中 plainParent 直返 + `_zwClickActivationConnected` 沿 plainParent 链上行到 sel/handle 节点即 true。③ **探针方法沉淀**——console.log 无出口场景用 assert message 携带 JSON（force-fail 打印 introspection）；13 组失败形态全 `got []` 指向子激活缺失而非父激活泄漏。fmt/clippy 双矩阵干净。commit `a6c154f4e` 已 push。
**上轮**: R179 — **M4 轻件：node-creation-realm 跨 realm 工厂通道（该用例 0P/13F→13P/13 全 100%、全量 9638P/225F/20T 净 +14P/-14F 零回归、native 逐计数一致、make test 18128P/0F 一次过）**：① **测试形态**——`inner.Document.prototype[name].apply(document, args)`（whatwg/dom#977）要求接口方法在 **prototype** 可达；shim 工厂方法全挂 document 对象自身，prototype 通道 undefined 直接崩。② **修六件**——Document.prototype 工厂转发（11 方法，own 优先零行为变化）+ DOMImplementation 构造器 + implementation 对象接原型 + iframe win 构造器转发补齐（DOMImplementation/Range/StaticRange）+ Range.prototype 方法通道（模板 own 转发）+ 节点原型链（_zwMText→Text/_zwMComment→Comment/CDATA→CDATASection）+ 工厂元素轻量 attachShadow（innerHTML 解析 + firstChild/lastChild）。③ **回归两轮**——title 查询修复首版 bodyHtml 前置致 createHTMLDocument body 视图 7F 回归（A/B 抓回）；二版踩字面量求值时序（doc 赋值前访问 doc 槽崩整页）——收口为局部变量 `_r179HeadHtml` 并入 detHtml 包装层。fmt/clippy 干净。commit `76791c528` 已 push。
**上轮**: R178 — **M4 轻件：Range 点查询方法族 + Attr-rooted mutation 守卫（Range-attribute-nodes 11F→0F 26P/26 全 100% + Range-comparePoint-2 连带转绿、全量 9624P/239F/20T 净 +11P/-12F、native 唯一分歧 = 已知 flaky、make test 18127P/0F 一次过）**：① **根因**——Range 缺 4 方法（comparePoint/isPointInRange/intersectsNode/compareBoundaryPoints 直接 not-a-function 8F）+ insertNode/surroundContents 对 Attr 形态不抛 HRE（静默 no-op 3F）。② **实现**——`_rootOf178`（parentNode 上行到根；Attr parent null → 根 = 自身，WrongDocumentError 短路判据）+ 四方法 spec 语义（intersectsNode 的 **parent-null 恒 true** 首版被 collapsed 前置误伤由 WPT 抓回）+ insertNode/surroundContents 的 WebIDL TypeError + nodeType-2 HRE 双守卫。③ **跨流小修**——page-runtime 两处 `chunks_exact(4)` 触发 clippy 1.98 新 lint（上游 SW 流引入、干净树复现、阻塞全仓 clippy 门），按既有 as_chunks 惯例机械替换。fmt/clippy 干净。commit `657b438e2` 已 push。
**上轮**: R177 — **M4 轻件：window.frames + appendChild WebIDL/adopt 语义（Node-removeChild 9F→0F 28P/28 全 100% + Node-appendChild 7F→0F 11P/11 全 100%、全量 9613P/251F/19T 净 +16P/-16F 零回归、native per-file 零差异、make test 18125P/0F）**：① **frames**——`frames[0].document` ReferenceError 整簇根因；Proxy 动态枚举（首版快照失败——建 frames 时 about:blank iframe contentWindow 尚未 lazy 物化；每次 get 现查 iframe 列表保证任意时刻可达）。② **removeChild 四修**——空文档 documentElement 合成 html 元素（about:blank iframe docEl null）+ iframe createTextNode 补 ownerDocument + text 节点 childNodes:[] 叶子视图（own-childNodes 分支按视图抛 NotFoundError）+ frames。③ **appendChild 四修**——WebIDL TypeError 前置（元素 + 叶子 mutation 族的 null/非 Node 参数，先于 pre-insert 一切步骤）+ 主文档 doctype 补 appendChild/insertBefore 抛 HierarchyRequestError + Document 不能作 child（旧静默 no-op）+ **跨文档 adopt**（plain 子 append 到主文档元素后 ownerDocument 重指——sel 父/handle 父两路径，spec concept-node-adopt）。fmt/clippy 干净。commit `daefda52b` 已 push。
**上轮**: R176 — **M4 轻件：Document-createElement-namespace 40F 全收口——fixture 子目录 fetch + 空文件放行 + svg kind（该用例 11P/40F→51P/0F 全 100%、全量 9597P/267F/19T 净 +40P/-40F 零回归、native per-file 零差异、make test 18123P/0F 一次过）**：① **根因两层**——fetch-dom-subset.sh 的 fetch_dir_html 只拉 .html/.js 且不递归子目录，Document-createElement-namespace-tests/（4 扩展 × 10 形态外部文档）整个缺失 → contentDocument null 整簇 fail（30F）；fixture 就位后剩 .svg 族——R175 kind 三分漏 svg → contentType 'application/xml' ≠ 'image/svg+xml'（10F）。② **修三件**——fetch 脚本补子目录 + 扩展过滤加 .xml/.xhtml/.svg + **空文件放行**（empty.* 是设计为 0 字节的测试形态，旧 `test -s` 把 curl 成功空响应当失败致 .tmp 残留永远拉不到，改 `test -e`）；part04 kind 四分（.xhtml/.html/.svg/其它）；part05 svg 分支 contentType 'image/svg+xml'。③ **全量 fail Top 重聚类**——Event-dispatch-single-activation 14F / node-creation-realm 13F / Range-attribute-nodes 11F / Node-removeChild 9F / Node-appendChild 7F。fmt/clippy 干净。commit `60ab238fa` 已 push。
**上轮**: R175 — **M4 轻件：Fragment body 过滤 + Document:new NodeList + iframe kind 三分判定（全量 9557P/307F/19T 净 +3P/-4F、ParentNode 5→2F、native per-file 零差异、make test 18118P/0F）**：① **Fragment body 2F**——fragment QSA 序列化源包字面 `<body>` 使 host `filter_synthetic` 的 `contains("<body")` 保留合成容器命中（`body` 选择器误返 1）；修 = 子串直发（源无 body 标签，合成容器正确剔除；host 单测 `zz_r175_frag_body_filter` 守住）。② **Document:new NodeList 1F**——DBG 探针链：append 后计数不增 → 新 div nodeName 小写 ≠ want 大写 → iframe doc contentType 'application/xhtml+xml' → **R156 kind 判定把 .html 也判 'xhtml'**；spec 核实（真浏览器 .html → 'text/html' 大写 / .xhtml → XHTML 大小写保持，WPT Document-createElement XHTML 断言）；修 = kind 三分（.xhtml/.html/xml）+ part05 'html' 分支 contentType 'text/html'。③ **剩 2F** = tree order（identity 深结构，R171 评估定格）。fmt/clippy 干净。commit `2f94eced2` 已 push。
**上轮**: R174 — **M4 轻件：ns setup 树碎片化统一——iframe 工厂 IDL 反射 + no-ns 标记协议（全量 9554P/311F/18T 净 +22P/-22F、ParentNode 29→5F ns 族 24F 全收口、native per-file 零差异、make test 18107P/0F）**：① **根因链**（探针实证）——setupSpecialElements 的 iframe 工厂产物三缺陷串联：`el.id=x` 无 IDL 反射不进 attributes（序列化丢 id）+ 无 outerHTML（compound 门 walk abort 守卫触发整体打回 JSON）+ 序列化丢 ns（re-parse 后恒 HTMLNS，`|div` EmptyNs 恒 miss）。② **修复三层**——part05 工厂补 id/className 反射 accessor + removeAttribute + outerHTML（复 `_zwMSerialize`）+ appendChild 对齐（adopt 摘原父）；**no-ns 标记协议**（`_zwMSerialize`/`_zwMOuterHtml` 对 `!namespaceURI` 元素输出 `data-zw-empty-ns`，host `Document::apply_empty_ns_markers` 查询前还原 ns 置空+剔标记）；engine bridge 接线。③ **ns 值语义纠正（A/B 门抓回归）**——首版「空串保空串」+97F 回归（WPT "empty string namespace" 断言 `=== null`，WebIDL DOMString? 归一）→ 回滚 null 语义 + 标记判据改 `!namespaceURI`。④ **剩 5F** = tree order 2 + Fragment body 2 + Document:new NodeList 1。zero-dom 单测 `zz_r174_empty_ns_marker_roundtrip`。fmt/clippy 干净。commit `a3ffdfb24` 已 push。
**上轮**: R173 — **M4 轻件：:lang/:target element 查询文档上下文 + owner 溯源槽（全量 9532P/333F/18T 净 +10P/-10F、ParentNode 33→29F、Element-matches 3→0F、native per-file 零差异、make test 18093P/0F）**：① **根因**——element 查询的子树序列化丢两个文档级上下文：`<html lang="en">` 祖先（:lang 继承链断）+ fragment URL（:target 恒 miss）。② **修复两件**——`_zwOwnerTree` owner 溯源槽（ensureTree 全树盖，与 root-only `_zwOwnerDetDoc` 解耦；ownerDocument accessor 增读——iframe 树内元素溯源源 doc）+ `_zwMQueryAll` 上下文注入（:lang 拼 owner 的 html lang 包装 / :target 传 `_zwFragmentUrl`；detached clone 无槽天然排除——spec 脱离文档无祖先继承，首版无守卫 Detached 反转实证）。③ **两轮实验**——全文档源方案 +8 回归回退；html-lang 包装 + 槽守卫收口。④ **顺带**——Element-matches 3F（`[*|TiTlE]`）随 owner 槽归零。fmt/clippy 干净。commit `c7e2c2a78` 已 push。
**上轮**: R172 — **M4 轻件收口：ParentNode 剩 33F 聚类 + zero_dom ns 前缀词法修复（全量 9522P/343F/18T 逐计数一致中性、zero-dom 852P、make test 18090P/0F）**：① **聚类**——33F = ns 族 24（`|*`/`|div`/`*|div` × 4 上下文）+ `:lang`/`:target` 4 + tree order 2 + Fragment NodeList 2。② **ns 词法修复**（zero_dom `selector_lexically_valid`）——`|` 分支只认整串段首的显式空 ns 前缀，`#id |div` 后代段被误判未声明 ns 抛 SyntaxError；修 = `|` 前是空白/组合器（段首）放行 + idx==0 放行 + `ns|div` 有名前缀仍拒（回归测试 8 合法 + 3 非法形态）。③ **匹配层核实**——组合链 ns 段（`#any-namespace *|div`）dom crate 匹配正常（probe 实证转回归知识）。④ **ns 24F 完整收口路径**——WPT probe 实证 `getElementById("no-namespace")` 返 null（setup 运行时 createElement 产物树碎片化），fail 形态已从 SyntaxError 进到合法 0-命中——剩余在树碎片化统一域（L2 深结构）。fmt/clippy 干净。commit `d538dee36` 已 push。
**上轮**: R171 — **M1 L2-d3d 评估（element 上下文回退）+ 解析器共用化提取（全量 9522P/343F/18T = R168 逐计数一致、make test 18087P/0F）**：① **两轮实验**——element 上下文 compound 全形态本树前置 33→35F（+2 identity）；收缩纯 tag 仍 +2F/-2F 持平互换；**均 0 subtest 改善**——R165 的「902F wrapper 依赖」结论在 R170 key 修复后**部分幸存**（element 消费面对产物形态敏感度高于 doc）。② **+2 机制**——`:enabled` 形态 `querySelector !== querySelectorAll[0]`（两路径产物形态时序变化使归一缓存键命中不同步）。③ **保留**——`_zwParseCompoundSel` 共用解析器提取（行为等价，未来 gate 消费者复用）。④ **d3 域状态定格**——doc 上下文本树化已落地（R170 tag+compound）、element/fragment 维持 JSON 往返（重启前置 = 产物归一路径统一，记 RFC）；M1 到**实用收口点**。commit `8a825479b` 已 push。
**上轮**: R170 — **M1 L2-d3c 落地：compound gate 正式开通 doc 上下文本树查询（全量 9521P/343F/19T 双路径 per-file 零差异、三簇全回基线、make test 全绿）**：① **R169 假设推翻**——iframe 双工厂是伪根因（src-iframe 的 bodyHtml 有内容树正常；R169 探针误用 srcdoc 无 src 分支）；② **单变量实验 + DBG 内探针**定位真根因——`_zwWrapCached` 的 key 构造单一形态假设（gate 前只收 JSON info 的 `.tag`；gate 直出真实节点的 `.tagName` 使 tag 段空键撞车返空壳，`[id="root"]:hits=1:t=DIV` 正确但外层包装变空实证）；③ **修两件**——key 双形态兼容（`.tag||.tagName` / `.outer||.outerHTML`）+ 组合器守卫（`/[\s>+~:]/` 整体拒绝——解析器吞空白把 `#descendant div` 误判同 compound 返 root 自身）；④ gate 保持开启：compound 形态（tag/#id/.class/[attr]/[attr="v"]）doc 查询消 JSON 往返，零命中回落 host 权威。fmt/clippy 干净。commit `0a4146465` 已 push。
**上轮**: R169 — **M1：master.md 恢复（R168 docs 提交截断事故）+ L2-d3c 尝试（回退 + iframe 双工厂根因确立）（全量 9522P/343F/18T = R168 逐计数一致零行为变化、make test 18071P/0F）**：① **事故恢复**——R168 的 docs 提交 `470b0e0e6` 把 master.md 从 441KB 截断至 3.4KB（python 块级 replace 边界吞文件主体）；`git show 6431f125f` 恢复 + 行级编辑重做记录（`ac2a56e97`）；learning 沉淀（`3772390e2`）。② **d3c 尝试**——compound gate 启用实测 ParentNode 33→905F（沙箱探针全命中 vs runner iframe 全灭对照）；根因 = **iframe doc 双工厂**（iframe contentDocument 的树/查询走 part05 iframe 工厂，`_makeDetachedDocument` 的 bodyHtml 对 iframe 恒空——probe 实证 bodyHtml:0）；**d3 前置项确立：iframe doc 与 detached doc 树源统一**。③ **保留**——`[attr]` 存在性匹配修复（R165 首版 op null 落 else 恒 false，gate 关闭时不可达零行为变化，d3c 重启必要前置）。commit `f0939b188` 已 push。
**上轮**: R168 — **M1 L2-d3b2：body 根归一收口 bubbles 残留两变体（全量 9522P/343F/18T 净 +4P/-4F、bubbles 双文件 10P 回基线、native per-file 零差异、make test 18068P/0F）**：① **二分定位**（沙箱探针过 → runner 探针过 → 复刻原用例 fail 最小对 → probe4 链 dump 实证 fail 变体 bodyInChain:false——注册 body 站是 D 域 wrapper、链上 fire 的是 C 域树根，身份错位；根因 = body 查询 JSON key.outer 与树根 `_zwMSerialize` 序列化细节不一致 → 归一键 miss）。② **修复三件**（每件由中间回归实证约束）——`_zwMFindRealNode` root-hit 特判（`_zwOwnerDetDoc` 印章 + tag 段一致 → root 本体即真实节点；首版不限印章致元素子树查询返自身 241F）+ 印章只盖根（全树 stamp 致 139F "got root"）+ iframe body 属性落根（`_r159BodyAttrs` 解析，+2F 当轮修平）。fmt/clippy 双矩阵干净。commit `9d62b27fa` 已 push。
**上轮**: R167 — **M1 L2-d3b：三查询面产物经 identity 桥归一 + 四件配套修复（全量 9518P/347F/18T 净 +2P F 持平、ParentNode 1936→1942P、native per-file 零差异、make test 18056P/0F）**：① **归一落地**——`_zwWrapCached`/`_zwMWrapCached`/fragment QSA 三面构造 wrapper 前经共用 `_zwMFindRealNode`（tag+id+outer 键索引，挂 root 槽）查同键真实节点，命中返桥对象；JSON 往返语义不变。② **四件配套**（全量 A/B 驱动，每件是归一暴露的真缺口）——matches 重写（整根上下文查询 + 强键找自身，Element-matches 中途 305F 回基线 3F）/ `_zwMEl.dispatchEvent` 三阶段链派发（B 域 proxy 入链不续上行 + listenerStore 委托 + `_zwOwnerDetDoc` 印章接 doc 站）/ listener 槽化 `_zwMEvLs`（中间站可 fire）/ ownerDocument accessor（setter 转 own 写，R126/R132 回归当轮修平）。③ **负结果记录**——lit e2e 双触发（链进 proxy 站，二分定位）+ bubbles 残留两变体 12vs14 站（new Document 的 docEl 身份错位，**记 d3b2**）。fmt/clippy 双矩阵干净。commit `5258ab632` 已 push。
**上轮**: R166 — **M1 L2-d3：RFC 级切片计划成文 + d3a identity 桥基建落地（零行为变化全中性——全量 9516P/347F/18T 双路径 per-file 零差异、make test 66 套件 18051P/0F）**：① **方向裁定勘误**——上轮 CONTINUE 候选「M6 QuickJS native S1q」为过时信息（master.md R75 已记 M6 S0q–S5q 全量收口），本轮执行 L2-d3 RFC 计划。② **RFC v0.1**（`docs/specs/p1b-l2d3-unified-matcher-identity-bridge-rfc.md`）：四对象域盘点（sel proxy / handle proxy / mutTree 节点 / 解析 wrapper，跨域既有桥仅 R100/R163/R164 三个局部点）+ identity 桥设计（模块级首登记者胜登记表）+ 统一匹配器复用路径（part05 `_handleSubtreeNodes`+`_matchComplexAgainst` 已是完整本树选择器引擎先例）+ 切片序 d3a→d3e。③ **d3a 落地**：`_zwNodeBridgeMap`+`_zwBridgeSet/_zwBridgeGet`（part03 模块级——首版误放 `_makeDetachedDocument` 内致 `_zwMEl` 出口 ReferenceError，acorn 全拼接 shim scope 链分析定位后提升）+ 三登记点（queryBody 直出 / fragment real163 / `_zwMEl` 工厂出口全覆盖 cloneNode/树建/fragment 解析）+ 5 处树代际 bump 同步清桥 + globalThis 导出（R79 poke 模式）+ part22 三单测。**活证据**：单测实证 createElement（handle 域）入 fragment 后 QSA 键匹配 miss——四域分歧运行时样本（d3b 输入）。fmt/clippy 双矩阵干净。commit `8d40bb957` 已 push。
**上轮**: R165 — **M1 L2-d2 尝试：compound 匹配器基建落地 + gate 评估回退（全基线中性——ParentNode 1937P/37F、全量 9516P/347F/18T、zero-engine 2310）**：两轮迭代——① 完整 compound（`.cls[attr]`）首版 **1072P/902F 大回归**（probe 定位 `docQ:1` 但 `detQ/fragQ/elQ:0`——element/fragment 上下文 identity 依赖 wrapper 语义，真实节点返回破坏消费面）；② 收窄纯 `#id` 仅 `#id-li-duplicate` 一处回归（duplicate-id 树首命中 vs JSON 往返首命中 identity 分歧）。**结论**：compound 迁移不能按形态逐个 gate——需 **L2-d3 统一匹配器 + identity 桥**（wrapper↔真实节点双向映射）；`_queryTreeByCompound` 匹配器基建已落地（守卫同 L2-d1），queryBody gate 维持 L2-d1 纯 tag 基线。zero-engine 2310 全绿；fmt/clippy 干净。commit `296c173b5` 已 push。
**上轮**: R164 — **M1 L2-d1：detached factory 纯 tag 本树查询（queryBody 对 `div`/`*` 遍历自身 `_tree`，消序列化往返；全基线中性零回归——ParentNode 1937P/37F、全量双路径 9516P/347F/18T 逐计数一致）**：实现 + 三件 probe 驱动守卫——① html/body 容器例外（容器只在 detHtml 包装层，走 JSON 往返）；② 空 outerHTML 元素中止（bare createElement 形态下序列化/活树分歧，整体 abort 回落 JSON——probe 实证 nodeName 空串）；③ 零命中落 JSON（早退 `[]` 曾致 post-mutation QSA 102≠103 回归）。真实节点直接返回（identity 与遍历/mutation 面统一，L2 方向落点）。zero-engine 2310 全绿；make test 域套件全绿（一个已知 net flake `stale_etag` 单跑绿）；fmt/clippy 干净。**L2-d2 候选（R165+）**：compound 形态（`#id`/`.class`/`[attr]`）迁移本树（无兄弟/祖先上下文需求，`_queryTreeByTag` 扩展 compound 判定）；组合器/伪类留 L2-d3。commit `cef0f64ed` 已 push。
**上轮**: R163 — **M1 L2 首片：fragment QSA 真实节点优先 + L2 切片评估（全量 9516P/347F/18T 双路径逐计数一致，+1P 零回归）**：① **落地**：fragment `querySelectorAll` 查询结果与子树真实节点（`_zwMEl`）按 tag+id+outer 键命中 → 返回**节点本体**（查询与 traverse/mutation 面 identity 统一——L2 方向首落点）；miss 回落 wrapper 缓存零变化。② **评估结论**：fragment tree-order 探针实证**内容分歧**（查询序列化源含 `<head>` 内容返 309，遍历面只走 1——不止 identity 不同，**查询源与活树内容不同**）——per-site 映射无法闭合；完全收口需 detached factory `queryBody` 直接查自身 `_tree`（消序列化→host re-parse→JSON 往返）。**建议切片序（R164+）**：L2-d1 纯 tag → L2-d2 compound（#id/.class/[attr]）→ L2-d3 组合器+伪类（part05 `_matchComplexAgainst` + nodeInfo 树上下文可复用）；每片 make test + 全量双路径 net≥0 才 land。**A/B**：全量 **9516P/347F/18T** 双路径一致；zero-engine 2308 全绿；make test / fmt / clippy 干净。commit `c9d2f00fe` 已 push。
**上轮**: R162 — **M4 nodes：nth 公式内空白 + matches 的 :target fragment（Element-matches 669P→672P/3F——剩 3F 全部为树碎片化域 `[*|TiTlE]`；全量 9515P/9516P 双路径等价）**：两件——① **`parse_nth` 公式内空白全剥**（spec an+b microgrammar 允许记号间任意 ASCII whitespace——WPT `2n \t\r\n+ \t\r\n4` 旧版 trim-only 后内部空白 parse fail 判非法）；② **matches 根上下文查询透传 fragment URL**（`_zwOwnerDoc._zwFragmentUrl` → host `:target` 判定，与 doc 级 queryBody R160 同源）。**残余收敛结论**：Element-matches 剩 3F + ParentNode 剩 37F（tree order identity / Fragment dynamic NodeList / ns 簇 / `[*|TiTlE]`）共同根因 = 查询面（缓存 wrapper）/遍历面（mutTree 节点）/setup 面（createElement proxy）**三对象域 identity 不统一**——正解 L2 统一 live Document（M1），轻件已榨干。**A/B**：全量 polyfill 9515P/347F/19T，native 9516P/347F/18T（±1 边缘 Timeout）；七轮累计 **+3225P**。zero-dom 849 / zero-engine 2307 全绿；make test / fmt / clippy 干净。commit `0ed5d7ceb` 已 push。
**上轮**: R161 — **M4 nodes：合成容器过滤 + iframe win 构造器 + QSA pending 回落（ParentNode-querySelector-All 1917P→1937P；全量 9490P/373F→9510P/353F 双路径逐计数一致）**：三件——① **合成容器过滤**：html5ever 解析任意片段补 html/body 容器——元素子树查询的 `querySelectorAll("html")` 命中合成祖先（WPT element 上下文 expect 0）；`parse_html_element_json_full` 新全参变体（`filter_synthetic` 剔源串无标签的容器命中，`__zw_parse_html_query` arg[5] opt-in——仅三个元素/fragment 子树调用点；doc 级 detHtml 真实包装 + DOMParser 合成 body 导出不受影响，engine 2307 全绿验证）；② **iframe win 构造器**：NodeList/HTMLCollection 从父 global 转发（R140 EventTarget 同款——旧 "RHS of instanceof is not an object"）；③ **QSA tag pending 回落**：R145 querySelector 单点回落镜像（host 快照 miss 的纯 tag 查询扫 `_zwPendingAdded`，identity 去重——WPT `querySelectorAll(null)` 对 setup 添加的 `<null>` expect 1）。**A/B**：全量 **9510P/353F/18T** 双路径一致；六轮累计 **+3220P**。zero-dom 849 / zero-engine 2307 全绿；make test / fmt / clippy 干净。**未收（R162 候选）**：ns 族 + `[*|TiTlE]`（树碎片化域 L2 正解）、tree order、Fragment dynamic NodeList、Element-matches 剩 ~14F。commit `874c50604` 已 push。
**上轮**: R160 — **M4 nodes：`:empty` 注释/PI 语义 + `:target` fragment（ParentNode-querySelector-All 1907P→1917P；Element-matches 族 +16；全量双路径 9490P/373F/18T 逐计数一致）**：两件——① **`:empty`**（CSS L4 语义）：元素为空 = 无子**或全子是注释/PI**（空白文本非空——WPT p1+p2 expect / p3-p5 不中；旧 any-children 判空漏注释子）；② **`:target` fragment**：`__zw_parse_html_query` 新第 4 参 URL → `parse_html_element_json_with_url` → `doc.set_url` → `is_target_element`；iframe fragment 经 `_zwFragmentUrl` 槽透传（旧版重 parse 无 URL 恒 miss）。**深结构结论（记 R161+）**：树碎片化桥接实验实证——序列化回写在 `_zwMEl` 序列化时**丢 createElement 产物的 id/属性**（`<div><div></div></div>`），正解 = L2/M1 统一 live Document，非本层可修（实验已回退）。**跨流归因（rule 10）**：~25 error-page 文件 summary-line 翻转（testRanges undefined 族）归因 **Cache API 提交** `aaa1753c5`（service-workers 流改 part01/02/07 shim——stash 干净 HEAD 复现）；SW/IDB 两个 load-order flake 单跑绿。**计数修正**：R159 的 9503P 含 ~39 Cache 引入 summary 漂移；本轮真实净增 +26P；五轮累计真实 ≈+3200P。zero-dom 849（+1）/ zero-engine 2307 全绿；fmt/clippy 干净。commit `87bb059dc` 已 push。
**上轮**: R159 — **M4 nodes：伪元素 valid-no-match + ns type selector + NodeList instance（ParentNode-querySelector-All 1811P→1907P；全量 9400P/464F→9503P/360F）**：四件——① **伪元素**（~64F 簇）：spec querySelector 不匹配伪元素——`PseudoClass::PseudoElement` 新变体（matches 恒 false）+ before/after/first-line/first-letter/slotted 等收录 + `::` 双冒号剥前导 + `::slotted(foo` 未闭合括号伪参上下文宽容（顶层裸 `(` 仍拒）；② **ns type selector**：`*|div` AnyNs 命中任意 ns / `|div` EmptyNs 仅空 ns 命中（HTML 元素 HTMLNS 不中 ✓ spec）——`NsKind` 字段 + tag 剥前缀；`ns|div` 有名前缀仍拒；③ **html/body 保真**：detHtml 包装恢复 `<html id><body id>` 属性（doc 槽从 iframe markup 提取）——`querySelectorAll('html')`/`:root` 命中带正确 id；④ **NodeList instance**：QSA 产物 `__zwQSA` 标记 + hasInstance 接受。**A/B**：polyfill 9503P/360F/18T，native 9494P/368F/19T（差 1T 边缘漂移）；四轮累计 6290P→9503P（**+3213P**）；zero-dom 848（+1）/ zero-engine 2306 全绿；make test / fmt / clippy 干净。**已知限制（R160 候选，深结构）**：`#any-namespace *|div` Document 上下文 fail——probe 实证 setupSpecialElements 的 appendChild 落 per-element mutable tree，与 doc 级查询树**互不合并**（`getElementById("any-namespace")` 都查不到）——polyfill 树碎片化结构性问题（L2 三方合一正解域）。commit `fc1611bd9` 已 push。
**上轮**: R158 — **M4 nodes：查询 identity 缓存 + 非法选择器守卫 + 空值运算符（ParentNode-querySelector-All 941P→1811P；全量 8534P→9400P/464F 双路径逐计数一致）**：四件——① **identity 缓存**：WPT runFinderTest 断言 `querySelector(x) === querySelectorAll(x)[0]` 同对象——shim 三条查询面（detached doc 工厂 `_zwWrapCached`（代际失效）/ Element.prototype `_zwMWrapCached` / `_zwParseEl`+fragment）加 per-root wrapper 缓存（键 tag+id+outer，上限 512），~600F 大簇收口；② **`_zwQueryGuard`** 六查询 API 边界（含 part04 proxy 的 sel+handle 双分支）：非法选择器 SyntaxError DOMException + 真无参 TypeError（`querySelector(undefined)` 按浏览器语义查字面 "undefined" 不抛），~120F；③ **split 转义感知**：`#\.\.\,\:\!` 的 `\,` 字面逗号旧在转义处切列表→残段 parse None→整链误判非法（guard 新暴露的既有缺陷，engine r118 回归守住当轮修）；④ **空值 `^=`/`$=`/`*=` 恒不匹配**（`[class^=""]` expect 0）+ U+000B VT 按 R124 语义算字面类名字符。**A/B**：全量双跑稳定 9400P/464F/18T，native 逐计数一致；R156 以来三轮累计 6290P→9400P（**+3110P**）；zero-dom 847（+3）/ zero-engine 2306 全绿；make test / fmt / clippy 干净。**方法论**：全量 WPT 计数须文件捕获读数——裸管道 awk 曾出 6381P 截断误读，双跑文件 diff 证实稳定。**未收（R159 候选）**：ParentNode-querySelector-All 剩 163F 分散单例（伪元素 0 匹配 / `:root`/`:empty`/`:not(*|*)` / ns 三形态 / `[*|TiTlE]` 第 3 命中缺——setAttributeNS 在 mutable tree 不入 host 快照 / NodeList instance / tree order）；Element-matches 剩 18F。commit `42143fd1a` 已 push。
**上轮**: R157 — **M4 nodes：CSS 转义反解 + universal `*` + Selectors-API 宽容 attr 形态（Element-matches 598P→657P/18F；全量 8371P→8534P/1329F 双路径逐计数一致）**：三件 host 引擎修复——① **`unescape_css_string`**（CSS Syntax §4.3.2 hex code-point 转义：`\e9`→é、`\0000e9`+空白终结符、`\x` 字面）应用于属性值/名 + `unescape_css_ident` 同源升级（wire 对 escape_css_ident 只转义非 hex 字符，向后兼容）；② **universal `*`**：matches 的 tag 比对旧把 `"*"` 当字面——`querySelectorAll("*")` 返空（单一 if 修复，Universal 12F 簇根源）；③ **Selectors-API 宽容**：截断属性段自动补 `]`（`#a [align="center"` WPT expect 命中）+ substring 族空白宽容（`[class*= banana ]`）+ `*|` 前缀剥离 + 词法 bracket/escape 感知（`|=` 运算符、`.example.` 值、`.test\.foo\[5\]bar` 转义类不再误杀）。**R157(a) 重要勘误**：R156 记录的「native iframe contentDocument body 空」为**陈旧 release 二进制虚警**——重建后双路径全量 **8534P/1329F/18T 逐计数一致**，native 无缺口；教训：native WPT 验证前必须经 make 入口重建。**A/B**：双路径完全对等；zero-dom 845（+4 回归）/ zero-engine 2304 全绿；make test / fmt / clippy 干净。**未收（R158 候选）**：Element-matches 剩 18F（`:empty` 8F 引擎伪类、`:lang`/`:root`/`:target`/`:nth-child` matches 位置上下文、`[TiTlE]` matches 面 case-insensitive）；ParentNode-querySelector-All 剩余簇。commit `53efe41b0` 已 push。
**上轮**: R156 — **M4 nodes：selector tokenizer 符号吞段 + Element-matches 激活（整页 error 3 轮 → 598P/77F；全量 6290P/264F → 8371P/1491F +2081P）**：**host 根因**——`tokenize_combinators` 的 pending-explicit 边界按下一段首字节记录，切分左段**含符号本体**（`#a1+div` → `"#a1+"`/`"div"`，id 吞 `+`）→ **无空格 `+`/`>` 与全 tilde 形态全线 miss**（影响一切 querySelector 消费者；tilde 无 upgrade 路径全灭）；修 = 符号起点记录 + 切分 monotonic clamp（病态 `a > > b` 旧版 slice panic）。**激活链七件**：① 查询根挂 `_zwRootHtml` 源串（sibling 组合器整树上下文）② deepClone 经 `_zwMEl` 工厂（原型链+mutation 面；traverse 的 setAttribute 旧 TypeError）③ cloneNode(deep) 语义 + ownerDocument 继承 ④ Element.prototype 查询面（assert_idl_attribute 要求原型链非 own；闭包函数提升模块级——原型方法 ReferenceError 教训）⑤ 新 `__zw_selector_valid` 探针 + `selector_is_valid`（词法预检 33 invalid 形态 → SyntaxError DOMException）⑥ 无参 TypeError + Node 常量挂原型 ⑦ R134 handle-only `urn:ns|h` 前移保序。**A/B**：polyfill 598P/77F；native 全量 6289P/264F 零回归（Element-matches 在 native 仍整页 error——native iframe doc body 空独立缺口，R157 候选）；`make test` 全绿 + fmt/clippy 干净；zero-dom 841 / zero-engine 2304 全绿。**未收（R157 候选）**：attr operator 边缘（`~=`/`|=`/`*=` unquoted/`\e9` 转义 ~30F）、伪类 in matches（`:lang`/`:empty`/`:root` ~10F）、native iframe contentDocument body 空（全量 +2081P 的 native 对齐面）；ParentNode-querySelector-All 1076F 新解锁簇。commit `2ba3ccdf1` 已 push（上轮 429 中断 WIP 由本轮收口）。
**上轮**: R155 — **M4 events：LABEL 激活转发 + inline handler 时序 + oninput inline 执行（single-activation 107P/25F → 118P/14F，全量净 +11P/-11F 零新增 fail）**：四件修复——① **node.click() 祖先 LABEL 转发**：`_zwMEl` click 旧只处理本节点 tag——非激活 target（LABEL 内 span）click 无激活。补 parentNode 链最近激活祖先判定，LABEL → 转发内部第一个 labelable 控件 click；遇其他激活元素止步（nearest 语义）。② **inline onclick 时序**（LABEL 簇 11F 根因）：旧版 inline 编译执行在最前（INPUT 翻转前）→ `onclick="this.checked ? activated(this) : null"` 恒 false 不触发。spec：inline handler 是 listener，在 pre-click activation 后触发——修=执行移到翻转账建立后（与 proxy R108 同序）。③ **inline oninput/onchange**（仅本地链）：本地派发只到 doc/docEl/body 三站，inline 非 listener——补显式编译执行；**proxy 路径不加**（R2934 已注册为 listener，双触发 acts:2 单测验证后删）。④ **connected 宽容 fallback**：clone 产物无 `_zwNodeParent` 反链——补 document.contains/parentNode 上行/_handleChildren 反查三 fallback。**方法学**：内联 WPT 诊断用例（zz-*.html + assert_equals 'MARKER' 强制失败输出诊断串）定位 testharness-vs-沙箱差异，比单测猜测快一个量级。**A/B**：single-activation **118P/14F**（LABEL 簇 11F 全收）；全量 **6290P/264F/18T**（vs R154 净 +11P/-11F 零新增）；`make test` 66 套件全绿；fmt/clippy 干净；单测 r155 两件 + 2299 全绿。**未收（R156 候选）**：剩 14F = clone checkbox 的 `_zwNodeParent` 反链**结构性缺失**（正解在 cloneNode deep 分支给重建子建反链，clone/registry 深结构）；Element-matches.html 整页 error（连续三轮未动，下轮优先）；Attr-prefix 2F / MO-document 3F。commit `24702bb89` 已 push。
**上轮**: R150 — **M4 events：进程内 gBCR + MouseEvent offsetX/offsetY 派发期计算 + Event timeStamp 5µs 量化（441P/15F/9T → 444P/12F/9T 双路径一致，+3P/-3F）**：三件修复——① **进程内 `__zw_getBoundingClientRect`**（callbacks.rs 新回调）：rect 读共享 `LayoutRectSnapshot`（webview render/apply/native-repaint 后 `refresh_layout_rect_snapshot` 刷新），selector→NodeId 经 LIVE_QUERY_DOC live doc 优先、miss 回落查询快照 re-parse；`register_dom_callbacks` 签名加 `rect_snapshot_opt` 参数——多进程 renderer js_worker 传 `None`（空快照返空串），其后 `RectBridge::register` 后注册者生效（V8 `global.set` 顺序遍历 = last-wins）→ 多进程路径零回归；进程内 webview 传 `Some(&self.layout_rect_snapshot)`。附带 `pipeline.layout_rect_for_selector` + `hit_test::fill_rect_from_layout_box_pub` 公开入口（当前无调用方，为 follow-up 按需查询留口）。② **MouseEvent offsetX/offsetY 派发期计算**（part03 `_dispatchWithBubble`）：spec CSSOM View §dom-mouseevent-offsetx（offset = client − target padding 边缘，近似 gBCR 左/上）；构造未显式给 offset init（真实浏览器 MouseEvent initDict 无 offsetX 字段，恒派发期计算；shim 旧恒 0）→ 派发到 target 后计算一次（`_zwOffsetComputed` 印章防重算）；显式 init 经 `_zwOffsetInit` 印章保持构造值（part05 `_defineEventSubclass` 工厂两形态盖章）；WPT mouse-event-retarget 1F→0F。③ **Event timeStamp 5µs 量化**（定时侧信道缓解，WPT Event-timestamp-safe-resolution：千样本 GCD ≥ 5µs——真实浏览器对 Event timeStamp 施 coarse 粒度防 timing attack，高分辨率原值使相邻事件 µs 差 GCD 跌到 1）：shim `_makeEvent`（JS `Math.ceil(t*200)/200`）与 native `event.rs perf_now_ms`（Rust 同款 ceil）双路径同语义；ceil 非 round——任意正 elapsed 量化后恒 ≥ 0.005ms 且 >0（R22 断言 timeStamp>0 不回归）。附带 **GamepadEvent 构造器**（part05 `_defineEventSubclass`，spec Gamepad §gamepadevent）解锁 Event-timestamp-high-resolution.https。**A/B**：timestamp 2 件 + retarget 1 件双路径 Pass；events **444P/12F/9T**（vs R149 fail 集仅 3 件消失：safe-resolution/high-resolution.https/mouse-event-retarget）；native 逐文件一致（fail+timeout 集合 diff 为空）；`make test` 66 套件全绿；fmt/clippy 双矩阵干净；单测 r150 三段断言（offset 计算值/显式保持、timeStamp 单调+量化+首值>0、GamepadEvent 构造链）。**429 打断恢复**：上一轮 session 在 10 次 API 重试后中断于 R150 实现完成但未验证未提交——本轮 stash→rebase（12 commit，含并行流 part05 MessagePort/service-worker 改动，pop 无冲突）→验证→补测→land。
**上轮**: R149 — **M4 events：customElements.define 既有元素自动升级（EventTarget-add-listener-platform-object 1F→0F 双路径 100%）**：用例 parser 先建 `<my-custom-click>` 后 define——spec `custom-element-registration` define 末步要求文档中已存在同名元素自动升级（ctor 体 + connectedCallback 立即触发），旧 define 只注册不升级 → connectedCallback 永不跑（`addEventListener(this)` 未注册 → click 后 dataset.yay undefined）。**两层修复**——① define 末尾 `_ceUpgradeSubtree(document)`（复用 R3269/R94 升级管线；同步执行，spec upgrade queue 微任务的 headless 同步等价）② 自动升级暴露 `_ceFireInitialAttrChanges`（R3274）best-effort 偏差：对 observedAttributes 每项无条件派发（缺失属性 null→null）——spec/真实浏览器（upgrade enqueue「if its value is not null」）仅对**存在**属性派发；修=getAttribute null 跳过。**两处既有测试断言更新**：R3274 webview multi-attr 测试改 spec 语义（仅 a=1，原断言 a=1,b=null,c=null）；R3205 engine 测试经修复自然通过（零改动）。**event-global 2F 判不追**——依赖 cross-realm frames[N].Function（iframe 跨 realm 基建深项，同 handleEvent-cross-realm 簇）。**A/B**：1P 双路径；events **441P/15F/9T**（vs R148 +1P/-1F fail 集仅该件消失）；native 逐文件一致；lit e2e 6P + CE e2e 11P 不回归（define 自动升级触达全部 CE 路径）；nodes Node-parentNode-iframe 隔离复跑在 stash 基线同 Timeout（既存 flake）；`make test` 66 套件全绿；fmt/clippy 双矩阵干净；单测 r149 四段断言。
**上轮**: R148 — **M4 events：焦点所有权统一 + relatedTarget shadow retargeting（shadow-relatedTarget 2F→0F 双路径 100%）**：用例链路——closed shadow 的 `root.innerHTML` 解析子 `shadowInput.focus()`（解析节点 _zwMEl 形态）→ `lightInput.focus()`（proxy 形态），断言 focus 事件 `e.relatedTarget === host`（closed-shadow 内容不泄露）。**两层根因修复**——① **_zwMEl.focus() 补焦点迁移**：解析节点获焦时取代 proxy 焦点态（清 _activeElKey + 旧 proxy 派 focusout/blur + 已聚焦 no-op 守卫）；旧实现只设 _zwMElFocused 使 proxy 视角焦点未变 → 后续 proxy .focus() no-op（第二 subtest Timeout 悬死根因）② **proxy focus() 的 relatedTarget + retargeting**：旧焦点为解析节点时清 _zwMElFocused（所有权互斥）+ focus/focusin 携带 relatedTarget——旧焦点在 shadow 树内（__zwFragHostHandle 命中 _shadowHandleMeta，R136 宿主印章链）→ 以 **shadow host** proxy 为 relatedTarget（spec UI Events Focus + Shadow DOM retargeting）；非 shadow 解析节点原样；旧 proxy 焦点路径零变化。附带 activeElement getter 的 _zwMElFocused 优先回落。**A/B**：shadow-relatedTarget **2P 双路径**；events **440P/16F/9T**（vs R147 438P/17F/10T +2P/-1F/-1T fail 集仅该件消失）；native 逐文件一致；焦点敏感（no-focus-events/focus-event-document-move）不回归；nodes 5472P/230F/10T（3 diff 项隔离全 Pass 系 flake）；`make test` 66 套件全绿；fmt/clippy 双矩阵干净；单测 r148 五段断言。
**上轮**: R147 — **M4 events：strict classic 脚本的全局函数发布（webkit-animation/transition 4 文件整页 error → 438P/+17P 系统性收益）**：根因——`script_run_classic_page` 经间接 `(0,eval)` 执行页面脚本，源内 'use strict'/"use strict" 指令使 eval 建独立变量环境，**顶层 `function` 声明不落 globalThis**（真实浏览器 classic 脚本即便 strict 也创建全局绑定——script 是 global code 非 eval code）；WPT 外链测试库（prefixed-animation-event-tests.js 的 `function runAnimationEventTests` 等）跨 `<script>` 不可见 → "is not defined" 整页 error。修复——eval 源尾拼接 `;globalThis.NAME=NAME;`（**行首零缩进** `function NAME(` 启发式——IIFE 内缩进函数不误匹配，testharness.js 全 IIFE 源零导出；初版 trim_start 曾使 harness 内部函数名 eval 报 ReferenceError，零缩进锚定修复；strict 局部声明经此导出、non-strict 恒等；拼接在两侧不改 strict 指令须为源首语句的语义）。**系统性收益**：所有 strict 声明顶层函数的跨脚本调用修复。**A/B**：webkit 4 文件每件 4/13 subtest Pass（旧整页 error）；events **438P/17F/10T**（vs R146 421P/21F 净 **+17P/-4F**，fail+timeout 文件集无真新增——4 diff 项均既存 Timeout 形态）；native 438P/17F/10T fail 集逐文件一致；nodes 5473P/230F/9T 零新增（appendchild-crash 隔离 Pass 系 flake）；traversal/collections 干净；`make test` 66 套件全绿；fmt/clippy 双矩阵干净；单测 r147（strict 两形态 + IIFE 不误发布 + sentinel 干净）。**残留**：webkit 4 文件各 9 promise_test 待真动画事件——runner 无动画时钟 pump（take_pending_animation_events 无消费者），runner 架构项记档不追本轮。
**上轮**: R146 — **M4 events：语义收口三件（Event-dispatch-other-document / Event-propagation / EventListenerOptions-capture 3F→0F 双路径 100%——3 文件全 subtest 通过）**：三件独立全在 shim 层——① **_zwMEl dispatchEvent 缺 target/srcElement**：detached doc（createHTMLDocument）createElement 产物派发后 ev.target/srcElement 均 null（listener 触发正常），修=派发前 own-set target/srcElement（spec dom-event-dispatch；own-set 覆盖构造器 data 属性同 R138 手法）② **dispatch 末步无条件清 stop/immediate flag**：Event-propagation "After stopImmediatePropagation()" 一次 dispatch 零触发后**第二次**应恢复触发——旧「外设 flag 保留至 initEvent」条件清理违背 spec（concept-event-dispatch 末步 unset 无限定）；附带发现 `_immediateStopped` 不清时 bubble 循环 `!immediateStopped()` 前置守卫被 stale flag 跳过全部 non-capture listener（单独清 _propagationStopped 不够）；dispatch 前零触发由 dispatch 开始的 flag 检查保证（Event-dispatch-propagation-stopped/cancelBubble/stopPropagation-cancel-bubbling 复跑全 Pass）③ **_optCapture WebIDL boolean 转换**：capture=2.3 期望 CAPTURING（Boolean(2.3)=true），旧 `opts===true` 仅认字面 true 使 primitive 真值落 opts.capture 误判 false；改 null→false / 非 object→Boolean() / object→Boolean(.capture)（四调用面共享 helper 单点修复）。**A/B**：三文件全 subtest Pass 双路径；events **421P/21F（native 422P/21F，fail 集两路径逐文件一致）** vs R145 419P/24F 净 +2P/-3F；nodes 5473P/230F fail 集 85 文件与 R145 逐文件一致零回归；traversal 50P、collections 0F；`make test` 66 套件全绿；fmt/clippy 双矩阵干净；单测 part21 新文件三件（part20 已 2217 行超 2000 上限）。**未收口**：redispatch 需 isTrusted 事件可信度模型（broader 设计）；shadow-relatedTarget 需 focus relatedTarget retargeting + _zwMElFocused/_activeElKey 双焦点态统一（中等深改，判专项）。
**上轮**: R145 — **M4 events：pointer-event-document-move 1F→0F 双路径 100%（template contents 查询不可见 + handle identity 派发桥 + testdriver selector 延迟解析）**：用例 `template.content.cloneNode(true)` → clone 内 p 注册 pointerup → `body.append(clone)` → `test_driver.click(querySelector('p'))` 断言 listener 随节点移动可达。**四层根因修复（dom/webview/wpt-runner/shim 四 crate）**——① **dom crate 查询遍历 R145 template opacity**：querySelector(All) 不进入 template 子树（spec HTML §4.7.4 contents 在 inert DocumentFragment 非文档树后代；ZW 解析器内联为 template 子故遍历等效排除——`querySelector('p')` 旧命中 template 内解析产物 p 而非 append 的真 p）+ 显式含 template 段的组合链 direct-address 例外（shim template.content 子代理的结构路径 `body > template > p` 依赖可解析）② **testdriver selector 延迟解析**：stub enqueue 存元素引用，宿主出队时经 `__zw_td_selector` 现场解析（enqueue 时 mutation 未 apply 正置表空是 "no stable selector" 根因）+ 新 `__zw_selector_for_handle` 正置反查回调（webview `handle_selector_forward` 镜像表）③ **part06 `__zw_dispatch_event` sel→handle identity 桥**：listener 注册在 handle proxy 的 `_listenerStore['@'+handle]`，sel-key 派发 miss——正置反查命中后以 handle 形态派发（未命中原路径零回归）④ **part06 querySelector 纯 tag pending 回落** + **part04 sel-based template content 视图**（`template.content.cloneNode(true)` 旧返 undefined）。**lit 回归教训**：part04 初版对 `_tplContent` 无条件加 own `cloneNode` 遮蔽 handle 形态依赖的 Node.prototype 泛型克隆 → `make test` 抓到 e2e_lit_library 4F（首渲染不落地）；修=cloneNode 仅 sel 形态——**shim 的 own-property 修复必须区分节点形态**（sel/handle 委托链不同，own 遮蔽对一形态是修复对另一形态是破坏）。**A/B**：pointer-event-document-move **1P 双路径**；events **419P/24F**（vs R144 416P/33F：该件消失 + 净 3P；DocumentFragment-getElementById/svg-template 为既存 Fail 经 git stash 隔离核实非回归）；nodes 5474P/230F（vs R141 232F 零回归）；`make test` 66 套件全绿；fmt/clippy 双矩阵干净；product-smoke 23.61% 与基线逐字节一致（既存 drift 非本轮引入）；单测 dom 3 件 + webview 2 件 + html_actions pre_events 4 事件断言。账本 + evidence r145。
**上轮**: R144 — **M4 events：指针激活序列 pre_events（focus-event-document-move 1F→0F 双路径 100%；mousedown→mouseup→click 事件序落地）**：真实浏览器指针激活序 mousedown→mouseup→click（spec UI Events），用例在 onmousedown 内把节点移入新 Document 断言不崩 + 节点从原文档消失——旧 Generic 激活只派发 click 无 mousedown/mouseup 使 onmousedown 从不触发。**修复（page-runtime + webview）**——`HtmlActionPlan` 新增 `pre_events` 字段（cancelable event **前**派发的事件——区别于 click 后 followup_events，事件序锚点在两侧；11 个既有 plan 构造全 vec![] 零行为变化）+ Generic 计划填 [mousedown, mouseup] + webview dispatch_user_action_impl 在 cancelable click 前循环派发 pre_events。**同簇 click-on-absolute-pseudo 维持 1F（Chromium 专有限制）**——依赖专有 Element.pseudo()/event.pseudoTarget 扩展（Firefox/Safari 均不支持，headless 无 hit-testing 无解），与真实浏览器对齐即 Fail，记档不追。**A/B**：focus-event-document-move **1P 双路径**、no-focus-events 2P 不回归、events 416P/33F（fail 集 vs R143 仅该件消失 + 2 个既知调度 flake Timeout 零真新增）；`make test` 66 套件全绿；fmt/clippy 双矩阵干净；单测 generic_activation 扩展 pre_events 断言。commit `07afee9be` 已 push；账本 + evidence r144。
**上轮**: R143 — **M4 events：handler-count 2F→0F 双路径 100%（重复 listener 丢弃 + window on* 全族 IDL 属性；R142 Actions 放行后新可见簇首件收口）**：**两层根因**——① spec「add an event listener」步骤 4 的重复 listener（同 type/callback/capture/槽位）静默丢弃未实现——shim 四个注册面（window `_globalAddEventListener` / document.addEventListener / element proxy addEventListener / _zwMEl plain-node）全无条件 push（"Duplicate listener is discarded" 断言 3≠2）② `_defineWinOnHandler` 类型表仅 WindowEventHandlers 子集+focus/blur，缺 GlobalEventHandlers 鼠标/pointer/键盘/input/media/animation*/transition* 全族——`window.onclick = fn` 落 plain 属性派发不触发。**修复（part03/04/06）**——四注册面各加 dedup（同 type/fn/capture/tgt 槽位 return）+ 类型表扩到 ~70 类型（setter 移旧注册新语义沿用 R2932 机制，纯表扩展）。**A/B**：handler-count **2P 双路径**；events 417P/32F（fail 集 vs R142 仅 handler-count 消失零新增）；traversal fail 集一致、collections 49P；`make test` 66 套件全绿；fmt/clippy 双矩阵干净；单测 `test_listener_dedup_and_window_on_family_r143`（6 断言段）。**教训**：Rust 字符串 line-continuation 与 JS `//` 行注释互斥——合并行后注释吞掉剩余脚本（完成值 undefined），单测 JS 载荷禁用 // 注释。commit `946ed15fd` 已 push；账本 + evidence r143。
**上轮**: R142 — **M4 events：no-focus-events 2F→0F 双路径 100%（testdriver Actions 指针链 + Generic 激活态 + 点击 focus 步骤）**：用例经 `new test_driver.Actions().pointerMove(x,y,{origin}).pointerDown().pointerUp().send()` 合成指针点击 contenteditable 元素，断言 focus 序列恰 `[focus, focusin]`（target=点击元素）。**三层根因**——① stub 无 Actions 类且在依赖白名单外（整文件 Unsupported 中性不可见）② `ActionTargetState` 无普通元素态——span[contenteditable] 非表单/链接/summary/option，Activate 落 NotApplicable noop 连 click 都不派发 ③ 指针激活的 focus 步骤缺失（宿主管线无 focus 语义，shim element.focus() 的 R3247 focusout→focus→focusin 派发无人调用）。**修复（四处）**——stub Actions 链式构造器（pointerMove 记 origin / send() 入队 click 走宿主 Activate 管线 / key 系列reject）+ Actions 入白名单 + page-runtime `ActionTargetState::Generic`（普通元素激活=纯 click 事件无默认动作；webview Activate 兜底 NotApplicable→Generic）+ runner click 分支先 element.focus() 再 Activate（真实浏览器指针序列：可聚焦目标先 focus 后 click）+ stub selectorFor 唯一化（属性筛选器→nth-of-type）。**附带集成断言语义对齐**：details 非首 summary 激活 noop NotApplicable→None（click 照派仅无 toggle 默认——观察串三事件不变，第 3 事件来自第二次 #summary preventDefault 分支）。**A/B**：no-focus-events **2P 双路径**；events 413P/36F（fail 集 vs R141：no-focus 消失 + **3 个 Actions 白名单放行后新可见的真实 Fail**[click-on-absolute-pseudo/focus-event-document-move/handler-count，clean 基线同形非回归——Unsupported 中性转可见是白名单放行的账面效应]）；`make test` 66 套件全绿；fmt/clippy 双矩阵干净；单测 `generic_activation_dispatches_click_without_default`（page-runtime plan 层）。commit `aded218d8` 已 push；账本 + evidence r142。
**上轮**: R141 — **M4 nodes：characterSet-normalization 654F→0F 双路径 100%（wpt-data 滚动新增文件的单一根因收口）**：用例经 `<iframe src="encoding.py?label=X">` 取子文档断言 contentDocument.characterSet/inputEncoding/charset === WHATWG encoding label 表归一化后的编码名，**三层缺口**——① runner fetch handler 不认 .py（上游是 wptserve Python 脚本，wpt-data 无静态文件）→ iframe contentDocument 恒 null（654F 全部根因）② shim iframe 相对 src 不解析（旧仅根相对/绝对）③ meta charset → 编码名归一化缺失（iframe 子文档 characterSet 恒 UTF-8）。**修复（三处）**——testharness.rs wpt_data_fetch_handler 内置 encoding.py 生成器（percent-decode ?label= → `<!doctype html><meta charset="X">`，上游 pin 3159769 逐字等价）+ part04 iframe 相对 src 按 location.href 目录段解析（./ 与 ../ 逐层弹开）+ part05 _zwMakeIframeDoc 解析 meta charset → `_zwEncodingFromLabel` 归一化（`_ZW_ENC_LABELS` 全表 34 编码 + HTML meta 特例 utf-16 系→UTF-8 / x-user-defined→windows-1252，spec documentEncoding）→ 覆写 characterSet/charset/inputEncoding；**首跑补漏** ISO-8859-6 的 -e/-i 两 label（6F 暴露表漏）。**A/B**：-1 315P/-2 339P **双路径 0F**；nodes 4799P/874F→**5452P/232F（+653P）**（其余 fail 集零新增，3 Timeout 为调度 flake 隔离复跑 Pass）；events/traversal fail 集一致、collections 49P；`make test` 66 套件全绿；fmt/clippy 干净；单测 `encoding_py_handler_generates_meta_charset_from_label_query`（4 断言段）。commit `104460741`（rebase over 并行流 206e6a135）已 push；账本 + evidence r141。
**上轮**: R140 — **M4 nodes：live childNodes（Node-childNodes 簇 5F→0F 双路径 100%，含上轮 429 中断 WIP 收口）**：spec `dom-node-childnodes` 返 **live NodeList** 六断言族（caching 同一性 / append+remove 后旧引用反映 / item() / 迭代器 identity / instanceof NodeList / doc+fragment 形态）。**实现（part03/04/05）**——真数组承载（Array 原生迭代器天然过 identity 断言）+ `_zwLiveNLCache`（elKey→数组 identity 缓存）+ `_r140Refresh` 融合视图同步（容器 handle→registry / sel→`_childNodeList(sel,null)` overlay / handle→local+registry+detached 物化，splice 级更新 length+索引）+ mutation 入口 `_zwLiveNLSync` 全接（record/unrecord/appendChild/insertBefore/removeChild/replaceChild/remove/replaceWith/prepend/replaceChildren）+ NodeList.item 就地挂 + instanceof NodeList 经 NodeList 构造器 Symbol.hasInstance 认 `__zwLiveNL` 标记（原型链保持 Array）+ `_zwChildBaseInvalidateAll` 同批失效。**当轮修正两处**：① 命中缓存分支原直引 `_r140Refresh`（var hoisting=undefined，TypeError 被 catch 吞→命中路径**永不刷新**——prepend 等无 sync 钩子入口读回 stale，R119 单测回归暴露）；修=refresh 闭包挂数组自身 `__zwRefresh` ② replaceWith 直接 splice `_handleChildren` 绕过 record/unrecord 入口——补手动双路同步。**A/B**：Node-childNodes{,-cache,-cache-2} **8 subtest 双路径 0F**；nodes 4799P/874F fail 集与基线**逐条一致**（含本轮 wpt-data 滚动新增的 characterSet-normalization 654F 既存——口径差异非回归，fail 集 diff 为证）、events 32F/traversal 6F fail 集一致、collections 49P 全绿；`make test` 66 套件全绿；fmt/clippy 干净；单测 `test_live_childnodes_r140`（10 断言段含 prepend/replaceWith live 回归）。**教训**：① 同函数作用域「先引用后声明」的 var 是 undefined 不是 ReferenceError——被 catch 吞掉即静默 stale，防回归要留无 sync 钩子路径的断言 ② 直接操作内部 registry（splice 绕过 record/unrecord）的旧代码是新 live 语义的隐形旁路——每个此类入口都要补同步或改走统一入口。commit `f4909831b`（rebase over 并行流 804566931）已 push；账本 + evidence r140。
**上轮**: R139 — **M4 events：EventTarget 对象 listener 全链 + named iframe window 注册 + Text 非 bubbling（events 28F→22F 双路径同步，traversal 15F→10F 涟漪 +5）**：**四层根因**——① EventTarget.addEventListener 旧版 `typeof cb !== 'function'` 直接 return 使对象 listener **根本不注册**（WebIDL EventListener callback 前置根因——派发点逻辑再对也无从生效）② dispatch 循环 `arr[i].call` + `catch(_)` 全吞：无 handleEvent 分派、无非 callable TypeError 上报、无 revoked Proxy Get/call 抛上报（spec inner invoke 步骤 1-2 + report the exception）③ `<iframe name=x>` 的 window named access 缺失（cross-realm 用例的 `eventListenerGlobalObject.Object/TypeError/Proxy/addEventListener` 全依赖）+ lazy 注册对「load listener 内直接读全局名」来不及 ④ Text.dispatchEvent 无条件转父 `p.dispatchEvent(event)` 使父成**新 target**（非「冒泡」是「重派发」）——非 bubbling 的 Text click 也触发父 pre-click activation 翻 checked。**修复（四处）**——part05 addEventListener 接受对象 listener（null/undefined 忽略，callable 判定移派发点）+ dispatch 循环补 handleEvent 分派（this=对象）/Get 抛与非 callable TypeError 经 _zwReportListenerError 上报（part03 导出 globalThis）/call 抛按 spec report + part04 contentWindow lazy 注册两分支 + part06 window dispatchEvent load 分支 __zwRegisterNamedIframes 派发前物化 + _zwMakeIframeWin 补 Object/Function/Array/Error/TypeError/Proxy 与 addEventListener 转发 + part03 Text.dispatchEvent 仅 bubbles 转父。**A/B**：cross-realm 5F→**0F（5P 双路径 100%）** + dispatch-click bubbles 1F→0F；events 28F→**22F 双路径 fail 集一致**；traversal 1589P/15F→**1594P/10F（+5 涟漪——NodeIterator 同源受益）**；nodes 8464P/188F 零回归。**附带归因修复（并行流红灯）**：`js_executes_ok_detects_throw_r3076` 红——bisect 定位 9ef914d10（service-worker 流 complete update cache matrix）把 classic 脚本 try-finally 改顺序执行后用 `execution.or(clear)` **误吞 strict 错误**（execution Err 时 .or() 取 clear 的 Ok → 抛错脚本 strict 返 Ok）；修=clear 独立执行 + 以 execution 错误为准（webview.rs 共享面，按归因纪律修在 js-dom 轮）。`make test` 66 套件全绿；fmt/clippy 双矩阵干净；单测 `test_eventtarget_object_listener_r139`（6 断言段——首版 sel-based input 无 R84 反链，改 createElement handle-based 复现 WPT 形态）；账本 + evidence r139。**教训**：① 「注册即过滤」是对象形态类用例的第一道暗墙——对象接受/拒绝判定要放派发点（spec callback 转换发生在 invoke）② catch-吞错与 spec「report the exception」相反——listener 异常不传播但**必须上报**（error 事件 + onerror），全吞对「跨 realm TypeError identity」类断言不可见 ③ lazy 注册有初始化竞态——named access 页面级不变量应 load 派发前一次性物化 ④ 代理转发使 target 漂移——p.dispatchEvent(event) 是重派发非冒泡，转发条件=event.bubbles ⑤ 单测环境要贴用例形态——sel-based vs handle-based 对子节点反链（R84）行为不同。
**上轮**: R138 — **M4 events：native 叠加路径事件面三缺口（native events 39F→28F，fail 集与 polyfill 完全重合，双路径对等再下一城）**：**三根因（探针实证）**——① **事件方法族不可达**：shim _makeEvent 把 stopPropagation/preventDefault/stopImmediatePropagation 作为 own 挂工厂普通对象上，native MouseEvent/KeyboardEvent 实例（V8 FunctionTemplate，R109 已重接 prototype 到 shim UIEvent）没有这些 own 方法且 Event.prototype 也没有 → listener 内 event.stopPropagation() 报 not a function（Event-stopPropagation-cancel-bubbling native 1F）；② **timeStamp origin 独立**：native Event.timeStamp 自有 origin（R22 OnceLock）vs performance.now() 回调另一 origin，WPT 断言 ev.timeStamp >= before=performance.now()（spec same time origin）恒 false（Event-timestamp-high-resolution 2F，探针 expected>=28.87 got 0.000038）；③ **srcElement/returnValue 缺位**：native set_event_init own data srcElement=null 遮蔽原型 accessor，shim _dispatchWithBubble 只设 event.target（工厂事件 accessor 自动跟随，native data 属性不跟随）→ dispatch 后 srcElement 仍 null + native 无 returnValue（Event-dispatch-click post-click native 2F）。**修复（三处）**——part05 事件方法族 + returnValue/srcElement accessor 幂等上 Event.prototype（**模块顶层**执行——首版放构造器体内首构造前不生效，移到 R23 常量块前；shim 产物 own 遮蔽零变化）+ part03 _dispatchWithBubble 设 target 时同步 own-set srcElement（own-set 覆盖 native data 属性两形态统一）+ callbacks.rs SHARED_PERF_ORIGIN 线程本地共享（performance.now 回调 + dom_bindings perf_time_origin 三方同源）。**A/B**：native events 39F→**28F（412→423P）fail 集与 polyfill 完全重合**（逐行 diff 空；polyfill 422-423P/28F 不变，Timeout 12↔10 既知 flake）；nodes 双路径 **8464P/188F 完全一致**；traversal 1589P/15F、collections 49P 零回归。`make test` 全绿 66 套件；fmt/clippy 双矩阵干净；单测 `test_event_proto_methods_native_overlay_r138`（10 断言段——首跑抓 typeof accessor 返 boolean 纠正断言）；账本 + evidence r138。**教训**：① shim 的 own-方法模式对 native 实例系统性失明——工厂挂实例 own 的方法族，凡有 native 兄弟构造器都要原型版双保险 ② 构造器体内补挂=首构造前不生效——原型补挂必须模块顶层立即执行 ③ spec「same time origin」是多钟一致性约束——Event.timeStamp 与 performance.now() 须共享 origin，独立 origin 各自正确但跨钟比较必炸 ④ own data 属性遮蔽原型 accessor——native 构造器设 alias 属性为实例 data 时 dispatch 的 own-set 覆盖是唯一统一路径。
**上轮**: R137 — **M4 nodes：native 叠加路径原型链重接 + DOMException 构造器 name（Document-createElementNS native 596F→0F 全 100%，native nodes +578，双路径 fail 集首次完全重合）**：**双根因（探针实证链）**——① **原型链断层**：native 只注册 HTMLElement（不注册 Node/Element ctor），native HTMLElement.prototype（FunctionTemplate 产物）直链 Object.prototype，shim 自建链被 _zwBuiltNodeChain=false 跳过 → Element.prototype（R3019 parentNode/childNodes/remove/cloneNode）与 Node.prototype（常量族+getRootNode）都不在 native 链上 → native created 元素（getPrototypeOf trap 返 HTML*Element.prototype→native HTMLElement.prototype）instanceof Node/Element 恒 false、el.ELEMENT_NODE undefined；② **DOMException.name 空串**：WPT testharness assert_throws_dom(type, ctor, fn) 按 funcOrConstructor.name === "DOMException" 分派构造器形态，native ctor 默认 name 空 → 走错分支把 ctor 当被测函数调用（"did not throw" 假失败，330 assert 簇全此形态；fail message 的 `function () { [native code] }` 是分派错误指纹——被调 fn 显示 native code = ctor 被 当 fn 调）。**修复（两处）**——part03 native 模式幂等 setPrototypeOf 三连（HTMLElement.prototype→Element.prototype[R128 Attr 同款] + Element.prototype→Node.prototype + Node 常量族补挂[R130 字面量表同款，prototype+ctor 双面]）+ dom_exception.rs build_and_register 用 **Function::set_name**（`f.set("name",..)` 被 V8 只读拦截——FunctionTemplate 产物 name non-writable，descWritable=false 实证，须 C++ 侧 SetName）。**A/B**：Document-createElementNS native 596F→**0F（596P 双路径 100%）**；nodes native 7684→**8262P（+578）** fail 968→**188 与 polyfill 逐行 diff 完全一致（双路径 fail 集首次完全重合——native 对等里程碑）**；polyfill 8463P/188F、events 423P/28F、traversal 1589P/15F、collections 49P 与 R136 逐项一致（native events 412P/39F 基线既存 stash A/B 证，记 R138 候选）；MutationObserver-document 3F 与 R136 同（并行流漂移待查不变）。`make test` 全绿 66 套件；fmt/clippy 双矩阵干净；单测 `native_dom_exception_ctor_name_r137`；账本 + evidence r137。**教训**：① V8 FunctionTemplate 构造器 name 属性 non-writable——对象 set 静默拦截不报错，改名必须 Function::set_name（native HTMLElement.name 仍空，后续 native ctor 注册应统一 set_name）② testharness assert 系列按 ctor.name 分派形态——「did not throw」类假失败先探 ctor.name 再查被测函数 ③ native 叠加路径的原型链是两个世界的接缝（R109 教训重演）：凡 native 注册 ctor 而 shim 依赖链上成员（常量/方法）的，须显式 setPrototypeOf 接缝。
**上轮**: R136 — **M4 nodes：getRootNode 泛型 + composed shadow root（5F→0F 全 100%，nodes +5 净，native +5 同步；上轮 429 中断 WIP 收口）**：**根因（五层）**——① 旧实现仅 part04 proxy get trap 的 sel 版分支（`_ancestorChain`），handle-only 元素（createElement/Text/PI/fragment 产物）/document/fragment/text 均 `getRootNode is not a function`（5F 全此形态——R134 `!sel` 短路同族的系统性盲区）② composed 选项不支持（spec shadow-including root 须穿 host 上行）③ shadow root 的 parentNode 语义缺失（spec ShadowRoot 不在树恒 null）④ **shadow innerHTML 解析子链断裂**（`_zwFragmentAdded` 顶层子 parentNode 指向 `_zwMBuildBodyTree` 内部 body 快照 plain object——非任何可达节点，shadowChild.getRootNode() 应为 shadowRoot 却断裂在假 body）⑤ **native 叠加路径原型链断链（本轮补）**——native HTMLElement.prototype 是 FunctionTemplate 产物直链 Object.prototype 不经 polyfill Node.prototype（native 不注册 Node ctor），shadow 解析子 `_zwMEl` 链经 HTMLDivElement.prototype → native HTMLElement.prototype，getRootNode 不可达（polyfill 5P / native 4P+1F 分叉根因）。**修复（五处全 JS 侧）**——part03 Node.prototype.getRootNode 泛型（receiver 分派：proxy 经 _parentNodeFor/plain 经 R84 反链/document·无父返 this；环守卫；composed 经 .host 续上行）+ get trap 分支改委托泛型 + part04 shadow root parentNode 恒 null + part03 _zwFragmentAdded 顶层子 parentNode 重指宿主容器 proxy（_wrapHandle 幂等缓存）+ part03 native 链幂等补挂（R130 XMLDocument 常量同款模式）+ part06 document.getRootNode 返自身。**A/B**：rootNode 5F→**0F（5P 双路径 100%）**；nodes 8459→**8464P（+5）**/native 7679→**7684P（+5 同步）**；fail 187→188（+1 = MutationObserver-document parser-insertion 3F/1P——非本轮改动面[零 host 回调零 mutation 路径]，判并行流漂移记 R137 复核）；events 423P/28F（复跑；首轮 421 超时抖动）、collections 49P、traversal 1589P/15F 与 R135 逐项一致。**既有断言 spec 纠正**：R2815 `getRootNode().tagName==="HTML"` 是旧 _ancestorChain 只返元素父的 shim 旧语义——WPT rootNode.html 断言文档内节点根 === document（documentElement 的 parentNode 是 document[R79]，继续上行），探针实证后改断言 `=== document` + detached 返自身。`make test` 全绿 66 套件；fmt/clippy 双矩阵干净；单测 `test_get_root_node_generic_r136`（11 断言段）；账本 + evidence r136。**教训**：① Node 接口方法优先上 Node.prototype 泛型（receiver 分派覆盖 sel/handle 双形态），proxy get trap 分支只做委托 ② 解析快照树的 parentNode 指向内部哨兵对象（假 body）是沿链 API 的隐雷——顶层子挂宿主 proxy 是 R123 印章的 parentNode 版通用修复面 ③ native 叠加路径的原型链断链是常态——幂等 defineProperty 补挂（own 已有不动）是既定模式（R130/R136）④ 全量计数对比要留并行流漂移余量——非改动面 ±1 先隔离复跑归因，不动本轮结论。
**上轮**: R135 — **M4 nodes：name-validation spec regex 校验族（5F→0F 全 100%，nodes +21 净，native +21 同步；上轮 429 中断 WIP 收口）**：**根因（四层）**——① JS `/\s/` 与 spec ASCII 空白五字符集不重合（`\x0B` 垂直制表是 JS 空白但非 spec ASCII whitespace——旧 `/[\s>]/` 把 `A\x0B` 误判 invalid）② NUL 漏校验（`\0` 非 `\s` 成员，`null\0` local 不抛）③ element/attribute/NS-prefix/NS-local 四名单语义未分（attribute 名无首字符限制但禁 `=`；NS prefix 段禁 `:` 允 `=`；NS local 段禁 `=` 允 `:`；element 名走 spec valid-name regex）④ **detached 路径遗漏**（createDocument 经 part03 `_makeDetachedDocument` 的 createElementNS 建 root，该校验器未升级——主文档 part06 修了 createDocument 仍 Fail 的最后根因：同名 API 双实现）。**修复（五处全 JS 侧）**——part01b 新 `_r135NameRegex`（spec regex 直引）+ `_r135IsValidName`/`_r135IsValidQualifiedNameSpec`/`_r135AttrNameRegex`/`_r135IsValidAttrQNameSpec` 四 helper；part03/part06 createElementNS（双实现）显式字符集 + local 段 spec regex；part04 setAttribute（attr 名单）+ setAttributeNS（段语义——单测首跑抓到的遗漏 `p:a=b` 不抛）；part06 createAttribute(NS)/createProcessingInstruction/createDocumentType。**关键实证修正**：element NS 的 prefix 段**从宽不校验**（spec regex 对 ≥0x80 首 prefix 的 NameChar 限制与 WPT validNamespacePrefixes 全码点实证宽松冲突——实证优先），只校验 local 段。**A/B**：name-validation 5F→**0F（5P 双路径 100%）**；nodes 8438→**8459P（+21）**/native 7658→**7679P（+21 同步）**；fail 集 stash A/B diff 核实**零真回归**（60 新增行 = 57 realm 族基线超时截断本轮跑完 + 2 Attr-prefix 基线预存[探针实证 baseline getAttributeNodeNS(xlink) 同 null] + 1 query-target 超时抖动）；相邻校验域（createElement 754P/createElementNS 596P/createDocumentType 82P/MO-attributes 41P）零回归；events 423P/28F、collections 49P、traversal 1589P/15F 与 R134 一致。**ranges 全量盘点**（dataChange/insertBefore 慢用例外）：P=4108 F=32927 T=5——mega-case「Set up range N」setup 簇（foreignDoc 容器）是 M1 L2 域，记 R136 候选。`make test` 全绿 66 套件（双矩阵；第一轮 network_loading::stale_etag flake 重跑绿）；fmt/clippy 干净；单测 `test_name_validation_spec_regex_r135`（7 断言组）；evidence r135。**教训**：① 校验器字符集一律显式枚举（`\u0009\u000A\u000C\u000D\u0020`），不用 `\s` 简写 ② spec regex 是仲裁但不是全貌——WPT 名单是浏览器实证，冲突时实证优先 ③ 同名 API 双实现（主文档/detached）校验器升级要两处同步——「主路径修了仍 Fail」先查调用链是否走另一实现 ④ 全量 fail 集 diff 要考虑 test-guard 截断——基线超时被杀的文件不是「通过」。
**上轮**: R134 — **M4 nodes：matches NS type selector + [Unscopable] 表 + on* handler 缓存失效（matches-namespaced 6F→0F 全 100% + remove-unscopable 0P→3P，nodes +9 净 fail -9，native +9 同步）**：**三层**——① `_r134MatchTypeSelector`（handle-only 元素[createElementNS detached 产物，sel=null]的 type selector JS 侧匹配，spec selectors-4 §6.1：裸 type 不比较 ns[API matches 默认 ns 语义——WPT 空 ns/urn:ns 两形态都断言真]、ns|type 全等、*|type 任意、|type 显式无 ns、* 任意；ASCII 大小写不敏感；复合选择器保守 false 返 null 回落）——旧 `!sel` 短路使 handle 元素恒 false（**handle-only 元素的系统性盲区**——closest/webkitMatchesSelector 同族）② Element.prototype 挂 [Unscopable] 表（spec ChildNode 四方法 + prepend/append）+ proxy get trap 的 Symbol.unscopables 分支透出（V8 with(proxy) 语义：has trap true 后经 get trap 读 unscopables 表对属性排除——裸 remove 解析 window 非 this.remove）③ on* setAttribute 一律失效 handler 双表缓存（`_onHandlers` + `_listenerStore` 旧条目剔除）+ 立即重编译（旧仅 body/frameset 失效——普通元素首编译永久缓存，六变体只跑首个体）。**A/B**：Element-matches-namespaced 6F→**0F（6P 双路径 100%）** + remove-unscopable 0P→**3P**（remove/prepend/append；剩 before/after/replaceWith 3F = inline handler 派发时序深结构——直接 new Function 编译的双向语义全对，静态标记+setAttribute+dispatch 链路三方法 result1 恒 undefined，R135 候选）；nodes 8429→**8438P（+9）** fail 196→**187（零新增）**/native 7649→**7658P（+9 同步）**；events 422P/27F、collections 49P、traversal 1589P/15F 与 R133 逐项一致。`make test` 全绿 66 套件（双矩阵）；fmt/clippy 干净；单测 `test_matches_ns_and_unscopables_r134`；账本 + evidence r134。**教训**：① `!sel` 短路是 handle-only 元素的系统性盲区——同族 API 逐个补 JS 侧能力面 ② with(proxy) 的 unscopables 经 get trap 透出原型表即可，无需改 has trap ③ 双表缓存（编译 fn + listener 注册）的失效要同步两处，setattr 即重编译使派发路径零改动。
**上轮**: R133 — **M4 nodes：insertAdjacentElement 入口校验（SyntaxError 用例 1F→0F + 涟漪 1F；sel 子移动深结构评估记录——首版回退，+2 净）**：**已修（三层）**——① 非法 position 同步抛 SyntaxError DOMException（四合法值 ASCII case-insensitive；旧 catch 吞 host 错恒返 null）② element 参数非节点 → TypeError（WebIDL 校验；旧宽容返 null——**连带纠正既有 e2e 断言**：test_insert_adjacent_text_and_element_e2e 的「非节点返 null」断言按 R2818 宽容行为写，与 spec/WPT 相反）③ documentElement 的 beforebegin/afterend 前置抛 HierarchyRequestError（spec pre-insert step 6——host 报错经 mutation 异步 apply 同步不可达，JS 侧按目标身份前置判定）。**深结构评估（未竟，首版已回退）**：剩余 5F 同根因 = sel 子（静态页面元素 #test1-4 无 handle）的**同步移动**——用例同 turn 内读 previousSibling/firstChild，host mutation turn 后 apply，需 JS 侧 pending overlay；首版 `_zwSelParentOf` 移动链 + 新旧父桶记账翻车两处：① 自建桶与 `_zwPendBucket` 工厂并行集（addedSet/removedSet）不一致 → _zwHCLiveInvalidate TypeError（探针 phase 标记定位 @before-notify）② 二次移动 overlay 状态漂移（__zw_parent 混合 host 快照与 overlay 视图）——**结论：sel/handle 双形态移动语义统一是 M1 L2 live-document 正解**，已记深结构清单。**A/B**：Element-insertAdjacentElement SyntaxError 1F→0F + 涟漪 1F；nodes 8427→**8429P（+2）** fail 197→**196（零新增）**/native 7648→**7649P**；events 422P/27F、collections 49P、traversal 1589P/15F 与 R132 逐项一致。`make test` 全绿 66 套件（双矩阵）；fmt/clippy 干净；单测 `test_insert_adjacent_element_validation_r133` + e2e 旧断言 spec 纠正；账本 + evidence r133。**教训**：① host 报错经 mutation 异步 apply 不可达——同步校验必须 JS 侧前置 ② pending 桶的并行集是结构契约——新记账一律经 _zwPendBucket 工厂 ③ 同步 overlay 的二次移动是试金石——polyfill 双形态移动语义统一只能靠 L2，补丁路走不通果断回退记深结构（沉没成本不加码）。
**上轮**: R132 — **M4 nodes：importNode spec 语义 + detached body 属性族（5F→0F 全 100%，nodes +4 净 fail -5，native +1）**：**根因（三层）**——① 副本无 ownerDocument（4F：旧委托 cloneNode——detached 元素 clone 走 Element.prototype.deepClone 产 plain object 无 ownerDocument 字段，WPT 四变体断言 newDiv.ownerDocument === document 全灭）② 浅克隆语义缺（deepClone 无 shallow 分支恒深复制 + plain 副本缺 firstChild getter 读 undefined ≠ null）③ detached body 无属性方法（`doc.body.setAttributeNS is not a function`——createHTMLDocument 的 body 是字面量 plain object）。**修复**：importNode = clone + **adopt**（adoptAll 递归 defineProperty ownerDocument getter 指本文档 + 浅变体剥子 + Attr 走 _zwMakeAttr 显式复制 prefix/ns/localName）+ plain 副本补叶子导航三件套（R130 title 文本子同款教训的**复发面**——任何产出 plain-object 节点的工厂/克隆路径都要过三件套检查）+ detached body 补 set/get/has/removeAttribute(NS) 族 + getAttributeNode(NS)（NS 元数据落本地 _r132BodyAttrNS 表——_zwMEl 树无 __zwHandle 不能复用 _attrNSMeta 键空间）。**A/B**：Document-importNode 5F→**0F（5P 双路径 100%）**；nodes 8423→**8427P（+4）** fail 202→**197（逐文件 diff 零新增）**/native 7647→**7648P**；events 422P/27F、collections 49P、traversal 1589P/15F 与 R131 逐项一致（native events 38F/traversal 36F 持平）。`make test` 全绿 66 套件（双矩阵）；fmt/clippy 干净；单测 `test_import_node_spec_semantics_r132`；账本 + evidence r132。**教训**：① **clone 委托不等于 import 语义**——spec import = clone + adopt 两步，只做 clone 丢 ownerDocument 归属 ② **叶子导航三件套复发面**（R130→R132 第二次）——plain-object 产出路径统一过三件套检查 ③ plain-object 侧 NS 元数据不能挂 handle 键空间——本地表是可达形态，跨形态共享需显式桥（记 L2 方向）。
**上轮**: R131 — **M4 nodes：isEqualNode spec 逐类型字段比较（6F→0F 全 100% + compareDocumentPosition 涟漪 6F→0F，nodes +13 净 fail -12，native +20）**：**根因**——旧 R2819 `_nodeSig` 用 outerHTML 序列化签名比对，三个语义洞：① NS/prefix 不参与序列化（different namespace/prefix 误等——HTML 序列化本不携带 NS）② PI 的 target 不进签名（handle 形态 data 读丢 target 前缀——different target 误等）③ doctype/document/fragment plain object 无方法直接 TypeError（Node.prototype 上没有泛型）。**修复**：统一 `_zwIsEqualNode`（part03 与 contains/compareDocumentPosition 同区域）按节点类型分派——元素 ns+prefix+localName+属性三元组集[ns/local/value；**属性序无关配对 + 属性 prefix 不参与**——spec 属性等价只看三字段，与元素层 prefix 参与性相反] + PI target+data（_piHandles 元数据） + doctype name/publicId/systemId 三字段 + Document/Fragment 仅子节点递归 + 子节点逐对递归（_zwChildNodesOf 融合读）；Node.prototype 泛型挂载（R128 原型链使 doctype/document/fragment 可达）+ part04 get trap 分支改委托 + **合成 docEl/head/body 补 namespaceURI=XHTML/prefix=null 标注**（spec：HTML 文档的 html/head/body 均 XHTML ns——"default HTML documents created different ways" 两径结构对齐）。**涟漪收益**：ns 标注连带修好 Node-compareDocumentPosition foreignDoc 域 6F（foreignComment↔foreignDoctype/foreignPara 族——期望值计算经节点排序/位置键，ns 字段参与）。**A/B**：Node-isEqualNode 6F→**0F（9P 双路径 100%）** + compareDocumentPosition 6F→0F；nodes 8410→**8423P（+13）** fail 214→**202（逐文件 diff 零新增）**/native 7627→**7647P（+20）**；events 422P/27F、collections 49P、traversal 1589P/15F、native events fail 集 diff 零新增逐项一致。`make test` 全绿 66 套件（双矩阵）；fmt/clippy 干净；单测 `test_is_equal_node_spec_fields_r131`（8 断言组）；账本 + evidence r131。**教训**：① **序列化签名的等价语义是近似**——outerHTML 对 NS/prefix/PI-target 不可分辨，spec 逐字段比较没有序列化捷径，须按节点类型分派 ② **属性等价的字段面与元素相反**——元素 prefix 参与、属性 prefix 不参与，读 spec 字段表避免想当然 ③ **合成节点的 ns 标注是结构正确性而非装饰**——一并修好 compareDocumentPosition 6F，字段标注类修复的影响面在全量回归中捕捉。
**上轮**: R130 — **M4 nodes：DOMImplementation doc 族 detached 语义 + crash 用例跑法（createHTMLDocument 7F→0F 100% + createDocument 434P 全绿 + crash 族 8 文件 Timeout→PASS，nodes +461 净 fail -23，native +1182）**：**根因（五类）**——① createHTMLDocument title 子树缺失（spec 步骤 4「create a title element, append to head」——title undefined 不建/null→'null' String 转换；detached headEl 旧恒空）② 原型接线缺失（docEl/head/body plain object 使 `documentElement instanceof HTMLHtmlElement` 恒 false；createDocument 产物 getPrototypeOf ≠ XMLDocument.prototype）③ createDocument 语义缺（WebIDL 必参缺省不抛 TypeError；qualifiedName 非空不建 root；doctype 参数不校验）④ 无 src iframe contentDocument 返 null（spec about:blank 初始导航——saved-implementation/null-browsing-context-crash 取 .implementation 直接 TypeError）⑤ **runner 对 crash 用例的 Timeout 伪失败**（`*-crash.html` 不引 testharness.js——纯脚本页断言「不崩溃」，completion 探针永远等不到）。**九层修复**：title 子树（含叶子导航面三件套）+ 原型接线族 + head/body 兄弟 getter + documentElement 惰性 getter（首个元素子；XML omit root 返 null；**proxy 子保持返内部 docEl——R112 派发以内部 docEl 为 tag-registry 站点，身份换掉 = 链断**）+ createDocument 必参/doctype 校验/root 创建 + XMLDocument.prototype 接线（Node 常量族幂等补挂）+ createDocumentType 宽松校验 + iframe 空文档 + A/AREA href IDL percent-encode + **runner crash 支持**（无 harness 引用用例预置 harness 内联 + terminal 零注册按 PASS 计）。**当轮三回归三修**：Event-dispatch-bubbles 2F（documentElement 惰性 getter 首版对 proxy 子返克隆——R112 站点脱钩，按子形态分派修复）+ native traversal foreignDoc 20F（title 文本子缺 hasChildNodes——oracle nextNode 统一调用，叶子三件套补齐）+ native createDocument 111F（native 叠加路径 Node 构造器常量全缺——diag7 探针实证，无守卫幂等补挂）。**A/B**：createHTMLDocument 7F→**0F（13P 双路径 100%）** + createDocument 434P/0F 双路径 + createDocumentType 82P + baseURI 9P/0F + crash 族 8P；nodes 7949→**8410P（+461）** fail 237→**214（零新增）**/native 6445→**7627P（+1182）**；events 420P fail 集 diff **IDENTICAL** 零新增、collections 49P、traversal native 37F→36F（还修好 TreeWalker-realm 1F 零新增）。`make test` 全绿 66 套件（双矩阵）；fmt/clippy 干净；单测 `test_dom_implementation_doc_family_r130`；账本 + evidence r130。**教训**：① 「首个元素子」的 spec 语义要按子节点形态分派——polyfill 桥里 API 返回身份（内部站点 vs 克隆 proxy）决定派发/查询链是否连通，A/B 全量 diff 是抓回此回归的唯一手段 ② plain-object 叶子节点的最小导航面（hasChildNodes/firstChild/lastChild）是 oracle 遍历硬依赖——新增合成节点必补三件套 ③ crash 用例的 WPT 跑法是「页面脚本不崩」——runner 只认 harness completion 探针则永远 Timeout 伪失败，按 has-harness-ref 分派是 runner 侧语义对齐非用例豁免 ④ native 叠加路径的常量缺口是系统性的——native 构造器静态常量不随 polyfill 自建链守卫走，须独立幂等补挂。
**上轮**: R129 — **M4 nodes：CharacterData 方法族 spec 校验 + WebIDL 转换语义（八文件 44F→0F 全 100%，+47 净双路径同步）**：**根因（四类）**——① 叶子节点 mutation 族无校验（18F：Text/Comment/PI 的 appendChild/insertBefore/replaceChild 落元素分支静默执行——spec pre-insert「parent 非 Element/Document/Fragment」须 HierarchyRequestError）② WebIDL 参数语义（~12F：appendData 缺参 TypeError；null/undefined 显式 String 转换；data= 是 **[LegacyNullToEmptyString]**——null→''、undefined→'undefined'；旧 `s == null ? '' : s` 把两者都吞成 ''）③ **offset/count 校验方向错**（~10F：只有 offset 越界抛 IndexSizeError；**count 是 unsigned long**（WebIDL 回绕 -1→2^32-1）后 spec「clamp 到 length-offset」——负小值 wrap 后恒 clamp 不抛，`deleteData(2,-1)`→'te'）④ 方法存在性与 remove 空缺（~9F：`'remove' in textNode` 恒 false；text.remove() 对 handle 父 registry 不生效）。**关键定位**：`_makeProxy` 的 Proxy handler 字面量跨 part 拼接——**`has:` 键定义两次（part03 FV 版 + part05 expando 版）后者胜**，首版白名单加进 part03 死键无效，探针实证（`'validity' in t` true 来自 target own key 而非 trap）后转 part05 生效版。**七层修复**：get trap 叶子 mutation HRE + 五方法参数族 + set trap LegacyNullToEmptyString + remove trap CharacterData 分支（registry 剔除 + record）+ part05 has trap 方法白名单（20 项）+ part03 死键注释标记 + parsed-node setter 同源。**A/B**：CharacterData 八文件 44F→**0F（157P 双路径 100%）**；nodes 7902→**7949P（+47 净）** fail 284→**237（逐文件 diff 零新增）**/native 6398→**6445P（+47 同步）**；events 419/27F、traversal 1595/9F、collections 48/0F、MO 105/10F、classlist 1420/0F 逐项一致零回归。`make test` 全绿 66 套件（双矩阵）；fmt/clippy 干净；单测 `test_character_data_method_family_r129`（7 断言组）；账本 + evidence r129。**教训**：① **拼接式 handler 字面量的重复键静默覆盖**——改 trap 行为先 grep `键名: function` 全 part 定位生效版 ② WebIDL 数值参数先查类型再写校验（unsigned long 回绕 vs signed 抛错）③ DOMString 参数 null/undefined 三态按 attribute 分类（LegacyNullToEmptyString vs 普通）。

**上轮**: R128 — **M4 nodes：Node-cloneNode 全节点形态族（主文件 14F→0F 全 100%，簇 24F→5F，+26 净双路径）**：**根因**——cloneNode 只对元素 proxy 实现，其余节点形态（fragment/text/comment/PI/Attr/doctype/Document）要么 `is not a function` 要么落到元素克隆返 nodeType 1 错形态；instanceof 面全断（canvas/unknown/DocumentType/XMLDocument/Document）。**八层收口**：① `Node.prototype.cloneNode` 泛型分派（own-property 委托[第三次数训应用]→text/comment 工厂→PI/Attr 四字段独立/doctype/fragment/Document contentType 分派+deep 复制 doctype→兜底 Element deepClone）② 原型链接族（Attr.prototype→Node + DocumentType/XMLDocument 构造器占位 + detached doc setPrototypeOf(Document.prototype) + constructor 惰性 getter 按 contentType 返 XMLDocument/Document）③ doctype 工厂**后置** setPrototypeOf（对象内 IIFE tdz 拿不到 dt 本体——首版静默失败根因）④ proxy trap text/comment/PI/fragment handle 感知（`_textHandles`/`_commentHandles`/`_piHandles`/`_fragmentHandles` 各自重建保 nodeType）⑤ NS 源恢复（`_nsHandles` 元数据→`__zw_create_element_ns` 重建+重注册——cloneNode().nodeName 'FOO:DIV'）⑥ canvas 全套（setPrototypeOf[original instanceof] + attributes 视图 + cloneNode 方法）⑦ 未知 tag→HTMLUnknownElement.prototype + **`_zwUserProto` 用户原型存取**（setPrototypeOf trap 新增——旧默认落 target 且 getPrototypeOf 不读 target，用户原型被静默丢弃）⑧ DOMParser doc cloneNode（createHTMLDocument('') 承载）。**A/B**：Node-cloneNode 14F→**0F（135P 双路径 100%）** + document-with-doctype 3F→0F + XMLDocument 1F→0F；nodes 7876→**7902P（+26 净）** fail 310→**284（逐文件 diff 零新增）**——**涟漪收益**：Document-constructor 1F + append-on-Document 3F + prepend-on-Document 3F（Document.prototype 链接连带）；native 6375→**6398P**；events 419/27F、traversal 1595/9F、collections 48/0F、MO 105/10F 逐项一致零回归。`make test` 全绿 66 套件（双矩阵）；fmt/clippy 干净；单测 `test_clone_node_all_node_kinds_r128`（12 断言组）；账本 + evidence r128。**剩 5F 记深结构**：svg 4F（探针实证**源** markup 元素本身 ns 读 XHTML/属性丢 xmlns: 前缀——parser NS 元数据面深结构，源修后 clone 自然跟随）+ inactive-iframe 1F（无 src iframe contentDocument null——frame 域）。**教训**：① 对象字面量内 IIFE 拿不到本对象（tdz）——构建后接线必须移到字面量之后 ② own-property 委托判定已形成范式（R126 removeChild→R127 replaceChild→R128 cloneNode）③ **原型链接的涟漪收益真实但不可预测**——为 instanceof 而做的 setPrototypeOf 连带修好 append/prepend 7F，原型链接类修复的回归面要跑全量子目录捕捉全部收益。

**上轮**: R127 — **M4 nodes：Node-replaceChild spec replace-child 校验族 + replace 语义（15F→0F 全 100%，+16 净双路径 + MO 两悬挂 async_test 解锁修平）**：**根因（四层）**——① 泛型 `Node.prototype.replaceChild` 的 own-delegation bug（**R126 removeChild 教训的 replaceChild 复发面**：`typeof this.replaceChild` 命中原型方法自身 → own.call 无限递归栈溢出）② Document pre-insert step 6「给定当前子」校验全缺（8F：fragment 含 text/多元素、element 位置冲突、doctype 位置约束）③ replace 语义错误（旧「先定位 old 后 adopt new」在 new 是 old 兄弟时 adopt 移除使 old index 前移 → splice 错位；replace-with-self 无短路）④ proxy 层 **selector-based newChild 无路径**（主文档既有元素作 newChild——wire 只支持 handle 子插入，旧静默 no-op：`b.parentNode` 残留 + MO record 永不产生 → **async_test 悬挂不结算 = 测试看不见**，加严后被解锁暴露）。**五层收口**：泛型 own-property 委托 + `_r127DocPreInsertCheck`（**kids+oldChild 位置感知**——element 拒「另有 element 或 doctype 在插入位**后**」/doctype 对称拒 element-在前/fragment 数元素拒 text；首版「存在性」近似误拦合法 replace-docElement，方向性纠正）+ detached doc replaceChild（fragment flatten + **adopt ownerDocument 重指**[concept-node-adopt——`doctype2.ownerDocument === doc` 断言]）+ part04 proxy NotFound 校验（`_r126IsChildOf` 提升 get trap 作用域 removeChild/replaceChild 共用）+ R100 handle 路径 **adopt-first**（先移 new 自身条目再定位 old）+ **selector-newChild 新路径**（① adopt record：newChild 旧父发 removed+prev/next ② replace record：单条 removed=[old]+added=[new] ③ old 走 remove 同款记账）。**A/B**：Node-replaceChild 14P/15F→**29P/0F 双路径 100%**；MO-childList 19P→**22P**（两悬挂解锁修平，剩 1F 既知 surroundContents 基线同值）；Node-childNodes-cache-2 1F 意外修复；nodes fail 集 326→**310（逐文件 diff 零新增）**/native 6375P；events 419/27F、traversal 1595/9F、collections 48、MO 全族 105/10F（文件级分布逐项一致）、classlist 1420 零回归。`make test` 全绿 66 套件（双矩阵）；fmt/clippy 干净；单测 `test_replace_child_validation_and_semantics_r127`（9 断言段）；账本 + evidence r127。**教训**：① **R126 的同族教训有复发面**——原型方法 own-delegation bug 在 removeChild/replaceChild/insertBefore 同族逐个复发，修复时应枚举同族方法一并收口 ② **spec 位置约束的方向性**——「element 须在 doctype 后」必须以**插入位**判 before/after，存在性近似会误拦合法 replace ③ **静默 no-op 的 MO 测试形态是「悬挂」不是「通过」**——async_test 的 mutation no-op → record 永 empty → 悬挂不结算；加严解锁后 fail 先涨后修平是 reveal-then-fix 正常形态非回归。

**上轮**: R126 — **M4 nodes：Node-removeChild spec pre-remove 校验族（28F→9F，+19 净双路径 + target-moved 回归当轮修平）**（commit `7b8849bd0`；上轮 429 中断、变更遗留暂存区，本轮开头核对后提交）：**根因**——shim removeChild 全层 lenient（任意 child 静默返 child），spec `dom-node-pre-remove` 步骤 1 的 child 包含检查与 WebIDL Node 参数校验全缺。**四层收口**：① part04 proxy：`_r126IsChildOf` 融合视图子判定（handle registry / `_childNodeList` 快照∪pending）+ **两个 lenient 分支**（已移除节点 `_zwIsRemovedNode`——移动后 parentNode 读 host 快照仍指旧父，调用方按旧父重试是 shim 视图限制下的合法形态[Event-dispatch-target-moved 16 listener 以 `parent === target.parentNode` 旧值守卫重试，严格校验下抛 NotFound 炸 listener——本切片引入的回归当轮修平]；parentNode 残留 `target.parentNode === this`——同一融合视图体系内自洽）② Node.prototype 泛型：**步骤顺序纠正**（包含检查先于父类型检查——spec 算法步骤序即断言序）+ **own-property 委托判定**（`typeof this.removeChild` 命中原型方法自身 → 无限递归栈溢出 RangeError，V8 报 RangeError 非 call stack exceeded）+ 有 childNodes 视图的叶子节点（_zwMText/_zwMComment）就地校验 ③ `_zwMEl` + detached docEl 校验补齐（docEl 缺 appendChild/insertBefore——synthetic 变体 `doc.documentElement.appendChild(s)` 旧直接 TypeError）④ native removeChild 无 slot 参数分级（null/非对象→TypeError；有 nodeType 无 slot 的叠加路径 polyfill 节点→NotFoundError）+ `_zwDomException` globalThis 化（`(doc.defaultView||self).DOMException` identity）。**A/B**：Node-removeChild polyfill 0P/28F→**19P/9F**（剩 9F 全 frame 域 R117 既知缺口）/native 0P→13P（+13；6F 为叠加路径 polyfill document 无 slot——未解决问题 #9 范畴）；nodes fail 集 261→**238（逐文件 diff 零新增）**/native 6120→**6137P**；events 419（含 target-moved）/traversal 1595/collections 48/MO 102/classlist 1420 零回归。`make test` 全绿 66 套件（双矩阵）；fmt/clippy 干净；单测 engine `test_remove_child_not_found_validation_r126`（11 断言段）+ native `native_remove_child_type_and_slotless_validation_r126`；账本 + evidence r126。**教训**：① own vs inherited——原型方法内「有自身实现则委托」判定必用 hasOwnProperty（typeof 不分自有/继承→自引用无限递归）② **加严校验的回归面 = 既有调用方的 leniency 依赖清单**（lenient 承载了 stale 视图下重试的合法形态，先枚举再设计 lenient 分支，而非全局加严后逐个救火）③ spec 步骤顺序即断言顺序——「先抛哪个错」由算法步骤序决定。

**本轮续121**: R122 — **M4 nodes：Attr identity 绑定表 + 同名多实例覆盖层（attributes.html 全 100%，+42 净双路径）**（commit `1037252cc`；上轮 429 中断的 WIP 由本轮收口并修复三处半成品 bug）：六层修复。① **`_zwAttrBindings`**（elKey→Map(限定名→Attr)）：setAttributeNode/getAttributeNode/setNamedItem/removeNamedItem 同一 Attr 对象往返（含 '\x00#k' 合成后缀键——同 qname 多 ns 实例各有 identity）+ removeAttribute 族解绑 ownerElement（闭合 "Attribute loses its owner" 后 setAttributeNode 误抛 InUse）。② **`_zwAttrInstances`**：host 扁平限定名存储无法表达同 local 多 ns——JS 侧权威多实例视图；**非 NS 读 host 优先、实例 local 兜底**（classList/style/className 直写 host 不经实例同步面——首版 instance-first 致 Element-classlist 550F 回归，当轮修平）。③ **`Attr.prototype.value/nodeValue` accessor**（_r122V 存储 + 幂等护栏）：attr.value=v 写回传播到 ownerElement（spec dom-attr-value）。④ **NamedNodeMap 方法 identity**：per-element Proxy 缓存 `_zwNNMCache`（缓存命中先于原型重赋值）+ named 'item' 不遮蔽方法。⑤ **setAttributeNS validate-and-extract 全分支** + 小写化收窄到 HTML-ns 元素（非 HTML ns 大小写敏感保留）。⑥ **`el.style=` cssText setter 写 style 属性**（旧落 expando→toggleAttribute('style') 误加属性）。**A/B**：attributes.html 36F→**67P/0F（100%）** + attributes-namednodemap 2F→**8P/0F（100%）** + Attr-prefix HTML 4F→P；nodes 7631→**7673P（+42）**/native 6144→**6186（+42 同步）**；events 419/collections 48/traversal 1595 不变（fail 文件集逐文件比对零新增）。`make test` 全绿 exit 0（含 quickjs 矩阵）；fmt/clippy 干净；engine 单测 `test_attr_identity_and_multi_instance_r122`（10 断言组）+ 三个旧断言按 spec 纠正（r46 'xml' 前缀 ns 绑定 / r3022 plain-object→createAttribute / r44 顺序）；账本 + evidence r122。**教训**：① **读权威按 API 族分野**——instance 层是 NS 语义的权威、host 是渲染/直写路径的权威，非 NS 读写走 host-first 保直写路径一致，NS 读写 instance-first，两个权威面不在同一路径混用 ② JS 闭包内 var 函数表达式按**执行序**赋值——定义在分支后、分支先 return 时闭包捕获 undefined（_r116NsQName 'not a function' 回归）③ readNames 融合顺序：实例 local 集**涵盖** host 名单 local 集→实例序权威（插入序），否则 host 名单权威、实例按 local 位次展开（parser 快照属性与 JS 写属性混存时）。

**上轮**: R121 — **M4 nodes：Text/Comment 构造器 + CharacterData 孤立代理保真（三文件全 100%，+32 净双路径）**（commit `013f45bc1`，rebase over 渲染流）：两层修复。① Text/Comment **真构造器**（旧空 stub——new Text().data undefined 全簇 26F）：经 _zwMText/_zwMComment 完整实例（data 参数 String 转换[null→'null'] + ownerDocument getter + setPrototypeOf 三层原型链[Text→CharacterData→Node instanceof 真]；R108 dispatchEvent 保留）。② **`_zwTextDataCache` JS 侧覆盖缓存**（R118「wire FFFD」深结构的局部可达绕过）：spec 允许 replaceData/deleteData/insertData 按 UTF-16 code unit **切开代理对**、切开半对读回保真（WPT surrogates），wire（to_rust_string_lossy）必然 FFFD——缓存写双写（JS 保真 + wire 尽力供渲染）、读缓存优先，十处统一接线（data/nodeValue/textContent/wholeText/length + setter + append/delete/insert/replace/substringData）。**A/B**：Text-constructor 13F→**15P** + Comment-constructor 13F→**15P** + CharacterData-surrogates 6F→**8P**（三文件 100%）；nodes 7599→**7631P（+32）**/native 6112→**6144（+32 同步）**；traversal/events/collections 不变。`make test` 65 套件全绿 exit 0；fmt/clippy 干净；engine 单测 `test_text_comment_constructors_and_surrogates_r121`（6 断言组）；账本 + evidence r121。**教训**：wire 层编码边界的「不可达」语义（孤立代理）可用 JS 侧覆盖缓存局部绕过——渲染侧接受降级（FFFD 对渲染无意义）而 API 语义面保真，是深结构约束下的最小成本路径。

**本轮**: R120 — **M4 nodes：getElementsBy* 族 NS 感知匹配（四文件全 100%，+48 净双路径）**（commit `efe3be11a`，rebase over 渲染流）：六层修复。① `_zwFilterByTagNameNS` 统一匹配器（非 NS：qualifiedName 双方 ASCII 小写[HTML ns]/原样精确[非 HTML ns] + **uppercase-localName never-matches**；NS：localName 原样精确 + ns 先行匹配['*' 任意/null 匹配无 ns/精确串]）② `_zwDocAllElements` document 级枚举（快照 '*' ∪ pending 动态子 + R54 in-doc 门；快照不支持时回落 html/body 子树展开且与 pending 并存——首版回落被 pending 独占 A/B 抓到）③ live collection 三入口 liveSpec 接线 + `_zwHCLiveInvalidate` **作用域放行**（scopeHandle/scopeSel——detached 容器 element 级集合按容器放行，文档级维持 in-doc 门）④ named 暴露不对称（ownKeys/namedFor/prototype namedItem 三处：**id 全元素、name 仅 HTML ns**——WPT own-props z/w 双向期望）⑤ NodeList/HTMLCollection 构造器 + prototype item/namedItem/length（expando 断言 fn === HTMLCollection.prototype.item 要求 identity——p 层转发同一函数不定义副本）⑥ **tagName Unicode 大写化 bug**（createElementNS(HTMLNS,'ä') 的 .toUpperCase() 把 'ä'→'Ä'，spec 是 ASCII uppercase——case.html non-ASCII 2F 根因）。**A/B**：四 driving 用例 45F→**0F 全 100%** + case.html 283→285P；nodes 7551→**7599P（+48）**/native 6072→**6112**；traversal/events/collections 不变（**中途回归 9F 当轮修平**：python 大段替换误删 p 层 Symbol.iterator/toPrimitive + prototype namedItem 缺空串早退 + r54 pending 缺 in-doc 门 + r12 旧断言按 spec 纠正[null 只匹配无 ns]）。`make test` 65 套件全绿 exit 0；fmt/clippy 干净；engine 单测 `test_get_elements_by_tag_name_family_r120`（13 断言组）；账本 + evidence r120。**教训**：① python 大段替换前必须核对切除边界（Symbol 定义块误删由 A/B 门当场抓回）② null-ns 匹配、id/name 暴露不对称、uppercase-never-match 三处与直觉 spec 读法相反——WPT 期望表是唯一仲裁 ③ live collection 的「作用域」与「文档级」是独立观察面，in-doc 门对文档级是防泄漏、对容器作用域是误伤。

**本轮**: R119 — **M4 nodes：prepend/replaceChildren handle 容器路径（双用例 100%，+25 净双路径同步）**（commit `b27d95c78`，rebase over 渲染流）：四层修复。① `_prependHandleVariadic`（part05）——handle 容器（createElement/DocumentFragment/cloneNode）prepend 此前无实现（仅 sel-based，用例族 undefined 崩）：registry 头插保持参数序（物化后逆序 unshift）+ R101 全 handle wire + fragment flatten + 移动语义 + WebIDL null 文本。② replaceChildren handle 分支——清空（`_zwRemoveHandleNode`=registry+反链+`__zw_remove_handle`）+ 移动记账（**旧父每子一条 removed record、本容器单条合成 record**——spec replace-all 粒度分叉，首版逐子 notify 被 move-order 用例 A/B 当场否决）+ MO/CE 传播。③ detached doc replaceChildren 三缺口——firstChild-while 清空（旧快照循环状态分裂漏删）+ 校验后置（whatwg/dom#1045）+ 字符串参数抛 HRE；附带 appendChild 补 fragment flatten。④ doc prepend 补 doctype-vs-doctype 校验（pre-insert 步骤 6 II）。**A/B**：prepend 8F→**21P（100%）** + replaceChildren 13F→**29P（100%）**；nodes 7526→**7551P（+25）**/native 6047→**6072（+25 同步）**；traversal 1595/events 419/collections 48 双路径零回归（per-case 26F→P 零新增 fail）。`make test` 全绿 exit 0；fmt/clippy 干净；engine 单测 `test_prepend_replace_children_handle_paths_r119`（9 断言组）；账本 + evidence r119。**教训**：① replace-all 的记账粒度按观察面分叉——旧父 observer 收逐子 remove、新父 observer 收单条合成 record，统一粒度必错一方 ② 探针顶层 const 跨 script 不可见是 R3258 既有设计（try 包装块作用域），探针一律单 script + var ③ handle 容器族 API 的「静默 no-op」缺失（非 TypeError）是 polyfill 桥的系统性盲区——sel-based 分支存在让用例在主文档路径通过、detached 路径全灭。

**本轮**: R118 — **M4 nodes：querySelector CSS 转义（ParentNode-querySelector-escapes 17P→64P，+47 净双路径同步）**：四层修复全在 part05 客户端选择器解析（CSS Syntax 4.3.4 consume-escaped-code-point）。① `_readCompoundToken` 转义解码（上一轮 429 中断的 WIP 收口）：hex 1-6 位 + 可选单空白终止符（CRLF 整体一个）→ 码点解码（零/越界/孤立代理 → U+FFFD）、`\`+任意字符字面转义、EOF 反斜杠 → U+FFFD，返回 `{raw,value}` ② `_splitComplex` 转义感知——hex 终止符/转义分隔符不再是组合器边界（旧把 `\30 next` 切两段，解码层收不到完整形态）③ **JS `/\s/` ≠ CSS 空白**——CSS 空白仅 5 ASCII 字符，U+2003/U+00A0 是 ident 字符（`# ` 直接可查），`_splitComplex`/`_readCompoundToken` 两处改 `[ \t\n\f\r]` ④ `_splitSelectorListOf` 转义感知（`\,` 非组边界）。**A/B**：escapes 17P/51F→**64P/4F**（polyfill/native 同步 +47）；nodes 7479→**7526P**/native 6000→6047；traversal 1595/events 419/collections 48 双路径零回归（per-case 唯一 diff 为 escapes 簇 + create-element-realm-after-adoption 既存 flake——HEAD 同败 3/3 单跑必败，`_zwMakeIframeWin` 无 customElements，per-realm CE registry 深结构）。**剩 4F 记深结构**：孤立代理 id never-match ×2 + 字面 NUL selector ×2——wire 协议 `to_rust_string_lossy`（v8 crate wtf16_to_string）在 JS→Rust 边界必然 FFFD 替换，需 WTF-16 wire 保留，非本切片可达。`make test` 全绿 exit 0；fmt/clippy 干净；engine 单测 `test_query_selector_css_escapes_r118`（13 形态）；账本 + evidence r118。**教训**：① raw string 内嵌 JS 有四层转义（Rust raw → JS 源级 → JS 值 → CSS 解码值），`\30` 在 JS 非严格串是八进制转义——selector 传值须双反斜杠、控制字符 fromCharCode 构造 ② engine 单测环境（register_dom_callbacks 直注）存在位置相关状态差（同构造序列后段 HIT/wpt-runner 稳定 Pass）——shim 函数层用 node 提取验证是快速仲裁手段 ③ JS 正则 `\s` 的 Unicode 空白语义与 CSS 空白集的差异是选择器解析的系统性坑。

**本轮续117**: R117 — **M4 nodes：ChildNode/ParentNode mutation 族（+113 净）**：七件 + 三次回归修正。① `_zwDetachFromRegistry`（spec pre-insert 先移除旧父——移动非复制）② before/after 重写为 **spec viable-sibling 顺序 pre-insert**（固定 ref + 逐参数移动；三缺口闭合：残留复制/after(self) 语义/顺序保持——真实 DOM `[x,c,y].before(y,x)`→`[y,x,c]`）③ replaceWith handle 路径（self 作参数 = 移动重插，末段移除被抵消）④ 层级校验族（祖先环 + Document 收 Text·CDATA·Document 拒 / **PI/Comment 可进 Document**——第一版误拒 7/8 致 NodeIterator PI/comment 回归）⑤ **Node.prototype 泛型变异族**（replaceChild/insertBefore/removeChild——校验顺序 spec 对齐 parent-type→ancestor→NotFound→node 类型；doctype/text/PI/CDATA 原型链接）⑥ null/undefined → WebIDL 文本转换（append(null)→'null'）⑦ detached doc/主文档/_zwMEl 补齐变异族全家族。**三次回归修正（lenient 收窄）**：removeChild NotFound 校验回退（sel 父融合视图不完整——NodeIterator-removal 14F）+ element replaceChild NotFound 回退（webview 集成 pending 子）+ 泛型 insertBefore lenient（browser blank 页加载）——**教训：JS 侧融合视图非权威，NotFound 严格校验挂原型泛型层、trap 层 lenient，L2 后收口**。**A/B**：ChildNode 三文件 **123P/0F（100%）**、Node-replaceChild 9→30P、Node-removeChild 0→12P（余 frames 域）、dom/nodes 7366→**7479P（+113 净）**/716F；events 419P/collections 48P/traversal 1595P·10F 零回归（中途回归修平）。`make test` **18,244P/0F**（exit 0，webview 集成 + browser IndexedDB 回归修正后复验）；fmt/clippy 干净；engine js_dom_bridge 604 全绿；evidence r117。

**本轮续111**: R110 — **M4 events 续作：init-while-dispatching + legacy initXxxEvent 族三簇 100%**：**① initUIEvent/initMouseEvent/initKeyboardEvent 方法补齐**（shim 此前只实现 initEvent/initCustomEvent，WPT 三 subtest `is not a function`）——legacy 位置签名、基类字段经原型链 initEvent 复用；**② 派发中 init 全 no-op**：spec 各 init 方法「dispatch flag set → return」，R106 已建 `_zwDispatching` 计数，本切片把它接进 initEvent/initCustomEvent 头部 + 三个新方法（五入口全接线，R106「单一 helper 全入口」教训落地）；**③ init 首参 mandatory**（`e.initEvent()` 抛 TypeError，WebIDL 位置参数 non-optional）；**④ CustomEvent detail 缺省 null**（`_makeEvent` 落 undefined 是 Event 语义，spec CustomEventInit `detail:any=null`，构造 + init 双路径补）。**A/B**：init-while-dispatching 0P/5F→**5P/0F** + 连带 Event-initEvent 9P/3F→**12P/0F** + CustomEvent 1P/2F→**3P/0F**（三簇 100%）；events polyfill 363→**373P（+10 净）**/native 354→**364P（+10 净同步）**；nodes 6656/collections 48/traversal 1595 不变。`make test` 65 套件全绿（v8+quickjs）；fmt/clippy 双矩阵干净；账本 + evidence。**M4 events 六轮累计（R105-110）：236P→373P（+137 净）**。

**本轮续109**: R109 — **M4 events 续作：Event-subclasses-constructors 双路径 100%**（commit `74a04ccc9`）：**Event 构造器真 [[Construct]] 化**——`class X extends Event` 的 super() 在旧「工厂返对象」形态下拿不到 this（子类 ctor 体 `this.customProp=5` 抛 TypeError、instanceof 子类 false、子类 getter 失效），改为 new.target 派发 proto + **整对象搬运**（`getOwnPropertyNames` + descriptor 逐个 defineProperty——首版 for-in 漏非枚举 accessor 致 cancelBubble 8P→0P 回归，A/B 门当场抓到同轮修）。**UIEvent view 类型校验**（非 null/undefined 非 globalThis 抛 TypeError，WebIDL dictionary 校验，WPT "view argument with wrong type"）。**native 叠加路径原型链接线**（双路径差 6.12pp 主成分）：ZW_NATIVE_DOM=1 下 native bindings 先装 MouseEvent/KeyboardEvent（V8 FunctionTemplate），shim 后装覆盖 Event/UIEvent——guard 早退留下的 native 子类模板原型链指向已替换的 native Event 模板 → `instanceof UIEvent/Event` 全断；修 = prototype 重接 shim UIEvent 链 + `_eventSubclassProps` 注册表补登（WheelEvent 父链 props 收集断链根因）+ KeyboardEvent 缺省字段 wrapper（含 native init_string 的**字符串 "undefined"** 伪装形态）+ native event.rs 补 KeyboardEvent detail。**A/B**：polyfill events 356→**363P（+7 净）**、native 337→**354P（+17 净，双路径差 6.12pp→~2.5pp）**、簇 42P/7F·native 31P/18F→**双路径 49P/0F（100%）**；nodes 6656/collections 48/traversal 1595 不变；cancelBubble 8P/returnValue 7P/initEvent 9P 回归验证同值。`make test` 65 套件全绿（v8+quickjs）；fmt/clippy 双矩阵干净；账本 + evidence（md+json）。**教训**：① 「工厂返对象」构造器对 `class extends` 是结构性破坏——shim 内所有可被 extends 的构造器都须过 new.target 检查 ② 对象搬运禁 for-in（漏 defineProperty 非枚举 accessor）③ native 叠加路径的原型链是两个世界的接缝，V8 模板链在 shim 覆盖全局后成孤儿——重接 prototype 是最小修法。**M4 events 五轮累计（R105-109）：236P→363P（+127 净）**。

**本轮续108**: R108 — **M4 events 续作：Event-dispatch-click 16F→100%**（commit `02c34ac38`）：合成 click 的 **pre-click activation**（spec `concept-event-dispatch` legacy-pre-activation 行）落地——`_zwPreClickActivation` helper（target 起沿 sel 链/`_zwNodeParent` 反链上行找**最近** INPUT[checkbox/radio]，checkbox 翻 checked / radio 勾选+同 name 组互斥）接入 `_dispatchWithBubble`（仅 MouseEvent 实例或 `click()` 合成标记——`new Event('click')` 不激活）。**disabled 双入口语义**：`click()` 对 disabled 不激活不上行不派发 listener；`dispatchEvent(new MouseEvent)` 仍激活但抑制 disabled target 的 listener。**legacy-canceled-activation**：cancelable + listener preventDefault → dispatch 结束回滚 checked（账经全局 pickup 挂 event——helper 无 event 参）。**Text 是 EventTarget**：`_zwRegisterTextEl` 包装与 `Text.prototype` 都补 dispatchEvent（guard + 委托父链冒泡）。**submit 按钮的 form owner 反链兜底**：handle 子树 `.form` getter miss 时经 `_zwNodeParent` 上行找 FORM 触发 onsubmit。**过程两坑（都当场探针定位）**：块手术吞 `else` → shim 整体 SyntaxError（node --check 提取 helper 速检）；rollback 账写在无 event 参数的 helper 内 → TypeError。**A/B**：events 341→**356P（+15 净）**全部差异限于本簇；nodes/collections/traversal 不变。engine v8 **2211（+1）**/quickjs 1431/integration 784/webview 605 全绿；fmt/clippy 双矩阵干净；账本记录。**教训**：① disabled 语义按入口分（click() vs dispatchEvent）——同「disabled」两用例期望相反，逐用例对 spec ② JS shim 的结构性改动必须语法自检（node --check 提取片段）再跑 e2e ③ engine 沙箱与 runner 的环境差（Event instanceof 面/Text 接链时机）使部分断言只宜在 WPT 层守。**M4 events 四轮累计（R105-108）：236P→356P（+120 净）**。

**本轮续107**: R107 — **M4 events 续作：Body-FrameSet-Event-Handlers 24F→100%**（commit `ea96ab64c`）：spec HTML `handler-body-attributes`/`handler-frameset-attributes` 落地——body/frameset 的 6 个 forwarding handler（onblur/onerror/onfocus/onload/onscroll/onresize）**IDL 与 content attribute 均转发 window 同名 handler**，三面实现：① **IDL 转发**：元素 proxy get/set trap 对 BODY/FRAMESET tag 的这 6 个名先路由 `window.on<type>`（先于 generic on* 分支；非 function 写 null 化 window）② **content attribute 转发**：`setAttribute('on<type>', code)` 即时编译 inline handler（`_ensureInlineHandler` 缓存逐写失效）并赋 `window.on<type>`——Forward 用例**直读 window** 不经 element getter；element getter 的 forward 路径也懒编译（Reflect 用例）③ **枚举面**：`HTMLBodyElement.prototype`/`HTMLFrameSetElement.prototype` 补 6 个 enumerable data 属性（值 null，for-in 可见；proxy get trap 仍先拦截读值）。**A/B**：events 317→**341P（+24 净）**全部差异限于本簇（24 条 Fail→Pass）零非簇回归；nodes/collections/traversal 不变。engine v8 **2210（+1）**/quickjs 1431/integration 784 全绿；fmt/clippy 双矩阵干净；账本记录。**过程教训**：跨函数作用域变量引用（`nLower`/`targetTag` 属 removeAttribute 作用域）在插入 setAttribute 分支时两度踩空——插入点变量须当场核实作用域；转发语义的三面（IDL/content/枚举）是独立观察面，逐面修（Set 先过 → Reflect → Forward → Enumerate）。**M4 events 三轮累计（R105-107）：236P→341P（+105 净）**，events 通过率 62.4%→79.6%。

**本轮续106**: R106 — **M4 events 续作：EventTarget-dispatchEvent 24F→1F**（commit `514ba49d7`）：spec `dom-eventtarget-dispatchevent` 四入口语义落地——① **dispatchEvent(null/非 Event) 抛 TypeError**：新 `_zwDispatchGuard` helper（part05）接入全部四个 dispatch 入口（window/document/元素 proxy/EventTarget.prototype——后者旧 lenient「构造空事件」回落删除）+ `_zwMEl` 本地面同款 ② **createEvent 未 initEvent 派发抛 InvalidStateError**：createEvent 返回的事件带 `_zwUninitialized`，initEvent 清除（构造器路径 new Event() 即初始化不受影响）③ **派发中重入抛 InvalidStateError**：`_dispatchWithBubble` 入口/finally 维护嵌套安全 `_zwDispatching` 计数（与其他 dispatch flag 复位同处）④ **listener 异常不传播**：`fire()` catch + console.error 上报 + 继续后续 listener（dispatchEvent 返 true）。**A/B**：events 293→**317P（+24 净）**全部差异限于本簇（per-case 核对零非簇回归）；nodes 6656/collections 48/traversal 1595 不变（nodes -2F 为 2 条既存文件级 flake 消退）。engine v8 **2209（+1）**/quickjs 1431/integration 784 全绿；fmt/clippy 双矩阵干净；账本记录。**剩余 1F 记深结构清单**：「listeners added during dispatch」需 **detached handle 树祖先链派发**（append 建的 b>b>b 无 sel 链，`_dispatchWithBubble` 对 handle-only target 仅 target 站派发）——与 R4/M1 L2 的 handle 树限制同族。**教训**：入口守卫类语义（TypeError/InvalidStateError）应做**单一 helper + 全入口接线**——四个 dispatch 入口 + 一个本地事件面，逐点散写必有漏。

**本轮续105**: R105 — **M4 events 最大簇修复：passive-by-default 57F→100%**（commit `a8d5182cd`）：dom/events 最大失败簇（passive-by-default.html 57F）全修——**57F→100P/0F（100%）**，events 总数 236→**293P（+57 净）**。根因：spec HTML `default-passive-value`——touchstart/touchmove/wheel/mousewheel listener 在 **window/document/documentElement/body 四类 target** 注册且未显式指定 `passive` 时默认 passive:true，passive listener 内 `preventDefault()` 是 no-op（in passive listener flag，invoke 步骤 8）。**实现**（shim 三层）：① `_optPassive`/`_listenerPassiveDefault` helper（显式 opts 优先，缺省按 target+type 查 `_ZW_PASSIVE_DEFAULT_TYPES` 表）② 三注册点（window `_globalAddEventListener`/document part06/元素 part04——body+html sel 是 pd-target）entry 加 `passive` 字段 ③ `_dispatchToListeners` 的 `fire()` 用嵌套安全计数器 `_zwInPassive` 包裹调用，`preventDefault` 与 `returnValue` setter 检查 flag。**诊断关键**：首轮修完剩 8F 全是「for Object」形态——`constructor.name` 读 Object 的 target 是 **documentElement**（html 元素 proxy 无构造器名），补 `sel === 'html'` 判定后全绿。**A/B**：events +57 净全为 passive Fail→Pass（57 条 diff 逐条核对零非 passive 回归）、nodes 6656/collections 48/traversal 1595 逐字节一致。engine v8 **2208（+1）**/quickjs 1431/webview 605 全绿；fmt/clippy 双矩阵干净；driving 用例记入 imported-tests.txt 账本。**教训**：WPT 用例名里的 `constructor.name`（如「for Object」）是识别 shim 侧 target 身份的线索——documentElement 在 shim 无 HTMLHtmlElement 名以 Object 呈现，首轮四类 target 漏了它。

**本轮续104**: R104 — **M1 L2 收口：三方合一验证资产落地**（commit `05fe9db29`，纯测试资产零生产代码）：`l2_three_way_write_paths_converge_r104`（tests_ab_compare）——同一 DOM 写序列（setAttribute / create+append / remove）经 **A（native：install_dom_bindings 直改 DOM_SOURCE）** 与 **B（polyfill：__zw_* 收集 DomMutation → apply_dom_mutations）** 两条路径后的 **C 侧（live doc 最终 outer_html）逐字节相等**——**一次通过**，是 DC-1「native(A)=polyfill(B)=renderer(C) 三方合一，无独立快照」的直接行为资产（读路径等价自 R0 起由 READ_CASES 守）。新 helper：`run_script_return_doc_html`（tests.rs，reset 前序列化 live doc）+ `run_polyfill_applied_html`（mirror webview `apply_pending_shared_mutations` 机制）。**附带盘点（L3/S7 路线图输入）**：`cached_html` 快照依赖面 = webview.rs 内 24 处 self 引用（load_html 赋值/apply 后同步/render 重入/execute 的 dom_html Arc 源）+ renderer/browser 消费面（apps/renderer/main.rs、apps/browser/tab_scripts.rs 等 6 文件）——L3「移除 cached_html String 路径」的萎缩顺序应从**消费方改读 live doc 序列化**开始（webview pub API 返 html 的调用方逐步换）再到 webview 内部。engine v8 **2207（+1）**；nodes 6656P per-case 与 R103 逐字节一致；fmt/clippy 干净。

**本轮续103**: R103 — **M1 L2 续作：helper 类查询 live 化收口**（commit `2d1be48ef`）：R102 留下的三处快照路径 helper——`query_match_in_subtree`/`query_all_in_subtree`（元素级 querySelector(All) 子树作用域）+ `form_control_selectors`（form.elements）——各加 `_doc` 变体（R52 模式：html 版薄封装 parse_html，doc 版纯逻辑），回调经 `with_query_doc_live_aware(&snap, live_ok, ...)` 走 live 读。**至此全部 `__zw_*` 查询类回调在 live 发布且无 pending structural mutations 时直读 live Document**（L2 读路径完整覆盖）。**A/B**：四子目录（nodes 6656P/traversal 1595P/collections 48P/events 236P）与 R102 per-case **逐字节一致**零回归；engine v8 2206/quickjs 1431/webview 605/integration 784 全绿；fmt/clippy 双矩阵干净。

**本轮续102**: R102 — **M1 L2 首切片落地：查询回调 live Document 读路径**（commit `899b5e9d3`）：`__zw_query_match`/`__zw_get_attr`/`__zw_child_nodes` 等查询回调可直读 pipeline live `cached_doc`——**L2「polyfill 桥读 live Document」的首个 land 点**。机制：新 thread-local `LIVE_QUERY_DOC`（`Rc<RefCell<Document>>`，镜像 dom_bindings gc.rs DOM_SOURCE 模式），webview 三注册点（run_page_scripts / execute_script_with_dom / dispatch_event）从 `pipeline.cached_doc_shared()` 发布、`load_html` 发布 None；`apply_pending_query_html` 返回值扩 `(view_html, live_ok)`——live_ok = 无 pending structural（InsertAdjacentHtml）mutations 时 `with_query_doc_live_aware` 经 `try_borrow` 直读 live（借用冲突/未发布回落快照路径——engine 测试与 reftest 等直接调用方零行为变化，渐进 adoption）。**行为契约单测**（part18 `test_live_query_doc_read_path_r102` 三断言组）：publish 后 live 独有元素可见 + None 后回落快照 + **R57 同批 insertAdjacentHTML 可见语义保持**（pending 路径绕过 live）。**A/B（clean-HEAD 重建二进制）**：nodes **6656P per-case 逐字节一致**、traversal 1595/collections 48/events 236 不变；engine v8 **2206（+1）**/quickjs 1431/webview 605/integration 784 全绿；fmt/clippy 双矩阵干净。**性能注记**：Range 慢用例（insertData 4.2s=4.5s、appendChild 8.3s=8.3s）无提速也无回归——R52 查询缓存后其瓶颈不在查询 re-parse；live 读消除的是**每 execute 后快照变化导致的 QUERY_DOC_CACHE miss 全量 re-parse**（后续 L2 写路径切片与高频查询调用方的收益面）。**教训**：① live 读正确性条件 =「无 pending structural mutations」+「apply 后 cached_html 与 live 同步」（webview apply 流程保证）——两个不变式都要显式编码（live_ok flag）不能隐式依赖调用顺序 ② FnOnce 闭包双路径消费用 `Option::take` 包装（try_borrow 回落路径与 live 命中路径恰用一次）③ 批量 sed/python 替换须按「调用点上下文」分组校验（首轮把无 live_ok 的 10 处误改成 true、又把有 live_ok 的 9 处覆盖——双向错误，靠编译错误+逐点核对收口）。

**本轮续101**: R101 — **M3 续作：Vue reconciliation 深场景落地 + QuickJS 矩阵全量核实**（commit `776fca842`）：① **QuickJS 矩阵核实**：Vue e2e 双组 + lit/WC 14 组在 `--features quickjs` 下全绿（R78 execute 全局语义 + R100 桥层修复双引擎通用——M3 验收页两 feature 均通过，DC-2 双引擎验证实质证据）② **reconciliation 断言组 C**：v-if 翻转卸载（`cond:yes`→`cond:null`）+ keyed v-for 中间插入（`lis:A,B`→`lis:A,C,B`）+ 清空（`lis:0`）——组件树结构性 diff 非同元素文本 patch，DC-2「非仅静态渲染」深验收达成。**根因**：shim insertBefore 的「handle 父 + handle ref」分支（R97）只做 JS registry splice **不发 host mutation**——此形态挂接的节点 JS 视图可见、host 文档永久缺失（innerHTML 空、querySelectorAll 全 miss）。Vue v-for 的 li 挂接正是此形态（anchor + 父都是 createElement handle，无 selector 可翻译）。**修复**：新 `DomMutation::InsertBeforeByHandleHandle`（父/子/ref 全 handle）+ `__zw_insert_before_handle_handle` 回调 + shim R97 分支补发；apply 三 handle 经 ephemeral/persistent 表解析，ref miss 降级 append（insertBefore(node,null)==appendChild 语义）。**诊断链**（三层 trace 定位，全部已删）：host 回调 trace（li createElement 到达但无挂接 wire）→ JS 侧 insertBefore trace（捕获精确调用形态 `parent=__n1 ref=__n3`）→ apply 侧 trace（NodeId 解析成功）→ 最终发现首探针「innerHTML 空」部分是**同 execute 读数时机**（host mutation 在 execute 结束才应用），post-drain 读数修复后全通。**A/B（clean-HEAD 重建二进制）**：nodes **6659P/1563F per-case 逐字节一致**（两次重跑亦逐字节一致）、traversal 1595/collections 48/events 236 不变；engine v8 2205/quickjs 1431/integration **784（+1）**/webview 605 全绿；fmt/clippy 双矩阵干净。**flake 处置**：首轮全目录跑 Node-insertBefore 文件级 Fail（1 条粗 Fail 替代 40 细分）——3 次复跑未再现 + 两次全量逐字节一致，判定 flake 非回归（若再现按 R99 flake 池流程归因）。**教训**：① 诊断「innerHTML 空」类分裂先区分**同 execute vs post-drain 读数时机**再查桥层 ② 全 handle 形态（无 selector 可翻译）是 detached 树操作的真实主流形态——Vue/Preact 的 fragment/anchor 挂接全走它，wire 缺失是静默丢失非显式错误 ③ 全目录跑的单次文件级 Fail 须复跑 3 次再定性（本目录 flake 池既存）。

**本轮续100**: R100 — **M3 里程碑时刻：真实 Vue 3 端到端落地（DC-2 第一项「SPA 框架」实质达成）**——**R101 补充（同 session）：QuickJS 矩阵核实 Vue e2e 双组 + lit/WC e2e 14 组全绿**（`cargo test -p zero-integration-tests --no-default-features --features quickjs`——R78 execute 全局语义 + R100 桥层修复对双引擎通用，M3 验收页在两 feature 下均通过，DC-2 双引擎验证实质证据）。——vendored `vue@3.5.13` global build（jsdelivr 快照，MIT，~550KB，`globalThis.Vue` 消费）+ 两组 e2e：**组 A mount 落地**（`createApp({data,template}).mount('#app')` → 模板编译 `{{ msg }}` 插值 → post-flush `html:<p class="msg">Hello Vue!</p>` 真实 DOM 断言）+ **组 B 响应式+事件**（`@click="inc"` → dispatch click post-drain → `t0:0` → `text:1`——handler 跑 + count 更新 + patch 重渲染 commit）。**四层根因修复**：① `generate_dom_api_polyfill` 每次 execute 覆写 `globalThis.document`（空 stub）——改幂等安装（`__zwShimInstalled` 标记不覆盖）② `execute_script_with_dom` 不收集/不应用 DOM 变更——镜像 run_page_scripts 机制（注册回调→执行→apply），**共享 append-only mutations 队列**（三注册路径同一 Arc，读回调看全量历史，apply 取游标尾部）③ **跨 execute handle 身份**——HANDLE_COUNTER thread-local 单调（旧版每次注册从 0 重启致 `__n0` 碰撞、lit awaited continuation 错挂旧 registry）+ `selector_handle_map` 持久反查（apply 后 merge handle→selector，query 返回点 `_zwQueryWrapIdentity` 命中返回原 handle proxy——Vue `vm.$el` 与 `querySelector('button')` 同一 identity，@click invoker 可达）+ `MUTATION_HISTORY` 线程本地已应用历史（读回调跨注册 latest-wins 回落）+ `apply_dom_mutations_full` 持久 handle 解析（NodeId 直达预植 + selector 翻译重入队——跨 execute 旧 handle 的写 mutation 不再 `unknown handle` 硬错）+ pipeline `persistent_handle_nodes`（跨 apply handle→NodeId，render_html 换代抢救重锚定）④ SVG 接口构造器 ~36 个 stub（Vue resolveRootNamespace 读 `SVGElement`——ReferenceError 使 mount 中止）。**顺带修复**：cloneNode deep 对 handle 容器递归复制 `_handleChildren`（nodes `Node-cloneNode "node with children"` Fail→Pass）、replaceChild handle-handle 形态 JS registry 原位替换、结构伪类白名单解析+求值（first/last/only-child、nth(-last)-child、empty、not、checked——detached 容器查询）、纯文本 innerHTML 不入 `_handleChildren`（textContent 双计）。**装载方式发现**：bundle 不 inline——HTML tokenizer script data double-escaped 状态吞 `</script>`（spec 行为，真实部署一律外链；装载序 = onerror 捕获脚本→bundle→页面脚本）。**A/B（clean-HEAD 重建二进制）**：nodes **6568→6569P（+1 零回归；master.md 旧记 6673 为 R99 时点基线，并行流 landing 后基线漂移——name-validation 输出含二进制字符致 grep 计数噪声已核实排除）**、collections 48=48、traversal 1595=1595、events 236=236 per-case 逐字节一致。engine v8 **2205**/quickjs **1431**/integration **783（+2 Vue e2e）**/webview v8 **602**/quickjs **555** 全绿；fmt/clippy 双矩阵干净；product-smoke 23.61% = clean-HEAD 同值（**ZRG-2026-08-17-01 已归因渲染流 hmtx 既存红灯，oracle re-capture 属用户决策，非本切片回归**）；bench-gate FAIL 10 项**全部落在 CI-GUARD-20260818 已记录预存漂移签名内**（stroke_rect ~16×/worker_create_terminate ~12×/csp ~3.9×/canvas·engine paint 族 1.4-1.7× runner 漂移 + NEW=1 为 render-foundation raster 指标）——**本切片改动面（webview_bench 8 指标 + page 系 13 指标）全 PASS**，非本切片回归（run-rules §12 禁自主 relax，re-capture 属 infra/用户决策）。**教训**：① execute 路径的「静默丢弃」类 bug（回调未注册）比显式错误更隐蔽——Vue mount 后 host 侧全 miss 无任何报错 ② 跨 execute 元素 identity 是 SPA 事件链的命门——selector↔handle 双向持久表是 polyfill 架构下的最小可行形态 ③ stash A/B 必须重建二进制（R79 教训第四次实证）。

**本轮续99**: R99 — **M3：lit 事件链落地 + CI flake 池 ⑤ 修复（WC 端到端全链闭环，DC-2 第二项实质达成）**（commit `bfb64b2aa`）：验收 `btn:BUTTON|t0:0|dispatch:true` → post-drain `t1:1|count:1`——`@click` 绑定（EventPart addEventListener on 解析节点）→ dispatch → handler `_inc` → count+1 → 响应式重渲染按钮文本，lit 组件**渲染/响应式/事件交互**三段全通。**双根因**：① `_zwMEl` 解析节点缺 addEventListener/removeEventListener/dispatchEvent——lit EventPart 的 `_$AI` commit 调 `this.element.addEventListener(name, this, t)`，缺方法使 commit 抛 TypeError、**整次首渲染中止**（e2e 实证 renderRoot 仅 marker、hasUpdated:false）；补本地事件面（listener 数组 + handleEvent 协议[lit 传 part 对象] + capture/once 双形态选项 + 注册序派发 + cancelable/defaultPrevented 返回语义）② **附带修复 CI flake 池 ⑤**：part06 顶层无条件 `setTimeout(0)`（R34xx fonts settle 兜底）每次 shim 注入在 `__zw_pending` 注册瞬时 `_t_` 键与 renderer reset 断言竞态（8/16 起 3+ CI 轮次，守护记录归因完整）——`fonts.ready` 改 thenable 包装（消费首调时 arm 一次 fallback），无 font 消费页面零注册、语义零损失；renderer reset 测试本地 **20/20** 通过。**A/B**：nodes 6673=base（0 新增 fail + 22 条既存 flake 消退）、collections 48、traversal 1595、events 236。engine v8 **2205**/quickjs **1431**/integration **781**/renderer 134 全绿；`make test` exit 0；fmt/clippy 双矩阵干净；pre-commit-guard PASS。**教训**：框架的 listener 形态是对象（handleEvent 协议）——事件面不能只支持 function；shim 顶层无条件宏任务注册是 reset 类测试的系统性竞态源——惰性化（消费时 arm）是通用修复模式。

**本轮续98**: R98 — **M3 续作：lit 响应式更新链落地（define observedAttributes + CE accessor 派发）**（commit `d90be269d`）：三段式验收 MID`[t1:Hello, ZeroWeb!|uc-changed:true|pending-at-set:true]` POST`[t2:Hello, Updated!!|pending:false]`——`el.name = 'Updated!'` → accessor setter → requestUpdate → 二次 render → ChildPart 文本 commit，Web Components 响应式闭环达成（lit e2e 组 E 新增，全组 5/5）。**三重根因**：① **`customElements.define` 不读 `ctor.observedAttributes`**（spec define step 5 的 Get）——lit 静态 getter 内调 `finalize()` → `createProperty` 装 prototype accessor；不读则 accessor 从未安装（实证 `getOwnPropertyDescriptor(GreetingEl.prototype,'name')===undefined`，property set 永不触发 requestUpdate）② set trap 不派发原型链 accessor setter（R93 只做了 get 镜像）——lit setter 内 `this.requestUpdate` 不执行（uc-changed:false）③ **symbol-keyed 写丢失**（lit fallback `this[s]=v` 以 Symbol 存值被 `String()` 后写 host attr → 值丢、首渲染插值空）+ 首层 accessor getter 优先级（真实 DOM 原型链序用户 prototype accessor 先于 HTMLElement 反射 getter——shim 反射分支先吞）。**A/B 捕获回归同轮收窄**：初版派发对全部元素生效破坏 `Element-getElementsByTagNameNS('*','body')`（普通 div）+ HTMLCollection expando 1 例——**CE-only 收窄**（首层原型 constructor 须 `getName` 命中 registry）后两处归零、非 CE 元素零路径变化。**A/B**（clean-HEAD 二进制）：nodes 6673=base（per-case 一致 + name-validation 4F flake 消退）、collections 48=48 per-case 一致、traversal 1595、events 236。engine v8 **2205**/quickjs **1431**/integration **780** 全绿；`make test` exit 0；fmt/clippy 双矩阵干净；pre-commit-guard PASS。**教训**：get/set trap 原型链派发必须 CE-scoped（过宽派发改变方法读取路径，A/B 是唯一安全网）；响应式链分两段断言（set 时点同步观察 + post-drain 异步观察是独立面）；define 的 observedAttributes Get 是 lit finalize 唯一触发点。

**本轮续97**: R97 — **M3 里程碑时刻：lit 首渲染落地（R95 异步 update 链阻塞全链打通）**（commit `42f303db2`）：post-drain 验收 `pending:false|hasUpdated:true|rr-kids:2|p-tag:P|p-class:greet|p-text:Hello, ZeroWeb!`——真实 lit 库 LitElement 组件首渲染 DOM 落地 shadow root，`performUpdate → render → lit-html render() → Template 构建 → importNode → TreeWalker parts 提取 → insertBefore commit` 全链贯通。**四重根因**（staged probes 逐步收窄，全部已修）：① **TreeWalker currentNode 跨树重定位失效**——lit-html 用**单个全局 TreeWalker**（root=document）经 `P.currentNode = fragment` 重定位遍历 template parts（框架级 TreeWalker 消费模式，WPT 覆盖不到）；shim order 快照不含 root 外节点 → 重定位后 nextNode 仍从 document 头走（探针实证 `walk:HTML>HEAD>BODY>DIV>SCRIPT`）。修复 `nextNodeOffOrder()` 导航式步进（firstChild/nextSibling/parentNode 状态机；上行后 descend=false 防重入子树死循环；REJECT/0 剪枝按 walker 类型分叉）② `insertBefore(node, null)` handle 父不记 registry（appendChild 有 `_recordHandleChild`）——marker 插入后容器 childNodes 漏子 ③ **无 handle fragment 视图插入静默 no-op**（首渲染最终根因）——template.content 派生的 imported fragment（nodeType 11 无 `__zwHandle`，子为 `_zwMEl` 解析对象）落到 no-op 分支；appendChild/insertBefore 各加无 handle fragment 展开分支（handle 子记反链、_zwMEl 子直接入 registry、带位 splice）④ `_zwMEl` 缺 `hasAttributes()/getAttributeNames()`——lit Template 属性 parts 提取 TypeError。**A/B 捕获同轮回归**：`insertBefore(b,b)` 自引用（spec no-op）被新分支重复插入——WPT Node-insertBefore "before itself" 捕获后入口早退修复。**验收资产**：`lit_first_render_lands`（新，post-drain 读数）+ `lit_html_render_direct`（新）+ R95 两组件维持 = **4 组件全绿**。**A/B**（clean-HEAD 二进制）：traversal 1593→**1595P**（TreeWalker-currentNode ×2 转 Pass——正是本次语义）；nodes 6673=base + fail **-24**（getElementsByClassName-whitespace 22F 消退）；events 236/collections 48 逐项一致。engine v8 **2205**/quickjs **1431**/integration **779** 全绿；`make test` 65 套件 **18086** exit 0；fmt/clippy 双矩阵干净；pre-commit-guard PASS。**过程发现**：make test 首跑的 product-version 失败为**跨午夜陈旧 rlib**（build.rs 未声明日期 rerun，昨日 26.8.16 残留），`cargo clean -p` 恢复——独立可复现缺陷留档非本切片回归。**教训**：异步框架链验收读数必须在 microtask 排水后（第二次 execute）；导航式步进上行后必须只横移（firstChild 重入 = 死循环）。

**本轮续96**: R96 — **M3 续作：get trap 表查原型链污染收口（R95「第二层缺口」根因闭合）**：**根因三重**——① `_REFLECTED_UINT[prop]` 裸下标查表：`hasOwnProperty`/`valueOf`/`toLocaleString`/`isPrototypeOf`/`propertyIsEnumerable` 经 Object.prototype 原型链返 truthy 函数 → `if (_ruEntry)` 误入 → `parseInt(entry.a=undefined)`=NaN → `return undefined` **提前吞掉**（bisect 实证：midA=8 全过、midB=2——5 个 miss 名消失在 2733-2744 查表区；`constructor` 侥幸因 R95 顶部短路、`toString` 有显式分支）② `_attributesProxy` get trap miss 名无原型链回落（`obj.hasOwnProperty(prop)` 抛 TypeError 整 subtest 崩）③ `_zwMEl`（detached createElement 元素）`.attributes` 裸数组（getOwnPropertyNames 返方法名 + named 语义全无 + setAttributeNS 缺失）。**修复**：①查表改 own-property 判定（六名全可达 + R93 原型链回落走通 pchain0=MyEl）②③见 evidence——`_attributesProxy` 补 Object.prototype 回落 + named descriptor `enumerable:false`（for-in 只见索引，WPT attributes.html 仲裁；R44 断言③按 spec 纠正）+ 新增 `supportedNames()` 双层（HTML 文档+HTML ns 元素 named keys 仅全小写 qualified name；索引语义走全量 readNames——期望 `["0".."5","g:h","j"]` 索引全可达）+ `_zwMEl.attributes` 改 NamedNodeMap 视图 Proxy（length/item/get·set·removeNamedItem/索引/named/ownKeys/gOPD/**has trap** 防 Array 泛型 slice hole）+ setAttributeNS。**结果**：**nodes 净 +4P 零回归**（6654→6658P——attributes.html Own-property×2 + qualified-names×2 + namednodemap 1 解锁）；events/collections/traversal per-subtest **零差异**；passive-by-default A/B 零差异；engine v8 **2192**（+1 R96 单测）/ quickjs **1427** / integration **777** 全绿；fmt/clippy 双矩阵干净。**lit 解锁意义**：`el.hasOwnProperty`/`el.valueOf` 可达 = ReactiveElement 属性初始化面（`this.hasOwnProperty` 探测、Object.prototype 方法以元素为 receiver）不再中断——R95 诊断的异步 update 链阻塞点（`await this._$ES` 永不 resume）中「hasOwnProperty 不可达」层已闭合，剩余为 enableUpdating 原型 noop 读回层（下轮验证 lit e2e 是否落地首渲染）。**教训**：Proxy get trap 内查表必须 own-property 判定（症状远离病因，中点标记 bisect 是长 trap 链定位最快手段）；枚举语义分层（索引可见性 vs supported property names 独立观察面——动 readNames 连锁破坏索引语义，首轮过滤 readNames 致索引丢失 A/B 捕获后拆双层）。

**本轮续95**: R95 — **M3 续作：真实 lit 库 e2e 首切片**：vendored lit（jsdelivr 快照，BSD-3，reactive-element 2.1.1 + lit-html 3.3.2 + lit-element 4.2.1，五模块 bundle 成单一 classic script，`globalThis.lit` API）+ **两个 shim 缺口修复**——① part03 get trap `constructor` 顶部短路（lit `_$E_` 读 `this.constructor.elementProperties`，旧中间分支返 undefined 致 ctor 链中断）② part04 `<template>`.content fragment 视图（lit-html Template parts 管线前置原语）。**验收资产**：`e2e_lit_library` 组 A（bundle 求值 + ctor 桥 + el.constructor===用户类 + instanceof 双层 + lit internals + shadow root）+ 组 B（template.content fragment 语义）。**结果**：integration **777**（+2）/ engine v8 2188 / quickjs 1427 / webview 601 全绿；WPT nodes/collections/traversal 逐字节一致 + **events 净 +46P**（passive-by-default 整用例崩溃→执行 43P/57F、disabled-elements 0P→3P——constructor 修复解锁页面执行）；fmt/clippy 干净；pre-commit-guard PASS。**诊断归档（下一切片输入）**：异步 update 链剩余——`this.enableUpdating`（ctor 内 Promise executor 赋值）读回原型 noop → `await this._$ES` 永不 resume → 首渲染不落地；expando-first 与 define 期 finalize 两尝试均因暴露第二层缺口回退——**`hasOwnProperty` 在 get trap 长链不可达**（bisect：part03 标记 0..52 全达、part04 expando/R93 计数器未达，控制流在 part03→part04 交接区被吞，待定位）。

**本轮续94**: R94 — **M3 续作：Proxy-ctor 桥（CE 用户 ctor 体执行）**（commit `d26f8a29`）：**闭合 R90 已知限制「class ctor 体不可重放」**——不是重放，是 **this 注入**：polyfill HTMLElement stub 在 `_zwCeExisting` 已设时返回既有元素，derived ctor 的 `super()` 把它作为整条 ctor 链的 this，用户 ctor 体以 this=元素 proxy 继续执行（node 探针全套实证：identity/链前挂/多层派生链/顺序升级/new.target/异常消费/嵌套 create）。**实现**：part03 HTMLElement stub → ctor 桥 hook（仅 polyfill 自建路径，native_dom 走 native S5b slot 零交互）+ `_ceRunCtor`（setPrototypeOf 先挂 + class 经 new+super() 注入 / function 经 .call；toString class 语法探测判别——否决 .call-抛错探测（function 体抛错会 double-run）；异常吞 + finally 清槽）+ `_ceUpgradeNode` 升级也执行 ctor 体 + part06 createElement CE 分支换 `_ceRunCtor`。**WC 资产断言组 8**（6 路径：ctor-ran/instanceof、expando 持久、ctor→conn 顺序、function ctor、抛错隔离、普通元素不受扰）。**结果**：integration **775**（+组 8）/ engine v8 2188 / quickjs 1427 / webview 601 全绿；WPT dom 四子目录 per-case 与 R93 **逐字节一致**零回归；fmt/clippy 干净；pre-commit-guard PASS。**lit 意义**：constructor 内初始化面（attachShadow/属性初始化）现在可达——真实 lit 库 e2e fixture 是 M3 剩余主项（最后已知阻塞已解除）。

**本轮续93**: R93 — **M3 续作：CE 原型方法派发**（commit `7469a804`）：**WC 资产断言组 7**——custom element 用户原型方法（`MyEl.prototype.bump`，lit/stencil 组件形态）经 proxy 派发可达且 this 绑定正确（method typeof/call±arg/this 持久化/原型 getter/per-instance 覆盖/undefined miss）。**根因**：R90 的 getPrototypeOf trap CE registry 分支已把链顶换成 ctor.prototype，但 part04 get trap 的原型链回落**仅限 SCREAMING_SNAKE 常量**（R80 补丁）——方法名恒返 undefined（`el.bump is not a function`）；且链上直接取 `_pchain[prop]` 让 accessor getter 的 this 落在 prototype 对象（`doubled` NaN 实证）。**修复**：回落放宽到全部未命中属性（getOwnPropertyDescriptor own 命中，8 层上限）+ accessor 以元素 proxy 为 this 求值 + data property 直取值（this 调用期自然绑定）。**结果**：traversal 1593P/11F、collections 48P/0F per-case 逐字节一致零回归；nodes 6654P/1568F（stash A/B 重建干净 R92 二进制核实 textContent 4F 与 createElementNS 390F 均为既存——**勘误**：R80/R81 的 187P/409F、81P/0F 数字已过时）；integration 773（+组 7）/ engine v8 2188 / quickjs 1427 全绿；fmt/clippy 干净；pre-commit-guard PASS。**上轮中断 session 遗留取证**：ignored wpt-data 里的 zz-probe-r93.html 探针输出证实 Proxy-ctor 桥可行信号（bridge ctor 调用 + setPrototypeOf 生效 + 用户 ctor 体执行 state=5/bump()=6），取证后删除。**延后**：Proxy-ctor 桥（lit constructor 内初始化）下轮候选。

**本轮续92**: R92 — **M3 续作：lit 风格模板渲染进 shadow root**（commit `f59a4be7`）：**WC 资产断言组 6**——innerHTML 模板（含插值）写 shadow root + tag/class/id 查询 + 后代组合器（`.nested em` depth-3）+ childNodes 计数 + 二次渲染覆盖（lit 核心渲染原语面）。**根因**：innerHTML= 在 handle 容器的解析子树是 `_zwMEl` 快照代理（无 `__zwHandle`）——只 depth-1 子入 `_handleChildren` registry，`_handleSubtreeNodes` registry DFS 收不到嵌套层 → shadow 根查询 h2/#t/.v 全 miss。**修复**：visit 对 handle-less 解析节点直接沿 childNodes 展开（元素子入 result + 文档序 + 兄弟上下文 + infoByProxy identity + expandDeep 递归嵌套）。**结果**：nodes 净 +3 per-case（Node-parentElement document-parent 修复 + 2 flake 消退）；traversal 1593P/11F / events / collections per-case 不变；integration **773**（+lit 组）/ engine v8 **2188** / quickjs **1427** 全绿；fmt/clippy 干净；pre-commit-guard PASS。**教训**：① selector-kind 分解 probe（div:1/h2:0/#t:0 + card.childNodes 可见 H2）一次定位层级边界 ② 手动 appendChild 对照排除匹配器本身——纯数据源问题。**延后**：Proxy-ctor 桥（解 lit constructor 内初始化）本轮未做，下轮候选。

**本轮续91**: R91 — **M3 续作：shadow 树 isConnected（容器反链 + 边界穿越）+ whenDefined 断言闭环**（commit `6fa0a6df`，rebase over 并行流——R90 已知限制两项收口）：**三根因**——① handle 容器（shadow root/fragment/handle 宿主）appendChild 只记 `_handleChildren` 不记 `_zwNodeParent` 反链——isConnected 反链上行到容器即断 ② 反链到达容器 handle 后无 shadow→host 边界穿越（spec `#connected`：shadow-including root 是 document 即 connected——host 在文档内整棵 shadow 树 connected）③ WC 资产 whenDefined 只挂探针未读回。**修复**：`_recordHandleChild` 同步记反链（parentHandle 匹配对称清于 unrecord）+ isConnected 循环经 `_shadowHandleMeta` 跳 host（hostSel 直返/续链/rect 回落）+ 断言组 4 第二次 execute 读回 `__futureResolved`。**结果**：Node-isConnected-shadow-dom 2F→**2P（100%）**、nodes 净 +2；traversal 1593P/11F per-case 不变、events/collections 逐字节一致；integration 772（whenDefined 强化）/ engine v8 2187 / quickjs 1427 全绿；fmt/clippy 双矩阵干净；pre-commit-guard PASS。**教训**：`_makeProxy` 的 target 是 `_fvTarget` 空对象非节点——isConnected 内不能经 target.parentNode 取父，容器侧 append 记账是唯一可靠路径。

**本轮续90**: R90 — **M3 Web Components 端到端验收首切片**（commit `dc7775c9`，DC-2 开刀）：**常驻资产** `tests/integration/src/e2e_web_components.rs`——5 断言组经真实页面脚本管线（define/升级/lifecycle、observedAttributes 反射、Shadow DOM 基础、registry 反查、CustomEvent），`make test` 内运行（integration 772 全绿含 5 新增）。**基线暴露三缺口全修**——① createElement 不查 CE registry（返回 generic proxy 升级路径断）→ 命中注册 tag `setPrototypeOf(ctor.prototype)` 立即升级（与 manual upgrade 同型）② getPrototypeOf trap 动态派发覆盖 setPrototypeOf（instanceof 仍 false）→ trap 先查 CE registry（tag 命中返 ctor.prototype，先于 iface 表——parser 建与升级元素同源）③ connectedCallback 时刻 isConnected false（旧依赖 layout 后 rect）→ 先沿 `_zwNodeParent` 反链上行（到 sel 节点即 connected），rect 降回落。**已知限制记档**：class ctor 体不可重放（`B.call(el)` 抛 without 'new'；Reflect.construct 新建 this）——升级=原型挂接+connectedCallback 承载初始化（imperative WC 模式，lit 的 constructor 内初始化面受限，后续可探索 Proxy-ctor 桥）；~~shadow 树内 isConnected false~~（**R91 已修**）；~~whenDefined 断言未闭环~~（**R91 已修**）。**验证**：integration 772（+5）/ engine v8 2187 / quickjs 1427 全绿；traversal per-case 不变（1593P/11F）；**nodes 净 +1**（Node-isConnected ordinary-child 修复）；events/collections 逐字节一致；fmt/clippy 双矩阵干净；pre-commit-guard PASS。

**本轮续89**: R89 — **M4 traversal 分散失败：惰性 currentNode setter + previousNode dig-on-accept**（commit `2d58d788`）：**两根因**——① TreeWalker.currentNode setter eager materialize 让 filter 提前执行（消耗 filter 副作用窗口 + 重入 InvalidStateError 从 setter 泄漏——WPT 期望 setter 纯赋值不抛、首个遍历方法才抛；中间方案「包 active + 吞异常」不解决副作用窗口消耗，纯赋值才是正解——idx 延迟到下次遍历经 relocated 分支定位）② previousNode 的 ACCEPT 分支立即返回——对**有子的 accepted 节点应先入子树尾**（filtered 序前驱：cur=B2、sibling=B1（ACCEPT 有子 C1/C2-rejected）期望 C1 非 B1；childless 直返使 traversal-reject 期望两模型同果零回归）。**结果**：traversal 1591P/13F→**1593P/11F（净 +2，per-case 零回归）**；TreeWalker.html "Recursive filters need to throw" + previousNodeLastChildReject 双修；nodes/events/collections per-case 一致（name-validation flake 波动外）；ranges 未跑全量（既有 >420s 慢用例 M1 L2 归因在案，变更面限于 walker 工厂 range 不消费）。**验证**：engine v8 **2187**（+2 单测）/ quickjs **1427** 全绿；fmt 无 diff；clippy 双矩阵零警告；pre-commit-guard PASS。**教训**：① 语义拿不准交叉验证两个 WPT 用例期望——dig-on-accept 同时满足 previousNodeLastChildReject（dig）与 traversal-reject（childless 直返），纸面推演后再实现 ② Rust 字面量裸换行（\n 丢失）致 ReferenceError——单测先跑再 land。剩余 11F 全深结构：cross-realm 7F（iframe）+ currentNode-outside-root/regraft 4F（「currentNode 位置优先于 root 快照」order 模型本体重构，Acid3 006a 语义）

**本轮续88**: R88 — **M4 NodeIterator filter 执行中移除的 in-flight retarget**（commit `aa368299`，removal-during-filtering 收口 4P/0F 100%）：**五重根因**——① pre-remove 只作用于 referenceNode（spec `concept-nodeiterator-traverse` 要求对 in-flight 候选生效）② 遍历 wrapper 在 filter 返回后无条件回写 refNodeVal=返回值——覆盖 retarget 已落的存活节点 ③ 抑制回写对 already-visited 场景（移除无关已访问节点）是错的——按成因分流 `retargetInFlight`（仅 in-flight 命中置 flightRetargeted）vs `retarget`（常规 reference 命中，filter 返回后 ref 正常落新 accepted）④ previousNode before=false 半边返回 refNodeVal——被 filter 内 retarget 改掉后返回值成了 retarget 目标（ref 快照先于 check）⑤ 清理时序——check finally 先清标记则 wrapper 读到已清零值（清理移 wrapper 尾部）。**实现**：registry entry 增 getInFlight/getInFlightBefore（check() 执行 filter 前后登记/恢复 + 方向态由遍历方法入口设置）；notify 循环 in-flight 在 removed 子树内 → 以 inFlight 参与 effBefore 判定并经 retargetInFlight。**结果**：traversal 1589P/15F→**1591P/13F（净 +2，per-case 零回归）**；removal-during-filtering **4P/0F（100%）**；nodes（name-validation flake 1 条波动外）/events/collections per-case 一致。**验证**：engine v8 **2185**（+2 单测）/ quickjs **1427** 全绿；fmt 无 diff；clippy 双矩阵零警告；pre-commit-guard PASS。**教训**：① notify hook 打印 entry 状态一次定位「retarget 没执行 vs 执行后被覆盖」② 一刀切抑制回写在同文件第 2 用例立即暴露——机制按成因路径分流 ③「returns the filtered node」与「reference retargeted」是两个独立观察面，返回值与 reference 可合法分叉。

**本轮续87**: R87 — **M4 NodeIterator removal 恢复段二次周期 retarget + previousNode before-flip**（接手 429 中断的 R87 WIP 收口，commit `237a6d7e`）：**七重根因**——① 恢复段 insertBefore 不清移除标记（只 appendChild 清）② detached doc/body/主 document 缺 insertBefore（主文档缺 removeChild-doctype）→ 恢复段 TypeError 崩子测试 ③ **R87 首版 guard 误用 child 查注册表**（`_zwRegisterTextEl` 键是父 el proxy）恒 miss → 文本子 remove 静默 no-op ④ 元素 remove/restore 周期后注册文本被 `_zwUnregisterTextSubtree` 注销——二次 remove 须走物化缓存路径（guard 放宽 + `_zwDetachChildFromCache` 剔除 removed）⑤ previousNode 的 before=false 半边返 ref 不过 filter（Recursive filters 断言失效）⑥ registry 压实 `length=0` 全清在档迭代器（改 65536 上限保尾 1024）⑦ retarget 后继未约束 root 子树 + **主文档 firstChild/lastChild getter 被 WIP 误删**（重建二进制 A/B 捕获的真回归：NodeIterator.html 766P→758P）。**结果**：traversal 1575P/29F→**1589P/15F（净 +14，per-case comm 零回归）**；NodeIterator-removal.html 10P/15F→**23P/1F**；nodes/events/collections per-case 逐字节一致（name-validation 3F/5F 波动为既存 flake，standalone 双态同 5F）。**验证**：engine v8 **2183**（+2 单测）/ quickjs **1427** 全绿；fmt 无 diff；clippy 双矩阵零警告；pre-commit-guard PASS。**教训**：① A/B 必须重建二进制（R79 教训第三次实证——本轮抓到 WIP 自带 8F 真回归）② 跨子测试状态必须复现完整前序 remove/restore 周期（standalone probe 全绿 ≠ 全量绿）③ testharness assert message 的对象形态（handle proxy vs plain text）比计数差更快锁定分支。

**本轮续56**: R56 — **M8 path-objects 接手首切片**（R55 文档收尾同轮完成后开刀）：① 用例导入 205 个 `.html`（fetch 脚本整跑 5min 超时，改单列目录 + 逐文件 raw 拉取 205/205，pinned WPT_REV）；② **roundRect 批量 panic 修复**（DC-8 第一优先级）——4 处 `sort_by(partial_cmp.unwrap_or(Equal))` 在 NaN 下违反全序传递性 → driftsort panic → wpt-runner 整目录 abort，全部改 `f32::total_cmp`（backtrace 实证栈 blit_path_to_pixels/rasterize_path_coverage/blit_path_gradient/set_sat）；③ **roundRect 语义对齐**（shim `zwNormRadius`：空/>4 项 RangeError、负半径 RangeError、NaN→0、BigInt TypeError、单个 DOMPointInit 走 p<x>,<y> 编码——旧版落 r='0' 全丢）；④ **host 角对解析 bug**：JS join(',') 的 `p40,20` 被 split(',') 拆两项，旧 split_once 失败 → DOMPoint(40,20) 解成 (40,40)（像素边界椭圆拟合 probe 定位）；⑤ **负 w/h 归一化 + 角序镜像**（参数坐标系 tl 恒贴 (x,y)；swap 在 1-4 项 radii 展开后做）；⑥ **扫描线段式迭代**——三个 fill 光栅消费端改按独立段 chunks_exact(4) 迭代，消灭多子路径虚假边（negative 用例逐步 probe：加第 3 个不接触的子路径后整片翻色）；连带 **closepath-on-fill**（flatten_path_opts 拆 fill 闭合/stroke 开放两变体；初版 &&→|| 笔误由 probe 定位）、ctx.rect() 补 closePath、join 三角闭合段扇、roundRect 子路径起点基准、退化矩形自包含 4 边。**并行流叠加**：canvas 流同轮 land `afd8ec08`（同款 total_cmp×5 + arc 角度归一化），rebase 冲突仅 2 处注释级。**验证**：path-objects **139→156 pass / 64→47 fail**（panic 消除全程跑完）；zero-canvas 777→783 单测全绿（+3 新单测 + 11 处计数断言语义更新）；全 canvas 449P/53F（shadows 6 个 HEAD 既存）；5 个新 fail 为边缘精度（arc.shape.5/curve2、fill.overlap/winding.add 需 nonzero fill rule、rect.end.2）换 12 修复 + panic 消除。fmt/clippy 双矩阵干净，pre-commit-guard PASS（commit `ac7240f5`，rebase over `a749de94`）。make test 唯一失败 `default_actions_work_without_javascript` 为并行流既存（clean HEAD 同败，表单导航域）。上轮背景：R55（childNodes/sibling 基底缓存）已 land `4c7a3582`

**本轮续37**: R84 — **M4 traversal 兄弟链断链 + walker 语义四重修复**（R83 CONTINUE 方向落地）：**五重根因**——① sibling 对 text 节点 `_wrapNodeEntry(pair, null)` 独立包装 → parentNode=null + 兄弟静态 null（WPT oracle `nextNode()` 树序导航链断链，NodeIterator/TreeWalker document root 整簇 expected-null-but-got-object）② CDATA（handle-less）append 入 `_handleChildren` 不接 parentNode/兄弟 getter ③ detached doc headEl/docEl 无兄弟 getter（foreignDoc/xmlCmd root 遍历到 HEAD 即停）④ NodeIterator 缺 detach()（spec 历史 no-op）+ filter 重入无 active 守卫（InvalidStateError）⑤ TreeWalker filter 语义三处：返 false(0) 应按 REJECT 剪枝（旧 dig 子树）+ root 不应被 filter（iteration collection 含 root）+ currentNode 重定位被滤节点 effPos 误走 fresh 分支。**核心修复**：sibling 对改经 `_childNodeList(parentSel)` 取（与 childNodes[i] **同 identity**，indexOf 定位命中——probe 实证独立包装 `kids[1]===tn` false 是断链根源）；两接口 REJECT 分叉（TreeWalker 非 1/3 → 剪枝 / NodeIterator 不剪——首轮统一归一致 NodeIterator -12P 回归后纠正）；walkRoot() root 不 filter（order-scan 仍 check root 保迭代器 fresh 语义）；七遍历方法 _guarded active flag；CDATA/headEl/docEl 兄弟 getter 补齐。**附带 unblock**：并行流 `a8d5a22d` 的 v8+quickjs **组合态**（workspace cargo 并集）五处编译断（worker.rs 死分支调错签名/es_module config 双 move/quickjs_worker ambiguous re-export/webview 重复 Drop+method/tab_js_worker config 双 move）——not(v8) 门控 + clone 修复（CI 单 feature 矩阵掩盖、make test 必踩的组合态债）。**结果**：traversal 1527→**1556P（+29 净）/24F**（NodeIterator.html 19F→0F、TreeWalker.html 18F→8F）；nodes 6635→**6643P（+8 顺带）**；events/collections 零回归。**验证**：part18 +3 单测（sibling 链/CDATA+detach+重入/filter 剪枝+root+重定位）；engine v8 **2176**/quickjs **1427** 全绿；fmt 无 diff；clippy workspace 默认+quickjs 矩阵+webview/browser/script-sandbox 零警告。**教训**：① oracle 与实现的遍历原语分歧（firstChild/nextSibling 链 vs childNodes 递归）是断链根因——identity 不统一时正确实现也 fail ② TreeWalker/NodeIterator 的 REJECT 语义天然分叉（剪枝 vs 不剪），同工厂须按接口归一 ③ CI 单 feature 矩阵掩盖 v8+quickjs 并集态编译断——cfg 双分支代码须在并集态验证（commit 待 push）

**本轮续39**: R86 — **M4 detached 子树保留其子 + NodeIterator 移除 retarget**（R85 CONTINUE 方向落地）：**四重根因**——① removeChild 注销注册文本（R51c 防泄漏）后 detached 元素 firstChild/childNodes 不可读（spec 要求保留其子——NodeIterator-removal 的 setup 在 remove paras[0] 后读 firstChild 直接崩 null.parentNode，**整文件 0 执行**）② 迭代器 order 快照对移除不感知（spec 迭代集合 live）③ referenceNode 移除 retarget 缺失（spec 双指针分支：后置→前驱/前置→后继；root inclusive-ancestor no-op）④ 通知点不全（_zwMEl.removeChild + detached-doc 三族 removeChild 不通知）。**修复**：_zwMaterializeDetachedChildren（注销前物化子视图，512 软上限 + 读路径回落——可读性与防泄漏兼得）+ _zwMarkRemovedHandle/_zwIsRemovedNode（双标记 + 沿父链子树判定 + appendChild 清标记）+ order 扫描跳过移除节点（前向整子树/逆向逐节点）+ _zwIterRegistry/_zwNotifyIteratorsRemove（pred/succ 树序计算**先于**树状态变化 + inSubtree/isAnc 判定 + 双指针 retarget）+ 五个 removeChild 形态全接通知。**结果**：traversal 1565→**1575P（+10 净）**；removal.html 整文件崩溃 → **10P/15F 执行**（执行暴露型 F 增长，同 R8 基线真实化——不是回归）；nodes/events/collections 零回归。**验证**：part18 +2 单测；engine v8 **2181**/quickjs **1427** 全绿；fmt/clippy 全矩阵干净。**教训**：① 崩溃文件是零信号——修崩溃让用例执行本身就是基线真实化，判定标准是「可执行子测试的 pass 是否增长」② 防泄漏与 spec 可读性冲突时物化快照两头兼得 ③ 通知必须先于树状态变化（pred/succ 读移除前链）④ isAnc 方向 = removed 在 root 的父链上（inclusive ancestor of root），首轮写反过

**本轮续38**: R85 — **M4 TreeWalker 导航式重写 + previousNode 规范镜像**（R84 CONTINUE 方向落地）：**四重根因**——① accepted/parentAcceptedIdx 扫描模型无法表达 WPT oracle 的「0（filter 返 false）按方法分叉」语义（children 循环只横移不 dig；sibling 循环经 firstChild dig；父站 truthy 即止——R84 统一归一是过度概化）② previousNode 的 order-scan 逆向不剪 REJECT 子树（逆向候选序中子树内节点先于被拒祖先命中）③ html.previousSibling 恒 null（host 对 html 无父）——oracle expected 与 walker 走同一导航链 → expected null vs walker doctype 假分歧 ④ 父站判定 truthy 止过宽（spec 仅 FILTER_ACCEPT 止）。**修复**：navKids/navSiblings 导航式层级方法（经真实 getter 步进精确镜像 oracle 循环）+ previousNode 拆两态（TreeWalker 用 DOM 规范算法：sibling=previousSibling；REJECT→prev；SKIP/0→lastChild||prev；兄弟尽→parent 续 / NodeIterator 结构序逆向不剪）+ nextNode 显式分叉（TreeWalker 2+0 剪 / NodeIterator 仅 2 剪）+ html 兄弟走 document.childNodes 派生（previousSibling=doctype）。**结果**：traversal 1556→**1565P（+9 净）/15F**（basic/skip/reject/skip-most 全修，TreeWalker.html 8F→1F）；nodes 6643→**6650P（+7）**；events/collections 零回归。**验证**：part18 +3 单测；engine v8 **2179**/quickjs **1427** 全绿（R82-R84 旧 walker 测试零回归）；fmt/clippy 全矩阵干净。**教训**：① oracle 的 expected 在我们引擎里同源计算——修 getter 即同时修 expected 与 actual 两端，只修一侧制造假分歧 ② filter 返回 0 的语义随方法分叉，统一归一必错 ③ 树序前驱绝不是自身子节点——previousNode 正确形态是规范 sibling 循环+parent 续，非「下钻最深」镜像直觉

**本轮续36**: R83 — **M4 walker 全 nodeType 掩码 + fresh 起点语义 + handle before/after**（commit `d38a9081`，R82 CONTINUE 方向落地）：**六重根因**——① maskFor 仅 element/text/comment 三类 → doctype/fragment/PI/document/CDATA 全被掩掉 ② TreeWalker 与 NodeIterator 的 fresh 起点混同（walker 的 currentNode=root 已位于 root → 首步越过；iterator 集合含 root → 首步返 root；R2803 旧断言固化错误语义）③ previousNode 旧 accepted-index 模型对滤掉的 currentNode 错误回落「从尾」④ WebIDL optional 三态缺 undefined 形态（R82 只修省略）⑤ handle 元素 before()/after() 的 _insertAdjacentVariadic 仅 sel-based → 静默 no-op（ChildNode-before/after 84F 整簇）⑥ handle innerHTML 只反映 host SetInnerHtmlOnHandle——appendChild 子树不可见。**修复**：maskFor 全表 (1<<(nt-1))>>>0 + fresh 起点按类型区分 + previousNode 结构序 lazy 逆向（与 nextNode 对称）+ undefined→SHOW_ALL/null→0 + before/after JS 侧父 registry splice 插入（字符串经 __zw_create_text + 反链 + notify）+ innerHTML 融合序列化。**结果**：nodes 6596→**6635P（+39 净）**、traversal 1486→**1527P（+41 净）**、events/collections 零回归。**验证**：part18 +2 单测 + R2803 两断言 spec 纠正；engine v8 **2172**/quickjs **1427** 全绿；fmt/clippy 双矩阵干净；pre-commit-guard PASS。**教训**：① TreeWalker/NodeIterator 的 root 语义天然分叉（currentNode 位置 vs 集合指针）——同一 walk 引擎须按类型区分 fresh 起点 ② 旧实现自洽的断言可能固化错误语义——WPT 期望表是最终仲裁 ③ previousNode 与 nextNode 对称性是正确性结构证据——两种模型混用必出边角

**本轮续35**: R82 — **M4 NodeIterator/TreeWalker whatToShow 无符号 + referenceNode 同步**（commit `8a8d458e`，R81 CONTINUE 方向落地）：**三重根因**——① `whatToShow | 0` 是 ToInt32（0xFFFFFFFF 读回 -1；spec WebIDL unsigned long 须 ToUint32=4294967295，WPT 152 subtest）② 缺省与显式 null 混同（`== null → 0xFFFFFFFF` 把显式 null 当 SHOW_ALL；spec optional 参数**省略**才缺省，显式 null 走 ToUint32(null)=0——arguments.length 区分）③ **referenceNode/pointerBeforeReferenceNode 同步 wrapper 被 R51 lazy 重赋值静默覆盖**（wrapper 定义在 lazy `walker.nextNode =` 之前被覆盖 → 步进后 reference/pointer 恒不更新，40+ subtest 根因）。**修复**：`>>> 0` + arguments.length 区分 + wrapper 移工厂尾部（全部 lazy 定义后统一包装）；语义：nextNode 命中 → reference=node + before=false；**耗尽返 null 不动**（首版「到尾也翻 false」被全量跑出 597 fail/-306P 否决——WPT 期望「立即耗尽的迭代器 before 保持 true」）。**结果**：traversal 1195P→**1486P（+291 净）**、nodes 6596P/events 189P/collections 48P 零回归。**验证**：part18 +1 单测；engine v8 **2170**/quickjs **1427** 全绿；fmt/clippy 双矩阵干净；pre-commit-guard PASS。**教训**：① 工厂内多次重赋值同名方法会静默覆盖此前的 wrapper——wrapper 一律放工厂尾部 ② WPT 期望值两种形态（expected true/false）对应「耗尽 vs 命中」两态，全量 A/B 是语义仲裁者 ③ WebIDL optional 的「省略 vs 显式 null」必须 arguments.length 区分

**本轮续**: R56b — **M8 path-objects 回归收口**（R56 首切片 4 个真回归 + 1 个 stroke 语义修复，commit `b1446e9b`）：① **arc span 归一化方向感知**（顺时针同向 mod ∈ [0,2π)/逆时针 ∈ (−2π,0]；旧 `raw % TAU` 顺时针负差得负 span 弧反向，且归一化后仍 `* dir` 双重取反再翻弧——angle.2/3/4/5、shape.2、twopie.5）② flatten Arc 分支补 spec「current→弧首」连线段（`has_any_subpath` 标志；moveTo(圆心)+arc 整圆 fill 缺边 → 扫描线弧首角配对破裂）③ roundRect 非有限半径（NaN/±Inf 含 DOMPointInit 成员）spec 静默忽略整次调用（旧对 −Inf 抛 RangeError）④ DOMPoint 构造器 `+x || 0` 吞 NaN → `(x==null)?0:+x`（unrestricted double 保留 NaN/Inf；二分定位 roundrect.nonfinite 调用组 #70）⑤ stroke closed 语义：`any(ClosePath)` → **末命令** ClosePath（rect 闭合后接 lineTo 的开放末子路径不再丢 round cap，rect.end.2）。**验证**：真基线（pre-R56 `a749de94` 干净 checkout 构建）62F → **37F/166P，净 +25 修复零回归**；跨目录 line-styles 1 预存 Fail（join.round d3edff50 同败）+ 三目录零 Fail；canvas 单测 +5 + 4 旧 arc 计数更新（+1 连线段）；engine shim 单测 +1；engine v8 **2149** / quickjs **1416** 全绿（含并行流 `83fa7fbd` 修复的 canvas_element_bridges_to_image_primitive 预存红灯——布局 flow-root fold 把 canvas 固有高度 2 压 0，渲染流域）；clippy 双矩阵零警告，fmt 无 diff。**过程教训**：① 工作树旁 /tmp 基线快照不可信（与真基线差 17 条，4 个真回归一度误判预存）——基线必须来自干净 checkout 的全新构建；② stash pop 输出被 tail 截断掩盖失败（一次 pop 未执行，工作树被并行流 commit 成 6f5ee7d1，reflog 找回零丢失——同 clone 双流第三次违反 run-rules §8）

**本轮续2**: R56c — **M8 nonzero fill rule + fillRule 透传 + roundRect 镜像绕组**（接手 429 中断的工作树遗留，commit `bd027c98`）：① raster `fill_rule_spans` 方向感知扫描线（交点带方向 ±1，NonZero 累计绕组取非零区间 / EvenOdd 奇偶配对），三个 fill 消费端统一收口；② `fill_with_rule`/`fill_path_with_rule` API 族 + `FillRule` pub 导出；③ roundRect 单轴镜像段序反转 `reverse_subpath`（真浏览器沿参数边方向环绕——roundrect.winding 实证）；④ host op fill（rule=args[0]）/fillPath（rule=args[1]）解析；⑤ shim WebIDL 可选前置参省略嗅探——`ctx.fill("evenodd")` rule 落第一参位（typeof path==='string' 嗅探交换，evenodd.1 调用形式）。**验证**：path-objects 166P/37F → **168P/35F**（fill.overlap/winding.add/evenodd.1 修复，零回归）；canvas +6 单测（含同区域反向对两规则都不填的分析修正）+ engine +1 e2e 四场景；canvas 788 / engine v8 2152 / quickjs 1416 全绿，clippy 双矩阵干净；跨目录 line-styles/drawing-rectangles/transformations/reset 0F、shadow 6F 基线既存（stash 验证）。**过程教训**：① shim↔host 参数索引按实际 wire 形式核对（同一 op 有/无 path 形式参数位不同），probe hook `__zw_canvas_op` 实参日志最快定位；② 同区域反向矩形对 nonzero（绕组对消）与 evenodd（计数偶）都不填——两规则区别需同向双矩形验证。rebase 合并并行流 colorMatrix filter（apply_filter_color 在包装层应用后传入 rule 变体）

**本轮续3**: R56d — **M8 isPointInPath 族 nonzero 对齐**（commit `16f0d885`）：① `is_point_in_path_rule`/`_for_rule` 复用 fill_rule_spans（默认 nonzero 绕组，spec dom-context-2d-ispointinpath）+ 路径上点算 inside（span 闭区间 + on-segment 兜底，零长度退化段排除）② 巨 scale ±Inf 顶点收缩 ±MAX/4 + host scale op 钳 ±f32::MAX（矩阵合成恒等元 0×Inf=NaN 毒化顶点根因）③ 无 MoveTo 直接 arc 整圆 = 自包含子路径（subpath_start 重置弧首点，消杂散闭合对角线）④ host isPointInPathPath 新 op（Path2D 形式）+ fillRule 参数 ⑤ shim WebIDL union/枚举校验（'gazonk'/null/[] → TypeError）+ path 形式参数位修正。**验证**：path-objects 168P/35F → **172P/31F**（isPointInPath.basic/edge/invalid/multi.path 修复；bigarc 过程回归 A/B 捕获同轮修，零回归）；canvas 789 / engine v8 2152 / quickjs 1416 全绿，clippy 双矩阵干净；跨目录六目录 0F、shadow 6F 基线既存。**教训**：① 单测 f32::MAX（有限）vs e2e Number.MAX_VALUE（→inf）差异误导定位——0×Inf=NaN 是矩阵合成陷阱；② 自闭合命令（arc/roundRect）须重置 subpath_start 到自身起点（flatten 初值 (0,0) 隐性陷阱）

**本轮续4**: R56e — **M8 ensuresubpath 族 + clip 相交/空 + ellipse 负半径**（commit `eab37ea1`）：① flatten_path_opts lineTo 无子路径=moveTo（不画隐含 (0,0) 连线）；quadratic/bezier 无子路径=**第一控制点成起点、曲线照画**（初版误读 no-op，WPT 期望图纠正——ensuresubpath.2 期望整片绿=退化直线必须画）；arcTo 无子路径=P1 加入+跳过 current→切点1 前导线（flatten_arc_to 加 has_subpath 参，调用点捕获 pre-call 值——先置位再传参使守卫恒真的顺序 bug 由 probe 17 段暴露）② clip_path Option→clip_paths Vec——spec 相交语义（clip_applies 全 AND；空路径 clip 交集空全裁；旧实现空路径 early-return+多次 clip 覆盖）③ shim ellipse 负半径→IndexSizeError（-0/0 合法）。**验证**：path-objects 172P/31F → **181P/22F**（+9 修复零回归）；canvas 793（+4 单测）/ engine v8 2153（+1 e2e）/ quickjs 1416 全绿，clippy 双矩阵干净；跨目录 10 子目录 0F（clip 重构影响面广全查）、shadow 6F 基线既存。**教训**：① 语义拿不准先看 WPT 期望图；② 守卫变量须在守卫调用求值后才置位

**本轮续5**: R56f — **M8 arcTo 切线弧中心修正 + 各向异性 CTM + shim 负半径守卫补齐**（commit `9ef44a8d`）：① `arc_to_tangent_segments` 提取 path.rs 共享函数——flatten_path_opts（ctx stroke/fill）与 flatten_path_for 统一消费真切线弧（并行流 `76655cc4` 只修了 path.rs hit-test 路径，ctx 路径仍走旧线段近似——**flatten 双实现漂移**）；② 圆心计算修正：候选 `T1 ± rot90(u1)·r` 取与 T2 距离=r 者（旧「u2−u1 平分」不垂直 u1 → 弧张角偏差，probe 实证弧止于 (114,6) 而非 T2(150,0)）；③ 各向异性 CTM（sx≠sy）：弧在用户空间是圆经 CTM 变换为设备椭圆——逆变换控制点→用户空间构造→输出逐点正变换（arcTo.scale 的 scale(0.1,1)）；④ shim ctx+Path2D arcTo 负半径 IndexSizeError——**76655cc4 message 声称已加但 hunk 未随提交进入**（合并态 arcTo.negative 仍 fail，归因后补齐）；⑤ 死代码清理 flatten_arc_to/compute_arc_to_geometry + 10 旧单测；⑥ R56e no-subpath arcTo 断言修正（真浏览器什么都不画）。**验证**：path-objects 181P/22F → **186P/17F**（arcTo 五用例零回归）；canvas 786 / engine v8 2153 / quickjs 1416 全绿，clippy 双矩阵干净；六跨目录 0F。**教训**：① 并行流提交可能丢 hunk——合并态先跑 fail 集核对；② flatten 双实现漂移是结构性风险，共享函数根治；③ 圆心几何用候选+距离恒等比平分向量推导可靠

**本轮续6**: R56g — **M8 弧自适应段数 + 真圆环带 + 用户空间 CTM 弧**（commit `d0e93adc`）：弧 stroke 折线化的**三类独立缺陷**逐一修——① 弦切向偏差 δ=τ/2N 在粗线（lw/r>0.25）下把 butt 端面投影翻正（shape.1/3 距弧 40px 区域被首弦斜段矩形覆盖）→ N=min(512, 64·lw/r) 自适应；② 折线伪节点覆盖洞（shape.2 的 (1,1) 距弧 18.6<half 50 却 miss——超短弦投影全落段外 + lineJoin=Miter 不画伪节点圆盘）→ `blit_arc_annulus` 真圆环带后处理（|dist−r|≤half ∧ θ∈[span]，butt=θ 窗自然截断）；③ arc() 只变换圆心、半径未过 CTM（scale.2 的 scale(100,100) r=0.6 应成 r=60 却画 0.6px 小点）→ 非恒等 CTM 逆变换→用户空间圆弧→逐点正变换。**过程**：interior-clamp 方案被 selfintersect.1 回归否决（A/B 捕获）；identity 判定 t.d==0 笔误致 annulus 覆盖为零（debug trace 定位）。**验证**：path-objects 186P/17F → **192P/11F**（arc.shape×4 + arc.scale×2，零回归）；canvas 788（+2）/ engine v8 2153 / quickjs 1416 全绿，clippy 双矩阵干净；六跨目录 0F。**教训**：弧 stroke 折线化三类缺陷不能单手段修复（加段数修一引入一）——annulus 直判 + 用户空间构造才是正解

**本轮续7**: R56h — **M8 曲线段数自适应 + stroke 像素方形覆盖**（commit `e332878f`）：① 曲线 flatten 段数自适应 N=clamp(控制点折线长/8, 8, 512)（quadratic/bezier 双分支）——固定 8 段对巨坐标曲线（shape 用例折线 ~13000px、画布内 t 窗口仅 ~2.4 段）弦偏差 48px，自适应后画布内 ~154 段（~0.01px）；② stroke 像素判定半径 half+0.5（像素内切半径）——真实光栅是面积覆盖，中心距带 0.4px 外但方形与带相交的临界像素须着色（(1,1)：中心 27.9/half 27.5/方形角 27.2 相交），应用于 solid+gradient 双光栅化器。**验证**：path-objects 192P/11F → **196P/7F**（bezier/quadratic shape/scaled 四用例零回归）；canvas 789（+1）/ engine v8 2153 / quickjs 1416 全绿，clippy 双矩阵干净；六跨目录 0F。**教训**：曲线与弧的段数问题同构但参数不同（弧按 lw/r 端面精度、曲线按折线长弦偏差）——各自独立推导；scaled 与 shape 同构（等比放大）修一处双收

**本轮续8**: R56i — **M8 stroke 零长段剪除 + roundRect 显式闭合 + 环绕 join**（commit `c218f196`）：① spec stroke 前移除零长段——moveTo(p)+lineTo(p) 不画 round cap 圆盘（prune.line/arc/curve 三用例，剪除在段列表构造处空列表则 cap/join 全不画）；② `Path2D::round_rect` 追加显式 ClosePath + join 循环闭合环绕 `(i+1)%len`（solid+gradient 双光栅化器）——roundRect 是 spec 闭合子路径，环绕 join 缺失曾致 roundrect.closed 的 (50,25) 漏画；③ **方案否决记录**：几何闭合探测（首尾相接）把「lineTo 显式回到起点」的开放子路径误判闭合丢 square cap（2d.line.cap.open 跨目录 A/B 捕获）——**闭合判据在命令层不在几何层**。**验证**：path-objects 196P/7F → **200P/3F**（prune×3 + roundrect.closed 零回归；line-styles 2F 过程回归跨目录捕获同轮修）；canvas 790（+1）/ engine v8 2153 / quickjs 1416 全绿，clippy 双矩阵干净；六跨目录 0F。**剩余 3F 记深项清单**（stroke.skew 斜切平行四边形端面 / roundrect.end.3 miter-limit 边界 / isPointInStroke.scaleddashes dash 相位）
**本轮续9**: R57 — **M6 S0q QuickJS 原生绑定骨架 PoC**（commit `b781252f`，DC-7 双引擎对等首刀）：① 新 `crates/engine/src/quickjs_dom_bindings.rs`（feature-gated quickjs + engine optional rquickjs dep）——线程局部 `Rc<RefCell<Document>>` DOM 源（镜像 V8 gc.rs）+ `__zw_native_element_for_id` 工厂（**与 V8 同名同 wire**，miss→null 一致）+ `nodeType`/`tagName`/`nodeName`/`id`(+setter) 原生 Accessor getter 直读 live Rust DOM + NodeId 经隐藏非枚举 own property `__zwNodeFfi`（f64 Number 承载 slotmap ffi，version<2^21 无损，单测断言）+ `Persistent<Value>` 身份缓存（同 NodeId 同对象）+ `reset_quickjs_state`（webview Drop，镜像 V8 R3334 disposed-runtime 防护）；② `Sandbox::install_native_bindings_quickjs` escape-hatch（QuickJSSandbox 进持久 Context，非持久返 false 语义同 V8 版）；③ webview `install_native_dom_bindings` QuickJS 变体 + 两调用点 cfg 放宽 any(v8,quickjs)（kill-switch `native_dom` 仍默认关零回归）。**rquickjs API 关键经验**（S1q–S5q 全程受益）：闭包「Ctx 参数 + 'js 值返回」有 HRP 生命周期困难（两生命周期不合 + Value invariant）→ **具名 fn 统一 'js**；Accessor 的 this 经 **`This<Object>`** 参数接收（闭包直收 this 会以缺实参抛异常）。**验证**：engine quickjs **1418**（+2 PoC）/ webview quickjs **552**（+2 wiring，与 V8 R3097 同断言面）/ engine v8 2153 / webview v8 599 / script-sandbox 76 全绿，clippy 双矩阵干净，fmt 无 diff；make test 唯一失败 default_actions 为并行流既存（clean HEAD 同败）。**过程勘误**：R43 诊断的 -10 根因描述（case.js iframe doc）与用例事实不符——case.js 实际在主文档 documentElement 建容器查询、期望非空（live 合并方向正确，失配在 tag 大小写匹配语义）；R43「最小切片绕不开深改」结论仍成立但 L2 待 M1 完整方案，本轮转 M6 S0q（入口文档明示的 M6 首切片路径）。**遗留**：strong Persistent 的 GC 泄漏（weak/finalizer 是 S0q 续）+ S1q 属性族 + bench 对照
**本轮续10**: R58 — **M6 S1q QuickJS 只读属性族**（commit `84c76b25`）：① className getter/setter（class 反射，缺省空串；setter ToString 写 class——镜像 V8 read/write_reflected_attr）；② namespaceURI getter（空 ns/非元素 → **JS null** 非 undefined，镜像 V8 Some(None)→null 分支——String 返回形态做不到 null，需 Value 返回形态 + ctx 构造）；③ localName getter（parser ns 感知大小写透传）；④ textContent getter（子树文本拼接，setter 属 S2q）；⑤ `reflected_attr_string_of`/`element_ns_of` 共用 helper 沉淀（后续反射族复用）。注册面：id/className enumerable、其余非 enumerable（与 V8 ObjectTemplate accessor 默认一致）。**验证**：engine quickjs **1419**（PoC 扩展 + 新 namespace_uri_variants SVG/XHTML）/ webview quickjs **552**（wiring 扩 S1q 断言面）/ engine v8 2153 / webview v8 599 全绿，clippy 四矩阵零警告，fmt 无 diff。**S0q 具名 fn + This<Object> 模式零摩擦复用——API 踩坑成本一次性，后续纯语义映射**。**S1q 剩余**：字符串反射族其余项（title/lang/accessKey/dir/hidden/tabIndex）+ 复合对象（attributes/classList/dataset，需二级身份缓存）+ 方法族（S2q 写入）
**本轮续11**: R58b — **M6 S1q 续字符串反射族**（commit `b9136b38`）：title/lang/accessKey getter/setter——共享 helper `reflected_attr_string_of`/`set_reflected_attr`（V8 native_string_reflected_* 的 name-dispatch 在 QuickJS Accessor 无 name 回调，改 per-property 具名 fn + 静态 IDL→content 映射 accessKey→accesskey，语义等价：缺省空串 + ToString setter + LegacyNullToEmptyString 由 Coerced 覆盖）；PoC 断言反射族读写闭环 + Object.keys enumerable 面更新（id,className,title,lang,accessKey）。engine quickjs 1419 全绿、clippy/fmt 干净。S1q 剩余：复合对象（attributes/classList/dataset 二级身份缓存）+ dir/hidden/tabIndex 枚举·boolean 反射 + S2q 写入族
**本轮续12**: R59 — **M6 S2q 属性方法族**（commit `fe0a23d5`）：getAttribute（missing → JS null 区别空串值，V8 同语义）/setAttribute（共享 set_reflected_attr）/removeAttribute（**真移除**——布尔属性 unset 语义，区别 set 空串，镜像 V8 RemoveAttrOnHandle 修正）/hasAttribute。Function prop 非 enumerable（Object.keys 面不变，与 V8 ObjectTemplate 方法注册一致）。PoC 断言全闭环（set → get/has true → remove → has false → get null）。engine quickjs 1419 全绿、clippy/fmt 干净。S2q 剩余：setAttributeNS 族 + textContent/innerHTML setter + 子树 mutation（appendChild/insertBefore/removeChild——R4 已有 DomError→DOMException 映射参照）
**本轮续13**: R60/R60b — **M6 S2q 写入族：textContent setter + mutation 起步**（commit `be577323` / `b807e5e9`）：① textContent setter——清子 + 非空追加单 Text（镜像 V8 native_text_content_setter）；**LegacyNull 语义关键发现**：`Coerced<String>` 的 JS ToString 对 null 产出 'null' 字面量不可区分，setter 须收 raw Value 手动判 null/undefined 再 coerce；② `__zw_native_create_element(tag)` 全局工厂（与 V8 同名同 wire）——detached 元素入 arena + `get_or_build_node_value` 统一身份缓存入口（两工厂共享消重复）；③ appendChild/removeChild——child 经隐藏 `__zwNodeFfi` 判 native 族 + spec 移动语义（zero_dom 内建 reparent）+ 返回 child（spec）；DomError → null（DOMException 构造器基建延 S4q，注记在案）。**PoC 全链路**：create（detached）→ append（父 textContent 反映）→ remove（父 textContent 空）。**验证**：engine quickjs 1419 / v8 2153 零回归、clippy/fmt 干净。**M6 累计元素面**：3 构造 getter + 9 属性（6 setter）+ 6 方法 + 2 全局工厂 + 身份缓存
**本轮续14**: R61 — **M6 S2q 续树读回 getter**（commit `784aab22`）：childNodes（**Array 返回形态**——live NodeList 语义延 S1q 复合对象域，快照数组与 V8 tests 断言面一致；子对象经 get_or_build_node_value 共享身份缓存包装）/parentNode（detached→null）/firstChild/lastChild（共用 first_last_child_getter）。**PoC 双向闭环**：append → childNodes[0] === child（身份缓存）+ child.parentNode === parent；remove → firstChild null + detached parentNode null。**验证**：engine quickjs 1419 / v8 2153 零回归、clippy/fmt 干净。**M6 元素面累计**：2 工厂 + 13 属性（6 setter）+ 6 方法——树构建+树读回闭环完成。**下一步**：S3q 查询族（querySelector/getElementById 原生）或 S4q EventTarget + DOMException 基建
**本轮续15**: R62 — **M6 S3q 查询族**（commit `e438af32`）：`__zw_native_query_selector/_all` 全局工厂（与 V8 同名同 wire）+ 元素级 querySelector/querySelectorAll 方法（子树作用域）——消费 zero_dom 全量选择器引擎（tag/*/\#id/.class/[attr]+运算符/伪类/组合器）；查询结果经共享身份缓存包装（**跨工厂 identity**：qs('#main') === element_for_id('main')）；miss/空/非法 → null/空 Array。PoC：id/class/复合选择器 + miss 语义 + Array 文档序 + 跨工厂身份 + live 状态跟随（className 改值后 .c 不再命中）。engine quickjs 1419 / v8 2153 零回归、clippy（needless_question_mark 一处修复）/fmt 干净。**本 session（R58→R62）M6 累计**：4 全局工厂 + 13 属性（6 setter）+ 8 方法——从骨架到可用元素面，元素可达性经查询族大增。**M6 剩余**：S4q EventTarget+DOMException 基建 → S5q customElements → S0q 续 weak/finalizer → S1q 复合对象
**本轮续16**: R63 — **M6 S4q EventTarget**（commit `21f6c4b2`，rebase over CI-watchdog `7a3be5cc` 零冲突）：addEventListener（非 callable 静默忽略 spec 语义 + capture truthiness）/removeEventListener（最早匹配条目移除，callback identity 经 Persistent restore JS 严格等）/dispatchEvent（**target 站派发**——祖先虚站镜像 V8 R40 延后；注册序；轻量 plain 事件对象 type/target/currentTarget；**listener this = target 经 Args::this + 低层 apply——rquickjs 无 call_with_this 高层封装（本轮 API 发现）**；回调异常吞掉不中断；返 true）。监听器存储镜像 V8 LISTENERS：`(ffi, type) → Vec<(capture, Persistent<Value>)>` 单列表保全局注册序。**PoC**：注册 A/B → dispatch 序 `a:click:true|b`（序+e.type+this===target 三合一）→ remove A → 仅 B。engine quickjs 1419 / v8 2153 零回归、clippy/fmt 干净。**M6 累计（R57→R63 七轮十一切片）**：4 工厂 + 13 属性 + 11 方法——元素可达+可变+可交互。**剩余**：S5q customElements → weak/finalizer → S1q 复合对象；S4q 完整化（capture/bubble 链 + Event 构造器 + DOMException）候选
**本轮续17**: R64 — **M6 S5q customElements 五件套 + lifecycle**（commit `243811c6`）：① `__zw_native_customElements` 全局——define（tag ASCII 小写 + ctor Persistent 入 **Rust CE_REGISTRY**——QuickJS native 域无 polyfill registry，V8 版「复用 polyfill」路径不可镜像改 Rust 权威）/get/getName（反向 identity）/whenDefined（PoC 立即 resolve 经 Promise::new 三元 + resolve 调用；真 pending 延异步域）/upgrade（PoC no-op）；② create 命中 registry → `set_prototype(ctor.prototype)` upgrade（prototype 方法可达 + lifecycle 可派发；完整 ctor 执行 super() NodeId 注入链延后）；③ lifecycle——appendChild/removeChild 成功路径派发 connected/disconnectedCallback（子树 DFS tag 含 '-' fast-path 镜像 R3271 + CONNECTED_CUSTOM 状态 + 先标记后派发 + this=native 元素 + 回调缺失静默）。**PoC**：define/get 大写规范化/getName/miss + create 命中 greet()='hi-MY-EL'（prototype+native tagName 联动）+ append 'conn:MY-EL' + remove 'conn|disc'。engine quickjs 1419 / v8 2153 零回归、clippy/fmt 干净。**M6 里程碑：S0q–S5q 全部有 land 实现**（S5q PoC 深度）；剩余 = PoC→生产深度补齐（完整 ctor 执行链 / capture·bubble 链 / Event 构造器 / DOMException / weak·finalizer / S1q 复合对象）
**本轮续33**: R80 — **M4 nodes createElementNS 簇：HTML 大写 tagName + validate-and-extract + Node 常量**（commit `f8e75c28`，R79 CONTINUE 方向落地）：**根因五重**——① HTML 文档+HTML ns 的 tagName 须 ASCII 大写（spec create-element-ns，旧保留原值）② validate-and-extract 校验全缺失 ③ 非 HTML ns 元素被 instanceof HTMLElement=true ④ Node 接口常量（element.ELEMENT_NODE）不可见——proxy get trap 未知属性恒 undefined，且 Reflect.get(target) 不可用（target {} 真实原型 ≠ getPrototypeOf trap 声明的链）⑤ Element.nodeValue 返 undefined（spec null）。**实现**：_nsHandles 增 htmlUpper 标记（tagName/nodeName 大写、prefix/localName 原值解析）；validate-and-extract（空前缀/空 localName 段/非 Name 字符→InvalidCharacterError；空 ns 带 prefix/段内二冒号/xml·xmlns 保留绑定→NamespaceError）；getPrototypeOf 非 HTML ns → Element.prototype + HTML ns 按精确大小写 localName 查 iface（'SPAN'→HTMLUnknownElement）；Node 常量挂 Node.prototype+构造器，get trap SCREAMING_SNAKE 沿 getPrototypeOf 链手工查找；Element.nodeValue=null。**结果**：Document-createElementNS 0P/596F→**187P/409F**（全部 HTML-document 变体通过；剩余 409F 全为 iframe contentDocument 深结构既存 #13）；nodes 5568→**6294P（+726）**；quickjs=quickjs-native=187P（v8-native 0P 为既存分歧，clean HEAD 重建同 0P，default-on 前对齐项）；collections/events/traversal 零回归。**验证**：单测 +2；engine v8 2163/quickjs 1424、wpt-runner 171、integration **767P/0F**（html_compat 已被并行流修复）全绿；fmt/clippy 干净。**教训**：Proxy get trap 原型链回落不能 Reflect.get(target)——target 真实原型与 trap 声明原型是两回事，沿链手工查找才 trap 一致

**本轮续34**: R81 — **M4 Document/Element 导航面 + createElementNS 校验 + textContent 语义**（commit `322cf584`，接手 429 中断的工作树遗留后收口）：**驱动簇**——Document-createElement-namespace（0P→**11P/11 100%**）、Node-textContent（→**81P/0F 100%**）、Node-properties（595P/163F→**726P/0F 100%**）、Document-createElement(NS) HTML-document 变体（**195/195**）。**修复**：① createElement ns 按文档类型派生（contentType/_docNS 贯穿 createDocument/createHTMLDocument/new Document/DOMParser；XML doc preserveCase）② textContent 全语义（融合 childNodes 拼接读 + setter 替换全部子——registry 清 + _zwNodeParent 反链删 + 不解析 markup + _zwTextWritten 写入值优先；undefined=null=空串，R3184 旧语义纠正）③ **ASCII-only 大小写**（_zwAsciiUpper + localName 内联循环——JS Unicode 全量 toUpperCase 把 'ı'→'I'、'K'(U+212A)→'k' 错误转换）④ HTML createElement Name 校验（'}'/'<'/'￿' 非首合法、拒空白/'>'；含冒号名不拆 prefix）⑤ createElementNS 校验按 WPT 期望表重写（'f:o:o' 有 ns 合法、'0:a' 合法 'a:0' 非法、XMLNS ns 仅 xmlns 元素）⑥ 导航面大补（doctype/fragment/CDATA/PI/comment/text firstChild/lastChild/sibling/parentElement/length/wholeText + document.childNodes=[doctype,html] + Document 元数据族 + detached doc 同款 + body appendChild 子 parentNode 重指 body）⑦ TreeWalker sibling 结构序步进（currentNode 被滤时 _siblingByOrder lazy 找同父可接受节点）。**过程回归两起同轮修**：body 缺 hasChildNodes（traversal -21）+ walker 滤节点 sibling null——修复后 traversal 净 +7。**结果**：nodes 6294→**6596P（+302 净）**、traversal 1188→**1195P**、events/collections 零回归（clean-HEAD 重建二进制 stash A/B）。**验证**：part18 +3 单测；engine v8 **2166**/quickjs **1424**/wpt-runner **172** 全绿；fmt/clippy 双矩阵干净；pre-commit-guard PASS。**教训**：① DOM 的 ASCII-lowercase/uppercase 必须手写 ASCII 循环（Unicode 全量 API 会破坏 non-ASCII 用例）② WPT 期望表比记忆中的 spec 算法可靠（'f:o:o'/'0:a' 按表对齐）③ document.childNodes 加 doctype 波及 walker accepted 模型——结构序 lazy 步进是通用解 ④ shim 内部代理树（_tree）与公开对象（body）identity 分裂在 parentNode 断言暴露——appendChild 后重指

**本轮续32**: R79 — **M4 nodes 最大双 fail 簇：Node.contains/compareDocumentPosition 全节点形态**（commit `3214d883`，R78 CONTINUE 方向落地）：**根因**——旧实现走 host sel 快照查询（`__zw_contains`/`_ancestorChain`+`__zw_element_children`），WPT setupRangeTests 的 testNodes 全是 createElement+appendChild 建的 **pending 节点**（快照不可见）；text/comment/document/doctype/fragment 节点零方法。**实现**（JS 侧统一，host 零改动）：`_zwNodeContains`（沿 other.parentNode 链 identity 上行，与 oracle 同构）+ `_zwCompareDocumentPosition`（双链+LCA+childNodes 序；跨树 DISCONNECTED 带方向位 35/37 反对称），接线到 element proxy/_wrapNodeEntry/_zwRegisterTextEl/document/detached 工厂族。**基础设施顺修**（oracle 一致性前提）：html.parentNode→document（spec）、document.doctype + createDocument 附接 doctype + createHTMLDocument 预置、detached doc 树链接（head/body→docEl→doc）、handle 元素 previousSibling/nextSibling 经父 childNodes 融合派生（旧恒 null）、hasChildNodes 与 firstChild 视图对齐（旧矛盾）。**结果**：Node-contains 480P/1002F→**1482P/0F（100%）**、Node-compareDocumentPosition 144P/1300F→**1444P/0F（100%）**，**四路一致**（v8=quickjs=v8-native=quickjs-native）；nodes 3115P/4396F→**5568P/1943F（+2453 净 pass 零回归**，对照**重建的** clean-HEAD 二进制）；traversal 953→1188P（+235 顺带）；collections/events 零回归。**验证**：单测 +3（part18 R79 族）+ 2 处旧断言 spec 纠正；engine v8 2161/quickjs 1424、wpt-runner 171/106、integration 765P/2F（既存）、make test 除 compositor dmabuf（clean HEAD 同败）全绿；fmt/clippy 干净。**教训**：① stash A/B 必须**重建二进制**（JS shim 嵌入二进制，旧二进制含修复时基线假等——首轮对照 5568P 假一致）；② WPT oracle 用页面导航面推导期望值，导航面不一致（sibling null/hasChildNodes 矛盾）会使正确实现也 fail——修导航面即修期望一致性

**本轮续31**: R78 — **M4 quickjs WPT 矩阵最大缺口修复：execute 全局代码语义**（commit `d4eb0641`，R76 下一步首选⑤落地）：**根因**（三轮 probe + C 层确认）——`QuickJSSandbox::execute` 以 JS 包装 `String(eval(code))` 执行页面脚本，JS 层**直接 eval** 在 QuickJS 下 function/var 声明落临时词法环境而非全局对象（quickjs.c `js_parse_program` 的 `fd->is_global_var` 只对 `JS_EVAL_TYPE_GLOBAL/MODULE` 或非 strict 为真），跨 execute（跨页面 `<script>`）全部丢失；V8 经 `Script::run` 是全局代码语义——**两引擎行为分歧**。traversal 47F 整簇 `assert_node is not defined`（内联 support .js 的声明在下一 `<script>` 不可见）。**修复**：`eval_in_ctx` 改 C API `JS_Eval` + `JS_EVAL_TYPE_GLOBAL`（不加强制 STRICT；rquickjs `EvalOptions::default()` 的 strict:true 会误伤非严格页面脚本且 `#[non_exhaustive]` 无法外部构造——本就不该用），结果经 `JS_ToString`+`JS_ToCStringLen2` 字符串化（返回值契约不变）。**方案否决**：JS 间接 eval `(0,eval)`——probe 实测对 'use strict' 脚本体声明仍落临时环境（跨 eval 不可见），WPT 用例大量 strict，否决。**效果**：quickjs **traversal 7P→953P（= v8 基线，DC-7 引擎对齐）**、collections 38→48P（= v8）、nodes 2201→3113P、events 156→185P；native 路径 traversal 953P 与 polyfill 完全一致（R76 对等性保持）；抽样 ranges 用例与 v8 逐字节一致；nodes/events 剩余 fail 核实与 v8 共有（compareDocumentPosition 语义缺口两引擎同败）。**验证**：script-sandbox quickjs 76+1（新回归 `quickjs_scope_repro_r78.rs`）/ engine quickjs 1424 / wpt-runner quickjs 106 / script-sandbox v8 155 / integration quickjs 747P/2F（html_compat default_actions clean HEAD 同败既存，stash 验证）全绿；clippy/fmt 干净；ranges 全量被 insertBefore 慢用例吃满 test-guard 2400s（M1 L2 遗留，v8 同样）。**教训**：双引擎「同一 shim」≠「同一行为」——shim 之下的执行原语（eval 形态）是行为面的一部分，脚本装载层此前从未被 WPT 级对拍

**本轮续30**: R77 — **M1 L2 第二刀：handle identity 反查验证 + 剩余 -8 诊断（负结果归档）**：① `with_query_doc_live`（R43 形态）+ `LIVE_SELECTOR_TO_HANDLE` 反查表（apply 返回 map 倒置）+ `__zw_selector_handle` 回调 + `_wrapSelector` 前置查询三件套——**handle identity 打通**（live 命中返回原 `__n20` proxy，探针实证）② probe3/probe4（case.js 确切子集）全过——append→query→finally remove 语义正确 ③ case.html 全量 130→122（-8）未达 net≥0 → **回退归档**（同 R43 模式）。**增量设计输入 3 条**（evidence md）：反查三件套是 live 查询与 handle proxy 身份统一的可行形态（完整方案直接采用）/ probe 与 case.html 全量差异在 setup+多 subtest 组合（下刀带 per-subtest pending diff）/ `_wrapSelector` 全局前置反查波及 attributes 等路径（完整方案应 wire 层标记限定）。**L2 结论**：两刀共验证 live 视图 + identity + remove 语义单点全对，卡在多 subtest 累积——**M1 完整方案（`Rc<RefCell<Document>>` 三方合一）绕不开，最小切片路线两次实证不通**

**本轮续29**: R76 — **M4 quickjs WPT dom 矩阵基线 + Proxy loose 语义修复**（commit `68f09748`，DC-7 验证面直接证据）：① **基础设施**——quickjs release runner（独立 target-dir 防覆盖 v8 二进制）+ ZW_NATIVE_DOM=1 管线首次全量贯通（quickjs 构建走 R57 install_native_bindings 接线）② **双路径基线**：quickjs polyfill **2442P/1341F = 64.55%** vs native **2441P/1342F = 64.53%**——对等差 **0.02pp**（DC-7 A/B 等价量化证据；用例侧 document 走 shim，native 叠加两引擎同构）③ **真缺口发现**：collections 38P vs v8 48P——HTMLCollection Proxy set/deleteProperty trap 返 false 在 QuickJS **loose 也抛**（V8 仅 strict）→ 10 loose 断言崩用例 ④ **修复**：`_zwV8ProxySetSemantics()` lazy 引擎探测 + 拒绝路径 QuickJS 分支返 true 不写——loose 断言全过、用例完整执行；strict 断言仍挂（引擎无 caller-mode API，取舍记录）；**v8 collections 48P/0F 零回归**、events/nodes/traversal 不变。engine 1419/2153 + wpt-runner 106/171 全绿、clippy 干净、fmt 无 diff。**注记**：v8 全量 JSON 被 insertBefore 慢用例（M1 L2 遗留）吃满 1800s——分目录补齐；清理 4 个历史残留 runner 实例

**本轮续28**: R75 — **M6 S5q 完整化：whenDefined 真 pending（M6 全量收口）**（commit `4bfa87e3`）：① CE_WHEN_DEFINED 等待列表（tag → Vec<resolve Persistent>）+ ce_when_defined 已定义立即 resolve(ctor)/未定义 pending 入列表 + ce_define 成功后 flush（resolve(ctor)，取走清列表）② reset 清理 ③ **rquickjs 教训**：execute_pending_job 不能在 Ctx 借用存活时调用（safe_ref RefCell 重入 panic）——drain 须在 ctx.with 块间（测试镜像生产 quickjs_runtime.rs eval 后 drain / webview 事件循环职责）；长 PoC 断言用全新 tag（前序已注册 tag 走已定义分支 resolve 旧 ctor）。PoC 四组断言。engine quickjs **1419** / v8 **2153** 零回归、clippy 双矩阵干净、fmt 无 diff。**里程碑：M6（QuickJS native 移植）S0q–S5q 全部完整落地收口**——S0q weak 以 R74 结论关闭 / S1q 属性+复合对象三件套 / S2q 写入+mutation+树读回 / S3q 查询 / S4q EventTarget+三阶段+双构造器 / S5q 五件套+完整 ctor+observed+whenDefined pending。QuickJS native 具备与 V8 对等的元素面生产能力

**本轮续27**: R74 — **M6 S0q 续：weak/finalizer 生命周期实验 + 结论关闭**（commit `4efd76aa`，负结果切片——实验-回退-归档，同 R43/R53 模式）：① **API 事实**：QuickJS C API 无 JS_NewWeakRef 导出、rquickjs sys 未绑 JSWeakRef——Rust 侧无 weak 句柄无 finalizer 回调（V8 R3133 两支柱都缺）；JS 侧 WeakRef/FinalizationRegistry intrinsic 可用（锚点断言）② 实验一（回退）：NODE_OBJECTS 存 JS WeakRef——两次 eval 间 GC 回收树内包装 → 同 NodeId 重建新对象 → parentNode identity 断裂 ③ 实验二（回退）：removeChild 时机 evict 子树——JS 持旧引用 + re-append 重建 → childNodes[0]===held 断裂 ④ **终态结论**：strong Persistent + reset 换代全清是唯一正确形态（有界泄漏 = 单文档生命周期 detached 包装，与真浏览器 document 强链模型一致）；rquickjs finalizer 钩子缺失记 TBD ⑤ 附带发现：QuickJS GC 比 V8 激进（eval 语句间即回收）。正向证据 PoC 三组（deref 命中/无引用回收/strong identity 稳定）。engine quickjs **1419** / v8 **2153** 零回归、clippy 双矩阵干净、fmt 无 diff。**M6 仅剩 whenDefined 真 pending（微任务域）——接近全量收口**

**本轮续26**: R73 — **M6 S4q 完整化：DOMException 构造器 + instanceof 面**（commit `be431968`）：① JS 胶水构造器（R71/R72 模式三次复用）——new DOMException(msg?, name?) 缺省 ''/'Error'（R31 V8 parity）+ legacy code 映射 + prototype 链挂 Error.prototype + toString + 21 个 *_ERR 常量 + new.target 守卫 ② **throw_dom_exception 升级**：经全局构造器 construct_args new 实例（Constructor::construct 的 IntoArgs tuple 组合易碰 trait 界——Args+construct_args 是稳定形态）——全部 R66 错误路径现抛真实例（instanceof 可观测），install 前回落 plain 对象 ③ QuickJS 经单一全局构造器天然避免 V8 R6 的 wrong-global 三重根因。PoC 三组断言。engine quickjs **1419** / v8 **2153** 零回归、clippy 双矩阵干净、fmt 无 diff。**M6 剩余**：S0q 续 weak/finalizer → whenDefined 真 pending

**本轮续25**: R72 — **M6 S4q 完整化：Event/CustomEvent 构造器**（commit `1a1d8227`）：① **形态决策（rquickjs 两条 API 边界）**——`set_constructor(true)` 不绑 this 到 new 实例（QuickJS ctor 协议需 JS_CallConstructor2，高层 API 不暴露）；`This<Object>` 在 glue 调用形态收不到绑定 → **JS 构造器胶水 + Rust 原语**（R71 模式二次复用，确立为 QuickJS 标准形态）② Rust 原语两件：event_init（8 字段 init 面 + 单调 timeStamp——R22 恒 0 死循环教训 + target/currentTarget 预置 null）+ prevent_default（cancelable gate）③ JS 胶水：实例方法 stopPropagation/stopImmediatePropagation（__zw_stop flags 由 R67 dispatch 消费）/preventDefault/initEvent + Event 常量 0/1/2/3 + CustomEvent.prototype 链挂 Event.prototype + detail（缺省 null）④ 构造器实例经 R67 三阶段派发（type/instanceof/detail 保留 + 返值 !(cancelable && defaultPrevented) 生效）。PoC 五组断言。engine quickjs **1419** / v8 **2153** 零回归、clippy 双矩阵干净、fmt 无 diff。**M6 剩余**：S0q 续 weak/finalizer → DOMException 构造器 instanceof 面 → whenDefined 真 pending

**本轮续24**: R71 — **M6 S1q 复合对象收口：dataset DOMStringMap**（commit `93eccce4`）：① 形态决策——rquickjs 无 named-property-handler 等价物，动态键拦截改 **JS Proxy 胶水**（install 注入一次性工厂 `__zw_native_ds_make` 六 trap + Rust 原语四件 ds_get/set/delete/keys）② 驼峰↔kebab 转换镜像 V8 dataset.rs ③ DATASET_OBJECTS 二级身份缓存（**R69 模式第三次复用**）④ delete miss 键 no-op 成功（spec DOMStringMap——QuickJS Proxy invariant 要求 trap 返 true 否则 TypeError）⑤ set/delete 派发 attributeChangedCallback。PoC 四组断言。engine quickjs **1419** / v8 **2153** 零回归、clippy 双矩阵干净、fmt 无 diff。**里程碑：S1q 复合对象三件套（attributes/classList/dataset）全部落地，S1q 收口**。**M6 剩余**：S0q 续 weak/finalizer → Event 构造器 → DOMException 构造器 instanceof 面 → whenDefined 真 pending

**本轮续23**: R70 — **M6 S1q 复合对象续：classList DOMTokenList 面**（commit `7e4c63c4`）：① 二级身份缓存 CLASS_LIST_OBJECTS（复用 R69 模式）② 方法面全量：length/item/contains/add/remove/toggle(force+切换)/replace(四路)/value(读写)/toString ③ token 校验（空→SyntaxError、空白→InvalidCharacterError，复用 R66 throw_dom_exception；add/remove all-or-nothing）④ contains 空/空白不抛返 false（spec 例外）⑤ 写路径派发 attributeChangedCallback（R68 old 语义）。PoC 六组断言（自建元素隔离）。engine quickjs **1419** / v8 **2153** 零回归、clippy 双矩阵干净（4 处 manual_contains 修复）、fmt 无 diff。**注记**：期望值三轮修正（toggle 末尾空串/join 计数/分隔符）——期望值先逐段模拟再写。**S1q 复合对象剩余**：dataset（DOMStringMap）后收口

**本轮续22**: R69 — **M6 S1q 复合对象首片：attributes NamedNodeMap 面**（commit `18843344`）：① 二级身份缓存 ATTR_MAP_OBJECTS（owner ffi → Persistent，spec `el.attributes === el.attributes`，镜像 V8 namednodemap.rs）② 方法面：length getter（live 计数）/item(i) 越界 null/getNamedItem miss null/setNamedItem（入参对象读 name/value 兼容 plain+Attr 形）/removeNamedItem（返被移除 {name,value} 条目，miss null）③ set/removeNamedItem 派发 attributeChangedCallback（R68 old 语义扩展到 map 面）④ 条目 {name,value} plain object（Attr instanceof 面延后）⑤ reset 清缓存。PoC 五组断言（身份/零属性/闭环/miss/remove+live 跟随）。engine quickjs **1419** / v8 **2153** 零回归、clippy 双矩阵干净、fmt 无 diff。**教训**：长 PoC 断言用 #main（前序脚本累积 6 属性）致 item(0) 期望错位——detached 新建元素隔离属性序。**S1q 复合对象剩余**：classList（DOMTokenList + token 校验抛错复用 R66 基建）→ dataset

**本轮续21**: R68 — **M6 S5q 完整化：observedAttributes 过滤 + oldValue 写前捕获**（commit `d6dc00d5`，闭合 R65 两项简化注记）：① setAttribute old 写前捕获（get_attribute 先读后写，spec (name, old, new) 三参——R65 恒 null）② removeAttribute 也派发（new=null；缺失属性移除 no-op 不派发）③ observedAttributes 过滤（ctor 数组求值，name ∈ 数组才派发；缺失声明 = observe-all 保持 R65 兼容）④ 同值 set 仍派发（spec 无值变化短路，区别 MO same-value）。PoC 两组断言（v1:v2 old 捕获 + remove null + 缺失 no-op；data-skip 跳 / data-obs 派）。engine quickjs **1419** / v8 **2153** 零回归、clippy 双矩阵干净、fmt 无 diff。**M6 剩余**：S0q 续 weak/finalizer → S1q 复合对象 → Event 构造器 → DOMException 构造器 instanceof 面

**本轮续20**: R67 — **M6 S4q 完整化：三阶段 capture/bubble 派发**（commit `a8fa85b5`）：dispatchEvent 从 target 站单段升级**三阶段传播**（镜像 V8 R3128/R3135 dispatch_event_impl）——① parent 链 with_dom 纯读收集、释放 borrow 后派发 ② visits 模型：capture 祖先倒序(1，仅 capture 监听器)→target(2，全部注册序)→bubble 祖先正序(3，仅 bubble，需 bubbles:true) ③ currentTarget/eventPhase 逐站更新、派发后复位 null/NONE ④ stopPropagation/stopImmediatePropagation 缺失注入（具名 fn This<Object> 写 flag 绕闭包 HRP）+ fresh flag 复位（同 event 可重派发）+ immediate 止当前节点剩余 ⑤ listener_present 存活检查（派发中 removeEventListener 的监听器 skip，spec inner-invoke 步骤 5）⑥ 返值 !(cancelable && defaultPrevented) ⑦ dispatchEvent('type') 字符串形态兼容。PoC 四组断言（全链 p:1|m:2|g:3 / bubbles:false 跳 bubble / capture 站 stop / 复位）。engine quickjs **1419** / v8 **2153** 零回归、clippy 双矩阵干净、fmt 无 diff。**过程教训**：① 断言失败 → panic 跳过 Persistent 清理 → JS_FreeRuntime gc_obj_list SIGABRT 是**失败断言的放大器非独立内存 bug**（期望值误写全序一度误判泄漏走 bisect）② bisect 中途误用 git checkout 清工作树丢实现（本应 stash），从上下文重建——**回退工作树一律 stash** ③ capture 标志双向过滤（capture 监听器不入 bubble 站、反之亦然），期望值按 spec invoke 规则推导

**本轮续19**: R66 — **M6 S4q 完整化：DOMException 错误路径**（commit `4024e525`，接手 429 中断的 WIP：stash → rebase over canvas 流 R56h 五提交 → pop 零冲突）：① `throw_dom_exception` helper（Error 对象带 name/message/stack 经 `Ctx::throw`——JS catch 得 e.name 与 DOMException 可观测面等价；构造器 instanceof 面延后，V8 R6 identity 教训在案）+ `dom_error_name` 映射（镜像 V8 node.rs dom_error_exception）② appendChild/insertBefore 失败→HierarchyRequestError、removeChild 非子→NotFoundError（此前 null 吞错）③ createElement 非法 tag→InvalidCharacterError（`is_valid_tag_name` 镜像 V8 R3）④ customElements.define 非 callable→TypeError / 重复→NotSupportedError（此前静默覆盖）⑤ 四方法签名 `-> Value` 改 `-> Result<Value>`（Err 须返回才生效）。PoC 三断言（e.name catch 可观测）。engine quickjs **1419** / v8 **2153** 零回归、clippy 双矩阵干净、fmt 无 diff。**M6 剩余**：capture/bubble 祖先链虚站 → observedAttributes 过滤+oldValue → weak/finalizer → S1q 复合对象 → Event/DOMException 构造器

**本轮续18**: R65 — **M6 S5q 深化：完整 ctor 执行 + attributeChangedCallback**（commit `7a486499`）：① **关键发现**——`JS_CallConstructor2`（Args::construct）即使传 this 也遵循 construct 语义**新建 this 对象**（ctor body 字段落临时对象，native 元素读回 NaN 实证）；正确路径 = **this 绑定普通调用**（Args::this + apply，等价 V8 super() 注入链的 Rust 侧目标形态）——字段初始化直接落 native 元素（this.count=41 生效 + greet ++count 42/43 状态保持断言）；② attributeChangedCallback——setAttribute 路径对 registry 命中元素派发 (name, null-old, new)（写前捕获重排 + observedAttributes 过滤延后注记）。engine quickjs 1419 / v8 2153 零回归、clippy/fmt 干净。**M6 剩余**：S4q 完整化（capture/bubble 链 + Event 构造器 + DOMException）/ observedAttributes / weak·finalizer / S1q 复合对象

> **本文件由执行 agent 于 2026-08-13 按入口文档「首轮进入检查清单」逐项核实重写**，替换 bootstrap 占位符。
> 所有状态带证据（commit hash / 文件路径 / 行号 / 测试命令）。并行双流下 main 随时漂移（run-rules §10），每轮开工先 `git pull --rebase`。

---

## 当前状态（2026-08-13 核实，基线 commit `f7219b2c`）

| 项 | 状态 | 证据 |
|----|------|------|
| P1a（event loop / fetch / Observer） | ✅ 实质完成 | 入口文档基线块（fetch/定时器/MO e2e 在 `apps/browser/src/tab_js_worker.rs`）；非阻塞 follow-up 见下「缺口」 |
| P1b **V8** native bindings S0–S5 | ✅ 已 land，默认关 | `crates/engine/src/dom_bindings/` 19 文件；`mod.rs:55 native_dom_enabled()` 读 `ZW_NATIVE_DOM` env，`OnceLock` 缓存 |
| L1 Live Document 共享（V8） | ✅ 已 land | `webview.rs user_actions.rs:367` `config.native_dom` + `install_native_dom_bindings()`；`mod.rs` `parse_and_install`（`Rc<RefCell<Document>>`，注释「read-only 快照」未完全 live） |
| **V8 native API 面（核实后更完整）** | ✅ 超基线 | `mod.rs:558-624` 已注册：`__zw_native_query_selector/_all`、`create_element/text_node/comment/document_fragment`、`documentElement/body/head`、`get_element_by_id`——比入口文档基线描述更完整 |
| L2 polyfill-live 合一（V8） | ❌ 未做（M1） | `js_dom_bridge/callbacks.rs:119-137` `__zw_query_match/_all` 仍 `with_query_doc(snap, …)` 经 `parse_html(dom_html)` 重解析快照；`callbacks.rs:1454` 注释明示每次查询 re-parse |
| S6 高层 API 去字符串（V8） | ❌ 未做（M2） | Fetch/Observer/FontFaceSet 仍经 `__zw_*` String ser/deser（part02.js / part01.js） |
| **QuickJS 原生 DOM 绑定（DC-7）** | 🟡 **S0q–S5q 全部有 land 实现（R57 `b781252f` → R64 `243811c6` 十切片；S5q PoC 深度）**：4 全局工厂 + customElements 五件套（Rust CE_REGISTRY）+ 13 属性（6 setter）+ 11 方法 + LISTENERS/CONNECTED_CUSTOM/CE_REGISTRY 存储 + lifecycle 派发 + escape-hatch + webview 接线（kill-switch 仍默认关）；剩余 = 深度补齐（~~ctor 执行链~~ R65 已落 / capture·bubble / Event 构造器 / DOMException / weak·finalizer / S1q 复合对象） | `script-sandbox/src/lib.rs:167 install_native_bindings` 仅 `#[cfg(feature="v8")]` 存在，QuickJS trait 方法**不存在**；`quickjs_runtime.rs:357 register_callback` 走 `__zw_*` polyfill 桥（与 V8 共用 `js_dom_shim`） |
| S7 死代码清理 + shim 萎缩 | ❌ 未做（M5/M7） | `js_dom_shim/part01-06.js` 共 ~815KB（part01 111KB+part01b 28KB+part02 149KB+part03 148KB+part04 127KB+part05 150KB+part06 103KB） |
| **双引擎** default-on + 删 kill-switch | ❌ 未做（V8=M5, QuickJS=M7，改 Mission 级单向门） | `WebViewConfig.native_dom` 默认 `false`（`webview_builder.rs:79`） |
| 真实 SPA/WC 端到端验收 | 🟡 **双资产落地（M3 进行中）**：**WC = lit 全链闭环**（R90-R99：定义/升级/lifecycle/Shadow DOM/首渲染/响应式/事件交互，e2e_web_components + e2e_lit_library 6 组件组全绿，DC-2 第二项实质达成）；**SPA = Vue 3 mount/响应式/事件落地**（R100：e2e_vue_library 2 组——模板编译插值渲染 + @click handler + count 更新 patch，DC-2 第一项实质达成）。剩余：DC-2 验收资产化收尾（hydration/reconciliation 深场景 + QuickJS feature 同页对齐） | `tests/integration/src/e2e_web_components.rs` + `e2e_lit_library.rs` + `e2e_vue_library.rs`（`fixtures/lit/` + `fixtures/vue/vue.global.js` vendored）；R100 四层根因修复详见本轮记录 |
| WPT dom 上游基线 | ✅ **dom/nodes polyfill 55.63% / native 54.98% 双基线对等**（178 用例 / 4502 subtest；Element-classlist.html 100%）+ **dom/events polyfill 57.62% / native 51.52%**（81 用例 / 331 subtest，R40 doc/win 入链后；双路径差 6.10pp）+ **dom/collections polyfill/native 双路径 **97.96%（48P/0F，R50 Proxy 承载全灭 24F；唯一遗留 messagechannel timeout 非 DOM 域）**（R37 导入 R38-R44 渐进 + R50 收口）**（R37 导入 R38 namedItem + R43 indexed/named legacy + R44 NamedNodeMap own 枚举后，10 用例 / 48 subtest，双路径对等差 0pp）+ **dom/traversal polyfill 16.36%→49.09%（R50 live overlay）→953P（R51-R53 walker 族）→**1589P/15F=99.06%（R82-R87：whatToShow 无符号 + referenceNode 同步 + 全 nodeType 掩码 + 兄弟链 identity + 导航式 previousNode + 移除 retarget 恢复段）**+ **dom/ranges polyfill/native 双路径 39.80%**（R42 导入 + Range/StaticRange/Attr API 面，44 用例 / 98 subtest，对等差 0pp；剩余 iframe mega-case/ShadowRoot/comparePoint 深结构） | `testharness-dom`（polyfill）+ `testharness-dom-native`（ZW_NATIVE_DOM=1）双入口；R29 Event.cancelBubble setter + R32 Event.srcElement + R33 Window.event + R34 AT_TARGET stopPropagation + R35 eventPhase（Event-dispatch-order-at-target 双路径 0P→全 Pass）+ R37 dom/collections 基线 + R38 HTMLCollection-empty-name 双路径 7P/0F（100%）+ R39 pre-set stop flag 零触发 + R40 doc/win 入派发链（multiple-stopPropagation/omitted-capture 双路径 100%，bubbles-true/false 主路径 Pass）。失败聚类：dom/nodes iframe.contentDocument（深结构 html-compat）/querySelector-mixed-case（selector 域）；dom/events Event-dispatch 系列（document/window 不入 dispatch chain 深结构——R39 已诊断：host 哨兵 + 12 派发点 + 三合一 key 拆分）/shadow tree window.event（Shadow DOM 域）/~27 个 0-pass 用例；dom/collections HTMLCollection own 属性枚举（~13 fail 深结构：live + indexed/named property 完整语义 + 数字边界）/NamedAttributeMap own 枚举/文档级 getElementsByTagName('*') 返空独立 bug |
| **Canvas path-objects JS 侧 API（DC-8, v1.2 接手）** | 🟡 **基线已建**：R56→R56i 九轮用例导入 205 + panic 消除 + 语义/几何/方向/nonzero/isPointInPath/ensuresubpath/clip 相交/arcTo 切线弧/arc 环带/曲线自适应/零长剪除/roundRect 闭合对齐 → **200P/3F**（真基线 pre-R56 62F → 3F 累计净 +59 零回归）；剩余 3 深项（skew 斜切端面 / end.3 miter 边界 / scaleddashes dash 相位）记深项清单 | R56（commit `ac7240f5`）：total_cmp 修 NaN 排序 panic（backtrace 实证）+ zwNormRadius 语义（RangeError/TypeError/NaN→0/DOMPointInit）+ host p-对解析 + 负 w/h 镜像 + 扫描线段式迭代（多子路径虚假边）+ closepath-on-fill；R56c（`bd027c98`）：fill_rule_spans 方向感知 nonzero/evenodd + fillRule 透传（WebIDL 前置省略嗅探）+ roundRect 镜像绕组；evidence/2026-08-16-r56c-nonzero-fill-rule.json |
| `make test` / clippy / coverage（含 quickjs 矩阵） | ✅ 基线全绿（入口文档） | workspace ~13,000+ 测试，行覆盖 95.46%，clippy 零警告；Makefile `QUICKJS_TEST_CRATES`/`QUICKJS_CLIPPY_CRATES` CI 强制 `--features quickjs` |
| dom_bindings 独立 coverage 口径 | ✅ **已补齐（R30）+ 提升（R31/R36）**：源码 94.35%（4629/4906，15 文件）/ 全部 96.01%（20 文件）；`scripts/check-dom-bindings-coverage.sh`（lcov 解析 + 源码/测试分离 + 逐文件明细，双 feature 可参数化），`check-coverage.sh --dom-bindings` 集成 | cargo-llvm-cov 0.8.7 + llvm-tools-preview **已安装**（R30 核实推翻 R0「本地未装」记录）；R31 提升 dom_exception 71.4%→94.4% / css_style_declaration 86.7%→92.7%（+ 真 bug 修复）；R36 node.rs 91.6%→92.3%（mutation 错误路径）+ aria/role 反射；custom_elements 89.0%（剩 19 行防御/OOM 边缘） |

**核心缺口**（本目标要消除，按优先级）：
1. ~~**WPT dom 上游用例 0 导入**（DC-3）~~ → ✅ **R1 首切片已建**：dom/nodes 141 用例，41.25% pass 基线。**剩余**：扩展子目录（events/collections/...）+ native 路径对照（`ZW_NATIVE_DOM=1`）。基线暴露的真实差距已重排见下。
2. ~~**DOMException 抛出语义未实现（classList 部分）**~~ → ✅ **R2 已修**：classList token 校验抛真 DOMException（空→SyntaxError、空白→InvalidCharacterError），双路径同步 + native DOMException 构造器。dom/nodes **41.25% → 56.08%（+14.83pp）**。✅ **R3 已修**：createElement 非法标签名抛 InvalidCharacterError（双路径 + spec Name production 校验 helper），dom/nodes **56.08% → 56.45%（+0.37pp）**。✅ **R4 已修（native 路径）**：appendChild/insertBefore/removeChild/replaceChild 的 DomError 转 DOMException（WouldCreateCycle/CannotInsertDocumentRoot→HierarchyRequestError，NotAChild→NotFoundError）。**注**：R4 仅 native（node.rs），polyfill 路径架构限制（mutation 延迟批处理 + shim 无 live 祖先链）无法同步抛——待 M1 L2 polyfill-live 合一。testharness-dom 基线走 polyfill 故 R4 不提升基线数字（native 修复经单测验证，是 default-on 生产路径合规）。**剩余**：testharness-dom native 路径对照（让 R2/R3/R4 native 修复基线可见）；createElement XML/XHTML iframe 上下文（独立缺口）。
3. **polyfill vs native A/B 对照门**（DC-4）→ ✅ R0 读路径骨架 + R2 异常路径扩展（classList 抛 DOMException 等价）
3. **dom_bindings 独立 coverage 口径缺失**（DC-4）——`check-coverage.sh` 无子模块报告（M0 项 4）
4. V8 native 路径默认关 → 生产仍走 polyfill 字符串桥（DC-1，M5）
5. V8 polyfill 桥仍 re-parse String 快照，三方 Document 未合一（DC-1，M1 L2）
6. V8 高层 API 仍经 `__zw_*` String ser/deser（DC-1，M2 S6）
7. **QuickJS 页面引擎 native 完全真空**（DC-7，M6）
8. canvas path-objects 用例需重新导入 + roundRect panic/精度（DC-8，M8，但当前 canvas 流活跃碰撞）

---

## Active Milestone

**当前活跃里程碑**: **M0 — 基线建立 + polyfill-live 合一起刀**（见入口文档「Single Active Milestone」）

**M0 must-complete 入口动作进度**（入口文档「首轮进入检查清单」）：
- [x] 1. 探索 `dom_bindings/` + `js_dom_bridge.rs` + `js_dom_shim/` 事实，核实 RFC L1/L2 与现状一致 ✅（本文件即产出）
- [x] 2. 创建/重写 `master.md`（本文件）
- [x] 3. 确认 `archive/` + `evidence/` 目录存在（均空，待首轮证据追加）
- [x] 4. 补齐 dom_bindings 独立 coverage 口径（**cargo-llvm-cov + llvm-tools 已安装，R30 落地**：`check-dom-bindings-coverage.sh` + 基线 93.14% 源码）
- [x] 5. 建 polyfill vs native A/B 对照门骨架（双 feature 可参数化）✅（R0，`tests_ab_compare.rs`）
- [x] 6. 选定首切片并直接动手推进 ✅（R0：A/B 门；R1：WPT dom 基线）

**M0 状态**: 项 1/2/3/4/5/6 ✅ **全部完成**（R30 补齐项 4：cargo-llvm-cov 已装 + dom_bindings 口径 93.14% 源码）。**M0 里程碑收口完成**，转入 M4 推进（入口文档允许 M4 与 M1–M3 早期并行）。

**当前推进: M4 — WPT dom 上游基线 + 按聚类驱动修复（R5 已 land）**
- R1 已建：`testharness-dom` 子命令 + `fetch-dom-subset.sh` + `DOM_TEST_SUBDIRS=["dom/nodes"]`；基线 41.25%
- R2 已修：classList token 校验抛 DOMException（双路径）+ native DOMException 构造器。polyfill 41.25% → 56.08%
- R3 已修：createElement 非法标签名抛 InvalidCharacterError（双路径）。polyfill 56.08% → 56.45%
- R4 已修（native）：node mutation（append/insert/remove/replace）DomError→DOMException。native 路径合规
- R5 已建：`testharness-dom-native` 入口 + native DOMException constructor 修复（部分）；建立 polyfill 56.45% vs native 41.25% 双基线，定位 native 落后根因
- R6 已修（native 追平 polyfill）：DOMException identity 三重根因修复。native 41.25% → 56.08%
- R7 已做：① fetch-dom-subset 补齐 .js 依赖（.html + .js + dom 根共享 .js + testharnessreport.js）——基线真实化（polyfill 56.45%→51.12%、native 56.08%→50.79%，双路径对等，差 0.33pp；此前虚高因用例 .js 缺失被跳过）② native createProcessingInstruction API（factories invoke + document.rs 注册 + node.rs PI target/data getter + node_name PI→target 修正 + spec 校验 invalid target/data→InvalidCharacterError）+ native 单测
- R7 发现：PI 用例（Document-createProcessingInstruction.html）超时——用例外层 `test()` 嵌套多个 `test()` + `pi instanceof ProcessingInstruction`（ProcessingInstruction 构造器未装）→ testharness completion callback 未调。需 ProcessingInstruction 构造器（下轮）
- R9 已做：① polyfill document.createProcessingInstruction 桥接（`js_dom_bridge.rs` 新增 `DomMutation::CreateProcessingInstruction` + apply + query 分支；`callbacks.rs` 注册 `__zw_create_processing_instruction`；shim part06 方法 + part01 `_piHandles` + part04 PI 节点包装 nodeType=7/nodeName=target/target/data getter + part03 ProcessingInstruction 构造器占位）② **DOMException identity 对等修复**（createElement/PI 校验抛错改用 `globalThis.DOMException`，避免 native 叠加路径 wrong-global；顺带修 R3 createElement 既存对等 bug）③ polyfill PI 单测 `test_create_processing_instruction_r9`。基线 polyfill 37.82%→38.03%、native 37.59%→37.81%（双路径对等差 0.22pp）；PI 用例双路径 1P/11F→6P/6F。engine v8 2076 / quickjs 1407 / wpt-runner v8 168 / quickjs 103 单测全绿，双矩阵 clippy 干净
- R9 关键发现：**用例侧 `document` 始终是 polyfill document（即使 ZW_NATIVE_DOM=1）**——native document template（R7 PI API）用例访问不到。故 PI 必须在 polyfill document 实现，双路径用例才能过。DOMException identity：native 路径 shim 裸 `new DOMException` 抛的异常 → testharness assert_throws_dom "wrong global"，改 `globalThis.DOMException` 修复（R6 教训）
- R10 已做：① polyfill Proxy `_makeProxy` handler 加 `getPrototypeOf` trap（part05 handler 闭合处）——element→HTMLElement.prototype（链 Element→Node）、PI→ProcessingInstruction、fragment→DocumentFragment、text/comment→Node；构造器缺失回落 Object.prototype。仅影响 instanceof/getPrototypeOf/原型链查找，不影响 get/set ② DOM 原型方法不可枚余（part03 cloneNode/addEventListener/removeEventListener 改 Object.defineProperty enumerable:false，修 getPrototypeOf 副作用——for-in 枚举到原型方法污染 expando）③ **顺带修并行 canvas 流 2 个回归**：CSS.escape/supports 合并（canvas 流 part05:920 先建 CSS={percent,deg} 致 part06 `||` 短路，escape/supports 丢失 → CSS.escape is not a function；改 part06 合并模式）+ testharness.rs fetch_handler 编译错误（`wpt_root.and_then` → `wpt_data_fetch_handler(wpt_root)`，&Path 无 and_then）④ instanceof 单测 `test_instanceof_prototype_chain_r10`。基线 polyfill 38.03%→39.23%、native 37.81%→38.96%（双路径对等差 0.27pp）；cloneNode 用例 0P→51P。engine v8 2071 / quickjs 1407 单测，双矩阵 clippy 干净
- R10 归因：**6 个 fetch/response 测试既存失败**（clean R9 tree 同样存在，并行流引入）——`instanceof Response`=false（fetch 结果非 Response 实例）+ fetch abort/binary/stream/signal/forbidden-headers。归因 fetch/net 域，非 js-dom DOM 桥工作面，记未解决问题不硬解（run-rules §9）。本切片修了同源 CSS 回归（part06 工作面内）
- R11 已做：① element.localName getter（part04 get trap：HTML 元素 = tagName 小写；带 prefix 限定名去 prefix；非 Element→null）② **HTML 元素子类 instanceof**（part03 注册 ~64 个 HTML*Element 构造器 prototype→HTMLElement.prototype + `__zwHtmlTagIface` tag→接口映射表 div→HTMLDivElement 等覆盖 spec HTML 元素接口全表；part05 getPrototypeOf element 分支按 tag 查映射返对应子类 prototype）③ localName 单测 + 子类 instanceof 单测。基线 polyfill 39.23%→40.89%、native 38.96%→40.63%（双路径对等差 0.26pp）；cloneNode 用例 51P→121P（+70）。engine v8 2082 / quickjs 1408 单测，双矩阵 clippy 干净
- R11 已知边缘：`createElement('canvas') instanceof HTMLCanvasElement` 仍 false——canvas 经 `_zwMakeCanvas()` 特殊 proxy（canvas 流专用路径），不走 _makeProxy/getPrototypeOf。记未解决问题
- R12 已做：① element.prefix getter（part04 get trap：限定名冒号前；无冒号→null；非 Element→null。注：_realTag 大写化致 prefix 大写，case.js abc/Abc 态仍 fail，待 createElementNS 保留原 tag 深改）② getElementsByTagNameNS（part04 元素级 + part06 document 级，忽略 ns 按 localName 查，元素级支持 `*` 通配，返 HTMLCollection）③ prefix+getElementsByTagNameNS 单测。基线 polyfill 40.89%→41.71%、native 40.63%→41.45%（双路径对等差 0.26pp）；polyfill 净 +37 pass。engine v8 2083 / quickjs 1408 单测，双矩阵 clippy 干净
- R12 评估：**iframe.contentDocument 是深结构跨面改**（createElementNS XML/XHTML 路径需 iframe src 真实解析 + 独立 Document + 独立 window（defaultView.DOMException）+ 跨文档节点归属 ownerDocument===doc，完整 iframe 子文档 = html-compat 域），记未解决问题待评估，转零碰撞面
- R13 已做：① classList token 有序去重（part03 `_classListProxy` cur() 加 Object.create(null) seen 表，spec DOMTokenList 有序去重 + ASCII 空白分隔，`"a a a"`→["a"]）② contains() 空串/含 ASCII 空白 token → false 不抛（spec `dom-domtokenlist-contains`，区别于 add/remove/toggle/replace 的 check 抛；原 contains 误用 check 抛）③ classlist 单测。基线 polyfill 41.71%→46.60%、native 41.45%→46.33%（双路径对等差 0.27pp）；classlist 用例 1140P/280F→1360P/60F（+220，迄今最大单切片提升）。engine v8 2084 / quickjs 1408 单测，双矩阵 clippy 干净
- R14 已做：① 注册 5 缺失 event 子类构造器（part05 `_defineEventSubclass`：BeforeUnloadEvent/DeviceMotionEvent/DeviceOrientationEvent/TextEvent/TouchEvent）② createEvent map 全覆盖（part06：扩全集 alias 含复数 Events/HTMLEvents/SVGEvents→Event、MouseEvents→MouseEvent、UIEvents→UIEvent、custom→CustomEvent + 缺失子类）③ 未知 type 抛 NotSupportedError（spec `dom-document-createevent`，原 lenient 回落 Event 改 spec 合规抛，globalThis.DOMException 保 identity）④ 更新 test_event_subclasses2_r2812（UnknownEvent 改期望抛）+ 新增 createEvent alias/NotSupported 单测。基线 polyfill 46.60%→50.33%、native 46.33%→50.07%（双路径对等差 0.26pp，**dom/nodes 突破 50%**）；createEvent 用例 96P/183F→264P/15F（+168）。engine v8 2085 / quickjs 1408 单测，双矩阵 clippy 干净
- R15 已做：① implementation.createDocumentType 返 DocumentType（part06 主 document + part03 detached doc，spec `dom-domimplementation-createdocumenttype` 不校验，返 name/nodeName=qualifiedName、publicId、systemId、nodeType 10、ownerDocument、nodeValue/textContent=null；原 return null stub）② detached doc（_makeDetachedDocument）加 implementation 块（hasFeature + createDocumentType，ownerDocument 指 detached doc；用例 doTest(doc,...) 经 doc.implementation.createDocumentType）③ **顺带修并行 canvas 流 2 个 clippy 红灯**（main 既有：crates/canvas types.rs:199 `.or_else(||Some)`→`.or(Some)` + js_dom_bridge/canvas.rs:873 setWordSpacing 嵌套 if 合并，机械修正无逻辑变化）④ createDocumentType 单测。基线 polyfill 50.33%→52.11%、native 50.07%→51.84%（双路径对等差 0.27pp）；createDocumentType 用例 1P/81F→80P/2F（+79）。engine v8 2086 / quickjs 1408 单测，双矩阵 clippy 干净
- R16 已做：① classList write 加 runUpdate 比较（spec DOMTokenList update 算法：新 token 集合序列化 vs 原 attribute 原始值，相同不 setAttribute；add/remove/replace 总经此，原值含尾空格/重复时规范化重写）② toggle force 分支 no-op（force 与现状一致直接 return 不 write，保持 attribute 原样；spec toggle(token,force)，WPT checkToggle 保持原样非规范化）③ replace 顺序 + 同名 runUpdate（oldT===newT 存在→runUpdate 规范化；replace 在 oldT 位置换 newT + 移除后续重复有序去重保位置，WPT checkReplace("c b a","c","a")→"a b"）。基线 polyfill 52.11%→53.00%、native 51.84%→52.73%（双路径对等差 0.27pp）；classlist 用例 1360P/60F→1400P/20F（+40）。engine v8 2086 / quickjs 1408 单测，双矩阵 clippy 干净
- R17 已做：① createEvent 移除 9 个 non-createable modern interface（part06 map：wheelevent/pointerevent/popstateevent/progressevent/transitionevent/animationevent/pagetransitionevent/clipboardevent/errorevent，按 WPT someNonCreateableEvents 列表；spec createEvent 仅支持 legacy event interface，modern 走 `new XxxEvent()` 构造器）→ 对其抛 NotSupportedError ② 更新 2 受影响单测（test_event_subclasses2 ProgressEvent 改断言抛、test_window_onerror ErrorEvent 改 new 构造）③ **核实 event target null gap 实际不存在**（_makeEvent part03:1063-1064 已设 target/currentTarget=null，createEvent 初始化测试已 Pass，R14 误记）。基线 polyfill 53.00%→53.20%、native 52.73%→52.93%（双路径对等差 0.27pp）；createEvent 用例 264P/15F→273P/6F（+9）。engine v8 2086 / quickjs 1408 单测，双矩阵 clippy 干净
- R18 已做：① createElementNS 改经**新回调** `__zw_create_element_ns` → host `doc.create_element_ns`（DomMutation::CreateElementNS + apply + callback + shim part06 createElementNS 调本回调），**保留原 qualifiedName 大小写 + prefix + namespace**（spec createElementNS 不小写，区别 createElement HTML 小写）② shim `_nsHandles`（part01，与 `_piHandles` 对称）存 `{qualifiedName, namespace}` 原值 ③ part03 大小写敏感解析 helper `_nsLocal`/`_nsPrefix`/`_nsQualified` ④ part04 get trap：tagName/nodeName/localName/prefix isNs 走 `_nsHandles` 原值读回（不经 `_realTag` 大写化）；**新增 namespaceURI getter**（isNs 读 ns，普通 createElement 元素恒 XHTML）⑤ 更新 R12 prefix 断言（`'SVG'`→`'svg'`）+ 新增 R18 单测（abc/Abc/ABC 三态 prefix + 裸名无 prefix + localName 大小写敏感 + namespaceURI SVG/null/HTML）。基线 polyfill 53.20%→55.11%、native 52.93%→54.46%（双路径对等差 0.65pp）；case.html createElementNS abc/Abc/ABC 三态全 Pass（R12 仅 ABC）。engine v8 2087 / quickjs 1408 单测，双矩阵 clippy 干净
- R19 已做：① classList replace **校验顺序特殊**（两 token 空串 SyntaxError 先于两 token 空白 InvalidCharacterError，区别 add/remove 逐参先空后空白；`replace(" ","")`→SyntaxError）② replace **去重算法重写**（splice(i,1,newT) + 全局有序去重 seen 表，统一覆盖 newT 在 oldT 前后所有情形；`"a b c" replace("c","a")`→`"a b"`）③ `write(arr, force)` 加 force 参数（replace 返 true 时 `write(p,true)` 强制 setAttribute+notify，绕过 runUpdate「值相同 return」；spec replace 返 true 必触发 mutation）④ classList set trap readonly 分支（return true no-op，早于 className/generic fallthrough）⑤ `_clsProxyCache` per-element 缓存（part01+part03，同 `_proxyCache` 模式；spec classList cached accessor identity，`e.classList===e.classList`）+ R19 单测（校验顺序+去重+mutation+assignment no-op+identity）。基线 polyfill 55.11%→55.55%、native 54.46%→54.91%（双路径对等差 0.64pp）；**Element-classlist.html 全量 1400P/20F→1420P/0F（100%）**。engine v8 2088 / quickjs 1408 单测，双矩阵 clippy 干净
- R20 已做：① testharness `map_harness_results` status 映射精确化（原 `0=>Pass,2=>Timeout,_=>Fail` 把上游 status 3(NOTRUN)/4(PRECONDITION_FAILED) 误计 Fail）② `HarnessStatus` 新增 `NotRun`/`PreconditionFailed` 中性变体（精确映射 3→NotRun/4→PreconditionFailed，未知 5+ 保守回落 Fail）③ 通过率统计改 WPT 标准口径 pass/(pass+fail)（中性从分母排除，与上游 dashboard 一致）④ R20 单测（6 种 status 编码精确映射）。解 createEvent 剩 6F（TouchEvent `assert_implements_optional` 失败 = optional legacy touch API 不支持，spec 中性非 Fail；runner exit 1 判定不变仍作 rally 推进信号）。dom/nodes：polyfill 55.55%→55.63%（2501P/1995F+6中性）、native 54.91%→54.98%（2472P/2024F+6中性，双路径对等差 0.65pp，各 6 fail→中性）。wpt-runner v8/quickjs 单测全绿，双矩阵 clippy 干净
- R21 已做：① 扩展 `DOM_TEST_SUBDIRS` + `fetch-dom-subset.sh` SUBDIRS 加 `dom/events`（81 .html + 16 .js，jsdelivr CDN 拉取 0 失败，零生产逻辑改动）② 建立 polyfill dom/events 基线 **31.61%（98P/212F/9timeout，319 subtest，81 cases）**③ 失败聚类分析（Event 对象缺属性[30]/事件分发断言[25+12]/EventListener handleEvent[12]/returnValue[4]/incumbent-global 测试设施[5]；54 个 0-pass 用例）④ 核实 dom/nodes 双路径零回归（native classlist 1420P/0F）。**误报 native events dispatchEvent hang**（R22 推翻——非系统性 hang，是单用例 timeStamp 死循环）。wpt-runner 双矩阵 clippy 干净
- R22 已做：① 定位修复 native dom/events 死循环（**更正 R21 误报**：经诊断插桩 native dispatchEvent+polyfill __zw_parent 均 0 调用→用例走 polyfill dispatch；直接 binary 单用例 Event-dispatch-click 10P 正常；逐用例 timing 定位唯一真死循环 `Event-timestamp-safe-resolution`）② 根因 native `Event.timeStamp` 恒 0（event.rs 硬编码，注释「沙箱无 perf timer」）→ WPT `do-while(e2.timeStamp-e1.timeStamp==0)` 死循环（polyfill 用 `__zw_performance_now` 单调 timer 不 hang）③ 修复 native Event.timeStamp 改 `perf_now_ms()`（OnceLock<Instant> origin elapsed，DOMHighResTimeStamp，Number f64 保子毫秒精度）+ R22 单测（非0/有限 + 连续创建差值可收集非零）④ 建立 **native dom/events 基线 31.29%（97P/213F/9timeout，96s，从 570s 超时→96s）**，双路径对等差 0.32pp。dom_bindings v8 单测全绿，双矩阵 clippy 干净
- R23 已做：① polyfill Event eventPhase 常量补全（part05：Event 构造器 + Event.prototype 各挂 NONE=0/CAPTURING_PHASE=1/AT_TARGET=2/BUBBLING_PHASE=3，Object.defineProperty enumerable:false，guard 幂等；实例经原型链继承，CustomEvent.prototype=Object.create(Event.prototype) 链继承）② R23 单测（4 对象 Event/Event.prototype/createEvent('Event')/createEvent('CustomEvent') × 4 常量 = "0,1,2,3"×4 + 不可枚举）。Event-constants.html 双路径 0P→4P/4（100%）；dom/events：polyfill 31.61%→32.90%（102P/208F）、native 31.29%→32.58%（101P/209F，双路径各 +4 pass，对等差 0.32pp 不变）。engine v8 单测全绿，双矩阵 clippy 干净
- R24 已做：① polyfill `_defineEventSubclass` 父链继承（`_eventSubclassProps` 注册表记录 [ownProps, parentName]，构造器沿父链收集全部 props 设值——MouseEvent extends UIEvent 实例缺 view/detail 根因；子类先父类后，子类覆盖父类 spec 一致；null/undefined 用默认）② KeyboardEvent 改用工厂（extends UIEvent，补 EventModifierInit + key/code/location/repeat/isComposing/charCode/keyCode/which + getModifierState 复用 MouseEvent）③ R24 单测（MouseEvent 默认/设定 + KeyboardEvent 默认含父链 + WheelEvent 三层父链）。Event-subclasses-constructors polyfill 0P→42P/49（native 24P/49）；dom/events：polyfill 32.90%→42.58%（132P/178F，+30 pass）、native 32.58%→36.13%（112P/198F，+11 pass，**双路径差扩至 6.45pp**——native dom_bindings event.rs 旧实现缺父链继承/KeyboardEvent which/MouseEvent instanceof/UIEvent view 校验，R25 对齐）。engine v8 单测全绿，双矩阵 clippy 干净
- R25 已做：① 经 runner 实测诊断（`MouseEvent.toString()` 探 native/polyfill + forced-fail message 带属性状态）确认 native_dom=1 下 `new MouseEvent()` 走 native（覆盖 polyfill），native MouseEvent 缺 UIEvent `view` 父链属性 + KeyboardEvent 缺 `which` ② `set_ui_view` helper（设 view：缺省 null，init dict 对象原样）+ MouseEvent/KeyboardEvent 调之 ③ KeyboardEvent which（缺省回退 keyCode，spec legacy）④ R25 单测（MouseEvent view 缺省/设定 + KeyboardEvent view + which 缺省/显式）。**双路径差未缩**（6.45pp 保持，Event-subclasses native 仍 24P/49——剩余多点分散缺口：WheelEvent 子类链断[父 native MouseEvent 不在 polyfill 注册表]/SubclassedEvent class 语义/MouseEvent 属性细节/UIEvent view 校验）。R25 view/which 是 native 正确性净正（单测证明，default-on 后合规），转高 ROI 切片。dom_bindings v8 单测全绿，双矩阵 clippy 干净
- R26 已做：① polyfill Event.cancelBubble（part03 `_makeEvent` 加 cancelBubble:false 初始 + stopPropagation/stopImmediatePropagation 设 true，与 defaultPrevented 同款「公开镜像+私 flag」；part05 initEvent 重置 cancelBubble+stop flags，spec `concept-event-initialize`）② R26 单测（初始/initEvent 重置/stopPropagation/stopImmediatePropagation 联动）③ **Event-dispatch 系列评估为深结构**（capture/bubble 链不含 document/window + document/window/html 共享 listener key 无法独立派发 + document.cloneNode/new Document/new Text 基础设施缺），转 cancelBubble 轻量切片。Event-cancelBubble.html 双路径 0P→4P/8；dom/events：polyfill 42.58%→45.48%（141P，+9 pass）、native 36.13%→39.03%（121P，+9 pass，双路径各 +9 同步提升，对等差 6.45pp 不变）。engine v8 单测全绿，双矩阵 clippy 干净
- R27 已做：① polyfill EventListener handleEvent（part03 `_dispatchToListeners` 的 fire 按类型分支：函数直接 call(this=currentTarget) / 对象 Get handleEvent.call(fn)(this=对象)，每次派发都 Get 支持 getter；handleEvent 非 callable 跳过不抛）② R27 单测（对象 listener handleEvent+this=对象+evt 正确 / getter 每次 Get / 函数 listener 不回归）。EventListener-handleEvent.html 双路径 0P→3P/5；dom/events：polyfill 45.48%→46.75%（144P，+3 pass）、native 39.03%→40.26%（124P，+3 pass，双路径各 +3 同步提升，对等差 6.49pp 基本不变）。engine v8 单测全绿，双矩阵 clippy 干净
- R28 已做：① polyfill Event.returnValue（part03 `_makeEvent` 用 Object.defineProperty：getter 返 !\_defaultPrevented[canceled flag 反向镜像]，setter 仅 cancelable+false 触发 prevent，true/非cancelable no-op；preventDefault/initEvent 经 _defaultPrevented 自动联动）② R28 单测（7 spec 场景全覆盖：初始/preventDefault cancelable gate/returnValue setter gate/initEvent 重置/已 canceled no-op）。Event-returnValue.html 双路径 0P→7P/7（100%）；dom/events：polyfill 46.75%→49.68%（153P，+9 pass，**突破 49.68% 接近 50%**）、native 40.26%→43.18%（133P，+9 pass，双路径各 +9 同步提升，对等差 6.49pp 不变）。engine v8 单测全绿，双矩阵 clippy 干净
- R29 已做：① polyfill Event.cancelBubble 改 defineProperty getter/setter（part03 `_makeEvent`：getter 返 `_propagationStopped`[stop propagation flag]，setter true→置 flag 等同 stopPropagation，false→no-op；R26 普通 data 字段升级——外部 `ev.cancelBubble=true` 现有副作用止上溯）② stopPropagation/stopImmediatePropagation 移除冗余 `this.cancelBubble=true`（getter 现读 flag，flag 已设）③ `_dispatchWithBubble` try/finally 重置 `_propagationStopped=false`（spec concept-event-dispatch 步骤14，dispatch 后 cancelBubble 返 false）④ initEvent（part05）移除无效 `this.cancelBubble=false`（R29 setter false no-op），保留 `_propagationStopped=false` 重置 ⑤ R29 单测 4 场景（setter=false no-op / setter=true 置 flag / dispatch 后 flag 清 / 保留 R26 4 场景）。**Event-cancelBubble.html 双路径 4P→8P/8（100%）；dom/events：polyfill 49.68%→52.13%（159P，+6 pass，**突破 52%**）、native 43.18%→45.57%（139P，+6 pass，双路径各 +6 同步提升，对等差 6.49→6.56pp 基本不变）**。engine v8 2097 / quickjs 1410 单测全绿，双矩阵 clippy 干净
- R30 已做（M0 项 4 收口）：① 核实 cargo-llvm-cov 0.8.7 + llvm-tools-preview **已安装**（推翻 R0「本地未装」记录）② 新建 `scripts/check-dom-bindings-coverage.sh`：cargo llvm-cov -p zero-engine --lib --lcov → 内联 Python 解析，源码/测试分离（src 15 文件 / tests_* 5 文件）+ 逐文件明细 + <90% 提升候选提示 + `--json` 机器可读 + 双 feature 可参数化（`--features v8` 默认 / 透传 quickjs）③ `check-coverage.sh --dom-bindings` 集成（cov run 额外 ~15s，flag 开启不阻默认快路径）④ 基线：**dom_bindings 源码 93.14%（4561/4897）/ 全部 95.15%（6590/6926）**；最低 3 文件 custom_elements 89.0% / css_style_declaration 86.7% / dom_exception 71.4%。基线 JSON 入 evidence。纯脚本+度量零生产代码改动，**M0 里程碑 6/6 项全完成收口**
- R31 已做（DC-4 coverage 提升 + 真 bug 修复）：① **修真 bug**：`new DOMException()` 无 name 参返 `"undefined"` 而非 spec `"Error"`——`string_arg(args,1)` 对缺省参 ToString 成 "undefined"，`is_empty()` false 故 name=undefined。修 `native_dom_exception_constructor_invoke` 先判 `args.get(idx).is_undefined()`（message 同款修，spec message 缺省 ""）② 补 9 单测：DOMException 构造默认值/name→code 全表/toString/legacy 常量/constructor identity + custom element connect/disconnect/attr-change 派发路径 + CSSStyleDeclaration getPropertyPriority/item 边界/named-deleter ③ **dom_bindings 源码 93.14%→94.27%（+1.13pp）/ 全部 95.15%→95.94%**；dom_exception 71.4%→94.4% / css_style_declaration 86.7%→92.7% / custom_elements 89.0%（lifecycle 派发路径补测，剩 19 行=防御 OOM `Some else return`/`node.get None`/非元素 tag 分支，不实际触发）。engine v8 2107 / quickjs 1410 全绿，双矩阵 clippy 干净。DOMException 修正是 WPT assert_throws_dom 正确性净正（default-on 后合规）。基线 JSON 入 evidence
- R32 已做（接手 429 打断切片 + 验证 land）：① **Event.srcElement**（spec `dom-event-srcelement`，target 的 legacy IE 别名）双路径实现：polyfill part03 `_makeEvent` 加 `srcElement:null` 占位 + `Object.defineProperty` getter 读 `this.target`（dispatch 期 target 更新即反映，data 属性占位会在 dispatch 后读 null fail；setter 不定义 spec 只读）；native event.rs `set_event_init` 初始化 null + event_target.rs `dispatch_event_impl` dispatch 期与 target 同步设（data 属性镜像，与 target 同生命周期，避免 native prototype accessor 复杂度）② native 单测 `native_event_src_element_r32`（初始 null + dispatch 期 === target/派发目标）③ **fetch-dom-subset.sh 幂等快路径**：目录已含 .html 且非 FORCE=1 跳过 GitHub Contents API 列目录（避免 60/h 限流 403 阻断 `make testharness-dom`；缺文件时 fetch_raw 自身 -s 跳过已存在；FORCE=1 强制重列）。WPT `event-src-element-nullable.html` 双路径 0P→**全 Pass**。engine v8 2109 / quickjs 1411 全绿，双矩阵 clippy 干净，fmt 无 diff
- R33 已做：① **Window.event**（HTML `current event` legacy global，spec `window-event`）polyfill 双点实现：part01 初始化 `Object.defineProperty(globalThis,'event',{value:undefined,writable:true,configurable:true,enumerable:true})`（window own 属性 + for-in 可见，WPT event-global 第一个 subtest）② part03 `_dispatchWithBubble` 加 `var prevEvent=globalThis.event; globalThis.event=event;`（try 前）+ finally `globalThis.event=prevEvent`（嵌套 dispatch restore 外层 innermost-first，多个 return 经 finally 配对）③ polyfill 单测 `test_window_event_current_event_global_r33`（5 场景：own 属性/初值 undefined/dispatch 期===event/裸 event 全局可用/嵌套 dispatch restore 外层）④ **native 不改**（用例侧 dispatch 走 polyfill 未解问题 #9，native dispatch_event_impl 设 globalThis.event 是死路径 + 多 return 点全覆盖 restore 易漏致 stale event，留 default-on 前对齐）。WPT `event-global.html` 双路径 0P→**5P/7F**（剩 7F=shadow tree 内派发 + window.onerror restore 深结构）。dom/events：polyfill 174P（R29 159P→+15）/ native 154P（R29 146P→+8），双路径对等差 6.28pp。engine v8 2110 / quickjs 1411 全绿，双矩阵 clippy 干净，fmt 无 diff
- R34 已做：① **AT_TARGET stopPropagation 止同元素 listener + 双 stop-flag 兼容**（R33 发现 `Event-stopPropagation-cancel-bubbling.html` 根因两层）：`_dispatchToListeners` 'all' 模式 non-capture 循环加 `stopped()` 早退（AT_TARGET capture stopPropagation 止同元素 non-capture）；新增双 flag 兼容 helper（`stopped()`=`_propagationStopped||__zw_stop`，`immediateStopped()`=`_immediateStopped||__zw_stop_immediate`）——polyfill Event 设 `_propagationStopped`，叠加路径 native Event 设 `__zw_stop`，polyfill dispatch 须同认两 flag（未解问题 #9：dispatch 走 polyfill）② `_dispatchWithBubble` 3 处阶段检查改用 `bubbleStopped()` 双 flag + finally 重置 `__zw_stop`/`__zw_stop_immediate`（叠加路径 native dispatch_event_impl 未跑，polyfill 负责 fresh 重派发）③ polyfill 单测 `test_at_target_stop_propagation_halts_same_element_r34`（4 场景：polyfill flag 止/注册序无关/native flag 兼容/无 stop 不误伤）④ **canvas clippy 红灯归因**：R34 初版顺带删并行 canvas 流 c1e0bcc8 的冗余 `as i32`，但 push 前 `git pull --rebase` 撞并行流重写该段为 i64（自带正确性 + 无 clippy 问题）→ rebase 采纳并行流版本，js-dom 流 canvas 改动自然消解（并行流已自行修，零碰撞验证通过）。WPT `Event-stopPropagation-cancel-bubbling.html` 双路径 0P→**全 Pass**。dom/events：polyfill 175P（R33 174P→+1）/ native 155P（R33 154P→+1），双路径对等差 6.13pp。engine v8 2111 / quickjs 1411 全绿，双矩阵 clippy 干净，fmt 无 diff
- R35 已做（Event-dispatch 系列最小子集）：诊断 Event-dispatch-*.html 失败聚类发现两层缺口——① **eventPhase 全程 0(NONE)**：`_dispatchToListeners` 收 phase 参数但没翻译成 eventPhase ② document/window 不入 dispatch chain（深结构，跳过）。修：① part03 `_dispatchToListeners` 设 currentTarget 后按 phase 设 `event.eventPhase = phase==='capture'?1 : phase==='bubble'?3 : 2`（target 'all' 阶段 AT_TARGET=2，capture+non-capture listener 都 AT_TARGET，WPT Event-dispatch-order-at-target）② `_dispatchWithBubble` finally 复位 `event.eventPhase=0` + `event.currentTarget=null`（spec concept-event-dispatch 末尾）③ polyfill 单测 `test_event_phase_during_dispatch_r35`（三阶段 capture1/AT_TARGET2/bubble3 + dispatch 后复位 NONE + currentTarget null）。WPT `Event-dispatch-order-at-target.html` 双路径 0P→**全 Pass**。dom/events：polyfill 177P（R34 175P→+2）/ native 157P（R34 155P→+2），双路径对等差 6.11pp。engine v8 2112 / quickjs 1411 全绿，双矩阵 clippy 干净，fmt 无 diff
- R36 已做（DC-4 coverage 提升）：Event-dispatch document/window 入 chain 诊断为**深结构**（probe 实测：document listener 触发但 currentTarget 错位为 html——document/window/html 共享 listener key `_elKey('html')`，part06.js:152/1265/1809 postMessage/onerror/inline-handler 依赖此三合一，改之波及面大）→ 按护栏跳过；**namespaceURI getter 核实已完成**（element.rs:61 闭合 R3163，勘误）→ 转 coverage（干净低风险纯测试）。补 2 单测：① `native_node_mutation_error_paths_r36`（5 错误路径 try/catch：appendChild/insertBefore cycle→HierarchyRequestError、removeChild/replaceChild 非 child→NotFoundError，覆盖 node.rs `Some(Err(e))` + dom_error_exception WouldCreateCycle/NotAChild 映射）② `native_aria_role_idl_reflection_r36`（5 aria 反射：ariaLabel↔aria-label/role↔role/缺省空/ariaLabelledBy 多段驼峰/null→"null"，覆盖 idl_to_attr 全分支）。**dom_bindings 源码 94.27%→94.35%（+0.08pp）/ 全部 95.95%→96.01%**；node.rs 91.6%→92.3%（+4 行）。engine v8 2114 / quickjs 1411 全绿，双矩阵 clippy 干净，fmt 无 diff
- R37 已做（DC-3 扩展，纯资产导入）：导入 **dom/collections** 子目录（10 用例，HTMLCollection/NodeList/NamedAttributeMap/DOMStringMap，纯 DOM API 不依赖深结构）。testharness.rs:62 `DOM_TEST_SUBDIRS` + fetch-dom-subset.sh `SUBDIRS` 同步加 `dom/collections`；fetch 10 .html（jsdelivr CDN，首次 GitHub API 偶发慢改 CDN，R32 幂等快路径 cached 后 0.004s）。建立基线 **polyfill/native 双路径 11P/37F/1timeout = 22.9%（49 subtest，10 cases，双路径对等差 0pp——用例侧 collection 经 polyfill，native 同源）**。失败聚类：① **HTMLCollection own 属性枚举（~19 fail 主力，`_makeProxy` 缺 getOwnPropertyNames trap，返原型方法 length/item/namedItem 而非 indexed+named）** ② NamedAttributeMap own 枚举（3 fail）③ indexed/named set+delete 边缘 ④ messagechannel timeout。下一切片 R38 候选 HTMLCollection own 属性枚举。clippy 双矩阵干净，fmt 无 diff
- R38 已做（R37 collections 基线驱动）：HTMLCollection own 属性枚举（~19 fail）诊断**深结构**（live collection + 完整 indexed/named property get/set/has/delete/ownKeys + 数字 id 边界）→ 转 empty-name 轻量切片。probe 诊断：namedItem("") 命中空 id/name 元素（spec 空串非 supported property name）+ Element.children 返纯数组缺 namedItem。修：① part05 `_zwMakeCollection` namedItem 加 `if(n==='')return null` 守卫 ② part04 children 经 `_zwMakeCollection(true)` 返 HTMLCollection（_splitSelectors 已 map _wrapSelector）③ polyfill 单测 `test_htmlcollection_nameditem_empty_and_children_is_htmlcollection_r38`（namedItem 空串 + children HTMLCollection + 正向 namedItem）。WPT `HTMLCollection-empty-name` 双路径 1P/6F→**7P/0F（100%）**；dom/collections polyfill/native 双路径 22.9%→**35.4%**（+12.5pp）。dom/events 净 +1 零回归，engine v8 2115 / quickjs 1411 全绿，双矩阵 clippy 干净，fmt 无 diff
- R39 已做（Event-dispatch 系列最小子集）：完整 document/window 入 dispatch chain 诊断确认**深结构**（host `parent_selector_for('html')` 返空 → chain 止于 html；document/window/html 三合一 `_elKey('html')` listener key 被 part06:1265/1285 document/window.dispatchEvent + `_globalAddEventListener` + postMessage/onerror/inline-handler + part02 5 派发点依赖，共 **12 个 `_dispatchToListeners` 派发点**，拆分波及面大）→ 提取最小子集：**pre-set stop flag 零触发**。probe 诊断根因：`_dispatchWithBubble` 各阶段循环**先派发后才查** `bubbleStopped()` → dispatch 前外部 stopPropagation()/stopImmediatePropagation()/cancelBubble=true（R29 setter 置 flag）时 capture 第一站 html 先触发 2 次才止（WPT 实测 expected [] got [html,html,html] length 3）。修：part03 `_dispatchWithBubble` try 块开头加 `if (bubbleStopped()) return !event._defaultPrevented;`（spec `concept-event-dispatch` 步骤 2：dispatch 开始时 stop propagation flag 已设 → 跳过全部 listener 触发，三阶段全不进；target/composedPath/Window.event 赋值保留，finally 正常清 flag 保同 event 再派发恢复）+ polyfill 单测 `test_preset_stop_flag_zero_dispatch_r39`（三形态零触发 + 无 flag 正常 dispatch 不受影响）。WPT `Event-dispatch-propagation-stopped` + `Event-dispatch-bubble-canceled` 双路径各 0P/1F→**1P/0F（100%）**；dom/events polyfill 54.13%→**54.74%**（179P/148F/3TO）/ native 47.87%→**48.62%**（159P/168F/3TO，双路径各 +2 同步，对等差 6.12pp）。dom/nodes polyfill 2502P 零回归。engine v8 2120 / quickjs 1415 / wpt-runner 171 / webview 595 全绿，双矩阵 clippy 干净，fmt 无 diff
- R40 已做（document/window 入派发链，不拆三合一 key）：① listener entry 加 tgt 槽位标记（'doc'=document.addEventListener part06 直写 / 'win'=window 注册 _globalAddEventListener / undefined=html 元素注册），存储仍共存 _elKey('html') key（postMessage/onerror/inline-handler 12 派发点无 slot=全触发=旧行为零改动）② `_dispatchWithBubble` 元素链后追加 document/window 虚站（capture 反序 win→doc→链反序；bubble 正序链→doc→win；各站 `_dispatchToListeners(htmlKey,…,slot)` 过滤，listener 以注册身份触发）③ document/window.dispatchEvent 传 targetSlot（'doc'/'win'）：元素链空，path=[document,window]/[window]，doc AT_TARGET 一次+win 冒泡一次；composedPath[0]=本体 ④ detached（handle-only/不在 html 子树）不经虚站 ⑤ html 元素 target（host lifecycle `__zw_dispatch_event('html',type)`）target 站过滤 null slot——防 doc/win 注册 target 站+虚站双 fire（renderer quickjs R2941/R2943 回归 `dcl,dcl,img,window,window` 捕获，单测沙箱最小复现后修）⑥ once 移除从原始 store 数组过滤（slot 子集副本写回会丢其他槽位注册）+ 单测 `test_doc_win_dispatch_chain_slots_r40`（5 场景：元素链 doc/win 身份/doc 派发=[doc,win]/win 派发=[win]/祖先 stopPropagation 止虚站/detached 不经）。WPT multiple-stopPropagation + omitted-capture 双路径 0P/1F→1P/0F（100%）；bubbles-true/false 主路径 Pass；dom/events polyfill 54.74%→57.62%（189P/139F/3TO）/ native 48.62%→51.52%（169P/159F/3TO，+10 同步，对等差 6.10pp）。dom/nodes 2502P / collections 17P 零回归。engine v8 2121 / quickjs 1415 / webview 595 / wpt-runner 171 / renderer quickjs 124 全绿，双矩阵 clippy 干净，fmt 无 diff
- R41 已做（导入 dom/traversal + TreeWalker/NodeIterator API 面）：① 导入 17 用例（testharness.rs DOM_TEST_SUBDIRS + fetch-dom-subset.sh SUBDIRS 加 dom/traversal + support/ 3 文件手动补——fetch_dir_html 只列顶层）② `extract_script_src` 支持无引号属性值（src=../common.js 合法 HTML；此前只匹配带引号 → 外部 fetch failed 用例不跑；dom/nodes +1）③ NodeFilter 常量对齐上游全表（SHOW_PROCESSING_INSTRUCTION 0x10→0x40 修正 + SHOW_ATTRIBUTE/ENTITY_REFERENCE/ENTITY/NOTATION 补齐）④ root 校验抛 TypeError（spec document-createtreewalker 步骤 1）⑤ root/whatToShow/filter getter-only readonly ⑥ whatToShow 显式 null→0（ToUint32(null) 区别缺省 SHOW_ALL）⑦ toString branding ⑧ currentNode setter 非 Node 抛 TypeError + 合法 Node 重定位游标 ⑨ 单测 test_treewalker_api_surface_r41（10 断言组）。WPT NodeFilter-constants 双路径 100%、TreeWalker-basic 5P/1F、currentNode 2P/2F；dom/traversal 首个真实基线 polyfill 9P/46F=16.36% / native 8P/47F=14.55%。失败主力 = detached 树遍历（~30 fail，createElement+appendChild 建树不挂 body → polyfill childNodes 恒空，与 R4 同根因属 M1 L2）+ lazy 续走语义 + cross-realm。dom/events 189P / nodes 2503P / collections 17P 零回归。engine v8 2122 / quickjs 1415 / wpt-runner 171 / webview 595 全绿，双矩阵 clippy 干净，fmt 无 diff
- R42 已做（导入 dom/ranges + Range/StaticRange/Attr 端点 API 面）：① 导入 44 用例 + Range-mutations.js（DOM_TEST_SUBDIRS + fetch script 加 dom/ranges；raw.githubusercontent 间歇超时 FORCE 批量多次失败 → 逐文件 5 次重试循环 45/45；跳 crashtests[非断言]/tentative[实验 API]）② 基线聚类三簇修：part04 getAttributeNode(NS)（经 _zwMakeAttr 返真 Attr——instanceof Attr true/value/name/ownerElement，缺省 null；解 25 fail 最大簇）/ new Range() 空函数 stub→返 _makeRange() 实例 / StaticRange(init) 构造器（readonly accessor 四属性+collapsed 派生+非 Node 容器抛 TypeError；WPT StaticRange-constructor 14 subtest）③ Range setStart/setEnd spec 校验：仅拒 DocumentType 容器（Attr 允许 length=0——offset 0 合法 >0 IndexSizeError，正反断言都过）；字符数据 offset>length 抛 IndexSizeError；元素容器免上限校验（detached/handle-only proxy childNodes 视图恒空与真 0 不可分，放宽防误伤）；setStartBefore/After/EndBefore/After/selectNode 无 parent（Attr/detached）抛 InvalidNodeTypeError ④ 迭代修正：初版误拒 Attr + 元素误抛，两轮修正净 +30 ⑤ 单测 test_range_apis_r42（11 断言组）。dom/ranges 双路径 9.18%→**39.80%**（39P/59F，对等差 0pp）；Range-attribute-nodes 0→15P、StaticRange-constructor 0→14P。events 189P / nodes 2503P / collections 17P / traversal 9P 零回归。engine v8 2123 / quickjs 1415 / wpt-runner 171 / webview 595 全绿，双矩阵 clippy 干净，fmt 无 diff
- R43 已做（HTMLCollection legacy 属性 + L2 诊断）：**Part 1（未 land）**：L2 live 视图最小切片完整实现后实测净负回退——`with_query_doc_live`（thread_local 缓存键 (html, mutations.len())，全量重放——增量不可行：apply_dom_mutations ephemeral handle map 单次调用作用域）升级 4 回调；单测同块 appendChild→查询可见 ✓；WPT 实测 traversal 9P 持平（detached 树 handle-based 走 `_handleChildNodes` JS registry）/ ranges 39P 持平 / **nodes -10**（case.html iframe document 建元素期望主文档 getElementsByTagName 返 []，live 视图跨文档 pending 并入主视图）→ 全回退零残留；诊断归档 evidence/2026-08-14-r43-l2-live-view-diagnosis.md（M1 设计输入 4 条）。**Part 2（已 land）**：`_zwMakeCollection` ① indexed 0..n-1 configurable:false accessor（元素包装前快照防 a===arr 自递归 + descriptor guard 防二次包装 redefine）→ delete c[0] loose no-op/strict TypeError（spec legacy platform object；普通数组 delete 挖洞致 "before" 断言也炸）② named getter：id/name 文档序首个命中 configurable:false accessor（c.foo/delete 语义同 indexed）③ 纯数字 id 跳过 named 暴露（数组 defineProperty 数字键把 length 推到 index+1；getElementsByClassName-32 numeric IDs 回归经 clean/after 4183 行 per-subtest diff 定位同轮修）。WPT HTMLCollection-delete 双路径 0P/4F→**4P/0F（100%）**；dom/collections 35.4%→**43.75%**（21P，对等 0pp）。零回归：nodes 2503P/events 189P/traversal 9P/ranges 39P。engine v8 2124 / quickjs 1415 全绿，双矩阵 clippy 干净，fmt 无 diff
- R44 已做（NamedNodeMap own 枚举）：WPT namednodemap-supported-property-names（0P/3F）驱动——`Object.getOwnPropertyNames(el.attributes)` 期望 [indices…,names…] 但 `_attributesProxy`（Proxy({},…)）无 ownKeys trap 恒 []。修三点：① ownKeys trap 返 [数值索引 + 属性名] 文档序（spec `dom-namednodemap-supported-property-names`）② getOwnPropertyDescriptor trap（indexed/named enumerable data property / length 非枚举——Proxy invariant 要求 ownKeys 键均有 descriptor）③ get trap named 分支（attrs.<name> 命中当前属性名返 Attr 节点，与枚举一致）+ 单测 `test_namednodemap_own_enumeration_r44`（4 断言组：getOwnPropertyNames 顺序/named descriptor 访问/for-in 枚举/移除收缩）。WPT namednodemap-supported-property-names 双路径 0→**3P/0F（100%）**；dom/collections 43.75%→**50.00%**（24P 对等 0pp）；dom/nodes 2503→2507P（+4 named getter 解锁 el.attributes.<name> 模式）。诊断归档（未修）：querySelector-mixed-case Test 2 [viewbox] 得 0 根因非 selector（zero-dom has_attribute 经 attr_name_effective 已 HTML 小写/SVG 精确——语义正确）而是用例建 **detached 树**后 root.querySelectorAll（detached root 无 selector 元素作用域 host 查询返空，M1 L2 handle 树同根因）；M8 canvas 碰撞仍热延后。零回归 events 189P/traversal 9P/ranges 39P。engine v8 2125 / quickjs 1415 全绿，双矩阵 clippy 干净，fmt 无 diff
- R45 已做（MutationObserver 语义 + S6 评估）：S6 正体（MO target 用 native node 去 ser/deser）评估结论 = **依赖 L2 身份桥**（ZW_NATIVE_DOM=1 叠加下页面 proxy 仍 polyfill selector/handle，换 native NodeId 需 L2 per-node 桥）→ 延后记 M2 前置条件；评估暴露两 MO 语义 bug 修：① `apply_dom_mutations` 批内 id 追链——`el.id=;el.className=` 双 mutation selector 均取 proxy 建立时（#old），首条应用后 #old 消失次条失配旧 `?` 硬错中止整批（MutationObserver-attributes 整用例崩）；`rewrite_pending_id_selectors`（for→while-let VecDeque + 借用调整）id 改名成功后重写剩余队列 #旧→#新（精确/后代前缀，nth-child 不受影响，其他 stale 仍原错误路径）② attributeOldValue——IDL 反射 setter（id/className/title/lang/type）与 classList write notify 在写入后 old 恒 null；part04 set trap 头预判内容属性名预捕获 + part05 尾 notify 携带 + part03 classList 写前捕获（setAttribute 路径 R3025 已有）+ 单测 `test_mutation_observer_id_chain_and_oldvalue_r45`（双 mutation 入 record + oldValue id=old/class=c0，microtask 轮询 flush）。WPT MutationObserver-attributes 双路径 0P（崩）→**30P/8F**；dom/nodes 2507→**2539P（+32）**/native 2509P。零回归 events 189P/collections 24P/traversal 9P/ranges 39P。剩余聚类：setAttributeNS attributeNamespace 字段 + same-value records 数量 + childList fragment 展开。engine v8 2126 / quickjs 1415 全绿，双矩阵 clippy 干净，fmt 无 diff
- R46 已做（MutationObserver NS/no-mutation/classList record）：R45 后剩 8F 三簇修：① NS record——setAttributeNS/removeAttributeNS 原委托 setAttribute（record 带限定名 + namespace null）；NS 族自带 notify（attributeName=localName 拆 prefix + attributeNamespace=ns + 写前 old 捕获）+ 直写 host 回调绕 delegate 无 ns notify（初版双发同轮修）② removeAttribute(NS) 缺失属性不发 record（spec 仅已存在属性被移除才 queue；presence 同 hasAttribute 源）③ classList record——update 步骤 8 值相同仍 set+notify（移 R16 early-return；"same value mutation" 2 条）+ remove 空集原缺失不写不 notify（`_readClassRaw` absence 判定；中途 classlist -10 回归同轮修复）+ 单测 `test_mutation_observer_ns_and_no_mutation_r46`（NS record 三断言 + no-record 两场景 + same-value add）。WPT MutationObserver-attributes 双路径 30P/8F→**38P/0F（100%）**；dom/nodes 2539→**2547P（+8）**/native 2517P；Element-classlist 1420P/0F 维持。零回归 events 189P/collections 24P/traversal 9P/ranges 39P/childList 10P。剩余：childList fragment 展开 + insertNode/surroundContents 几何。engine v8 2127 / quickjs 1415 全绿，双矩阵 clippy 干净，fmt 无 diff
- R47 已做（childList fragment 展开 + sibling 字段 + remove record + surroundContents 解耦）：① fragment record——appendChild/insertBefore/replaceChild(DocumentFragment) 的 addedNodes = fragment 子节点（ceAdded 快照；fragment 自身不出现）② sibling 字段——appendChild previousSibling=写前容器 lastChild/nextSibling=null；insertBefore prev=refNode 前兄弟/next=refNode；remove 双兄弟移除前捕获 ③ el.remove() 补 childList removed record（旧缺）+ _zwSuppressRemoveRecord 组合抑制 flag ④ surroundContents record 文档序与树操作逆序解耦（正序 remove 破坏 nth-child——renderer R2930 quickjs 测试同轮捕获两次回归均修：正序 2 子残留 + 闭包外 sel/handle 未定义）+ 单测 test_mutation_observer_childlist_fragment_r47。WPT MutationObserver-childList 10P/4F→**15P/1F**；dom/nodes 2547→**2552P（+5）**/native 2522P；剩 1F = surroundContents 第 2 record previousSibling 读 host 延迟态（M1 L2 同根因诊断归档）。零回归 events 189P/collections 24P/traversal 9P/ranges 39P/classlist 1420P/renderer quickjs 55+126P。engine v8 2128 / quickjs 1415 全绿，clippy 干净，fmt 无 diff
- R48 已做（parsed CharacterData + SetChildText 全链）：MutationObserver-characterData 4P/12F 整簇 = appendData 等 is not a function（方法仅 handle-based 有，WPT 编辑 parsed 文本节点无 handle）。修：① host `DomMutation::SetChildText { parent_selector, child_index, text }`（父 sel + childNodes 索引，与 __zw_child_nodes 同序）+ `__zw_set_child_text` 回调 ② `_wrapNodeEntry` 文本/注释补五方法 + data/nodeValue setter（父 sel + `__zwChildIndex` 盖章；本地同步防 stale）+ characterData record 带写前 oldValue ③ `MO.observe(textNode)` 无 id 时回落父元素 id。同轮 notify 参数序 bug probe 修。WPT MutationObserver-characterData 双路径 4P/12F→**18P/0F（100%）**；dom/nodes 2552→**2566P（+14）**/native 2536P；Node-textContent 17P 持平。R45-R48 MO 族累计：attributes/characterData 双 100%、childList 10→15P、nodes +59。零回归五子目录。engine v8 2129 / quickjs 1415 全绿，双矩阵 clippy 干净，fmt 无 diff
- R49 已做（observe options + textContent 语义 + 注册文本可编辑）：① options 校验（全缺/oldValue 矛盾/filter 矛盾抛 TypeError）② 隐含启用（oldValue/filter/charOldValue 存在即隐含观测）③ filter 不提供 oldValue（_mo_obs_wants_attr_old 收紧）④ textContent= spec 语义——同值 no-op（写前读+本地优先，两轮回归修正：写后 latest-wins 恒同值跳注册→firstChild 复 null；本地编辑元素级 lw 读不到）+ 异值发 childList 不发 characterData（R3027 pragmatic 移除）+ firstChild/lastChild 接 _zwLocalChildNodes ⑤ _zwRegisterTextEl 节点补 data/nodeValue setter+五方法（__zw_set_child_text 索引 0 + __zw_mo_notify_text 全局 hook 发 record target=文本节点）⑥ host：SetChildText 越界 fallback 父 set_text_content + 入 id 追链集 ⑦ R3025/R3027/R3028 遗留测试按 spec 语义更新 + 单测 test_mo_observe_options_and_textcontent_records_r49。sanity 11→**16P/0F**、takeRecords 1→**3P/0F**、attributes 38→**40P/0F**、characterData 19P/0F 维持（四 100%）、childList 15→**18P/1F**；dom/nodes 2566→**2579P（+13）**/native 2549P。R45-R49 五轮累计四 100% + nodes +72。零回归五子目录。engine v8 2131 / quickjs 1415 全绿，双矩阵 clippy 干净，fmt 无 diff
- R50 已做（HTMLCollection Proxy 承载 + live overlay）：① Proxy 全 trap（own 枚举/描述符/set/defineProperty/delete/has + prototype length/item/namedItem/@@iterator + illegal invocation）② canonical 索引（`_zwIsCanonicalIndex` 0≤n<2^32−1 + `_zwToUint32` mod 2^32）③ named 仅 HTML ns + 空串排除 ④ live overlay（`_zwHCLiveInvalidate` 挂 _mo_notify childList 汇流点 + pending 表 handle 子树展开 + 构建时并入）⑤ getElementsByTagName 匹配模型（HTML ns 查询 ascii-lower+精确；非 HTML ns 原样精确）⑥ handle 元素 childNodes registry 回落。**collections 双路径 24P/24F→48P/0F**、nodes +29、**traversal +18**；六轮过程回归同轮修。engine v8 2132 / quickjs 1415 全绿，双矩阵 clippy 干净，fmt 无 diff
- R51 已做（TreeWalker NodeFilter + Document 构造器解锁）：① NodeFilter callback 语义四件（Get-per-traverse/抛错传播/this 绑定/缺失 TypeError）② nextNode lazy 步进（结构序+filter 步进时执行）③ NodeIterator referenceNode/beforeRef ④ new Document() + detached 工厂族 + ownerDocument 链 ⑤ runner meta-long 60s + TIME_LIMIT 透传。**traversal +889（925P）/native 893P、nodes +449（2957P）、ranges 非 mutations +1808（1847P）**；acceptNode-filter 100%；两子目录零回归。engine v8 2132 / quickjs 1415 全绿
- 剩余聚类（按 ROI，R39 后重排）：① **HTMLCollection own 属性枚举**（dom/collections ~13 fail，深结构已诊断：live + indexed/named property 完整 get/set/has/delete/ownKeys + 数字 id 边界 + 文档级 getElementsByTagName('*') 返空独立 bug，评估为独立深结构切片或随 M1 L2）② **Event-dispatch 剩余**：R40 已落 doc/win 入链（虚站+槽位）；剩 cloneNode(true)/new Document()/createHTMLDocument() 独立 Document 实例（bubbles-true/false 各 3F）+ Event-dispatch-detached-*/other-document 等跨文档变体——独立 Document 基础设施深结构（html-compat 邻域）③ 双路径差 6.12pp 收口（dom/events WheelEvent 子类链/SubclassedEvent，分散低 ROI）④ NamedAttributeMap own 枚举（dom/collections 3 fail）⑤ iframe.contentDocument（深结构 html-compat 域）⑥ querySelector-mixed-case（selector 域）⑦ polyfill appendChild 闭环（M1 L2）⑧ 扩 DOM_TEST_SUBDIRS（dom/traversal 17 / dom/ranges 44）⑨ ~~Event-dispatch-multiple-cancelBubble.html / multiple-stopPropagation~~（R40 doc/win 入链已顺带解锁，R41 核实双路径 Pass）⑩ dom_bindings coverage 剩余（custom_elements 89.0% 剩 19 防御行低 ROI）⑪ native Window.event / 双 stop-flag 统一（default-on 前对齐项，随 M1 L2 自然解决）

**M0 首切片（R0）**: **polyfill vs native A/B 对照门骨架（must-complete 项 5）**
- 理由：入口文档明列 must-complete；纯新增测试文件，零生产代码改动、零碰撞；为后续 M1(L2)/M6(QuickJS) 所有迁移切片提供「行为不退化」安全网（DC-4）；双 feature 可参数化设计为 M6 提前铺路。
- 设计：对同一 HTML + 同一可观测 DOM 操作（如 `getAttribute`/`textContent`/`tagName`/`querySelector` 命中），断言 native 路径（`install_dom_bindings` + `__zw_native_*`）与 polyfill 路径（`generate_js_dom_shim` + `register_dom_callbacks` + `__zw_*`）返回一致。聚焦**行为等价**（可观测结果），不强求 API 形态同构（native=真对象 vs polyfill=Proxy）。

**为何本轮不选 L2-read-only / canvas path-objects 作为首切片**（决策记录）：
- **L2 完整改 polyfill 桥是深结构跨面改**：`register_dom_callbacks` 签名收 `Arc<Mutex<String>>`（dom_html 快照），改读 live Document 需改签名为 `Rc<RefCell<Document>>`，触及 renderer/browser/reftest 三处调用点（callbacks.rs:18-21 注释明示）→ 触发入口文档「深结构护栏」，不宜作首切片。L2 的最小只读子集（仅 getElementById/querySelector getter）虽可行，但缺 A/B 对照门前无法证明行为等价，风险高于先建安全网。
- **canvas path-objects 当前是热碰撞面**：`git log` 核实 canvas 流最近非常活跃（`f7219b2c` 刚改 `js_dom_shim/part05.js` + `canvas.rs` + `testharness.rs`），part05.js 的 canvas 段与本目标 v1.2 接手段并发编辑 → 按入口文档护栏「碰 canvas 共享面前先 git log，有活跃编辑则转零碰撞面」，本轮转 A/B 对照门（DOM 面，零碰撞）。canvas path-objects 待 canvas 流告段落再接手（记「未解决问题」）。

**本轮/本切片进度**: ✅ **完成**——polyfill vs native A/B 对照门骨架已实现并验证（`crates/engine/src/dom_bindings/tests_ab_compare.rs`）。
- 9 条读操作用例（tagName/nodeType/getAttribute/hasAttribute/querySelector(All)/getElementById/descendant/反射 id）+ querySelectorAll 索引读 + 2 个 sanity → **native 与 polyfill 读路径全等价**（4 测试函数全绿）
- 双 feature 验证：v8 矩阵 zero-engine 2063 测试全绿（含新 4 个）；quickjs 矩阵 zero-engine 1405 全绿（A/B 模块 `#[cfg(feature="v8")]` 排除，不影响 quickjs）；双矩阵 clippy 零警告
- 关键结论：**实证 native 读路径与 polyfill 读路径行为等价**——为 M1 L2-read-only 切片（polyfill 桥改读 live Document）提供了可直接复用的 A/B 验收门

---

## 测试基线

| 基线 | 命令 | 当前值 |
|------|------|--------|
| zero-engine 测试（v8） | `cargo test -p zero-engine --features v8 --lib` | ✅ 2153 passed（R56f 实测维持） |
| zero-wpt-runner 测试（v8/quickjs） | `cargo test -p zero-wpt-runner --features v8 --lib` | ✅ 181 passed（含 R20 status 映射单测，R20 实测） |
| dom/nodes 通过率口径 | pass/(pass+fail)（中性 NotRun/PreconditionFailed 从分母排除） | polyfill 5452P/232F（R141 全量实测——characterSet-normalization 654F→0F 后；剩余 232F 以 realm/adopt 族为主 + 少量 API 语义簇） |
| zero-engine 测试（quickjs） | `cargo test -p zero-engine --no-default-features --features quickjs --lib` | ✅ 1416 passed（R56b 实测零回归） |
| zero-webview 测试（v8） | `cargo test -p zero-webview --features v8` | ✅ 17 passed（native_dom 接线回归） |
| clippy（v8 + quickjs 双矩阵） | `cargo clippy -p zero-engine ...` | ✅ 零警告（双矩阵） |
| workspace 全量 `make test` | `make test` | ✅ 66 套件全绿零失败（R141 实测复核，双矩阵） |
| 行覆盖率（全量） | `scripts/check-coverage.sh` | 95.46%（基线；**本地缺 `cargo-llvm-cov`，本轮无法实测**） |
| dom_bindings 覆盖率（独立） | `scripts/check-dom-bindings-coverage.sh`（R30）→ R31/R36 提升 | ✅ 源码 94.35%（4629/4906）/ 全部 96.01%（R36 +0.08pp），双 feature 可参数化，逐文件明细 |
| product-smoke | `make product-smoke` | 本轮非渲染热路径变更，A/B 骨架（纯测试）豁免 |
| bench-gate | `make bench-gate` | 本轮非 JS 桥热路径代码变更，豁免 |

---

## Coverage 矩阵

| crate/模块 | 行覆盖率 | 趋势 | 备注 |
|------------|----------|------|------|
| dom_bindings（zero-engine 子模块） | **源码 94.35%（4629/4906，15 文件）/ 全部 96.01%（20 文件）** | R30 首建 → R31 +1.13pp → R36 +0.08pp | `scripts/check-dom-bindings-coverage.sh`（lcov 解析 + 源码/测试分离 + 逐文件明细）；R31 提升：dom_exception 71.4%→94.4% / css_style_declaration 86.7%→92.7%（+ 真 bug 修复）；R36 node.rs 91.6%→92.3%（mutation 错误路径）+ aria/role 反射；custom_elements 89.0%（剩 19 行防御/OOM 边缘不实际触发）；持续提升不退化 |

**覆盖率口径规则**：不缩范围伪造达标；新代码必带测试；持续提升、不退化。

---

## Latest Evidence

| 日期 | 轮次 | 证据 | 结果 |
|------|------|------|------|
| 2026-08-27 | R305 | tree-order 4F 诊断（无代码 land，wpt-data 探针已还原）+ evidence/2026-08-27-r305-tree-order-identity-diagnosis.md | 四上下文首分歧点定位（Document=结构桥 iframe 缺口 / 其余=wrapper identity 深结构）；R306 候选三项记档；基线无扰动 |
| 2026-08-27 | R304 | innerHTML 同 turn 读视图三件（part04/05.js）+ evidence/2026-08-27-r304-same-turn-innerhtml-view.md | MutationObserver-inner-outer **1P/1F→2P/0F（100%）**；MO 全族恰 -1F；ParentNode/Range-mutations/nodes（Fail set-diff 恒等）/traversal 全持平；engine 2442 绿 |
| 2026-08-27 | R303 | MO disconnect record 清空（part01.js + part24.rs）+ evidence/2026-08-27-r303-mo-disconnect-record-purge.md | MutationObserver-disconnect **1P/1F→2P/0F（100%）**；MO 全族恰 -1F；inner-outer 归因 R220 记档；engine 2442 绿 |
| 2026-08-27 | R302 | cross-realm MO 机制三件（part01/05.js + part24.rs）+ evidence/2026-08-27-r302-cross-realm-mo-reporting.md | cross-realm 1F 形态深化（构造器/绑定/上报全通，剩工厂 body 可观察域记 R220）；MO 全族 +1P；EventListener/realm 族持平；engine 2440 绿 |
| 2026-08-27 | R301 | appendChild move record 兄弟字段 + surround 顺序移除语义（part04/06.js + part24.rs）+ evidence/2026-08-27-r301-mo-move-record-siblings.md | MutationObserver-childList **23P/2F→25P/0F（100%）**；MO 全族恰 -2F；Range-mutations/insertNode/surround 全持平；engine 2439 绿 |
| 2026-08-27 | R300 | insertBefore step-3 序 + step-6 死代码复活 + trap step-6（part03/04.js + part24.rs）+ evidence/2026-08-27-r300-insert-before-step3-ordering.md | Node-insertBefore **32P/8F→40P/0F（100%）**；R296 回归哨兵全绿（insertNode/surround/NodeIterator/delete/extract/clone 全 0F）；replaceChild 等 8 套件持平；engine 2438 绿 |
| 2026-08-27 | R299 | 属性选择器 i/s 大小写标志（part05.js + part24.rs）+ evidence/2026-08-27-r299-attr-case-flags.md | mixed-case detached 阶段 41 断言全过（探针实证）；indoc 恒 0 归因宿主 handle-append 不可见（R220 深结构转档）；selector 全族/attr 消费方全持平；engine 2437 绿 |
| 2026-08-27 | R298 | querySelector :scope 子树作用域（selector_match.rs + part24.rs）+ evidence/2026-08-27-r298-scope-pseudo-class-subtree.md | ParentNode-querySelector-scope **2P/2F→4P/0F（100%）**；selector 全族 2048P/7F→**2050P/5F**（set-diff 恰 -2）；closest/matches/shadow 全持平；engine 2436 绿 |
| 2026-08-27 | R297 | querySelector escapes 孤立代理 never-match（part01/03/04.js + **part24.rs 新段**）+ evidence/2026-08-27-r297-escapes-lone-surrogate-raw-id.md | escapes **66P/2F→68P/0F（100%）**；dom 全量 sweep 双跑 54101P/94F→**54106P/92F**（set-diff Fail 恰 -2；Timeout 差为 All-content flaky 基线 4 跑 2 Timeout 实证）；engine 2435 绿（part24 +1） |
| 2026-08-27 | R296 | insertBefore pre-insertion 校验族（part03/04.js + part23.rs）+ evidence/2026-08-27-r296-insert-before-validation.md | Node-insertBefore 文件级崩→**32P/8F（+30P）**；dom 全量 54070P→**54100P**（+30，其余域零回归）；三处首版回归当场抓回记档；engine 2434 绿 |
| 2026-08-27 | R295 | iframe realm Text/Comment 构造器（part05.js + part23.rs）+ evidence/2026-08-27-r295-iframe-realm-constructors.md | Text/Comment-constructor 双 **16P/0F（100%）**；dom 全量 89F→**87F**（set-diff 恰 -2）；MO parser 3F 归因 parse-time 基建记档；engine 2433 绿 |
| 2026-08-27 | R294 | MO record 兄弟字段 + addedNodes textEl identity（part04.js + part23.rs）+ evidence/2026-08-27-r294-mo-record-siblings.md | MO-childList 22P/3F→**23P/2F** + inner-outer **-1F**；dom 全量 54066P→**54070P/91F→89F（-2）**；R129 试加 record 回退教训记档；engine 2432 绿 |
| 2026-08-27 | R293 | insertAdjacent 家族 spec 语义（part03/04/05.js + part23.rs）+ evidence/2026-08-27-r293-insert-adjacent-family.md | Element-insertAdjacentText **6P/0F** + insert-adjacent **14P/0F（双 100%，均整文件崩）**；dom 全量 54045P→**54066P/93F→91F（+21/-2）**；engine 2431 绿 |
| 2026-08-27 | R292 | querySelector 结构元素身份归一 + docEl 属性反射（part03/05.js + part23.rs）+ evidence/2026-08-27-r292-struct-node-identity.md | Element-matches **675P/0F** + webkitMatchesSelector **669P/0F（双 100%）**；dom 全量 95F→**93F**（set-diff 恰 -2 零新增）；engine 2430 绿 |
| 2026-08-27 | R291 | matches 的 doc-root 序列化 + 键回落（part03.js）+ evidence/2026-08-27-r291-matches-doc-root-serialization.md | 直测 body/head matches false→true；套件数字不变（归因收窄至 querySelector wrapper identity）；dom 全量 set-diff **0/0**；engine 2429 绿 |
| 2026-08-27 | R290 | Element-remove registry 清理（通知后）+ Node-constants 下划线正则 + 接口 constructor 自反 + XML 原型回落（part03/04/05.js + part23.rs）+ evidence/2026-08-27-r290-nodes-remove-constants-constructors.md | Element-remove/Node-constants/Document-constructor 全 **100%**；dom 全量 54041P→**54045P / 101F→95F（-6）**；nodes 12669P/51F；engine 2429 绿 |
| 2026-08-27 | R289 | Range 构造器初始边界 + 叶子工厂 childNodes 统一 + selectNodeContents node-length + intersectsNode 严格不等（part05/06.js + part23.rs）+ evidence/2026-08-27-r289-constructor-selectnode-intersects.md | Range-constructor/selectNode/intersectsNode 全 **100%**（selectNode 整文件崩→304P）；dom 全量 53733P→**54041P（+308）**；delete/extract/clone +4/+5/+4 连带；ranges **真实失败面归零**；engine 2426 绿 |
| 2026-08-26 | R288 | comparePoint 顺序 + compareBoundaryPoints 重写 + setStart/End 精确校验 + iframe load 微任务延迟（part01/06.js + part21/23.rs）+ evidence/2026-08-26-r288-compare-point-boundary-set.md | compareBoundaryPoints **592F→0F** + Range-set **240F→0F** + comparePoint **124F→0F**（三大域全 100%）；dom 全量 52778P→**53733P（+955）**；ranges 九套件 100%；engine 2423 绿；A/B 证实剩余 ranges Fail 全 pre-existing |
| 2026-08-26 | R287 | clone 侧 doc 守卫（scParOk 对称移植）+ part23 doc-sc clone 单测 + evidence/2026-08-26-r287-clone-doc-guard-full-ab.md | cloneContents **187P/0F（100%）——ranges 六套件全 100%**；dom 全量 A/B 九轮累计 +61P（0 回归）；engine 2420 绿 |
| 2026-08-26 | R286 | 容器 handle innerHTML 空态（part04 _isContainerHandle 分支）+ part23 shadow 全删单测 + evidence/2026-08-26-r286-container-innerhtml-empty.md | ShadowRoot deleteContents **4P/0F（100%）**；ranges 全量 37857P（set-diff 0 回归）；五套件 100%；detach 连带翻绿；engine 2419 绿 |
| 2026-08-26 | R285 | firstPartial clone 引导（extract/clone 双侧，限定 sc.parentNode===cac）+ r283 f53 期望更新 + evidence/2026-08-26-r285-firstpartial-clone-bootstrap.md | extractContents 186P/1F→**187P/0F（100%）** + clone 184P/3F→185P/2F；ranges 全量 37855P（set-diff 0 回归）；四套件 100%；engine 2418 绿 |
| 2026-08-26 | R284 | doc 容器 covered children（_coveredChildren 白名单 +nodeType 9）+ frag 域归一（extract/clone frag 归 ownerDocument）+ part23 doc 容器单测 + evidence/2026-08-26-r284-doc-container-realm-fragments.md | extract 183P/4F→**186P/1F** + clone 180P/7F→**184P/3F**（+7）；ranges 全量 37853P（set-diff 0 回归）；其余套件全保持；engine 2418 绿 |
| 2026-08-26 | R283 | element-sc 一层+sibling 提取形态（part06 R283 形态 A/B + sc-is-cac 尾段）+ part23 f48/f53 单测 + evidence/2026-08-26-r283-element-sc-extract-forms.md | extract 178P/9F→**183P/4F（+5）**；ranges 全量 37846P（set-diff 0 回归）；其余套件全保持；engine 2417 绿 |
| 2026-08-26 | R282 | extract doctype 抛（R282 IIFE）+ doc-sc 父守卫修正（R280 段 scParOk）+ part23 doc-sc 单测 + evidence/2026-08-26-r282-extract-doctype-doc-guard.md | extract 168P/19F→**178P/9F（+10）**；ranges 全量 37841P（set-diff 0 回归）；其余套件全保持；engine 2416 绿 |
| 2026-08-26 | R281 | cloneContents 路径克隆分支（part06 R281 段：跨容器组树 + 同节点 CD 切片 + doctype 抛 + 空切片守卫）+ part23 四形态单测 + evidence/2026-08-26-r281-clonecontents-path-clone.md | clone 158P/29F→**180P/7F（+22）**；ranges 全量 37831P（set-diff 0 回归）；deleteContents/insertNode/surround 100% 保持；engine 2415 绿 |
| 2026-08-26 | R280 | 跨容器 extractContents 分支（part06 R280 段：路径克隆组树 + moveIn 兜底 + doc-sc 尾段）+ part23 52,x 形态单测 + evidence/2026-08-26-r280-cross-container-extract.md | extract 156P/31F→**168P/19F（+12）**；ranges 全量 37809P（set-diff 0 回归）；deleteContents/insertNode/surround 100% 保持；engine 2414 绿 |
| 2026-08-26 | R279 | sc=element 跨容器 deleteContents 分支（part06 R279 段：尾部止于 ec 路径子 + 同树位守卫 + 塌缩序分叉）+ part23 三形态单测 + evidence/2026-08-26-r279-sc-element-cross-container-delete.md | deleteContents 117P/8F→**125P/0F（100%，十三轮累计 +45）**；dom 全量 52717P（+10，set-diff 0 回归）；engine 2413 绿 |
| 2026-08-26 | R278 | iframe 域元素兄弟 getter（part05 `_zwMDefineSiblings` 尾簇）+ deleteContents sc-CharData/ec-element 分支（part06 R278 段）+ insertBefore 自插入重定向（part05 R279 前置）+ part23 单测 + evidence/2026-08-26-r278-iframe-realm-sibling-getters.md | deleteContents 112P/13F→**117P/8F**（22/28/52,x 全解 +5P）；insertNode 1840P/0F 持平（自插入回归修复）；engine 2412 绿；24,x 翻真（sc=element 缺口记 R279） |
| 2026-08-24 | R218 | CDATA splitText（nt=4 + 尾保型）+ detached fragment insertBefore（part03 三件 + part21 单测）+ evidence/2026-08-24-r218-cdata-splittext-fragment-insertbefore.md | insertNode 916P→**952P（+36）** + surround 865→**873P**；全量 51589P→**51631P/3405F/23T**（净 +42P）；native **51634P** flips 仅 3 既存 flaky；engine 2358 全绿 |
| 2026-08-24 | R219 | insertBefore own-property 防递归 + proxy 委托（part03）+ insertNode endOffset 同步（part06）+ deep-clone Text/Comment 工厂化 + 三方法原型兜底 kill-switch 暂缓 + part21 单测 + evidence/2026-08-24-r219-insert-before-anti-recursion.md | insertNode 952P→**1094P（+142，12–15,x 溢出全消）** + clone 149→153P + extract 98→100P；surround 873→854P（-19 跨轮残留定位）；净约 **+126P**；engine 2360 全绿 |
| 2026-08-24 | R220 | deep-clone CDATA/PI 保型（part03）+ docEl linkage 成对实验回退记录（part05）+ part21 单测 + evidence/2026-08-24-r220-deep-clone-cdata-pi.md | insertNode 1094P 零位移 + 跨轮残留结构根因定位（docEl 空壳克隆链 + head/body 平行树——fresh-doc 深项实证）；engine 2366 全绿 |
| 2026-08-25 | R221 | fresh-doc：iframe doc body/head 重绑（part03 appendChild R221 段 + part21 单测）+ evidence/2026-08-25-r221-fresh-doc-body-rebind.md | insertNode 1094P→**1637P（+543）** + mutations 0→**1338P（整族解锁）** + surround +11 + delete +12 + extract/clone +3/+3；ranges 族净 **≈ +1910P**；engine 2367 全绿 |
| 2026-08-25 | R222 | doctype 入 childNodes（part05 unshift + part21 退化路径单测）+ evidence/2026-08-25-r222-doctype-in-childnodes.md | insertNode 1637P→**1669P（+32）** + surround 865→**893P（+28）** + nodes/events +2/+2；净 ≈ +64P；engine 2368 全绿 |
| 2026-08-25 | R223 | _r223SetParent 健壮写入（part03 四站点 + part21 单测）+ evidence/2026-08-25-r223-robust-parent-node.md | insertNode 1669P→**1693P（+24）** + traversal +7；净 ≈ +29P；R222「fd 空」修正为探针伪影；engine 2376 全绿 |
| 2026-08-25 | R224 | insertNode node 自身类型合法性（part06 `_r215Validate` 第四查：非 Frag/Doctype/Element/CharData → HRE，Document(9)/Attr(2) 拒）+ part21 单测 + evidence/2026-08-25-r224-insert-node-node-type-validity.md | insertNode 1693P→**1817P（+124）** 文件级 148F→**23F**；nodes/events/collections/traversal/surround 全稳；净 ≈ +124P；engine 2377 全绿 |
| 2026-08-25 | R225 | fragment 展平三站点（part03 `_zwMEl` + part05 工厂 insertBefore）+ syncEnd handle-aware identity + fragment newOffset + doc 级 insertBefore 先摘除（part03 7568）+ part23 回归单测 + evidence/2026-08-25-r225-fragment-flatten-doc-detach.md | insertNode 1817P→**1838P（+21）** 文件级 23F→**2F**（仅 30,4）；refNode-advance 实测净 0 已回退（30,4+28,0 成对记 R226）；净 ≈ +21P；engine 2378 全绿 |
| 2026-08-25 | R254 | 幽灵烘焙点定位（gen 探针排除缓存域 + GHOST/UPLIFT 深展开）+ part06 clone 循环前摘除 newParent + r254 单测 + r210 场景②期望修正（R239 遗留红灯 bisect）+ evidence/2026-08-25-r254-clone-loop-ghost-fix.md | surround 1806P/34F→**1810P/30F（+4，13,0/14,0 全解）**；ranges 全量 38676P→38680P set-diff 0 回归；native 同值 1810P/30F；engine 2394 全绿 |
| 2026-08-25 | R255 | iframe 子文档 body 内容标准化提取 + docEl own cloneNode(deep) 保真 head/body（part05 两段 + part23 r255 单测）+ evidence/2026-08-25-r255-iframe-docelement-clonenode-fidelity.md（R256 轮补录） | surround 1810P/30F 持平；ranges set-diff 0/0；engine 2395 全绿；16,x 定位为异步 fetch-rebuild 时序深项 |
| 2026-08-25 | R256 | factory docEl 三 mutation 后 `_r130WireSiblings` 重接线 + `_r223SetParent` 替裸赋 + part04 sibling trap plainParent 分支 + title ns/prefix 补齐（part03/part04 + part23 r256 单测）+ evidence/2026-08-25-r256-factory-docelement-sibling-rewire.md | surround 1810P/30F→1822P/18F（17,x 全簇）；ranges 上游 set-diff +13 F2P / 0 P2F；nodes/events/coll/trav 持平；engine 2396 全绿 |
| 2026-08-25 | R257 | surroundContents 元素主路径清子后 inclusive-ancestor 检查 + HRE 先变更后抛（part06 + part23 三场景单测）+ evidence/2026-08-25-r257-self-surround-hre.md | surround 1822P/18F→1826P/14F（18,0/19,6）；ranges set-diff +4 F2P / 0 P2F；engine 2397 全绿 |
| 2026-08-25 | R258 | surround 三 selectNode 尾部（R212/R236/R242）parentNode miss 时 sc.childNodes 回退（part06 + part23 回归单测）+ evidence/2026-08-25-r258-selectnode-sc-fallback.md | surround 1826P/14F→1829P/11F（28,0/30,4/30,11）；ranges set-diff +3 F2P / 0 P2F；engine 2398 全绿 |
| 2026-08-25 | R259 | surroundContents kids===0 分支 extract-first（part06 + part23 回归单测）+ evidence/2026-08-25-r259-leaf-hre-extract-first.md | surround 1829P/11F→1840P/0F（100%）；ranges set-diff +11 F2P / 0 P2F；engine 2399 全绿 |
| 2026-08-26 | R260 | live-range 注册表 + _zwAdjustRangesForData 三键跨域匹配 + 四域接线（part03/04/05/06 + part23 r231 更新与 r260 回归单测）+ evidence/2026-08-26-r260-live-range-data-mutation-adjust.md | insertData/deleteData/appendData 三套件 100%（382/564/384P）；extract +19P；ranges 全量 +196 F2P / 0 P2F；engine 2400 全绿 |
| 2026-08-26 | R261 | splitText 两段 live-range 边界更新（part03/part04 + part23 回归单测）+ evidence/2026-08-26-r261-splittext-range-retarget.md | splitText 96P/20F→116P/0F（100%）；ranges set-diff +20 F2P / 0 P2F；engine 2401 全绿 |
| 2026-08-26 | R262 | removeChild pre-remove 边界迁移：_zwAdjustRangesForRemove + 六站点接线（part03/part04 + part23 r257 期望更新 + r262 回归单测）+ evidence/2026-08-26-r262-removechild-range-migration.md | removeChild 2P/18F→**20P/0F（100%）**；splitText/surround/insertNode 100% 持平；appendChild/replaceChild/deleteContents stash A/B 零回归；engine 2402 全绿 |
| 2026-08-26 | R263 | insert 侧边界调整：_zwAdjustRangesForInsert + 九站点（part04 appendChild/replaceChild 全路径 + part03 doc own/通用 replaceChild + part23 r263 单测）+ evidence/2026-08-26-r263-insert-range-adjust.md | appendChild 42P/28F→**70P/0F（100%）** + replaceChild 30P/30F→**60P/0F（100%）**；mutations 非超时套件全 100%；engine 2403 全绿 |
| 2026-08-26 | R264 | insertBefore 两段钩子 + text-ref 死循环根因定位（探针链五轮：表格二分→clean-HEAD 复现→PRE 视图→引擎直调→indexOf 自旋结论）+ 诊断注记 + evidence/2026-08-26-r264-insertbefore-hang-diagnosis.md | appendChild/replaceChild/removeChild 100% 全保持；insertBefore hang 为预存（clean-HEAD 同形）；engine 2403 全绿 |
| 2026-08-26 | R265 | textEl ref registry 插入两分支（物化+前插 / 物化后位次 splice）+ part23 r265 单测 + evidence/2026-08-26-r265-textref-registry-insert.md | insertBefore 0P/超时→**76P/0F（100%）**；mutations 八套件全 100%；insertNode/surround 100% 持平零 P2F；engine 2404 全绿 |
| 2026-08-26 | R266 | deleteContents 同节点 CharData（detached）放宽（R228 extract 同款对齐：guard + si 门两处）+ part23 r266 单测 + evidence/2026-08-26-r266-deletecontents-detached-chardata.md | deleteContents 80P/49F→**92P/37F（+12）**；extract/clone/surround 持平；engine 2405 全绿 |
| 2026-08-26 | R267 | deleteContents ancestor 分支（ec 头部 deleteData + contained 逆序移除 + 塌缩；remove 校验兜底）+ part23 r267 单测 + evidence/2026-08-26-r267-deletecontents-ancestor.md | deleteContents 92P/37F→**98P/31F（+6，两轮累计 +18）**；extract/clone 持平；engine 2406 全绿 |
| 2026-08-26 | R268 | deleteContents 跨容器 CharData 分支（sc/ec 爬升 + cac 中段四段；element 端点收敛排除）+ part23 r268 单测 + evidence/2026-08-26-r268-deletecontents-cross-container.md | deleteContents 98P/31F→**102P/27F（+4，三轮累计 +22）**；全套件持平；engine 2407 全绿 |
| 2026-08-26 | R269 | deleteContents document 容器分支（nodeType 9 限定的 contained 删除 + 塌缩）+ 24,x oracle 回退定性 + part23 r269 单测 + evidence/2026-08-26-r269-deletecontents-document-container.md | deleteContents 102P/27F→**108P/21F（+6，四轮累计 +28）**；全套件持平；engine 2408 全绿 |
| 2026-08-26 | R270 | foreignDoc title 域 cDP/contains 补齐（R243 同款委托）+ 全树走查探针 + part23 r270 单测 + evidence/2026-08-26-r270-title-cdp-methods.md | deleteContents 108P/21F→**113P/16F（+5 含 24,x oracle 连带，五轮累计 +33）**；全套件持平；engine 2409 全绿 |
| 2026-08-26 | R271 | removeChild plain 子 registry 剔除放宽（容器限定→_handleChildren 成员判定）+ 探针链三轮 + part23 r271 单测 + evidence/2026-08-26-r271-plain-child-removechild.md | deleteContents 113P/16F 持平（6,x actual 侧算法已正确——残余归因 expected 克隆域记 R272）；全套件全持平；engine 2410 全绿 |
| 2026-08-26 | R272 | 6,x expected 侧归因（dual-iframe probe 复刻 + oracle 内部步骤直调）+ evidence/2026-08-26-r272-6x-expected-side-attribution.md | 诊断轮无代码 land；POST-A 正确（R271 上下文生效实证）+ 根因定位 wrapper identity churn（oracle nextNode 遍历 wrapper ≠ childNodes wrapper）|
| 2026-08-26 | R273 | CDATA 兄弟导航 getter（part03 createCDATASection 字面量 + parentNode.childNodes 现算）+ oracle 相位追踪方法论 + part23 r273 单测 + evidence/2026-08-26-r273-cdata-sibling-navigation.md | deleteContents 113P/16F→**115P/14F（+2，6,x 全解；七轮累计 +35）**；全套件持平；engine 2411 全绿 |
| 2026-08-26 | R274 | 22,x oracle walk 归因（真实文件注入法 + nextNode 链追踪）+ evidence/2026-08-26-r274-22x-oracle-walk-attribution.md | 诊断轮无代码 land；根因 = 克隆域 P#a.nextSibling falsy（part04 元素 sibling trap 缺口，22/48/52/53,x 共同前置）；测试文件已 restore 零残留 |
| 2026-08-26 | R275 | expected iframe 扁平 body 定位（sc 父链三件 dump + 三轮假设演进）+ evidence/2026-08-26-r275-expected-iframe-flat-body.md | 诊断轮无代码 land；根因 = restoreIframe 双 iframe 轮转的 expected 侧 body 绑定缺口（R221 邻域）；测试文件已 restore 零残留 |
| 2026-08-26 | R276 | 22,x 比较遍历归因（PRE dump 推翻 R275 + isEqualNode 比较循环注入 + 双侧同步 nextNode walk）+ evidence/2026-08-26-r276-22x-comparator-walk-attribution.md | 诊断轮无代码 land；真根因 = 两个遍历面的平行 text wrapper（data 源不同——range 端点修剪 vs 导航读 host）；R277 = 单一对象源修复 |
| 2026-08-26 | R277 | text wrapper 复现域收窄（顶层/单 iframe/单轮克隆轮转三域 probe 全 clean）+ evidence/2026-08-26-r277-repro-domain-narrowing.md | 诊断轮无代码 land；分歧需多轮克隆轮转累积或双 iframe 交错——R278 二分复现 |
| 2026-08-25 | R253 | detached-build + 缓存失效实验（负结果，已回退）+ evidence/2026-08-25-r253-detached-build-negative.md | 基线 1806P/34F 零漂移；R252 假设证伪；无代码 land |
| 2026-08-25 | R252 | 幽灵定性收口（wrap 否定 + 数组标记 + wrapperChurn 身份中继）+ evidence/2026-08-25-r252-ghost-is-cache-midstate.md | 基线 1806P/34F 零漂移；幽灵 = 序列化缓存 clone 循环中途快照非活树；无代码 land |
| 2026-08-25 | R251 | 幽灵溯源四连（标记/身份/唯一性/父链）+ evidence/2026-08-25-r251-ghost-provenance.md | 基线 1806P/34F 零漂移；幽灵 = 真插入的 P|a{HEAD-only} 非 paras[0]；无代码 land |
| 2026-08-25 | R250 | 双树重 dump 深度 3/5（主幽灵已消 + 残余 P|a{HEAD{titleless}} 签名）+ evidence/2026-08-25-r250-ghost-p-resolved-new-signature.md | 基线 1806P/34F 零漂移；无代码 land |
| 2026-08-25 | R249 | 幽灵 P 栈捕获（Node.prototype.remove 数组分支 splice 视图副本）+ part05 iframe 元素 own removeChild + r249 单测 + evidence/2026-08-25-r249-iframe-element-own-remove-child.md | surround 1806P/34F 净 0；ranges set-diff 0/0；insertNode 1841P；nodes 57=57；native 同值；engine 2393 全绿 |
| 2026-08-25 | R248 | run() 内联探针 12 轮（幽灵 P 定位）+ part06 显式摘除守卫 + evidence/2026-08-25-r248-harness-inline-probe.md | surround 1806P/34F 净 0；ranges set-diff 0/0；insertNode 1841P；nodes 57=57；make test 1F 环境项 |
| 2026-08-25 | R247 | 引擎级复现证伪 no-op 假设（r247_tmp_repro 已清理，无代码 land）+ evidence/2026-08-25-r247-engine-repro-disproves-noop.md | 基线 1806P/34F 零漂移；fdZ Zbl:2,2 + 顶层全链工作；R246「no-op」判定为 dump 伪影 |
| 2026-08-25 | R246 | 17,x/13–14,x 树等价诊断（R246-probe 三轮，已清理，无代码 land）+ evidence/2026-08-25-r246-17x-tree-divergence-diagnosis.md | 基线 1806P/34F 零漂移；三发现：iframe-window factory docEl no-op / newParent 克隆引用错位 / documentElement 对象域分裂 |
| 2026-08-25 | R245 | factory parentNode getter-only 移动语义（part03 两站点 + r245 单测）+ evidence/2026-08-25-r245-factory-parent-move-semantics.md | surround 1806P/34F 净 0（17,x 形态迁移）；ranges set-diff 0/0；insertNode 1841P 保持；nodes 57=57；engine 2392 全绿 |
| 2026-08-25 | R244 | Document 容器 doctype contained surround HRE（part06 `_r244DoctypeCheck` + r244 单测）+ evidence/2026-08-25-r244-doc-container-doctype-contained-hre.md | surround 1794P→**1806P（+12 纯增，25/26,x 12F 簇全解）**；ranges 全量 set-diff 12 Fail→Pass/0 Pass→Fail；insertNode 1841P 100% 保持；nodes 57=57 零扰动；events/collections/traversal 基线不变；native 同值；engine 2391 全绿 |
| 2026-08-25 | R243 | cDP 重评转正（part03 + r243 单测）+ evidence/2026-08-25-r243-cdp-retest-positive.md | surround 1768P→**1794P（+26 净）** + extract +8 + clone +7；ranges 全量净 +110；insertNode 100% 保持；nodes 零扰动；native 同值；engine 2389 全绿 |
| 2026-08-25 | R242 | 元素 ec 祖先 extract/surround 全序（part06 三件 + r242 单测）+ evidence/2026-08-25-r242-element-ec-ancestor.md | surround 1734P→**1768P（+34，24,x 全解）** + extract +4；ranges 全量 38 Fail→Pass/0 反向；insertNode 100% 保持；native 同值；engine 2388 全绿 |
| 2026-08-25 | R241 | wrapper 域 move 语义兜底（part06 + r241 单测）+ evidence/2026-08-25-r241-wrapper-move-semantics.md | surround 1701P→**1734P（+33，28,x 簇主解）** + extract +2；ranges 全量 36 Fail→Pass/0 反向；insertNode 100% 保持；native 同值；engine 2387 全绿 |
| 2026-08-25 | R240 | 祖先 extract contained 中段子移动 + 快照（part06 + r240 单测）+ evidence/2026-08-25-r240-ancestor-extract-contained.md | extract 125P→**126P**；surround 持平（28,x 深诊断推进）；insertNode 100% 保持；native 同值；engine 2386 全绿 |
| 2026-08-25 | R239 | partial 检查序对调 + nextNode 遍历（part06 + r239 单测）+ evidence/2026-08-25-r239-partial-check-order.md | surround 1671P→**1701P（+30，INVALID_STATE 簇全解）**；ranges 全量 30 Fail→Pass/0 反向；insertNode 100% 保持；native 同值；engine 2385 全绿 |
| 2026-08-25 | R238 | Node.prototype.remove 泛型（part03 + r238 单测）+ evidence/2026-08-25-r238-node-prototype-remove.md | surround 1612P→**1671P（+59）** + extract +4 + nodes +2；events 失败集一致；insertNode 100% 保持；native 同值；engine 2385 全绿 |
| 2026-08-25 | R237 | surround 路径 4 全序（part06 三件 + r237 单测）+ evidence/2026-08-25-r237-surround-path4-full-order.md | surround 1543P→**1612P（+69，12–14,x 全解）**；ranges 全量 69 Fail→Pass/0 反向；insertNode 100% 保持；native 同值；engine 2384 全绿 |
| 2026-08-25 | R236 | 祖先元素区间 extract/surround 全序（part06 三件 + r236 单测）+ evidence/2026-08-25-r236-ancestor-element-range.md | surround 1509P→**1543P（+34，23,x 全解）** + extract +3；ranges 全量 37 Fail→Pass/0 反向；insertNode 100% 保持；native 同值；engine 2383 全绿 |
| 2026-08-25 | R235 | leaf-newParent extract-first 两形态（part06 同节点守卫）+ r235 单测 + cDP own-prop 负结果回退 + evidence/2026-08-25-r235-leaf-newparent-extract-first.md | surround 1421P→**1509P（+88，0 新失败）**；ranges 全量 88 Fail→Pass/0 反向；insertNode 100% 保持；native 同值 +88；engine 2382 全绿 |
| 2026-08-25 | R234 | 动态 documentElement getter（part05）+ extract 记账两件（part06）+ r234 单测（part23）+ evidence/2026-08-25-r234-dynamic-docelement-getter.md | surround 1385P→**1421P（+36，12–14,x 解锁）** + ranges 全量 39571→**39640P**；insertNode 100% 保持；native 同值；engine 2381 全绿 |
| 2026-08-25 | R233 | sim 全源注入复刻（13 函数依赖链）——复刻形态 sim/host 一致（均 INVALID_STATE），实证 HRE/INVALID_STATE/cDP 三簇同根（R219 × fresh-doc）（evidence/2026-08-25-r233-sim-full-injection-replica.md） | 零代码 land；R234 转向 fresh-doc 深项（~250F 钥匙） |
| 2026-08-25 | R232 | cDP-only 开关拆解（-28P 与三开相同，有害轴=cDP 本身）+ leaf kids>0 分支不可达实证 + HRE 簇 sim 考古（evidence/2026-08-25-r232-cdp-only-and-hre-analysis.md） | 455F 聚类就绪；两实验回退零 land；R233 靶点 = sim 全源注入探针 |
| 2026-08-25 | R231 | 同节点区间 extract 不塌缩（part06 R211 同节点分支移除 collapse + part23 r231:2,8 断言）+ 沙箱直跑 sim 源方法论 + evidence/2026-08-25-r231-same-node-no-collapse.md | surround 1269P→**1385P（+116 纯增）** + 全量 ranges **+116**；insertNode 100% 保持；engine 2380 全绿 |
| 2026-08-25 | R230 | Text 容器 leaf-newParent extract 先行（part06 leaf 分支 3/4 路径 + part23 r230 断言）+ evidence/2026-08-25-r230-text-leaf-extract-first.md | surround 1190P→**1269P（+79 纯增）** + 全量 ranges **+79**；insertNode 100% 保持；engine 2380 全绿 |
| 2026-08-25 | R229 | sim 全序复刻三步（part06 leaf-newParent 7/8 先 extract / R228 detached 不塌缩 / R211 同节点 self-collapse + part23 三断言扩展）+ evidence/2026-08-25-r229-same-node-collapse.md | surround 943P→**1190P（+247 纯增）** + 全量 ranges **+251**；insertNode 100% 保持；engine 2380 全绿 |
| 2026-08-25 | R228 | detached 同节点 CharData 区间 surround（part06 R211/_r212 守卫放宽 + null 三守卫 + HRE 不吞 + part23 单测）+ evidence/2026-08-25-r228-detached-chardata-interval.md | surround 893P→**943P（+50 纯增）** + extract +12 + 全量 ranges **+62**；insertNode 100% 保持；engine 2380 全绿 |
| 2026-08-25 | R227 | surround 947F 重聚类（evidence/2026-08-25-r227-surround-recluster.md）——两 A/B 负结果：R219 kill-switch 复测仍 -28P 保持关；单侧祖先 HRE 预检 -2P 回退（sim 在 extract 塌缩后 parent_ 判定） | 893P/947F 基线聚类就绪；comment 分裂族 84F + sim 全序复刻 153F 记 R228 靶点；零代码 land |
| 2026-08-25 | R226 | `_zwMEl` insertBefore ref 位先取后摘（part03 `_r226RefIdx`，spec pre-insert 的 referenceNode index 先于 adopt 摘除固定）+ part23 回归单测 + evidence/2026-08-25-r226-self-ref-insert-before.md | insertNode 1838P→**1841P/0F 整文件 100%**（30,4/28,0 全 4 断言转绿）；nodes +1、其余全稳；engine 2379 全绿 |
| 2026-08-24 | R217 | insertNode Document 子位置规则（part06 Fragment/Element/DocumentType 三组 + part21 单测）+ evidence/2026-08-24-r217-insert-node-document-position-rules.md | insertNode 902P→**916P（+14）**；全量 51576P/3463F/20T→**51589P/3449F/21T**（净 +13P **P2F=0 纯增**）；native **51589P 逐计数一致** flips 仅 2 既存 flaky；engine 2357 全绿 |
| 2026-08-24 | R216 | iframe docEl 入 doc 树（part05 parentNode/childNodes/children + part21 单测；doctype 入首位 -55 回退）+ evidence/2026-08-24-r216-docelement-in-doctree.md | insertNode 902P 持平（25,x setup IndexSizeError 消除——DOM 层 R217）；全量 51575P→**51576P**（净 +1P 中性结构正确）；surround/delete/extract/clone 零扰动；native **51575P** flips 仅 1 既存 flaky；engine 2355 全绿 |
| 2026-08-24 | R215 | insertNode ensure-pre-insertion validity 前置（part06 校验四件 + _r215NoValidate 序 + part21 单测；parentNode 环根因 upwalk 实证）+ evidence/2026-08-24-r215-insert-node-pre-insertion-validity.md | insertNode 628P→**902P（+274）** + surround 829→865 + extract 86→98 + clone 137→149；全量 51302P/3737F/20T→**51575P/3463F/21T**（净 +273P **P2F=0 纯增**）；native **51576P** flips 仅 1 既存 flaky；engine 2354 全绿 |
| 2026-08-24 | R214 | iframe documentElement 结构修复两件（part05 docEl ownerDocument 两形态 + 无 `<html>` fallback 门控 + part21 单测）+ evidence/2026-08-24-r214-iframe-docelement-structure.md | 全量 51243P/3795F/21T→**51302P/3737F/20T**（净 +59P：surround +36 / clone +12 / extract +12 / delete +6-8）；native **51301P** flips 仅 1 既存 flaky；engine 2353 全绿 |
| 2026-08-24 | R213 | extract 收缩偏移修正 + deleteContents CharData 删侧（part06 + part21 单测 + 两旧断言 spec 纠正；忠实复刻探针方法论）+ evidence/2026-08-24-r213-extract-collapse-offset-delete-branch.md | surround 817P→**829P**（+12 P2F=0）+ delete 45P→**58P** + extract 85→86；全量 51216P/3820F/21T→**51243P/3795F/21T**（净 +27P **P2F=0 纯增**）；native **51244P** flips 仅 1 既存 flaky；engine 2352 全绿 |
| 2026-08-24 | R212 | surroundContents CharData 路径闭合三根因（part05 fragment 展平 + part06 步骤1 nt=11/步骤3 清子/CharData 路径 + part03 CDATA cloneNode + part21 单测）+ evidence/2026-08-24-r212-surround-chardata-path.md | surround 823P→817P（,20 族转绿+6,x DOM 转绿，剩 6,x positionTests R213）+ clone 127→137 + extract 81→85；全量 51208P/3828F/21T→**51216P/3820F/21T**（净 +8P）；native **51216P 逐计数一致** flips 仅 2 既存 flaky；engine 2351 全绿 |
| 2026-08-24 | R211 | extractContents CharData 区间分支（part06 三段算法 + part21 单测）+ evidence/2026-08-24-r211-extract-chardata-interval.md | extractContents 61P→**81P** + cloneContents 127P→**137P**；全量 51188P/3847F/20T→**51208P/3828F/21T**（净 +20P，F2P 24 全在 extractContents）；native **51208P 逐计数一致** flips 仅 2 既存 flaky；engine 2350 全绿 |
| 2026-08-24 | R210 | surroundContents 步骤 1/2 校验序（part06 InvalidStateError 部分包含 DFS + 步骤 1 前移 + part21 单测）+ evidence/2026-08-24-r210-surround-invalid-state.md | surround 733P/1107F→**823P/1017F**（P2F=0/F2P=90 纯增）；全量 51096P/3937F/22T→**51188P/3847F/20T**（净 +92P/-90F）；native **51187P/3847F/21T** flips 仅 1 既存 flaky；engine 2349 全绿 |
| 2026-08-24 | R209 | iframe/detached 工厂节点方法面 + surround/insert 叶子异常链（part03/05/06/01b + native dom_exception.rs + part21 单测；探针 win.run() 直驱 22 形态）+ evidence/2026-08-24-r209-iframe-node-method-face.md | surround 645P/1195F→**733P/1107F** + insert 487P→**628P**；全量 50796P/4238F/21T→**51096P/3937F/22T**（净 +300P/-301F；surround 逐 subtest F2P 286 / P2F 198 全为「sim 修好、host 真实缺口暴露」非回归）；native **51098P/3937F/20T** flips 仅 2 既存 flaky Timeout；engine 2348 全绿 |
| 2026-08-24 | R208 | iframe doc 查询缓存同键失效（part03 三处 removeChild + part21 单测；探针链 11 轮实证键撞车）+ evidence/2026-08-24-r208-iframe-query-cache-invalidation.md | surround 0P/1840F→645P/1195F + insert 1P/1839F→486P/1354F；全量 49541P/5004F/22T→**50796P/4238F/21T**（净 +1255P/-766F，零真回归 Pass→Fail=0，364 新失败行全为新暴露 subtest）；native fail 集逐条一致；engine 2347 全绿 |
| 2026-08-13 | goal 拆分 bootstrap | 入口文档基线事实块 | 框架占位 |
| 2026-08-13 | R0 核实 | 代码实测（commit `f7219b2c`）：dom_bindings 19 文件 / QuickJS native 真空确认 / L2 仍 re-parse 确认 / canvas wpt-data 缺失确认 / cargo-llvm-cov 未装确认 | 本文件重写，勘误见下 |
| 2026-08-13 | R0 首切片 | A/B 对照门骨架 `tests_ab_compare.rs`（9 读用例 + 索引读 + 2 sanity）；v8 2063 + quickjs 1405 + webview 17 全绿，双矩阵 clippy 干净 | **native 读路径 ≡ polyfill 读路径**（行为等价实证），M0 项 5+6 完成 |
| 2026-08-13 | R1 | WPT dom/nodes 基线：`testharness-dom` 子命令 + `fetch-dom-subset.sh` + `DOM_TEST_SUBDIRS` + Makefile；141 用例 / 2696 subtest / **41.25% pass** | DC-3 首切片达成；暴露 DOMException 抛出语义（~474）/ createProcessingInstruction（44）/ XML doc 模型（98）为最高 ROI 修复方向 |
| 2026-08-13 | R2 | classList token 校验抛 DOMException（双路径）+ native DOMException 构造器（`dom_exception.rs`）+ A/B 门异常路径扩展；v8 2065 / quickjs 1406 / wpt-runner 167 全绿，双矩阵 clippy 干净 | **dom/nodes 41.25% → 56.08%（+14.83pp，400 subtest 净 pass，0 回归）**；Element-classlist.html 单用例 80.3% |
| 2026-08-13 | R3 | createElement 非法标签名抛 InvalidCharacterError（双路径 + spec Name production 校验 helper `is_valid_qualified_name` native / `_zwIsValidQualifiedName` polyfill）+ A/B 门 createElement 异常路径扩展；v8 2068 / wpt-runner 167 全绿，双矩阵 clippy 干净 | dom/nodes 56.08% → **56.45%（+0.37pp，createElement HTML 上下文 invalid 全转 pass，0 回归）** |
| 2026-08-13 | R4 | native node mutation（append/insert/remove/replace）DomError→DOMException 映射（dom crate 已有校验，dom_bindings 此前吞错）+ `dom_error_exception` helper + 3 单测；v8 2072 全绿，双矩阵 clippy 干净 | native 路径规范合规（default-on 生产路径）；**基线不提升**（testharness-dom 走 polyfill，polyfill appendChild 闭环架构限制待 L2）；net≥0 |
| 2026-08-14 | R5 | `testharness-dom-native` 入口（ZW_NATIVE_DOM=1）+ native DOMException constructor 修复（throw_dom_exception 取全局构造器 new + 构造器 invoke 用 This；prototype.constructor 属性 V8 Fatal 回退留下轮）；dom_bindings 197 全绿，双矩阵 clippy 干净 | **建立 polyfill 56.45% vs native 41.25% 双基线**（DC-3 native 对照达成）；定位 native 落后 15.2pp 根因（DOMException prototype.constructor 缺失 → assert_throws_dom "wrong global" ~414） |
| 2026-08-14 | R6 | DOMException identity 三重根因修复（prototype.constructor 补齐 + build_and_register 幂等 + shim part03 check 用 globalThis.DOMException）+ webview overlap 诊断测试；zero-engine v8 2073 / webview_coverage 17 全绿，双矩阵 clippy 干净 | **native dom/nodes 41.25% → 56.08%（+14.83pp，追平 polyfill 56.45%，差仅 0.37pp）**；classList 单用例 native 80.3%（=polyfill） |
| 2026-08-14 | R7 | fetch-dom-subset 补齐 .js 依赖（.html+.js+dom 根共享.js+testharnessreport.js）+ native createProcessingInstruction API（factories invoke + document.rs 注册 + node.rs PI target/data getter + node_name PI→target + spec 校验）+ native PI 单测；dom_bindings 199 全绿，双矩阵 clippy 干净 | **基线真实化**（用例 .js 缺失致跳过→真跑暴露 gap）：polyfill 56.45%→51.12%、native 56.08%→50.79%（双路径对等差 0.33pp）。native PI API 净正（单测证明），PI 用例待 ProcessingInstruction 构造器 |
| 2026-08-14 | R8 | testharness 运行时本地 .js 内联（`inline_local_scripts` + `extract_script_src` + `normalize_relative`）+ `wpt_root`/`case_path` 贯穿 `prepare_harness_html`/`run_testharness_html`/`run_canvas_testharness_html`/`run_testharness_html_inner`；与并行 canvas 流（G5 image fetcher）合并（统一 `&Path`）；wpt-runner v8 168 / quickjs 103 单测全绿，clippy 干净 | **基线进一步真实化**（用例 .js 运行时内联→用例真正跑起来）：subtest ~2696→4490（+1794），polyfill 51.12%→37.82%、native 50.79%→37.59%（双路径对等差 0.23pp，**非回归**——暴露真实 gap：createElementNS 596/createEvent/createProcessingInstruction instanceof 89 等）。完整 JSON 快照入 evidence |
| 2026-08-14 | R9 | polyfill document.createProcessingInstruction 桥接（DomMutation::CreateProcessingInstruction + __zw_create_processing_instruction callback + shim part06 方法/part01 _piHandles/part04 PI 节点包装/part03 构造器占位）+ DOMException identity 对等（createElement/PI 用 globalThis.DOMException，顺带修 R3 既存对等 bug）+ PI 单测；engine v8 2076 / quickjs 1407 / wpt-runner v8 168 / quickjs 103 全绿，双矩阵 clippy 干净 | **基线提升 + PI 双路径对等**：polyfill 37.82%→38.03%、native 37.59%→37.81%（双路径对等差 0.22pp）；PI 用例双路径 1P/11F→6P/6F。关键发现：用例侧 document 始终是 polyfill document（即使 native_dom=1）。完整 JSON 快照入 evidence |
| 2026-08-14 | R10 | polyfill Proxy getPrototypeOf trap（part05 _makeProxy handler：element→HTMLElement.prototype 链、PI→ProcessingInstruction 等）+ DOM 原型方法不可枚举（part03，修 for-in 副作用）+ 顺带修并行 canvas 流回归（part06 CSS.escape/supports 合并 + testharness fetch_handler 编译）+ instanceof 单测；engine v8 2071 / quickjs 1407 全绿，双矩阵 clippy 干净 | **基线提升**：polyfill 38.03%→39.23%、native 37.81%→38.96%（双路径对等差 0.27pp）；cloneNode 用例 0P→51P（+51，instanceof 直接解锁）。归因：6 fetch 既存失败（clean R9 同样）非本切片引入。完整 JSON 快照入 evidence |
| 2026-08-14 | R11 | element.localName getter（part04：HTML 小写/去 prefix/非 Element→null）+ HTML 元素子类 instanceof（part03 注册 ~64 HTML*Element 构造器 + __zwHtmlTagIface tag→接口映射；part05 getPrototypeOf 按 tag 返子类 prototype）+ localName/子类 instanceof 单测；engine v8 2082 / quickjs 1408 全绿，双矩阵 clippy 干净 | **基线提升**：polyfill 39.23%→40.89%、native 38.96%→40.63%（双路径对等差 0.26pp）；cloneNode 用例 51P→121P（+70）。完整 JSON 快照入 evidence |
| 2026-08-14 | R12 | element.prefix getter（part04：限定名冒号前/无冒号→null）+ getElementsByTagNameNS（part04 元素级 + part06 document 级，忽略 ns 按 localName 查）+ 单测；engine v8 2083 / quickjs 1408 全绿，双矩阵 clippy 干净。iframe.contentDocument 评估为深结构跨面改（html-compat 域），记待评估 | **基线提升**：polyfill 40.89%→41.71%、native 40.63%→41.45%（双路径对等差 0.26pp）；polyfill 净 +37 pass。完整 JSON 快照入 evidence |
| 2026-08-14 | R13 | classList token 有序去重（part03 cur() seen 表，`"a a a"`→["a"]）+ contains 空串/含 ASCII 空白→false 不抛（区别于 add/remove/toggle/replace 抛）+ 单测；engine v8 2084 / quickjs 1408 全绿，双矩阵 clippy 干净 | **基线大幅提升**：polyfill 41.71%→46.60%、native 41.45%→46.33%（双路径对等差 0.27pp）；classlist 用例 1140P/280F→1360P/60F（+220，迄今最大单切片提升）。完整 JSON 快照入 evidence |
| 2026-08-14 | R14 | createEvent alias 全覆盖（注册 5 缺失 event 子类 BeforeUnloadEvent/DeviceMotionEvent/DeviceOrientationEvent/TextEvent/TouchEvent + map 扩全集含复数别名）+ 未知 type 抛 NotSupportedError（spec 合规，原 lenient 改抛）+ 更新 event_subclasses2 测试 + 新增 createEvent 单测；engine v8 2085 / quickjs 1408 全绿，双矩阵 clippy 干净 | **基线提升 + 突破 50%**：polyfill 46.60%→50.33%、native 46.33%→50.07%（双路径对等差 0.26pp）；createEvent 用例 96P/183F→264P/15F（+168）。完整 JSON 快照入 evidence |
| 2026-08-14 | R15 | implementation.createDocumentType 返 DocumentType（part06 主 + part03 detached doc，spec 不校验）+ detached doc 加 implementation + 顺带修并行 canvas 流 2 个 clippy 红灯（main 既有，机械修正）+ 单测；engine v8 2086 / quickjs 1408 全绿，双矩阵 clippy 干净 | **基线提升**：polyfill 50.33%→52.11%、native 50.07%→51.84%（双路径对等差 0.27pp）；createDocumentType 用例 1P/81F→80P/2F（+79）。完整 JSON 快照入 evidence |
| 2026-08-14 | R16 | classList toggle force no-op（无变化不 write 保持原样）+ write runUpdate 比较（新集合序列化 vs 原 attribute，相同不 setAttribute）+ replace 顺序/同名 runUpdate（有序去重保位置）；engine v8 2086 / quickjs 1408 全绿，双矩阵 clippy 干净 | **基线提升**：polyfill 52.11%→53.00%、native 51.84%→52.73%（双路径对等差 0.27pp）；classlist 用例 1360P/60F→1400P/20F（+40）。完整 JSON 快照入 evidence |
| 2026-08-14 | R17 | createEvent 移除 9 个 non-createable modern interface（WPT someNonCreateableEvents，spec createEvent 仅 legacy）→ 抛 NotSupportedError + 更新 2 单测（ProgressEvent/ErrorEvent）+ 核实 event target null gap 不存在；engine v8 2086 / quickjs 1408 全绿，双矩阵 clippy 干净 | **基线提升**：polyfill 53.00%→53.20%、native 52.73%→52.93%（双路径对等差 0.27pp）；createEvent 用例 264P/15F→273P/6F（+9）。完整 JSON 快照入 evidence |
| 2026-08-14 | R18 | createElementNS 改经 `__zw_create_element_ns` → host `create_element_ns`（保留大小写+prefix+namespace）+ shim `_nsHandles` + part03 helper + part04 getter（tagName/nodeName/localName/prefix/namespaceURI 大小写敏感）+ 更新 R12 断言 + 新增 R18 单测（三态 prefix + namespaceURI）；engine v8 2087 / quickjs 1408 全绿，双矩阵 clippy 干净 | **基线提升**：polyfill 53.20%→55.11%（2481P/2021F）、native 52.93%→54.46%（2452P/2050F，双路径对等差 0.65pp）；case.html createElementNS abc/Abc/ABC 三态全 Pass（R12 仅 ABC）。`querySelector-mixed-case` 既存失败非回归（R14 同失败，selector 域）。完整 JSON 快照入 evidence |
| 2026-08-14 | R19 | classList replace 四 bug 修复（校验顺序两空串先于两空白 + 去重算法重写 splice+seen + write(force) 强制 mutation + classList set trap readonly + `_clsProxyCache` identity 缓存）+ R19 单测（校验顺序+去重+mutation+assignment no-op+identity）；engine v8 2088 / quickjs 1408 全绿，双矩阵 clippy 干净 | **Element-classlist.html 全量 100%**（1400P/20F→1420P/0F）。dom/nodes：polyfill 55.11%→55.55%（2501P）、native 54.46%→54.91%（2472P，双路径对等差 0.64pp，各 +20 pass）。完整 JSON 快照入 evidence |
| 2026-08-14 | R20 | testharness `map_harness_results` status 精确化（`HarnessStatus` 新增 NotRun/PreconditionFailed 中性变体，3→NotRun/4→PreconditionFailed，原 `_ => Fail` 误计修正）+ 通过率口径改 WPT 标准 pass/(pass+fail) + R20 单测；wpt-runner v8/quickjs 全绿，双矩阵 clippy 干净 | createEvent 剩 6F（TouchEvent `assert_implements_optional` 失败 = optional legacy touch API 不支持）从 fail→中性。dom/nodes WPT 标准口径：polyfill 55.55%→55.63%（2501P/1995F+6中性）、native 54.91%→54.98%（2472P/2024F+6中性，双路径对等差 0.65pp，各 6 fail→中性）。完整 JSON 快照入 evidence |
| 2026-08-14 | R21 | 扩展 DOM_TEST_SUBDIRS + fetch-dom-subset.sh 加 dom/events（81 .html + 16 .js，jsdelivr CDN 0 失败，零生产逻辑）+ polyfill dom/events 基线 31.61%（98P/212F/9timeout）+ 失败聚类 + dom/nodes 双路径零回归核验；wpt-runner 双矩阵 clippy 干净 | **polyfill dom/events 基线 31.61%**（54 个 0-pass 事件分发缺口：Event 属性/三阶段/EventListener/returnValue）。**误报 native events dispatchEvent hang**（R22 推翻：单用例 timeStamp 死循环）。不阻 CI（make test 不含 testharness-dom-native）。完整 JSON 快照入 evidence |
| 2026-08-14 | R22 | 定位修复 native Event.timeStamp 死循环（更正 R21 误报：诊断插桩+直接 binary+逐用例 timing 三步定位到 `Event-timestamp-safe-resolution` 的 do-while(timeStamp差==0) 死循环，根因 native timeStamp 恒 0）+ 修复 native timeStamp 改 perf_now_ms()（OnceLock<Instant>，DOMHighResTimeStamp）+ R22 单测；dom_bindings v8 全绿，双矩阵 clippy 干净 | **native dom/events 基线建立 31.29%**（97P/213F/9timeout，96s，从 570s 超时→96s）。双路径对等差 0.32pp（polyfill 31.61%）。完整 JSON 快照入 evidence |
| 2026-08-14 | R23 | polyfill Event eventPhase 常量（part05 Event 构造器+prototype 挂 NONE/CAPTURING_PHASE/AT_TARGET/BUBBLING_PHASE，enumerable:false，实例经原型链继承）+ R23 单测（4 对象×4 常量 + 不可枚举）；engine v8 全绿，双矩阵 clippy 干净 | **Event-constants.html 双路径 100%**（0P→4P/4）。dom/events：polyfill 31.61%→32.90%（102P/208F）、native 31.29%→32.58%（101P/209F，双路径各 +4 pass，对等差 0.32pp 不变）。完整 JSON 快照入 evidence |
| 2026-08-14 | R24 | polyfill 事件子类 init 属性父链继承（_defineEventSubclass 沿父链收集 props，_eventSubclassProps 注册表）+ KeyboardEvent 改用工厂（extends UIEvent，补 EventModifierInit+key/code/location/repeat/isComposing/charCode/keyCode/which）+ R24 单测（MouseEvent 默认/设定 + KeyboardEvent 父链 + WheelEvent 三层）；engine v8 全绿，双矩阵 clippy 干净 | **Event-subclasses-constructors polyfill 0P→42P/49**（native 24P/49）。dom/events：polyfill 32.90%→42.58%（132P，+30 pass）/ native 32.58%→36.13%（112P，+11 pass）。**双路径差扩至 6.45pp**（native dom_bindings event.rs 待 R25 对齐）。完整 JSON 快照入 evidence |
| 2026-08-14 | R25 | native MouseEvent/KeyboardEvent view（set_ui_view helper，缺省 null/init dict 对象）+ KeyboardEvent which（回退 keyCode）+ R25 单测；经 runner 实测诊断确认 native_dom=1 下 MouseEvent 走 native 覆盖 polyfill；dom_bindings v8 全绿，双矩阵 clippy 干净 | native 正确性净正（view/which，default-on 后合规）。**双路径差未缩**（6.45pp 保持——Event-subclasses native 仍 24P/49，剩余 WheelEvent 子类链/SubclassedEvent class 语义/MouseEvent 属性细节/UIEvent view 校验多点分散缺口，转高 ROI 切片）。dom/events 基线不变（polyfill 42.58% / native 36.13%） |
| 2026-08-14 | R26 | polyfill Event.cancelBubble（_makeEvent 加 cancelBubble:false + stopPropagation/stopImmediatePropagation 设 true；initEvent 重置 cancelBubble+stop flags）+ R26 单测；Event-dispatch 系列评估为深结构（document/window listener 独立 + cloneNode/Document/Text 基础设施）转 cancelBubble 轻量；engine v8 全绿，双矩阵 clippy 干净 | **Event-cancelBubble 双路径 0P→4P/8**。dom/events：polyfill 42.58%→45.48%（141P，+9 pass）/ native 36.13%→39.03%（121P，+9 pass，双路径同步提升，对等差 6.45pp 不变）。完整 JSON 快照入 evidence |
| 2026-08-14 | R27 | polyfill EventListener handleEvent（_dispatchToListeners fire 按类型分支：函数 call / 对象 Get handleEvent.call(fn)，每次派发都 Get 支持 getter）+ R27 单测（对象 listener+this=对象+evt / getter 每次 Get / 函数不回归）；engine v8 全绿，双矩阵 clippy 干净 | **EventListener-handleEvent 双路径 0P→3P/5**。dom/events：polyfill 45.48%→46.75%（144P，+3 pass）/ native 39.03%→40.26%（124P，+3 pass，双路径同步提升，对等差 6.49pp 基本不变）。完整 JSON 快照入 evidence |
| 2026-08-14 | R28 | polyfill Event.returnValue（_makeEvent Object.defineProperty：getter 返 !_defaultPrevented，setter cancelable+false 触发 prevent，true/非cancelable no-op）+ R28 单测（7 spec 场景）；engine v8 全绿，双矩阵 clippy 干净 | **Event-returnValue 双路径 0P→7P/7（100%）**。dom/events：polyfill 46.75%→49.68%（153P，+9 pass，突破 49.68% 接近 50%）/ native 40.26%→43.18%（133P，+9 pass，双路径同步提升，对等差 6.49pp 不变）。完整 JSON 快照入 evidence |
| 2026-08-14 | R29 | polyfill Event.cancelBubble setter dispatch 副作用（_makeEvent 普通 data 字段→defineProperty getter/setter，后端复用 _propagationStopped；setter true→置 flag 等同 stopPropagation，false→no-op；stopPropagation/stopImmediatePropagation 移除冗余赋值；_dispatchWithBubble finally 重置 flag=spec dispatch 步骤14；initEvent 移除无效 setter 调）+ R29 单测 4 场景（setter no-op/置 flag/dispatch 后 flag 清 + 保留 R26 4）；engine v8 2097 / quickjs 1410 全绿，双矩阵 clippy 干净 | **Event-cancelBubble 双路径 4P→8P/8（100%）**。dom/events：polyfill 49.68%→52.13%（159P，+6 pass，突破 52%）/ native 43.18%→45.57%（139P，+6 pass，双路径同步提升，对等差 6.49→6.56pp 基本不变）。完整 JSON 快照入 evidence |
| 2026-08-14 | R30 | dom_bindings 独立 coverage 口径（M0 项 4 收口）：核实 cargo-llvm-cov 0.8.7 + llvm-tools 已装（推翻 R0「未装」）+ 新建 `scripts/check-dom-bindings-coverage.sh`（lcov 解析 + 源码/测试分离 + 逐文件明细 + JSON 模式 + 双 feature 参数化）+ `check-coverage.sh --dom-bindings` 集成；纯脚本+度量零生产代码改动 | **dom_bindings 基线：源码 93.14%（4561/4897，15 文件）/ 全部 95.15%（20 文件）**；最低 dom_exception 71.4% / css_style_declaration 86.7% / custom_elements 89.0%。基线 JSON 入 evidence。**M0 里程碑 6/6 项全完成收口** |
| 2026-08-14 | R31 | dom_bindings coverage 提升（DC-4）+ 真 bug 修复：`new DOMException()` 无 name 参返 "undefined"（`string_arg` 缺省 ToString）→ 改 `args.get(idx).is_undefined()` 判（spec name 缺省 "Error"/message 缺省 ""）+ 9 单测（DOMException 构造/code 表/toString/legacy 常量/constructor identity + CE connect/disconnect/attr-change 派发 + CSS getPropertyPriority/item 边界/named-deleter）；engine v8 2107 / quickjs 1410 全绿，双矩阵 clippy 干净 | **dom_bindings 源码 93.14%→94.27%（+1.13pp）/ 全部 95.15%→95.94%**；dom_exception 71.4%→94.4% / css_style_declaration 86.7%→92.7% / custom_elements 89.0%（剩 19 防御行）。DOMException 修复=WPT assert_throws_dom 正确性净正。基线 JSON 入 evidence |
| 2026-08-14 | R32 | Event.srcElement（target legacy IE 别名，spec dom-event-srcelement）双路径实现（polyfill part03 defineProperty getter 读 this.target / native event.rs init null + event_target.rs dispatch 同步设）+ native 单测 + fetch-dom-subset.sh 幂等快路径（已含 .html 跳过 GitHub API 列目录，避免 60/h 限流阻断）；engine v8 2109 / quickjs 1411 全绿，双矩阵 clippy 干净，fmt 无 diff | **event-src-element-nullable.html 双路径 0P→全 Pass**（DC-3 dom/events 各 +1 用例）。接手 429 打断切片，本轮核对+验证 land。完整 JSON 入 evidence |
| 2026-08-14 | R33 | Window.event（HTML current event legacy global，spec window-event）polyfill 双点实现（part01 init globalThis.event=undefined writable+enumerable / part03 _dispatchWithBubble try 前 set + finally restore 嵌套 dispatch 正确）+ polyfill 单测（5 场景含嵌套 restore）；native 不改（用例侧 dispatch 走 polyfill 未解问题 #9）；engine v8 2110 / quickjs 1411 全绿，双矩阵 clippy 干净，fmt 无 diff | **event-global.html 双路径 0P→5P/7F**（剩 7F=shadow tree 内派发+window.onerror restore 深结构）；dom/events polyfill 174P/153F（+15 pass）/ native 154P/174F（+8 pass）。Event-stopPropagation-cancel-bubbling 根因另定位（AT_TARGET stopPropagation，下轮）。完整 JSON 入 evidence |
| 2026-08-14 | R34 | AT_TARGET stopPropagation 止同元素 listener + 双 stop-flag 兼容（根因两层：_dispatchToListeners 'all' non-capture 未检查 stop flag + polyfill Event `_propagationStopped` vs 叠加路径 native Event `__zw_stop` 双 flag 不互认）；part03 _dispatchToListeners/_dispatchWithBubble 加双 flag helper + AT_TARGET 早退 + finally 重置 native flag + polyfill 单测 4 场景；顺带修并行 canvas 流 c1e0bcc8 clippy unnecessary_cast 红灯（context_impl.rs:1285-1286 删 2 冗余 as i32）；engine v8 2111 / quickjs 1411 全绿，双矩阵 clippy 干净，fmt 无 diff | **Event-stopPropagation-cancel-bubbling.html 双路径 0P→全 Pass**；dom/events polyfill 175P/152F（53.52%）/ native 155P/173F（47.25%）。canvas clippy 修复恢复 main 全绿。完整 JSON 入 evidence |
| 2026-08-14 | R35 | eventPhase 反映 dispatch 阶段（Event-dispatch 系列最小子集；诊断两层缺口：① _dispatchToListeners 收 phase 未翻译 eventPhase→全程 0 ② document/window 不入 dispatch chain[深结构跳过]）；part03 _dispatchToListeners 按 phase 设 eventPhase（capture→1/AT_TARGET[all]→2/bubble→3，target 的 cap+non-cap 都 AT_TARGET）+ _dispatchWithBubble finally 复位 eventPhase=0+currentTarget=null；polyfill 单测（三阶段+复位+currentTarget null）；engine v8 2112 / quickjs 1411 全绿，双矩阵 clippy 干净，fmt 无 diff | **Event-dispatch-order-at-target.html 双路径 0P→全 Pass**；dom/events polyfill 177P/151F（54.13%）/ native 157P/171F（47.87%）。完整 JSON 入 evidence |
| 2026-08-14 | R36 | dom_bindings coverage 提升（Event-dispatch doc/window 入 chain 诊断深结构跳过 + namespaceURI 核实已完成勘误 → 转 DC-4 coverage）；补 2 单测（node mutation 错误路径 try/catch：cycle→HierarchyRequestError/非 child→NotFoundError + aria/role IDL 反射 idl_to_attr 全分支）；engine v8 2114 / quickjs 1411 全绿，双矩阵 clippy 干净，fmt 无 diff | **dom_bindings 源码 94.27%→94.35%（+0.08pp）/ 全部 95.95%→96.01%**；node.rs 91.6%→92.3%（+4 行）。完整 JSON 入 evidence |
| 2026-08-14 | R37 | 导入 dom/collections 子目录建立基线（DC-3 扩展，纯资产零源码逻辑改动；testharness.rs DOM_TEST_SUBDIRS + fetch-dom-subset.sh SUBDIRS 同步加 dom/collections + fetch 10 .html）；clippy 双矩阵干净，fmt 无 diff | **dom/collections polyfill/native 双路径 22.9%（11P/37F/1timeout，49 subtest，10 cases，双路径对等差 0pp）**；失败聚类：HTMLCollection own 属性枚举（~19 fail 主力，_makeProxy 缺 getOwnPropertyNames trap）。完整 JSON 入 evidence |
| 2026-08-14 | R38 | HTMLCollection namedItem 空串守卫 + Element.children 返 HTMLCollection（R37 collections 基线驱动；own 属性枚举诊断深结构转 empty-name 轻量）；part05 _zwMakeCollection namedItem 加 if(n==='')return null + part04 children 经 _zwMakeCollection(true)；polyfill 单测；engine v8 2115 / quickjs 1411 全绿，双矩阵 clippy 干净，fmt 无 diff | **HTMLCollection-empty-name 双路径 1P/6F→7P/0F（100%）**；dom/collections polyfill/native 双路径 22.9%→**35.4%**（+12.5pp）；dom/events 净 +1 零回归。完整 JSON 入 evidence |
| 2026-08-14 | R39 | pre-set stop propagation flag → dispatch 零触发（Event-dispatch 系列最小子集；完整 document/window 入 chain 诊断深结构：host parent_selector_for('html') 返空 + document/window/html 三合一 _elKey('html') key + 12 派发点）；part03 _dispatchWithBubble try 开头加 bubbleStopped() 早退（spec concept-event-dispatch 步骤 2）+ polyfill 单测 4 场景；engine v8 2120 / quickjs 1415 / wpt-runner 171 / webview 595 全绿，双矩阵 clippy 干净，fmt 无 diff | **Event-dispatch-propagation-stopped + Event-dispatch-bubble-canceled 双路径各 0P/1F→1P/0F（100%）**；dom/events polyfill 54.13%→**54.74%**（179P）/ native 47.87%→**48.62%**（159P，双路径各 +2 同步，对等差 6.12pp）。dom/nodes 2502P 零回归。完整 JSON 入 evidence |
| 2026-08-14 | R40 | document/window 入派发链（槽位标记 tgt=doc/win/undefined 不拆三合一 key + _dispatchWithBubble 虚站 capture win→doc→链 / bubble 链→doc→win + targetSlot=[doc,win]/[win] + detached 不经 + html target 站 null slot 防双 fire[renderer quickjs 回归同轮修] + once 移除原始数组过滤）+ 单测 5 场景；engine v8 2121 / quickjs 1415 / renderer quickjs 124 全绿，双矩阵 clippy 干净，fmt 无 diff | **multiple-stopPropagation + omitted-capture 双路径 0P/1F→1P/0F（100%）**；bubbles-true/false 主路径 Pass（剩各 3F 深结构）；dom/events polyfill 54.74%→**57.62%**（189P +10）/ native 48.62%→**51.52%**（169P +10，对等差 6.10pp）；dom/nodes 2502P / collections 17P 零回归。完整 JSON 入 evidence |
| 2026-08-14 | R41 | 导入 dom/traversal 17 用例建基线（DOM_TEST_SUBDIRS + fetch script + support/ 3 文件）+ extract_script_src 无引号属性值支持 + TreeWalker/NodeIterator API 面（NodeFilter 常量全表修正/readonly/root TypeError/null→0/toString/currentNode setter）+ 单测 10 断言组；engine v8 2122 / quickjs 1415 全绿，双矩阵 clippy 干净，fmt 无 diff | **dom/traversal 首个真实基线 polyfill 9P/46F=16.36% / native 8P/47F=14.55%**（NodeFilter-constants 双路径 100%、TreeWalker-basic 5P/1F）；dom/nodes +1（inline 修复）；dom/events 189P / collections 17P 零回归。失败主力 detached 树遍历深结构（M1 L2）。完整 JSON 入 evidence |
| 2026-08-14 | R42 | 导入 dom/ranges 44 用例建基线（逐文件重试拉齐 45/45）+ getAttributeNode 返真 Attr + new Range() 实例 + StaticRange 构造器 + Range setStart/setEnd spec 校验（Attr 允许/DocumentType 拒/offset 越界/无 parent）+ 单测 11 断言组；engine v8 2123 / quickjs 1415 全绿，双矩阵 clippy 干净，fmt 无 diff | **dom/ranges 双路径 9.18%→39.80%（+30 同步对等 0pp）**（Range-attribute-nodes 0→15P、StaticRange-constructor 0→14P）；events/nodes/collections/traversal 全零回归。剩余 ~59F = iframe 驱动 mega-case（html-compat 域）+ ShadowRoot + comparePoint 族（M1 L2）。完整 JSON 入 evidence |
| 2026-08-14 | R43 | Part1 L2 live 视图最小切片实现-实测-回退（nodes -10 跨文档 pending 并入主视图，诊断归档 M1 设计输入）；Part2 HTMLCollection indexed/named configurable:false accessor + 数字 id 跳过（delete 语义 + length 保护）+ 单测 7 断言组；engine v8 2124 / quickjs 1415 全绿，双矩阵 clippy 干净，fmt 无 diff | **HTMLCollection-delete 双路径 0→4P/0F（100%）**；dom/collections 35.4%→**43.75%**（21P 对等 0pp）；四子目录零回归；nodes 数字 id 回归（-1）clean/after diff 捕获同轮修。完整 JSON + 诊断 md 入 evidence |
| 2026-08-14 | R44 | NamedNodeMap own 枚举（ownKeys + getOwnPropertyDescriptor + named getter 三 trap）+ 单测 4 断言组；querySelector-mixed-case 根因诊断（detached 树非 selector）归档；engine v8 2125 / quickjs 1415 全绿，双矩阵 clippy 干净，fmt 无 diff | **namednodemap-supported-property-names 双路径 0→3P/0F（100%）**；dom/collections 43.75%→**50.00%**（24P 对等 0pp 过半）；dom/nodes 2507P（+4 named getter 解锁）；events/traversal/ranges 零回归。完整 JSON 入 evidence |
| 2026-08-14 | R45 | MutationObserver 批内 id 追链（rewrite_pending_id_selectors，for→while-let 重构）+ attributeOldValue 写前捕获（IDL 反射/classList）+ S6 依赖 L2 诊断 + 单测；engine v8 2126 / quickjs 1415 全绿，双矩阵 clippy 干净，fmt 无 diff | **MutationObserver-attributes 双路径 0P（整用例崩）→30P/8F**；dom/nodes **2507→2539P（+32）**；四子目录零回归。S6 正体延后（L2 前置）。完整 JSON 入 evidence |
| 2026-08-15 | R46 | MutationObserver NS record（localName+namespace，NS 族自带 notify 绕 delegate）+ removeAttribute 缺失不发 record + classList 值相同仍 record/空集缺失不写（classlist -10 回归同轮修）+ 单测；engine v8 2127 / quickjs 1415 全绿，双矩阵 clippy 干净，fmt 无 diff | **MutationObserver-attributes 双路径 30P/8F→38P/0F（100%）**；dom/nodes 2539→**2547P（+8）**/native 2517P；Element-classlist 1420P 维持；四子目录零回归。完整 JSON 入 evidence |
| 2026-08-15 | R47 | childList fragment 展开 record（addedNodes=子节点）+ previous/nextSibling 字段 + el.remove() record + surroundContents 文档序/逆序解耦（renderer R2930 两起回归同轮修）+ 单测；engine v8 2128 / quickjs 1415 全绿，clippy 干净，fmt 无 diff | **MutationObserver-childList 10P/4F→15P/1F（+5）**；dom/nodes 2547→**2552P**/native 2522P；五子目录零回归；剩 1F M1 L2 同根因。完整 JSON 入 evidence |
| 2026-08-15 | R48 | parsed CharacterData（DomMutation::SetChildText 全链 + _wrapNodeEntry 五方法 + observe 父回落 + notify 参数序 bug 同轮修）+ 单测 4 断言组；engine v8 2129 / quickjs 1415 全绿，双矩阵 clippy 干净，fmt 无 diff | **MutationObserver-characterData 双路径 4P/12F→18P/0F（100%）**；dom/nodes 2552→**2566P（+14）**/native 2536P；五子目录零回归；R45-R48 MO 族 attributes+characterData 双 100%、nodes +59。完整 JSON 入 evidence |
| 2026-08-15 | R49 | observe options 校验/隐含启用 + filter 不供 oldValue + textContent= childList 语义（同值 no-op 两轮回归修正）+ firstChild 本地视图 + 注册文本可编辑（__zw_mo_notify_text hook）+ 遗留 3 测试 spec 更新 + 单测；engine v8 2131 / quickjs 1415 全绿，双矩阵 clippy 干净，fmt 无 diff | **sanity 16P/0F + takeRecords 3P/0F + attributes 40P/0F + characterData 19P/0F（四 100%）**、childList 18P/1F；dom/nodes **2566→2579P（+13）**/native 2549P；五子目录零回归；R45-R49 累计 nodes +72。完整 JSON 入 evidence |
| 2026-08-15 | R50 | HTMLCollection Proxy 承载（legacy platform object + live overlay）：Proxy 全 trap + prototype（illegal invocation/无 values-entries-forEach）+ canonical 索引 + named 仅 HTML ns + live overlay（_mo_notify 汇流点 + pending 子树展开）+ getElementsByTagName 匹配模型 + handle childNodes registry 回落 + R50 单测 8 断言组 + R3033 更新；engine v8 2132 / quickjs 1415 全绿，双矩阵 clippy 干净，fmt 无 diff | **dom/collections 双路径 24P/24F→48P/0F（100%）**、dom/nodes 2479→2508P（+29）、**dom/traversal 9→27P（+18）**、events 189P/ranges 39P 零回归；六轮过程回归同轮修。完整 JSON + md 入 evidence |
| 2026-08-15 | R51 | TreeWalker NodeFilter callback 语义（Get-per-traverse/抛错传播/this/缺失 TypeError）+ nextNode lazy 步进 + NodeIterator referenceNode + new Document() 构造器族解锁 dom/common.js（testNodes=38）+ runner meta-long 60s + TIME_LIMIT 透传；engine v8 2132 / quickjs 1415 全绿，clippy 干净，fmt 无 diff | **traversal 36→925P（+889）/native 893P、nodes 2508→2957P（+449）、ranges 非 mutations 39→1847P（+1808）**、acceptNode-filter 12/12 100%、collections/events 零回归；CDATA 融合深结构记 M1 L2 邻域。完整 JSON + md 入 evidence |
| 2026-08-15 | R54 | live collection els 泄漏「并入点过滤」（R53 收口）：`_zwMutationInDoc` 挂载点判定（sel→`__zw_contains`；handle→`_zwNodeParent` 逐跳上行），应用于失效循环 add 分支 + 构建期 pending 并入；els 快照基线与 removed 剔除不动；单测 +3（part18）；engine v8 2143 / quickjs 1415 全绿，双矩阵 clippy 干净，fmt 无 diff（commit `ba775545`，rebase 过 canvas 流 `9cb32ab9` 零冲突） | **deleteData 71s→35s（2.0x）、insertData 31s→23s**、其余 mutations 持平；零回归 nodes 3006P / collections 48P/0F / traversal 953P / events 189P（name-validation diff 为计时抖动，隔离复跑两树一致）；insertBefore/dataChange >420s 维持 R51c/R52 归因（M1 L2）。完整 JSON + md 入 evidence |
| 2026-08-15 | R55 | childNodes 基底缓存 + 兄弟对缓存（R52 per-subtest testFn 成本第一刀）：`_zwChildBaseCache`（part05 按 sel 键，512 软上限；pending overlay 每读现算语义不变；no-pending 快路径返 slice 防污染）+ `_zwSiblingBaseCache`（part05 声明 + part04 previous/nextSibling 消费）+ `register_dom_callbacks` 开头失效脚本（注册即 dom_html Arc 换代全量清，幂等）——缓存安全性来自 Arc 回合内不可变；附带 identity 收益 `p.childNodes[0]===p.childNodes[0]`；单测 +2（part18）；engine v8 2145 / quickjs 1415 全绿，双矩阵 clippy 干净，fmt 无 diff（commit `7936da79`） | 同树 stash A/B（release runner）：appendChild 1.37→0.50s、splitText 0.70→0.40s、appendData 3.40→2.05s、deleteData 38.6→32.6s，7 文件 pass/fail 逐项一致；四子目录零回归（nodes 3096P per-case diff 逐字节一致 / collections 48P/0F / traversal 953P / events 189P）；遗留 insertBefore/dataChange/replaceData >90s（M1 L2 正题）。完整 JSON + md 入 evidence |
| 2026-08-15 | R56 | M8 path-objects 接手首切片：用例导入 205（fetch 脚本超时改逐文件 raw 205/205）+ roundRect panic 修复（4 处 partial_cmp+Equal→total_cmp，NaN 全序；backtrace 实证）+ zwNormRadius 语义（空/>4/负→RangeError、BigInt→TypeError、NaN→0、DOMPointInit p<x>,<y>）+ host p-对解析（split 拆两项配对）+ 负 w/h 归一化+角序镜像（展开后 swap）+ 扫描线段式迭代（三 fill 消费端 chunks_exact(4)，多子路径虚假边）+ closepath-on-fill（flatten_path_opts 拆两变体，&&→|| 笔误 probe 定位）+ rect() 补 closePath + join 闭合段扇；zero-canvas 777→783 全绿（+3 单测 +11 计数语义更新）；fmt/clippy 双矩阵干净（commit `ac7240f5`，rebase 过并行流 `afd8ec08` 同款 total_cmp 仅注释冲突） | **path-objects 139→156 pass / 64→47 fail，panic 消除全程跑完**；roundrect 语义 6 + 几何 2（negative/winding）+ rect 族 5 + isPointInPath.subpath/stroke.prune.rect/scale×2/transformation.basic/arc.shape.2 修复；5 新 fail 为边缘精度（arc.shape.5/curve2、fill.overlap/winding.add 需 nonzero fill rule、rect.end.2）；全 canvas 449P/53F（shadows 6 个 HEAD 既存）；make test 唯一失败为并行流既存 default_actions（clean HEAD 同败）。完整 JSON + md 入 evidence |
| 2026-08-15 | R56b | M8 path-objects 回归收口：arc span 方向感知归一化（同向 mod + 去 dir 双重取反）+ flatten Arc 补 spec「current→弧首」连线段（has_any_subpath）+ roundRect 非有限半径静默忽略 + DOMPoint NaN/Inf 保留（+x||0 吞 NaN）+ stroke closed 末命令判定（rect.end.2 round cap）；canvas 单测 +5 + 4 旧计数更新、engine shim 单测 +1；engine v8 2149 / quickjs 1416 全绿、clippy 双矩阵干净（commit `b1446e9b`） | **真基线（pre-R56 `a749de94` 干净构建）62F → 37F/166P，净 +25 修复零回归**；arc.angle.2-5/shape.2/twopie.5/default + rect 族 + roundrect 语义 8 + stroke.prune.rect/scale1/scale2 + isPointInPath.subpath + moveTo.basic；跨目录 line-styles 1 预存（join.round）+ 三目录零 Fail。完整 JSON 入 evidence |
| 2026-08-16 | R56c | M8 nonzero fill rule：raster `fill_rule_spans` 方向感知扫描线（三 fill 消费端统一）+ `fill_with_rule` API 族 + roundRect 单轴镜像段序反转 + host op fill/fillPath fillRule 解析（args[0]/args[1] 各按 wire 形式）+ shim WebIDL 可选前置参省略嗅探（`ctx.fill("evenodd")` rule 落第一参位）；canvas 单测 +6（同区域反向对两规则都不填的分析修正）+ engine e2e +1 四场景；canvas 788 / engine v8 2152 / quickjs 1416 全绿、clippy 双矩阵干净、fmt 无 diff（commit `bd027c98`，rebase 合并并行流 colorMatrix filter） | **path-objects 166P/37F → 168P/35F（fill.overlap/winding.add/evenodd.1 修复，零回归）**；跨目录 line-styles/drawing-rectangles/transformations/reset 0F、shadow 6F 基线既存（stash 验证）。完整 JSON 入 evidence |
| 2026-08-16 | R56d | M8 isPointInPath 族 nonzero：is_point_in_path_rule/_for_rule 复用 fill_rule_spans（路径上点 inside + 零长退化段排除）+ 巨 scale Inf 收缩/钳制 + 无 MoveTo 整圆 arc 自包含子路径 + host isPointInPathPath op + shim WebIDL union/枚举校验；canvas +1 单测；canvas 789 / engine v8 2152 / quickjs 1416 全绿、clippy 双矩阵干净（commit `16f0d885`） | **path-objects 168P/35F → 172P/31F（isPointInPath.basic/edge/invalid/multi.path，bigarc 回归同轮修，零回归）**；跨目录六目录 0F。完整 JSON 入 evidence |
| 2026-08-16 | R56e | M8 ensuresubpath 族 + clip 相交/空 + ellipse 负半径：lineTo 无子路径=moveTo / 曲线第一控制点成起点照画 / arcTo 跳前导线（flatten_arc_to has_subpath 参）/ clip_paths Vec 相交语义（全 AND + 空 clip 全裁）/ shim ellipse IndexSizeError；canvas +4 单测 + engine +1 e2e + 1 旧反 spec 断言修正；canvas 793 / engine v8 2153 / quickjs 1416 全绿、clippy 双矩阵干净（commit `eab37ea1`） | **path-objects 172P/31F → 181P/22F（+9 修复零回归）**；跨目录 10 子目录 0F。完整 JSON 入 evidence |
| 2026-08-16 | R56f | M8 arcTo 切线弧：共享 arc_to_tangent_segments（flatten 双实现统一）+ 圆心候选恒等修正 + 各向异性 CTM 用户空间构造 + shim 负半径守卫补齐（76655cc4 丢 hunk 归因）+ 死代码清理；canvas +1 单测 +1 断言修正 -10 死代码测试；canvas 786 / engine v8 2153 / quickjs 1416 全绿、clippy 双矩阵干净（commit `9ef44a8d`） | **path-objects 181P/22F → 186P/17F（arcTo 五用例零回归）**；六跨目录 0F。完整 JSON 入 evidence |
| 2026-08-16 | R56g | M8 弧自适应段数 + 真圆环带 + 用户空间 CTM 弧：N=min(512,64·lw/r) / blit_arc_annulus（θ 窗 + dist 判定 + 逆变换用户空间）/ 非恒等 CTM 用户空间圆弧；interior-clamp 方案被 selfintersect.1 A/B 否决、identity t.d 笔误 debug trace 定位；canvas +2 单测 + 5 旧计数更新；canvas 788 / engine v8 2153 / quickjs 1416 全绿、clippy 双矩阵干净（commit `d0e93adc`） | **path-objects 186P/17F → 192P/11F（arc.shape×4 + arc.scale×2，shape.2 过程回归同轮修，零回归）**；六跨目录 0F。完整 JSON 入 evidence |
| 2026-08-16 | R56h | M8 曲线段数自适应 + 像素方形覆盖：N=clamp(折线长/8,8,512) 双分支 + stroke 判定半径 half+0.5（面积覆盖近似，solid+gradient 双光栅化器）；canvas +1 单测 + 4 旧计数更新；canvas 789 / engine v8 2153 / quickjs 1416 全绿、clippy 双矩阵干净（commit `e332878f`） | **path-objects 192P/11F → 196P/7F（bezier/quadratic shape/scaled 四用例零回归）**；六跨目录 0F。完整 JSON 入 evidence |
| 2026-08-16 | R56i | M8 stroke 零长段剪除 + roundRect ClosePath + 环绕 join：spec prune 语义（段列表构造处剪除）+ 命令层闭合标记 + join 环绕（双光栅化器）；几何闭合探测方案被 cap.open 跨目录 A/B 否决（命令层才是判据）；canvas +1 单测 + 4 旧计数更新；canvas 790 / engine v8 2153 / quickjs 1416 全绿、clippy 双矩阵干净（commit `c218f196`） | **path-objects 196P/7F → 200P/3F（prune×3 + roundrect.closed；line-styles 2F 过程回归同轮修，零回归）**；六跨目录 0F；剩 3F 记深项清单。完整 JSON 入 evidence |
| 2026-08-16 | R57 | M6 S0q QuickJS 原生绑定骨架 PoC：quickjs_dom_bindings 新模块（feature-gated quickjs + optional rquickjs）+ Sandbox::install_native_bindings_quickjs escape-hatch + webview QuickJS install_native_dom_bindings 接线（native_dom 仍默认关）；具名 fn 统一 'js 绕 rquickjs 闭包 HRP + This<Object> 接 this；engine +2 PoC / webview +2 wiring 测试；engine quickjs 1418 / webview quickjs 552 / engine v8 2153 / webview v8 599 / script-sandbox 76 全绿、clippy 双矩阵干净、fmt 无 diff（commit `b781252f`） | **DC-7 QuickJS native 真空打破**：__zw_native_element_for_id 与 V8 同名同 wire（双引擎复用同一 A/B 脚本）+ nodeType/tagName/nodeName/id 原生 getter 直读 live Rust DOM + 身份缓存 + Drop reset（镜像 R3334）；R43 根因描述勘误（case.js 主文档非 iframe doc）。完整 JSON + md 入 evidence/archive |
| 2026-08-16 | R58 | M6 S1q 只读属性族：className(+setter)/namespaceURI(null 语义)/localName/textContent 4 getter（具名 fn + This<Object> 零摩擦复用）+ reflected_attr_string_of/element_ns_of helper 沉淀 + PoC/wiring 测试扩展；engine quickjs 1419 / webview quickjs 552 / v8 双绿零回归、clippy 四矩阵零警告、fmt 无 diff（commit `84c76b25`） | **S1q 高频属性首批落**：与 V8 dom_bindings 语义对齐（反射缺省/ null-not-undefined / ns 感知 localName / 子树文本拼接）；S1q 剩余 = 反射族其余项 + 复合对象（attributes/classList 需二级身份缓存）。完整 JSON + md 入 evidence/archive |
| 2026-08-16 | R58b | M6 S1q 续字符串反射族：title/lang/accessKey（共享 helper + 静态 IDL→content 映射，QuickJS Accessor 无 name-dispatch 的等价实现）+ PoC 读写闭环断言 + Object.keys 面更新；engine quickjs 1419 全绿、clippy/fmt 干净（commit `b9136b38`） | **S1q 字符串反射族收口**；剩余复合对象（二级身份缓存）+ 枚举·boolean 反射 + S2q 写入族 |
| 2026-08-16 | R59 | M6 S2q 属性方法族：get/set/remove/hasAttribute（missing→null / 真移除 / 非 enumerable Function prop）+ PoC 全闭环断言；engine quickjs 1419 全绿、clippy/fmt 干净（commit `fe0a23d5`） | **QuickJS native 方法族开刀**（此前仅 Accessor 属性）；框架高频 getAttribute/setAttribute 原生直达 |
| 2026-08-16 | R60/R60b | M6 S2q 写入族：textContent setter（LegacyNull 语义——Coerced ToString 吞 null 须 raw Value 判）+ __zw_native_create_element 工厂（get_or_build_node_value 统一身份入口）+ appendChild/removeChild（spec 移动语义 + 返 child；DomError→null 基建注记）；PoC 全链路 create→append→remove；engine quickjs 1419 / v8 2153 零回归、clippy/fmt 干净（commit `be577323` / `b807e5e9`） | **QuickJS native 获得树构建能力**（此前只读+属性写）；M6 元素面 = 2 工厂 + 9 属性 + 6 方法。完整 JSON + md 入 evidence/archive |
| 2026-08-16 | R61 | M6 S2q 续树读回：childNodes（Array 形态 + 身份缓存包装）/parentNode/firstChild/lastChild + PoC 双向闭环（childNodes[0]===child / parentNode 指回 / remove 后 null 语义）；engine quickjs 1419 / v8 2153 零回归、clippy/fmt 干净（commit `784aab22`） | **树构建+读回闭环完成**——M6 元素面 2 工厂 + 13 属性 + 6 方法。完整 JSON + md 入 evidence/archive |
| 2026-08-16 | R62 | M6 S3q 查询族：__zw_native_query_selector/_all 全局工厂 + 元素级方法（zero_dom 全量选择器引擎）+ 跨工厂 identity + PoC 多选择器/miss/live 跟随断言；engine quickjs 1419 / v8 2153 零回归、clippy/fmt 干净（commit `e438af32`） | **QuickJS native 元素可达性大增**（查询族开道）；session R58→R62 五轮八切片把 M6 从骨架推到可用元素面。完整 JSON + md 入 evidence/archive |
| 2026-08-16 | R63 | M6 S4q EventTarget：add/removeEventListener + dispatchEvent（LISTENERS 注册序存储 + target 站派发 + this=target 经 Args::this 低层 apply）+ PoC 序/type/this/移除四合一断言；engine quickjs 1419 / v8 2153 零回归、clippy/fmt 干净（commit `21f6c4b2`） | **QuickJS native 事件交互能力落地**；M6 累计 4 工厂+13 属性+11 方法（元素可达+可变+可交互）。完整 JSON + md 入 evidence/archive |
| 2026-08-16 | R64 | M6 S5q customElements：五件套（Rust CE_REGISTRY 权威 + whenDefined PoC resolve）+ create 命中 set_prototype upgrade + lifecycle conn/disc 派发（DFS fast-path + 先标记后派发）；PoC define/get/getName/create-greet/conn-disc 全链路；engine quickjs 1419 / v8 2153 零回归、clippy/fmt 干净（commit `243811c6`） | **M6 里程碑：S0q–S5q 全部有 land 实现**（S5q PoC 深度）——QuickJS native 具备 WC 基础；剩余为深度补齐清单。完整 JSON + md 入 evidence/archive |
| 2026-08-16 | R65 | M6 S5q 深化：完整 ctor 执行（construct 语义新建 this 的发现 → this 绑定普通调用；字段初始化+状态保持断言）+ attributeChangedCallback（setAttribute 派发）；engine quickjs 1419 / v8 2153 零回归、clippy/fmt 干净（commit `7a486499`） | **custom class 字段初始化真正生效**（R64 纯原型挂载 → ctor body 执行）；construct-vs-this 语义发现是 upgrade 场景普适结论。完整 JSON + md 入 evidence/archive |
| 2026-08-16 | R66 | M6 S4q 完整化：DOMException 错误路径——throw_dom_exception（Error 对象 name/message/stack 经 Ctx::throw）+ dom_error_name 映射 + appendChild/insertBefore→HierarchyRequestError / removeChild→NotFoundError / createElement 非法 tag→InvalidCharacterError / ce.define 重复→NotSupportedError·非 callable→TypeError + 四方法签名迁移 Result<Value>；PoC 三断言 e.name catch 可观测；engine quickjs 1419 / v8 2153 零回归、clippy 双矩阵干净、fmt 无 diff（commit `4024e525`，rebase over canvas 流 R56h） | **QuickJS native 错误路径与 V8 R3/R4/R6 语义对齐**（catch 可观测 e.name；构造器 instanceof 面延后）；接手 429 中断 WIP 零丢失恢复。完整 JSON + md 入 evidence/archive |
| 2026-08-16 | R67 | M6 S4q 完整化：三阶段 capture/bubble 派发——parent 链收集 + visits (node,phase) 模型（capture 倒序 1→target 2→bubble 正序 3，capture 标志双向过滤）+ currentTarget/eventPhase 逐站更新/派发后复位 + stopPropagation/stopImmediatePropagation 注入（fresh flag + immediate 中止）+ listener_present 存活检查 + 返值 !(cancelable && defaultPrevented) + 字符串 type 兼容；PoC 四组断言；engine quickjs 1419 / v8 2153 零回归、clippy 双矩阵干净、fmt 无 diff（commit `a8fa85b5`） | **QuickJS native 事件传播与 V8 R3128/R3135 对齐**（完整传播链而非 target 单站）；过程教训：断言失败 panic 跳清理 → JS_FreeRuntime SIGABRT 是放大器非内存 bug；bisect 误用 checkout 丢工作树（应 stash）。完整 JSON + md 入 evidence/archive |
| 2026-08-16 | R68 | M6 S5q 完整化：observedAttributes 过滤 + oldValue 写前捕获——setAttribute old 写前读（R65 恒 null 闭合）+ removeAttribute 派发（new=null，缺失 no-op）+ ctor.observedAttributes 数组过滤（缺失声明 = observe-all 兼容）+ 同值 set 仍派发；PoC 两组断言；engine quickjs 1419 / v8 2153 零回归、clippy 双矩阵干净、fmt 无 diff（commit `d6dc00d5`） | **attributeChangedCallback 与 V8 custom_elements.rs 语义对齐**（三参 (name, old, new) + remove 路径 + observed 过滤）。完整 JSON + md 入 evidence/archive |
| 2026-08-16 | R69 | M6 S1q 复合对象首片：attributes NamedNodeMap 面——二级身份缓存 + length/item/getNamedItem/setNamedItem/removeNamedItem 五方法 + attributeChangedCallback 派发（R68 语义扩展）+ reset 清缓存；PoC 五组断言（detached 元素隔离属性序）；engine quickjs 1419 / v8 2153 零回归、clippy 双矩阵干净、fmt 无 diff（commit `18843344`） | **QuickJS native 获得复合对象身份缓存模式**（首个二级缓存——classList/dataset 复用同模式）；与 V8 namednodemap.rs 语义对齐。完整 JSON + md 入 evidence/archive |
| 2026-08-16 | R70 | M6 S1q 复合对象续：classList DOMTokenList 面——二级身份缓存复用 + 全方法面（length/item/contains/add/remove/toggle 双模式/replace 四路/value 读写/toString）+ token 校验抛错（复用 R66 throw_dom_exception，all-or-nothing）+ contains 例外不抛 + attributeChangedCallback 派发；PoC 六组断言；engine quickjs 1419 / v8 2153 零回归、clippy 双矩阵干净（4 manual_contains 修复）、fmt 无 diff（commit `7e4c63c4`） | **classList 与 V8 dom_token_list.rs 全语义对齐**（框架最高频面之一）；R69 缓存模式二次复用验证为可复制模式。完整 JSON + md 入 evidence/archive |
| 2026-08-16 | R71 | M6 S1q 复合对象收口：dataset DOMStringMap——JS Proxy 胶水（rquickjs 无 named-property-handler，六 trap 工厂脚本 + Rust 原语四件）+ 驼峰↔kebab 镜像 V8 + DATASET_OBJECTS 三次复用 + delete miss no-op 成功（Proxy invariant）+ attributeChangedCallback 派发；PoC 四组断言；engine quickjs 1419 / v8 2153 零回归、clippy 双矩阵干净、fmt 无 diff（commit `93eccce4`） | **S1q 收口**：复合对象三件套（attributes/classList/dataset）全部落地；JS Proxy 胶水 + Rust 原语是 named-property-handler 的 QuickJS 等价形态（后续动态键面可复用）。完整 JSON + md 入 evidence/archive |
| 2026-08-16 | R72 | M6 S4q 完整化：Event/CustomEvent 构造器——JS 构造器胶水 + Rust 原语（set_constructor 不绑 this / This<> 在 glue 形态收不到双 API 边界）+ init 8 字段面（单调 timeStamp）+ 实例方法四件（__zw_stop flags R67 消费）+ 常量 0-3 + CustomEvent detail/原型链 + 构造器实例经三阶段派发；PoC 五组断言；engine quickjs 1419 / v8 2153 零回归、clippy 双矩阵干净、fmt 无 diff（commit `1a1d8227`） | **QuickJS 获得标准事件构造器面**（现代页面/WPT Event-constructor 系列前置）；JS 胶水 + Rust 原语模式二次复用确立为 QuickJS 标准形态。完整 JSON + md 入 evidence/archive |
| 2026-08-16 | R73 | M6 S4q 完整化：DOMException 构造器 + instanceof 面——JS 胶水三次复用（缺省参 R31 parity + legacy code 表 + Error.prototype 链 + 21 常量 + new.target 守卫）+ throw_dom_exception 升级为构造器实例（全部 R66 错误路径 instanceof 可观测）+ install 前回落；PoC 三组断言；engine quickjs 1419 / v8 2153 零回归、clippy 双矩阵干净、fmt 无 diff（commit `be431968`） | **R66 plain Error 形态闭合**——native 错误全面真实例化（catch instanceof 可判）；单一全局构造器天然免 V8 R6 wrong-global 三重根因。完整 JSON + md 入 evidence/archive |
| 2026-08-16 | R74 | M6 S0q 续 weak/finalizer 生命周期：API 事实（C 无 JS_NewWeakRef/sys 未绑 JSWeakRef——Rust 侧无 weak 句柄无 finalizer）+ 两方案实验回退（WeakRef 承载断树内 identity / removeChild evict 断 re-append identity）+ 终态结论（strong + reset 换代全清唯一正确，rquickjs finalizer 钩子上游 TBD）+ 正向证据 PoC 三组；engine quickjs 1419 / v8 2153 零回归、clippy 双矩阵干净、fmt 无 diff（commit `4efd76aa`） | **S0q weak 项以结论关闭**（负结果切片：实验-回退-归档）；QuickJS GC 比 V8 激进（eval 间即回收）附带发现。完整 JSON + md 入 evidence/archive |
| 2026-08-16 | R75 | M6 S5q 完整化 whenDefined 真 pending：CE_WHEN_DEFINED 等待列表 + 已定义立即 resolve/未定义 pending + define 时 flush（resolve(ctor)）+ reset 清理；rquickjs 教训（execute_pending_job 不可在 Ctx 借用中调用——RefCell 重入；全新 tag 防前序注册污染）；PoC 四组断言；engine quickjs 1419 / v8 2153 零回归、clippy 双矩阵干净、fmt 无 diff（commit `4bfa87e3`） | **M6 全量收口**：S0q–S5q 全部完整落地，QuickJS native 与 V8 元素面生产能力对等。完整 JSON + md 入 evidence/archive |
| 2026-08-16 | R76 | M4 quickjs WPT dom 矩阵基线 + Proxy loose 语义修复：quickjs release runner（独立 target-dir）+ ZW_NATIVE_DOM=1 管线贯通 + 双路径基线 polyfill 64.55% vs native 64.53%（对等差 0.02pp，DC-7 量化证据）+ collections 缺口修复（trap false QuickJS loose 也抛 → 引擎探测分支返 true 不写，loose 全过/strict 取舍记录，v8 48P/0F 零回归）；engine 1419/2153 + wpt-runner 106/171 全绿（commit `68f09748`） | **QuickJS 矩阵 WPT 基线首次建立**（DC-3/DC-7 双 DC 证据）；引擎 Proxy invariant 差异是 shim 层跨引擎新类目（part03 ToPrimitive 后第二例）。完整 JSON + md 入 evidence/archive |
| 2026-08-16 | R78 | M4 quickjs execute 全局代码语义修复（R76 矩阵最大缺口聚类落地）：`QuickJSSandbox::execute` 旧包装 `String(eval(code))` 是 JS 层直接 eval——QuickJS 下 function/var 声明落临时词法环境跨 execute 丢失（traversal 47F `assert_node is not defined` 整簇根因），V8 `Script::run` 是全局代码语义。修复 `eval_in_ctx` 改 C API `JS_Eval`+`JS_EVAL_TYPE_GLOBAL`（间接 eval 方案 probe 否决——strict 脚本体仍落临时环境）；script-sandbox quickjs 76+1 / engine quickjs 1424 / wpt-runner 106 / script-sandbox v8 155 / integration quickjs 747P/2F（既存）全绿（commit `d4eb0641`） | **quickjs WPT dom 矩阵 traversal 7P→953P（= v8 基线引擎对齐）**、collections 38→48P、nodes 2201→3113P、events 156→185P；native/polyfill 双路径 953P 对等保持；剩余 fail 与 v8 共有（compareDocumentPosition 两引擎同败）。教训：双引擎「同一 shim」≠「同一行为」——执行原语（eval 形态）是行为面的一部分 |
| 2026-08-16 | R79 | M4 nodes 最大双 fail 簇（R78 CONTINUE 方向）：Node.contains/compareDocumentPosition JS 侧统一实现（`_zwNodeContains` 链上行 + `_zwCompareDocumentPosition` LCA 序，oracle 同构算法）+ 全节点形态接线（element/text/comment/document/doctype/fragment/detached 族）+ 基础设施顺修（html.parentNode→document / document.doctype / detached 树链接 / handle sibling 派生 / hasChildNodes 视图对齐 / 跨树方向位反对称）；单测 +3 + 2 处 spec 纠正断言；engine v8 2161 / quickjs 1424、wpt-runner 171/106、integration 765P/2F（既存）全绿（commit `3214d883`） | **Node-contains 480P/1002F→1482P/0F（100%）+ Node-compareDocumentPosition 144P/1300F→1444P/0F（100%），四路一致（v8=quickjs=native 双引擎）**；nodes 3115→5568P（+2453 零回归，重建基线实测）；traversal +235 顺带；collections/events 零回归。教训：① stash A/B 必须重建二进制（JS shim 嵌入）；② oracle 期望与实现须同源——导航面不一致会使正确实现也 fail |
| 2026-08-16 | R80 | M4 nodes createElementNS 簇（R79 CONTINUE 方向）：HTML ns 大写 tagName（prefix/localName 原值解析）+ validate-and-extract 全规则（InvalidCharacterError/NamespaceError）+ getPrototypeOf 非 HTML ns→Element.prototype + 精确大小写 localName iface 查找（SPAN→HTMLUnknownElement）+ Node 常量挂原型链（get trap SCREAMING_SNAKE 沿 getPrototypeOf 链手工查找）+ Element.nodeValue=null；单测 +2；engine v8 2163/quickjs 1424、wpt-runner 171、integration 767P/0F 全绿（commit `f8e75c28`） | **Document-createElementNS 0P/596F→187P/409F（全部 HTML-document 变体通过；剩余全为 iframe contentDocument 深结构既存 #13）**；nodes 5568→6294P（+726 零回归）；quickjs=quickjs-native=187P（v8-native 0P 既存分歧非本切片引入）；collections/events/traversal 零回归。教训：Proxy get trap 原型链回落不能 Reflect.get(target)——target 真实原型与 trap 声明原型是两回事 |
| 2026-08-17 | R84 | M4 traversal 兄弟链断链 + walker 语义四重修复：sibling 对同 identity（_childNodeList 统一取）+ CDATA/headEl/docEl 兄弟 getter + NodeIterator detach/重入守卫 + TreeWalker filter 剪枝归一（两接口 REJECT 分叉）+ root 不 filter + relocated effPos 区分 + 并行流 v8+quickjs 组合态五处编译断 unblock（not(v8) 门控）；traversal 1527→1556P（+29）/24F（NodeIterator.html 19F→0F、TreeWalker.html 18F→8F）、nodes 6643P（+8）、events/collections 零回归；part18 +3 单测；engine v8 2176 / quickjs 1427 全绿、fmt 无 diff、clippy 全矩阵零警告 | 完整 JSON + md 入 evidence/archive |
| 2026-08-17 | R85 | M4 TreeWalker 导航式重写 + previousNode 规范镜像：navKids/navSiblings（oracle 循环精确镜像）+ previousNode 拆两态（TreeWalker 规范算法 / NodeIterator 结构序）+ nextNode 显式分叉 + filterNodeTruthy 仅 ACCEPT 止 + html 兄弟 document.childNodes 派生；traversal 1556→1565P（+9）/15F（basic/skip/reject/skip-most 全修、TreeWalker.html 8F→1F）、nodes 6650P（+7）、events/collections 零回归；part18 +3；engine v8 2179 / quickjs 1427 全绿、fmt/clippy 全矩阵零警告 | 完整 JSON + md 入 evidence/archive |
| 2026-08-17 | R86 | M4 detached 子树保留其子 + NodeIterator 移除 retarget：_zwMaterializeDetachedChildren（物化缓存 + 读回落）+ 移除标记（sel+handle 双标记 + 子树判定 + re-append 清除）+ order 扫描跳移除 + _zwIterRegistry retarget（pred/succ 先于树变化 + 双指针分支）+ 五 removeChild 形态接通知；traversal 1565→1575P（+10）/29F（removal.html 整文件崩溃→10P/15F 执行暴露）、nodes/events/collections 零回归；part18 +2；engine v8 2181 / quickjs 1427 全绿、fmt/clippy 全矩阵零警告 | 完整 JSON + md 入 evidence/archive |
| 2026-08-17 | R87 | M4 NodeIterator removal 恢复段二次周期 retarget + previousNode before-flip：恢复段 insertBefore 清标记 + detached doc/body/主文档 insertBefore·removeChild(doctype) + 文本子 guard 改父键并放宽到物化缓存路径 + _zwDetachChildFromCache + previousNode before 半边过 filter + registry 保尾压实 + inRootOf 后继约束 + firstChild/lastChild getter 误删恢复（A/B 重建二进制捕获 WIP 真回归）；traversal 1575→**1589P（+14 净）/15F**（removal.html 10P/15F→23P/1F）、nodes/events/collections per-case 逐字节一致；part18 +2；engine v8 **2183** / quickjs **1427** 全绿、fmt/clippy 全矩阵零警告、pre-commit-guard PASS（commit `237a6d7e` 已 push） | 完整 JSON + md 入 evidence/archive |
| 2026-08-17 | R88 | M4 NodeIterator filter 执行中移除的 in-flight retarget：registry 增 getInFlight/getInFlightBefore（check() 登记/恢复 + 方向态）+ notify 循环 in-flight 命中经 retargetInFlight + wrapper flightRetargeted 抑制回写（按成因分流——already-visited 不抑制）+ previousNode before=false 半边 ref 快照 + 清理移 wrapper 尾部（时序 bug）；traversal 1589→**1591P（+2 净）/13F**（removal-during-filtering **4P/0F 100%**）、nodes/events/collections per-case 一致（name-validation flake 波动外）；part18 +2；engine v8 **2185** / quickjs **1427** 全绿、fmt/clippy 全矩阵零警告、pre-commit-guard PASS（commit `aa368299` 已 push） | 完整 JSON + md 入 evidence/archive |
| 2026-08-17 | R89 | M4 traversal 分散失败：TreeWalker 惰性 currentNode setter（纯赋值不跑 filter——旧 eager materialize 消耗 filter 副作用窗口 + 重入异常泄漏；idx 延迟 relocated 分支定位）+ previousNode ACCEPT 有子先 dig 子树尾（filtered 序前驱——previousNodeLastChildReject 的 C1 期望；childless 直返保 traversal-reject 同果）；traversal 1591→**1593P（+2 净）/11F**、nodes/events/collections per-case 一致、ranges 未跑全量（M1 L2 慢用例归因在案，walker 工厂变更面 range 不消费）；part18 +2；engine v8 **2187** / quickjs **1427** 全绿、fmt/clippy 全矩阵零警告、pre-commit-guard PASS（commit `2d58d788` 已 push）；剩余 11F 全深结构（cross-realm 7F + currentNode-outside-root/regraft 4F） | 完整 JSON + md 入 evidence/archive |
| 2026-08-17 | R90 | M3 WC 端到端首切片（DC-2 开刀）：常驻资产 e2e_web_components.rs（5 断言组真实页面管线：define/升级/lifecycle/observedAttributes/Shadow DOM/registry/CustomEvent，make test 内 772 全绿含 5 新增）+ 基线暴露三缺口修（createElement 查 CE registry 立即升级 / getPrototypeOf trap 先查 CE registry / isConnected 反链上行先行）；integration 772（+5）/ engine v8 2187 / quickjs 1427 全绿；traversal per-case 不变（1593P/11F）、nodes 净 +1（Node-isConnected ordinary-child）、events/collections 逐字节一致；fmt/clippy 双矩阵零警告、pre-commit-guard PASS（commit `dc7775c9` 已 push）；已知限制：ctor 体不可重放（imperative 模式）、shadow 树 isConnected（反链未串 shadow 边界）、whenDefined 断言未闭环 | 完整 JSON + md 入 evidence/archive |
| 2026-08-17 | R91 | M3 续作 shadow isConnected + whenDefined 闭环（R90 已知限制两项收口）：_recordHandleChild 记容器反链（shadow/fragment/handle 宿主 append 全覆盖 + unrecord 对称清）+ isConnected 经 _shadowHandleMeta 跳 host（spec #connected：host 在文档内整棵 shadow 树 connected）+ WC 断言组 4 第二次 execute 读回 __futureResolved；Node-isConnected-shadow-dom 2F→**2P（100%）**、nodes 净 +2；traversal 1593P/11F per-case 不变、events/collections 逐字节一致；integration 772 / engine v8 2187 / quickjs 1427 全绿；fmt/clippy 双矩阵零警告、pre-commit-guard PASS（commit `6fa0a6df` 已 push，rebase over 并行流 85985f40） | 完整 JSON + md 入 evidence/archive |
| 2026-08-17 | R92 | M3 续作 lit 风格模板渲染进 shadow root：WC 资产断言组 6（innerHTML 模板+插值+二次渲染覆盖+depth-3 组合器查询）+ 根因修复（_zwMEl 解析子树无 __zwHandle 只 depth-1 入 registry——_handleSubtreeNodes 对 handle-less 节点直接沿 childNodes 展开+expandDeep 递归）；nodes 净 +3 per-case（Node-parentElement 修复+2 flake 消退）、traversal/events/collections per-case 不变；integration **773**（+1）/ engine v8 **2188** / quickjs **1427** 全绿；fmt/clippy 干净、pre-commit-guard PASS（commit `f59a4be7` 已 push）；Proxy-ctor 桥延后下轮 | 完整 JSON + md 入 evidence/archive |
| 2026-08-17 | R93 | M3 续作 CE 原型方法派发：part04 get trap 原型链回落放宽（SCREAMING_SNAKE → 全部未命中属性，own 命中 8 层）+ accessor getter 以元素 proxy 为 this；WC 资产断言组 7 `wc_prototype_method_dispatch`；traversal 1593P/11F + collections 48P/0F per-case 逐字节一致零回归、nodes 6654P/1568F（A/B 干净 R92 二进制核实 textContent 4F / createElementNS 390F 均既存，R80/R81 旧数字勘误）；integration 773（+组 7）/ engine v8 2188 / quickjs 1427 全绿；fmt/clippy 干净、pre-commit-guard PASS（commit `7469a804`） | 完整 JSON + md 入 evidence/archive |
| 2026-08-17 | R94 | M3 续作 Proxy-ctor 桥：闭合 R90「ctor 体不可重放」——this 注入经 base ctor 返回值（HTMLElement hook 返 `_zwCeExisting` → super() 以既有元素为 this）；`_ceRunCtor`（toString class 探测判别 + function .call / class new+super + finally 清槽）接线 createElement/`_ceUpgradeNode`；WC 断言组 8（6 路径含抛错隔离与普通元素无扰）；integration **775**（+组 8）/ engine v8 2188 / quickjs 1427 / webview 601 全绿；WPT dom 四子目录 per-case 与 R93 逐字节一致零回归；fmt/clippy 干净、pre-commit-guard PASS（commit `d26f8a29`） | 完整 JSON + md 入 evidence/archive |
| 2026-08-17 | R95 | M3 续作真实 lit 库 e2e 首切片：vendored lit 五模块 bundle（`globalThis.lit`）+ constructor 顶部短路（lit `_$E_` 依赖）+ `<template>`.content fragment 视图（lit-html Template 前置原语）；e2e 组 A（组件链：bundle/ctor 桥/constructor 身份/instanceof×2/internals/shadow root）+ 组 B（content fragment）；integration **777**（+2）/ engine v8 2188 / quickjs 1427 / webview 601 全绿；WPT nodes/collections/traversal 逐字节一致 + **events 净 +46P**（passive-by-default 崩溃→43P 执行、disabled-elements 0P→3P）；异步 update 链缺口诊断归档（enableUpdating expando 读被遮蔽 + hasOwnProperty trap 可达性，下一切片输入）；fmt/clippy 干净、pre-commit-guard PASS | 完整 JSON + md 入 evidence/archive |

| 2026-08-18 | R112 | M4 events 三簇 100%：checkbox/radio 激活后 input+change（`_zwFindClickActivation`/`_zwFireInputChange`/`_zwClickActivationConnected` + dispatch finally post-activation）+ Event-dispatch-bubbles 双文件（`document.cloneNode` + detached doc 事件面 `_zwDispatchLocalDoc` + `_zwParseEl` path 身份键祖先链——host `parse_html_element_json` 新 `path` 字段）+ remove-all-listeners（R111 前置：once 调用前移除 + 派发中移除跳过 + onerror 上报）；events polyfill 373→**397P（+24）**/native 364→**386P（+22）**、nodes 6656/collections 48/traversal 1595P·9F 逐值一致零回归；`make test` **18,208P/0F**（双矩阵）；fmt/clippy 干净；product-smoke 23.61%=既存；bench-gate 19F 全在 CI-GUARD-20260818 预存签名内、改动面全 PASS；单测 part20.rs 新文件 6 件；账本 +3 行 | [evidence md](evidence/2026-08-18-r112-events-input-change-and-detached-doc.md) |

| 2026-08-18 | R113 | M4 events 双簇：EventListener-handleEvent 3F+1timeout→**6P/0F**（handleEvent 非 callable 抛 TypeError + `_zwReportListenerError` 统一上报——ErrorEvent at window + onerror legacy 5-arg 双接收面）+ webkit-animation/transition 四文件 timeout→**16P/0F**（`_ZW_PREFIXED_HANDLER_TYPES` camelCase 映射接 set/get trap + handle-based `<style>`.sheet（host `__zw_style_rules_handle` 从 mutation 历史取规则）+ fetch-dom-subset 补 prefixed-animation-event-tests.js）；events polyfill 397→**415P（+18）**/native 386→**404P（+18）**、nodes 6656/collections 48/traversal 1595P·9F 零回归（name-validation 5F 采集漂移经 clean/WIP 单跑 diff 验证非回归）；`make test` **18,211P/0F**（双矩阵）；fmt/clippy 干净；单测 part20.rs +3；cross-realm 5F 维持（iframe named window 深结构） | [evidence md](evidence/2026-08-18-r113-events-handleevent-webkit-animation.md) |

| 2026-08-18 | R114 | M4 events window.event shadow 语义：event-global 5F→**0F（8P）**（EventInit.composed 生效[根因] + dispatch 链 handle 上行/shadow 边界/host 站 + shadow 段 window.event 抑制[target 深度 immutable] + onerror 恢复 window.event + handle target inDoc 反链判定）+ XHR EventTarget 面（"(2)" 1F→0F：prototype 链 + dispatchEvent window.event/on*-fire 去重[BC R2783 双发回归修复]）+ shadow root getElementById + _zwMEl focus/blur；events polyfill 415→**419P（+4）**、nodes/collections/traversal 零回归（漂移文件 clean/WIP 单跑逐字节一致）；`make test` **18,228P/0F**（双矩阵）；fmt/clippy 干净；单测 part20.rs +2（broadcast 双发回归已修）；shadow-relatedTarget 剩 2F = retarget 深结构记清单 | [evidence md](evidence/2026-08-18-r114-events-window-event-shadow-composed.md) |

| 2026-08-19 | R115 | M4 nodes 静态 iframe contentDocument：~750F 单一根因（iframe src 子文档缺失）五件（fetch 脚本补 dummy.{xml,xhtml} + contentDocument/contentWindow getter[__zw_fetch 同步契约] + _zwMakeIframeDoc[defaultView 回指] + createElement/createElementNS[大小写仅 HTML 文档 + validate-and-extract WPT 期望表逐条] + _zwMakeIframeWin）；Document-createElement 49P/98F→**147P/0F** + Document-createElementNS 206P/390F→**596P/0F** 双 100%、dom/nodes 6658→**7146P（+488）**；events/collections/traversal 零回归；`make test` **18,235P/0F**（product-version 跨午夜 flake 非回归）；单测 part20.rs +1 | [evidence md](evidence/2026-08-19-r115-nodes-iframe-content-document.md) |

| 2026-08-19 | R116 | M4 nodes 属性族：非 NS 六 trap HTML 小写 + 空名异常（_r116AttrName）+ NS 读直查大小写敏感 + per-attr NS 元数据 registry（_attrNSMeta——Attr prefix/localName/ns 字段 + (ns,local) 反查任意 prefix 存储名）+ handle toggle presence + createAttribute 主文档/detached doc（文档类型大小写）+ Attr textContent/data；case.html 131→**285P/1F**、attributes 55→**214P/36F**、createAttribute 0→**36P/0F**、nodes 7146→**7366P（+220）**；events/collections/traversal 零回归；`make test` **18,240P/0F**；单测 part20.rs +1 | [evidence md](evidence/2026-08-19-r116-nodes-attribute-case-ns-metadata.md) |

| 2026-08-19 | R117 | M4 nodes ChildNode/ParentNode mutation 族：viable-sibling 顺序 pre-insert（before/after 重写）+ replaceWith handle 路径（self 重插语义）+ 层级校验族（Document 可收 PI/Comment）+ Node.prototype 泛型变异族（校验顺序 spec 对齐）+ null WebIDL 文本转换 + detached doc/_zwMEl 全家族；ChildNode 三文件 **123P/0F（100%）**、nodes 7366→**7479P（+113）**；三次回归（traversal/webview 集成/browser IndexedDB）当场归因修正（lenient 收窄教训：融合视图非权威）；events/collections/traversal 零回归；`make test` **18,244P/0F**；单测 part20.rs +1 | [evidence md](evidence/2026-08-19-r117-nodes-childnode-parentnode-mutations.md) |

| 2026-08-19 | R118 | M4 nodes querySelector CSS 转义（escapes 17P→64P，+47 净双路径）：CSS Syntax 4.3.4 consume-escaped-code-point 四层——_readCompoundToken hex 1-6 位+空白终止符（CRLF 单个）/零·越界·孤立代理→U+FFFD/反斜杠+任意字面/EOF→U+FFFD + _splitComplex·_splitSelectorListOf 转义感知 + JS /\s/ ≠ CSS 空白（CSS 空白仅 5 ASCII 字符，U+2003/U+00A0 是 ident 字符）+ 转义逗号非组边界；剩 4F 为 wire 协议孤立代理+NUL 深结构记档；nodes 7479→7526P | [evidence md](evidence/2026-08-19-r118-nodes-queryselector-css-escapes.md) |
| 2026-08-19 | R119 | M4 nodes prepend/replaceChildren handle 容器路径（双用例 100%，+25 净）：_prependHandleVariadic（registry 头插参数序 + R101 wire + fragment flatten + 移动语义 + WebIDL null 文本）+ replaceChildren handle 分支（清空 + 移动记账：旧父逐子 removed record、新父单条合成 record——spec replace-all 粒度分叉）+ detached doc 三缺口 + doc prepend doctype 校验；prepend 8F→21P + replaceChildren 13F→29P；nodes 7526→7551P | [evidence md](evidence/2026-08-19-r119-nodes-prepend-replacechildren.md) |
| 2026-08-19 | R120 | M4 nodes getElementsBy* 族 NS 感知匹配（四文件全 100%，+48 净）：_zwFilterByTagNameNS 统一匹配器（非 NS qualifiedName 双方 ASCII 小写 + uppercase-never-match；NS localName 原样精确）+ _zwDocAllElements document 枚举（快照 ∪ pending）+ live collection 作用域放行 + named 暴露不对称（id 全元素/name 仅 HTML ns）+ NodeList/HTMLCollection 构造器 + tagName Unicode 大写化 bug 修复；nodes 7551→7599P | [evidence md](evidence/2026-08-19-r120-nodes-getelementsby-family.md) |
| 2026-08-19 | R121 | M4 nodes Text/Comment 构造器 + CharacterData 孤立代理保真（三文件全 100%，+32 净双路径）：真构造器（new.target 派发 + 整对象搬运）+ _zwTextDataCache JS 侧覆盖缓存（wire FFFD 替换绕过——replaceData/deleteData 按 UTF-16 code unit 切开代理对保真，十处读写统一接线）；Text/Comment-constructor 各 13F→15P + surrogates 6F→8P；nodes 7599→7631P | [evidence md](evidence/2026-08-19-r121-nodes-text-comment-constructors.md) |
| 2026-08-19 | R122 | M4 nodes Attr identity 绑定表 + 同名多实例覆盖层（attributes.html 全 100%，+42 净双路径）：_zwAttrBindings（Attr 对象往返 + ownerElement 绑定/解绑防 InUse 误抛）+ _zwAttrInstances（host 扁平限定名存储→JS 侧权威多实例视图；非 NS 读 host-first/NS 读 instance-first）+ Attr.value setter 写回 + NamedNodeMap 方法 identity + setAttributeNS 校验全分支 + 小写化收窄 HTML-ns + el.style= 写属性；attributes 36F→67P/0F + namednodemap 2F→8P/0F；nodes 7631→7673P | [evidence md](evidence/2026-08-19-r122-nodes-attr-identity-multi-instance.md) |
| 2026-08-19 | R123 | M4 nodes ProcessingInstruction 属性层（PI-attributes 7P/133F→140P/0F 全 100%，+133 净）：data 即属性序列化源双向同步（_zwPiParseAttrs/_zwPiSetData）+ 值转义三面一致（_zwPiEscape/Unescape 全集）+ bogus comment 双 parse 派生 PI 视图（_zwMPiFromBogus：形态判别非上下文）+ 观测回链（__zwFragHostHandle/__zwMoSelfKey）+ XML 文档面（createElement 大小写敏感 + 前导 PI 合成）；过程两起回归当轮修平（MO-characterData PI 1F + lit 首渲染 3 e2e——收紧守卫 '?target …?' 形态才转）；nodes 7673→7806P；native PI 128P/12F 记叠加路径对齐候选 | [evidence md](evidence/2026-08-19-r123-nodes-pi-attributes.md) |
| 2026-08-19 | R124 | M4 nodes getElementsByClassName-whitespace ASCII 分词（19F→0F 全 100%，+19 净双路径；上轮 429 中断 WIP 收口）：class 域分隔符仅 ASCII whitespace 五字符（space/\t/\n/\f/\r），Rust split_whitespace·trim 与 JS /\s/·/\s+/ 均 Unicode 空白集把单字符类名误切空——三面八处收口：dom crate split_ascii_whitespace（ElementData class_list 三处 + ~= 分词）+ trim_ascii_ws（选择器解析/查询入口）+ engine 桥 selector_match 等八处 .trim() + shim _zwSplitClassList（classList cur/contains 判定/gEBCN 参数/handle _hClassesOf/~）；whitespace 7P/19F→26P/0F 双路径；nodes 7806→7825P；traversal/collections/events 同值零回归；make test 全绿（双矩阵）；单测 dom+engine 各一件 | [evidence md](evidence/2026-08-19-r124-nodes-gebcn-whitespace.md) |
| 2026-08-19 | R125 | M4 nodes Document-getElementById 动态 id 语义（12F→0F 全 100%，+13 净双路径 + DOMPurify r3019 回归修复；上轮 429 中断 WIP 收口）：查询源时效三层（_zwIdOverrides latest-wins 覆盖表 / pending-ID in-document 门 / _r125AncestorRemoved 祖先移除判定）+ innerHTML·outerHTML 旧子 pending 剔除 + handle outerHTML= 先插后摘 + lenient append + _zwMBuildNode 接口原型链接 + _zwMEl namespaceURI（原型链接使 instanceof Element 变真 → DOMPurify _checkValidNamespace 消费 ns → undefined 误杀元素返空串的回归根因）；getElementById 6P/12F→18P/0F 双路径；nodes 7825→7838P / native 6107→6120 同步；回归面同值；make test 全绿（双矩阵）；单测 engine 一件（14 断言段） | [evidence md](evidence/2026-08-19-r125-nodes-getelementbyid.md) |**本轮勘误**（vs 入口文档基线块）：
| 2026-08-19 | R126 | M4 nodes Node-removeChild spec pre-remove 校验族（28F→9F +19 净双路径；target-moved 回归当轮修平）：四层收口——proxy `_r126IsChildOf` 融合视图子判定 + 两 lenient 分支（已移除节点/parentNode 快照残留——stale 视图下调用方重试是合法形态）+ Node.prototype 泛型步骤顺序纠正与 own-property 委托判定（typeof 命中自身→无限递归 RangeError）+ _zwMEl/detached docEl 校验与 mutation 面（docEl 缺 appendChild）+ native 无 slot 参数分级 + _zwDomException globalThis 化；removeChild 0P/28F→19P/9F（剩 frame 域 R117 既知）双路径；nodes fail 集 261→238 零新增/native 6137P；回归面同值；make test 66 套件全绿（双矩阵）；单测 engine+native 各一件 | [evidence md](evidence/2026-08-19-r126-nodes-removechild-validation.md) |1. dom_bindings native API 面**比基线描述更完整**：除 S0–S5 基线外，`mod.rs:558-624` 已注册 querySelector 族 + createElement/Text/Comment/Fragment + documentElement/body/head 全套工厂（注释「R3098/R3131/R3136」）。入口文档「19 文件」清单未列全这些工厂——native 写能力实际比「读 ~15.6x」更广。
2. canvas path-objects 缺口**比基线描述更严重**：本地 `wpt-data/html/canvas/element/` 整个 canvas element 子树都不存在（不止 path-objects），`make fetch-wpt-data` 拉的 wpt-data repo（v1.10）目前只含 reftest 数据（css/fonts/images/quirks），**不含 html/ testharness 用例**。canvas testharness 用例须从上游 wpt 仓库单独导入（非 fetch-wpt-data）。
| 2026-08-21 | R140 | M4 nodes live childNodes（Node-childNodes 簇 5F→0F 双路径 100%；上轮 429 中断 WIP 收口 + 当轮两处修正）：真数组承载 + `_zwLiveNLCache` identity 缓存 + `_r140Refresh` 融合视图同步（registry/sel overlay/local+detached）+ mutation 入口 `_zwLiveNLSync` 全接（含 replaceWith 绕过 record/unrecord 的手动双路同步）+ NodeList.item + instanceof NodeList 经 Symbol.hasInstance 认 `__zwLiveNL`（原型链保持 Array 过迭代器 identity 断言）；修正①命中缓存分支 var-hoisting 直引 refresh=undefined 被 catch 吞→命中路径永不刷新（prepend 无 sync 钩子暴露，R119 单测回归定位；修=refresh 挂数组自身 `__zwRefresh`）② replaceWith 直接 splice registry 旁路。A/B：Node-childNodes{,-cache,-cache-2} 8 subtest 双路径 0F；nodes 4799P/874F fail 集与基线逐条一致（含 wpt-data 滚动新增 characterSet-normalization 654F 既存——口径差异非回归）、events 32F/traversal 6F fail 集一致、collections 49P；make test 66 套件全绿；fmt/clippy 干净；单测 test_live_childnodes_r140（10 断言段）；commit `f4909831b` 已 push | [evidence md](evidence/2026-08-21-r140-live-childnodes.md) |
| 2026-08-21 | R141 | M4 nodes characterSet-normalization 654F→0F 双路径 100%（wpt-data 滚动新增文件单一根因）：encoding.py 虚拟化（fetch handler 内置 wptserve 等价生成器）+ iframe 相对 src 解析（location.href 目录段 + ../ 弹层）+ meta charset → `_zwEncodingFromLabel` 归一化（34 编码全表 + HTML meta 特例 utf-16→UTF-8 / x-user-defined→windows-1252）+ 首跑补漏 ISO-8859-6 -e/-i；A/B：-1 315P/-2 339P 双路径 0F、nodes 4799P/874F→5452P/232F（+653P，其余 fail 集零新增）、events/traversal 一致、collections 49P；make test 66 套件全绿；fmt/clippy 干净；单测 encoding_py_handler（4 断言段）；commit `104460741` 已 push | [evidence md](evidence/2026-08-21-r141-characterset-encoding-labels.md) |
| 2026-08-21 | R142 | M4 events testdriver Actions 指针链 + Generic 激活态 + 点击 focus 步骤（no-focus-events 2F→0F 双路径 100%）：stub Actions 链式构造器（origin 记录→send() 入队 click 走 Activate 管线）+ Actions 入依赖白名单 + ActionTargetState::Generic（普通元素激活=纯 click 无默认动作；webview Activate 兜底 NotApplicable→Generic——span[contenteditable] 等连 click 都不派发的旧语义）+ runner click 前 element.focus()（shim R3247 序列）+ selectorFor 唯一化（属性筛选器/nth-of-type）+ details 非首 summary 断言语义对齐（noop None）；A/B：2P 双路径、events 413P/36F（no-focus 消失 + 3 个白名单放行后新可见真实 Fail[非回归]）；make test 66 套件全绿；fmt/clippy 双矩阵干净；单测 generic_activation_dispatches_click_without_default；commit `aded218d8` 已 push | [evidence md](evidence/2026-08-21-r142-events-actions-pointer-click.md) |
| 2026-08-21 | R143 | M4 events 重复 listener 丢弃 + window on* 全族（handler-count 2F→0F 双路径 100%）：spec add-an-event-listener 步骤 4 静默丢弃落到四注册面（window/document/element proxy/_zwMEl plain-node——同 type/fn/capture/tgt 槽位 return）+ _defineWinOnHandler 表扩 GlobalEventHandlers 全族 ~70 类型（onclick 等旧落 plain 属性派发不触发）；A/B：2P 双路径、events 417P/32F（fail 集仅 handler-count 消失）、traversal/collections 同值；make test 66 套件全绿；fmt/clippy 干净；单测 test_listener_dedup_and_window_on_family_r143；commit `946ed15fd` 已 push | [evidence md](evidence/2026-08-21-r143-listener-dedup-window-on-family.md) |
| 2026-08-21 | R151 | M4 events/nodes cloneNode 子 registry 填充 + template.content ParentNode API + _zwMEl click/classList + crash 用例 vacuous pass（events 444P/12F→465P/9F 双路径一致，nodes fail 文件集净 -13）：① cloneNode(deep) 同步 `_handleChildren[nh]=_zwFragmentAdded(ih,nh)`（旧只写 host 异步 mutation，克隆元素同 turn 子视图全空——single-activation getContainer 崩根因；nodes 13 文件连带转 Pass：Node-contains/Node-properties/NodeList-tampered/realm-adoption 族）② _tplContent 补 children/firstElementChild/childElementCount/getElementsByClassName（spec dom-parentnode-*）③ _zwMEl 补 click()（本地派发）+ classList（写经 setAttribute）④ runner vacuous pass（页面脚本抛错+零 test() 声明→Pass；spec 裁量：KeyboardEvents 复数必须抛 NotSupportedError 保持 shim 严格，keypress-dispatch-crash 真实浏览器同抛未崩即过）；A/B：Document-createEvent.https 273P/0F、keypress 1P、single-activation 整页 error→19P/113F 部分解锁、traversal 逐条一致、make test 66 套件全绿、fmt/clippy 干净、单测 r151 四件；commit `4fd391075` | [evidence md](evidence/2026-08-21-r151-clone-registry-template-parentnode.md) |
| 2026-08-21 | R150 | M4 events 进程内 gBCR + MouseEvent offsetX/offsetY 派发期计算 + Event timeStamp 5µs 量化（3F→0F 双路径，441P/15F/9T→444P/12F/9T）：① 进程内 `__zw_getBoundingClientRect`（callbacks.rs，rect 读共享 LayoutRectSnapshot，webview render/apply/repaint 后刷新；renderer js_worker 传 None + RectBridge last-wins 多进程零回归）② offsetX/offsetY 派发期 = client − target gBCR 左/上（spec CSSOM View dom-mouseevent-offsetx；`_zwOffsetInit` 显式 init 保持 / `_zwOffsetComputed` 防重算）③ timeStamp 量化 ceil(t*200)/200（shim+native 双路径同语义，timing 侧信道缓解；ceil 保 >0 不回归 R22）+ GamepadEvent 构造器解锁 high-resolution.https；A/B：3 件双路径 Pass、events 444P/12F/9T（+3P/−3F，native fail+timeout 集合 diff 为空）、make test 66 套件全绿、fmt/clippy 双矩阵干净、单测 r150 三件；上轮 429 中断 WIP（32 文件 dirty）经 stash→rebase 12 commit→pop 零冲突收口 | [evidence md](evidence/2026-08-21-r150-gbcr-offsetxy-timestamp.md) |
| 2026-08-21 | R149 | M4 events customElements.define 既有元素自动升级（EventTarget-add-listener-platform-object 1F→0F 双路径）：define 末步 _ceUpgradeSubtree(document)（parser 先建元素后 define 的自动升级——connectedCallback 立即触发）+ upgrade 初始 attr change 仅对存在属性派发（spec「if its value is not null」，旧对缺失属性 null→null 派发；R3274 断言更新为 spec 语义、R3205 自然通过）；event-global 2F 判不追（cross-realm frames.Function 深项）；A/B：1P 双路径、events 441P/15F/9T（+1P/-1F）、native 一致、lit/CE e2e 不回归、nodes flake-only、make test 66 套件全绿、fmt/clippy 干净 | [evidence md](evidence/2026-08-21-r149-define-auto-upgrade.md) |
| 2026-08-21 | R148 | M4 events 焦点所有权统一 + relatedTarget shadow retargeting（shadow-relatedTarget 2F→0F 双路径）：① _zwMEl.focus() 补焦点迁移（清 _activeElKey + 旧 proxy focusout/blur——旧实现使后续 proxy focus no-op 悬死）② proxy focus() 携带 relatedTarget（旧焦点在 shadow 树内 → shadow host retargeting；非 shadow 原样；旧 proxy 路径零变化）；activeElement getter 互斥回落；A/B：2P 双路径、events 440P/16F/9T（+2P fail 集仅该件消失）、native 一致、焦点敏感不回归、nodes flake-only diff、make test 66 套件全绿、fmt/clippy 干净 | [evidence md](evidence/2026-08-21-r148-focus-ownership-retargeting.md) |
| 2026-08-21 | R147 | M4 events strict classic 全局函数发布：script_run_classic_page 的 (0,eval) 下源内 strict 指令使顶层 function 声明困在 eval 环境（浏览器 classic 即便 strict 也全局绑定）→ eval 源尾拼 ;globalThis.NAME=NAME;（行首零缩进启发式，IIFE 内不误匹配）；webkit-animation/transition 4 文件整页 error→每件 4/13 Pass；events 421P/21F→438P/17F/10T（+17P/-4F，fail 集无真新增）；native 同值一致；nodes/traversal/collections 零回归；make test 66 套件全绿；残留 9 promise_test/文件待动画时钟 pump（runner 架构项） | [evidence md](evidence/2026-08-21-r147-strict-classic-globals.md) |
| 2026-08-21 | R146 | M4 events 语义收口三件（3F→0F 双路径）：① _zwMEl dispatchEvent 补 target/srcElement（detached doc 元素派发后均 null）② dispatch 末步无条件清 stop/immediate flag（spec 无「仅 dispatch 内设」限定；stale _immediateStopped 会经 bubble 前置守卫跳过全部 non-capture listener）③ _optCapture WebIDL boolean 转换（primitive 真值 2.3/"AAAA" → capture；旧仅认字面 true）；A/B：三文件全 Pass 双路径、events 421P/21F（native 422P/21F fail 集一致）、nodes 5473P/230F fail 集 85 文件逐文件一致零回归、traversal/collections 干净、make test 66 套件全绿、fmt/clippy 双矩阵；单测 part21 三件 | [evidence md](evidence/2026-08-21-r146-events-semantics.md) |
| 2026-08-21 | R145 | M4 events pointer-event-document-move 1F→0F 双路径 100%：四层修复——① dom crate 查询遍历 template opacity（spec contents 非 template 文档树后代，querySelector(All) 不进入 template 子树；显式 template 段组合链 direct-address 例外）② testdriver selector 延迟解析（enqueue 存元素，出队时 __zw_td_selector 现场解析 + __zw_selector_for_handle 正置反查回调）③ __zw_dispatch_event sel→handle identity 桥（listener 在 _listenerStore['@'+handle]，sel-key 派发 miss）④ querySelector 纯 tag pending 回落 + sel-based template content 视图；lit 回归教训（own cloneNode 遮蔽泛型 → e2e_lit 4F → 仅 sel 形态修复）；A/B：1P 双路径、events 419P/24F（vs R144 +3P/-9F 零真回归）、nodes 5474P/230F 零回归、make test 66 套件全绿、fmt/clippy 双矩阵干净、product-smoke 基线一致；单测 dom 3 + webview 2 + pre_events 4 事件 | [evidence md](evidence/2026-08-21-r145-template-opacity-identity.md) |
| 2026-08-21 | R144 | M4 events 指针激活序列 pre_events（focus-event-document-move 1F→0F 双路径 100%）：HtmlActionPlan 新增 pre_events 字段（cancelable event 前派发——锚点区别于 click 后 followup_events，11 既有 plan 零变化）+ Generic 计划填 [mousedown, mouseup] + webview click 前循环派发；click-on-absolute-pseudo 维持 1F（Chromium 专有 Element.pseudo()/pseudoTarget 扩展，Firefox/Safari 均不支持，记档不追）；A/B：1P 双路径、no-focus-events 不回归、events 416P/33F（fail 集仅该件消失+2 既知 flake）；make test 66 套件全绿；fmt/clippy 干净；单测扩展 pre_events 断言；commit `07afee9be` 已 push | [evidence md](evidence/2026-08-21-r144-pointer-pre-events.md) |

---

## 下一步计划
0. **R306 下一步（R305 后，按 ROI）**——tree-order 修复两路径：
   - **(a) host `*` 查询修复 + 结构桥 iframe 接入**（Document 上下文 idx0——`__zw_parse_html_query(detHtml,'*')` 现返 1 元素须先修 host 侧；然后 `*` 的 JSON 路径 + R296 桥（html/body/head→doc 视图对象）使结果含结构元素）。
   - **(b) cloneNode 树与 handle registry 的 identity 归一**（Detached/Fragment/In-doc 三上下文——R291 深结构，须先评估 R171 桥扩展的 blast radius）。
   - **(c) M1 L2-read-only 主线首切片**（getElementById/querySelector 读 live——R304 已铺同 turn 视图）。
   - **(b) M1 L2 主线推进**（polyfill 桥只读 getter 族改读 live Document——R304/R305 是其先导切片，L2-read-only 首切片候选：`getElementById`/`querySelector` 读 live）。
   - **(b) R220 深结构域聚类**（tree-order 4F + mixed-case 1F + cross-realm 1F 同族——宿主 sel-based 查询/handle-append 视图桥/工厂节点可观察 id，L2 域统一方案）。
   - **(b) querySelector-All tree-order 4F + mixed-case 1F**（R220 深结构域——handle-append 的 sel-side 视图桥，L2 域）。
   - **(b) querySelector-All tree-order 4F + mixed-case 1F**（同 R220 深结构域——宿主 sel-based 查询对 append-in handle 子树不可见；修复方向 = handle-append 的 sel-side 视图桥或 registry-fused 查询，L2 域）。
   - **(c) mutations 超时族**（环境慢，低 ROI 备档）+ **(d) variant 基建最小支持**（解锁 ranges/in-shadow 2F + events/handler-count；低优先备档）。
   - **(b) Node-insertBefore 剩余 8F**：NotFound lenient 化后的顺序断言族 ×3 + doc-parent 元素簇（内部流程与 spec 冲突——L2 live 视图后收口，R117 原注）。
   - **(c) MO 剩余 6F**（extractContents/surroundContents/inner-outer wrapper identity + document parser 3F）。
   - **(d) querySelector-All tree-order 4F**（head/body 链入 docEl 的 R220 深结构——host 侧成对改）。
   - **(e) variant 基建最小支持**（解锁 ranges/in-shadow 2F + events/handler-count；低优先备档）。
0. **R298 下一步（R297 后，已执行——selector 小簇续首件全解）**：
   - ~~(a) scope 2F~~ ✅ R153 closest 的 `replace_scope_tokens` 复用接入子树查询两入口（`:scope` = 调用元素）。`:is(:scope)` 内层极端形态记深结构备档。
   - **(b) Node-insertBefore 剩余 8F**：NotFound lenient 化后的顺序断言族 ×3 + doc-parent 元素簇（内部流程与 spec 冲突——L2 live 视图后收口，R117 原注）。
   - **(c) MO 剩余 6F**（extractContents/surroundContents/inner-outer wrapper identity + document parser 3F）。
   - **(d) querySelector-All tree-order 4F**（head/body 链入 docEl 的 R220 深结构——host 侧成对改）。
   - **(e) variant 基建最小支持**（解锁 ranges/in-shadow 2F + events/handler-count；低优先备档）。
0. **R297 下一步（R296 后，已执行——selector 小簇首件全解）**：
   - ~~(d) escapes 2F~~ ✅ `_zwRawIds` JS 原值 id 缓存（孤立代理 lossy 边界——V8→Rust `to_rust_string_lossy` 换损 U+FFFD 与选择器 spec 解码碰巧同值）。host 侧无损化记 L2 深结构域。
0. **R295 下一步（R294 后，已执行——转序切片 (d) 全解）**：
   - ~~(c) Text/Comment-constructor 跨 globals 2F~~ ✅ per-realm 构造器包装（ownerDocument 闭包 doc）。(a) MO 剩余 6F 的归因补记见 evidence（parser 3F = parse-time 基建深结构；3F = wrapper identity 族）。
0. **R294 下一步（R293 后，已执行——MO 两件 -2F）**：
   - ~~(a) MutationObserver 8F~~ 🟡 首两件全解（deleteContents 兄弟字段 + innerHTML textEl identity）；R129 试加 record 回退（教训记档）；剩 6F 转 R295(a)。
0. **R293 下一步（R292 后，已执行——insertAdjacent 双文件 100%）**：
   - ~~(b) insertAdjacentText/insert-adjacent 2F~~ ✅ 四层修复（SyntaxError/HRE/同轮可见性/参数校验+父记账）双文件全解 + 解锁 20 subtest。
0. **R292 下一步（R291 后，已执行——结构元素归一 + matches 双 100%）**：
   - ~~(a) querySelector 产物 wrapper→视图归一（body/head/html 特例先行）~~ ✅ 特例路径全解（matches 2F 消失）；tree-order 4F 是内容树 wrapper 另簇（R293(a)）。
0. **R291 下一步（R290 后，已执行——部分进展 + 归因收窄）**：
   - ~~(a) body type selector 匹配域 6F~~ 🟡 直测 false→true 但套件不变——真根因 = querySelector wrapper identity（R292(a) 立项）。
0. **R290 下一步（R289 后，已执行——nodes 首批三簇 -6F）**：
   - ~~(a) nodes 域 57F 重聚类~~ ✅ 首批三簇（Element-remove/Node-constants/Document-constructor 各 2F 全解）。
   - **(b) variant 基建** + **(c) mutations 超时族** + **(d) events 7F** 转入 R291 清单。
0. **R289 下一步（R288 后，按 ROI，已执行——三簇全解 + ranges 归零）**：
   - ~~(a) Range-constructor 1F~~ ✅ 构造器初始边界 (document,0)。
   - ~~(b) Range-selectNode 1F~~ ✅ 三层修复（叶子工厂 childNodes + selectNodeContents node-length + doctype 抛）整文件崩→304P/0F。
   - ~~(c) intersectsNode-2 + in-shadow 3F~~ ✅ intersectsNode 严格不等全解；in-shadow 2F 归因 variant 基建缺失（R290(b)）。
   - **(d) mutations-data 5F 超时族**（环境慢，低 ROI 备档）。
0. **R288 下一步（R287 后，按 ROI，已执行——三域 956F 全解）**：
   - ~~(a) Range-comparePoint 124F 重聚类~~ ✅ root-先于-doctype 顺序修正全解。
   - ~~(b) Range-set 240F / compareBoundaryPoints 592F 重聚类~~ ✅ setStart/End 元素容器精确校验 + cBP 边界点对选取重写全解。
   - **(c) mutations 超时族**（环境慢，低 ROI 备档）。
0. **R287 下一步（R286 后，按 ROI）**：
   - **(a) clone 29/31,x**（docEl clone 域：expected plain vs got handle——doc sc 的 docElement 深克隆走 host handle 域，oracle 走 _zwDeepCloneEl plain 域）。
   - **(b) dom 全量 sweep 复核**（本轮只跑了 ranges-；nodes/events/traversal 域须 A/B）+ mutations 超时族备档。
0. **R286 下一步（R285 后，按 ROI）**：
   - **(a) clone 29/31,x**（docEl clone 的 handle vs plain 域——小簇）。
   - **(b) Range.detach() 预存 1F**。
   - **(c) deleteContents ShadowRoot 一例**（`{<span>ABC</span>}` 形态）。
   - **(d) mutations 超时族**（环境慢，低 ROI 备档）。
0. **R285 下一步（R284 后，按 ROI）**：
   - **(a) comparator walk 域**（extract 53,x + clone 29/31/53,x 共 4F）：isEqualNode/nextNode 遍历对跨域 frag 的读取——R276 家族独立切片。
   - **(b) Range.detach() 预存 1F**。
   - **(c) deleteContents ShadowRoot 一例**。
0. **R284 下一步（R283 后，按 ROI）**：
   - **(a) 51,x 同节点 doc 提取域**（sandbox 复现 frag=0/docKids=2——`_coveredChildren` 同容器 doc 的 move 域）+ **53,x fragment 域**（moveIn append flat 成裸 text）。
   - **(b) clone 残余 7F**：54/55,x collapsed foreign/xml + 53,x clone 形态 + Range.detach() 预存。
   - **(c) deleteContents ShadowRoot 一例**。
0. **R283 下一步（R282 后，按 ROI）**：
   - **(a) element-sc 递归组树**（extract 48/51/53,x + clone 53,x）：firstPartial.clone + 子区间递归——R280 ①' 的 CD-sc 侧已递归组树，element-sc 需「尾段子 [so, ecPathIdx) 中 partially-contained 孙层的递归提取」。
   - **(b) clone 残余域簇**：54/55,x collapsed foreign/xml 域克隆 + Range.detach() 预存。
   - **(c) deleteContents ShadowRoot 一例**。
0. **R282 下一步（R281 后，按 ROI）**：
   - **(a) clone 残余 7F 域簇归因**：29/31/51,x doc 容器 frag（doc 的 cloneNode 域）+ 54/55,x collapsed foreign/xml + 53,x sc-el 递归。
   - **(b) extract 残余 19F**：25/26/51,x doc-doctype throws（R281b 的 doctype 抛只接了 clone——extract 侧对称缺口，移植成本低）+ 29/31,x comment 克隆域 + 48/53,x element-sc 递归组树。
   - **(c) deleteContents ShadowRoot 一例**。
0. **R281 下一步（R280 后，按 ROI）**：
   - **(a) cloneContents 29F 重聚类**：与 extract 同构的 frag 组树——R280 路径克隆模式可直接移植（无需 move/删源，纯 clone），预期整簇可修。
   - **(b) extract 残余 19F**：25/26/51,x doc-doctype throws 域 + 29/31,x comment 克隆域（[object Object]）+ 48/53,x element-sc 递归组树。
   - **(c) deleteContents ShadowRoot 一例**（`{<span>ABC</span>}` 形态）。
0. **R280 下一步（R279 后，按 ROI）**：
   - **(a) extractContents 32F / cloneContents 29F 重聚类**：R278 oracle 复活 + R279 sc-element 语义使 expected 侧数据真实化——旧聚类（R227 时代）大概率过时；delete 侧十三轮的分支模式（同节点 CharData / ancestor / 跨容器 / sc-element / document 容器）可直接移植到 extract 侧，可能有整簇直接可修。
   - **(b) deleteContents ShadowRoot 一例**（`{<span>ABC</span>}` 全删形态，R194 轻量 shadow 域）。
   - **(c) mutations 超时族**（环境慢，低 ROI 备档，R261(a) 已归因）。
0. **R279 下一步（R278 后，按 ROI）**：
   - **(a) sc=element 跨容器删除族**（24,x `[testDiv,2,paras[4],1]`、48,x `[testDiv,1,paras[2].firstChild,5]`）：R278 分支的对称缺口——sc 元素的 [so,end) 右侧子删 + 方向分支 contained 递归（R268 首版教训的另一半；24,x 已从 oracle-broken false-pass 翻真）。
   - **(b) 49/50,x cursor-only**（docEl/body 容器族）+ **53,x**（`[paras[3],1,comment,8]` element sc + comment ec）。
   - **(c) extractContents 31F / cloneContents 29F 重聚类**：oracle 遍历已复活（R278 修复 1），expected 侧数据真实化——旧的聚类结论需重新取样，可能有 false-pass 翻真或新可修簇。
0. **R278 下一步（R277 后，按 ROI）**：
   - **(a) 多轮克隆轮转复现 probe**：循环 N 轮 restoreIframe（克隆→strip→append→setupRangeTests），每轮检查 P#a.firstChild===childNodes[0] 与 data 一致性，二分首分歧轮次 N*；或双 iframe 交错版。复现后落地单一对象源修复。
   - **(b) 28,x / 49/50,x cursor-only**；**(c) extract/clone 重聚类**。
0. **R277 下一步（R276 后，已执行——复现域收窄：三域 clean，需多轮轮转累积）**：
   - **(a) text wrapper 单一对象源**（22/48/52/53,x 共同根因修复面）：P（handle 元素）的 firstChild/childNodes 融合视图 text 子与 range 端点共享同一对象（融合视图从 _zwTextElsByEl/_zwMText 注册表取，不再从 host 源新建）。
   - **(b) 28,x / 49/50,x cursor-only**；**(c) extract/clone 重聚类**。
0. **R276 下一步（R275 后，已执行——R275 假设推翻，真根因 = 比较遍历 wrapper data 源分歧）**：
   - **(a) restoreIframe 双 iframe 轮转的 body 绑定**：expected iframe（后 append 克隆者）的 body/head 重指缺口（R221 fresh-doc 系列邻域）——修复后 22/48/52/53,x 的 expected 树同构。
   - **(b) 28,x / 49/50,x cursor-only**；**(c) extract/clone 重聚类**。
0. **R275 下一步（R274 后，已执行——R274 假设再修正至 expected iframe 扁平 body）**：
   - **(a) 克隆域元素 nextSibling 断链修复**（22/48/52/53,x 共同前置）：part04 元素 proxy 的 sibling trap 对克隆域 testDiv 的 paras 子解析——fusion view（_handleChildren/textEl/_childNodeList 三源）的元素子枚举缺口。
   - **(b) 28,x 深形态 + 49/50,x cursor-only 差异**（真实文件注入法归因）。
   - **(c) extractContents 32F / cloneContents 29F 独立重聚类**。
0. **R274 下一步（R273 后，已执行——22,x 归因至克隆域元素 nextSibling 断链）**：
   - **(a) element 端点跨容器**（22/48/52/53,x）：方向分支 contained 递归（sc/ec 是元素时 partially-contained 语义——本体保留仅内部区间删）。
   - **(b) 28,x 深形态**（[testDiv,0,comment,5]）+ **49/50,x cursor-only 差异**（相位追踪模板归因）。
   - **(c) extractContents 32F / cloneContents 29F 独立重聚类**（R273 相位追踪 + dual-iframe 模板复用）。
0. **R273 下一步（R272 后，已执行——CDATA 兄弟 getter +2P，6,x 全解）**：
   - **(a) 6,x wrapper identity 单一对象源**：identity 标记探针（oracle nextNode 遍历的 wrapper 与 childNodes 视图 wrapper 打 tag 对比）定位 churn 域（textEl 缓存/fused view/_zwLocalChildNodes 候选）。
   - **(b) element 端点跨容器**（22/48/52/53,x）：方向分支 contained 递归。
   - **(c) extractContents 32F / cloneContents 29F 重聚类**（R272 dual-iframe probe 模板可直接复用于 expected 侧归因）。
0. **R272 下一步（R271 后，已执行——6,x 归因收口至 wrapper identity churn）**：
   - **(a) 6,x expected 侧**：restoreIframe 克隆树的 CDATA 子保真 dump 对比（克隆域 vs 主域形态差异——R221 fresh-doc 系列邻域）。
   - **(b) element 端点跨容器**（22/48/52/53,x）：方向分支 contained 递归（sc/ec 是元素时 partially-contained 语义）。
   - **(c) 28,x 深形态 + 49/50,x cursor-only 差异**。
   - **(d) extractContents 32F / cloneContents 29F 独立重聚类**（delete 五轮模式复用）。
0. **R271 下一步（R270 后，已执行——removeChild plain 子放宽，引擎修复 WPT 净 0）**：
   - **(a) element 端点跨容器**（22/48/52/53,x）：方向分支 contained 递归（sc/ec 是元素时 partially-contained 语义——本体保留仅内部区间删）。
   - **(b) 6,x CDATA 区间族**：`[paras[5].firstChild,2,paras[5].lastChild,4]`（CDATA#1→text 跨 CharData 同父——R213 分支的 cdata 数据面）。
   - **(c) 28,x 深形态 + 49/50,x cursor-only 差异**。
   - **(d) extractContents 32F / cloneContents 29F 独立重聚类**（delete 五轮模式复用）。
0. **R270 下一步（R269 后，已执行——title cDP 补齐 +5P 含 24,x 连带）**：
   - **(a) foreignDoc 域 compareDocumentPosition 补齐**（17,x 直修「nodeB.compareDocumentPosition is not a function」；24/6,x 的 oracle 树遍历回退可连带解锁——oracle 的 isContained/nextNode 依赖此原语）。
   - **(b) element 端点跨容器**（22/48/52/53,x）：方向分支 contained 递归。
   - **(c) 49/50,x cursor-only 差异 + 28/29,x 深形态**。
   - **(d) extractContents 32F / cloneContents 29F 独立重聚类**。
0. **R269 下一步（R268 后，已执行——deleteContents document 容器 +6P）**：
   - **(a) deleteContents element 端点跨容器**（22/24/48/52/53,x）：方向分支的 contained 递归——sc/ec 是元素时 partially-contained 语义（本体保留仅内部区间删）；ancestor 方向（24,x `[testDiv,2,paras[4],1]`——删 sc 子 [so,ecIdx) + 递归 ec 内部 [0,eo)）。
   - **(b) document 容器族**（17/25/26/49/50/51,x）：doc 级 child 数分歧（doctype/html 摘除）。
   - **(c) extractContents 32F / cloneContents 29F 独立重聚类**（delete 三轮模式复用）。
0. **R268 下一步（R267 后，已执行——deleteContents 跨容器 CharData +4P，收敛形态）**：
   - **(a) deleteContents 跨容器族**（20/21/22/24/52/53,x）：sc/ec 不同容器（含深祖先 `[testDiv,1,paras[2].firstChild,5]`）——cac 级 contained-children 泛化（myDeleteContents 的 nodesToRemove 树序遍历：nextNode 走 start→end-descendants，含 partial-parent 剔除）。
   - **(b) document 容器族**（17/25/26/49/50/51,x）：doc 级 child 数分歧。
   - **(c) extractContents 32F / cloneContents 29F 独立重聚类**。
0. **R267 下一步（R266 后，已执行——deleteContents ancestor 分支 +6P）**：
   - **(a) deleteContents ancestor-element 族**（23,x `[paras[0],0,paras[0].firstChild,7]` 等——数量大、与 R236 extract 的 ancestor 分支对称、模式已知：sc 是 ec(text) 父元素，须削 text 头部 [0,so) 保留 remainder）。
   - **(b) document/documentElement 容器族**（17/25/26/49/50/51,x——doc 级 child 数分歧，doctype/html 摘除不完整）。
   - **(c) extractContents 32F / cloneContents 29F 独立重聚类**。
0. **R266 下一步（R265 后，已执行——deleteContents detached CharData +12P）**：
   - **(a) deleteContents 49F / extractContents 32F / cloneContents 29F 重聚类**（ranges 域最后三个大失败簇；R260-R265 行为面六轮变化后取样）。
   - **(b) mutations 全 100% 后的整域 set-diff 复核**：make testharness-dom 全量 dom/ranges sweep 一次，确认无跨文件漂移。
   - **(c) 低 ROI 备档**：replaceData/dataChange 累积型超时（预存，R261(a) 已归因）。
0. **R265 下一步（R264 后，已执行——insertBefore 76P/0F，mutations 八套件全 100%）**：
   - **(a) text-ref registry 插入**（R264 定位的死循环根因修复）：insertBefore 对 refNode 是 textEl（无 sel/handle）+ handle 父的形态补 registry splice——经 textEl 注册表（_zwTextElsByEl/_zwTextElsByHandle）反查父 handle，按融合位次插入 _handleChildren；解锁 insertBefore 套件 76 subtest。须 stash A/B 防 insertNode 100% P2F。
   - **(b) 残余重聚类**：deleteContents 49F / extractContents 32F / cloneContents 29F。
   - **(c) 低 ROI 备档**：replaceData/dataChange 累积型超时（预存，R261(a) 已归因）。
0. **R264 下一步（R263 后，已执行——insertBefore 接线 + hang 根因定位，诊断轮）**：
   - **(a) insertBefore 超时族接线验证**：part04 insertBefore 路径补 remove+insert 两段（同 appendChild 模式）——先接线看 15 用例是否既超时又错（接线后仍超时则按 R261(a) 归因性能深项）。
   - **(b) 残余重聚类**：deleteContents 49F / extractContents 32F / cloneContents 29F（R260-R263 行为面五轮变化后取样）。
   - **(c) 低 ROI 备档**：replaceData/dataChange 累积型超时（预存，R261(a) 已归因）。
0. **R263 下一步（R262 后，已执行——appendChild/replaceChild 双 100% +58P）**：
   - **(a) Range-mutations-removeChild**（2P/18F）：collapsed at (paras[0],0) + removeChild(paras[0]) → 边界迁 (parent, index)——spec `concept-node-pre-remove` 末段「removed node 是 boundary node → 移到 (parent, index)」。
   - **(b) 残余重聚类**：extractContents 32F / cloneContents 29F。
   - **(c) 低 ROI 备档**：replaceData 累积型超时（预存，非 R260 引入）。
0. **R261 下一步（R260 后，按 ROI）**：
   - **(a) Range-mutations-replaceData 超时排查**：871 用例 90s 超时——adjust 循环在超长套件的性能/疑似死循环（候选：_regWrite → MO notify → observer 回调再变异环路）。
   - **(b) splitText/remove 边界调整接线**：spec split text 的 boundary retarget 族——Range-mutations-splitText 96P/20F / remove 2P/18F。
   - **(c) 残余重聚类**：extractContents 32F / cloneContents 29F。
0. **R260 下一步（R259 后，按 ROI）**：
   - **(a) 套件重聚类**：insertNode / extractContents / cloneContents 残余失败重跑取样（R256–R259 行为面四轮变化，旧聚类消息可能过期）。
   - **(b) 深项**：customElements 多 registry / :scope query-root / dom/nodes 57F 失败集聚类。
0. **R259 下一步（R258 后，按 ROI）**：
   - **(a) 16,x startOffset 11F（surround 最后一簇）**：`[document.body,4,document.body,5]`——R255 已定位 iframe contentDocument 异步 fetch-rebuild 时序（onload 时序 vs 同步 rebuild），深项：harness onload 链重建文档的同步化评估。
   - **(b) 深项**：customElements 多 registry / :scope query-root / insertNode 套件残余重聚类。
0. **R258 下一步（R257 后，按 ROI）**：
   - **(a) 16,x startOffset 11F**：`[document.body,4,document.body,5]`——R255 已定位 iframe contentDocument 异步 fetch-rebuild 时序（onload 时序 vs 同步 rebuild），深项：harness onload 链重建文档的同步化评估。
   - **(b) 残余簇**：28,0 1F（`[foreignDoc.body,0,foreignTextNode,36]`）/ 30,4 + 30,11 2F（foreignPara1/2 newParent）——重跑聚类按新消息取样（R256/R257 行为面已变）。
   - **(c) 深项**：customElements 多 registry / :scope query-root。
0. **R257 下一步（R256 后，按 ROI）**：
   - **(a) 18,0/19,6 4F**：`[paras[0],0,paras[0],1]` self-surround / `[detachedPara1,0,detachedPara1,1]` 同形态——R256 的 sibling 重接线已改变此形态的行为面，重跑聚类按新消息取样。
   - **(b) 16,x startOffset 11F**：`[document.body,4,document.body,5]`——R255 已定位 iframe contentDocument 异步 fetch-rebuild 时序（onload 时序 vs 同步 rebuild），深项：harness onload 链重建文档的同步化评估。
   - **(c) 残余**：28,0 1F / 30,4+30,11 2F / customElements 多 registry / :scope query-root。

0. **R255 下一步（R254 后，按 ROI）**：
   - **(a) 16,x startOffset 11F**：`[document.body,4,document.body,5]`——harness-iframe index 算术（indexOf 链回溯）。
   - **(b) 17,x 12F**：`[foreignDoc.documentElement,0,…,1]` 元素 newParent 族——R254 摘除修复后重评（部分形态可能与 13/14,x 同源已解，需重跑确认剩余形态）。
   - **(c) 18/19,x 4F**：`[paras[0],0,paras[0],1]` self-surround / detachedPara1 同形态。
   - **(d) 深项**：28,x 1F / 30,x 2F / customElements 多 registry / :scope query-root。
0. **R254 下一步（R253 后，按 ROI）**：
   - **(a) 烘焙点重定位**：in-window wrap + _zwQWrapGen 代际读数探针（surround 各步骤前后 gen 值 + cache size）——候选：removal 循环序列化读取 / _rmSnap 兄弟链 getter / domTests 前置断言烘焙后未失效。
   - **(b) 16,x startOffset 11F**：`[document.body,4,document.body,5]`——harness-iframe index 算术。
   - **(c) 深项**：17,x "[object Object]" / 30,x 2F / 18/19,x 4F / 28,x 1F / customElements 多 registry / :scope query-root。
0. **R253 下一步（R252 后，已执行——负结果轮：clone 循环烘焙假设证伪，实验已回退）**：
   - **(a) 缓存中途烘焙修复**：surround clone 循环 build detached 后一次 append（不中途触发序列化 getter），或收尾统一失效 _zwQWrapCache/序列化源——验证：walk dump 幽灵消失 + 13/14,x 翻绿。
   - **(b) 16,x startOffset 11F**：`[document.body,4,document.body,5]`——harness-iframe index 算术。
   - **(c) 深项**：17,x "[object Object]"（同缓存域候选）/ 30,x 2F / 18/19,x 4F / 28,x 1F / customElements 多 registry / :scope query-root。
0. **R252 下一步（R251 后，已执行——探针轮：wrapperChurn 定性，幽灵 = 缓存中间态快照）**：
   - **(a) 幽灵插入者栈捕获**：in-window wrap testDiv.appendChild/insertBefore（run() 期安装 + Error().stack）——幽灵 gp=DIV 是真实插入，一步定位插入栈（候选：_mo_notify wrapper 重建 / 双调用中间态 / clone 循环意外 append）。
   - **(b) 16,x startOffset 11F**：`[document.body,4,document.body,5]`——harness-iframe index 算术。
   - **(c) 深项**：17,x "[object Object]" / 30,x 2F / 18/19,x 4F / 28,x 1F / customElements 多 registry / :scope query-root。
0. **R251 下一步（R250 后，已执行——探针轮：幽灵是真插入对象（gp=DIV），R252 栈捕获）**：
   - **(a) 幽灵 P|a{HEAD{titleless}} 创建点**：对象 id 标记溯源（setup 期给 factory headEl/克隆 HEAD/paras[0] 打标签，positionTests dump 幽灵携带的标签）——候选：surround clone 循环 kids[0] 误取 factory headEl（_coveredChildren 的 sc.childNodes 视图 iframe-doc 域混取）/ _rmSnap 通知路径包装重建。
   - **(b) 16,x startOffset 11F**：`[document.body,4,document.body,5]`——harness-iframe index 算术。
   - **(c) 深项**：17,x "[object Object]" / 30,x 2F / 18/19,x 4F / 28,x 1F / customElements 多 registry / :scope query-root。
0. **R250 下一步（R249 后，已执行——探针轮：主幽灵 P 已消 + 残余新签名定位）**：
   - **(a) 13/14,x 残余首差重 dump**：own removeChild 生效后（幽灵 P 的 splice 已修）重新 dump 双树首差——新分歧点待定位。
   - **(b) 17,x "[object Object]"**：isEqualNode 对工厂对象的深层比较（first-differing 节点为无 nodeType 对象）。
   - **(c) 深项**：16,x startOffset 11F / 30,x 2F / 18/19,x 4F / 28,x 1F / customElements 多 registry / :scope query-root。
0. **R249 下一步（R248 后，已执行——栈捕获定位幽灵 P 写入者 + own removeChild land，WPT 净 0）**：
   - **(a) 幽灵 P 的 defineProperty 写入者**：grep surround 链（part03/05/06）所有 `defineProperty(...,'parentNode',{value:` 写点——「值变更时旧父 childNodes 未 splice」处加同步摘除（R248 探针实证：surround 期零 removeChild/零 setter 写入而 p0.parentNode 终值 null）。
   - **(b) 17,x "[object Object]" 首差节点同源排查**：isEqualNode 对含幽灵对象的树 walk。
   - **(c) 深项**：16,x startOffset 11F / 30,x 2F / 18/19,x 4F / 28,x 1F / customElements 多 registry / :scope query-root。
0. **R248 下一步（R247 后，已执行——run() 内联探针 12 轮：幽灵 P 定位 + 显式摘除守卫 land，WPT 净 0）**：
   - **(a) 17,x harness 内联探针**：R222 式 win.run() 插桩注入 Range-test-iframe.html 的 run()（range 创建点）——dump sc/newParent 创建时对象形态（factory vs 克隆 vs wrapper 域标签：__zwHandle/_zwOwnerDetDoc/ownerDocument 链）+ domTests 后双树 walk 差分，定位 sim/host 分歧的对象域断点（engine 单测路线已证伪）。
   - **(b) 16,x startOffset 11F**：`[document.body,4,document.body,5]`——harness-iframe index 算术。
   - **(c) 深项**：30,x 2F / 13/14,x 4F / 18/19,x 4F / 28,x 1F / customElements 多 registry / :scope query-root。
0. **R247 下一步（R246 后，已执行——证伪轮：engine 级 17,0/13,0 全通过，「no-op」为 dump 伪影；方法论转 win.run() 内联探针）**：
   - **(a) 17,x iframe-window factory docEl no-op 断点**：host surround 在 iframe-window factory docEl 容器整体 no-op（deA=[HEAD,BODY] P 缺失；顶层 implementation factory 形态 R245 单测已通）——对两条 factory 路径做同款 dump 差分定位 `_coveredChildren`/`_r237` insertBefore 链断点。
   - **(b) 13/14,x newParent 移动本体语义**：host 克隆/引用错位使 newParent 原件留 testDiv（spec selectNode 要求移动本体后 range 指本体父）。
   - **(c) 深项**：16,x startOffset 11F / 30,x 2F / 18/19,x 4F / 28,x 1F / customElements 多 registry / :scope query-root。
0. **R246 下一步（R245 后，已执行——探针轮：17,x/13–14,x 三发现，修复切片记 R247）**：
   - **(a) 17,x sim 树等价 12F**：sim ret:ok 后 assertNodesEqual walk 的 expected 树含无 nodeType 对象（"[object Object]"）——探针 dump mySurroundContents 成功后 expectedRoots 首差节点（HEAD 移入 newParent 后的子树形态 vs host clone 形态）；17,4/11/13 现为树 walk TypeError（.length undefined）。
   - **(b) 16,x startOffset 11F**：`[document.body,4,document.body,5]`——harness-iframe index 算术。
   - **(c) 深项**：30,x 残余 2F / 13/14,x 残余 4F / 18/19,x 残余 4F / 28,x 1F / customElements 多 registry / :scope query-root。
0. **R245 下一步（R244 后，已执行——引擎 move 语义修复 land，WPT 净 0）**：
   - **(a) 17,x differing 12F**：`[foreignDoc.documentElement,0,…,1]` × 元素 newParent——"First differing node: expected object "[object Object]", got Comment node"（sim 侧 foreignDoc 深入 walk 产物含无 nodeType 对象——R243 cDP 解锁后暴露的 host/sim 深分歧，探针 dump sim expectedRoots 的首差节点形态）。
   - **(b) 16,x startOffset 11F**：`[document.body,4,document.body,5]`——harness-iframe index 算术（indexOf 链回溯）。
   - **(c) 深项**：30,x 残余 2F / 13/14,x 残余 4F / 18/19,x 残余 4F / 28,x 1F / customElements 多 registry / :scope query-root。
0. **R244 下一步（R243 后，已执行——25/26,x 12F 全解 +12P 纯增）**：
   - **(a) 25/26,x Document 容器 HRE**（12F）：`[document,0,document,1/2]`——surround 对 doc 级容器的 insertNode 位序校验（spec doc 级 pre-insertion 的 doctype/element 位序规则）。
   - **(b) 17,x position 9F**：cDP 解锁后暴露的 host/sim 深分歧（expected "[object Object]" 形态——sim walk 产物含无 nodeType 对象）。
   - **(c) 深项**：startOffset 16,x 11F（harness-iframe index 算术）/ 30,x 残余 2F / 18/19,x 残余 4F / customElements 多 registry / :scope query-root。
0. **R243 下一步（R242 后，按 ROI）**：
   - **(a) cDP 40 重评**：R235 负结果（-28）复测——给 detached-doc docEl/head/body 附 cDP own-prop 的实验在 R236–R242 十一个新 surround/extract 分支后重跑，翻转面可能已消失。
   - **(b) 25/26,x Document 容器 HRE**（12F）：`[document,0,…]` doc 级位序校验。
   - **(c) 深项**：startOffset 16,x 11F（harness-iframe index 算术）/ differing 13/14,x 4F / assert_unreached 18/19,x 残余 / customElements 多 registry / :scope query-root。
0. **R242 下一步（R241 后，按 ROI）**：
   - **(a) 24,x assert_unreached 32F**：`[testDiv,2,paras[4],1]` 与 28,x 同族（wrapper 域）但 so=2 且 ec 是元素内 text——祖先分支二级后代形态扩展（ec.parentNode.parentNode === sc）+ contained-move 复用。
   - **(b) 25/26,x Document 容器 HRE**（12F）：`[document,0,…]` doc 级位序校验。
   - **(c) 深项**：cDP 40（17/30,x 绑 foreignDoc surround 全序）/ startOffset 16,x 11F（harness-iframe index 算术）/ differing 残余 6F / customElements 多 registry / :scope query-root。
0. **R241 下一步（R240 后，按 ROI）**：
   - **(a) 28,x generic cross-container surround 全序**：R236 surround 分支接通新 contained-move extract + walk 深层形态对齐（28,0 已推进到 isEqualNode 深层）。
   - **(b) 25/26,x Document 容器 HRE**（12F）：`[document,0,…]` 的 doc 级位序校验。
   - **(c) 深项**：**克隆树 wrapper-domain 挂链**（R240 实证 querySelector("#test") 返 wrapper 域使 setupRangeTests append 落 wrapper 列表、克隆树 sibling 断链——assert_unreached 24/x 32F + 28,x 共同根因候选）/ cDP 40 / startOffset 16,x 11F。
0. **R240 下一步（R239 后，按 ROI）**：
   - **(a) 28,x differing**（28F）：`[foreignDoc.body,0,foreignTextNode,36]`——ec 是 body 直接 CD 子但 foreignDoc.body 方法面/祖先分支对 foreignDoc 形态的适配（R236 分支重评）。
   - **(b) 25/26,x HRE**（12F）：`[document,0,document,1/2]` Document 容器 surround 全序（insertNode 的 doc 级位序校验）。
   - **(c) 深项**：cDP 40（17/30,x 绑 foreignDoc surround 全序）/ 24,x 跨子区间 32F（generic cross-container extract）/ startOffset 16,x 11F / customElements 多 registry / :scope query-root。
0. **R239 下一步（R238 后，按 ROI）**：
   - **(a) INVALID_STATE 30**（20–22,x 各 6 + 29/31,x 各 6）：跨 text 边界部分包含检查（`[paras[0].firstChild,0,paras[1].firstChild,0]`）——host surround 入口 partial-check 对多容器形态漏判，需对齐 sim isPartiallyContained 遍历。
   - **(b) HRE 24,x**（11F+16 unreached）：跨子区间 `[testDiv,2,paras[4],1]` leaf-newParent 的 extract 全序（generic cross-container）。
   - **(c) 深项**：cDP 40（17/30,x 绑 foreignDoc surround 全序）/ differing 28,x 17F / customElements 多 registry / :scope query-root。
0. **R238 下一步（R237 后，按 ROI）**：
   - **(a) HRE ~36 重评**：R236/R237 全序落地后 host 行为面已变——重跑聚类按新消息取样（旧归因「sim 全序残余」可能部分失效）。
   - **(b) 19,x detached differing**（~28F）：`[detachedPara1,0,detachedPara1,1]` 族。
   - **(c) 深项**：assert_unreached ~55 克隆子树内部残留 / 17,x/30,x cDP 绑 host foreignDoc surround 全序 / INVALID_STATE 30 / customElements 多 registry / :scope query-root。
0. **R237 下一步（R236 后，按 ROI）**：
   - **(a) 12–14,x 残余 differing**（~36F）：R234 动态 getter 后 host 对 docEl 容器 extract 的首差形态（expected `<p id="a"><head>…` vs got object）——疑 clone+remove 记账与 sim move 语义在 docEl 容器上的残余。
   - **(b) 19,x detached differing**（~28F）：`[detachedPara1,0,detachedPara1,1]` 族。
   - **(c) 深项**：assert_unreached 63 克隆子树内部残留 / 17,x/30,x cDP 绑 host surround 全序 / HRE 37 / INVALID_STATE 30 重聚类 / customElements 多 registry / :scope query-root。
0. **R236 下一步（R235 后，按 ROI）**：
   - **(a) 23,x node-与-后代区间**（~32F）：`[paras[0],0,paras[0].firstChild,7]`——host `_coveredChildren` 对 sc≠ec 恒 null 使 extract 空转；spec 需 end 在 text 内时 splitText + 部分移动（sim 的 myExtractContents 全序）。
   - **(b) 12–14,x 残余 differing**（~36F）：R234 动态 getter 后 host 真实提取的树形态与 sim 首差节点（expected `<p id="a"><head>...` 形态 vs got object）。
   - **(c) 深项**：assert_unreached 63 的克隆子树内部残留 / 17,x/30,x cDP 绑 host surround 全序（R235 负结果）/ HRE 37 / INVALID_STATE 30 重聚类 / customElements 多 registry / :scope query-root。
0. **R235 下一步（R234 后，按 ROI）**：
   - **(a) foreignDoc cDP 残余 39F**（17,x/30,x）：R234 解了 12–14,x 主簇（69F）后同根残余——foreignDoc = createHTMLDocument 路径（documentElement 走 R130 惰性 getter 理论已动态），探针确认 17,x nodeB 形态后对症。
   - **(b) surround 419F 重聚类**：HRE 37 / INVALID_STATE 30 / assert_unreached 133 在 cDP 簇部分瓦解后的独立性确认（R233 三簇同根假设已修正）。
   - **(c) 深项**：assert_unreached 133 的克隆子树内部节点级残留定位 / customElements 多 registry / :scope query-root / lone-surrogate wire / MO-document parser 记录 / insertBefore 自旋。
0. **R234 下一步（R233 后，按 ROI）**：
   - **(a) fresh-doc 深项本体**：restoreIframe 残留形态 dump——每轮 doc 首末子清理循环后 head 内残留是什么（跨轮累积的具体节点）；这是解锁 assert_unreached 133 + cDP 108 + HRE 37（~250F 三簇同根）的钥匙。
   - **(b) startOffset 11F**（16,x）：harness-iframe index 算术——独立小簇，可轻量。
   - **(c) 深项**：customElements 多 registry / :scope query-root / lone-surrogate wire / MO-document parser 记录 / insertBefore 自旋。
0. **R233 下一步（R232 后，已执行——本轮收口，负结果轮无代码 land）**：
   - **(a) mySurroundContents 全源注入探针**（R231 方法论复用）：对 24,x/18,0/16,x 形态 dump sim 的 partial-check 终止点与 insertNode validity 触发位——HRE 37F + INVALID_STATE 30F + startOffset 11F 的共同根因层。
   - **(b) assert_unreached 133**：fresh-doc 深项入口评估（restoreIframe 残留形态 dump）。
   - **(c) 深项**：fresh-doc 残余 / customElements 多 registry / :scope query-root / lone-surrogate wire / MO-document parser 记录 / insertBefore 自旋。
0. **R232 下一步（R231 后，已执行——本轮收口，负结果轮无代码 land）**：
   - **(a) surround 剩 455F 重聚类**：differing ~170 优先（沙箱直跑 sim 探针法可复用——注入 mySurroundContents 全源对照）；HRE+INVALID_STATE ~67；assert_unreached ~110 / cDP 108 绑 fresh-doc 深项。
   - **(b) 深项**：fresh-doc 残余（解锁 R219 开关）/ customElements 多 registry / :scope query-root / lone-surrogate wire / MO-document parser 记录 / insertBefore 自旋。
0. **R231 下一步（R230 后，已执行——本轮收口）**：
   - **(a) endOffset expected 8/9 got 2 簇（~93F）**：2,x/27,x 等——sim 的 myExtractContents 对工厂 text 的塌缩行为与 spec 不同（疑 extract 中途 catch 使 range 未动即 HRE）。逐行复刻 sim 的 early-return 形态（探针 dump sim 中间态）。
   - **(b) HRE+INVALID_STATE 残余 67F** / assert_unreached 133 / cDP 108（fresh-doc 深项）。
   - **(c) 深项**：fresh-doc 残余 / customElements 多 registry / :scope query-root / lone-surrogate wire / MO-document parser 记录 / insertBefore 自旋。
0. **R230 下一步（R229 后，已执行——本轮收口）**：
   - **(a) surround 剩 650F 重聚类**：Text differing ~180（extract 边界精度——head/tail 切片的偏移计算）优先；assert_unreached ~253 / cDP 108 绑 fresh-doc 深项。
   - **(b) 深项**：fresh-doc 残余（解锁 R219 开关）/ customElements 多 registry / :scope query-root / lone-surrogate wire / MO-document parser 记录 / insertBefore 自旋。
0. **R229 下一步（R228 后，已执行——本轮收口）**：
   - **(a) surround 剩 897F 重聚类**：assert_unreached 282 / cDP 108（fresh-doc 深项解锁 R219 开关）/ HRE ~43 + INVALID_STATE 30 残余（sim 全序复刻剩余步骤：clear newParent 序位 / appendChild(frag) / selectNode 同步）。
   - **(b) 深项**：fresh-doc 残余 / customElements 多 registry / :scope query-root / lone-surrogate wire / MO-document parser 记录 / insertBefore 自旋。
0. **R228 下一步（R227 后，已执行——本轮收口）**：
   - **(a) comment/PI 区间 surround**（~84F）：`[detachedComment,3,4]` 族——host 的 R212 CharData 路径 `_r212isCd2` 已含 7/8 但 extractContents 的 R211 分支只走 Text/CDATA（3/4），comment/PI 部分选中不 split（"Stuwxyz" vs "Stuvwxyz" 族）。
   - **(b) surround 的 sim 全序复刻**（HRE 93 + INVALID_STATE 30 + startOffset ~30）：CharData 路径按 mySurroundContents 序——extract 塌缩 → clear newParent → insertNode（含完整 pre-insertion validity，parent_ = 塌缩后容器）→ appendChild(frag) → selectNode 同步。
   - **(c) fresh-doc 残余深项**（解锁 R219 开关 108F + assert_unreached 282F）。
0. **R227 下一步（R226 后，已执行——本轮收口，负结果轮无代码 land）**：
   - **(a) surround ~350F 重聚类**（893P 基线）——insertNode 整文件 100% 后 ranges 族最大剩余簇（947F）。
   - **(b) 深项**：customElements 多 registry / :scope query-root / lone-surrogate wire / MO-document parser 记录 / insertBefore 自旋。
0. **R226 下一步（R225 后，已执行——本轮收口）**：
   - **(a) 30,4 + 28,0 成对**：insertNode 的「node 是 referenceNode 时 ref 前移 nextSibling」步骤须与 sel 容器 proxy trap（part04 3723 insertBefore）的 detach/advance 语义连贯实现（R225 单独实现实测净 0：30,4 +2 / 28,0 -2）。
   - **(b) surround ~350F 重聚类**（893P 基线）。
   - **(c) 深项**：customElements 多 registry / :scope query-root / lone-surrogate wire / MO-document parser 记录 / insertBefore 自旋。
0. **R225 下一步（R224 后，已执行——本轮收口）**：
   - **(a) insertNode 剩 23F**：25/26/29/31,x:16/18（document/foreignDoc/xmlDoc 容器 × PI/comment node 的 doc 级子位序，resulting DOM + position 双断言）+ 30,4（foreignDoc.body 容器 insert foreignPara1）+ 0/4/8/10/15,20（docfrag 作 node 的 resulting range position 5F）。
   - **(b) surround ~350F 重聚类**（893P 基线）。
   - **(c) 深项**：customElements 多 registry / :scope query-root / lone-surrogate wire / MO-document parser 记录 / insertBefore 自旋。
0. **R224 下一步（R223 后，已执行——本轮收口）**：
   - **(a) insertNode 剩 148F**：HRE ~70（foreignDoc/xmlDoc/document 作 node 的跨容器族——host 与 sim 抛/不抛对齐）+ text-differ/assert_unreached ~78 散布。
   - **(b) surround ~350F 重聚类**（893P 基线）。
   - **(c) 深项**：customElements 多 registry / :scope query-root / lone-surrogate wire / MO-document parser 记录 / insertBefore 自旋。
0. **R223 下一步（R222 后，已执行——本轮收口）**：
   - **(a) row 29 foreignDoc.childNodes 空**（22F）：A/B 实证预存（与 R222 无关）——setup 后 foreignDoc 的 doc 级子列表为空，定位 iframe realm 内 implementation.createHTMLDocument 返回形态（疑走主 document.implementation 单例 + appendChild 落空）。
   - **(b) HRE 73 跨容器族**（foreignDoc/xmlDoc/document 作 node——host 与 sim 的抛/不抛对齐）。
   - **(c) surround ~350F 重聚类**（893P 基线）。
   - **(d) 深项**：customElements 多 registry / :scope query-root / lone-surrogate wire / MO-document parser 记录 / insertBefore 自旋。
0. **R222 下一步（R221 后，已执行——本轮收口）**：
   - **(a) insertNode 剩 204F 重聚类**（1637P 基线：rows 25/26/29 跨容器族（[document,0,document,1..2]/[foreignDoc,1,foreignComment,2]，22F×3）+ HRE 69 + null-nodeType 66——fresh-doc 后的真实语义残余，非残留态）。
   - **(b) surround 452F 重聚类**（865P 基线——fresh-doc 后新形态面；三方法 kill-switch 的泄漏缝隙同场定位）。
   - **(c) 深项**：customElements 多 registry / :scope query-root / lone-surrogate wire / MO-document parser 记录 / insertBefore 自旋。
0. **R221 下一步（R220 后，已执行——本轮收口）**：
   - **(a) fresh-doc 深项实施**：iframe contentDocument 每轮整体重建（R220 实证的结构根因：restoreIframe 克隆空壳 + document.head/body 平行树）。方案：`__zw_fetch`/contentDocument getter 对 `Range-test-iframe.html` 类重载返回新 `_zwMakeIframeDoc`（docEl/head/body 一体克隆 referenceDoc 内容）——落地后 rows 16/18/20–28 跨轮残留簇（~350F）+ surround -19 + 三方法 kill-switch 三项同时回收。
   - **(b) HRE 86F 跨容器簇**（fresh-doc 后重新聚类——当前混入残留形态）。
   - **(c) 深项**：customElements 多 registry / :scope query-root / lone-surrogate wire / MO-document parser 记录 / insertBefore 自旋。
0. **R220 下一步（R219 后，已执行——本轮收口）**：
   - **(a) insertNode ~740F 重聚类**（1094P 新基线：assert_unreached 516「DOMs were not equal」/ HRE 86 / null-nodeType 66 三簇——上一轮防递归解锁 sim 后的新形态面）。
   - **(b) iframe contentDocument 每轮 fresh-doc 深项评估**（R208 家族）：surround -19 与三方法 kill-switch 的共同根因（restoreIframe 只清 doc 首末子，sim 深入后的 head 内残留跨轮累积）——落地后 surround 回收 + 三方法启用（预计 +300P 量级）。
   - **(c) 深项**：customElements 多 registry / :scope query-root / lone-surrogate wire / MO-document parser 记录 / insertBefore 自旋。
0. **R219 下一步（R218 后，已执行——本轮收口）**：
   - **(a) 12,x Maximum call stack 72F 页面上下文插桩**（三种探针形态全 clean——溢出在 testharness 页面自身 JS 累积态；在页面脚本流内 dump 前 12 帧调用链）。
   - **(b) insertNode HRE 86F 残余**（952P 基线重新聚类——Document 规则覆盖后的剩余形态）。
   - **(c) 深项**：customElements 多 registry / :scope query-root / lone-surrogate wire / MO-document parser 记录 / insertBefore 自旋。
0. **R218 下一步（R217 后，已执行——本轮收口）**：
   - **(a) 12,x Maximum call stack 72F**（docEl 入树后 upwalk 链已通——新溢出点定位，疑 clone 链或 restoreIframe 内循环）。
   - **(b) insertNode 剩余 ~925F 重新聚类**（HRE 残余 / is-not-a-function / null 族在 916P 新基线下的分布）。
   - **(c) 深项**：customElements 多 registry / :scope query-root / lone-surrogate wire / MO-document parser 记录 / insertBefore 自旋。
0. **R217 下一步（R216 后，已执行——本轮收口）**：
   - **(a) 25,x DOM 比较层**（insertNode into Document 的子位置规则——element 与既有 element 子冲突 / doctype 位序；R216 只解 setup 层，DOM 断言层新暴露）。
   - **(b) 12,x Maximum call stack 72F**（docEl 入树后 upwalk 已通——定位新溢出点，疑 clone 链或 restoreIframe 循环内）。
   - **(c) insertNode HRE 94F 残余 + null-nodeType 残余**（902P 后新形态聚类）。
   - **(d) 深项**：customElements 多 registry / :scope query-root / lone-surrogate wire / MO-document parser 记录 / insertBefore 自旋。
0. **R216 下一步（R215 后，已执行——本轮收口）**：
   - **(a) insertNode 剩余 ~311F 聚类再扫描**（628→902 后的新基线——ensurePreInsertion 的 Document 子位置规则（frag 多 element 子 / element 与既有 element 子 / doctype 位序）与 NotFoundError 族）。
   - **(b) deleteContents 56P 与 R213 58 的 -2 复查**（R215 校验的连带形态）。
   - **(c) surround 剩余聚类**（865P 后新基线重聚类：INVALID_STATE / HRE / insertBefore 残余）。
   - **(d) 深项**：customElements 多 registry / :scope query-root / lone-surrogate wire / MO-document parser 记录 / insertBefore 自旋。
0. **R215 下一步（R214 后，已执行——本轮收口）**：
   - **(a) insertNode restoreIframe 累积循环定位**（Maximum call stack——探针 2 轮正常、~290 轮累积溢出；疑 R191 adoptAll 递归或 clone 链累积引用——循环复刻探针逐轮增长观察）。
   - **(b) deleteContents 8 个 P2F 形态复查**（range 18/24/27/40/41/46 元素容器形态——docEl 修复后 setup 路径变化的重分布，逐个比对 sim vs host）。
   - **(c) insertNode HRE 336F 簇**（ensurePreInsertionValidity 缺失——spec `dom-node-pre-insert` 的完整校验族）。
   - **(d) 深项**：customElements 多 registry / :scope query-root / lone-surrogate wire / MO-document parser 记录 / insertBefore 自旋。
0. **R214 下一步（R213 后，已执行——本轮收口）**：
   - **(a) insertNode 文件残余聚类**（628P/~162F——extract/surround/delete CharData 链已闭环，insertNode 的独立缺口待聚类扫描）。
   - **(b) surround 剩余聚类**：HRE 81F / INVALID_NODE_TYPE 27F / INVALID_STATE 25F 残余 + parent_.insertBefore 17F（R211-R213 修后需重新聚类）。
   - **(c) head/docEl/body 三站点方法面单独评估**（R210 实测 -9 非纯增）。
   - **(d) 深项**：customElements 多 registry / :scope query-root / lone-surrogate wire / MO-document parser 记录 / insertBefore 自旋。
0. **R213 下一步（R212 后，已执行——本轮收口）**：
   - **(a) 6,x positionTests 残余定位**（R212 探针双 iframe 手工模拟与 host 树一致，但真实 testharness 的 sim 结果与手工模拟有差异——疑 common.js myExtractContents 的 newOffset 计算或插入位；testharness 环境插桩 dump 预期树）。
   - **(b) deleteContents 的 CharData 区间分支**（extract 已有——delete 同款三段算法的删侧，当前 45P）。
   - **(c) head/docEl/body 三站点方法面单独评估**（R210 实测 -9 非纯增）。
   - **(d) surround 剩余聚类**：HRE 81F / INVALID_NODE_TYPE 27F / INVALID_STATE 25F 残余 + parent_.insertBefore 17F。
   - **(e) 深项**：customElements 多 registry / :scope query-root / lone-surrogate wire / MO-document parser 记录 / insertBefore 自旋。
0. **R212 下一步（R211 后，已执行——本轮收口）**：
   - **(a) surroundContents 的 CharData 路径 + CDATA cloneNode 成对 land**（R211 回退切片——先定位首版两缺陷：docfrag newParent（nt=11 非叶子集 3/4/7/8）为何被 leaf-throw 误伤（疑 kids===null 时的分支序）+ frag appendChild 末子丢失；修好后与 CDATA cloneNode（nt=4 分支代码已验证）成对 land，6,x 族 34F + extractContents 再 +4）。
   - **(b) head/docEl/body 三站点方法面单独评估**（R210 实测 -9 非纯增）。
   - **(c) deleteContents 的 CharData 区间分支**（extract 已有——delete 同款三段算法的删侧，当前 45P）。
   - **(d) surround 剩余聚类**：HRE 81F / INVALID_NODE_TYPE 27F / INVALID_STATE 25F 残余 + parent_.insertBefore 17F。
   - **(e) 深项**：customElements 多 registry / :scope query-root / lone-surrogate wire / MO-document parser 记录 / insertBefore 自旋。
0. **R211 下一步（R210 后，已执行——本轮收口）**：
   - **(a) CDATA 跨容器 extract/insert + CDATA cloneNode 成对 land**（R210 评估回退的切片——cloneNode nt=4 分支代码已验证单独正确，配 host surroundContents/extractContents 的 CDATA 容器区间语义（6,x 族 34F + 相邻簇））。
   - **(b) 非折叠文本 range 的 host extract/delete 语义**（1,x positionTests——sim 侧已跑通，host 侧 `_coveredChildren` 对文本容器返 null defer）。
   - **(c) head/docEl/body 三站点方法面单独评估**（R210 实测 -9 非纯增——补方法面改变行走路径，需先定位哪条路径被切换）。
   - **(d) surround 剩余聚类**：HRE 81F / INVALID_NODE_TYPE 27F / INVALID_STATE 25F 残余 + parent_.insertBefore 17F。
   - **(e) 深项**：customElements 多 registry / :scope query-root / lone-surrogate wire / MO-document parser 记录 / insertBefore 自旋。
0. **R210 下一步（R209 后，已执行——本轮收口）**：
   - **(a) 非折叠文本 range 的 extract/insert 语义**（R209 暴露的 1,x positionTests 簇——sim 侧 myExtractContents 已跑通、host 侧缺文本区间 extract/delete；surround 剩 1107F 的最大簇）。
   - **(b) restoreIframe r1+ 剩余聚类**：`Cannot set properties of null (setting 'data')`（34F）+ `newParent.appendChild is not a function`（20F——detached doc 域 docfrag newParent 路径）+ docEl `cloneNode(true)` 空 childNodes（restoreIframe 的 referenceDoc 重建链——R209 探针实证 clone kids=0）。
   - **(c) Range-set 剩 1369F / comparePoint 124F 残余**。
   - **(d) 深项**：customElements 多 registry / :scope query-root / lone-surrogate wire / MO-document parser 记录 / insertBefore 自旋。
0. **R209 下一步（R208 后，已执行——本轮收口）**：
   - **(a) restoreIframe r1+ 轮已跑通至断言层的新聚类**（R208 解锁，按簇大小）：
     `node2.compareDocumentPosition is not a function`（surround 230F + insert 44F——common.js testNodes 的 eval 产物节点 doctype/PI/foreign 域缺方法面）；`node.hasChildNodes is not a function`（surround 138F——同域节点形态）；`assert_throws_dom HIERARCHY_REQUEST_ERR must be thrown`（insert 396F——insertNode/surroundContents 异常路径语义）；`range.startContainer.splitText is not a function`（insert 242F——Text 方法面在 eval 产物形态不挂）；clone/extract/delete 的 `Cannot read properties of undefined (reading '0')`（95F）+ `isEqualNode is not a function`（~79F——cloneContents 返 fragment 的子节点为轻量对象缺方法，与 R207 docEl 可变面同族）。
   - **(b) Range-set 剩 1369F / comparePoint 124F 残余**。
   - **(c) 深项**：customElements 多 registry / :scope query-root / lone-surrogate wire / MO-document parser 记录 / insertBefore 自旋。
0. **R208 下一步（R207 后，已执行——本轮收口）**：
   - **(a) iframe doc 查询的 live-tree 同步**（R207 精确定位）：restoreIframe 第二轮起 `querySelector('#test')` 服务 stale markup parse 树而 live 组装树已前进——评估 `_makeDetachedDocument` 查询读 live docEl 子树的最小切片（R159/R170 doc-query 架构域）。打通后 surroundContents/insertNode 3680F 的 r1+ 轮次开始转化。
   - **(b) Range-set 剩 1369F / comparePoint 124F / isPointInRange 34F 残余**。
   - **(c) 深项**：customElements 多 registry / :scope query-root / lone-surrogate wire / MO-document parser 记录 / insertBefore 自旋。

0. **R207 下一步（R206 后，已执行——本轮收口）**：
   - **(a) iframe 子文档下一层**：restoreIframe 的 contentDocument 失同步（`Cannot read null removeChild` ×919——移除全部子后 contentDocument 置 null/缓存失效）+ detached doc 元素的 append/cloneNode 面（`paras[5].append is not a function`——R206 通道暴露）+ `testRange undefined`（run() 的 eval/location.hash 路径）。三件打通后 surroundContents/insertNode 3680F 才开始实质转化。
   - **(b) Range-set 剩 1369F / comparePoint 124F / isPointInRange 34F 残余**。
   - **(c) 深项**：customElements 多 registry / :scope query-root / lone-surrogate wire / MO-document parser 记录 / insertBefore 自旋。

0. **R206 下一步（R205 后，已执行——本轮收口）**：
   - **(a) surroundContents/insertNode 各 1840F**：`iframe.contentWindow.setupRangeTests is not a function` ×920 + `typeof Range undefined` ×920——用例经 srcdoc iframe 子文档（restoreIframe + contentWindow 上的 common.js 面缺）。iframe 子文档域（R159/R177 家族），评估 srcdoc iframe contentWindow 的脚本求值通道轻量性。
   - **(b) isPointInRange 34F + comparePoint 124F 残余**：`nodeB.compareDocumentPosition is not a function`（部分节点形态无 cDP 方法）+ 混合形态。
   - **(c) Range-set 剩 1369F / mutations 族残余 / MutationObserver-document 3F**。
   - **(d) 深项**：customElements 多 registry / :scope query-root / lone-surrogate wire / MO-document parser 记录 / insertBefore 自旋。

0. **R205 下一步（R204 后，已执行——本轮收口）**：
   - **(a) 剩余 7489F 聚类**——compareBoundaryPoints 剩 601F 形态 + MutationObserver-document 3F + escapes 孤立代理 2F + 全量 fail Top 重聚类（identity 域残余 + foreign 递归残余已大幅收缩，重新扫描定下一簇）。
   - **(b) foreign-tree Maximum-call-stack 残余**：Range-selectNode 整文件死于此（R204 修了 createRange 递归后部分转绿，selectNode 等剩余形态再探）。
   - **(c) 深项**：customElements 多 registry / :scope query-root / lone-surrogate wire / MO-document parser 记录 / insertBefore 自旋。

0. **R204 下一步（R203 后，已执行——本轮收口）**：
   - **(a) compareBoundaryPoints 9313F**：两形态——`how` 参数 WebIDL enum 转换（非 0-3 抛 NotSupportedError，5272F）+ "Creating context/argument range threw"（common.js rangeFromEndpoints 在部分节点形态上异常，4041F）。前者是纯 WebIDL 校验轻件；后者接 rangeFromEndpoints 异常定位。
   - **(b) foreign-tree Maximum-call-stack ~1009**：foreignDoc 树上 range 操作递归（Range-selectNode 整文件死于此；R202 探针在部分 foreign 形态复现）。
   - **(c) 深项**：customElements 多 registry / :scope query-root / lone-surrogate wire / MO-document parser 记录 / insertBefore 自旋。

0. **R203 下一步（R202 后，已执行——本轮收口）**：
   - **(a) identity 域主簇**（24kF 的主阻塞，R202 前置能力面已就位）：Range-set 6767F / Range-compareBoundaryPoints 9313F 的 `expected object "[object Object]" but got "__n2"`——eval 解析的 var 对象 vs Range 持有 wrapper 的 identity 分歧（L2 统一域家族，mega-scale driving 集就绪）。评估 wrapper 归一登记表切片。
   - **(b) insertBefore 自旋定位**：中段 mutation 后 childNodes 视图失同步（indexOf while 死循环）——R51b 三根因同族，watchdog 已防阻塞。
   - **(c) Text-wholeText 相邻性**：wholeText 收全部 Text 子而非逻辑相邻段（`expected ab got abc`，既存独立缺口）。
   - **(d) 深项**：customElements 多 registry / :scope query-root / lone-surrogate wire / MO-document parser 记录。

0. **R202 下一步（R201 后，已执行——本轮收口）**：
   - **(a) identity 域主簇**（新增 24kF 的两大簇之一）：Range-set 6767F / Range-compareBoundaryPoints 9313F 的 `expected object "[object Object]" but got "__n2"` 形态——eval 解析的 var 对象（setupRangeTests 产物）vs Range 持有的 wrapper（另一查询路径产物）identity 分歧。属 L2 统一域家族（R171 评估定格），现有了 mega-scale 的 driving 集。评估统一方向切片（wrapper 归一登记表的 Range 持有面接入）。
   - **(b) foreign-doc CharacterData 方法面**（另一大簇）：foreignDoc.createTextNode 产物无 insertData 等五方法（探针实证 "insertData is not a function"）——对照 R48 主文档 SetChildText 全链补 foreign-doc 面。
   - **(c) insertBefore 自旋定位**：中段 mutation 后 childNodes 视图失同步（indexOf while 死循环）——watchdog 已防阻塞，根因待挖（R51b 三根因同族）。
   - **(d) 深项**：customElements 多 registry / :scope query-root / lone-surrogate wire / MO-document parser 记录（live parse stream）。

0. **R201 下一步（R200 后，已执行——本轮收口）**：
   - **(a) 100F 簇再扫描**——Range-in-shadow-after-shadow-removed 2F / Node-constants 2F / Element-remove 2F / Document-constructor 2F / Event-dispatch-redispatch 2F / Range-mutations 族 12F（"xxxTests is not defined"——Range-mutations.js 内联形态排查），逐簇评估轻量性。
   - **(b) M2/M6 面**：S6 高层 API 去字符串 / native dom_bindings 补齐。
   - **(c) 深项再评估**：MO-document parser 记录（live parse stream，html-compat 域）/ MO-inner-outer identity（L2 统一域）/ customElements 多 registry / :scope query-root / lone-surrogate wire。

0. **R200 下一步（R199 后，已执行——本轮收口）**：
   - **(a) 102F 簇再扫描**——MutationObserver-document 3F（parser insertion 记录）/ MutationObserver-inner-outer 2F / Range-in-shadow-after-shadow-removed 2F / ParentNode-querySelector-case-insensitive 2F / Node-constants 2F / Element-remove 2F / Document-constructor 2F / Event-dispatch-redispatch 2F，逐簇评估轻量性。
   - **(b) M2/M6 面**：S6 高层 API 去字符串 / native dom_bindings 补齐。
   - **(c) 深项再评估**：customElements 多 registry（per-doc registry 表）/ :scope query-root（scope NodeId 贯穿 matches 链，R199 记档）/ lone-surrogate wire（超出 HTML 序列化域，R199 记档）。

0. **R199 下一步（R198 后，已执行——本轮收口）**：
   - **(a) 104F 簇再扫描**——ParentNode-querySelector-escapes 4F（`#\d83d` 代理对形态 + `#\0` NUL id 逃逸）/ MutationObserver-document 3F（parser insertion 记录）/ Range-in-shadow-after-shadow-removed 2F / ParentNode-querySelector-scope 2F（:scope 伪类）/ Document-constructor 2F（metadata/URL-parsing），逐簇评估轻量性。
   - **(b) M2/M6 面**：S6 高层 API 去字符串 / native dom_bindings 补齐。
   - **(c) 深项再评估**：customElements 多 registry（node-realm 域正解——per-doc registry 表）。

0. **R198 下一步（R197 后，已执行——本轮收口）**：
   - **(a) 111F 簇再扫描**——ParentNode-querySelector-escapes 4F / MutationObserver-inner-outer 3F / MutationObserver-document 3F / Document-constructor 2F（metadata/URL-parsing——`document.createElement` 应是主文档 Element 而非 Object）/ Range-in-shadow-after-shadow-removed 2F 等，逐簇评估轻量性。
   - **(b) M2/M6 面**：S6 高层 API 去字符串 / native dom_bindings 补齐。
   - **(c) 深项再评估**：customElements 多 registry（node-realm 域正解——per-doc registry 表）。

0. **R197 下一步（R196 后，已执行——本轮收口）**：
   - **(a) 115F 簇再扫描**——Range-stringifier 2F + DocumentFragment-constructor 2F 双簇收口（本轮全 100%，净 -4F）；Document-constructor 2F / MutationObserver 6F / ParentNode-escapes 4F 剩余。
   - **(b) M2/M6 面**：S6 高层 API 去字符串 / native dom_bindings 补齐。
   - **(c) 深项再评估**：customElements 多 registry（node-realm 域正解——per-doc registry 表）。

0. **R185 下一步（R184 后，按 ROI）**：
   - **(a) 全量 fail Top 簇**——EventListener-handleEvent-cross-realm 5F / ParentNode-querySelector-escapes 4F / Node-cloneNode-svg 4F / getElementsByClassName-empty-set 3F / Node-isSameNode 3F（逐簇评估轻量性）。
   - **(b) 深项清单**——node-realm 剩 3F + Range-adopt #2（cross-realm/adopt 域）+ normalize #4（DOMParser 可变文档面）。
   - **(c) M2/M6 面**：S6 高层 API 去字符串 / native dom_bindings 补齐。

0. **R184 下一步（R183 后，按 ROI）**：
   - **(a) 全量 fail Top 簇**——EventListener-handleEvent-cross-realm 5F / ParentNode-querySelector-escapes 4F / Node-textContent 4F / Node-normalize 4F / Node-constants 4F（逐簇评估轻量性）。
   - **(b) node-realm 剩 3F + Range-adopt #2 深项**——cross-realm/adopt 基建域同族记档。
   - **(c) M2/M6 面**：S6 高层 API 去字符串 / native dom_bindings 补齐。

0. **R183 下一步（R182 后，按 ROI）**：
   - **(a) 全量 fail Top 簇**——EventListener-handleEvent-cross-realm 5F / Range-commonAncestorContainer-2 + Range-adopt-test 8F / ParentNode-querySelector-escapes 4F / Node-textContent 4F（逐簇评估轻量性）。
   - **(b) 跨流信号**——zero-webview SW 测试 repeated_registration_changes flake（clean main 6 跑 5 败，20s 超时形态）归 service-workers 流，js-dom 侧不硬解（run-rules §10）。
   - **(c) M2/M6 面**：S6 高层 API 去字符串 / native dom_bindings 补齐。

0. **R182 下一步（R181 后，按 ROI）**：
   - **(a) 全量 fail Top 簇**——Element-insertAdjacentElement 5F / EventListener-handleEvent-cross-realm 5F / Range-commonAncestorContainer-2 + Range-adopt-test 8F / ParentNode-querySelector-escapes 4F（逐簇评估轻量性）。
   - **(b) node-realm 剩 3F 深项**——cross-realm 基建域（iframe doc body adopt / removed-iframe getElementById / customElements 单 registry）。
   - **(c) M2/M6 面**：S6 高层 API 去字符串 / native dom_bindings 补齐。

0. **R181 下一步（R180 后，按 ROI）**：
   - **(a) 全量 fail Top 簇**——node-realm adoption 族（5+4+4+3=16F 四文件）/ Element-insertAdjacentElement 5F / EventListener-handleEvent-cross-realm 5F / Range-commonAncestorContainer-2 + Range-adopt-test 8F（逐簇评估轻量性）。
   - **(b) tree order 2F**——identity 深结构（R171 评估定格，L2 产物归一路径统一域，先记 RFC）。
   - **(c) M2/M6 面**：S6 高层 API 去字符串 / native dom_bindings 补齐。

0. **R180 下一步（R179 后，按 ROI）**：
   - **(a) 全量 fail Top 簇**——Event-dispatch-single-activation-behavior 14F / node-realm adoption 族（逐簇评估轻量性）。
   - **(b) tree order 2F**——identity 深结构（R171 评估定格，L2 产物归一路径统一域，先记 RFC）。
   - **(c) M2/M6 面**：S6 高层 API 去字符串 / native dom_bindings 补齐。

0. **R179 下一步（R178 后，按 ROI）**：
   - **(a) 全量 fail Top 簇**——Event-dispatch-single-activation-behavior 14F / node-creation-realm 13F（逐簇评估轻量性）。
   - **(b) tree order 2F**——identity 深结构（R171 评估定格，L2 产物归一路径统一域，先记 RFC）。
   - **(c) M2/M6 面**：S6 高层 API 去字符串 / native dom_bindings 补齐。

0. **R178 下一步（R177 后，按 ROI）**：
   - **(a) 全量 fail Top 簇**——Event-dispatch-single-activation-behavior 14F / node-creation-realm 13F / Range-attribute-nodes 11F（逐簇评估轻量性）。
   - **(b) tree order 2F**——identity 深结构（R171 评估定格，L2 产物归一路径统一域，先记 RFC）。
   - **(c) M2/M6 面**：S6 高层 API 去字符串 / native dom_bindings 补齐。

0. **R177 下一步（R176 后，按 ROI）**：
   - **(a) 全量 fail Top 簇**——Event-dispatch-single-activation-behavior 14F / node-creation-realm 13F / Range-attribute-nodes 11F / Node-removeChild 9F / Node-appendChild 7F（逐簇评估轻量性）。
   - **(b) tree order 2F**——identity 深结构（R171 评估定格，L2 产物归一路径统一域，先记 RFC）。
   - **(c) M2/M6 面**：S6 高层 API 去字符串 / native dom_bindings 补齐。

0. **R176 下一步（R175 后，按 ROI）**：
   - **(a) Document-createElement-namespace 40F 既存簇**——iframe XML/XHTML 文档域（R173 基线即有，本轮 kind 三分后可重估）。
   - **(b) tree order 2F**——identity 深结构（R171 评估：element 上下文本树化 0 改善，L2 产物归一路径统一域，先记 RFC）。
   - **(c) M2/M6 面**：S6 高层 API 去字符串 / native dom_bindings 补齐。

0. **R175 下一步（R174 后，按 ROI）**：
   - **(a) ParentNode 剩 5F 小簇**——tree order 2（QSA 结果序 vs 文档序）/ Fragment body 2 / Document: new NodeList 1。
   - **(b) Document-createElement-namespace 40F 既存簇**——iframe XML/XHTML 文档域（R173 基线即有）。
   - **(c) M2/M6 面**：S6 高层 API 去字符串 / native dom_bindings 补齐。

0. **R174 下一步（R173 后，按 ROI）**：
   - **(a) ns 24F setup 树碎片化统一**——R173 的 `_zwOwnerTree` 槽是中间基建（setup 的 createElement+appendChild 产物可循槽归树）；`getElementById("no-namespace")` null 的域。
   - **(b) ParentNode 剩 29F 其它簇**——tree order 2 + Fragment NodeList 2。
   - **(c) M2/M6 面**：S6 高层 API 去字符串 / native dom_bindings 补齐。

0. **R173 下一步（R172 后，按 ROI）**：
   - **(a) ns 24F 完整收口 = setup 树碎片化统一**——setupSpecialElements 的 createElement+appendChild 产物入 doc 查询树（L2 深结构，`getElementById("no-namespace")` null 的 probe 实证；R159 起多轮记录的域）。
   - **(b) `:lang`/`:target` In-document 4F**——伪类上下文域轻件评估。
   - **(c) M2 / M6 面**：S6 高层 API 去字符串 / native dom_bindings 补齐。

0. **R172 下一步（R171 后，按 ROI）**：
   - **(a) M1 收口评估**：L2「查询读 live 树」在 doc 上下文达成（tag+compound），element/fragment 维持 JSON——按 goal 的 M1 判据重估是否转 M2（S6 高层 API 去字符串）或 M4 修复面；RFC 补记 d3 域状态修订。
   - **(b) 轻件收口**：Element-matches 剩 3F（`[*|TiTlE]`）/ ParentNode 剩 33F 聚类。
   - **(c) M6 域**：native dom_bindings 补齐面（native 侧 coverage 提升）。

0. **R171 下一步（R170 后，按 ROI）**：
   - **(a) L2-d3d 重评估**：element/fragment 上下文本树化——R165 的 902F 回归面大半可能已被 key 双形态修复消解（同根因），重跑形态门扩展实验（element 上下文 compound 走 `_zwMQueryAll` 本树）。
   - **(b) 剩余 fail 聚类**：Element-matches 剩 3F（`[*|TiTlE]` 树碎片化）/ ParentNode 剩 33F。
   - **(c) 跨流信号**：SW 域 flake 持续观察。

0. **R170 下一步（R169 后，按 ROI）**：
   - **(a) d3 前置项：iframe doc 与 detached doc 树源统一**——iframe contentDocument 的查询/树走 part05 iframe 工厂（host 快照持内容），`_makeDetachedDocument.bodyHtml` 对 iframe 恒空（probe bodyHtml:0）；统一后 d3c compound gate 才能重启（本轮 905F 回归的根因）。
   - **(b) 转 M6 域或其它 ROI**：native dom_bindings 补齐面 / Element-matches 剩 3F（`[*|TiTlE]` 树碎片化域）。
   - **(c) 流程面**：master.md 编辑一律行级（learning 已沉淀）；大文件 docs 变更提交前 `git diff --stat` 量级核对。

0. **R169 下一步（R168 后，按 ROI）**：
   - **(a) L2-d3c doc compound gate**：queryBody 形态门扩到 `_queryTreeByCompound` 全形态（tag/#id/.class/[attr=v] 组合，无组合器无伪类）——R165 实证 doc 上下文无回归（`docQ:1` 保持），消 doc 上下文 compound 的 JSON 往返。
   - **(b) L2-d3d element/fragment 本树化**：R165 的 902F 回归面经 d3b/d3b2 桥+root-hit 消解大半，重评估迁移窗口。
   - **(c) 跨流信号**：SW 域 flake 持续观察（归 service-workers 流域）。


0. **R168 下一步（R167 后，按 ROI）**：
   - **(a) L2-d3b2 bubbles 残留**：`In window.document.cloneNode(true)` / `In new Document()` 两变体 12 vs 14 站——`new Document()` 的 docEl 是 plain 未经 `_zwWireLocalEvents` 接线 + `documentElement` 惰性 getter 返回内部 docEl 与链上 clone html proxy 身份错位（tag registry 兜底对该形态不生效）。修平后 bubbles 回基线 10P。
   - **(b) L2-d3c doc compound gate**：queryBody 形态门扩到 `_queryTreeByCompound` 全形态（R165 实证 doc 上下文无回归 `docQ:1` 保持）——消 doc 上下文 compound 的 JSON 往返。
   - **(c) L2-d3d element/fragment 本树化**：R165 的 902F 回归面在此被桥消解（归一产物 = 已登记对象）。
   - **(d) 跨流信号**：SW 域 flake（本轮 3 个不同测试单跑绿）持续观察，归 service-workers 流域。

0. **R167 下一步（R166 后，已执行——本轮收口）**：
   - **(a) L2-d3b 查询产物归一**（RFC 切片序下一片）：三查询面（queryBody / `_zwMQueryAll`→`_zwMWrapCached` / fragment QSA）返回前对可定位真实节点的产物调 `_zwBridgeGet` 归一——预期收口 `#id-li-duplicate` 类跨面 identity fail；全量双路径 net≥0 才 land。若 handle 域产物键匹配 miss 率高，评估 d3d（element 本树化）前移。
   - **(b) d3c doc compound gate**：queryBody 形态门扩到 `_queryTreeByCompound` 全形态（R165 实证 doc 上下文无回归——`docQ:1` 保持）——与 (a) 独立可并行。
   - **(c) 跨流信号**：SW 两个 flake（`navigator_update_succeeds_while_initial_worker_is_installing` / `update_permissions_follow_calling_worker_state_during_installation`）单跑绿 + 二次全量绿——归 service-workers 流域观察项（非 js-dom 面，本轮 webview 零改动）。

0. **R166 下一步（R165 后，已执行——本轮收口）**：
   - **(a) L2-d3 方向决策**：统一匹配器 + identity 桥（R165 结论）——影响面大，先写 RFC 级切片计划（identity 映射表设计 + 迁移面盘点）再动刀；或转 M6 QuickJS native（S1q）主线。
   - **(b) M6 QuickJS native**（S1q 只读属性族）——独立可并行。
   - **(c) 跨流信号**：Cache API 提交的 ~25 文件 summary 翻转仍归 service-workers 流。

0. **R165 下一步（R164 后，已执行——本轮收口）**：
   - **(a) L2-d2**：compound 形态（`#id`/`.class`/`[attr]`）迁移本树查询（`_queryTreeByTag` 扩展 `_queryTreeByCompound`——节点局部属性判定）；同款守卫（容器例外/空 outer 中止/零命中落 JSON）+ 双路径 net≥0。
   - **(b) M6 QuickJS native**（S1q 只读属性族）——与 L2 独立可并行。
   - **(c) 跨流信号**：Cache API 提交的 ~25 文件 summary 翻转仍归 service-workers 流。

0. **R164 下一步（R163 后，已执行——本轮收口）**：
   - **(a) L2-d1**：detached factory `queryBody` 纯 tag 选择器改查自身 `_tree`（JS 匹配器最简形态；JSON 往返保留为组合器/伪类回落）——R163 评估的切片序首片，net≥0 才 land。
   - **(b) M6 QuickJS native**（S1q 只读属性族）——与 L2 独立可并行。
   - **(c) 跨流信号**：Cache API 提交的 ~25 文件 summary 翻转仍归 service-workers 流。

0. **R163 下一步（R162 后，已执行——本轮收口）**：
   - **(a) M1 L2 启动评估**：Element-matches/ParentNode 残余已全部收敛到「三对象域 identity 不统一」——轻件榨干，下一 ROI 是 L2 统一 live Document（polyfill 查询读同一棵树）。先写 RFC 级切片计划（查询回调改读 live doc 的最小子集）再动刀——深结构护栏下逐片 land。
   - **(b) 主线里程碑**：M6 QuickJS native（S1q 只读属性族）——与 (a) 独立可并行。
   - **(c) 跨流信号**：Cache API 提交的 ~25 文件 summary 翻转仍归 service-workers 流。

0. **R162 下一步（R161 后，已执行——本轮收口）**：
   - **(a) tree order**（QSA 结果序 vs 文档序——host 返回序直出，`querySelectorAll` spec 要求树序；可能一处排序即收）+ "Fragment: new NodeList"（动态 NodeList）。
   - **(b) Element-matches 剩 ~14F**（`:lang`/`:nth-child`/Type html 上下文——与 R161 合成过滤同面，重跑聚类定根因）。
   - **(c) 跨流信号持续**：Cache API 提交的 ~25 文件 summary 翻转仍归 service-workers 流（master.md R160 记录）。
   - **(d) 主线里程碑**：M1 L2（live Document 统一）/ M6 QuickJS native（S1q）。

0. **R161 下一步（R160 后，已执行——本轮收口）**：
   - **(a) 跨流碰头信号（rule 9）**：Cache API 提交 `aaa1753c5`（service-workers 流）改 js_dom_shim part01/02/07 致 ~25 个 dom 文件 summary-line 翻转（testRanges/testNodes undefined 页提前 error）——已在 js-dom master.md 记录归因；service-workers 流侧处理（勿硬解，等其收敛或用户点名）。
   - **(b) ParentNode-querySelector-All 剩 57F**：element-上下文 html/:root 语义（子树不含 root 自身）+ `[*|TiTlE]` 第 3 命中（树碎片化域，L2 正解）+ tree order。
   - **(c) Element-matches 剩 14F**（:lang/:nth-child/Type html 上下文）。
   - **(d) 主线里程碑**：M1 L2（live Document 统一——树碎片化正解域）/ M6 QuickJS native（S1q）。

0. **R160 下一步（R159 后，已执行——本轮收口）**：
   - **(a) polyfill 树碎片化**（R159 已知限制）：per-element `_ensureMutTree` 与 doc 级查询树互不合并——评估统一到 doc 级树（L2 域深结构，先写单测复现再定切片）；这是 `#any-namespace` 族 + `[*|TiTlE]` 第 3 命中 + 多簇剩余 fail 的共同根因。
   - **(b) ParentNode-querySelector-All 剩 67F**：`:empty`（注释/空白文本判定）+ `:target` + element-上下文 html/:root 语义（子树不含 root 自身）+ tree order。
   - **(c) Element-matches 剩 18F**（R157 遗留）。
   - **(d) 主线里程碑**：M1 L2 / M6 QuickJS native（S1q 只读属性族）。

0. **R159 下一步（R158 后，已执行——本轮收口）**：
   - **(a) ParentNode-querySelector-All 剩 163F 分散簇**：伪元素 `:before`/`::first-line` 期望 0 匹配（one/two-colon）+ `:root`/`:empty`/`:not(*|*)` + ns 选择器三形态 + NodeList instance/tree order 断言——先聚类定根因再切片。
   - **(b) `[*|TiTlE]` 第 3 命中缺**：setupSpecialElements 的 setAttributeNS title 落 mutable tree 不入 host 快照——深结构（快照/树同步），评估。
   - **(c) Element-matches 剩 18F**（R157 遗留清单不变）。
   - **(d) 主线里程碑**：M1 L2 / M6 QuickJS native（S1q 只读属性族）。

0. **R158 下一步（R157 后，已执行——本轮收口）**：
   - **(a) ParentNode-querySelector-All 剩余簇**（R157 后全量 -162F 的大头在此文件）：跑 FILTER=ParentNode 聚类，共享引擎面的继续收口（R156/R157 的 tokenizer/转义/`*` 修复后此文件的剩余根因待重新聚类）。
   - **(b) Element-matches 剩 18F**：`:empty`（注释子节点/空白文本判定）8F + `:lang`/`:root`/`:target`/`:nth-child` matches 位置上下文 6F + `[TiTlE]` matches 面 case-insensitive 3F。
   - **(c) 主线里程碑**：M1 L2 / M6 QuickJS native（S1q 只读属性族）。

0. **R157 下一步（R156 后，已执行——本轮收口）**：
   - **(a) native iframe contentDocument body 空**（native 路径 Element-matches 整页 error 根因；全量 +2081P 的 native 对齐面）：ZW_NATIVE_DOM=1 下 iframe fetch 建档后 body.innerHTML 空（probe 实证 `ih:"empty"`、`q:"miss"`）——native 叠加路径的 R115 建档时序/写入路径独立诊断。
   - **(b) attr operator 边缘簇**（Element-matches 剩 77F 大头 + ParentNode-querySelector-All 1076F 共享引擎面）：`~=` 空白分隔表语义 / `|=` 连字符前缀 / `*=` unquoted 值 / `[attr="\e9"]` 转义值反解——zero-dom attr 匹配引擎补齐。
   - **(c) 伪类 in matches**：`:lang` 继承链 / `:empty` / `:root` / `:target` / `:not` 组合形态。
   - **(d) 主线里程碑**：M1 L2 / M6 QuickJS native（S1q 只读属性族）。

0. **R156 下一步（R155 后，已执行——本轮收口）**：
   - **(a) Element-matches.html 整页 error**（连续三轮未动，下轮**优先**）：setup 阶段 `Cannot read properties of null (reading 'appendChild')`——先跑内联诊断定位 null 来源。
   - **(b) single-activation 剩 14F 的反链正解**：cloneNode deep 分支（`__zw_set_inner_html_handle` + `_zwFragmentAdded`）给重建子 proxy 建 `_zwNodeParent` 反链 / parentNode 重指——clone/registry 深结构，评估后切片。
   - **(c) nodes 剩余非 realm 簇**：Attr-prefix 2F + MO-document 3F。
   - **(d) 主线里程碑**：M1 L2 / M6 QuickJS native（S1q 只读属性族）。

0. **R155 下一步（R154 后，按 ROI）**：
   - **(a) single-activation 剩余 25F 的 clone 子树链**：LABEL(child) 12 + checkbox/radio@FORM 12 + radio@LABEL 1。沙箱 createElement 单测全过 → 差异在 `template.content.children` clone 路径（`_zwFragmentAdded` 解析产物的 click()/input 事件链 vs createElement proxy）。定向：写 template.content 形态复现（template 元素 innerHTML 解析 + content.children + cloneNode）对比。
   - **(b) Element-matches.html 整页 error**（R154 未动）：setup 阶段 `Cannot read properties of null (reading 'appendChild')`。
   - **(c) nodes 剩余非 realm 簇**：Attr-prefix 2F + MO-document 3F。
   - **(d) 主线里程碑**：M1 L2 / M6 QuickJS native（S1q 只读属性族）。

0. **R154 下一步（R153 后，按 ROI）**：
   - **(a) single-activation 剩余 47F 四簇**（R153 已归因）：A/AREA hash 18（detached clone 上 location.hash + window.onhashchange newURL **字符串**形态——activated 收元素须转 `e.href` 映射的字符串）+ LABEL 转发 10 + checkbox/radio@FORM 12（FORM submit/reset 联动、radio 组互斥在 clone 子树）+ a-area href 映射 4。建议先做 a-area href 字符串映射（最轻——`getExpectedActivations` 的 tagName 判定 + href 读）再 LABEL。
   - **(b) Element-matches.html 整页 error**（pre-exists）：setup 阶段 `Cannot read properties of null (reading 'appendChild')`——独立诊断（R134 的 matches 工作面，可能与 clone/fragment 视图相关）。
   - **(c) nodes 剩余非 realm 簇**：Attr-prefix 2F + MO-document 3F + realm/adopt 族（R115 未解）。
   - **(d) 主线里程碑**：M1 L2 / M6 QuickJS native（S1q 只读属性族）。

0. **R153 下一步（R152b 后，按 ROI）**：
   - **(a) Element-closest 的 :scope 作用域语义**（4F）：closest/matches 的 spec 语义中 `:scope` = 调用元素自身（非文档根）。现 `closest_matching_selector`（selector_match.rs）经文档级 querySelectorAll 全匹配集（`:scope` 匹配 html）→ `:scope`/`:has(> :scope)`/`select > :scope` 全 miss。正解 = dom crate 选择器引擎加 per-callsite scope root 注入（`is_scope_element` 按 scope 参数判定）或 engine 侧 closest 改走 `Document::closest`；`:invalid` 4F 之一另需表单校验状态联动（`_validityState` 已有，选择器侧 `is_invalid_element` 复评路径核对）。中深结构，独立切片。
   - **(b) nodes 剩余非 realm 簇**（R152 候选 (b) 续）：Attr-prefix 2F + MO-document 3F + realm/adopt 族（iframe contentDocument 深项，R115 未解）。
   - **(c) events 剩余簇**（R151 遗留清单不变）：single-activation 剩余（并行 R152 `242b9b7d1` 已做 click() 激活三段，需核实其对 dispatchEvent 路径 single-activation 113F 的实际削减后重排）+ redispatch isTrusted + handlers-changed + incumbent-global。
   - **(d) 主线里程碑**：M1 L2 / M6 QuickJS native（S1q 只读属性族）。
   - **(e) 并行 R152 控制面补账**：click 激活切片（`242b9b7d1`）无 evidence/archive——若其 session 不再活跃，下轮顺手补 evidence 文件。

0. **R152 下一步（R151 后，按 ROI）**：
   - **(a) events 剩余簇**（R151 后 assert-fail 集 9 文件 + Timeout 9 文件）：single-activation 113F 的 activation behavior 模型（input checked 翻转+input 事件/form submit/a-area hash 导航/details toggle/label 转发——对 detached _zwMEl 克隆子树，激活语义深水区，建议切片化：先 label/details 轻件再 form/a-area）+ redispatch 2F（isTrusted——runner 注入的 DOMContentLoaded/load 是 `new Event` untrusted，需 host trusted 派发通道）+ handlers-changed 1F（17 vs 16 listener 二次拷贝时序）+ disabled-elements + click-on-absolute-pseudo（`target.pseudo()` API）+ incumbent-global 2F（frames undefined——iframe 跨 realm 深项）；~~crash 三件套 keypress~~（✅ R151 vacuous pass 语义已收；replace-event-listener/remove-and-adopt 仍需 adoptNode/contentDocument）。Timeout 侧真动画 9×4 需 runner 动画时钟 pump（架构项）。
   - **(b) nodes 剩余非 realm 簇续**（R151 后 fail 文件集 72：realm/adopt 族为主 + remove-unscopable 3F + Attr-prefix 2F + Element-closest 4F + Node-parentElement 3F + MO-document 3F；Node-contains/Node-properties/NodeList-tampered 族已随 R151 cloneNode registry 转 Pass）。
   - **(c) template contents 后续深化**（R145 遗留）：parser 建 inert fragment 正解——DocumentFragment-getElementById / svg-template-querySelector 疑似此域。
   - **(d) ranges mega-case setup 簇**（R135 盘点未动）。
   - **(e) 主线里程碑**：M1 L2 / M6 QuickJS native（S0q 已 land，下一片 S1q 只读属性族）。
   - **(f) R150 follow-up**：`pipeline.layout_rect_for_selector` + `fill_layout_rect_layout_box_pub` 无调用方（评估删除）。

0. **R151 下一步（R150 后，已执行）**：
   - **(a) events 剩余簇**（R150 后 assert-fail 集 12 文件 + Timeout 9 文件）：redispatch 2F（isTrusted 事件可信度模型）+ crash 三件套（keypress/replace-event-listener/remove-and-adopt）+ incumbent-global 2F（flake 复跑核实）+ handlers-changed/single-activation/disabled-elements + click-on-absolute-pseudo；~~mouse-event-retarget~~（✅ R150 gBCR + offsetX 派发期计算已收）~~timestamp 2F~~（✅ R150 5µs 量化 + GamepadEvent 已收）。Timeout 侧真动画 9×4 需 runner 动画时钟 pump（架构项）。event-global 2F 判不追（cross-realm frames.Function 深项，同 handleEvent-cross-realm 簇）。
   - **(b) nodes 剩余非 realm 簇续**（R149 后 230F：realm/adopt 族为主 + remove-unscopable 3F + Attr-prefix 2F + Element-closest 4F + Node-parentElement 3F + MO-document 3F）。
   - **(c) template contents 后续深化**（R145 遗留）：parser 建 inert fragment 正解——DocumentFragment-getElementById / svg-template-querySelector 疑似此域。
   - **(d) ranges mega-case setup 簇**（R135 盘点未动）。
   - **(e) 主线里程碑**：M1 L2 / M6 QuickJS native（S0q 已 land，下一片 S1q 只读属性族）。
   - **(f) R150 follow-up**：`pipeline.layout_rect_for_selector` + `fill_rect_from_layout_box_pub` 当前无调用方（进程内 webview 走常驻 snapshot 路径）——评估删除或接按需查询；gBCR 的 handle-identity（createElement 元素）路径经 RectBridge handler 的 handle→selector map，进程内空快照下 detached handle 仍返空（与多进程一致）。

0. **R150 下一步（R149 后，已执行）**：
   - **(a) events 剩余簇**（R149 后 assert-fail 集 15 文件 + Timeout 9 文件）：redispatch 2F（isTrusted 事件可信度模型）+ mouse-event-retarget + crash 三件套（keypress/replace-event-listener/remove-and-adopt）+ timestamp 2F + incumbent-global 2F（flake 复跑核实）+ handlers-changed/single-activation/disabled-elements；Timeout 侧真动画 9×4 需 runner 动画时钟 pump（架构项）。event-global 2F 判不追（cross-realm frames.Function 深项，同 handleEvent-cross-realm 簇——两项合计 7F 全依赖 iframe 跨 realm 基建）。
   - **(b) nodes 剩余非 realm 簇续**（R149 后 230F：realm/adopt 族为主 + remove-unscopable 3F + Attr-prefix 2F + Element-closest 4F + Node-parentElement 3F + MO-document 3F）。
   - **(c) template contents 后续深化**（R145 遗留）：parser 建 inert fragment 正解——DocumentFragment-getElementById / svg-template-querySelector 疑似此域。
   - **(d) ranges mega-case setup 簇**（R135 盘点未动）。
   - **(e) 主线里程碑**：M1 L2 / M6 QuickJS native（S0q 已 land，下一片 S1q 只读属性族）。

0. **R139 下一步（R138 后，按 ROI）**：
   - **(a) events 剩余 28F 双路径共享簇**（R138 后 fail 集完全重合，修复面统一）：Event-dispatch-click 7F（含 redispatch/target-moved 深结构）+ handleEvent-cross-realm 5F（iframe 跨 realm）+ timestamp 2F + no-focus 2F 等。
   - **(b) nodes 剩余非 realm 簇续**（双路径 188F）：remove-unscopable 3F（inline handler 派发时序）+ Node-childNodes 5F + Attr-prefix 2F + Element-closest 4F + Node-parentElement 3F + MO-document 3F（R136 判并行流漂移待查）。
   - **(c) native 剩余独立缺口**：native HTMLElement.name 仍空（R137 教训①统一 Function::set_name）+ Event ctor name（shim 覆盖但 native 直跑时缺）——低 ROI 小项。
   - **(d) ranges mega-case setup 簇**（R135 盘点未动）：Range-compareBoundaryPoints 9313F 等「Set up range N」setup 簇（foreignDoc 容器）——判 M1 L2 域还是可修语义面。
   - **(e) crash 用例剩余 Fail**：keypress-dispatch-crash + replace-event-listener（contentDocument.adoptNode 缺）+ remove-and-adopt-thcrash。
   - **(f) 主线里程碑**：M1 L2（sel/handle 双形态移动语义统一正解）/ M6 QuickJS native（S0q 已 land，下一片 S1q 只读属性族）。


0. **R138 下一步（R137 后，按 ROI）**：
   - **(a) native 剩余缺口收口**（R137 后 native fail 集已与 polyfill 完全重合，但**基线既存缺口**仍在双路径共享面）：native events 412P/39F vs polyfill 423P/28F 的 11 差（stash A/B 证基线既存——Event-dispatch-click 7F / handleEvent-cross-realm 5F 簇）+ native HTMLElement.name 仍空（R137 教训①——统一 Function::set_name）。
   - **(b) nodes 剩余非 realm 簇续**（双路径共 188F）：remove-unscopable 剩 3F（inline handler 派发时序）+ Node-childNodes 5F + Attr-prefix 2F + Element-closest 4F + Node-parentElement 3F + MO-document 3F（R136 判并行流漂移待查）。
   - **(c) native 路径常量族系统性排查收尾**（R130 发现）：Event 常量（NONE/AT_TARGET/CAPTURING/BUBBLING）、NodeFilter 常量、Document.POSITION 族——R137 已收 Node 常量 + DOMException.name，剩 Event/NodeFilter/Document 面（diag7 探针模式）。
   - **(d) ranges mega-case setup 簇**（R135 盘点未动）：Range-compareBoundaryPoints 9313F 等「Set up range N」setup 簇（foreignDoc 容器）——判 M1 L2 域还是可修语义面。
   - **(e) crash 用例剩余 Fail**：keypress-dispatch-crash + replace-event-listener（contentDocument.adoptNode 缺）+ remove-and-adopt-thcrash。
   - **(f) 主线里程碑**：M1 L2（sel/handle 双形态移动语义统一正解）/ M6 QuickJS native（S0q 已 land，下一片 S1q 只读属性族）。


0. **R137 下一步（R136 后，按 ROI）**：
   - **(a) nodes 剩余非 realm 簇续**：~~rootNode 5F~~（✅ R136 已收 100%）——下一候选 remove-unscopable 剩 3F（inline handler 派发时序）+ Node-childNodes 5F + Attr-prefix 2F（SVG/MathML parser-inserted 属性 ns/prefix 元数据）+ Element-closest 4F + Node-parentElement 3F。
   - **(b) MutationObserver-document 漂移复核**（R136 记录：parser-insertion 3F/1P vs R134 账面 41P MO-attributes 域——隔离复跑仍 3F，非 R136 改动面；`git log` 归因并行流或 wpt-data 波动）。
   - **(c) ranges mega-case setup 簇**（R135 盘点候选未动）：Range-compareBoundaryPoints 9313F / Range-set 8005F 等「Set up range N」setup 簇（foreignDoc 容器）——判 M1 L2 域还是可修语义面。
   - **(d) native 路径常量族系统性排查**（R130 发现，未做）：Event 常量（NONE/AT_TARGET/CAPTURING/BUBBLING）、NodeFilter 常量、Document.POSITION 族——diag7 探针模式统一排查幂等补挂（R136 HTMLElement.prototype 补挂是同族第 3 例，可一次收口）。
   - **(e) crash 用例剩余 Fail**：keypress-dispatch-crash + replace-event-listener（contentDocument.adoptNode 缺）+ remove-and-adopt-thcrash。
   - **(f) 主线里程碑**：M1 L2（sel/handle 双形态移动语义统一正解）/ M6 QuickJS native（S0q 已 land，下一片 S1q 只读属性族）。


0. **R136 下一步（R135 后，按 ROI）**：
   - **(a) nodes 剩余非 realm 簇续**：下一候选——remove-unscopable 剩 3F（inline handler 派发时序：before/after/replaceWith 三方法 result1 恒 undefined——归因 listener 注册时序）+ rootNode 5F + Node-childNodes 5F + Attr-prefix 2F（SVG/MathML parser-inserted 属性的 ns/prefix 元数据——`xlink:href` 存为扁平名后 getAttributeNodeNS(xlink ns) 反查 miss，探针已实证 baseline 同 null，属解析器 ns 段元数据面）+ Node-constants 4F。
   - **(b) ranges mega-case setup 簇**（R135 全量盘点新增候选）：Range-compareBoundaryPoints 9313F / Range-set 8005F / Range-isPointInRange 5694F 等「Set up range N」setup 簇（foreignDoc/detachedDoc 容器）——先跑单文件 fail 明细聚类，判 M1 L2 域还是可修语义面（对比 R96 基线判升降）。
   - **(c) native 路径常量族系统性排查**（R130 发现，未做）：Event 常量（NONE/AT_TARGET/CAPTURING/BUBBLING）、NodeFilter 常量（FILTER_ACCEPT/REJECT/SKIP）、Document.POSITION 族——diag7 探针模式统一排查幂等补挂。
   - **(d) crash 用例剩余 Fail**：keypress-dispatch-crash（NotSupportedError）+ replace-event-listener（contentDocument.adoptNode 缺）+ remove-and-adopt-thcrash（contentWindow.document 链）。
   - **(e) 主线里程碑**：M1 L2（sel/handle 双形态移动语义统一正解）/ M6 QuickJS native（S0q 已 land，下一片 S1q 只读属性族）。


   - **(a) nodes 剩余 187F 非 realm 簇续**：下一候选——rootNode 5F + name-validation 5F + Node-childNodes 5F + remove-unscopable 剩 3F（inline handler 派发时序——直接编译全对/完整链路三方法 undefined，归因 listener 注册时序）。
   - **(b) native 路径常量族系统性排查**（R130 发现，未做）：Event 常量（NONE/AT_TARGET/CAPTURING/BUBBLING）、NodeFilter 常量（FILTER_ACCEPT/REJECT/SKIP）、Document.POSITION 族——diag7 探针模式统一排查幂等补挂。
   - **(c) crash 用例剩余 Fail**：keypress-dispatch-crash（NotSupportedError）+ replace-event-listener（contentDocument.adoptNode 缺）+ remove-and-adopt-thcrash（contentWindow.document 链）。
   - **(d) 主线里程碑**：M1 L2（sel/handle 双形态移动语义统一正解）/ M6 QuickJS native（S0q 已 land，下一片 S1q 只读属性族）。

0. **R134 下一步（R133 后，已执行）**：
   - **(a) nodes 剩余 196F 非 realm 簇续**：下一候选——Element-matches-namespaced-elements 6F + remove-unscopable 6F + rootNode 5F + name-validation 5F + Node-childNodes 5F（insertAdjacentElement 剩 5F + Node-appendChild 7F 已归 sel-子同步移动深结构簇[M1 L2 正解]，Realm 族深结构）。matches-namespaced 是选择器引擎 NS 匹配语义面（R120 统一匹配器邻域）。
   - **(b) native 路径常量族系统性排查**（R130 发现，未做）：Event 常量（NONE/AT_TARGET/CAPTURING/BUBBLING）、NodeFilter 常量（FILTER_ACCEPT/REJECT/SKIP）、Document.POSITION 族——diag7 探针模式统一排查幂等补挂。
   - **(c) crash 用例剩余 Fail**：keypress-dispatch-crash（NotSupportedError）+ replace-event-listener（contentDocument.adoptNode 缺）+ remove-and-adopt-thcrash（contentWindow.document 链）——adoptNode 补齐是 iframe 域轻量项。
   - **(d) 主线里程碑**：M1 L2（sel/handle 双形态移动语义统一正解——R133 深结构结论）/ M6 QuickJS native（S0q 已 land，下一片 S1q 只读属性族）。

0. **R133 下一步（R132 后，已执行）**：
   - **(a) nodes 剩余 197F 非 realm 簇续**（R131 isEqualNode + R132 importNode 已收）：下一候选——Element-insertAdjacentElement 6F + Element-matches-namespaced-elements 6F + remove-unscopable 6F + Node-appendChild 7F（frames 域待查）+ rootNode 5F + name-validation 5F + Node-childNodes 5F。matches-namespaced 是选择器引擎 NS 匹配语义面；insertAdjacentElement 是位置插入 API 族（与 R119 prepend/replaceChildren 同域）。
   - **(b) native 路径常量族系统性排查**（R130 发现，未做）：Event 常量（NONE/AT_TARGET/CAPTURING/BUBBLING）、NodeFilter 常量（FILTER_ACCEPT/REJECT/SKIP）、Document.POSITION 族——diag7 探针模式统一排查幂等补挂。
   - **(c) crash 用例剩余 Fail**：keypress-dispatch-crash（NotSupportedError）+ replace-event-listener（contentDocument.adoptNode 缺）+ remove-and-adopt-thcrash（contentWindow.document 链）——adoptNode 补齐是 iframe 域轻量项。
   - **(d) 主线里程碑**：M1 L2 / M6 QuickJS native（S0q 已 land，下一片 S1q 只读属性族）。

0. **R132 下一步（R131 后，已执行）**：
   - **(a) nodes 剩余 202F 的非 realm 簇续**（R131 isEqualNode 已收）：下一候选按文件聚——Node-appendChild 7F + remove-unscopable 6F + Element-matches-namespaced-elements 6F + Element-insertAdjacentElement 6F + rootNode 5F + name-validation 5F + Node-childNodes 5F + Document-importNode 5F。importNode 是 spec adopting 算法可直接镜像（与 R131 isEqualNode 同模式的逐字段语义面）；appendChild 7F 涉及 frames 域待查（3 行 fail 消息含 frames）。
   - **(b) native 路径常量族系统性排查**（R130 发现，未做）：Event 常量（Event.NONE/AT_TARGET/CAPTURING/BUBBLING）、NodeFilter 常量（FILTER_ACCEPT/REJECT/SKIP）、Document.POSITION 族等——diag7 探针模式统一排查幂等补挂。
   - **(c) crash 用例剩余 Fail**：keypress-dispatch-crash（NotSupportedError）+ replace-event-listener（contentDocument.adoptNode 缺）+ remove-and-adopt-thcrash（contentWindow.document 链）——adoptNode 补齐是 iframe 域轻量项。
   - **(d) 主线里程碑**：M1 L2 / M6 QuickJS native（S0q 已 land，下一片 S1q 只读属性族）。

0. **R131 下一步（R130 后，已执行）**：
   - **(a) nodes 剩余 214F 的非 realm 簇**：node-creation-realm/node-realm 族（~28F）是 iframe 跨 realm 深结构（记深结构清单）；**非 realm 可修簇**按文件聚：Node-appendChild 7F + remove-unscopable 6F + Node-isEqualNode 6F + Element-matches-namespaced-elements 6F + Element-insertAdjacentElement 6F + rootNode 5F + name-validation 5F + Node-childNodes 5F + Document-importNode 5F——先跑逐文件 fail 明细聚类，选最大可修簇开刀（R131 候选首选 isEqualNode 或 importNode——两者都是 spec 算法可直接镜像的语义面）。
   - **(b) native 路径常量族系统性排查**：R130 发现 native 叠加路径 native 构造器（Node 等）静态常量全缺——同类缺口可能在 Event 常量（Event.NONE/AT_TARGET 等）、NodeFilter 常量、Document 位置族等；一次排查统一幂等补挂（diag7 探针模式可复用）。
   - **(c) crash 用例剩余 Fail**：keypress-dispatch-crash（NotSupportedError——keypress 事件构造不支持）+ replace-event-listener-null-browsing-context-crash（contentDocument.adoptNode 缺）+ remove-and-adopt-thcrash（contentWindow.document 链）——三文件小簇，adoptNode 补齐是 iframe 域轻量项。
   - **(d) 主线里程碑**：M1 L2 / M6 QuickJS native——均为深结构（M6 S0q 已 land，下一片 S0q 续或 S1q 只读属性族）。

0. **R100 后下一步（按 ROI，M3 双资产落地后的主线回归）**：
   - **(a) ~~M3 收尾：QuickJS feature 跑同一 Vue/lit 验收页~~** → ✅ **R101 已核实**：QuickJS 矩阵 Vue e2e 双组 + lit/WC 14 组全绿
   - **(b) ~~M3 收尾：reconciliation 深场景~~** → ✅ **R101 已落**：`vue_reconciliation_lands` 断言组（v-if 卸载 + keyed v-for 中间插入 + 清空）+ 全 handle insertBefore wire 根因修复。**M3 主验收面全部达成**（SPA=Vue mount/响应式/事件/reconciliation + WC=lit 全链 + 双 feature 验证）；剩余可选项：hydration（需 SSR 预渲染 markup，评估后定）
   - **(c) M1 L2 主线**（polyfill-live 合一）→ **R102 读路径首切片 + R103 helper 收口 + R104 三方合一资产**：全部 `__zw_*` 查询回调 live 读 + 写路径 A/B/C 收敛断言（`l2_three_way_write_paths_converge_r104` 一次通过）——**M1 L2 主验收面达成**；cached_html 依赖面盘点完成（R104，L3 萎缩顺序输入）。**下一步主线转 M2 S6**（高层 API 去字符串：Fetch/MutationObserver/FontFaceSet 逐族迁移）或 **M4 聚类提升**
   - **(d) WPT dom 持续扩展**（M4）→ **R105 已落最大簇**：events passive-by-default 57F→100%（events 293P）——**R105-113 九轮已落**：passive-by-default 57F→100% + dispatchEvent 入口 24F→1F + Body-FrameSet 24F→100% + dispatch-click 16F→100% + Event-subclasses-constructors 双路径 100%（R109，双路径差 6.12pp→~2.5pp）+ init-while-dispatching 三簇 100%（R110）+ R112 三簇 100%（detached-input-and-change 8F→0 + dispatch-bubbles 双文件 6F→0 + remove-all-listeners 2F→0）+ **R113 双簇**（EventListener-handleEvent 3F+1timeout→6P/0F via handleEvent 非 callable 抛 TypeError + error 事件上报两条接收面；webkit-animation/transition 四文件 timeout→16P/0F via prefixed handler camelCase 映射 + handle-based style.sheet + resources .js 补齐）（events 236P→**419P**，+183 净；R114 event-global 8P 全绿 + composed 边界穿越根因修复）+ **R115 nodes iframe 大簇**（静态 iframe contentDocument——createElement 147P/0F + createElementNS 596P/0F 双 100%）+ **R116 属性族**（大小写语义 + NS 元数据 registry——case 285P/1F + attributes 214P/36F + createAttribute 36P/0F）+ **R117 mutation 族**（ChildNode 三文件 **123P/0F 100%** + replaceChild/removeChild 校验族，nodes 7366→**7479P**）+ **R118 属性查询**（querySelector-escapes 17P→64P：CSS Syntax 4.3.4 转义解码四层——_readCompoundToken hex+终止符/CRLF/FFFD + _splitComplex·_splitSelectorListOf 转义感知 + JS /\s/ ≠ CSS 空白[U+2003/U+00A0 是 ident 字符]；剩 4F 为 wire 协议孤立代理+NUL 深结构）+ **R119 mutation 收口**（prepend 8F→21P + replaceChildren 13F→29P 双 100%：handle 容器 prepend/replaceChildren 路径落地 + replace-all 移动记账粒度 + detached doc 三缺口 + doctype 校验；nodes 7479→7551P）+ **R120 查询族**（getElementsBy* 四文件 45F→0F 全 100%：NS 感知统一匹配器 + document 枚举快照∪pending + live 作用域放行 + named id/name 不对称 + 接口构造器 + tagName ASCII 大写化修复[case.html 'ä' bug]；nodes 7551→7599P）+ **R121 构造器族**（Text/Comment-constructor 13F+13F→全 100% + CharacterData-surrogates 6F→100%：真构造器[ownerDocument/三层原型链] + _zwTextDataCache 孤立代理 JS 侧保真缓存；nodes 7599→7631P）+ **R122 属性 identity 族**（attributes.html 36F→67P/0F + attributes-namednodemap 2F→8P/0F 双 100%：_zwAttrBindings 绑定表[Attr 对象往返 + ownerElement 绑定/解绑] + _zwAttrInstances 多实例覆盖层[同 local 多 ns 并存，非 NS 读 host-first/NS 读 instance-first] + Attr.value setter 写回传播 + NamedNodeMap 方法 identity + setAttributeNS 校验全分支 + 小写化收窄 HTML-ns + el.style= 写属性；nodes 7631→7673P）+ **R123 PI 属性层**（PI-attributes 7P/133F→140P/0F 100%：data 即属性序列化源双向同步 + 值转义三面一致 + bogus comment 双 parse 派生 PI 视图 + XML 文档面；nodes 7673→7806P）+ **R124 whitespace 分词**（getElementsByClassName-whitespace 7P/19F→26P/0F 双路径 100%：class 域 ASCII whitespace 五字符——Rust split_whitespace·trim 与 JS /\s/ 均 Unicode 空白集，三面八处收口[dom crate split_ascii_whitespace·trim_ascii_ws + engine 桥 selector_match + shim _zwSplitClassList]；nodes 7806→7825P）+ **R125 getElementById**（动态 id 语义：查询源时效三层[覆盖表/in-doc 门/祖先判定] + innerHTML·outerHTML 剔旧 + lenient append + 解析节点原型链接[连带 DOMPurify namespaceURI 回归修复]；nodes 7825→7838P）+ **R126 removeChild**（spec pre-remove 校验族 28F→9F：融合视图子判定 + lenient 分支 + 泛型 own-property 委托 + 步骤顺序纠正；nodes 7838→7857P）+ **R127 replaceChild**（spec replace-child 校验族 + replace 语义 15F→0F 全 100%：own-delegation 复发面收口 + step 6 位置感知校验[插入位判 before/after 非存在性] + adopt-first 兄弟替换 + fragment flatten + adopt ownerDocument + selector-newChild 新路径[adopt record + replace record——MO 两悬挂 async_test 解锁修平]；nodes 7857→7876P，fail 326→310）+ **R128 cloneNode**（全节点形态族 主文件 14F→0F 100%：Node.prototype 泛型分派 + DocumentType/XMLDocument/Attr 原型链接族 + canvas/unknown/用户原型 + NS 源恢复 + DOMParser doc；nodes 7876→7902P，fail 310→284，含 append/prepend-on-Document 7F 涟漪收益）+ **R129 CharacterData 方法族**（八文件 44F→0F 全 100%：叶子 mutation HRE + WebIDL 参数族[缺参 TypeError/String 转换/LegacyNullToEmptyString] + offset 抛 count 回绕 clamp + text.remove 真移除 + has trap 方法白名单[拼接式 handler 重复键后者胜定位]；nodes 7902→7949P，fail 284→237）+ **R130 DOMImplementation doc 族**（createHTMLDocument 7F→0F + createDocument 434P 全绿 + createDocumentType 82P + Node-baseURI 9P/0F + crash 族 8 文件 Timeout→PASS[runner crash 支持：无 harness 引用用例预置内联 + terminal 零注册按未崩 PASS 计] + 无 src iframe contentDocument 空文档 + A/AREA href IDL percent-encode；nodes 7949→**8410P（+461）** fail 237→**214**/native 6445→**7627P（+1182）**；当轮三回归三修：Event-dispatch-bubbles 2F[documentElement 惰性 getter 对 proxy 子按形态分派返内部 docEl 保 R112 派发站点] + native traversal foreignDoc 20F[title 文本子叶子导航三件套] + native createDocument 111F[Node 常量无守卫幂等补挂——native 叠加路径构造器常量全缺]）——**剩余聚类**：events iframe 域（cross-realm 5F + event-global-is-still-set 2F + incumbent 5F）、shadow-relatedTarget 2F（retarget 深结构）、no-focus-events 2F、nodes 214F（node-creation-realm 13F + node-realm 族 15F + cloneNode-svg 4F[源 markup-NS 深结构] + ranges/insertBefore 慢用例 + Node-appendChild 7F + remove-unscopable 6F + isEqualNode 6F + matches-namespaced 6F + insertAdjacentElement 6F 等）、querySelector-escapes 4F（wire 深结构）、removeChild 9F（frame 域）
1. **R51（本轮，已完成）**：TreeWalker NodeFilter 语义 + Document 构造器解锁 dom/common.js（traversal +889、nodes +449、ranges 非 mutations +1808）→ land（五子目录零回归，engine v8 2132 / quickjs 1415 全绿，clippy 干净，fmt 无 diff）
2. **R51b（同轮 follow-up，已 land `aed92314`）**：**dom/ranges mutations 族死循环根因修复**——R51 的 `new Document()` 解锁 setupRangeTests 后，mutations 族首次真正执行即触三连锁死循环（common.js `indexOf` 的 `while (node != node.parentNode.childNodes[i])`）。三根因三修：① **child→parent 反向链缺失**（`_zwNodeParent` registry 挂 `_mo_notify` 汇流点 + `_parentNodeFor` handle 分支优先查反链、无链 detached 返 null 不再猜 body）② **sel 父 childNodes pending overlay**（`_childNodeList` 融合 `_zwPendingAdded/Removed`，nextSibling 定位插入）+ **textContent 视图与 registry 融合**（旧短路 append 的子不可见）③ **pre-insert HierarchyRequestError**（self-append 旧 shim 真执行 → registry 自环 → `_zwHCCollectSubtree` 栈溢出；ancestor-append → host「DOM 树循环」整批丢弃；`_zwIsAncestorOf` **从目标上行**判定）。mutations 族无限 spin→跑完（appendChild 2.5s 34P/36F、10 文件 586P/1010F）；四子目录回归全净（nodes 3049P/traversal 925P/collections 48P/events 189P）。单测 part18 六件。**遗留**：dataChange/insertBefore/replaceData >300s（次级性能/循环，下轮首查）+ mutations 语义面（Range 端点 identity）
3. **R51c（下轮，已 land `81ca25b5`）**：**三文件超时根因 = pending 差量表 O(n²) 无界膨胀**（诊断：1500 次 setup 5s→31s 线性增速；内部表窥视 pa +19/subtest、textEls +5/subtest）。五修：① **消零语义**（pending-added 节点 remove 后 add+remove 对冲为零，不入 removed 表）② **pending 按父分桶**（childNodes overlay 读 O(桶)，桶内并行 Set）③ **removed 表 512 软上限压实**（handle-only 条目对快照结构性 no-op，死数据丢弃）④ **querySelector('#id') host-miss 回落 pending id 索引**（setupRangeTests 开头 `querySelector('#test')` 查不到 pending 旧树 → 跳过 remove → 整树泄漏——+19/subtest 主源）⑤ `_appendVariadic` 补 registry 记账 + `_zwUnregisterTextSubtree` 子树注销 + collectSubtree 桶回落。**增速消除**（append 路径 929ms 增长→恒定）+ nodes 3049→3096P / traversal 925→953P（回落解锁）/ events/collections/mutations 586P 零回归；单测 part18 +2（R51c 回落/对冲）。**遗留**：dataChange 全量仍 >560s——**恒定 per-op 成本 ~7ms/append**（两次 host 往返 + re-parse）是 M1 L2 正题；detached 容器（foreignDoc/xmlDoc）子树泄漏（真实浏览器靠 GC，我们强引用）残余缓涨
3. **下轮候选（按剩余 ROI，R39 后重排）**：
   - **(a) R51c 遗留：per-op 恒定成本**（~7ms/append = 两次 host 往返 + `__zw_query_match` re-parse——**正是 M1 L2 正题**：polyfill 桥改读 live Document 消 re-parse 后，dataChange/insertBefore/replaceData 三文件大概率自然跑完；R43 的 L2 诊断文档是设计输入）。→ **R52 已实质解决大半**（见下）
4. **R52（已 land `ca81a76f`）**：**per-op 成本微基准归因 + 修复**——7ms/append 的真凶**不是** host 往返（0.4µs）也不是查询回调（5.6µs，已有缓存），而是 **DOM 遍历族回调没接 parse 缓存**：`__zw_child_nodes`/`__zw_sibling_nodes`/`__zw_element_children`/`__zw_element_siblings`/`__zw_parent`/`__zw_contains` 每次 `parse_html` 全文档重解析（`body.firstChild` 实测 1.2ms/次）。修：① 六函数加 `_doc` 版 helper（html 版薄封装）+ callbacks 接 `with_query_doc`（既有 QUERY_DOC_CACHE，键=html 文本）② `_zwTextEls` el/handle/sel **三重 Map 索引**（注册/注销/查询旧全表线性扫，testharness 每 subtest 6+6 次 → O(n²)）③ pending 剔除惰性 splice（Set 命中才动数组）④ 消零节点清 `_proxyCache`/`_clsProxyCache`/`_expando` 强引用（V8 堆涨）；**`_handleChildren`/`_zwNodeParent` 保留**（CE 断连传播仍走——R2994 测试实证提前清破坏 disconnectedCallback，同轮修）。**实测：body.firstChild 1171→221µs（5.3x）、append_handle 1983→74µs（27x）、append+remove 2212→336µs；setup 循环墙钟有界化**（1000-4000 次全 ~90s，非二次）。五子目录+mutations 族计数逐字节一致零回归。**遗留**：dataChange 全量仍 >560s——剩余是**每 subtest testFn 成本**（Range getter 断言族），setup 已非瓶颈；下轮从 testDataChange 内部归因
   - **(a2) mutations 族语义对齐**（Range 端点 container identity：append 同位节点后 startContainer 期望旧 proxy——host 重挂后 handle 变 `__n17`→`__n33`，疑 proxy 缓存失效或 NodeId 重建）。
   - **(b) ~~Event-dispatch 剩余（bubbles-true/false 各 3F 独立 Document 深结构）~~** → ✅ **R112 已落 100%**（document.cloneNode + detached doc 事件面 + path 身份键祖先链）。
   - **(c) events 剩余聚类（R114 后）**：EventListener-handleEvent-cross-realm 5F + event-global-is-still-set-* 2F + incumbent-global 3 timeout + 2F（iframe/frames 域深结构，同未解决问题 #13）、shadow-relatedTarget 2F（~~root.getElementById~~ ✅ R114 已修——剩余 = relatedTarget retarget 焦点历史模型）、no-focus-events-at-clicking 2F、EventListener-invoke-legacy timeout。~~event-global 3F~~ → ✅ **R114 已落 8P/0F**（composed init + shadow 抑制 + XHR EventTarget）。~~webkit-animation 4F~~ → ✅ R113 已落 16P/0F。
   - **(d) NamedAttributeMap own 枚举**（dom/collections 3 fail，轻量候选）。
   - **(e) native namespaceURI getter 独立化**（dom/nodes 双路径差 0.65pp）。
   - **(f) querySelector-mixed-case**（selector 域）。
   - **(g) iframe.contentDocument**（深结构 html-compat 域）。
   - **(h) dom_bindings coverage 剩余**（custom_elements 89.0%，剩 19 防御/OOM 行低 ROI）。
   - **(i) 主线里程碑推进**：M1 L2 / M6 QuickJS native——均为深结构，评估切片化可能（M1 L2 最小只读子集已有 A/B 门就绪；R51b 反链+overlay 已是 L2 方向实质推进）。
4. **后续主线**：M1 L2（polyfill-live 合一，解 polyfill appendChild 闭环限制）→ M2 S6 → M3 SPA/WC → M4 WPT dom 持续扩 → M5 V8 default-on（**方向已获 2026-08-19 用户批复，启动时点按 zero-web ⚡ 块执行序**，见待用户决策清单）→ M6 QuickJS native → M7 双引擎 default-on + 收尾；M8 canvas path-objects 待 canvas 流告段落接手
5. **R53（诊断归档未 land——负结果 + 精确方向，同 R43 模式）**：**dataChange testFn 成本归因完成**——testFn 原语（data 读写/parentNode/Range getter）全部 µs 级无辜；真凶是**「脏状态」级联**：干净 setup 1.5ms → 混入 testFn 的 data 写后 **334ms/setup（220x）**；分解定位 ce 段（createElement+appendChild）2ms/op。**根因链实证**（R53J/R53K 行为探针）：`getElementsByTagName('p')` live collection 的 els **每 setup 净 +2**（旧 detached/foreign paras 滞留）→ els 千级 → `_zwHCLiveInvalidate` 失效循环每 mutation O(els) → 全部 append 变慢。**两版修复试验均产生回归回退**：① 消零同步剔除 + `_zwPendingConnected` 并入过滤（反链上行判定）→ 快照基线 8 个 p 误清（len=0，R50 own-pts 必炸）——**sel 节点的快照基线剔除与 pending 泄漏剔除是两个独立问题，不能混在一个 remFlat 循环**；② 保守放行版仍 len=0。**已回退零残留**（part05 = R52 HEAD，engine 2140 全绿 + collections 48P 验证）。**下轮精确方向**：live collection els 的泄漏剔除应挂在「**detached/foreign 容器子树的 pending added 并入时就不该进文档级集合**」（并入点过滤而非移除点剔除），且过滤判据不能用 `_zwNodeParent` 反链（pending 树断链），应用 **`_mo_notify` 的 mutSel 链**（mutation 发生在 detached 容器上时其子树全部跳过并入——mutSel 无 host 快照即非主文档容器）
6. **R54（已 land `ba775545`）**：**R53 方向的落地**——`_zwMutationInDoc(mutSel, mutHandle)` 挂载点判定（sel→`__zw_contains('html', sel)`；handle→`_zwNodeParent` 逐跳上行——**注：R53 说「不能用反链」指的是从 pending 子节点上行；从挂载点上行用的是 append 当时刚记账的链，不断链**），应用于失效循环 add 分支 + 构建期 pending 并入两处。**deleteData 2.0x / insertData 加速，四子目录零回归，单测 +3**。细节见 archive/m4-slice-livecollection-merge-filter.md。**遗留**：insertBefore/dataChange >420s（per-subtest testFn 成本，M1 L2）+ detached 容器强引用缓涨（TBD WeakRef）
7. **R56（已 land `ac7240f5`）**：**M8 path-objects 接手首切片**——用例导入 205 + roundRect panic 消除（total_cmp）+ 语义/几何对齐 + 扫描线段式迭代，139→156 pass。细节见 archive/m8-slice-path-objects-roundrect.md。
8. **下轮候选（按 ROI）**：
   - **(a) M1 L2 主线开刀**：M8 已 200P/3F（剩 3 深项记清单非轻量切片）——转 polyfill 桥读 live Document 最小只读子集（getElementById/querySelector/getAttribute 读 live Document，写仍走旧路径；R43 诊断文档 + A/B 门 + R51b/R55 反链缓存是设计输入），验证三方合一管线。
   - (b) M8 续作：arc/arcTo 形状精度族（~14 fail；并行流 `afd8ec08` 已修角度归一化，剩 arc.shape.5/curve2 巨 lineWidth 端点覆盖 + bezier/quadratic shape）。
   - (c) M1 L2 正题开刀：insertBefore/dataChange 的 per-subtest testFn 成本（R52 已把 setup 侧修到恒定；R43 诊断文档 + R51b 反链/overlay 是 L2 方向实质推进；下一步「childNodes/parentNode 读缓存于 JS 侧 registry（写时维护）消 host 往返」最小切片——R55 基底缓存已是此方向第一刀）。
   - (d) mutations 语义面（Range 端点 identity：append 同位节点后 startContainer 期望旧 proxy）。
   - (e) Event-dispatch 剩余（bubbles-true/false 各 3F 独立 Document 深结构）。
   - (f) 双路径差 6.12pp 收口（WheelEvent 子类链/SubclassedEvent，分散低 ROI）。
   - (g) NamedAttributeMap own 枚举（dom/collections 3 fail，轻量候选）。
   - (h) 主线里程碑：M2 S6（依赖 L2）/ M6 QuickJS native——**S0q 已 land（R57 `b781252f`）**，下一片 S0q 续（weak/finalizer 生命周期）或 S1q 只读属性族。
   - **R76 后下一步首选（按 ROI）**：① ~~M6 全量~~（R57–R75）；② ~~quickjs WPT 矩阵基线~~（**R76 已落** `68f09748`——双路径对等 0.02pp + collections 语义缺口修复）；③ **M1 L2 主线**（polyfill-live 合一最小只读子集——R43 诊断 + R51b/R55 反链是设计输入，A/B 门就绪；顺带解 ranges/insertBefore 慢用例根源——per-op re-parse 成本正是 L2 消除对象）；④ M3 SPA/WC 端到端（QuickJS native 已备）；⑤ ~~quickjs 矩阵剩余缺口聚类~~（**R78 已落** `d4eb0641`——execute 全局代码语义修复：traversal 7P→953P 对齐 v8、collections/nodes/events 大幅提升、双路径对等保持；剩余 fail 与 v8 共有，非引擎特有）。

> **R51 轮碰头记录（run-rules §9）**：本轮 session 与并行 rally 流（另一 claude 实例）在同一 clone 上同时工作——并行流 08:18 提交 `b3d8feb2` 时把本 session 正在编辑的工作树（反链/overlay 初版）一并吸收提交。无代码丢失（增量以 `aed92314` 独立 land），但**双 rally 流跑同一 clone** 违反 run-rules §8（双独立 clone）——已由本记录显式标记；若再次发生，暂停一边。

---

## 待用户决策清单（深结构护栏）

| 事项 | 触发条件 | 状态 |
|------|----------|------|
| V8 `ZW_NATIVE_DOM` default-on（改 Mission 级单向门，M5） | M1–M4 完成、V8 native 路径生产就绪 | **方向已获 2026-08-19 用户批复启动**（zero-web ⚡ 块：P1b default-on 拍板，"js-dom 侧同门 = M5 V8 default-on"）；触发条件 M1–M4 完成仍待，M5 启动时点按 zero-web ⚡ 块执行序（先 default-off 全量基线 → 翻开关 → 双流守门 A/B net≥0）执行，无需再征询方向 |
| QuickJS `ZW_NATIVE_DOM` default-on（改 Mission 级单向门，M7） | M6 QuickJS native 移植完成 | 待 M7 启动前征询 |

> 本轮 A/B 对照门骨架为纯测试新增，不触发上述门禁。

---

## 未解决问题

1. ~~**`cargo-llvm-cov` 本地未安装** → dom_bindings 独立 coverage 口径（M0 项 4）~~ → ✅ **R30 已解**：cargo-llvm-cov 0.8.7 + llvm-tools-preview **已安装**（R30 核实推翻 R0 记录）。`scripts/check-dom-bindings-coverage.sh` 落地，基线 源码 93.14% / 全部 95.15%。**提升候选**：dom_exception 71.4% / css_style_declaration 86.7% / custom_elements 89.0%（纯补测试，下轮候选 b）。
2. **canvas path-objects 是热碰撞面**：canvas 流（`f7219b2c` 等）正在活跃编辑 `part05.js` canvas 段 + `canvas.rs`。M8 path-objects 接手须等 canvas 流告段落，或确认 part04/05 path-objects 段无并发编辑后再动。碰撞信号点：`git log --since="14 days" -- crates/engine/src/js_dom_shim/part05.js crates/canvas/src/`。
3. **canvas wpt-data html 子树缺失**：canvas testharness 用例（含 path-objects）需从上游 `web-platform-tests/wpt` 仓库单独导入到 `wpt-data/html/canvas/element/`，`make fetch-wpt-data` 不提供。M8 接手第一动作。
4. **polyfill appendChild/insertBefore 闭环校验架构限制**（R4 发现）：polyfill 桥的 mutation 经 `__zw_append_child` 回调延迟批处理（`apply_dom_mutations` 在脚本执行后 apply），且 shim 层 `_makeProxy` 只有 selector/handle 无 live 祖先链——无法在 `appendChild` 调用点同步抛 HierarchyRequestError。native 路径已修（R4），polyfill 待 M1 L2 polyfill-live 合一（shim 改读 live Document 后才有祖先链）。
5. **testharness-dom 仅测 polyfill 路径**（R4 发现，**R5 已解**）：R5 加 `testharness-dom-native` 入口（ZW_NATIVE_DOM=1）。
6. ~~**native DOMException identity**（R5 定位）~~ → ✅ **R6 已修**：三重根因全部修复。native dom/nodes 41.25% → 56.08% 追平 polyfill（注：R7 .js 依赖补齐后基线真实化为 50.79%，双路径对等）。
7. **PI 用例超时 + ProcessingInstruction 构造器缺失**（R7 发现，R8/R9 持续缓解）：Document-createProcessingInstruction.html 外层 `test()` 嵌套多个 `test()` + `pi instanceof ProcessingInstruction`（构造器未装）→ testharness completion callback 未调 → 超时。**R9 已交付 polyfill createProcessingInstruction + ProcessingInstruction 构造器占位**（PI 用例 1P/11F→6P/6F，超时不再），但 `instanceof ProcessingInstruction/Node` 仍 false（polyfill Proxy instanceof 需 getPrototypeOf 深改，属 instanceof 89 块，下轮候选 a）。
8. **基线真实化**（R7→R8→R9 持续）：R7 补 .js 文件 → 51.12%/50.79%；R8 让 .js 运行时内联执行 → 37.82%/37.59%；**R9 PI 桥接 + DOMException identity 对等** → polyfill 38.03% / native 37.81%（双路径对等差 0.22pp）。
9. **用例侧 document 始终是 polyfill document（R9 发现）**：即使 `ZW_NATIVE_DOM=1`，顶层 `globalThis.document` 仍是 polyfill shim 装的（part06.js）。native document template（dom_bindings 装的方法）用例访问不到。故 polyfill document 必须实现所有 document.* 方法，native dom_bindings document 方法仅作生产路径（default-on 后）能力。**对等含义**：driving 用例经 polyfill document 跑，native 路径修复须经 polyfill document 同步才能基线可见（R4 注释的架构限制的另一面）。
10. **DOMException identity 对等**（R9 修复）：native_dom 叠加路径下，shim 裸 `new DOMException(...)` 抛的异常 → testharness `assert_throws_dom` "wrong global"（词法作用域 part01b DOMException ≠ 全局 native DOMException）。R9 已修 createElement/PI 校验路径（改 `globalThis.DOMException`）。**R3 createElement 既存对等 bug 同步修**（但其 0P/147F 主因是 instanceof Element + iframe，非 DOMException）。其余 shim 抛 DOMException 路径（如需）应同样用 `globalThis.DOMException`。
11. **6 个 fetch/response 测试既存失败**（R10 归因，并行流引入）：`test_fetch_abort_signal_r3044`/`test_fetch_forbidden_headers_r3221_r3222`/`test_fetch_response_binary_body_r3021`/`test_request_signal_passthrough_r3045`/`test_response_body_readable_stream_r2967`/`test_response_request_constructors_r2968`。clean R9 tree 同样失败（非 js-dom 流引入）。根因 `instanceof Response`=false（fetch 结果非 Response 实例，`_makeResponseFromWire` 路由问题）+ fetch abort/binary/stream/signal/forbidden-headers。**归因 fetch/net 域**（并行 canvas/net 流引入），非 js-dom DOM 桥工作面（run-rules §9 工作面不重叠）。修复需深入 fetch 桥接，留给引入它的流或专项切片。注：本切片修了同源的 CSS.escape 回归（part06 工作面内，canvas 流 part05 CSS.percent/deg 破坏 part06 CSS 定义顺序）。
12. **instanceof 具体元素子类剩余**（R10 部分解，**R11 已修**）：R10 解了 instanceof Element/HTMLElement/Node（cloneNode 0P→51P）；**R11 注册 ~64 HTML*Element 构造器 + tag 映射，getPrototypeOf 按 tag 返子类 prototype**，cloneNode 51P→121P。**剩余边缘**：`createElement('canvas') instanceof HTMLCanvasElement` 仍 false——canvas 经 `_zwMakeCanvas()` 特殊 proxy（canvas 流专用路径），不走 _makeProxy/getPrototypeOf。canvas proxy instanceof 待 canvas 流或专项协调。
13. **iframe.contentDocument 是当前最大失败块，深结构跨面改（R12 评估确认）**：createElementNS/case/cloneNode 用例 XML/XHTML iframe document 路径 `Cannot read properties of undefined (reading 'documentElement')`（~390 subtest）。R12 评估：完整需求 = iframe src 真实解析 + 独立 Document + 独立 window（`doc.defaultView.DOMException`）+ 跨文档节点归属（`element.ownerDocument === doc`）+ createElementNS 子文档工作。属 **html-compat 域深结构**（跨文档 + iframe 解析），远超 js-dom 轻量切片。待评估为独立切片或转 html-compat 流。R12 转 prefix/getElementsByTagNameNS 零碰撞面切片。
14. **prefix getter 大写化限制（R12）**：polyfill `_realTag` 强制大写（HTML 语义），createElementNS('x','Abc:local') 的 prefix 返 'ABC'（大写）。case.js 测 abc/Abc/ABC 三态 prefix，仅 ABC 态匹配，abc/Abc 仍 fail。正确修复需 createElementNS 保留原 qualified name 大小写（深改 host `__zw_create_element`/tag 存储，目前 createElement 统一小写、_realTag 统一大写）。待 createElementNS 大小写深改切片。（注：R18 createElementNS 改经 `__zw_create_element_ns` 已解 case.js 三态，本条仅影响 createElement 非 NS 路径的 prefix，低优先级）
15. ~~**native dom/events dispatchEvent hang（R21 发现）**~~ → ✅ **R22 已修（更正 R21 误报）**：非系统性 dispatchEvent hang。R22 经诊断插桩（native dispatchEvent+polyfill __zw_parent 均 0 调用）+ 直接 binary 单用例（Event-dispatch-click 10P 正常）+ 逐用例 timing，定位到**唯一真死循环** = `Event-timestamp-safe-resolution` 的 `do-while(e2.timeStamp-e1.timeStamp==0)`——根因 native `Event.timeStamp` 恒 0（event.rs 硬编码）vs polyfill 用单调 perf timer。R22 修复 native timeStamp 改 `perf_now_ms()`（OnceLock<Instant>）。native dom/events 全量 570s 超时→96s，基线建立 31.29%（双路径对等差 0.32pp）。**注**：`make testharness-dom-native` 现可跑完（~96s events + nodes），10 个慢用例 ~10s each 命中 CASE_TIMEOUT 正常超时（非缺陷）。

---

16. **主文档 proxy 域 remove 的同步语义缺口（R254 单测首轮实证）**：proxy 域（`document.createElement` → `_wrapHandle` 产物）的 `remove()` 经 `__zw_remove(sel/handle)` host 回调**异步批处理**，JS 侧 `np.parentNode` 不即时置 null、旧父 `childNodes` 融合视图不变（iframe 工厂域 own removeChild 已同步，R249）。影响面：surround 等「先摘除再操作」语义在主文档 proxy 域依赖 host apply 后快照融合。WPT 13/14,x 走 iframe 域不受影响（已修）；主文档域的同类形态（如主文档内 createElement 后 surround）为 R255+ 候选——根修需 L2 polyfill-live 合一（M1）后 shim 有 live 祖先链才能同步摘除。

## 归档记录

> 已完成的 milestone/切片记录到 `archive/`。

- R0：M0 A/B 对照门骨架 → [archive/m0-slice-ab-gate-skeleton.md](archive/m0-slice-ab-gate-skeleton.md)
- R1：M4 WPT dom/nodes 基线首切片 → [archive/m4-slice-wpt-dom-nodes-baseline.md](archive/m4-slice-wpt-dom-nodes-baseline.md)
- R2：M4 classList DOMException 修复（双路径 + native DOMException 构造器）→ archive/m4-slice-classlist-dom-exception.md
- R3：M4 createElement 非法标签名校验（双路径 + spec Name production helper）→ archive/m4-slice-create-element-validation.md
- R4：M4 native node mutation DomError→DOMException（append/insert/remove/replace）→ archive/m4-slice-node-mutation-dom-exception.md
- R5：M4 testharness-dom native 路径对照 + native DOMException constructor 修复（部分）→ archive/m5-slice-native-baseline-domexception-constructor.md
- R6：M4 DOMException identity 三重根因修复（native 追平 polyfill 56.08%）→ archive/m6-slice-dom-exception-identity.md
- R7：M4 fetch .js 基线真实化 + native createProcessingInstruction API → archive/m7-slice-js-deps-createprocessinginstruction.md
- R8：M4 testharness 本地 .js 内联 + wpt_root 贯穿合并（基线真实化 178 用例 / 4490 subtest）→ archive/m8-slice-testharness-local-js-inline.md
- R9：M4 polyfill createProcessingInstruction 桥接 + DOMException identity 对等（PI 1P/11F→6P/6F）→ archive/m9-slice-createprocessinginstruction.md
- R10：M4 polyfill Proxy getPrototypeOf 解 instanceof + CSS 回归修复（cloneNode 0P→51P）→ archive/m10-slice-instanceof-prototype-chain.md
- R11：M4 element.localName getter + HTML 元素子类 instanceof（cloneNode 51P→121P）→ archive/m11-slice-localname-subclass-instanceof.md
- R12：M4 element.prefix getter + getElementsByTagNameNS（polyfill +37 pass）→ archive/m12-slice-prefix-getelementsbytagnamens.md
- R13：M4 classList token 去重 + contains 空白不抛（classlist +220 pass，迄今最大单切片）→ archive/m13-slice-classlist-dedupe-contains.md
- R14：M4 createEvent alias 全覆盖 + 未知 type 抛 NotSupportedError（createEvent +168 pass，dom/nodes 突破 50%）→ archive/m14-slice-createevent-aliases-notsupported.md
- R15：M4 implementation.createDocumentType + detached doc implementation（createDocumentType +79 pass）+ 修 canvas 流 2 clippy 红灯 → archive/m15-slice-createdocumenttype.md
- R16：M4 classList toggle no-op + write runUpdate + replace 顺序（classlist +40 pass）→ archive/m16-slice-classlist-toggle-replace.md
- R17：M4 createEvent 移除 9 non-createable modern interface（createEvent +9 pass）+ 核实 event target null gap 不存在 → archive/m17-slice-createevent-noncreateable.md
- R18：M4 createElementNS 大小写敏感（解 R12 case.js 三态）+ namespaceURI getter（polyfill +1.91pp / native +1.53pp）→ archive/m4-slice-createelementns-case-sensitive.md
- R19：M4 classList replace 校验顺序/去重/mutation + assignment readonly + identity 缓存（Element-classlist.html 100%，各 +20 pass）→ archive/m4-slice-classlist-replace-and-assignment.md
- R20：M4 testharness PRECONDITION_FAILED/NOTRUN 中性 status 精确化（createEvent 6 TouchEvent fail→中性，通过率口径 WPT 标准）→ archive/m4-slice-testharness-precondition-status.md
- R21：M4 导入 dom/events 子目录建立 polyfill 基线 31.61%（54 个 0-pass 事件分发缺口）+ 发现 native events dispatchEvent hang（R22 首要）→ archive/m4-slice-dom-events-baseline.md
- R22：M4 native Event.timeStamp 死循环修复（更正 R21 误报，timeStamp 恒 0→perf_now_ms）+ native dom/events 基线 31.29%（96s，双路径对等差 0.32pp）→ archive/m4-slice-native-event-timestamp-hang.md
- R23：M4 Event eventPhase 常量（NONE/CAPTURING_PHASE/AT_TARGET/BUBBLING_PHASE，Event-constants 双路径 100%，各 +4 pass）→ archive/m4-slice-event-phase-constants.md
- R24：M4 事件子类 init 属性父链继承 + KeyboardEvent 工厂化（Event-subclasses polyfill 42P/49，dom/events polyfill 42.58% +30P / native 36.13% +11P，双路径差扩至 6.45pp）→ archive/m4-slice-event-subclass-parent-chain.md
- R25：M4 native MouseEvent/KeyboardEvent view + KeyboardEvent which（native 正确性净正，双路径差 6.45pp 保持未缩——WheelEvent 子类链/SubclassedEvent 等多点分散，转高 ROI）→ archive/m4-slice-native-event-view-which.md
- R26：M4 polyfill Event.cancelBubble（stop propagation flag 公开镜像，Event-cancelBubble 双路径 4P/8，dom/events 各 +9 pass；Event-dispatch 深结构评估转轻量）→ archive/m4-slice-event-cancel-bubble.md
- R27：M4 polyfill EventListener handleEvent（对象 listener 调 .handleEvent，EventListener-handleEvent 双路径 3P/5，dom/events 各 +3 pass）→ archive/m4-slice-event-listener-handle-event.md
- R28：M4 polyfill Event.returnValue（defaultPrevented legacy 镜像，Event-returnValue 双路径 7P/7 100%，dom/events 各 +9 pass，polyfill 突破 49.68% 接近 50%）→ archive/m4-slice-event-return-value.md
- R29：M4 polyfill Event.cancelBubble setter dispatch 副作用（data 字段→defineProperty getter/setter 后端 _propagationStopped，dispatch finally 重置 flag，Event-cancelBubble 双路径 8P/8 100%，dom/events 各 +6 pass，polyfill 突破 52%）→ archive/m4-slice-event-cancel-bubble-setter.md
- R30：M0 项 4 dom_bindings 独立 coverage 口径收口（cargo-llvm-cov 已装核实 + check-dom-bindings-coverage.sh lcov 解析源码/测试分离 + check-coverage.sh --dom-bindings 集成 + 基线源码 93.14%/全部 95.15%，M0 里程碑 6/6 项全完成）→ archive/m0-slice-dom-bindings-coverage.md
- R31：M0 dom_bindings coverage 提升（DC-4）+ 真 bug 修复（new DOMException() name 缺省 undefined→Error）+ 9 单测（DOMException 构造/code 表/toString/legacy/identity + CE lifecycle 派发 + CSS getPropertyPriority/item/deleter），源码 93.14%→94.27% +1.13pp（dom_exception 71.4%→94.4% / css 86.7%→92.7%）→ archive/m0-slice-dom-bindings-coverage-lift.md
- R32：M4 Event.srcElement（target legacy IE 别名，spec dom-event-srcelement）双路径（polyfill defineProperty getter 读 this.target / native data 属性 init+dispatch 同步）+ native 单测 + fetch-dom-subset 幂等快路径（event-src-element-nullable 双路径 0P→全 Pass，避免 GitHub 60/h 限流）→ archive/m4-slice-event-src-element.md
- R33：M4 Window.event（HTML current event legacy global，spec window-event）polyfill 双点（part01 init globalThis.event=undefined writable+enumerable / part03 _dispatchWithBubble try 前 set + finally restore 嵌套 dispatch 正确）+ polyfill 单测（5 场景含嵌套 restore）；native 不改（未解问题 #9 用例侧走 polyfill）（event-global 双路径 0P→5P/7F，dom/events polyfill +15/native +8 pass）→ archive/m4-slice-window-event-global.md
- R34：M4 AT_TARGET stopPropagation 止同元素 listener + 双 stop-flag 兼容（_dispatchToListeners 'all' non-capture 加 stopped() 早退 + 双 flag helper _propagationStopped||__zw_stop + _dispatchWithBubble bubbleStopped + finally 重置 native flag）+ polyfill 单测 4 场景；顺带修 canvas 流 c1e0bcc8 clippy unnecessary_cast 红灯（Event-stopPropagation-cancel-bubbling 双路径 0P→全 Pass，dom/events polyfill 53.52%/native 47.25%）→ archive/m4-slice-at-target-stop-propagation.md
- R35：M4 eventPhase 反映 dispatch 阶段（Event-dispatch 系列最小子集；_dispatchToListeners 按 phase 设 eventPhase capture1/AT_TARGET2/bubble3 + _dispatchWithBubble finally 复位 eventPhase=0+currentTarget=null）+ polyfill 单测（三阶段+复位+currentTarget null）（Event-dispatch-order-at-target 双路径 0P→全 Pass，dom/events polyfill 54.13%/native 47.87%）→ archive/m4-slice-event-phase-during-dispatch.md
- R36：M4/DC-4 dom_bindings coverage 提升（node mutation 错误路径 try/catch cycle→HierarchyRequestError/非 child→NotFoundError + aria/role IDL 反射 idl_to_attr 全分支；Event-dispatch doc/window 入 chain 诊断深结构跳过 + namespaceURI 核实已完成勘误）（源码 94.27%→94.35% / node.rs 91.6%→92.3%）→ archive/m4-slice-node-mutation-error-paths-coverage.md
- R37：M4/DC-3 导入 dom/collections 子目录建立基线（纯资产零源码逻辑改动；testharness.rs DOM_TEST_SUBDIRS + fetch-dom-subset.sh SUBDIRS 同步加 dom/collections + fetch 10 .html）（polyfill/native 双路径 22.9%（11P/37F/1timeout，49 subtest，双路径对等差 0pp）；失败聚类 HTMLCollection own 属性枚举 ~19 fail 主力，R38 候选）→ archive/m4-slice-dom-collections-baseline.md
- R38：M4/DC-3 HTMLCollection namedItem 空串守卫 + Element.children 返 HTMLCollection（own 属性枚举诊断深结构转 empty-name 轻量；part05 namedItem if(n==='')return null + part04 children _zwMakeCollection(true) + polyfill 单测）（HTMLCollection-empty-name 双路径 1P/6F→7P/0F 100%，dom/collections 22.9%→35.4%，dom/events 净 +1 零回归）→ archive/m4-slice-htmlcollection-nameditem-empty.md
- R39：M4/DC-3 pre-set stop propagation flag → dispatch 零触发（Event-dispatch 系列最小子集；完整 document/window 入 chain 诊断深结构——host parent_selector_for('html') 返空 + 三合一 _elKey('html') key + 12 派发点；part03 _dispatchWithBubble try 开头 bubbleStopped() 早退 + polyfill 单测 4 场景）（Event-dispatch-propagation-stopped + bubble-canceled 双路径各 0P/1F→1P/0F 100%，dom/events polyfill 54.74%/native 48.62% 各 +2 同步，dom/nodes 零回归）→ archive/m4-slice-preset-stop-flag-zero-dispatch.md
- R40：M4/DC-3 document/window 入派发链（槽位标记+虚派发站，不拆三合一 key；html target 站防双 fire + detached 边界 + once 原始数组过滤）（multiple-stopPropagation + omitted-capture 双路径 100%，bubbles-true/false 主路径 Pass，dom/events polyfill 57.62%/native 51.52% 各 +10，零回归）→ archive/m4-slice-doc-win-dispatch-chain.md
- R41：M4/DC-3 导入 dom/traversal 基线 + TreeWalker/NodeIterator API 面修复（NodeFilter 常量全表 + readonly + root TypeError + toString branding + currentNode setter + extract_script_src 无引号支持）（NodeFilter-constants 双路径 100%，dom/traversal 首基线 polyfill 16.36%/native 14.55%，detached 树深结构 ~30 fail 属 M1 L2，零回归）→ archive/m4-slice-dom-traversal-baseline.md
- R42：M4/DC-3 导入 dom/ranges 基线 + Range/StaticRange/Attr 端点 API 面（getAttributeNode 真 Attr + new Range 实例 + StaticRange 构造器 + spec 校验）（dom/ranges 双路径 9.18%→39.80% +30 对等 0pp，四子目录零回归，剩余 iframe/ShadowRoot/comparePoint 深结构）→ archive/m4-slice-dom-ranges-baseline.md
- R43：M4+M1 HTMLCollection legacy 属性语义（indexed/named configurable:false + 数字 id 跳过，collections 35.4%→43.75%）+ M1 L2 live 视图诊断（实现-实测-回退，nodes -10 跨文档根因归档）→ archive/m4-slice-htmlcollection-legacy-props.md + evidence/2026-08-14-r43-l2-live-view-diagnosis.md
- R44：M4/DC-3 NamedNodeMap supported property names（ownKeys/getOwnPropertyDescriptor/named getter；collections 43.75%→50.00% 过半，nodes +4；querySelector-mixed-case detached 根因诊断归档）→ archive/m4-slice-namednodemap-own-enumeration.md
- R45：M4/M2 MutationObserver 语义（批内 id 追链 + attributeOldValue 预捕获；attributes 0 崩→30P、nodes +32；S6 正体依赖 L2 诊断归档）→ archive/m4-slice-mutation-observer-semantics.md
- R46：M4/DC-3 MutationObserver NS record + no-mutation 语义 + classList record 对齐（attributes 用例 100%，nodes 2547P；classlist 回归同轮修复维持 100%）→ archive/m4-slice-mo-ns-and-record-semantics.md
- R47：M4/DC-3 childList fragment 展开 record + sibling 字段 + remove record + surroundContents 顺序解耦（childList 10→15P，nodes 2552P；renderer R2930 两起回归同轮修）→ archive/m4-slice-mo-childlist-records.md
- R48：M4/DC-3 parsed DOM CharacterData（SetChildText 全链 + observe 父回落；characterData 用例 100%，nodes 2566P；R45-R48 MO 族双 100%）→ archive/m4-slice-parsed-characterdata.md
- R49：M4/DC-3 observe options 校验/隐含启用 + textContent childList 语义 + 注册文本可编辑（sanity/takeRecords/attributes/characterData 四用例 100%，childList 18P/1F，nodes 2579P；R45-R49 五轮 nodes +72）→ archive/m4-slice-mo-options-and-textcontent.md
- R50：M4/DC-3 HTMLCollection Proxy 承载 + live overlay（collections 双路径 48P/0F 全灭、nodes +29、traversal +18；R37 聚类深结构主簇闭合）→ archive/m4-slice-htmlcollection-proxy.md
- R51：M4/DC-3 TreeWalker NodeFilter 语义 + new Document() 解锁 dom/common.js（traversal +889/native 893P、nodes +449、ranges 非 mutations +1808；acceptNode-filter 100%）→ archive/m4-slice-treewalker-filter-document-ctor.md
- R51b：M4/DC-3 dom/ranges mutations 族死循环三根因修复（child→parent 反向链 + childNodes pending overlay/视图融合 + pre-insert HierarchyRequestError；mutations 族无限 spin→跑完 586P，四子目录零回归；单测 part18 六件）→ archive/m4-slice-ranges-mutations-hang-roots.md
- R51c：M4/DC-3 pending 差量表 O(n²) 膨胀消除（消零语义 + 父分桶 + 512 压实 + querySelector pending-id 回落 + registry/子树注销补齐；增速消除、nodes +47/traversal +28、mutations 族 586P 零回归；单测 part18 +2）→ archive/m4-slice-pending-table-growth.md
- R54：M4 live collection els 泄漏「并入点过滤」（R53 收口；`_zwMutationInDoc` 挂载点判定 + 失效循环/构建期两处过滤；deleteData 2.0x、四子目录零回归、单测 +3；commit `ba775545`）→ archive/m4-slice-livecollection-merge-filter.md + evidence/2026-08-15-r54-livecollection-merge-filter.json
- R56b：M8 path-objects 回归收口（arc 方向归一化 + line-to-arc-start + roundRect 非有限 + DOMPoint NaN + stroke closed 末命令；真基线 62F→37F 净 +25 零回归；commit `b1446e9b`）→ archive/m8-slice-path-objects-regression-closure.md + evidence/2026-08-15-r56b-path-objects-regression-closure.json
- R56c：M8 nonzero fill rule + fillRule 透传 + roundRect 镜像绕组（fill_rule_spans 方向感知扫描线 + WebIDL 前置省略嗅探；path-objects 168P/35F 零回归；commit `bd027c98`）→ archive/m8-slice-nonzero-fill-rule.md + evidence/2026-08-16-r56c-nonzero-fill-rule.json
- R56d：M8 isPointInPath 族 nonzero 对齐（fill_rule_spans 复用 + Inf 收缩/钳制 + 整圆 arc 自包含子路径 + WebIDL 校验；path-objects 172P/31F 零回归；commit `16f0d885`）→ archive/m8-slice-ispointinpath-rule.md + evidence/2026-08-16-r56d-ispointinpath-rule.json
- R56e：M8 ensuresubpath 族 + clip 相交/空 + ellipse 负半径（lineTo=moveTo / 曲线第一控制点照画 / clip_paths Vec AND / IndexSizeError；path-objects 181P/22F 零回归；commit `eab37ea1`）→ archive/m8-slice-ensuresubpath-clip.md + evidence/2026-08-16-r56e-ensuresubpath-clip.json
- R56f：M8 arcTo 切线弧中心修正 + 各向异性 CTM + shim 守卫补齐（共享几何统一 flatten 双实现 + 圆心候选恒等；path-objects 186P/17F 零回归；commit `9ef44a8d`）→ archive/m8-slice-arcto-tangent-transform.md + evidence/2026-08-16-r56f-arcto-tangent-transform.json
- R56g：M8 弧自适应段数 + 真圆环带 + 用户空间 CTM 弧（三类独立缺陷逐一修 + annulus 后处理；path-objects 192P/11F 零回归；commit `d0e93adc`）→ archive/m8-slice-arc-annulus.md + evidence/2026-08-16-r56g-arc-annulus.json
- R56h：M8 曲线段数自适应 + 像素方形覆盖（N=clamp(折线长/8) + half+0.5 判定；path-objects 196P/7F 零回归；commit `e332878f`）→ archive/m8-slice-curve-adaptive.md + evidence/2026-08-16-r56h-curve-adaptive.json
- R56i：M8 stroke 零长段剪除 + roundRect ClosePath + 环绕 join（spec prune + 命令层闭合 + join 环绕；path-objects 200P/3F 零回归；commit `c218f196`）→ archive/m8-slice-stroke-prune-roundrect.md + evidence/2026-08-16-r56i-stroke-prune-roundrect.json
- R57：M6 S0q QuickJS 原生绑定骨架 PoC（quickjs_dom_bindings + install_native_bindings_quickjs escape-hatch + webview 接线 + rquickjs HRP/This 踩坑经验；engine quickjs 1418 / webview quickjs 552 / v8 双绿零回归；commit `b781252f`）→ archive/m6-slice-s0q-quickjs-native-skeleton.md + evidence/2026-08-16-r57-quickjs-s0q-poc.json
- R58：M6 S1q QuickJS 只读属性族（className/namespaceURI/localName/textContent + helper 沉淀 + 双引擎同断言面测试；engine quickjs 1419 / webview quickjs 552 零回归；commit `84c76b25`）→ archive/m6-slice-s1q-readonly-family.md + evidence/2026-08-16-r58-quickjs-s1q-readonly-family.json
- R58b：M6 S1q 续字符串反射族（title/lang/accessKey 共享 helper + 静态映射；engine quickjs 1419 零回归；commit `b9136b38`）→ archive/m6-slice-s1q-readonly-family.md（同文件追加记录）
- R59：M6 S2q 属性方法族（get/set/remove/hasAttribute + PoC 全闭环；engine quickjs 1419 零回归；commit `fe0a23d5`）→ archive/m6-slice-s2q-attribute-methods.md + evidence/2026-08-16-r59-quickjs-s2q-attribute-methods.json
- R60/R60b：M6 S2q 写入族（textContent setter LegacyNull + create/append/remove mutation 族 + get_or_build_node_value 统一入口；engine quickjs 1419 / v8 2153 零回归；commit `be577323` / `b807e5e9`）→ archive/m6-slice-s2q-mutation-family.md + evidence/2026-08-16-r60-quickjs-s2q-textcontent-mutation.json
- R61：M6 S2q 续树读回（childNodes Array 形态/parentNode/firstChild/lastChild + PoC 双向闭环；engine quickjs 1419 / v8 2153 零回归；commit `784aab22`）→ archive/m6-slice-s2q-tree-read.md + evidence/2026-08-16-r61-quickjs-s2q-tree-read.json
- R62：M6 S3q 查询族（qs/qsa 全局工厂 + 元素级方法 + 跨工厂 identity；engine quickjs 1419 / v8 2153 零回归；commit `e438af32`）→ archive/m6-slice-s3q-query-family.md + evidence/2026-08-16-r62-quickjs-s3q-query-family.json
- R63：M6 S4q EventTarget（LISTENERS 存储镜像 V8 + target 站派发 + this 绑定 API 发现；engine quickjs 1419 / v8 2153 零回归；commit `21f6c4b2`）→ archive/m6-slice-s4q-eventtarget.md + evidence/2026-08-16-r63-quickjs-s4q-eventtarget.json
- R64：M6 S5q customElements 五件套 + lifecycle（Rust CE_REGISTRY + set_prototype upgrade + conn/disc 派发；engine quickjs 1419 / v8 2153 零回归；commit `243811c6`）→ archive/m6-slice-s5q-customelements.md + evidence/2026-08-16-r64-quickjs-s5q-customelements.json
- R65：M6 S5q 深化（完整 ctor 执行 this 绑定普通调用 + attributeChangedCallback；construct 语义发现；engine quickjs 1419 / v8 2153 零回归；commit `7a486499`）→ archive/m6-slice-s5q-ctor-attrchanged.md + evidence/2026-08-16-r65-quickjs-s5q-ctor-attrchanged.json
- R66：M6 S4q 完整化 DOMException 错误路径（throw_dom_exception + dom_error_name 映射 + mutation/createElement/ce.define 错误路径全接线 + PoC e.name 断言；engine quickjs 1419 / v8 2153 零回归；commit `4024e525`）→ archive/m6-slice-s4q-domexception.md + evidence/2026-08-16-r66-quickjs-s4q-domexception.json
- R67：M6 S4q 完整化三阶段 capture/bubble 派发（visits 模型 + stop 注入 + listener_present + 复位/返值语义；engine quickjs 1419 / v8 2153 零回归；commit `a8fa85b5`）→ archive/m6-slice-s4q-three-phase.md + evidence/2026-08-16-r67-quickjs-s4q-three-phase.json
- R68：M6 S5q 完整化 observedAttributes 过滤 + oldValue 写前捕获（setAttribute old 先读后写 + removeAttribute 派发 new=null + ctor 数组过滤；engine quickjs 1419 / v8 2153 零回归；commit `d6dc00d5`）→ archive/m6-slice-s5q-observed-attrs.md + evidence/2026-08-16-r68-quickjs-s5q-observed-attrs.json
- R69：M6 S1q 复合对象首片 attributes NamedNodeMap 面（二级身份缓存 + 五方法 + attributeChangedCallback 派发 + reset；engine quickjs 1419 / v8 2153 零回归；commit `18843344`）→ archive/m6-slice-s1q-attributes.md + evidence/2026-08-16-r69-quickjs-s1q-attributes.json
- R70：M6 S1q 复合对象续 classList DOMTokenList 面（二级身份缓存复用 + 全方法面 + token 校验 all-or-nothing + contains 例外；engine quickjs 1419 / v8 2153 零回归；commit `7e4c63c4`）→ archive/m6-slice-s1q-classlist.md + evidence/2026-08-16-r70-quickjs-s1q-classlist.json
- R71：M6 S1q 复合对象收口 dataset DOMStringMap（JS Proxy 胶水 + Rust 原语四件 + 驼峰镜像 + 三次复用缓存 + delete miss no-op；engine quickjs 1419 / v8 2153 零回归；commit `93eccce4`）→ archive/m6-slice-s1q-dataset.md + evidence/2026-08-16-r71-quickjs-s1q-dataset.json
- R72：M6 S4q 完整化 Event/CustomEvent 构造器（JS 构造器胶水 + Rust 原语 + init 8 字段面 + 实例方法四件 + 常量 + CustomEvent 原型链 + 三阶段派发集成；engine quickjs 1419 / v8 2153 零回归；commit `1a1d8227`）→ archive/m6-slice-s4q-event-ctors.md + evidence/2026-08-16-r72-quickjs-s4q-event-ctors.json
- R73：M6 S4q 完整化 DOMException 构造器 + instanceof 面（JS 胶水三次复用 + legacy code 表 + throw_dom_exception 升级真实例；engine quickjs 1419 / v8 2153 零回归；commit `be431968`）→ archive/m6-slice-s4q-domexception-ctor.md + evidence/2026-08-16-r73-quickjs-s4q-domexception-ctor.json
- R74：M6 S0q 续 weak/finalizer 生命周期实验+结论（API 事实 + 两方案实验回退 + strong/reset 终态结论 + rquickjs finalizer TBD；engine quickjs 1419 / v8 2153 零回归；commit `4efd76aa`）→ archive/m6-slice-s0q-weak-lifecycle.md + evidence/2026-08-16-r74-quickjs-s0q-weak-lifecycle.json
- R75：M6 S5q 完整化 whenDefined 真 pending（CE_WHEN_DEFINED 等待列表 + define flush + Ctx 借用外 drain 教训；engine quickjs 1419 / v8 2153 零回归；commit `4bfa87e3`）→ archive/m6-slice-s5q-whendefined.md + evidence/2026-08-16-r75-quickjs-s5q-whendefined.json —— **M6 全量收口**
- R76：M4 quickjs WPT dom 矩阵基线 + Proxy loose 语义修复（release runner 基建 + 双路径 64.55%/64.53% 对等 0.02pp + collections 38→48 语义缺口修复 v8 零回归；engine 1419/2153 + wpt-runner 106/171；commit `68f09748`）→ archive/m4-slice-quickjs-wpt-matrix.md + evidence/2026-08-16-r76-quickjs-wpt-matrix-baseline.json
- R77：M1 L2 第二刀负结果归档（handle identity 反查三件套验证可行，case.html -8 回退；增量设计输入 3 条）→ evidence/2026-08-16-r77-l2-live-view-identity-diagnosis.md
- R78：M4 quickjs execute 全局代码语义修复（JS_Eval GLOBAL 替代 JS 层直接 eval；traversal 7P→953P 对齐 v8 + collections/nodes/events 大幅提升 + 双路径对等保持；commit `d4eb0641`）→ archive/m4-slice-quickjs-global-eval.md + evidence/2026-08-16-r78-quickjs-global-eval-fix.json
- R79：M4 Node.contains/compareDocumentPosition 全节点形态（JS 侧统一实现 + oracle 导航面一致性顺修；两用例双 100% 四路一致 + nodes +2453；commit `3214d883`）→ archive/m4-slice-contains-comparedocpos.md + evidence/2026-08-16-r79-contains-comparedocpos.json
- R80：M4 createElementNS HTML 大写 tagName + validate-and-extract + Node 常量（HTML-document 变体全通过 187P；nodes +726；commit `f8e75c28`）→ archive/m4-slice-createelementns.md + evidence/2026-08-16-r80-createelementns.json
- R81：M4 Document/Element 导航面 + createElementNS 校验 + textContent 语义（Document-createElement-namespace/Node-textContent/Node-properties 三用例 100%；nodes +302 净/traversal +7 净；commit `322cf584`）→ archive/m4-slice-r81-document-navigation.md + evidence/2026-08-16-r81-document-navigation-textcontent.json
- R82：M4 NodeIterator/TreeWalker whatToShow 无符号 + referenceNode 同步（traversal 1195P→1486P +291 净；wrapper 被覆盖根因；commit `8a8d458e`）→ archive/m4-slice-r82-whattoshow-unsigned.md + evidence/2026-08-16-r82-whattoshow-unsigned.json
- R83：M4 walker 全 nodeType 掩码 + fresh 起点语义 + handle before/after（nodes +39 净/traversal +41 净；R2803 断言 spec 纠正；commit `d38a9081`）→ archive/m4-slice-r83-walker-mask-before-after.md + evidence/2026-08-16-r83-walker-mask-before-after.json
- R84：M4 traversal 兄弟链断链 + walker 语义四重修复（sibling 同 identity + CDATA/headEl/docEl 兄弟 getter + detach/重入守卫 + filter 剪枝两接口分叉 + root 不 filter + relocated effPos + 并行流 v8+quickjs 组合态编译 unblock；traversal 1527→1556P/24F、nodes +8、零回归；part18 +3；engine v8 2176/quickjs 1427）→ archive/m4-slice-r84-traversal-sibling-chain.md + evidence/2026-08-17-r84-traversal-sibling-chain.json
- R85：M4 TreeWalker 导航式重写 + previousNode 规范镜像（navKids/navSiblings + previousNode 两态 + html 兄弟 doctype；traversal 1556→1565P/15F、nodes +7、零回归；part18 +3；engine v8 2179/quickjs 1427）→ archive/m4-slice-r85-treewalker-navigational.md + evidence/2026-08-17-r85-treewalker-navigational.json
- R86：M4 detached 子树保留其子 + NodeIterator 移除 retarget（物化缓存 + 移除标记 + retarget 通知五形态；traversal 1565→1575P/29F、removal.html 崩溃→执行、零回归；part18 +2；engine v8 2181/quickjs 1427）→ archive/m4-slice-r86-removal-retarget.md + evidence/2026-08-17-r86-removal-retarget.json
- R55：M4 childNodes 基底缓存 + 兄弟对缓存（`_zwChildBaseCache`/`_zwSiblingBaseCache` + 重注册失效脚本；appendChild 2.7x/splitText 1.75x/appendData 1.66x、identity 稳定、四子目录零回归、单测 +2；commit `7936da79`）→ archive/m4-slice-childnodes-base-cache.md + evidence/2026-08-15-r55-childnodes-base-cache.json
- R56：M8/DC-8 path-objects 接手首切片（用例导入 205 + roundRect panic 消除 + 语义/几何对齐 + 扫描线段式迭代；139→156 pass、zero-canvas 783 全绿、commit `ac7240f5`）→ archive/m8-slice-path-objects-roundrect.md + evidence/2026-08-15-r56-path-objects-roundrect.json
- R93：M3 CE 原型方法派发（get trap 原型链回落放宽 + accessor this 语义；WC 断言组 7；四子目录零回归 + nodes 基线勘误；commit `7469a804`）→ archive/m3-slice-r93-ce-prototype-method-dispatch.md + evidence/2026-08-17-r93-ce-prototype-method-dispatch.json
- R94：M3 Proxy-ctor 桥（HTMLElement hook this 注入 + `_ceRunCtor` + 升级/createElement 接线；WC 断言组 8；闭合 R90 ctor 限制；四子目录零回归；commit `d26f8a29`）→ archive/m3-slice-r94-proxy-ctor-bridge.md + evidence/2026-08-17-r94-proxy-ctor-bridge.json
- R95：M3 真实 lit 库 e2e 首切片（vendored lit bundle + constructor 短路 + template.content；e2e 两组件断言；events 净 +46P；异步 update 链诊断归档）→ archive/m3-slice-r95-lit-e2e-first-slice.md + evidence/2026-08-17-r95-lit-e2e-first-slice.json
- R96：M3 get trap 表查原型链污染收口（own-property 判定 + attributesProxy 回落 + _zwMEl NamedNodeMap 视图；nodes 净 +4P 零回归）→ evidence/2026-08-17-r96-wpt-nodes-final.json
- R97：M3 **lit 首渲染落地**（walker 跨树重定位 nextNodeOffOrder + fragment 视图插入展开 + _zwMEl hasAttributes；traversal +2 / nodes fail -24；lit e2e 4 组件全绿；commit `42f303db2`）→ archive/m3-slice-r97-lit-first-render.md + evidence/2026-08-17-r97-lit-first-render.json
- R98：M3 **lit 响应式更新链落地**（define 读 observedAttributes 触发 lit finalize + CE-scoped accessor 派发 get/set + symbol expando；A/B 回归两处收窄归零；lit e2e 组 E，全组 5/5；commit `d90be269d`）→ archive/m3-slice-r98-lit-reactive-update.md + evidence/2026-08-17-r98-lit-reactive-update.json
- R99：M3 **lit 事件链落地 + CI flake 池 ⑤ 修复**（_zwMEl 事件面 handleEvent 协议 + fonts.ready 惰性化；renderer reset 20/20；lit e2e 组 F 全组 6/6——WC 端到端全链闭环，DC-2 第二项实质达成；commit `bfb64b2aa`）→ archive/m3-slice-r99-lit-event-chain.md + evidence/2026-08-17-r99-lit-event-chain.json
- R100：M3 **真实 Vue 3 端到端落地**（vendored vue.global.js + mount/响应式/@click 事件两组 e2e；四层根因：polyfill 幂等安装 + execute 路径 mutation 应用（共享 append-only 队列）+ 跨 execute handle 身份（单调计数器 + selector↔handle 持久反查 + mutation 历史 + apply 持久解析）+ SVG 构造器；nodes 净 +1 零回归；DC-2 第一项实质达成）→ archive/m3-slice-r100-vue-e2e-first-mount.md + evidence/2026-08-18-r100-vue-e2e-first-mount.json
- R101：M3 **Vue reconciliation 深场景 + 全 handle insertBefore wire**（v-if 卸载 + keyed v-for 中间插入 + 清空；lit/WC + Vue e2e 双 feature 验证；M3 主验收面达成）→ archive（见 evidence 表 R95-R101 行）
- R125：M4 **Document-getElementById 动态 id 语义**（12F→0F 双路径 100%，+13 净；查询源时效三层 + 解析节点原型链接 + DOMPurify namespaceURI 回归修复）→ [archive/m4-slice-getelementbyid-dynamic-id.md](archive/m4-slice-getelementbyid-dynamic-id.md)
- R126：M4 **Node-removeChild spec pre-remove 校验族**（28F→9F 双路径 +19 净；四层收口 + target-moved 回归当轮修平 + own-property 递归教训）→ [evidence/2026-08-19-r126-nodes-removechild-validation.md](evidence/2026-08-19-r126-nodes-removechild-validation.md)
- R127：M4 **Node-replaceChild spec replace-child 校验族 + replace 语义**（15F→0F 双路径 100% +16 净；五层收口 + MO 两悬挂 async_test 解锁修平 + 同族教训复发面/位置约束方向性/悬挂非通过三条教训）→ [evidence/2026-08-19-r127-nodes-replacechild-validation.md](evidence/2026-08-19-r127-nodes-replacechild-validation.md)
- R128：M4 **Node-cloneNode 全节点形态族**（主文件 14F→0F 双路径 100% +26 净；八层收口 + 原型链接涟漪收益 + tdz/own-property 范式/涟漪三条教训；剩 svg 4F 源 NS 深结构 + iframe 1F）→ [evidence/2026-08-19-r128-nodes-clonenode-kinds.md](evidence/2026-08-19-r128-nodes-clonenode-kinds.md)
- R129：M4 **CharacterData 方法族 spec 校验 + WebIDL 转换语义**（八文件 44F→0F 双路径 100% +47 净；叶子 mutation HRE + unsigned long 回绕 clamp + LegacyNullToEmptyString + has trap 白名单[拼接 handler 重复键后者胜]；三条 WebIDL/拼接教训）→ [evidence/2026-08-20-r129-characterdata-method-family.md](evidence/2026-08-20-r129-characterdata-method-family.md)- R130：M4 **DOMImplementation doc 族 detached 语义 + crash 用例跑法**（createHTMLDocument 7F→0F 双路径 100% + createDocument 434P 全绿 + baseURI 9P/0F + crash 族 8 文件 Timeout→PASS，nodes +461 净 fail -23 / native +1182；title 子树 + 原型接线族 + documentElement 惰性 getter[按子形态分派保 R112 派发站点] + runner crash 支持 + Node 常量 native 补挂；当轮三回归三修 + 四条身份/导航面/crash 跑法/常量教训）→ [evidence/2026-08-20-r130-domimplementation-doc-family.md](evidence/2026-08-20-r130-domimplementation-doc-family.md)（commit `823ff8b59`）
- R131：M4 **isEqualNode spec 逐类型字段比较**（6F→0F 双路径 100% + compareDocumentPosition 涟漪 6F→0F，nodes +13 净 fail -12 / native +20；_zwIsEqualNode 统一实现 + 属性三元组序无关配对[属性 prefix 不参与] + PI target + doctype 三字段 + Node.prototype 泛型 + 合成 docEl/head/body ns 标注；三条序列化近似/字段面相反/ns 标注涟漪教训）→ [evidence/2026-08-20-r131-isequalnode-spec-fields.md](evidence/2026-08-20-r131-isequalnode-spec-fields.md)（commit `21ca74921`）
- R132：M4 **importNode spec 语义 + detached body 属性族**（5F→0F 双路径 100%，nodes +4 净 fail -5；clone+adopt 两步/浅剥子/Attr 三字段复制/叶子三件套复发面[第二次]/本地 NS 元数据表；三条 clone≠import/三件套复发/plain NS 键空间教训）→ [evidence/2026-08-20-r132-importnode-spec-semantics.md](evidence/2026-08-20-r132-importnode-spec-semantics.md)（commit `0f5b6a5f9`）
- R133：M4 **insertAdjacentElement 入口校验 + sel 子移动深结构评估**（SyntaxError 用例 1F→0F + 涟漪 1F，nodes +2 净；三层入口校验/host 报错异步不可达前置判定/既有 e2e 断言 spec 纠正；sel 子同步 overlay 首版回退——桶并行集契约 + 二次移动试金石 + L2 正解三条教训）→ [evidence/2026-08-20-r133-insertadjacentelement-validation.md](evidence/2026-08-20-r133-insertadjacentelement-validation.md)（commit `9d4968219`）
- R134：M4 **matches NS type selector + [Unscopable] 表 + on* 缓存失效**（matches-namespaced 6F→0F 双路径 100% + remove-unscopable 0P→3P，nodes +9 净 fail -9 / native +9；handle-only 元素 !sel 盲区 / with-proxy unscopables 透出 / 双表缓存同步失效三条教训）→ [evidence/2026-08-20-r134-matches-ns-unscopables.md](evidence/2026-08-20-r134-matches-ns-unscopables.md)（commit `7726902a1`）
- R152（并行流）：M4 **click() 激活行为三段模型 + 删 R150 遗留死代码**（INPUT checkbox/radio 翻转+input/change 派发+preventDefault 回滚 / form submit·reset / LABEL 转发 / DETAILS toggle / A-AREA hash 导航；`layout_rect_for_selector`+`fill_rect_from_layout_box_pub` 删除）（commit `242b9b7d1`，控制面补账由 R152b 登记——其 session 未附 evidence）
- R152b：M4 **remove-unscopable 全族 + Node-parentElement + Node-lookupNamespaceURI**（3P/3F→6P、9P/3F→12P、0P/1F→75P 双路径；全量净 +81P/-7F 零新增；R134 重编译回读异步快照三重根因/has 白名单缺 prepend·append/document 层豁免表/namespace 查找完整算法；Rust 多行字符串注释+续行坑）→ [evidence/2026-08-21-r152b-unscopables-parentelement-lookupns.md](evidence/2026-08-21-r152b-unscopables-parentelement-lookupns.md)（commit `7c1de07c3`）
- R153：M4 **Element-closest 的 :scope 作用域语义 + :invalid form/fieldset 祖先形态**（24P/5F→29P/0F 双路径；全量 +4P/-4F 零新增；:scope token 文本替换为 start 唯一选择器/HTML selector-invalid 三类匹配补两类祖先形态；核实并行 R152 把 single-activation 削到 85P/47F 并归因四簇）→ [evidence/2026-08-22-r153-closest-scope-invalid-ancestors.md](evidence/2026-08-22-r153-closest-scope-invalid-ancestors.md)（commit `e2a17e0cf`）
- R154：M4 **nearest-activation 止步 + A/AREA click hash 默认动作 + _zwMEl boolean reflected**（single-activation 85P/47F→107P/25F；全量 +22P/-22F 零新增；激活元素五类枚举止步/fragment 导航补 click 入口/plain object 十属性 accessor）→ [evidence/2026-08-22-r154-nearest-activation-a-hash.md](evidence/2026-08-22-r154-nearest-activation-a-hash.md)（commit `92f301bd2`）
- R155：M4 **LABEL 激活转发 + inline handler 时序 + oninput inline**（single-activation 107P/25F→118P/14F；全量 +11P/-11F 零新增；祖先 LABEL 转发/inline onclick 移到翻转后/本地链 oninput 显式执行·proxy 防双触发/connected 三 fallback）→ [evidence/2026-08-22-r155-label-forward-inline-order.md](evidence/2026-08-22-r155-label-forward-inline-order.md)（commit `24702bb89`）
- R166：M1 **L2-d3 RFC 级切片计划 + d3a identity 桥基建**（零行为变化全中性：全量 9516P/347F/18T 双路径 per-file 零差异 = R165 基线；make test 66 套件 18051P/0F）：① RFC v0.1 `docs/specs/p1b-l2d3-unified-matcher-identity-bridge-rfc.md`——四对象域盘点（sel proxy/handle proxy/mutTree 节点/解析 wrapper）+ identity 桥设计（首登记者胜模块级登记表）+ 统一匹配器复用路径（part05 handle 域已是完整本树选择器引擎先例）+ d3a→d3e 切片序；② d3a 落地——`_zwNodeBridgeMap`+`_zwBridgeSet/_zwBridgeGet`（模块级；首版误放 `_makeDetachedDocument` 内致 `_zwMEl` 出口 ReferenceError，acorn 全拼接 shim scope 链分析定位）+ 三登记点（queryBody 直出/fragment real163/`_zwMEl` 工厂出口）+ 5 处树代际 bump 同步清桥 + part22 三单测；③ **方向裁定勘误**：上轮候选「M6 S1q」过时（R75 已收口 M6 全量）；④ 活证据：createElement（handle 域）入 fragment 后 QSA 键匹配 miss——四域分歧运行时样本（d3b 输入）→ [evidence/2026-08-22-r166-l2d3-rfc-d3a-bridge.md](evidence/2026-08-22-r166-l2d3-rfc-d3a-bridge.md)（commit `8d40bb957`）
- R167：M1 **L2-d3b 查询产物归一（identity 桥消费）**（全量 polyfill 9518P/347F/18T 净 +2P F 持平、native per-file 零差异、make test 66 套件 18056P/0F）：① 三查询面（queryBody/Element.prototype/fragment QSA）wrapper 构造前置 `_zwMFindRealNode` 同键查真实节点返桥对象——JSON 往返语义不变仅产物 identity 归一；② 四件配套（全量 A/B 驱动）——matches 整根上下文+强键自身判定（Element-matches 中途 305F 全修回基线 3F）/`_zwMEl.dispatchEvent` 三阶段链派发（proxy 入链不续上行+listenerStore 委托+`_zwOwnerDetDoc` 印章接 doc 站）/listener 槽化 `_zwMEvLs`/ownerDocument accessor（setter own 写）；③ ParentNode-querySelector-All 1936→1942P（+6P/-4F）；④ 负结果——lit e2e 双触发二分（链进 proxy 站）+ bubbles 残留两变体 12vs14（new Document docEl 身份错位，记 d3b2）→ [evidence/2026-08-22-r167-l2d3b-query-unification.md](evidence/2026-08-22-r167-l2d3b-query-unification.md)（commit `5258ab632`）
- R168：M1 **L2-d3b2 body 根归一**（全量 9522P/343F/18T 净 +4P/-4F、bubbles 双文件 10P 回基线、native per-file 零差异、make test 18068P/0F）：① 二分定位——fail 变体 bodyInChain:false（注册 body 站 D 域 wrapper vs 链上 C 域树根身份错位，JSON key.outer 与树根序列化不一致致归一 miss）；② 修复三件（中间回归实证约束）——`_zwMFindRealNode` root-hit 特判（印章+tag 限定；首版 241F）+ 印章只盖根（全树 stamp 139F）+ iframe body 属性落根（`_r159BodyAttrs`，+2F 当轮修平）→ [evidence/2026-08-22-r168-l2d3b2-body-root-unification.md](evidence/2026-08-22-r168-l2d3b2-body-root-unification.md)（commit `9d62b27fa`；**R169 勘误**：R168 的 docs 提交曾误截本文件至 3.4KB，本版本自 `6431f125f` 完整恢复后重做本条记录）
- R169：M1 **master.md 恢复 + d3c 尝试（回退）**（全量 9522P/343F/18T 零行为变化、make test 18071P/0F）：① R168 docs 提交截断事故恢复（441KB→3.4KB，git show 恢复 + 行级编辑重做；learning 沉淀 python 块级 replace 教训）；② d3c gate 启用 905F 回退——根因 iframe doc 双工厂（detached bodyHtml 恒空 vs iframe 工厂持 host 快照，沙箱全命中/runner iframe 全灭对照实证）；d3 前置项确立 iframe/detached 树源统一；③ 保留 `[attr]` 存在性匹配修复（gate 关闭不可达，d3c 重启必要前置）→ [evidence/2026-08-22-r169-master-restore-d3c-attempt.md](evidence/2026-08-22-r169-master-restore-d3c-attempt.md)（commits `ac2a56e97` + `3772390e2` + `f0939b188`）
- R170：M1 **L2-d3c compound gate 落地**（全量 9521P/343F/19T 双路径 per-file 零差异、ParentNode/Element-matches/bubbles 三簇全回基线、make test 全绿）：① R169 iframe 双工厂假设推翻（src-iframe 树正常，R169 探针误用 srcdoc）；② DBG 内探针定位真根因——`_zwWrapCached` key 单一形态假设（真实节点 `.tagName` 使 tag 段空键撞车返空壳）；③ 修两件——key 双形态 + 组合器守卫（`/[\s>+~:]/` 拒绝，解析器吞空白致 `#descendant div` 返 root）；④ gate 保持开启（doc 上下文 compound 消 JSON 往返）→ [evidence/2026-08-22-r170-d3c-compound-gate-live.md](evidence/2026-08-22-r170-d3c-compound-gate-live.md)（commit `0a4146465`）
- R171：M1 **L2-d3d 评估（回退）+ 解析器提取**（全量 9522P/343F/18T 逐计数一致、make test 18087P/0F）：① element 上下文本树前置两轮实验（compound 全形态 35F / 纯 tag 收缩仍 +2-2）均 0 改善 + identity 边缘（`:enabled` qS≠QSA[0]，产物形态时序致归一缓存不同步）——R165 wrapper 依赖结论部分幸存；② 保留 `_zwParseCompoundSel` 共用解析器（行为等价）；③ d3 域定格：doc 本树化落地 / element·fragment JSON 维持（重启前置 = 产物归一统一）→ [evidence/2026-08-22-r171-d3d-evaluation-parser-extraction.md](evidence/2026-08-22-r171-d3d-evaluation-parser-extraction.md)（commit `8a825479b`）
- R172：M4 **ParentNode 33F 聚类 + ns 词法修复**（全量 9522P/343F/18T 中性、zero-dom 852P、make test 18090P/0F）：① 聚类——ns 族 24 / `:lang`·`:target` 4 / tree order 2 / Fragment NodeList 2；② zero_dom 词法修复——`|name` 显式空 ns 前缀在任意段首合法（`#id |div` 后代段旧误判未声明 ns 抛 SyntaxError；`ns|div`/`||` 仍拒；回归 8+3 形态）；③ 匹配层组合链 ns 段正常（probe 实证）；④ ns 24F 剩余 = setup 树碎片化（`getElementById("no-namespace")` null，L2 深结构域）→ [evidence/2026-08-22-r172-ns-lexical-fix.md](evidence/2026-08-22-r172-ns-lexical-fix.md)（commit `d538dee36`）
- R173：M4 **:lang/:target element 文档上下文 + owner 溯源槽**（全量 9532P/333F/18T 净 +10P/-10F、ParentNode 33→29F、Element-matches 3→0F 顺带、native per-file 零差异、make test 18093P/0F）：① 根因——element 查询子树序列化丢 html lang 祖先 + fragment URL；② `_zwOwnerTree` 全树槽（与 root-only 印章解耦）+ `_zwMQueryAll` 注入（:lang 包装/:target URL；detached 无槽天然排除）；③ 全文档源首版 +8 回归回退→包装方案收口；④ Element-matches `[*|TiTlE]` 随槽归零 → [evidence/2026-08-22-r173-lang-target-element-context.md](evidence/2026-08-22-r173-lang-target-element-context.md)（commit `c7e2c2a78`）
- R174：M4 **ns setup 树碎片化统一**（全量 9554P/311F/18T 净 +22P/-22F、ParentNode 29→5F、native 零差异、make test 18107P/0F）：① iframe 工厂 IDL 反射（id/className/outerHTML/removeAttribute/appendChild 对齐）+ ② no-ns 标记协议（`data-zw-empty-ns` 序列化标记 → `Document::apply_empty_ns_markers` 还原，`|div` EmptyNs re-parse 后可命中）+ ③ ns 值语义 A/B 纠正（空串→null 回归 97F 抓回）→ [evidence/2026-08-22-r174-ns-setup-tree-unification.md](evidence/2026-08-22-r174-ns-setup-tree-unification.md)
- R175：M4 **Fragment body 过滤 + new NodeList + iframe kind 三分**（全量 9557P/307F/19T 净 +3P/-4F、ParentNode 5→2F、native 零差异、make test 18118P/0F）：① fragment QSA 子串直发（旧包字面 `<body>` 使 filter_synthetic 保留合成命中）；② R156 kind 判定修正——`.html`/`.xhtml`/其它 三分（.html → contentType 'text/html' 大写路径；.xhtml 保持 XML 语义——WPT XHTML 断言）→ [evidence/2026-08-22-r175-frag-body-newnodelist-html-kind.md](evidence/2026-08-22-r175-frag-body-newnodelist-html-kind.md)
- R176：M4 **Document-createElement-namespace 40F 全收口**（51P/51 全 100%、全量 9597P/267F/19T 净 +40P/-40F 零回归、native 零差异、make test 18123P/0F）：① fetch 脚本补 fixture 子目录 + 空文件放行（empty.* 0 字节是设计形态，旧 test -s 误判）；② kind 四分（.svg → 'image/svg+xml'）→ [evidence/2026-08-22-r176-ns-namespace-fixtures-svg-kind.md](evidence/2026-08-22-r176-ns-namespace-fixtures-svg-kind.md)
- R177：M4 **window.frames + appendChild WebIDL/adopt**（Node-removeChild 28P/28 + Node-appendChild 11P/11 双 100%、全量 9613P/251F/19T 净 +16P/-16F 零回归、native 零差异、make test 18125P/0F）：① frames = Proxy 动态枚举（快照形态 miss lazy iframe）；② removeChild 四修（空文档 docEl 合成 + text ownerDocument + childNodes 叶子视图）；③ appendChild 四修（WebIDL TypeError 前置 + doctype mutation 面 + Document-as-child 拒 + 跨文档 adopt ownerDocument 重指）→ [evidence/2026-08-22-r177-frames-appendchild-webidl.md](evidence/2026-08-22-r177-frames-appendchild-webidl.md)
- R178：M4 **Range 点查询方法族 + Attr 守卫**（Range-attribute-nodes 26P/26 全 100% + comparePoint-2 连带、全量 9624P/239F/20T 净 +11P/-12F、native 零差异、make test 18127P/0F）：① comparePoint/isPointInRange/intersectsNode/compareBoundaryPoints 四方法（root-of 短路族 + WPT 抓回 intersectsNode parent-null 恒 true）；② insertNode/surroundContents Attr 双守卫；③ 跨流 page-runtime clippy 1.98 chunks_exact 修复 → [evidence/2026-08-22-r178-range-point-methods.md](evidence/2026-08-22-r178-range-point-methods.md)
- R179：M4 **node-creation-realm 跨 realm 工厂通道**（13P/13 全 100%、全量 9638P/225F/20T 净 +14P/-14F 零回归、native 一致、make test 18128P/0F）：① Document/DOMImplementation/Range 的 prototype 方法通道（own 优先转发）；② iframe win 构造器补齐 + 节点原型链（Text/Comment/CDATASection instanceof）+ 工厂 attachShadow；③ title 查询两轮回归修正（bodyHtml→detHtml 包装层）→ [evidence/2026-08-22-r179-node-creation-realm.md](evidence/2026-08-22-r179-node-creation-realm.md)
- R198：M4 NodeList length 劫持三形态（Proxy plain-target 承载：expando→原型链 getter→计数解析序 + ownKeys 滤印记 + hasInstance 认 __zwNL）+ strict classic const/let 跨脚本导出（每名 try/catch 包裹——minified bundle IIFE 内零缩进 const 的 ReferenceError 中止整 eval 坑）+ support/ fixture 抓取（六用例 0P/6F→6P/6 全 100%，全量 9758P/111F→9765P/104F 零新增；lit e2e 六测试回归当轮抓回当轮修）→ evidence/2026-08-23-r198-nodelist-length-hijack-const-export.md
- R199：M4 选择器输入预处理 NUL→U+FFFD（css-syntax §5.3.3 三层同步：zero_dom preprocess_selector 三入口 + part05 _parseSelectorListOf 归一 + R181 工厂匹配器 ident 域放宽；escapes NUL 双 subtest 转绿，全量 9767P/102F 零新增、native IDENTICAL；深项记档 lone-surrogate wire 塌缩 2F + :scope query-root 2F）→ evidence/2026-08-23-r199-selector-nul-preprocessing.md
- R200：M4 属性选择器大小写标志 `[attr=value i]`（AttributeSelector.ci + strip_attr_case_flag + 六运算符 ci 比较；case-insensitive 双 subtest 转绿，全量 9767P/100F 零新增、native IDENTICAL；多字节 char 边界 panic 全量当场抓回；MO-document/MO-inner-outer 评估深域记档）→ evidence/2026-08-23-r200-attr-case-flag.md
- R201：M4 strict var accessor 导出（strict 门 + 多行续行 + get/set 双向转发）+ V8 脚本看门狗（script_timeout_ms/terminate_execution/90s runner + native TIME_LIMIT 透传）——dom/ranges mega-case 12 文件解锁，基线 9767P/100F→30142P/24395F（native 逐行 IDENTICAL），fail 文件集零新增 + 5 转绿，+24kF = identity 域 + foreign-doc CharacterData 两大真实缺口簇（R202 输入）→ evidence/2026-08-23-r201-var-accessor-watchdog-megacase-unlock.md
- R202：M4 foreign/detached doc 文本节点 CharacterData 方法面（_zwAttachCharacterDataMethods 三接线 + setter 自递归/内联对象两坑当轮抓回；探针五方法全通；subtest 计数持平零新增——解锁子测试前移到 identity 断言，R203 主输入）→ evidence/2026-08-23-r202-foreign-chardata-methods.md
- R203：M4 Range setStart/setEnd crossing 重设 + _zwRangeBpAfter 边界点序比较（深度感知双 climb 修首版深浅不一误序坑；全量 35453P/19085F 净 +5309P 零新增、native 逐字节相同；Range-set 4153→9297P；剩 compareBoundaryPoints 9313F + foreign 递归 ~1009 记档）→ evidence/2026-08-23-r203-range-crossing-reset.md
- R204：M4 compareBoundaryPoints 四件（WebIDL how 前置 + Range 常量缺失 + 方向位反转 + detached createRange 转发器自递归——探针链 own:false 定位）；簇 9313F→601F，全量 47049P/7489F 净 +11596P 零新增 + 4 文件转绿，native 逐行相同 → evidence/2026-08-24-r204-cbp-webidl-constants-recursion.md
- R205：M4 Range 点查询三方法边界点比较重写（isPointInRange/intersectsNode/comparePoint 统一复用 _zwRangeBpAfter；collapsed 前置 false 移除 + doctype 抛错步骤序两个 spec 纠正，214F 回归当轮抓回）；全量 49534P/5005F 净 +2485P 零新增、intersectsNode 双文件全绿，native 逐行相同 → evidence/2026-08-24-r205-range-point-queries.md
- R206：M4 iframe 子文档脚本执行通道（合并作用域 + per-part 隔离 + 顶层导出 + body onload；探针三坑：wire 分隔符/布局、顶层 throw 截断）；is-not-a-function 双簇消除，计数持平零新增，失败下移 restoreIframe/append 层（R207 输入）→ evidence/2026-08-24-r206-iframe-script-channel.md
- R207：M4 iframe 子文档层四件（append 族 + textContent replace-all + docEl 根 <html> 优先 + docEl 可变面；hasChildNodes/isEqualNode 两轮回归全量逐行 diff 抓回）；restoreIframe 首轮端到端打通，全量 49543P/5004F 零新增、native 逐行相同；R208 靶点 = iframe doc 查询 live-tree 同步 → evidence/2026-08-24-r207-iframe-mutation-face.md
- R218：M4 CDATA splitText（nt=4 继承 + 尾节点保型）+ detached fragment insertBefore（展平递归保 ref）；12,x 栈溢出三探针 clean 记 R219 页面插桩 → evidence/2026-08-24-r218-cdata-splittext-fragment-insertbefore.md
- R217：M4 insertNode Document 子位置规则（「If parent is a Document」四分支——Fragment/Element/DocumentType 三组）→ evidence/2026-08-24-r217-insert-node-document-position-rules.md
- R216：M4 iframe docEl 入 doc 树（25,x setup 前置解锁；doctype 入首位实测 -55 回退——R209 结论在新形态下复核仍成立）→ evidence/2026-08-24-r216-docelement-in-doctree.md
- R215：M4 insertNode ensure-pre-insertion validity（parentNode 环根因——「累积循环」假设被复刻推翻，upwalk chain dump 一行暴露；校验四件 + 帧标志序保持）→ evidence/2026-08-24-r215-insert-node-pre-insertion-validity.md
- R214：M4 iframe documentElement 结构修复（ownerDocument 两形态 + TITLE 根因——无 `<html>` fallback 门控到 XML kind；insertNode 138F createRange 簇根因）→ evidence/2026-08-24-r214-iframe-docelement-structure.md
- R213：M4 extract 收缩偏移修正（spec si+1——6,x positionTests 根因：忠实复刻探针 offsets 一行暴露）+ deleteContents CharData 删侧三段；方法论沉淀「注入真源函数跑完整流程」→ evidence/2026-08-24-r213-extract-collapse-offset-delete-branch.md
- R212：M4 surroundContents CharData 路径闭合（R211 首版 -48 三根因：步骤1 nt=11 漏检 / 工厂 appendChild fragment 展平缺失 / 步骤3 清子缺失；CDATA cloneNode 成对 land；6,x positionTests 残余记 R213）→ evidence/2026-08-24-r212-surround-chardata-path.md
- R211：M4 extractContents CharData 区间分支（三段算法；评估回退 CDATA cloneNode + surround CharData 路径两件——两侧对称性第三例，R212 成对 land）→ evidence/2026-08-24-r211-extract-chardata-interval.md
- R210：M4 surroundContents 步骤 1/2 校验序（InvalidStateError 非部分包含 DFS + InvalidNodeTypeError 步骤序还原；评估回退两件——head/docEl/body 方法面 -9、CDATA cloneNode 需配 host extract 成对 land——记 R211）→ evidence/2026-08-24-r210-surround-invalid-state.md
- R209：M4 iframe/detached 工厂节点方法面 + surround/insert 叶子 newParent 异常链（iframe createTextNode/createElement/fragment/PI 方法面补齐 + doc.doctype + host surroundContents 叶子先变更后抛 HRE + insertNode Text splitText 路径/自引用 HRE + DOMException 实例 legacy 常量双路径；两侧对称性教训——单侧修好使 positionTests 假绿暴露，须同步拉齐另一侧 spec 行为；doctype 入 childNodes 实测 -330P 的零扰动反例）→ evidence/2026-08-24-r209-iframe-node-method-face.md
- R208：M4 iframe doc 查询缓存同键失效（_zwQWrapCache 键 tag\x1fid\x1fouterHTML 撞车——restoreIframe 每轮重建结构全等 div#test，移除后缓存残留使同键查询返已移除节点；三处 removeChild 失效点：body 清缓存+DFS 索引、doc 级同款、_zwMEl 经 owner 溯源槽 bump；零真回归验证法 = 逐 subtest 状态转移比对）→ evidence/2026-08-24-r208-iframe-query-cache-invalidation.md
