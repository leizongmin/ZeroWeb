# R322 Evidence — L2 切片一：查询视图的 handle 子树归并（querySelector/querySelectorAll 的 sel 分支；+1P 零回归）

**日期**: 2026-08-28
**切片**: M4/L2——R322(a) 深结构面专项评估的首个可 land 切片
**改动面**: `part04.js`（querySelector/querySelectorAll 的 sel 分支归并）+ `part24.rs`（r322 归并矩阵回归）

## 一、L2 blast radius 评估结论

探针矩阵（append/insert 两形态 × 视图/查询/选择器三面）确证缺口边界：
**childNodes 视图可见（k=2/3）而 querySelectorAll/querySelector 恒 miss（q=1/m=miss）**——
缺口只在「查询站点」，不在视图层。消费面统计：`__zw_query_all_sub`/`_match_sub` 共 10 调用
点（querySelector/querySelectorAll/getElementsByClassName 三族）；`_childNodeList` 41 处
（视图层已接 overlay）。

## 二、切片设计（R309 教训约束下的保守归并）

- **归并条件精确**：本容器 pending 桶 added 中反链 `parentSel === sel` 的 handle 子，
  `_zwHCCollectSubtree` 展开后经 `_parseCompoundOf`/`_matchCompoundOf`（R2928/R309 既有
  客户端匹配器）过滤。
- **无双计前提（与 R309 innerHTML 域的本质差异）**：handle 子无 sel 字符串，host 快照
  结果（sel 列表）结构上不含它 → 并入与 host 结果无 identity 冲突，无 R309 的
  vue_reconciliation 双计风险。
- **compound-only 限定**：组合器/伪类等 unsupported 形态回落 host 快照语义零变化。
- **单数形态对称**：querySelector host miss 时归并域兜底首个匹配（探针 `m=hit` 实证）。

## 三、过程教训：全量 patch 破坏基线的二分定位

首版把归并逻辑一次性写入 querySelectorAll 的 try 块——**全探针归零**（q/snapQ/qsa 全 0，
连 host 路径都没返回）。stash A/B 确证回归在 patch 内；逐段回退到「删整块」恢复基线后，
改用**增量 land**（parse-only → +桶读匹配计数 → +early return），每步探针+全量验证。
真因未完全定位（三个增量各自安全、合体首次破坏——疑似变量遮蔽或 hoisting 级联），
但增量法让每一步都可回退、可归因。

## 四、A/B

| 项 | R319-R321 基线 | R322 | Δ |
|---|---|---|---|
| 全量 dom sweep | 54140P/58F/22T | **54141P/58F/21T** | **+1P**（WPT 面新增可见性解除一个被 miss 卡住的 subtest）Fail set 恒等 |
| 探针矩阵 | q=1/m=miss | **q=2/3、m=hit** | 归并生效 |
| engine --lib（v8/quickjs）| 2460/1460 | **2461**/1460 | +1（r322 归并矩阵回归）|
| fmt / clippy（v8 guarded + quickjs）| — | 干净/0 | — |
| Timeout diff | — | 1 例单跑 Pass（并发噪声）| — |

## 五、L2 后续切片路线（控制面登记）

1. ~~查询站点 handle 子树归并~~（本切片）
2. **tree-order 归并**（桶子按 nextSibling 定位插入 host 结果序——当前尾并，ParentNode
   tree-order 断言域）
3. **sel 子（R182 重定位形态）归并**（快照内既有节点的同父移动）
4. **视图层完整归一**（_childNodeList 的 sel 父分支与 registry 统一——深水区本体）
