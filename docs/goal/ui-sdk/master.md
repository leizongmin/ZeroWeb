# Zero UI SDK 抽取与浏览器迁移 — 运行时控制面板

> **入口文档**：`docs/goal/ui-sdk.md`（goal contract，稳定）。本文件是持续演进的增量控制面，**不是**一次性交付物。创建第一版只表示治理框架建立，**不表示**核心目标已被覆盖。
> **上游权威**：`docs/specs/ui-sdk-spec-rfc.md`（v1.6.1）。实现具体接口/模块/组件时必须查阅 spec 对应章节。
> **姊妹目标**：`docs/goal/rendering-compat.md`（只读引用，不得改写；其 `make test`/`make reftest`/`make product-smoke` 主线不得因本目标退化）。
> **工作分支**：`ui-sdk`。

---

## Active Milestone

**M2 — 浏览器首批组件迁移 + WebView adapter + text foundation 接入（spec §M2）**。依赖：M1（已完成）。

**✅ M1 UI SDK 核心骨架已收口（2026-07-01）**：23 个新 crate（22 通用 + 1 共享 + 2 耦合点 adapter-webview/chrome）全部存在、可编译、有无窗口单测；`cargo build --workspace` + `cargo clippy --workspace --all-targets -- -D warnings` + `cargo fmt --all` 全净；DC-1 依赖隔离机械验证 PASS（`evidence/dep-isolation-20260630-234530.txt`）；DC-17 coverage 聚合 line 93.00% / function 93.12% / region 93.41%（≥85%，per-crate 除 foundation/text stub 外全部 ≥85%）。M1 过程/决策/证据已归档 `archive/m1-skeleton-complete.md`。

**M2 目标**（按 spec §M2 / goal §Ordered Next Milestones）：
1. 抽取滚动条/按钮/文本输入/菜单/popup/toolbar 到 `ui/widgets`；SearchField/SuggestionList/TabBar 等到 `ui/patterns`；新增 `browser-ui/chrome` 首批领域组件（spec §8.4.1A 映射）。
2. text foundation 真实接入 `ui/render` 与 `zero-webview`（复用 fontdue/swash/rustybuzz，DC-11 / TBD-8）；`ui/render` Scene → render-foundation 后端 trait（TBD-2）。
3. 定义 `BrowserChromeModel` + `BrowserAction` + desktop/tablet/phone shell 共享合约；browser menu/shortcut/context menu 接 `ui/commands`；PermissionPrompt/DownloadPanel/SiteInfoPanel 接 `ui/overlay`；Downloads/Bookmarks/History/TabOverview 接 `ui/collections`。
4. **逐组件灰度迁移**（shim / feature-flag），任意提交点浏览器可运行（DC-14 零退化硬门禁）；涉及渲染/布局变更跑 `make product-smoke`。

**铁律**：M2 触碰浏览器运行时，必须保持 `make test` / `make reftest` / `make product-smoke` 不退化（姊妹目标 `rendering-compat` 主线）。

**🟢 M2 进展（2026-07-01）**：
- **DC-11 phase 1**：foundation/text 真实文本后端（`backend::FontdueBackend`，fontdue+rustybuzz 实现 FontProvider/TextShaper/TextMeasurer + 14 测，含 Ahem.ttf 真实 shaping/measure/换行/caret）。shaping/text_blob/text_measure stub 0→100%。
- **DC-11 phase 2 + TBD-2**：ui/render 接入 foundation/text —— 新增 `RenderPrimitive::TextBlob`（预 shape 文本单元，承载 `TextBlob`）+ `SceneRecorder::draw_text_blob`；新增 `backend::RenderBackend` trait + `paint_scene`（TBD-2：Scene→光栅后端抽象，ui/render 不直接依赖 render-foundation）；scene_snapshot 加 TextBlob 确定性分支；3 个 paint_scene 测试用真实 FontdueBackend shape→TextBlob→mock 后端验证派发。foundation/text 给 TextBlob 加 Serialize 并顶层导出。
- DC-1 复核：ui/render 经 foundation/text 仍**零浏览器业务 crate 依赖**（foundation/text 仅纯文本/字体栈 fontdue/rustybuzz/hashbrown/ttf-parser/unicode-*）。**本步 SDK-only，未触碰 render-foundation/浏览器**（无 product-smoke 风险）。
- **DC-7 批次**：browser-ui/chrome §8.4.1A 实现 6 个领域组件——`PageLoadIndicator`(ProgressIndicator)、`SecurityBadge`(Badge+Tooltip)、`BookmarksBar`(Toolbar)、`BrowserTabStrip`(TabBar)、`FindBar`(TextInputState+StatusBubble)、`AddressBar`(TextInputState+SuggestionList+SecurityBadge)。遵循 props+build+on_activate 模式（由通用 widgets/patterns 组合，输出进 UI scene）；加 zero-ui-patterns 依赖；19 个新测。**SDK-only，未触碰 apps/browser**（无 product-smoke 风险）。
- **DC-7 收尾（12/12）**：ui/widgets 补 FR-009 首批缺失 widget `IconButton`/`ContextMenu`(并入 menu.rs)/`Toggle`；BrowserAction 加 download actions（OpenDownload/CancelDownload/ShowDownload）；chrome 补 `BrowserMenu`(Menu+ContextMenu)、`PermissionPrompt`(DialogScaffold+Toggle)、`SiteInfoPanel`(Popover+ListView+权限切换)、`DownloadPanel`/`DownloadItemView`(Popover+ListView+ProgressIndicator)。**§8.4.1A 全 12 组件 + FR-009 widget 集合补齐**。
- **DC-12**：扩展 `BrowserChromeModel`（tabs/nav/address/security/bookmarks/downloads/permissions/find/page_load，三 shell 共享业务模型）+ `BrowserChromeShell` trait + `DesktopBrowserShell`/`TabletBrowserShell`/`PhoneBrowserShell`（build→WidgetSpec 声明树，layout→ShellLayout safe-area/keyboard 避让）+ `AdaptiveBrowserChrome`（按 ViewportClass/PlatformClass/InputClass 选 shell，§8.4.4A 表）+ 跨 shell 稳定 WidgetId（address_bar/viewport 等，断点切换保留输入状态）。**SDK-only，未触碰 apps/browser**。
- **DC-6 phase-1（M3 启动，与 M2 并行）**：ui/dsl `engine.rs` 表达式引擎落地——三阶段管线 parse（Pratt）/typecheck（推断 ValueType）/eval（EvalContext 确定性、无副作用求值）。覆盖字面量 / `$path` / 算术·比较·布尔·空值合并 / 条件 `?:` / 纯函数（count·contains·any·all·min·max·clamp·concat·starts_with·ends_with·format·field·index）。sandbox：禁用能力黑名单（eval/system/require/random/time/…）→ `ForbiddenCapability`，未注册 → `UnknownFunction`，`allow_functions=false` 全禁。资源上限：max_nodes/max_depth/max_iterations → `EvalResourceLimit`（+ parse-depth 守卫 64 防栈溢出）。26 测（含 sandbox negative + 资源上限）。**SDK-only**。
- **DC-6 phase-2（YAML→WidgetSpec loader）**：新增 `ui/dsl/src/yaml.rs` **受限 YAML 子集解析器**（仓内自实现，零新依赖；spec §8.4.7 倾向仓内自实现 + serde_yaml 已弃用）。支持块映射/块序列/嵌套缩进/单双引号+纯标量/流集合 `[..]{..}`/行内注释/类型推断；dash 展开（含 `- - item` 嵌套）；不支持锚点/多文档/块标量/Tab 缩进（超子集即报错）。新增 `loader.rs::YamlLoader`（impl `WidgetSpecLoader`）：YAML→`WidgetSpec` 递归（component/id/props/bindings/actions/control/children），表达式承载字段以**原文 CompactString** 存储（保持 ui/core 不依赖 ui/dsl），strict 模式加载时调 `Engine::parse` 提前校验 visible_when/enabled_when/for_each.source/binding.source 语法（§8.4.7 Validate 阶段），`i18n:` 对象以 `Value::Object` 原样保留。**22 新测**（12 yaml + 10 loader，含端到端 load→eval、strict/lenient、i18n 保留、错误路径）。**SDK-only，未触碰 render-foundation/浏览器**（无 product-smoke 风险）。`map`/`filter` lambda 仍需 spec 决策（`Expression` 枚举无 Lambda 变体；候选：作 pure function 取字段投影/注册 fn id），留 phase-3。
- 顺手抬升 ui/testing crate coverage：`scene_snapshot.rs` 补 StrokeRect/clip 测（70%→92%），ui/testing crate ~93%（per-crate 全部 ≥85% 收口）。
- **DC-2 retained 运行态闭环**：`ui/runtime/src/host.rs` 新增 `WidgetHost` —— 把声明树 `WidgetSpec` reconcile 为 retained **widget 实例树**（`Box<dyn Widget>` + 缓存几何），驱动 layout（measure 自下而上 + arrange 自上而下定绝对 rect；内置 Column/Row/Stack 容器布局）+ paint（每节点 widget 局部坐标 paint → 按绝对 origin 平移并入全局 `Scene`，clip = 祖先 clip ∩ 节点 rect）+ 事件命中（带位置事件 hit-test 到最深最上层节点 + 冒泡，位置转局部坐标，收集 `EmittedAction`）。`WidgetRegistry`（ComponentType→工厂闭包）让 host **不依赖 ui/widgets**（宿主注册具体控件）；失效门控 `needs_layout`/`needs_paint`/`mark`（DC-9）；`creation_epoch`/`rect_of` 查询支持断言 retained 复用与 IME rect。配套 `RenderPrimitive::translate` + `Scene::translated`（局部→绝对坐标）+ `Rect::translate` + `InvalidationFlags::remove` + `WidgetId`/`ComponentType: Default`。**6 host 测 + translate 全分支测**。**这是 counter 示例（DC-14）与 chrome paint→scene（DC-7 完整验收）的运行时基础**。SDK-only，未触碰 render-foundation/浏览器（无 product-smoke 风险）。
- **PaintRecorder::draw_text（M2 文本绘制补齐）**：core `PaintRecorder` trait 加 `draw_text`（原 SceneRecorder 仅有 inherent draw_text，widget 经 `&dyn PaintRecorder` 取不到 → 控件此前无法绘制文本，Button 只画背景）。现 widget 可直接 paint 文本；SceneRecorder 实现 trait 方法 + 保留 inherent `draw_text_blob`（预 shape 路径）。MockRecorder 同步。低 blast radius（仅 2 实现者）。
- **DC-14 counter 示例**：新增 `ui/examples`（`zero-ui-examples`）crate —— `CounterApp`（AppState + reducer + `build_spec`）+ 自定义 `Label` 展示控件（paint 文本）+ 复用 `ui/widgets::Button` + `register_counter_factories`。无窗口 `examples/counter` 二进制（脚本化点击驱动 retained 闭环：count 0→3→2，scene 3 图元）+ 2 集成测（retained 闭环断言：点击→emit→reducer→重建→Scene 文案随状态更新；稳定 WidgetId 跨重建复用 epoch）。**DC-1 机械验证：cargo tree 零浏览器 crate 依赖**（仅 core/render/runtime/widgets）——验证 SDK 外部可复用。SDK-only，未触碰浏览器。
- **DC-7 chrome paint → 统一 Scene**：新增 `browser-ui/chrome/src/render.rs` —— `ChromePanel` 叶子绘制控件（按 props 语义色 `bg` + 文案 `text` paint 进统一 Scene，走 `PaintRecorder`，**不绕过 ui/render**）+ `register_chrome_factories`（注册 8 个 `browser.*` 叶子工厂：AddressBar/SecurityBadge/NavigationButtons/BrowserMenu/BrowserTabStrip/BookmarksBar/FindBar/PageViewportFrame）+ `chrome_color`/`security_color_name` 语义色映射。host 扩展 `node_container_kind` 读 `props.layout`（`column`/`row`/`stack`）→ 容器节点无需硬编码业务组件名即可布局（host 保持浏览器无关）。shell `build()` 由模型填充 props（address_text/security/tab 标题/bookmark 数等）+ 容器 `layout` 声明。`examples/chrome_scene` 二进制产出 DC-7 scene 快照（desktop shell → 12 图元：URL/安全徽章绿/tab 标题/书签数/视口白底）。**2 新测**（desktop shell paint 进 Scene 断言：URL 文案 + ≥4 bar fills + 视口铺满；安全状态→语义色）。SDK-only，未触碰 apps/browser。
- **DC-3 WebViewWidget → 统一 Scene（phase-1）**：新增 `RenderPrimitive::ExternalSurface { rect, surface_id }`（轻量 marker：WebView/平台视图纹理由后端按 id 取回合成，**ui/render 不依赖 zero-webview**，DC-1）+ `PaintRecorder::draw_external_surface` trait 方法 + `RenderBackend::draw_external_surface`（paint_scene 派发）+ scene_snapshot 确定性分支。`WebViewWidget` **impl Widget**（layout 填充分配的外部矩形；paint 记录 ExternalSurface marker；event 处理 Scroll 更新 scroll 度量并钳到 max；semantics 单节点）。**架构边界**：UI SDK 只算外部矩形，不映射 DOM（WebViewWidget public API 仅 viewport/scale/theme/scroll/surface_id）。**3 新测**（paint→ExternalSurface marker；scroll 事件更新度量+钳制；host 注册→layout→paint→Scene 含 ExternalSurface）。SDK-only，未触碰 apps/browser。剩余：WebviewBackend.render → ExternalSurface 纹理的真实光栅合成（render-foundation 后端，M2 后期/需 product-smoke）。
- **DC-4 滚动语义 + ScrollBar**：重写 `ui/widgets/src/scrollbar.rs` —— ①`drag_to_command` 改用真实 `ScrollMetrics`（按 `track_extent - thumb_extent` 映射 thumb 拖动→内容滚动，**修正占位 max_scroll=1000 bug**）；②`ScrollBarHit`（None/Thumb/TrackBefore/TrackAfter）+ `hit_test`；③`hit_to_page_command`（track 点击→翻页）；④`ScrollBarStyle` + `paint_scrollbar`（track+thumb 记录进 `PaintRecorder` → 统一 Scene，与 chrome render 桥一致）；⑤闭环测试（drag→resolve_target→clamp）。**跨 crate 闭环测**（adapter-webview）：通用 ScrollBar 拖动 → `apply_scroll_command`(scroll_bridge) → `WebViewWidget.set_scroll_metrics`（页面 offset 由 WebView 管，spec FR-006）。SDK-only，未触碰 apps/browser。剩余：apps/browser page_scroll.rs 迁移（DC-14，product-smoke）。
- **DC-5 主题系统（contrast lint + semantic token 消费 + ThemeChanged→host）**：①ui/core `SemanticTokens` 扩 `success`/`warning`（+ on_*）+ `color_for(name)` 按名解析 token；②WCAG contrast lint（`relative_luminance`/`contrast_ratio`/`passes_wcag_aa`/`passes_wcag_aaa`）—— DC-5 验收要求的 contrast lint；③browser-ui/chrome `chrome_color_themed(name, &SemanticTokens)`：chrome 色名（chrome/accent/secure/insecure/mixed/dangerous/viewport）映射到 semantic token（surface/primary/success/warning/error/background），**组件消费 semantic token 不再硬编码色值**；`register_chrome_factories(host, &tokens)`；`ChromePanel` 默认文案色 = on_surface token。`chrome_scene` 示例 light/dark 同树不同色（viewport 1.00↔0.12，security badge success 绿↔亮绿）。④runtime `ThemeChanged`→`WidgetHost.mark` 端到端：系统 Light→Dark（仅色变）→ `needs_paint` 真 / `needs_layout` 假。SDK-only，未触碰 render-foundation/浏览器。
- **DC-8 焦点/键盘路由（phase-1）**：`Widget::focusable() -> bool`（默认 false；Button=true）；`HostNode.focusable`（build 时从 widget 取）；`WidgetHost` 加 `focused: Option<WidgetId>` + `focus_next(FocusDirection)`（Tab 按声明顺序 wrap，复用 ui/core::focus FocusDirection）+ `focused_id()`/`set_focus()`；`dispatch_event` 重构：位置事件仍 hit-test 冒泡，**键盘事件** —— Tab/Shift-Tab 推进焦点、其它键路由到 focused widget 的 `event()`（收集 emit）。**4 新测**（Tab 声明顺序 wrap / Tab 键事件推进焦点 / Enter 路由到 focused widget emit / 无焦点键事件忽略）。SDK-only。剩余：click-to-focus、IME rect（TextInput caret 屏幕 rect）、FocusScope（modal/popup focus trap）。
- **DC-14 form 示例（受控文本输入 + 焦点/键盘闭环）**：`ui/examples/src/form.rs` —— `TextField`（自定义 focusable 控件，演示 **受控输入**：聚焦时键盘 → `form.change`(新值) → reducer → 重建 props.text 回灌；Enter → `form.submit`）+ `FormApp`（reducer + 校验，非空→`Hello, X!` / 空→`Error`）+ `register_form_factories`（复用 counter 的 Label/Button + TextField）。`examples/form` 二进制：Tab 聚焦→输入 "Ada"→field "Ada|"→Enter→"Hello, Ada!"→清空→"Error: name is required"。**2 集成测**（typing+focus wrap+submit+validation；submit 按钮点击）。验证 SDK 多控件组合 + DC-8 焦点/键盘 + 受控文本输入闭环。SDK-only，未触碰浏览器。
- **DC-8 phase-2（click-to-focus + IME rect）**：①`Widget::ime_rect() -> Option<Rect>`（局部 caret 矩形，默认 None）+ `WidgetHost::ime_rect()`（focused widget 的 caret 局部矩形按节点 origin 平移为绝对坐标，供平台 IME/软键盘定位）；②click-to-focus：`dispatch_event` 在 Pointer Pressed 时用 `deepest_focusable_at(point)` 聚焦命中点最深 focusable widget（命中点外/无 focusable 不抢焦点）；③**bug 修正**：`build_node` mount 后补调 `update(props)`——此前首次建树不同步初始 props（受控控件如 TextField 的 props.text 首帧为空，ime_rect/caret 错位）。TextField(form) 实现 ime_rect（caret 在文本末尾）。**3 新测**（click 聚焦最深 / click 空白不抢焦点 / ime_rect 跟踪 caret 绝对位）。FocusScope（modal/popup trap）留 phase-3。SDK-only。
- **DC-6 phase-3（DSL map/filter）**：表达式引擎加 `map($items, "field")` / `filter($items, "field")` 纯函数，采用**字段投影**（非 lambda）—— map 对每个 object 元素取字段→新数组；filter 保留字段 truthy 的元素。**决策**：不给 `Expression` 枚举加 Lambda 变体（保持文法封闭 + sandbox 安全；每元素仅字段查找，计算量受数组大小 + 迭代预算 max_iterations=10_000 约束；嵌套路径投影留 follow-up）。复用 `field()` 的 object.get 逻辑（提取 `project_field` 共用）。PURE_FN/call_return_type/eval 三处接入。**2 新测**（map 投影 title / filter active / 组合 count / 缺失字段 Null / 非 array Typecheck / iter 预算上限）。**SDK-only**。
- **DC-6 phase-4（DSL responsive branch）**：`YamlLoader` 加 `branch: Option<AdaptiveBranch>` + `with_branch()`；解析 `responsive:` 节点（`{ default, compact, medium, expanded, mobile, desktop, embedded, touch, pointer, keyboard }`），按断点键优先级（viewport > platform > input）选**最高优先级匹配**子树，递归 `convert_spec`（支持嵌套）；无 branch / 无匹配 → 回落 `default`；缺 `default` 报错。使产出的 `WidgetSpec` 已按当前断点收敛（无需运行期再选支，与 DC-12 adaptive shell 互补）。**6 新测**（compact 子树 / expanded 子树 / 无 branch 回落 default / 未匹配键回落 default / 缺 default 报错 / 嵌套 responsive）。ui/dsl 50→56 测。**SDK-only**。
- **DC-8 phase-3（FocusScope trap + SemanticsNode a11y 树）**：①ui/core `FocusScope::next` 尊重 `trap` 字段——trap=true 到边界折返（焦点永不逃逸，modal/popup），trap=false 到边界返回 None（逃逸）；首次进入按方向落首/末项。补 `SemanticsFlags: BitOrAssign`。②ui/runtime `WidgetHost` 焦点作用域：`enter_focus_scope(root, trap)`/`exit_focus_scope()`/`active_focus_scope()`，`focus_next` 在活跃作用域内遍历（trap 折返 / 非 trap 逃逸落全局），`set_root` 后按 scope.id 重收集可聚焦项、根已移除则清除（防悬空）。③`WidgetHost::semantics() -> Option<SemanticsNode>`：遍历 retained 树，对有 widget 节点调 `Widget::semantics` 取自描述，填绝对 rect + OR 进 host 级 FOCUSABLE/FOCUSED，按实际层级组装；纯容器节点 semantics merge（子节点上浮）。**8 新测**（core 3：non_trap 逃逸 / first_entry；host 5：focus_scope trap wrap+退出 / scope 随子树移除清除 / semantics 结构+focus 标志 / semantics 透传 widget label/role / semantics set_root 前 None）。**SDK-only**。
- **DC-10（i18n flesh-out：诊断 + locale 感知 plural + RTL 集成）**：①plural.rs 新增 `plural_category_for(count, locale)`——手写 CLDR cardinal 规则覆盖英语/阿拉伯语（全 6 类 zero/one/two/few/many/other）/俄语·乌克兰语·白俄/波兰语；未覆盖语种回落英语（TBD-7 决策：手写子集，零新依赖，不引 ICU4X/Fluent）。②formatter `select_template` locale 感知 + `select_template_diag` 返回 plural 变体缺失诊断。③catalog `resolve` 产出诊断（DC-10「缺失 key 走 fallback 并产生 diagnostic」）：`ResolvedText.diagnostic` 字段，MissingKey（全 chain 未命中）/FallbackUsed（命中非首选 locale）/PluralFallback（plural 变体缺失）三档，优先级 plural>fallback。**9 新测**（plural 多语种 4 + formatter plural fallback 1 + catalog 诊断/RTL 快照/Arabic plural 4）。ui/i18n ~13→22 测。**SDK-only**。
- **DC-10（DSL `i18n:` 桥接 → 闭合 message id 端到端引用）**：ui/dsl 新增 `i18n_bridge` 模块 + Cargo.toml 加 `zero-ui-i18n` 依赖（通用 crate，DC-1 机械验证零浏览器耦合）。`i18n_value_to_message(value, engine, ctx)` 把 loader 保留的 `i18n:` 对象（`Value::Object{i18n:<id>, params?:{name:<expr>}}`）解析为 `LocalizedText::Message(MessageRef)`，参数表达式按 EvalContext 求值（Text→文本/Int·Float→计数）；`is_i18n_object()` 标记判断。**8 新测**含端到端（DSL i18n 对象 → LocalizedText → CatalogStore.resolve → "Tabs: 3"）。ui/dsl 56→64 测。依赖方向 ui/dsl→ui/i18n→ui/core，loader 自身不依赖 i18n（解析期由宿主调桥接）。**SDK-only**。
- **DC-14（browser-shell-demo 示例）**：新增 `browser-ui/chrome/src/shell_demo.rs`（参考组合模块，example+tests 共用）+ `examples/browser_shell_demo.rs` 二进制。组合 chrome 领域组件（AddressBar/SecurityBadge/TabStrip）+ `WebViewWidget`（ExternalSurface marker）+ 通用 ScrollBar（`ScrollBarWidget` 用 `ui/widgets::scrollbar` helper）+ 可聚焦 `FocusBarButton`，经 `WidgetHost` 跑 retained 闭环。`cargo run --example browser_shell_demo` 输出统一 Scene（14 图元：chrome fills+URL text + ExternalSurface + scrollbar track/thumb）+ layout 区域（address 填剩余宽/security 全宽/scrollbar 12px/webview 填宽）+ a11y 树（WebView "web content" 绝对 rect + FOCUSABLE 按钮）+ 焦点 Tab + FocusScope trap 演示。**5 新测**（统一 Scene 含 chrome+webview+scrollbar / a11y 覆盖 webview+focusable / Tab 遍历 / focus scope trap / 布局组合）。browser-ui/chrome 48→53 测。demo 放 chrome（浏览器耦合在唯一允许点），**ui/examples（counter/form）仍零浏览器依赖（DC-1）**。**SDK-only，未触碰 apps/browser**。已知限制：host Row 无 flex（顺序剩余宽分配），demo 据此编排每 Row 至多一填宽项；flex 为后续 host 增强。
- **DC-6 phase-5（DSL command·route·overlay·asset 声明式引用）**：①loader `actions:` 项除 `{trigger,action,payload?}` 外新增四种简写：`command: <id>`（糖，action=命令 id）/`navigate: <route>`（→ `nav.push` + `{route}` payload）/`open_overlay: <id>`（→ `overlay.open` + `{id}`）/`close_overlay: <id>`（→ `overlay.close` + `{id}`）；约定 ActionId 常量 pub；缺所有形式 → Parse 错。sandbox 安全（仅结构化 payload，无表达式求值）。②新增 `ui/dsl/src/asset_bridge.rs`（parallel i18n_bridge）：`is_asset_object`/`asset_id_of` 把保留的 `{asset:<id>}` prop 解析为 `AssetId`；Cargo.toml 加 zero-ui-assets 依赖。**8 新测**（action 简写 4 + asset 端到端 1 + asset_bridge 3）。ui/dsl 64→72 测。依赖 ui/dsl→ui/core+ui/i18n+ui/assets（全通用，DC-1 机械验证零浏览器耦合）。**SDK-only**。
- **DC-2 host flex 布局增强**：`ui/runtime/src/host.rs` Row/Column 加 **opt-in `flex` 弹性权重** —— 新增 `flex_from_props` + `MainAxis` + `measure_linear`（两遍布局：非弹性子节点贪心 measure 保留历史行为；弹性子节点按 `flex/Σflex` 瓜分 `free=max_main−gaps−used_nonflex`，tight 到份额，Expanded 语义）。**默认 flex=0 向后兼容**（无 flex 时与旧实现逐位等价，由 backward-compat 测试验证）。这让 chrome toolbar 等多个填宽子节点能共存。**6 host 测**（均分/固定+弹性/权重比+gap/Column 弹性/向后兼容/paint 进 Scene）+ chrome shell_demo 接入（AddressBar/WebView `flex=1`，实跑 address_bar 1144×36 填满 toolbar 剩余宽）+ **1 chrome 测**（flex 子节点填满 toolbar/content 剩余宽）。host.rs coverage 91.85%→**92.29%**。**SDK-only，未触碰 render-foundation/浏览器**（无 product-smoke 风险）。
- **DC-13 域 flesh-out #1 ui/commands**（IF-010 `CommandRegistry` + §8.4.1B 菜单/快捷键/palette 同 command）：从 129 行骨架升级为可用命令系统 + 新增 `menu.rs`。`CommandId=ActionId`；升级 `Command` 模型（label_msg/description_msg/shortcut/menu_path/in_palette/enabled builder）；`CommandResult{Executed,Unknown,Disabled}`（§8.4.1B 错误处理，不静默）；`CommandRegistry`（register 同 id 替换 / execute / resolve_shortcut 跳过禁用 / menu_model / palette_entries / palette_search 大小写不敏感）；`MenuModel`/`MenuNode` 按 menu_path 归组建树（嵌套子组 View/Zoom，叶子 label 走 message id）；`PaletteEntry`。移除旧 `CommandDispatcher`（无外部引用，registry resolve_shortcut 取代）。**§8.4.1B 验收测**：同一 reload command 可由菜单/快捷键/command palette 三入口触发，都解析到同一 ActionId。**13 测**（lib 9 + menu 4）。coverage commands/lib.rs **98.23%** / menu.rs **93.48%**。**SDK-only**。
- **DC-13 域 flesh-out #2 ui/restoration**（IF-010 `RestorationStore` + §8.4.1B session restore + DC-13 route/scroll/selection 可恢复）：从 80 行骨架升级 + 加 serde_json 依赖。`RestorationId` 类型化构造器（`route_stack()`/`scroll_y(widget)`/`selection(widget)`）+ `namespaced(prefix)` per-tab 隔离；`RestorationStore` save/restore/**take**（取出删除）/remove/**clear_namespace**（前缀批量清理）；**`to_json`/`from_json` JSON 持久化**（§8.4.1B 重启恢复，按 id 排序输出确定）。**9 测**（含 JSON roundtrip/确定性/命名空间清理/类型化 id）。coverage restoration/lib.rs **98.19%**。**SDK-only**。
- **DC-13 域 flesh-out #3 ui/navigation**（IF-010 `Navigator` + §8.4.1B route 可恢复 + phone sheet route + §8.8 route stack/modal/sheet）：从 86 行骨架升级。`RouteId(u64)` 稳定单调 id；`RouteKind{Page,Modal,Sheet}`（modal dialog / phone bottom sheet）；`Route`（=RouteSpec）Serde derive + 构造器/builder；`RouteStack` push/pop/replace 返回 RouteId + `route_of(id)` 跨重建查 + `top_overlay()`（最深 overlay 路由，事件屏障）+ `route_names()`/`from_routes()` 恢复快照与重启回填；`Navigator` trait（IF-010，RouteStack 实现，宿主可持 `&mut dyn Navigator`）。**8 测**。coverage navigation/lib.rs **96.67%**。**SDK-only**。
- **DC-13 域 flesh-out #4 ui/overlay**（IF-010 `OverlayHost` + §8.4.1B 权限/下载/site info + §8.8 focus trap/outside-click/escape dismiss）：从 114 行骨架升级。`OverlayId=WidgetId`；`DismissPolicy{None,OutsideClick,Escape,Any}`；`OverlayEntry` 构造器（popover/modal/tooltip/sheet）；`OverlayHost` `show()->OverlayId`/`dismiss`/`top`/`has_modal`/**`focus_trap_ids()`**（trap_focus 浮层 id 最上层在前，host 据最上层绑 runtime FocusScope，DC-8 phase-3）/**`dismiss_on_outside_click(point)`**（anchor=None 全屏 modal 覆盖整屏不命中）/**`dismiss_on_escape()`**（一次 dismiss 一个最上层项）。**10 测**。coverage overlay/lib.rs **95.52%**。**SDK-only**。
- **DC-13 域 flesh-out #5 ui/collections**（IF-010 `VirtualCollection` + §8.4.1B 万条记录 + §8.8 materialization/stable key）：从 103 行骨架升级。`VirtualCollection` trait（item_count/item_key/build_item）+ `materialize(c, window)`/`materialize_at(c, list, scroll_y)` 物化可视窗口（跳过越界）+ `MaterializedItem` + `find_duplicate_key`（key 去重 diagnostic）+ `Recycler<V>`（按 key 缓存状态，`retain_window` 滚动回收）+ `DynamicCollection`（闭包支撑，测试用）；Selection 扩 select/clear。**8 测**。coverage collections/lib.rs **96.74%**。**SDK-only**。
- **DC-13 域 flesh-out #6 ui/platform**（IF-010 `PlatformServices` + §8.4.1B 拖拽/剪贴板/file picker/通知走 platform service + §8.8 mock）：从 60 行骨架升级。重命名 Clipboard→`ClipboardService`/FilePicker→`FilePickerService`（IF-010）；新增 `DragItem`/`DragOutcome`/`DragDropService`（begin_drag/staged/accepts_mime/receive，组件 drag intent → runtime 调，drop 目标经 staged/receive 取回）+ `Notification`/`NotificationService`；`PlatformServices` 聚合结构体（clipboard/file_picker/drag_drop/notifications 访问器，宿主持一个）+ `in_memory_platform_services()`；内存 mock（FilePicker 队列+save 记录 / DragDrop 记录+暂存 / Notifications 记录）。**6 测**。coverage platform/lib.rs **99.47%**。**SDK-only**。
- **DC-13 域 flesh-out #7 ui/animation**（IF-010 `AnimationClock` + §8.8 spring/fake clock + §8.4.1B fling/reduced motion）：新增 `AnimationClock` trait（now->Duration + request_frame，FakeClock 实现）+ `Spring` 弹簧物理（预设 smooth/snappy/bouncy，半隐式 Euler + 子步长稳定，launch/retarget/step/is_settled）+ `MotionPreference` reduced-motion（sample_tween/settle_spring 直接到终态）。保留 Clock/Curve/Tween。**17 测**（原 4）。coverage clock 100% / spring 96.77% / motion 96.77–100%。**SDK-only**。
- **DC-13 域 flesh-out #8 ui/gestures**（IF-010 `GestureRecognizer` + §8.8 arena tap/drag/pinch/fling + §8.4.1B arena 仲裁）：从 118 行骨架升级，拆为 event/recognition/recognizers/arena 模块。`PointerEvent`（id/phase/position/timestamp_ms，winit 类型不泄漏）+ `GestureRecognizer` trait（handle_pointer→GestureResult + cancel）+ `Gesture{Tap,Pan,Pinch,Fling}`；`TapRecognizer`/`PanRecognizer`（pan_threshold→Start/Update，fling_threshold→Fling）/`PinchRecognizer`（双指距离比）；**`GestureArena`**（多识别器竞争，首个 Won 胜出其余 cancel，胜后独占路由，指针全抬复位）。**12 测**（lib 6 + arena 6；原 3）。coverage event/recognition 100% / arena 90% / recognizers 88%。**SDK-only**。
- **DC-13 域 flesh-out #9 ui/forms**（§8.4.1B 设置页 + §8.8 validation/dirty/touched/submit lifecycle）：新增 `MinLength`/`MaxLength`/`All` 组合校验器 + `FieldState::touch`；新增 **`FormState`**（builder `.field(name,initial,validator)`，dirty/touched/is_valid 跟踪，validate/validate_all，**`submit() -> SubmitResult{Ok(values),Err(errors)}`**，reset 清状态保值）。**7 测**。coverage forms/lib.rs **91.91%**。**SDK-only**。
- **DC-13 域 flesh-out #10 ui/assets**（IF-010 AssetProvider variant + §8.8 manifest variant resolution）：新增 `AssetVariant`（VariantTheme{Any,Light,Dark} × density 1/2/3，构造器 any/light/dark/with_*）；**`AssetProvider::load(id, variant)` 改为 IF-010 签名**（DSL asset_bridge 只用 AssetId，不受影响）；`InMemoryAssets` 变体清单（insert/insert_default）+ **fallback 解析**（精确 → 同密度 Any → 同主题 1x → Any 1x → NotFound，candidate 去重）。**6 测**。coverage assets/lib.rs **95.24%**。**SDK-only**。
- 门禁：`cargo build --workspace` + clippy `-D warnings` + fmt 全净；foundation/text 27 + ui/render **15** + ui/testing 5(+3) + ui/widgets **33** + browser-ui/chrome **54** + ui/runtime **33** + ui/adapters/webview **8** + ui/examples **5** + ui/dsl **72** + ui/i18n **22** + ui/core **38** + ui/commands **13** + ui/restoration **9** + ui/navigation **8** + ui/overlay **10** + ui/collections **8** + ui/platform **6** + ui/animation **17** + ui/gestures **12** + ui/forms **7**(DC-13 域#9) + ui/assets **6**(DC-13 域#10) tests 全绿；DC-17 十域 per-file 全部 ≥85%。

**M2 剩余**：①统一 render-foundation 现有 font 栈到 foundation/text（物理迁移/复用，**触碰渲染后端需 product-smoke**，使 zero-webview 也走 foundation/text，DC-11 完整闭环）；②chrome 组件 paint → scene snapshot（widget paint 管线接通后，DC-7 完整验收）+ chrome 组件 → `apps/browser` 灰度接线；③apps/browser 逐组件灰度迁移（DC-14）；④render-foundation 实现 `RenderBackend` trait（TBD-2 后端侧，product-smoke）；⑤移动端运行时（M4，DC-15：真实 Phone/Tablet shell 渲染 + gesture/软键盘/safe area 适配）。

**M3 进行中（DC-6）**：phase-1 表达式引擎 + phase-2 YAML→WidgetSpec loader + phase-3 map/filter（字段投影）+ phase-4 responsive branch（`responsive:` 节点按 AdaptiveBranch 选断点子树）+ **phase-5 command·route·overlay·asset 声明式引用**（action 简写 + asset_bridge）已落地（见上）；剩余 = ①嵌套路径投影 map/filter（follow-up，需 spec 决策）；②counter/form/browser-shell-demo 示例（counter+form+browser-shell-demo 已立，DC-14）。

## Done Criteria 进度

| DC | 标题 | 状态 | 证据 / 备注 |
|----|------|------|------------|
| DC-1 | 目录与依赖隔离 | ✅ skeleton（M1） | ui/ + foundation/text + browser-ui/chrome crate skeleton 全在；DC-1 机械验证 PASS（`evidence/dep-isolation-20260630-234530.txt`） |
| DC-2 | 三棵树 + 单向数据流 + retained | 🟡 retained 运行态闭环 + flex 布局（M2） | ui/core Widget/Element/RenderNode/EventResult/Invalidation 类型 + Element reconcile 单测；**`WidgetHost`（ui/runtime）打通 reconcile→layout→paint→Scene→事件命中闭环**（按 WidgetId+ComponentType 复用 widget 实例保留状态，DC-9 失效门控）；**Row/Column flex/grow 弹性权重**（opt-in `flex` prop，默认 0 向后兼容，多个填宽子节点按 `flex/Σflex` 瓜分剩余主轴空间，Expanded 语义）；剩余：接 winit 事件循环（M2/M4 adapter） |
| DC-3 | WebViewWidget 自定义组件 | 🟡 impl Widget + ExternalSurface 合成（M2 phase-1） | adapter/webview `WebViewWidget` **impl Widget**（paint 记录 `ExternalSurface{rect,surface_id}` marker → 统一 Scene，不映射 DOM）；`RenderPrimitive::ExternalSurface` + `PaintRecorder`/`RenderBackend::draw_external_surface`（ui/render 零 webview 依赖，DC-1）；剩余：WebviewBackend.render → 真实纹理光栅合成（render-foundation 后端，product-smoke） |
| DC-4 | 滚动语义与滚动条边界 | 🟡 ScrollBar 几何/命中/命令/视觉 已通（M2） | `ui/widgets::scrollbar`：`layout_scrollbar` + `hit_test`(Thumb/TrackBefore/TrackAfter/None) + `drag_to_command`(真实 metrics 映射，修正占位 bug) + `hit_to_page_command` + `paint_scrollbar`(track+thumb→Scene)；**跨 crate 闭环** ScrollBar→apply_scroll_command→WebViewWidget.scroll（spec FR-006，页面 offset 由 WebView 管）；剩余：apps/browser page_scroll.rs 迁移（DC-14，product-smoke） |
| DC-5 | 主题系统 | 🟡 contrast lint + token 消费 已通（M2 phase-2） | ui/core::theme 类型 + ThemeResolver + diff_invalidation（paint-only/color→paint，typography→layout）+ ThemeProvider；**本轮**：SemanticTokens +success/warning + `color_for(name)`；WCAG contrast lint（ratio/AA/AAA）；chrome `chrome_color_themed` 消费 semantic token（light/dark 同树不同色）；ThemeChanged→host.mark 端到端（色变→needs_paint 不布局）。剩余：System/Light/Dark/Custom/HighContrast 偏好切换 UI、contrast lint 接入 CI lint |
| DC-6 | YAML DSL + 完整表达式语言 | 🟡 phase-5（M3） | phase-1 表达式引擎（parse/typecheck/eval + sandbox + 资源上限，26 测）+ phase-2 YAML→WidgetSpec loader（22 测）+ phase-3 map/filter 字段投影（无 lambda，2 测）+ phase-4 responsive branch（6 测）+ **phase-5 command·route·overlay·asset 声明式引用**（action 简写 + asset_bridge，8 测）已落地；剩余：嵌套路径投影 map/filter（follow-up，需 spec 决策） |
| DC-7 | 首批组件（通用+组合+浏览器） | 🟡 组件 12/12 + paint→scene 已打通（M2） | FR-009 widget 集合补齐（+IconButton/ContextMenu/Toggle）；browser-ui/chrome §8.4.1A 全 12 组件已实现；**`render.rs` 桥接：chrome 叶子组件经 `WidgetHost` paint 进统一 Scene（`chrome_scene` 快照 12 图元，URL/安全徽章/tab 标题随模型数据），不绕过 ui/render**；剩余：`apps/browser` 灰度接线（真实组件替换手绘） |
| DC-8 | 无障碍/焦点/IME | 🟡 焦点遍历+键盘+click-focus+IME rect+FocusScope+a11y 树 已通（M2 phase-3） | ui/core::focus(FocusDirection/TraversalPolicy/FocusScope trap) + semantics + ui/runtime::ime/accessibility；phase-1：Widget::focusable + focus_next(Tab wrap) + 键盘路由；phase-2：click-to-focus + ime_rect（绝对 caret）+ 初始 props 同步；**phase-3**：FocusScope trap（`enter/exit_focus_scope`，trap 折返/非 trap 逃逸，set_root 防悬空）+ `WidgetHost::semantics()` a11y 树（绝对 rect + FOCUSABLE/FOCUSED + widget label/role + 容器 merge）。剩余：a11y tree 接平台屏幕阅读器后端（宿主 adapter 层，M2/M4） |
| DC-9 | 局部失效刷新 | 🟡 host 失效门控（M2） | ui/core::invalidation needs_layout/paint 区分单测；`WidgetHost::needs_layout`/`needs_paint`/`mark` 驱动重布局/重绘（paint-only 变化不触发布局）；剩余：paint-only 主题色切换的端到端 invalidation 断言 |
| DC-10 | 国际化资源与 message id | 🟡 flesh-out 完成（M2） | ui/i18n：locale/catalog/fallback/参数替换/plural（多语种 CLDR：英/阿/俄/乌/白俄/波）/RTL direction/diagnostic（MissingKey/FallbackUsed/PluralFallback）/DSL `i18n:` message id 端到端桥接（ui/dsl i18n_bridge → LocalizedText → resolve）全部可用；locale 切换→layout+paint+semantics 失效（ui/runtime I18nRuntime）。**本轮**：locale 感知 plural + resolve 诊断链 + RTL/Arabic plural 集成快照 + DSL i18n 桥（端到端 "Tabs: 3" 闭环）。剩余：浏览器文案 catalog 实际接入（browser-ui/chrome/i18n，M2 后期，非通用 SDK 能力） |
| DC-11 | 共享文本/字体基础层 | 🟡 ui/render 已接入（M2） | foundation/text 真实后端（FontdueBackend）+ ui/render `RenderPrimitive::TextBlob`/`RenderBackend`/`paint_scene` 已接入（phase 1+2）；render-foundation 字体栈统一到 foundation/text + zero-webview 接入待续（需 product-smoke） |
| DC-12 | 响应式/自适应 + 移动 chrome | 🟡 shell 已立（M2） | WindowMetrics/ViewportClass/PlatformClass/InputClass/AdaptiveBranch 在 ui/core::layout；BrowserChromeModel + BrowserChromeShell + Desktop/Tablet/Phone shell + AdaptiveBrowserChrome + ShellLayout(safe-area/keyboard 避让) + 跨 shell 稳定 WidgetId 已落地；剩余：移动端真实运行时（M4，DC-15） |
| DC-13 | 完整应用级 UI 能力（13 域） | 🟡 10/13 域已 flesh-out（M2） | 13 域接口 + 最小单测在 Wave 3；**已升级到接口可用 10 域**：commands / restoration / navigation / overlay / collections / platform / gestures / animation / forms（FormState + validators + submit lifecycle）/ assets（IF-010 AssetProvider variant + manifest fallback）。剩余 3 域（testing/devtools/design-system）仍 skeleton，testing/devtools 为 snapshot/inspector 骨架，design-system 待首个 Zero default 风格包（DC-15 终局） |
| DC-14 | 浏览器迁移完成 + 零退化 | 🟡 counter + form + browser-shell-demo 示例已立（M2） | **counter + form + browser-shell-demo 示例可构建/运行**（counter/form 在 `ui/examples` 零浏览器依赖；browser-shell-demo 在 `browser-ui/chrome/examples` 组合 chrome+WebView+ScrollBar+焦点/a11y，retained 闭环）；剩余：apps/browser 逐组件灰度迁移（用 browser-ui/chrome + render 桥替换手绘 chrome，product-smoke 零退化硬门禁） |
| DC-15 | 移动端运行时 | ⬜ M4 | — |
| DC-16 | 测试与质量不可退让 | 🟡 守门中 | 新 crate scoped test 全绿；详见 Testing & Quality Gates |
| DC-17 | Coverage 量化 | 🟡 持续推进（M1 阶段达标） | 聚合 line 93.33% / function 92.90% / region 92.51%（≥85%）；per-crate **全部 ≥85%**（host.rs 91.85%、render_node 96%、ui/dsl engine.rs 91.79%/yaml.rs 86.61%/loader.rs 92%、chrome shell 94%/chrome_model 100%、12 组件 + IconButton/Toggle 95–100%、foundation/text ~96%；聚合随新增代码体量小幅波动，非既有代码退化） |
| DC-18 | 证据持久化与文档自洽 | 🟡 守门中 | evidence/ 持续落盘；本表与 Latest Evidence 自洽 |

图例：⬜ 未开始 / 🟡 进行中或 skeleton / ✅ 完成。

## Current Proven Baseline（首轮复核，2026-06-30）

复核结论与入口文档「Current Proven Baseline」一致，**均不存在**项确认：

- `ui/` 顶层目录 — **不存在**。
- `foundation/` — **不存在**（text foundation 待新建）。
- `browser-ui/` — **不存在**。
- `docs/goal/ui-sdk/{master.md,archive/,evidence/}` — **不存在**（本轮创建）。
- Widget/Element/Render 三棵树、retained 运行时、Theme/i18n/DSL/表达式语言 — **均不存在**。

已有可复用基础（SDK 将扩展而非重写）：

- `apps/browser/src`：自绘 UI 耦合在 `app.rs`/`app_render.rs`/`app_input.rs`/`page_scroll.rs`/`colors.rs`/`app_platform.rs`/`layout.rs` 等（合计约 19.7k 行；多个文件 >2000 行：`app_input.rs` 2910、`app_render.rs` 2816、`main.rs` 2346、`headless.rs` 1761 — 迁移时需注意拆分）。
- `crates/render-foundation`：渲染后端（GPU/CPU 图元 + `src/font/{cache,loader,shaper}` 字体栈 + image_cache）；依赖 wgpu/tiny-skia/resvg/png。
- `crates/host-runtime`：winit 0.30 窗口/事件/IME/surface。
- `crates/webview`：嵌入式 WebView 稳定边界。
- `crates/browser-shell`：浏览器业务状态（标签/书签/历史/设置/下载，UI-agnostic）。
- workspace 已声明依赖：`fontdue 0.9`/`swash 0.2`/`rustybuzz 0.20`/`unicode-bidi 0.3`/`taffy 0.7`(本地 patch)/`winit 0.30`/`serde`/`serde_json`/`thiserror`/`tracing`/`slotmap`/`hashbrown`/`compact_str`/`parking_lot`。

## Testing & Quality Gates（本机实测基线）

**⚠️ 本机 `make test` 基线状态：RED（环境性，非本目标引入）**

- 本轮首轮跑 `make test`（= `test-guard -- cargo test --workspace --exclude zero-render-foundation`）在 **`zero-script-sandbox` (lib test) 链接阶段失败**：MSVC `link.exe` 报 `LNK2019: 无法解析的外部符号 __imp_EventRegister / RegOpenKeyExW / ...`（`advapi32` 系符号），源自 `rusty_v8` 的 debug test 二进制。
- **关键定性**：`cargo build --release -p zero-script-sandbox` **成功**（0.86s）；仅 debug **test** 二进制链接失败。即 V8 库与环境本身正常，是 rusty_v8 debug-test 在本 MSVC BuildTools 上未自动链接 `advapi32` 的既有环境问题。
- **与本目标无关**：`zero-script-sandbox` 属禁止修改区（脚本 sandbox），且为环境/工具链问题，非代码/逻辑失败。CI（Linux，真后端）按 `ci.yml` 跑全量 `cargo test --workspace` 正常。
- **UI SDK 验证路径**（本轮及后续）：对新增 `ui/*` + `foundation/text` + `browser-ui/chrome` 使用 **scoped test-guard**：`./target/test-guard -- cargo test -p zero-ui-<crate>`（OOM 防护 + 不触碰 script-sandbox）；并用 `cargo build --workspace` 确认不破坏其它 crate 编译。最终门禁仍以 `make test` 为准（待环境恢复或 CI 验证）。
- 跟踪项：本机 script-sandbox debug-test 链接环境问题记入「未解决缺口」，不阻塞本目标推进（属环境，非 UI SDK 代码）。

**其它门禁**：单 `.rs` ≤ 2000 行；`cargo fmt` 无变更；clippy `-D warnings`；新增能力必须带测试；依赖仅 MIT/Apache-2.0/BSD 且最小化。

## Coverage 基线

- 全仓 floor（来自 `rendering-compat` 基线）：line 95.46% / function 96.94% / region 94.88%。本目标不得显著下降。
- **`ui/*` + `foundation/text` + `browser-ui/chrome` DC-17 当前（2026-07-01，M2 DC-2 retained WidgetHost 后）**：
  - 聚合：**line 93.33% / function 92.90% / region 92.51%**（7586 行，超 85% 目标）。
  - 演进（同口径）：M1 基线 89.89% → M1 抬升 93.00% → foundation/text 93.66% → ui/render 93.40% → chrome 8 组件 93.88% → chrome 12/12+widget 94.28% → DC-12 shell 94.38% → DC-6 engine 93.97% → DC-6 YAML loader 93.58% → **DC-2 retained host 93.33%**（新增 ~440 行 host.rs 91.85%/render_node translate 96%，聚合 -0.25pp 为新增代码体量稀释，非既有代码退化；region 持平 92.51%）。
  - 当前证据：`evidence/coverage-20260701-073437.txt`。
  - 命令（统一口径）：`cargo llvm-cov -p zero-text-foundation -p zero-ui-* ... -p zero-browser-chrome --summary-only --ignore-filename-regex '[\\/](crates|apps)[\\/]' -- --test-threads=1`（`--ignore-filename-regex` 必须用 `[\\/](crates|apps)[\\/]` 匹配 profdata 绝对路径中的目录分量，`^` 锚定不生效）。口径含 `#[cfg(test)]` 内联模块（与仓库 `check-coverage.sh` 全仓口径一致，趋势可比）。注意：单 `-p zero-ui-dsl` standalone 数字偏低（~74%）是因为 ui/core 被插桩但未在该 run 测试；DC-17 以全量 run 为准。
- **per-crate line 曲线**（M3 DC-6 phase-1 后；全部 ≥85%）：

  | crate | line | 变化 |
  |-------|------|------|
  | ui/commands · ui/restoration · ui/navigation · ui/overlay · ui/collections · ui/platform · ui/gestures · ui/animation · ui/forms · ui/assets | 88–100% | **M2 DC-13 flesh-out** 10 域：commands、restoration、navigation、overlay、collections、platform、gestures（arena 90%/recognizers 88%）、animation（clock 100%/spring 97%/motion 97%）、forms（92%）、assets（95%） |
  | foundation/text | ~96% | M2：shaping/text_blob/text_measure 0→100、backend.rs 93 |
  | browser-ui/chrome | ~97% | M2：12 组件 95–100% + shell.rs 94% / chrome_model 100% |
  | ui/dsl | ~91% | M3 DC-6：engine.rs 91.79%、yaml.rs 86.61%、loader.rs 92.00%（真实 loader）、expression 100% |
  | ui/widgets | ~95% | M2 DC-7：补 IconButton/Toggle/ContextMenu |
  | ui/render | ~89% | M2：backend.rs 92、paint_ctx 87、render_node 100 |
  | ui/animation · ui/patterns · ui/devtools · ui/navigation · ui/design-system · ui/i18n · ui/adapters · ui/core | 92–98% | — |
  | ui/runtime · ui/testing | 87% / 93% | M3：scene_snapshot 补 StrokeRect/clip 测后 ui/testing 70%→93% |

- **下一步 coverage 推进**：所有 per-crate 已 ≥85%（DC-17 阶段达标）。可选抬 ui/render(~89%)/ui/runtime/ui/testing 边角，随 M2 组件迁移自然增长。

## 依赖决策日志

| 决策 | 结论 | 理由 | Spec 锚点 |
|------|------|------|-----------|
| TBD-8 text foundation 落点 | **新建独立 `foundation/text`（`zero-text-foundation`），M2 已落地真实 `FontdueBackend`**（fontdue+rustybuzz，不依赖 render-foundation） | render-foundation 耦合 wgpu/png/resvg/tiny-skia 图形后端；纯文本/字体基础层独立避免被图形后端污染，UI 与 WebView 都能依赖而不拖入 GPU 栈。M2 第一阶段：foundation/text 自立真实后端；第二阶段（风险步，需 product-smoke）把 render-foundation 现有 font 栈统一到 foundation/text | spec §6.5A / IF-008 / TBD-8 |
| TBD-2 ui-render 与 render-foundation 依赖方向 | **`ui/render` 定义自己的 Scene/RenderNode/PaintCtx 抽象 + M2 已加 `RenderBackend` trait + `paint_scene`**；ui/render 不直接依赖 render-foundation，通过 trait 由 render-foundation 后端实现（待 product-smoke） | 通用 UI 层不被 wgpu 后端耦合；ui/render 只依赖 foundation/text（纯文本层），不依赖 GPU 栈 | spec §8.4.1 / TBD-2 |
| TBD-9 text shaping/font 依赖 | **复用 workspace 已声明** fontdue/rustybuzz/unicode-bidi；M2 已把 fontdue+rustybuzz 加入 foundation/text（swash 暂未用） | 已在 workspace.dependencies；零新增外部依赖；fontdue=度量/光栅、rustybuzz=OpenType shaping | spec §6.4 / TBD-9 |
| TBD-7 i18n 依赖 | **M1 手写 minimal plural/RTL**，不引入 ICU4X/Fluent | 接口先行；依赖评估留 M3 | spec §6.5A / TBD-7 |
| TBD-6 表达式 parser | **仓内自实现 Pratt/precedence-climbing parser**（`ui/dsl/src/engine.rs`，M3 phase-1 已落地），不引 parser combinator crate | 表达式语法边界明确（spec §8.4.7 文法），手写 parser 可控 + 零新依赖；含 parse-depth 守卫防栈溢出 | spec §8.4.7 / TBD-6 |
| TBD-10 map/filter 语义 | **字段投影 pure function**（`map($items,"field")`/`filter($items,"field")`，M3 phase-3 已落地），**不**给 `Expression` 加 Lambda 变体 | 文法封闭（无闭包/递归）+ sandbox 安全：每元素仅 object 字段查找，计算量受数组大小 + 迭代预算（max_iterations=10_000）约束；覆盖常见集合派生（取标题数组/按布尔字段过滤）。嵌套路径投影（`map($items,"a.b")`）与谓词过滤（`filter($items, field>x)`）留 follow-up（后者需 lambda 或表达式-as-arg，超当前受控计算层范围）。决策待回写 spec §8.4.7 | spec §8.4.7 / TBD-10 |
| TBD-1 serde_yaml | **仓内自实现受限 YAML 子集解析器**（`ui/dsl/src/yaml.rs`，M3 phase-2 已落地），不引 serde_yaml（已弃用）/ serde_yml | DSL YAML 子集结构受限（组件树 + 标量/流集合/注释），仓内解析器可控、零新依赖、错误信息友好；spec §8.4.7 倾向仓内自实现；serde_yaml 上游已 archived | spec §8.4.7 / TBD-1 |

依赖自治条款（已与用户确认）：YAML 解析、表达式 parser、i18n、text shaping/font、布局均由执行器自主决策，硬约束 = 仅 MIT/Apache-2.0/BSD、最小化、优先复用 workspace 已有 crate、论证写入本表 + archive。

## Latest Evidence

- `evidence/test-20260630-223559.txt` — 首轮 `make test` 实测：RED（script-sandbox debug-test V8 链接环境失败；release 构建 + CI 绿；定性见上）。
- `evidence/dep-isolation-20260630-234530.txt` — **DC-1 机械验证 PASS**：22 通用 crate 零浏览器依赖；adapter-webview→zero-webview；chrome→ui/*+browser-shell+adapter-webview。
- `evidence/capability-matrix-20260630-234530.md` — M1 能力矩阵 + DC skeleton 证据锚点 + 未解决缺口。
- `evidence/coverage-20260701-021806.txt` — DC-17 抬升前基线（2026-07-01）：聚合 line 89.89% / function 89.98% / region 90.65%。
- `evidence/coverage-20260701-024441.txt` — **DC-17（M2 foundation/text 真实后端接入后，2026-07-01）**：聚合 line 93.66% / function 93.92% / region 93.79%；per-crate 全部 ≥85%（foundation/text ~96%，三个 stub 0→100%）。
- `evidence/coverage-20260701-025355.txt` — **DC-17（M2 DC-11 phase 2：ui/render 接入 foundation/text，2026-07-01）**：聚合 line 93.40% / function 93.87% / region 93.58%；per-crate 全部 ≥85%（ui/render 新增 backend.rs/paint_ctx TextBlob 路径 ~89%）。
- `evidence/coverage-20260701-030502.txt` — **DC-17（M2 DC-7：browser-ui/chrome §8.4.1A 组件批次，2026-07-01）**：聚合 line 93.88% / function 94.46% / region 93.96%；per-crate 全部 ≥85%（chrome 新 6 组件 96–100%）。+ DC-7 6 组件 component test 锚点（address_bar/find_bar/browser_tab_strip/bookmarks_bar/security_badge/page_load_indicator）。
- `evidence/coverage-20260701-031400.txt` — **DC-17（M2 DC-7 收尾 12/12 + FR-009 widget 补齐，2026-07-01）**：聚合 line 94.28% / function 94.92% / region 94.27%；per-crate 全部 ≥85%（chrome 12 组件 + IconButton/Toggle/ContextMenu 新控件 95–100%）。
- `evidence/coverage-20260701-032214.txt` — **DC-17（M2 DC-12：BrowserChromeModel + desktop/tablet/phone shell，2026-07-01）**：聚合 line 94.38% / function 94.88% / region 94.35%；per-crate 全部 ≥85%（chrome shell.rs 94% / chrome_model.rs 100%）。+ DC-12 测试锚点（select_shell §8.4.4A 表 / phone keyboard+safe-area 避让 / 跨 shell 稳定 WidgetId / adaptive 派发）。
- `evidence/coverage-20260701-034232.txt` — **DC-17（M3 DC-6 phase-1：DSL 表达式引擎，2026-07-01）**：聚合 line 93.97% / function 94.33% / region 93.07%；per-crate 全部 ≥85%（ui/dsl engine.rs 91.79%）。+ DC-6 测试锚点（parse/eval 各能力 / sandbox 黑名单 ForbiddenCapability / UnknownFunction / 资源上限 nodes·depth·iters）。
- `evidence/coverage-20260701-040111.txt` — **DC-17（M3 DC-6 phase-2：YAML→WidgetSpec loader + ui/testing 补测，2026-07-01）**：聚合 line 93.58% / function 92.92% / region 92.51%（6914 行）；per-crate 全部 ≥85%（ui/dsl yaml.rs 86.61%/loader.rs 92.00%、ui/testing scene_snapshot 70%→92% / crate ~93%）。+ DC-6 phase-2 测试锚点（YAML 块/流/嵌套/注释/类型推断；loader 递归 component/id/props/bindings/actions/control/children；strict 表达式校验；端到端 load→eval；i18n 对象保留）。
- `evidence/coverage-20260701-073437.txt` — **DC-17（M2 DC-2：retained WidgetHost + render_node translate 全分支测，2026-07-01）**：聚合 line 93.33% / function 92.90% / region 92.51%（7586 行）；per-crate 全部 ≥85%（host.rs 91.85%、render_node.rs 96%、scene.rs 95%）。+ DC-2 测试锚点（Column 堆叠绝对 rect / 点击命中正确节点 emit / stable WidgetId 复用 epoch / ComponentType 变更重建 / Row·Stack 几何 / 未知组件填充 / clip 交集 / translate 全图元变体）。
- `evidence/dc14-counter-20260701-074528.txt` — **DC-14（counter 示例，2026-07-01）**：`cargo run --example counter` 输出 retained 闭环（count 0→3→2，scene 3 图元）；2 集成测绿；DC-1 cargo tree 机械验证零浏览器 crate 依赖（仅 core/render/runtime/widgets）。
- `evidence/dc7-chrome-scene-20260701-075934.txt` — **DC-7（chrome paint→统一 Scene，2026-07-01）**：`chrome_scene` example 产出 desktop shell scene 快照（12 图元：address URL `https://example.com` / security_badge 绿 fill / tab 标题 `Example` / `0 bookmarks` / viewport 白底）；47 chrome 测绿；DC-1 复核 ui-runtime 零浏览器业务 crate 依赖。
- `evidence/dc3-webview-widget-20260701-080848.txt` — **DC-3（WebViewWidget impl Widget，2026-07-01）**：WebViewWidget paint 记录 `ExternalSurface` marker 进统一 Scene（不映射 DOM）；7 adapter-webview 测绿（paint→ExternalSurface / scroll 事件钳制 / host 注册→Scene）；DC-1 复核 ui/render 零 zero-webview 依赖。
- `evidence/dc4-scrollbar-20260701-081457.txt` — **DC-4（ScrollBar 几何/命中/命令/视觉，2026-07-01）**：scrollbar 7 测（hit zones / drag 真实映射 / track 翻页命令 / paint track+thumb / 闭环 clamp）+ adapter-webview 跨 crate 闭环（ScrollBar→apply_scroll_command→WebViewWidget.scroll）绿。
- `evidence/dc5-theme-20260701-082629.txt` — **DC-5（contrast lint + semantic token 消费 + ThemeChanged→host，2026-07-01）**：ui/core theme 8 测（color_for / contrast_ratio 黑白 21 / wcag AA 门槛 / baseline 文本对 AA）；chrome_scene light/dark 同树不同色（viewport 1.00↔0.12）；runtime ThemeChanged→host paint-only invalidation 测绿。
- `evidence/dc8-focus-keyboard-20260701-083216.txt` — **DC-8 phase-1（焦点/键盘路由，2026-07-01）**：runtime 20 测含 4 新焦点测（Tab 声明顺序 wrap / Tab 键推进焦点 / Enter 路由 focused widget emit / 无焦点键忽略）；Widget::focusable + host focused 状态 + focus_next + 键事件路由。
- `evidence/dc14-form-20260701-083901.txt` — **DC-14（form 示例，2026-07-01）**：`cargo run --example form` 输出受控输入闭环（type 'Ada'→field "Ada|"→Enter→"Hello, Ada!"→清空→"Error: name is required"）；2 集成测绿（typing+focus+submit+validation / submit 按钮点击）；DC-1 cargo tree 零浏览器 crate 依赖。
- `evidence/dc8-clickfocus-ime-20260701-084609.txt` — **DC-8 phase-2（click-to-focus + IME rect，2026-07-01）**：runtime click-to-focus 测（按下聚焦最深 focusable / 空白不抢焦点）+ examples ime_rect 测（focused TextField 的 ime_rect = 字段绝对 origin + caret 局部位）绿；build_node 初始 props 同步修正。
- `evidence/dc6-map-filter-20260701-085243.txt` — **DC-6 phase-3（DSL map/filter 字段投影，2026-07-01）**：ui/dsl 50 测含 2 新（map 投影 title / filter active / 组合 count / 缺失字段 Null / 非 array Typecheck / iter 预算上限）；map/filter 加入 PURE_FN，无 lambda。
- `evidence/dc6-responsive-20260701-090406.txt` — **DC-6 phase-4（DSL responsive branch，2026-07-01）**：`YamlLoader.with_branch(AdaptiveBranch)` 解析 `responsive:` 节点按断点键优先级（viewport>platform>input）选匹配子树，无匹配回落 `default`；6 新测（compact/expanded 子树 / 无 branch 回落 / 未匹配回落 / 缺 default 报错 / 嵌套）。ui/dsl 50→56 测。
- `evidence/dc8-focusscope-semantics-20260701-091500.txt` — **DC-8 phase-3（FocusScope trap + SemanticsNode a11y 树，2026-07-01）**：core `FocusScope::next` 尊重 trap（折返/逃逸）+ SemanticsFlags 补 BitOrAssign；runtime `WidgetHost::enter/exit_focus_scope`（set_root 防悬空）+ `semantics()` a11y 树（绝对 rect + FOCUSABLE/FOCUSED + widget label/role + 容器 merge）。8 新测（core 3 + host 5）；build/clippy/fmt 全净。
- `evidence/dc10-i18n-fleshout-20260701-093200.txt` — **DC-10（i18n flesh-out，2026-07-01）**：`plural_category_for(count,locale)` 手写 CLDR（英/阿/俄/乌/白俄/波）；`select_template_diag` plural 变体缺失诊断；`ResolvedText.diagnostic`（MissingKey/FallbackUsed/PluralFallback）；RTL + Arabic plural 集成快照。ui/i18n 13→22 测；build/clippy/fmt 全净。
- `evidence/dc10-dsl-i18n-bridge-20260701-095800.txt` — **DC-10（DSL `i18n:` 桥接，2026-07-01）**：ui/dsl `i18n_bridge`（`i18n_value_to_message` 把保留的 i18n 对象→LocalizedText，参数表达式求值）+ zero-ui-i18n 依赖；端到端 DSL→LocalizedText→CatalogStore.resolve→"Tabs: 3"。ui/dsl 56→64 测；DC-1 cargo tree 零浏览器依赖。
- `evidence/dc14-browser-shell-demo-20260701-105000.txt` — **DC-14（browser-shell-demo，2026-07-01）**：`browser-ui/chrome/src/shell_demo` + example 二进制；chrome+WebView(ExternalSurface)+ScrollBar+FocusBarButton 经 WidgetHost retained 闭环；实跑 14 图元统一 Scene + layout 区域 + a11y（WebView rect + FOCUSABLE）+ 焦点/FocusScope trap。browser-ui/chrome 48→53 测；DC-1 ui/examples 仍零浏览器依赖。
- `evidence/dc6-phase5-cmd-route-overlay-asset-20260701-114500.txt` — **DC-6 phase-5（DSL command·route·overlay·asset 引用，2026-07-01）**：loader action 简写（command/navigate/open_overlay/close_overlay → 约定 ActionId+payload）+ asset_bridge（`{asset:id}`→AssetId）+ zero-ui-assets 依赖。ui/dsl 64→72 测；DC-1 cargo tree 零浏览器依赖。
- `evidence/dc2-flex-layout-20260701-182519.txt` — **DC-2 host flex 布局增强（2026-07-01）**：ui/runtime Row/Column opt-in `flex` 弹性权重（`measure_linear` 两遍布局，默认 flex=0 向后兼容，多个填宽子节点按 `flex/Σflex` 瓜分剩余主轴空间）；6 host 测 + chrome shell_demo 接入（AddressBar/WebView flex=1）+ 1 chrome 测；实跑 address_bar 1144×36 / webview 1268×704 填满剩余宽；host.rs coverage 92.29%；build/clippy/fmt 全净。
- `evidence/dc13-commands-restoration-20260701-185244.txt` — **DC-13 域 flesh-out #1/#2（ui/commands + ui/restoration，2026-07-01）**：ui/commands 升级为 IF-010 `CommandRegistry`（register/execute/resolve_shortcut + menu_model 归组建树 + palette_search）+ §8.4.1B 菜单/快捷键/palette 同 command 验收测；ui/restoration 升级为 IF-010 `RestorationStore` + JSON 持久化（§8.4.1B 重启恢复）+ 类型化 id（route_stack/scroll_y/selection）+ clear_namespace。13+9 测；per-file coverage 93–98%；build/clippy/fmt 全净。
- `evidence/dc13-navigation-overlay-20260701-190632.txt` — **DC-13 域 flesh-out #3/#4（ui/navigation + ui/overlay，2026-07-01）**：ui/navigation 升级为 IF-010 `Navigator`（RouteId 稳定 id + RouteKind Page/Modal/Sheet + route_of/top_overlay + route_names/from_routes 恢复快照与重启回填）；ui/overlay 升级为 IF-010 `OverlayHost`（OverlayId + DismissPolicy + show/dismiss/focus_trap_ids + dismiss_on_outside_click/dismiss_on_escape，§8.8 focus trap/outside-click/escape dismiss 测）。8+10 测；per-file coverage 96.67%/95.52%；build/clippy/fmt 全净。
- `evidence/dc13-collections-platform-20260701-191622.txt` — **DC-13 域 flesh-out #5/#6（ui/collections + ui/platform，2026-07-01）**：ui/collections 升级为 IF-010 `VirtualCollection`（item_count/item_key/build_item）+ materialize/materialize_at 物化可视窗口 + find_duplicate_key 去重 + Recycler 滚动回收 + DynamicCollection；ui/platform 升级为 IF-010 `PlatformServices`（ClipboardService/FilePickerService/DragDropService/NotificationService 聚合 + DragItem/DragOutcome + 内存 mock）。8+6 测；per-file coverage 96.74%/99.47%；build/clippy/fmt 全净。
- `evidence/dc13-gestures-animation-20260701-193138.txt` — **DC-13 域 flesh-out #7/#8（ui/gestures + ui/animation，2026-07-01）**：ui/animation 加 IF-010 `AnimationClock`（now->Duration+request_frame）+ Spring 物理动画（smooth/snappy/bouncy + 子步长稳定 + fling）+ MotionPreference reduced-motion；ui/gestures 升级为 IF-010 `GestureRecognizer`（PointerEvent + Gesture{Tap,Pan,Pinch,Fling} + TapRecognizer/PanRecognizer/PinchRecognizer）+ GestureArena（竞争仲裁，首个 Won 胜出）。17+12 测；per-file coverage clock 100%/spring 97% / arena 90%/recognizers 88%；build/clippy/fmt 全净。
- `evidence/dc13-forms-assets-20260701-194105.txt` — **DC-13 域 flesh-out #9/#10（ui/forms + ui/assets，2026-07-01）**：ui/forms 加 MinLength/MaxLength/All 校验器 + FormState（builder/validate_all/submit lifecycle → SubmitResult{Ok,Err}/reset）；ui/assets 加 AssetVariant（theme×density）+ IF-010 AssetProvider::load(id,variant) 签名 + InMemoryAssets fallback 解析（精确→同密度 Any→同主题 1x→Any 1x→NotFound）。7+6 测；per-file coverage forms 91.91%/assets 95.24%；build/clippy/fmt 全净。

**M2/M3 门禁实测（2026-07-01）**：
- `cargo build --workspace` — Finished（0 错误）。
- `cargo clippy --workspace --all-targets -- -D warnings` — Finished（0 警告）。
- `cargo fmt --all --check` — 净（0 diff）。
- foundation/text 27 + ui/render 13(+render_node translate) + ui/testing 5(+3) + ui/widgets 29 + browser-ui/chrome 45 + ui/runtime 8(+6 host) + ui/dsl **48**(+22: 12 yaml + 10 loader) tests 全绿（scoped test-guard）。
- DC-1 复核：ui/dsl 仅依赖 compact_str/hashbrown/serde/thiserror/zero-ui-core（`cargo tree -p zero-ui-dsl` 机械验证），零浏览器业务 crate；YAML loader 仓内自实现，零新依赖；ui/runtime 仅依赖 ui/core+ui/render+ui/i18n+compact_str/hashbrown/thiserror，零浏览器业务 crate。
- DC-17 coverage：聚合 line 93.33%，per-crate 全部 ≥85%。

## Next Steps

1. **DC-11 完整闭环：render-foundation 字体栈统一到 foundation/text**（使 zero-webview 也走 foundation/text；**触碰渲染后端，需 `make product-smoke`**）。
2. **DC-3 phase-2：WebViewWidget 真实纹理合成**（render-foundation 后端实现 `RenderBackend::draw_external_surface`，按 surface_id 取 zero-webview `RenderPrimitives`/texture 合成；**触碰渲染后端，需 `make product-smoke`**）。
3. **apps/browser 逐组件灰度迁移（DC-14，product-smoke）**：用 browser-ui/chrome 组件 + render 桥替换 apps/browser 手绘 chrome（含 page_scroll.rs → ScrollBar 迁移，DC-14 零退化硬门禁）。
4. **DC-13 13 域 flesh-out（继续，10/13 已完成）**：commands + restoration + navigation + overlay + collections + platform + gestures + animation + forms + assets 十域已升级到接口可用；剩余 3 域仍 skeleton + 最小单测。剩余：①testing（scene/semantics snapshot 工具已有骨架，扩 layout bounds 断言 + 黄金快照对比，§8.8）②devtools（inspector/layout bounds/perf timeline skeleton，dev/test feature gate）③design-system（**首个风格包：Zero default semantic token bundle**，DC-15 终局需求）。逐域推进到接口可用 + 浏览器所需最小子集（§8.4.1B 矩阵）。
5. **host 布局继续增强（可选）**：flex 已落地；后续可加 cross-axis 对齐（Alignment prop）、flex basis、min/max 约束等，随 chrome 迁移真实排版需求驱动。SDK-only。
6. 跟踪项：本机 `make test` 受 script-sandbox debug-test V8 链接阻塞（环境性）；render-foundation 字体栈统一前 welcome.html 渲染走旧路径，未受本轮影响。
7. **工作流注意（git）**：`ui-sdk` 是已发布的长期分支，远端历史**未** rebase 到最新 main（R918 reftest shim 等姊妹目标提交不在本分支）。后续轮次**不要** `git rebase origin/main` 全量重写本分支（会改写所有 commit hash → push 被拒 → 需 force-push 重写已发布历史）。保持增量 fast-forward push；如确需 main 同步，用 merge 或 `--force-with-lease` 并在 archive 记录。
