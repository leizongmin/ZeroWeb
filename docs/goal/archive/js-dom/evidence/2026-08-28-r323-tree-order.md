# R323 Evidence — L2 切片二：tree-order 归并（匹配子按 nextSibling 锚点插入 host 结果序；+1P 零回归）

**日期**: 2026-08-28
**切片**: M4/L2——R323(a) tree-order 归并
**改动面**: `part04.js`（R322 归并块的锚点定位插入）+ `part24.rs`（r323 序断言回归）

## 一、缺口与修复

R322 切片一的归并是「host wrapper 在前 + handle 匹配在后」尾并——mid 插入形态序错：
`insertBefore(mid, childNodes[1])` 后 QSA 期望 `a.mid.b` 实测 `a.b.mid`。修：匹配子按反链
`nextSibling` 锚点插入——

- **锚点定位**：nextSibling 的 `__zwSelector`（sel 元素形态）在 host 结果 sel 列表中的
  索引 → wrapper 输出中同索引位（`_proxyCache` identity 稳定使 `===` 匹配成立）插到之前；
- **无锚回落**：nextSibling 是 pending handle / null（append 语义）→ 尾部；
- **数据源已就绪**：`_mo_notify` 汇流点（part01:1828，R51 时代）已记
  `parentSel + nextSibling`——查询站点只是消费既有记账，零新增状态。

探针：`mid=a.mid.b` ✓（此前 `a.b.mid`）、`end=a.mid.b.end` ✓（append 无锚尾部正确）。

## 二、A/B

| 项 | R322 | R323 | Δ |
|---|---|---|---|
| ParentNode-querySelector-All | 1975P | **1976P** | +1（tree-order 序断言解锁）|
| Element-matches 675P / querySelectorAll 2P / getElementsByTagName 35P/1F（既存）| — | 持平 | 零回归 |
| **全量 dom sweep** | 54141P/58F/21T | **54137P/58F/25T** | Fail set 恒等；4 Timeout 全部单跑 Pass（并发噪声，R196 同款 flake 家族）|
| engine --lib（v8/quickjs）| 2461/1460 | **2462**/1460 | +1（r323 序断言回归）|
| fmt / clippy（v8 guarded + quickjs）| — | 干净/0 | — |

## 三、L2 切片路线状态

1. ~~查询站点 handle 子树归并~~（R322）
2. ~~tree-order 归并~~（本切片）
3. **sel 子重定位形态归并**（R182 的快照内既有节点同父移动——查询结果旧位剔除 + 新位插入；`_zwSelPendingParent` 槽记账已存在，待归并消费）
4. 视图层完整归一（_childNodeList 的 sel 父分支与 registry 统一——深水区本体）
