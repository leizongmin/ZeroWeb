# R340 — DC-7 控制面勘误（2026-08-28，零源码改动轮）

## 勘误内容

master.md「当前状态」表的 DC-7 行此前停在 R64 时点（「S5q PoC 深度；剩余 = 深度补齐」），
与 R65-R78 的历史记录（同文件内）矛盾。以代码实况为准勘误：

| DC-7 项 | 状态 | 证据 |
|---------|------|------|
| 1. rquickjs 原生 DOM 绑定镜像 V8 S0–S5 | ✅ | M6 全量收口（R75 `4bfa87e3` 定稿）：S0q 骨架[R57]/S1q 属性+复合对象三件套[R58-R71]/S2q 写入+mutation+树读回/S3q 查询[R62]/S4q EventTarget+三阶段 capture/bubble[R67 `a8fa85b5`]+Event/CustomEvent 构造器[R72]+DOMException 构造器+instanceof[R73 `be431968`]/S5q 五件套+完整 ctor 执行[R65]+observedAttributes+whenDefined 真 pending[R75] |
| 2. GC/生命周期安全验证 | ✅ | R74 `4efd76aa` 生命周期实验+结论关闭：strong Persistent + reset_quickjs_state 换代全清是唯一正确形态（WeakRef 两方案实验回退归档）；rquickjs 缺 finalizer 钩子记 TBD |
| 3. 双 feature polyfill vs native A/B 等价 | ✅ | R76 `68f09748` quickjs WPT 双路径基线：polyfill 64.55% vs native 64.53%（对等差 0.02pp）；R78 `d4eb0641` execute 全局代码语义修复后 traversal/collections 与 v8 基线对齐 |
| 4. default-on 后走 native | ❌ | M7 改 Mission 级单向门，待用户点名（待用户决策清单已更新触发条件满足） |

## 同轮基线复核

- `make test` 全量：唯一失败 `window_surface_present_smoke`（XOpenDisplayFailed
  headless 环境项，既存备档）
- webview quickjs 611P（native wiring 断言面 coverage.rs）
- engine v8 dom_bindings 217P / engine quickjs PoC 3P

## 教训

控制面「当前状态」表是快照，历史记录在同一文件内持续追加——两者会失同步。
每轮进入时应先核对状态表与最新切片记录的一致性（本文件即 R339 轮后
通读历史时发现）；R340 的勘误把 DC-7 从「🟡 进行中」修正为「✅ 前三项满足」，
M7 的启动条件（M6 完成）实际已满足一个月。
