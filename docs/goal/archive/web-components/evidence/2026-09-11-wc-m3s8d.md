# M3 切片 8 第二增量（第三小步）— event-with-related-target 全清 + adjStation eventPhase 面（web-components）

- 日期：2026-09-11
- 对照：`2026-09-11-wc-m3s8c.md`（3494/4763）
- 成绩：**3525/4763（74.0%）净 +31，零回归**
- 全量 JSON：`2026-09-11-wc-m3s8d.json`
- 门禁：fmt/clippy 全过；`make test` 全绿

## 修复内容（纯 JS shim：part03/part04/part05）

### ① `label` IDL set/get 对称 round-trip（event-with-related-target 18F 根因）

- createFixedTestTree helper（上游 WPT）用 `element.label = name` / `shadowRoot.label =
  name` 做派发站身份。set 侧 R3069 把 `label` 无条件反射为内容属性，但 set trap 前段
  的 TRACK `kind/label/srclang` 分支（M1 切片 3）对所有元素命中、体内再 gate——非
  TRACK 的 `label` 写被静默吞；get 侧仅 OPTION 读 `label` 属性。
- 修：①TRACK 分支 tag gate 上提（非 TRACK `label` 落 R3069 反射分支）；②get trap 增
  泛化 `label` 读侧（非 OPTION 读同名内容属性，未设返 undefined；OPTION 专用分支——
  属性缺省回落 text——优先级不变）。

### ② relatedTarget 全语义面补完（handle 世界 + target 站）

- handle 世界 `_dispatchWithBubble`：每站 `rel`（`_zwRetargetRel` 对站 proxy retarget）
  + `_r114ApplyAdj` 逐站设 `event.relatedTarget` + root→host 跨越的**停止规则**
  （`_r114Crossing` 时 host === retargeted rel → path 止于 root）；target 站补独立
  retargeted rel（capture 段遗留值不得外溢——站 0 断言）。
- 修 `_r114Entry` 丢 `rel` 字段（与首增量丢 `shadow` 同型 bug）。

### ③ adjStation eventPhase 面（capturing-and-bubbling 全清）

- spec invoke：「item's shadow-adjusted target 非空 → eventPhase = AT_TARGET」——即
  target 本体站与**真 retarget 的跨界 host 站**在 capture/bubble 两阶段均报 AT_TARGET(2)；
  walk-back 站（含 light target 穿入穿出的 host 站）按阶段报 1/3。
- 实现：R167 `_s8AdjStation167` 逐站标记（crossing 时 `_s8NextAdjStation = !sii`——
  只在真 retarget 时成立）+ fireStation `_s8EffPhase`；handle 世界站对象带
  `adjStation: _r114Crossing` + `_zwAdjStation` 标记 + `_dispatchToListeners` 读标记。
- proxy 世界粗粒度兜底收窄为 `_s8fi === _s8RootIdx + 1`（仅 host 站）。

## 成绩分布

| 文件 | 前 | 后 |
|---|---:|---:|
| event-with-related-target | 0/18 | 18/18 |
| Extensions-to-Event-Interface | 2/12 | 12/12 |
| capturing-and-bubbling | 2/5 | 5/5 |

## 余项

- every-slots 2F：plain 克隆宿主 `.shadowRoot` 读值与 attachShadow 注册对象身份分裂
  （createTestTree walk 的 qSA 视图/真节点分裂——attachShadow 挂在视图宿主上，B-shadow
  二次克隆；根因已定位到 R366 查询归一域，独立小切片）
- event-composed-path-after-dom-mutation 1F、event-post-dispatch 尾 1F（listener 内
  mutation 形态）、slotchange 尾 12 案、disabledFeatures×attachShadow 尾 2 案照旧

## 累计

| 阶段 | Pass/Total | 通过率 |
|---|---:|---:|
| M1 基线 | 437/4730 | 9% |
| M3 切片 8 第二增量第二小步 | 3494/4763 | 73.4% |
| **M3 切片 8 第二增量第三小步** | **3525/4763** | **74.0%** |
