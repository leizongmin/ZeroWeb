# M2 收口 — XHTML 面 template.content + owner doc 尾差（web-components）

- 日期：2026-09-10
- 对照：`2026-09-10-wc-m2s1.md`（3013/4752）
- 成绩：**3014/4752 零回归**（+1：`template-contents-owner-test-002` 的
  createElement('template').content 断言链翻绿——M2 全部已知尾差清账）
- 全量 JSON：`2026-09-10-wc-m2-final.json`
- 门禁：make test 全绿（v8 16839P / quickjs 1763P / renderer、browser 全绿）+
  reftest 485/485 + fmt/clippy

## 修复内容

- **iframe/detached 工厂元素的 TEMPLATE content 视图**（part05 `_zwIframeCreateElement`）：
  `_zwMEl` 工厂（part03:6304）有 plain content getter，但 iframe 文档的 createElement 走
  part05 工厂——其元素无 content → WPT
  template-contents-owner-test-002 的 `createElement('template')` + `content.ownerDocument`
  断言链（inert contents owner 文档语义）Fail。补同款视图（per-template 独立 inert 文档
  ownerDocument + childNodes 别名 + appendChild/insertBefore/removeChild mutation 面）。

## M2 判定（对照入口文档 DC-3）

- [x] parser 层 `<template>` contents 为独立 inert DocumentFragment（不在文档树）——M2 切片 1
- [x] `content` 属性真实 fragment（克隆/实例化语义；R145 querySelector 例外规则收敛）——
  切片 1 + 本片（主文档 sel 路径 / iframe 工厂路径 / detached plain 路径三面全通）
- [x] template 内资源不加载、脚本不执行（inert 语义，WPT 为准）——R328 脚本提取跳过 +
  `extract_page_scripts_skips_template_contents_r328`（spec 对齐更新后）守护

## 累计

| 阶段 | Pass/Total | 通过率 |
|---|---:|---:|
| M1 基线 | 437/4730 | 9% |
| M1 切片 4 | 3008/4675 | 64% |
| M2 切片 1 | 3013/4752 | 63%（等值 64.4%） |
| **M2 收口** | **3014/4752** | **63%** |

## 余账（非 M2 范围）

- XHTML 悬空 body 查询域（`additions-to-parsing-xhtml-documents/node-document.html`
  5 案）——测试形态 `doc.body = createElement('body')` 不挂树 + doc 级查询以
  documentElement 起根；pre-existing（改动前同 Fail），与 template 真实化无因果。
  记「shim detached 文档查询域」后续项。
