fix(ui-sdk): P0-1 Overlay anchor_widget 真实定位 + outside-click 修复

OverlayEntry 新增 anchor_widget 字段（Option<WidgetId>），host 在 layout overlay 时
通过 rect_of(anchor_widget) 解析真实屏幕 rect 作为锚定，覆盖静态 anchor 字段。
新增 with_anchor_widget 构造器。gallery popover/tooltip 用 demo_popover_trigger /
demo_tooltip_btn 作为 anchor_widget，浮层正确定位到触发按钮下方/上方。

host outside-click dismiss 逻辑修复（P1-7）：点在 overlay_root 视觉 rect 内时不算
"外部点击"，避免 popover 内容（如 Close 按钮）被误关。

layout overlay 时根据 anchor 矩形 + overlay 尺寸 + viewport 自动选择上方/下方放置，
避免超出视口边缘。

更新 paint_node_skips_subtree_outside_viewport 测试预期（+1 scrollbar fill）。
