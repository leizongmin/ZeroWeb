# Changelog

本文件记录项目对外可感知的重要变更。

写法参考 Keep a Changelog，不过这里的版本号要结合项目现状来看：仓库还在实验阶段，所有预发布版本都应该按实验性版本理解。

## [Unreleased]

### 变更

- **浏览器进程模型固定化**：`zero-browser` 不再支持 `--single-process` / `--multi-process` 或相应环境变量开关；tab 固定使用 `zero-renderer`，页面帧固定经 `zero-compositor`，栅格图像固定由 renderer 启动 `zero-image-decoder`。`ZeroWebView` 仍保持进程内嵌入实现。
- **浏览器二进制瘦身**：发布版 `zero-browser` 不再编译进程内 `tab_worker`、页面脚本 worker 和页面脚本调度，也不再链接 `zero-webview`、V8、QuickJS 或脚本 sandbox；headless 的导航、内联 HTML、脚本执行及截图改由 `zero-renderer` IPC 驱动，browser 只保留宿主、网络代理、绘制快照导入和 chrome 呈现职责。默认 Windows release 从 71.4 MB 降至 18.5 MB；renderer 继续独占 V8 页面脚本执行。

### 新增

- **Android 多进程浏览器 bootstrap（M0）**：新增 `apps/android-browser` 应用（应用 ID `com.leizm.zeroweb`，Kotlin/Jetpack Compose chrome + Rust JNI 桥接层 `zero-android-browser` cdylib）——`NativeBridge` 五入口（`nativeVersion` / `nativeStartRole` / `nativeRunRole` / `nativeProbeDecoder` / `nativeProbeCompositor`），decoder/compositor 经 Android socket transport 复用共享 Rust role 循环（`run_role`），renderer 保持 Kotlin Service 拓扑待 M1；配套 role-entry 重构（renderer/compositor/image-decoder 暴露共享入口）与 spec RFC `docs/specs/android-browser-spec-rfc.md`；工作区 29→30 member。
- **媒体解码管线 `crates/media`（zero-media，media-playback M1a）**：webm/Matroska 容器解析 + VP9 纯 Rust 解码（`rusty_vp9`）+ YUV→RGBA 帧转换（RFC 路线 C：开源先行 + 进程内 crate，解码与播放解耦）；AV1 解码切片（feature `decode-av1`，dav1d 绑定，codec 自路由 `open_webm`）；音频解码面——symphonia mp3/ogg-vorbis、Ogg Opus（纯 Rust `opus-decoder`）、webm 内嵌 vorbis/opus 音轨同源解码；播放驱动 `VideoPlayer`（`VideoClock` trait，帧率时钟/play/pause/ended + currentTime 真值推进）+ fixture-mounted 播放面切片 1（webview 播放桥 + wpt-runner 播放泵 + cue 调度钩子）；音频输出面 `AudioSink`（headless `NullSink` 可观测 / `audio-cpal` 真设备 `CpalSink`）+ 多源混音总线 `Mixer` + Web Audio 振荡器合成最小面（`WebAudioContext`）。工作区 30→31 member。
- **媒体三 goal 推进（2026-09-01/02）**：media-elements 语义面 WPT 529P/0F（95.7%，M1–M3 扩批 XV：http VTT 加载 + WebVTT 解析深化 + canPlayType av1/webm-opus 扩表）；media-audio WPT webaudio 六批导入 331P/0F（接口语义族 headless 饱和）+ CpalSink 真设备冒烟；H.264 立项 RFC（`docs/specs/h264-increment-project-spec-rfc.md`）Proposed 态待批复。
- **IndexedDB WPT 80% 达成（[storage-indexeddb goal](docs/goal/archive/storage-indexeddb.md) Done）**：fixed revision 210 个 IndexedDB `.any.js` 文件 imported 168 个（80.00%），1073/1073 subtest 全 Pass / 0 Fail——M1（WPT 基线）、M2（JS↔Rust 接线：factory schema/transaction wire/object store/index/cursor stepping/mutation、key/structured clone、transaction scheduling）、M3（跨 renderer 连接与事务、数据库持久化与恢复、`Number.MAX_SAFE_INTEGER` 版本）全部完成；浏览器产品路径与嵌入式 WebView 均接入持久化。
- **js-dom M4 续作（R100–R227）**：真实 Vue 3 端到端落地（R100，M3 里程碑）+ QuickJS 矩阵全量核实（R101）+ M4 events 大簇（Event-subclasses 构造器、passive-by-default 57F 全修、EventTarget dispatchEvent、body/frameset event handlers、合成 click pre-click activation、init-while-dispatching + legacy initXxxEvent，R104–R110）+ M4 nodes（`ChildNode`/`ParentNode` mutation 族经 viable-sibling pre-insert 全对齐，R117）；window.event shadow 语义 + composed boundary + XHR EventTarget（R114）、attribute 大小写语义 + NS metadata + `createAttribute`（R116）、静态 iframe `contentDocument`（R115）；**R118–R140 续作**：M4 traversal/nodes/events 多轮收口——`getElementsBy*` NS 感知匹配（R120）、Text/Comment 构造器 + CharacterData 孤立代理保真（R121）、Attr identity 绑定表（R122）、Node removeChild/replaceChild/cloneNode spec 校验族（R126–R128）、CharacterData 方法族 spec 校验（R129）、matches NS type selector + `[Unscopable]`（R134）、name-validation spec regex 校验族（R135）、getRootNode 泛型 + composed shadow root（R136）、native 叠加路径原型链重接（R137）、**EventTarget 对象 listener 全链（handleEvent 分派、revoked Proxy 上报）+ named iframe window 注册 + Text 非 bubbling 修正（R139，cross-realm 5F→0F、events 28F→22F）**，并附带修复 service-worker 流误吞 strict 脚本错误（webview.rs `Result::or` 语义，R139 follow-up）；**R141–R156 续作**：live childNodes（R140）、characterSet-normalization 654F→0F（R141）、no-focus-events / handler-count 2F→0F（R142/R143）、指针激活序列 pre_events + pointer-event-document-move（R144/R145）、strict classic 脚本全局函数发布（R147）、焦点所有权统一 + relatedTarget shadow retargeting（R148）、`customElements.define` 既有元素自动升级（R149）、进程内 gBCR + MouseEvent offsetX/offsetY 派发期计算（R150）、**LABEL 激活转发 + inline handler 时序 + oninput（R155）**；**R157–R186 续作**：selector tokenizer CSS 转义反解 + universal `*` + Selectors-API 宽容（R157）、查询 identity 缓存 + 非法选择器守卫（R158）、`:empty` 注释/PI 语义 + `:target` fragment（R160）、**M1 L2 原生查询切片（R163–R171：fragment QSA 真实节点优先、detached factory 本树查询、compound 匹配器基建、三查询面经 identity 桥归一、compound gate 文档上下文本树查询）**、M4 轻件收口（Range 点查询方法族 R178、node-creation-realm 跨 realm 工厂通道 R179、`insertAdjacentElement` 三件 R182、Range offset/commonAncestor 活性 getter R183、Node 族三簇 R184、**isSameNode 三对象面 + 空类名分词 + cloneNode ns 保留 R185**）；**R187–R207 续作**：跨 realm 异常上报定向（R187）、DOMParser 可变面 + textContent setter MO 记录（R189）、NS 属性元数据域（R190）、NodeList length 劫持语义 + strict classic const/let（R198）、选择器输入预处理 NUL→U+FFFD（R199）、属性选择器大小写标志 `[attr=value i]`（R200）、**strict var accessor 导出 + V8 脚本看门狗（R201 里程碑轮）**、**Range 边界点序族（setStart/setEnd crossing reset R203、compareBoundaryPoints WebIDL + Range constants + detached createRange R204、boundary-point 点查询 R205）**、**iframe 子文档脚本执行通道（R206）+ iframe 工厂 mutation face + docEl 根优先 + textContent replace-all（R207）**；**R208–R227 续作**：iframe doc 查询缓存同键失效（R208）、surroundContents/extractContents/deleteContents CharacterData 区间分支族（R210–R213）、insertNode ensure-pre-insertion validity + Document 子位置规则（R215/R217）、CDATA splitText + detached fragment insertBefore（R218）、insertBefore 防递归 + insertNode endOffset 同步 + deep-clone CDATA/PI 保型（R219/R220）、**fresh-doc 落地（iframe doc body/head 重绑 R221）+ doctype-in-childNodes（R222）**、doc 级子 parentNode 健壮写入（R223）、**insertNode node 类型合法性（R224）+ fragment flatten + doc-level detach（R225）+ `_zwMEl` insertBefore ref 位先取后摘（R226）**、surround 947F 重聚类（R227）。
- **js-dom M4 续作（R228–R262，Range extract/surround 全序复刻 + live-range 边界迁移）**：**R228–R231**：detached 同节点 CharData 区间 surround（守卫放宽 + detached 子分支 + HRE 不再吞，+50P）、surround sim-order 三步复刻（+117P）、Text 容器 leaf-newParent extract 先行（+79P）、同节点区间 extract 不塌缩（+116P，common.js 全源注入沙箱直跑方法论突破）；**R232–R234**：负结果两轮（cDP-only 开关拆解 -28P 回退、sim 全源注入复刻实证 HRE/INVALID_STATE/cDP 三簇同根 R219×fresh-doc）→ fresh-doc 深项本体落地：**动态 documentElement getter（part05 首个元素子语义，restoreIframe 后读克隆子树）+ extract 记账两件（plain 子 move 语义、跨容器 collapse）**（+36P）；**R235–R241**：leaf-newParent extract-first 扩展两形态（异节点同父 CharData 区间 + 元素容器含覆盖子，+88P）、ancestor-element range extract + surround 全序（R236）、surround path-4 全序（clear kids/positional insert/selectNode，R237）、generic `Node.prototype.remove` 工厂文本节点（R238）、partial-containment 检查顺序 + nextNode 遍历（R239）、ancestor extract 快照迁移覆盖子（R240）、wrapper-domain 子 move 语义（R241）；**R242–R249**：element-ec ancestor extract + surround 全序（+38P）、cDP 面 detached-doc docEl/headEl/body（R235 retest 翻转 +26P）、doctype-contained surround HRE、factory-doc parentNode getter-only move 语义、17x/13-14x 树分歧诊断（probe-only，engine repro 证伪 iframe-factory no-op 假设）、`ChildNode.remove` 显式先行 + iframe factory 元素 own removeChild；**R250–R256**：ghost 溯源（serialization-cache mid-state 快照非 live tree，detached-build 实验阴性回退）、R210 scenario 2 对齐上游 R239 顺序 + nodeType-only 检查、surround clone loop 前 detach newParent、iframe docEl own cloneNode 保 head/body 视图（R255）、factory docEl mutation 重接线兄弟 getter + title ns 对齐（R256）；**R257–R259**：self-surround HRE（post-clear inclusive-ancestor 检查，+4P）、selectNode sc-fallback（newParent 是 covered child 时，+3P）、leaf-HRE 空覆盖子 extract-first——**Range-surroundContents 达 1840P/0F 100%**（+11P）；**R260–R262（live-range 系列）**：CharacterData 变更的 live-range 边界调整（mutations 三套件 100%，ranges 全量 +196P）、splitText live-range retarget two-phase（+20P）、**removeChild live-range 边界迁移（mutations-removeChild 100%，mutations 域 remove+data+splitText 三族全 100%）**——`_zwAdjustRangesForRemove` 六站点接线，spec `concept-node-pre-remove` 两规则逐字引证；R263 靶点 appendChild/replaceChild modifyForInsert 插入侧对偶。
- **js-dom M4 续作（R263–R294，mutations 八套件全 100% + Range 深水区全序复刻 + dom 全量 54070P/89F）**：**R263–R265**：insert 侧 live-range 边界调整（`_zwAdjustRangesForInsert` 九站点，spec modifyForInsert 逐字引证——appendChild 42P/28F→70P/0F + replaceChild 60P/0F 双 100%，+58P）、insertBefore 超时族五轮探针定位到 textEl ref 静默不入 registry 的 indexOf 死循环、**textEl ref 的 registry 插入（物化 + 前插保序 + 注销单源化）——mutations-insertBefore 76P/0F 100%，mutations 域八套件全 100%**（removeChild 20/appendChild 70/replaceChild 60/insertBefore 76/deleteData 564/insertData 382/appendData 384/splitText 116）；**R266–R270**：deleteContents 深水区（同节点 detached 放宽 +12P、ancestor 分支 +6P、跨容器 CharData 四段树序 +4P、document 容器限定分支 +6P、foreignDoc title 域 cDP/contains 补齐 +5P）；**R271–R278**：removeChild plain 子 registry 剔除放宽 + 四轮归因诊断（wrapper identity churn / expected iframe 扁平 body / oracle walk 提前终止 / 比较遍历 data 源分歧——真实文件注入 + identity 标记 probe 方法论）+ CDATA 兄弟导航 getter + iframe 域元素兄弟 getter + deleteContents sc-CharData/ec-element 分支（112P→117P）；**R279–R287**：**sc=element 跨容器 deleteContents 125P/0F 100%（R279）**、跨容器 extractContents 三段路径克隆组树 +12P（R280）、cloneContents 路径克隆分支 +22P（R281）、extract doctype 抛移植 + doc-sc 父守卫 +10P（R282）、element-sc 一层 + sibling 提取 +5P（R283）、doc 容器 covered children + frag 域归一双修 +7P（R284）、firstPartial clone 引导——**extractContents 187P/0F 100%（R285）**、容器 handle innerHTML 空态（R286）、clone 侧 doc 守卫 + 九轮全量 A/B 复核——**ranges 域六套件全部 100%（R287）**；**R288–R289**：**Range 三大域 956F 一轮全解**（compareBoundaryPoints 592F→0F + Range-set 240F→0F + comparePoint 124F→0F，四独立根因）+ ranges 域最后三簇收口（`new Range()` 初始边界、selectNode 整文件崩→304P/0F、intersectsNode 严格性——**dom 全量 53733P→54041P +308**）；**R290–R294**：nodes 域三簇（Element-remove registry 清理、Node-constants、Document-constructor 2F×3）、matches doc-root serialization + 键回退、**querySelector 结构元素身份归一（detached-doc body/head/html 动态读，Element-matches + webkitMatchesSelector 双套件 100%）**、**insertAdjacent 家族全解（insertAdjacentText 重写对齐 R133 + insert-adjacent 两文件整文件崩→100%）**、MutationObserver record 语义两件（removeChild 记录补 previousSibling/nextSibling + addedNodes identity 优先）——**dom 全量 54070P/89F**。
- **js-dom M4 续作（R295–R318，selector/insertBefore/traversal/collections 收口，dom 全量 54070P/89F→54140P/59F）**：**R295–R299**：iframe realm 的 Text/Comment 构造器 ownerDocument（双 16P/0F 100%）、Node-insertBefore pre-insertion 校验族（文件级 apply 崩→32P/8F，+30P）、Node-insertBefore 全文件收口（40P/0F 100%，探针连环挖出三层根因）、MO move-record 兄弟字段（MutationObserver-childList 25P/0F 100%）、cross-realm MO 机制三件、MO disconnect record 清空；**R299–R304（selector/L2 域）**：属性选择器 `i`/`s` 大小写标志（JS 匹配器 land）、querySelector `:scope` 子树作用域（4P/0F 100%）、querySelector escapes 孤立代理 never-match（JS 原值 id 缓存，68P/0F 100%）、sel-based innerHTML 同 turn 读视图（R220 视图桥第一步，MO inner-outer 100%）、tree-order 诊断 + doc 作用域裸 `*` 结构元素归位；**R305–R311（工厂域 identity 归一）**：tree-order 三上下文四层根因修复、iframe 工厂域查询 identity 归一（**Element-matches 530P/139F→675P/0F 100%**）、pending-fused 子树查询首片 + pending-removed 过滤（querySelector 全族 2055P/0F）、CDATA 计入 textContent 联接（Node-properties 726P/0F 100%）；**R312–R318**：trusted 事件语义三件（UA 合成置 trusted / redispatch 100%）、disabled 表单元素 click() 门（Event-dispatch-on-disabled 100%）、regraft 后 handle 兄弟链 identity 归一 + walker 着陆点 relocated 置位（**TreeWalker-walking-outside-a-tree 全文件转绿**）、doctype 品牌/元数据惰性读 + ParentNode `in` 可见性 + 元素子 getter 融合路由（dom/nodes 六文件收口，-6F）、**children 集合融合路由 + live 维护（HTMLCollection live 边缘收口，dom 全量 54140P/59F）**。
- **js-dom M4 收口（R319–R339，MO 全族清零 + DC-2 QuickJS parity 达成）**：**R319–R325**：wholeText contiguous 语义（Text-wholeText 全文件转绿）、replaceWith/insertAdjacent 的 DocumentFragment 参数展开、**L2 查询归并切片（R322 查询视图 handle 子树归并 + R323 tree-order 归并，querySelectorAll 逐位 identity）**、备档集巡检两件轻量修复（Document-createCDATASection HTML 守卫 + PI nodeValue 分流）；**R326–R331（执行路径测绘）**：R326 降级件精准重放——**Node-mutation-adoptNode 全文件转绿（三年备档收口）**、**`<template>` 内容 inert 化（页面脚本提取跳过 template 内联脚本，spec 正确性切片）**、WPT variant 基建最小支持 + document IDL on* 全族、getElementsByTagName 文档 HTML-ness 语义三层修复（change-document-HTMLNess 全文件转绿）、dispatch listener-swap 快照语义 + 归并 identity 双源去重（**vue_reconciliation 3/3 复活**）；**R332–R336（MO record 补口）**：normalize childList MO record、children live 集合归属收窄、sel 子移动族 childList record（pending 13→1）、**parsed-text splitText——MutationObserver-childList 38/38 全 Pass，本文件自 R37 导入以来首次全绿**、MO record 三处补口（同值 attr set / handle-text 编辑 / outerHTML）——**MutationObserver 全族 134P/4F/0T，4 个文件级 Timeout 全消除**；**R337–R339（巡检收口）**：events 备档集 Timeout 复核定性与 DC-8 path-objects 深项勘误（零源码改动）、append-domain identity 基线 + disabled form click() activation 回归、**DC-2 的 QuickJS feature 同页对齐达成（Vue e2e gate 放宽为双引擎，quickjs 下 3/3 全绿）**。
- **js-dom M4 深收口（R344–R368，sweep 55493P、已知 Fail 集 17→10）**：**R344–R348（events 域）**：execute_script live 查询源刷新（stale Rc 根因闭合）、transitioncancel/animationcancel 取消语义、testdriver 点击重试、user action 后恢复共享 mutation 队列绑定——**disabled-elements 全文件 9/9 全绿**（R342 时 4 Pass + 文件 Timeout）；**R349–R355（ranges data 族 Timeout 根治 + L2-d3 RFC v0.3）**：Range-mutations data 族文件级 Timeout 根因修复（live-range 注册表每条目 proxy trap/host 往返，**+789 subtest**）、R351 延续收口（**+1333 Pass，appendData/deleteData/insertData 三文件自导入以来首次全绿**）、L2 深水区专项总装（**L2-d3 RFC v0.2→v0.3**，性能证据 E1-E4 入册）、`_zwQueryKey` 归一缓存键统一 helper（d3d-r1 首片）；**R356–R361（live 集合与批内移动）**：getElementsByClassName sel/handle 分支 live 集合、children scoped liveSpec 重引入（两个 "should be a live collection" 转绿）、iframe 工厂元素 compound QSA 收口、Document-URL 重定向语义（X-Zero-Final-URL 消费链）、批内 detach→insert 移动语义 detached-stash（**Event-dispatch-target-moved 文件级 crash 转绿**）；**R362–R368（CE registry 专项 + DC-4）**：R362 勘误（嵌套 insert 重复 connect 为测试装配伪影）、custom_elements.rs 覆盖率 89.0%→91.9%（**DC-4 <90% 候选清单清零**）、CE registry 三片收口（per-realm registry 实例 / 创建路由 node-document realm 查表升级 / **native CE hooks per-realm 查表 Rust↔JS ownerId 域判据贯通**）、**无身份 plain 元素 append 的 host 身份盖章——node-realm-adoption-after-frame-removal 整文件转绿，已知 Fail 集合 11→10，sweep 55493P**。
- **js-dom M4 续收口（R370–R384，已知 Fail 集 10→6，M5/M7 双达成）**：**R370–R378（域导入 + 专项）**：cross-realm MO 观察链三件（cross-realm-callback-report-exception 整文件转绿）、作用域 QSA 空 host 归并 + replaceWith pending 重键（querySelector-mixed-case 转绿）、per-realm in-flight event（events onerror 跨 realm 转绿）、dom/abort 域导入 + AbortSignal realm 面、dom/lists 域导入 + DOMTokenList 品牌化（**36P/153F→189P/0F 全绿**）、dom 根目录散用例导入 + Element 反射族 gate（sweep +115）、插入期脚本执行（fragment-flatten 与直接插入路径）、**pending-apply 生命周期专项 RFC 化**（`docs/specs/js-dom-pending-apply-lifecycle-rfc.md` v0.1→v0.3）；**R379–R384（pa2 落地 + M5/M7）**：pa2 apply 代际令牌（M6 replaceWith 移除标记 + 换代清理 + host→shim apply 完成钩子）、sel-domain fused innerHTML + clone pure-text registry children（remove-next-sibling 1F→0F）、apply-generation gate 回归修复（vue_mount_lands 双份渲染）、**M7 QuickJS native_dom default-on（GB-20260829 用户批准）**、**M5 V8 `native_dom` default-on land + kill-switch 删除（DC-1 第一、二项双达成：`ZW_NATIVE_DOM` env 读点 / `WebViewConfig::native_dom` 字段 / 五处守卫全删）**——workspace v8 16814P/0F + quickjs 矩阵全绿。
- **js-dom goal 收官归档（R385–R391，2026-08-31，Active→Done）**：**R384 fast-path native install**（消除 per-execute template rebuild 的 flip bench 回归）+ **R385 final verify**（cfg-gate 修 quickjs-only build E0560 后 `make test` 18504P/0F 全绿）→ **DC-1~8 全达成**（逐项判定矩阵 `evidence/2026-08-31-r385-final-dc-verify-matrix.md`）；**R386 多进程 worker native-bindings 切片**（browser/renderer/engine 三端页面 JS worker 安装原生 DOM 绑定 + DC-1 verify addendum + 定向 bench-gate 42/42 PASS）；**R387 pa4-lite parse-backlog MO replay + appendChild 动态 classic 脚本执行**；**R388 MO-document 剩余 2F 重分类至 parse-position 域**（probe 证伪 L2-identity 假设，M4 sweep 55808P/11F）；**R389 combined-tree 全量门 18510P/0F**（layout-stream 合并后）+ DC-6 自一致性 sweep；**R390 DC-3 import surface 达上游天花板**（dom/observable tentative-only，账本一致性核对）；**R391 入口文档状态 Active→Done**；goal 目录经 `git mv` 归档至 `docs/goal/archive/js-dom/`（mode A，canvas-2d 先例），全仓引用同步更新。
- **CSS 简写/属性语法校验批量（R3470-F–R3555-F）**：scroll edge、grid-template、border-image 组件、text-emphasis、font-variant、columns 宽度、background size/attachment、transition/animation 的 fallback ident、iteration count 与 duration grammar、margin/border-width/gap/outline-width/outline-offset/positioned offset/font-size/scroll-margin/scroll-padding/contain intrinsic size 等无效语法拒绝/硬化（css-parser → style-system 双路径），持续收口 CSS 解析与 Chromium 行为差距；**CSS 支持类续推（R36xx–R37xx）**：logical border 简写、grid-template 简写、grid track feature queries、place/font-family/font-size/animation shorthand feature queries、quoted font family 正确分词、`env()` 大小写不敏感解析等。
- **样式与布局修复（2026-08-25）**：`box` / `gap` / `origin` 简写解析改为顶层空白感知（CSS Syntax 3 component-value，`split_top_level_whitespace`），`calc()` 等数学函数内部空白不再被误拆为多个值（style-system shorthand）；垂直书写模式下 bidi 文本 run 按方向分组（layout-engine inline，新增 `vertical_bidi` 测试簇）。
- **CSS 数学长度与布局修复（2026-08-27/28）**：`flex-basis` / `flex` 简写、`column-width` / `columns`、`contain-intrinsic-*`、`font` 简写 size 位、`background-size` 与 `background` 简写接受 `calc()` 等数学长度（css-parser / style-system 双路径）；BFC 空子元素的 margin 保留在 BFC 内部（layout-engine）；`contain: layout / paint` 的 margin collapsing 与子元素隔离（layout-engine）；content 解析器非 ASCII 崩溃修复（css-parser）。
- **line-clamp 与 SVG intrinsic 系列（R3762–R3770b，渲染流）**：SVG target-viewport 注入（R3762）+ table `box-sizing: border-box` UA default（R3763）、mixed abs+%+viewBox SVG ComputedIntrinsic（R3764，vector cluster 100%）、aspect-ratio transferred block-size 豁免 R1743 parent backfill（R3765）、**`line-clamp: auto` block-size 约束钳制 + `lh` 单位（R3766/R3767）**、跨块 line-clamp：skip-after-clamp-point（R3768）、paint 侧 cap=0 glyph 截断 + paint_node hidden 子树泄漏（R3769）、pre-text 容器高度塌缩三根因（remeasure preserve flags + remaining=0 hide + R109 anon multiline backfill，R3770）、祖先高度 compaction + ellipsis host + CB-contains abspos survival（R3770b）。
- **渲染兼容性 corpus 批量翻绿（R3800–R3807，周累计 +331）**：替换元素 §10.4 min/max 约束表（w→h→w 链式定点，R3800）、§10.4 约束表 border-box frame 折算（R3801）、`border-style: none` 边不计入 §10.4 border-box frame（R3802）、collapse 胜出边 transparent 颜色误黑（R3803）、**样式键缓存对兄弟组合器（`+`/`~`）误判修复——corpus +73 案翻绿（R3804）**、Row/Column 边框参与 collapse 左右缘冲突 §17.6.2.1 优先级补全（R3805）、`content: url()` 伪元素图片三段修复（R3806，+3 案）、零位移 float 不构成 BFC 回避约束（R3807，+1 案）。
- **渲染兼容性续推（R3809–R3845，corpus 13568→16814）**：**表格布局簇（R3810–R3830）**：col/colgroup 边框列宽 accounting（+3）、table-caption margin-bottom 计入表高、caption BFC + 表内 calc margin 残差、table 直接子块匿名 cell 包裹、匿名 cell 臂三分野修正（R3816「互斥」证伪为 box.float 失效伪象）、匿名行排序优先级（CSS §17.2.1 源序）、visibility:collapse 折叠行邻接 border-spacing 剔除（CSS Tables §4.1）、col min/max-width clamp 顺序（§10.4 min 优先）；**背景/盒模型簇（R3818–R3821）**：canvas 传播背景 anchor 改根 padding-box 原点（+4）、非替换 inline 垂直 padding 归零（CSS2.1 §10.8.1）、根元素百分比 margin 定位 border-box（CSS §8.3）；**3D transform 簇（R3831–R3834）**：奇异 transform 子树抑制（矩阵不可逆不渲染）、scaleZ() 解析与接线（2D 投影恒等）、rotateX/Y 2D 投影 + 渲染器 transform 采样双重 origin 修正（**transform3d 30 案净翻绿**）、transform 内 fixed=scroll 降级；**bidi/counter/display:contents 簇（R3835–R3845）**：inside marker 内容偏移 + CSS Counter Styles §6.2 CJK/日/韩 15 样式、RTL bidi-override 行级源 run 组反转 + pair-gap 镜像修订（**bidi-box-model 45/45 全 PASS**）+ inline 级逐 run 字符反转、RTL 全 LTR run identity（**corpus 净+21**）、**R3844 新 surface css/display 导入 +213 案（corpus 16601→16814）**、R3845 flex/grid 容器 `display: contents` 子级穿透展开（净+3）；plateau-guard 组合态基线复跑 GREEN（R3843）、oracle 重录执行 verified NO-OP（R3841）。
- **渲染兼容性续推（R3846–R3874）**：**display:contents 全布局域穿透（R3846–R3849）**：block 流容器子级穿透展开（净+11）、unbox 判定抽共享谓词、contents 文本子提升 + text-only 虚拟匿名块 + paint 侧装饰守卫（净+8）、根元素 contents 块化（CSS Display 3 §2.6）；`filter: drop-shadow` 投影发射（复用 ShadowPrimitive 三遍 box-blur，R3851）；**aspect-ratio 语义簇（R3854/R3859–R3862）**：`aspect-ratio: auto && <ratio>` 恒 content-box、definite min cross→min main 传递、stretched grid item 轨道钳制 + ratio 传递、block-stretch 遇 definite inline size 时忽略、显式零 min-size 不阻塞 flex stretch transfer；replaced box 全宽时下推至 float 之下而非收窄（R3857）、abspos insets 对非根 positioned 祖先重解析（R3858）、Ahem 方块跨基线 `[b-0.8em, b+0.2em]` 定位（R3856）、**`::first-letter` 伪元素三切片（R3867–R3871：paint 侧 color、typographic letter unit 簇选取、Dutch ij 连字识别）**、`margin-trim` 对 self-collapsing edge child 双 margin 修剪（R3872）、**wpt-runner mismatch 阈值 0.5%→0.01% 对齐上游语义（R3866）**、`@media not <unknown-type>` 归一为 always-match（R3874，MQ not-unknown）；R3875-P/R3876-P shallow-scatter 五簇归因——剩余项全部超出 scoped-fix 范围或 RFC/feature-gated。
- **Service Worker 控制语义推进（service-workers M3-4 → M3-22，2026-08-20/21）**：claim 匹配 document clients、document controller 投影（`navigator.serviceWorker.controller`）、page→worker 消息派发（shim + `ServiceWorkerManager` + webview 测试资产）——M3 控制语义 + page-to-worker message 完成；**update cache matrix 完成（M3，真实 update job/script byte comparison/updatefound）+ dynamic import update（M3-22，worker 内动态 import 与 update 流程对齐）**；**SW runtime CacheStorage open/put/match 桥接完成（2026-08-22，SW 侧 `caches` 走真实 Rust 存储）**；**CacheStorage WPT 基线接入（2026-08-23：cache-match WPT baseline + serviceworker CacheStorage wrappers 扩充）**；**M2 fetch 基础落地（fetch runtime/manager/IPC foundation + browser-process 页面 fetch 路由，`Cache.put()` / `matchAll()` / `keys()` 与 `ignoreSearch` 透传，2026-08-24）**；**fetch throw-after-respondWith iframe WPT baseline（2026-08-24）**。
- **页面 JavaScript 运行时**：`script-sandbox` 提供 V8（`v8` crate，rusty_v8 更名）/ QuickJS 双后端 feature gate（V8 持久化 Context 复用；QuickJS 全矩阵 parity，136 个失败清零）、Web Worker、ES Modules、WebAssembly JS API 到 `wasm-sandbox` 的自动桥接。
- **P1a DOM/JS Bridge 原生化（R30xx 系列，已主体落地）**：fetch 真实化（GET 端到端 + 二进制响应 body 真实字节）、setTimeout 真实延迟、MutationObserver（characterData / childList addedNodes 回填 / attributeFilter / subtree）与 IntersectionObserver / ResizeObserver 真实回调、getComputedStyle 动态 inline 覆盖 + 计算值序列化、classList 完整 DOMTokenList、HTMLCollection/NodeList item/namedItem、表单控件事件（input/textarea/select、focus/blur/change、Tab 焦点导航）、Selectors L4、DOM 遍历/变异 API（querySelector/closest/dataset/cloneNode/insertAdjacentHTML/prepend/before/after/replaceWith/createDocumentFragment/innerHTML childList emission）、布局几何（offsetWidth/getBoundingClientRect/scroll 尺寸）。
- **P1b V8 原生 DOM 绑定（R3095 起持续落地）**：native DOM bindings 替换 polyfill 字符串桥——S0 PoC 验证（internal-slot 值传递 + weak-handle GC，native 比 polyfill 快 ~15.6×）、S1 原生只读属性族 + NodeId↔对象身份映射（kill-switch 默认关）、S2 生产接线 + 树写/属性写原生（createElement/appendChild/insertBefore/removeChild/setAttribute）、live Document 共享（`Rc<RefCell<Document>>`，原生写触发重渲染）、S3 查询原生（querySelector/querySelectorAll 全选择器引擎）、S4 EventTarget 原生 + host→page 原生事件派发/对象丰富化/dispatchEvent 冒泡/stopPropagation、节点导航/childNodes/nodeValue/textContent/cloneNode/contains、attributes NamedNodeMap + 完整 Attr 节点、innerHTML/outerHTML getter/setter 原生；RFC §3.2 dom_bindings 五子模块化闭合。后续切片：document.referrer 原生（R3176）、MutationRecord.attributeName 有效名（R3175）、SVG/MathML 命名空间在 innerHTML/outerHTML setter 保留（R3181）、fragment 解析 context 元素（R3182）、textContent/innerHTML/outerHTML setter null→empty（R3184）。**S5 customElements/Web Components 里程碑完成（R3262–R3269）**：TBD-1 class 继承验证（R3262）→ S5a native HTMLElement 基类 + class-extends → S5b customElements.upgrade/createElement('my-el') 原生实例 → S5c connected/disconnected 生命周期原生路径 → S5d attributeChangedCallback + HTMLElement Element 接口 → S5e 子树 upgrade（R3269）→ upgrade 时 observed attrs 回调（R3274）。
- **DOM/CSS 选择器一致化（R3277–R3284）**：`:disabled`/`:enabled` disabled-state 传播、`:required`/`:optional`/`:read-only`/`:read-write`、`:placeholder-shown`/`:default`/`:indeterminate`、`:any-link`/`:scope`/`:lang()`/`:dir()`、`:nth-child(an+b of S)`、`:target`、`:valid`/`:invalid`/`:in-range`/`:out-of-range` 约束校验伪类——DOM `querySelector`/CSS matcher 双路径一致化；form_state.rs 子模块抽取（R3280）。
- **文档 API 补充（R3254–R3261）**：window resize 事件派发、matchMedia change 事件、console 桥接 host tracing、TreeWalker 层级方法、document.currentScript、document.scrollingElement、innerText（textContent 近似）、document.designMode；CSSStyleSheet addRule/removeRule legacy 别名（R3276）。
- **模块与 Worker 完整化（R3087–R3094）**：动态 `import()` 外部模块、transitive module 递归 fetch、外部 Worker fetch（`__zw_fetch_script`）、inline DedicatedWorker 真实消息往返、外部 script 源码 fetcher。
- **DOM API 补充**：JS 跨文档导航（R3058）、`Element.checkVisibility`（R3074）、boolean 反射属性 set-false 修复 + `_REFLECTED_BOOL` 扩展（R3039/R3040）。
- **反射属性与序列化 spec 合规（R3185–R3208）**：reflected string attrs null→empty（id/title/lang/accessKey）、enumerated 反射 spec 化（dir/contentEditable/draggable/input.type 等）、toggleAttribute/getAttribute 返回语义、inline style latest-wins（handle 与 parsed 两路径）、cloneNode/outerHTML/CE old-value latest-wins、SVG/外部命名空间属性前缀序列化保留、insertAdjacentHTML position ASCII 大小写不敏感、outerHTML setter Document parent 守卫。
- **Canvas**：gradient / Pattern 逐像素光栅化（`CanvasStyle::sample_at`，linear/radial/conic，R3079/R3085）。
- **多进程与自动化**：图像解码独立进程 `apps/image-decoder`（D1，隔离编解码器漏洞）、WebDriver 服务 `apps/webdriver`（W3C 协议骨架，wdspec 第一步）。
- **合成器进程（C2）RFC v2.1 五切片全部落地**：scroll transform bake、sync_token + Viz present、GPU mailbox fence + mmap 零拷贝、dma-buf fd 导出路径、owned window present surface、Linux landlock + seccomp（network/exec）沙箱、GPU device-lost 模拟 + CPU 回退、compositor crash E2E legacy 回退；Vulkan 真纹理 dma-buf 导出（跳过 read_pixels）仍为后续。
- **官网**：GitHub Pages 双语站点上线（[zeroweb.leizm.com](https://zeroweb.leizm.com)），含项目更新 feed（weekly cron 更新）。
- **字体（字体栈重建切片 R3230-F–R3344-F）**：OpenType features 贯通（font-face feature defaults、CSS feature precedence、shaping 携 feature）、generic layout advance 统一（fontdue/rustybuzz 对齐）、ordered fallback faces 全链路（shaping→IFC→layout→paint）、shaped fallback default-on（R3243-F）、**CSS Fonts 4 two-value `font-size-adjust` 全栈贯通（R3245-F，css-fonts Oracle 净改善 14.34pp）**、font-synthesis 属性（R3248-F）、font-size 绝对关键字 + HTML `<font size>` 映射（R3247-F）、font-variant 族（caps/numeric/east-asian/position + shorthand 展开与 font-stretch，R3250-F–R3253-F）、bidi override mirroring（R3319-F）、@font-face stretch/相对度量（R3341-F–R3344-F）；`unicode-segmentation`（pin =1.12.0）依赖加入。
- **HTML 行为兼容赛道（2026-08-12 启动）**：规范驱动并行开发线——表单兼容性基线 + 共享动作事务核心（form 动作/文本编辑/焦点经共享计划路由 renderer/tab worker/webview）、可取消文本输入事件、form POST 导航、无 JS 保留默认动作、稳定页面节点身份、焦点与 label 激活对齐；规格见 `docs/specs/html-behavior-compatibility-spec-rfc.md`，常驻断言见 `tests/integration/src/html_compat.rs`。
- **wgpu 24→30 升级**（R3275）：一次跨 6 个主版本（原 backlog 目标 29），compositor 真 dma-buf 前置；升级后全 workspace 门禁通过。
- **Canvas**：Path2D svgString 构造器 + SVG path data parser（R3307）、Path2D 复用 + `ctx.fill(path)`（R3306）、`createImageBitmap`（ImageData/HTMLCanvasElement 源 + source-crop 选项 + ImageBitmap drawImage，R3309–R3311）、OffscreenCanvas 主线程 + `transferToImageBitmap`（R3312）、`HTMLCanvasElement.transferControlToOffscreen`（R3313）、resize 重置 bitmap 与绘制状态（R3308）。
- **GPU 渲染**：draw_order 逐图元绘制修复 DC-10 分桶 z 序缺陷（R3277）、clip/blend 双 pass 源层混合（R3278）、窗口模式滤镜/变换离屏 ping-pong（R3279）、分桶路径 clip（R3284）、GPU 阴影模糊离屏画（R3287）、repeating 渐变首色标≠0（R3289）、inset 阴影 GPU + box blur 逐像素对齐 CPU（R3290/R3291）。
- **DOM/CSS 伪类与文档 API**：`:defined`/`:blank`/`:fullscreen`/`:modal`/`:focus`/`:focus-visible`/`:focus-within` DOM/CSS 一致化（R3299–R3302）、元素滚动 RFC S1/S2（ScrollEventParams cursor_x/y IPC，R3298）、DOMRect/DOMRectReadOnly 构造器（R3319）、navigator.serviceWorker 迁移 B-gen shim（R3318）、input.valueAsDate + stepUp/stepDown（R3317）、全局 ImageData 构造器（R3297）。
- **安全与存储修复**：CSP source-expr host 匹配 + mixed-content scheme 大小写（R3342/R3343，真安全绕过修复）、Web Storage auto-inc generator 显式数值 key 修复（R3341）。
- **zero-psl crate**（R3380）：公共后缀列表（PSL）解析与注册域名（eTLD+1）提取，接入 site-isolation；工作区 27→28 member。
- **Canvas WPT 驱动兼容性批量修复（R34xx）**：line-styles 33/33（真 join/cap/miter 几何 + setter 校验）、shadows 50/61（形状 alpha、可见区裁剪、真 join/cap）、compositing（source-out/Clear ops、uncovered-clear、composite enum）、gradient 语义（CTM 变换坐标、零长度、nonfinite、stop 分组插值）、pattern（验证/setTransform/图像错误/零尺寸）、text（fonts.ready settle、align/baseline、setter 校验）、roundRect 角对半径、CSS Color 4 getter 保留 `color()` 形式、clip draw_shadow_path 可见区裁剪。
- **js-dom M4**：native `createProcessingInstruction` + DOMException 身份一致性（polyfill/native 双路径对齐，classList/createElement 校验异常）、testharness-dom WPT 基线（DC-3，testharness local .js inline + baseline truthing）。
- **字体与 net**：WOFF2 webfont 安全解码（R3375-F）；net 修复（R3339 redirect 测试服务器完整请求头读取、R3367 dotdot Windows `to_file_path` 语义）。
- **浏览器打包**：compositor 随 launch/release 构建发布（`make browser` 与发布产物含合成器）、production chrome parity 证据集。
- **js-dom M4（原生/聚 polyfill DOM 对齐，R38–R49）**：MutationObserver 记录语义完整化（childList fragment 展开、NS 记录、no-mutation 守卫、classList parity、批量 id 重命名链、attributeOldValue 预捕获、observe options 校验、CharacterData 编辑 + SetChildText characterData 记录）、HTMLCollection legacy 索引/命名属性语义 + `Element.children` 返回 HTMLCollection、Range/StaticRange/Attr 端点 API、TreeWalker/NodeIterator 遍历 API、document/window slot-tagged 联合派发链、pre-set stopPropagation 标志跳过全部派发监听。
- **Canvas WPT 批量修复（R34xx 续）**：G7 聚类全灭（variationSelectors / ctor.basics / index-from-offset 边角）、`ctx.lang`、WPT 验证证据 662 Pass / 89.28% 覆盖率。
- **net resource loader 重构**：guarded DNS prefetch、异步 HTTP 连接预暖、资源请求/结果桥队列上限、动态模块 fetch 集中调度、HTTP2 优先级与协议遥测、截断流式响应测试覆盖。
- **字体栈续（R3422-F–R3426-F）**：可变轴贯穿光栅 IPC（R3422-F）、author face 布局 advance 对齐（R3424-F）、layout overrides Rc 共享 + 组合 memo（修 R3424-F 默认开启后 layout 10× 回归）、generic CJK contiguous 保留 opt-in（R3426-F）。
- **产品版本号**：`crates/product-version` 从构建日期推导版本。
- **渲染兼容性度量**：导入上游真实 WPT reftest（约 9967 个）、`make reftest-oracle` Chromium Oracle 像素一致率（诚实通过率）、`make product-smoke` / `make product-smoke-legacy` 产品回归门禁、`make import-wpt` 测试资产化流程。
- **性能预算体系**：`make bench-gate` / `make bench-capture`（测量 + 门禁比较 + 趋势，perf-gate）。
- **工程**：ci-watchdog 夜间 CI 任务、QuickJS 矩阵纳入 `make test`（v8/quickjs 接口一致性门禁）、QuickJS 后端完整化（Sandbox trait 抽象，aarch64 release 以 QuickJS 替代 V8）。
- **运行时配置集中化**：新增 `zero-runtime-config` crate（工作区 28→29 member）——`ENVIRONMENT_VARIABLES` 权威清单 + 统一解析函数（`enabled_when_true` / `enabled_by_default` / `enabled_unless_zero` / `positive_usize` 等），业务 crate 不再直接读环境变量。
- **Canvas 色彩管理**：f16 浮点像素存储 + linear 色彩空间（color-type 4/4）、display-p3 色彩管理（wide-gamut 9→12/12）——色彩空间经 GPU 管线贯通。
- **Canvas 滤镜与 GIF**：colorMatrix 滤镜渲染（filters 目录 13/13 全绿）、GIF 首帧解码（drawImage.animated.gif + pattern.animated.gif 全过）。
- **Canvas 路径几何（R34xx + js-dom M8 path-objects）**：arcTo 真切线弧（含无子路径 moveTo、负半径 IndexSizeError）、arc 归一化全圆/幅度语义 + anticlockwise 整圆特例、isPointInPath/fill 的 nonzero 绕组 + fillRule/Path2D 形式、ensuresubpath 语义 + clip 相交/空交集、椭圆负半径校验、曲线/弧段数自适应 + stroke 像素方形覆盖、stroke 零长段剪除 + roundRect 显式闭合 + 环绕 join、worker OffscreenCanvas 接口面——path-objects 用例 62F → 3F。
- **布局与文本测量（ZRG-2026-08-15）**：布局文本宽度改 hmtx 真实测量（替换 estimate 启发式）、paint 字符 advance 按字形实际字体测量（engine/browser 双端）、FreeType measure_advance hinting 取整致英文文本字距错乱修复——跨平台换行点一致（CI watchdog 同步修 shaping 基线跨平台字体 + net async redirect deflake）。
- **多进程与渲染**：renderer JS 状态跨文档加载隔离（lexical state 测试覆盖）、html5test.co 分数在多进程浏览器中正确渲染、compositor scrolling viewport repaint 修复、browser 构建缓存跨启动保留。
- **Canvas 2D goal 完成（R57 终态）**：路径填充边界 AA（非轴对齐 CTM 4×4 超采样）、渐变插值空间补全 CSS Color 4 全 16 空间（+DisplayP3/DisplayP3Linear 等）、setFilterDropShadow/setGradientInterpolation bridge op、逐格独立对齐 + TextCluster 字体锁定——Chromium Oracle 不一致归零（41/41 100%），Mission 中期 80% 达成、DC-1~4 全部满足。
- **表单验证（form-validation M1-M3）**：validationMessage 标准消息、requestSubmit 交互验证、提交阻断全链路（form-requestsubmit 全灭）、:invalid/:valid 匹配、BUTTON type 读取、step/pattern/date/time 校验矩阵、radio 组级 required、ValidityState——WPT 919/0 全灭。
- **js-dom M4 遍历 API 完整化（R82-R86）**：whatToShow unsigned + referenceNode 同步（R82）、walker full-nodeType mask + fresh-start + handle before/after（R83）、traversal 兄弟链同 identity + NodeIterator detach/重入守卫 + TreeWalker filter 剪枝（R84）、TreeWalker 导航式重写 + previousNode 规范镜像（R85）、detached 子树保留 + NodeIterator 移除 retarget（R86）——traversal 1527→1575P。
- **浏览器多进程重构**：require isolated process pipeline、release 二进制移除 page runtime（script runtime optional）、Windows caption button 行为恢复、空白标签页地址栏 ZeroWeb 图标、renderer 空闲 JS 轮询移除。
- **IndexedDB Rust 原生路由**：IndexedDB 工厂 schema / 事务 wire / object store / index / index cursor / Date key / graph 值保留 / continuePrimaryKey / 引擎请求事件对齐 + 事务任务生命周期 / per-origin 注册表 / 数据库持久化——storage/engine 双端接线，cursor 步进路由到 Rust（storage 696 测试）。
- **布局/样式/绘制性能（R3437-F–R3442-F）**：paint/IFC/构树 feature 开关进程级快照（getenv 热循环移除）、ComputedStyle 只读借用 + 父样式 owned 借用、style map 精确预留、连续 IFC 度量写入去重、字体 alias 精确查找、trusted NodeId SplitMix64 快哈希、post-order inline-block IFC 复用——medium layout/style/paint p95 多项降 3–41%，bench-gate 16/16 全 PASS。
- **js-dom lit 框架端到端（R97-R99）**：lit 首渲染落地（walker 跨树重定位 + fragment 视图插入展开）、lit 响应式更新链（define observedAttributes + CE accessor 派发）、lit 事件链（_zwMEl 事件面 + 惰性 fonts.ready fallback）——Web Components 渲染/响应式/事件交互三段全通，WC 端到端闭环。
- **官网**：新增 WPT 与性能趋势页（GitHub Pages 趋势可视化）。

### 变更

- 工作区从 20 个 member 扩展到 31 个（新增 `renderer` / `page-runtime` / `product-version` / `psl` / `image-decoder` / `compositor` / `webdriver` / `icon-gen` / `runtime-config` / `android-browser` / `media`）。
- 测试规模从约 1,000 增长到 16,000+（2026-08-25 静态统计 16,888 个 `#[test]`，v8 feature 全量全绿）。
- 跨平台打包：macOS `.app` bundle + zip、Linux AppImage / .deb、Windows .zip / NSIS 安装器，配套 CI release 工作流（`v*` tag 触发）；release zip 按平台分装、macOS 打包版本号按构建日期推导。
- 渲染兼容性：`freetype-raster` feature 默认开启（FreeType 替代 fontdue 光栅化，broad 一致率显著提升）；WPT reftest 对齐 Chromium Oracle（self-source 约 77%、oracle 真一致约 47.5%）；2026-08-09 字体栈重建 RFC v0.2.3 获批，恢复主动实施。
- 性能：停止页面加载后 CPU 空转、合并渲染进程 pending frames；`render_with_dom_mutations`（JS DOM 变更直接应用到 live doc，跳过 HTML round-trip）+ 增量样式/布局/绘制；`Arc<LayoutBox>` 消除整树深拷贝、cascade 借用去 3 次 String 分配、tokenizer 字节流扫描、stylesheets/query-doc 解析缓存、glyph cache 免 O(n) 驱逐、跨帧 advance 缓存、DOM 遍历合并。
- CI：benchmarks job 授予 `contents: write`（perf 基线/趋势回写 main，修复 403）；Lato-Medium.ttf 纳入 git-tracked fonts 目录（include_bytes 打包修复）。

### 文档更新

- 重写根 `README.md`，补齐开源协作入口、贡献说明和风险提示。
- 新增面向贡献者的文档：`CONTRIBUTING.md`、`CODE_OF_CONDUCT.md`、`SECURITY.md`、`docs/architecture.md`。
- 在 `.github/` 下新增 issue forms 和 pull request 模板。
- 在关键文档中统一强调：项目仍是实验性代码库，默认不应视为可直接用于生产环境。

## [0.1.0-alpha.0] - 2026-05-30

这是 ZeroWeb 工作区的首个公开预发布版本。

### 新增

- 建立面向浏览器内核的 Cargo workspace，拆出 DOM、CSS 解析、样式计算、布局、渲染、宿主运行时、网络、存储、IPC、WebView API、Canvas 2D 和 WASM 沙箱等模块边界。
- 打通 `render-foundation` 与 `host-runtime` 的基础集成，并提供基于 `wgpu` 的 `apps/webview-demo` 演示程序。
- 新增 `dom` crate，集成 `html5ever` 并提供 DOM 树操作能力。
- 新增 `css-parser` crate，覆盖 tokenizer、parser、selector 和 CSS 值解析。
- 新增 `style-system` crate，覆盖 cascade、inheritance、computed values、selector matching 和 shorthand expansion。
- 新增 `layout-engine` crate，在 `taffy` 之上整合 block / flex / grid 布局能力。
- 新增 `engine` 渲染管线，覆盖 paint、dirty tracking 和 compositing 基础能力。
- 新增 `net` 与 `security` crate，覆盖 URL、导航历史、Cookie、同源检查、CORS 和 CSP 基础能力。
- 新增 `protocol` 与 `storage` crate，覆盖 IPC 消息、localStorage、sessionStorage 和 IndexedDB 基础能力。
- 新增 `canvas` crate，提供 Canvas 2D 图元和绘制能力。
- 新增 `webview` crate，作为可嵌入渲染和生命周期 API 的对外入口。
- 新增基于 `wasmi` 的 `wasm-sandbox` crate。
- 新增跨 crate 集成测试、criterion benchmarks 和 GitHub Actions CI。

### 变更

- 把工作区测试规模扩展到约 1,000 个测试。
- 持续增强 `style-system`，包括 selector matching、shorthand expansion 和长度值处理修正。
- 收紧项目文档，把许可证边界、AI 辅助贡献流程和实验性风险提示说明得更清楚。

### 已知限制

- `browser-shell` 仍以占位实现为主，距离可用浏览器产品还有明显距离。
- `script-sandbox` 仍是占位 crate，页面级 JavaScript 执行尚未完成。
- 真实站点兼容性、WPT 覆盖率和生产级加固仍在推进中。
- 本版本仅面向学习、研究和工程探索。任何商用或其他生产用途都需要自行评估风险。
