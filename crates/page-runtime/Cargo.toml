[package]
name = "zero-page-runtime"
description = "WPT / TabWorker / zero-renderer 共享的页面运行时契约与处理逻辑"
version.workspace = true
edition.workspace = true
license.workspace = true

[features]
default = ["v8"]
v8 = ["zero-engine/v8"]
quickjs = ["zero-engine/quickjs"]

[dependencies]
zero-engine = { workspace = true }
zero-render-foundation = { workspace = true }
