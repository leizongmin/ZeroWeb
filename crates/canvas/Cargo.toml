[package]
name = "zero-canvas"
description = "Canvas 2D 实现 — 渲染目标、路径、图像数据"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
zero-render-foundation = { workspace = true }
# R34xx：measure 墨迹亚像素 bbox（字形轮廓，位图在亚像素字号下量化）——
# 2d.text.measure.actualBoundingBox.small-font。rustybuzz 经 workspace 同版本复用。
rustybuzz = { workspace = true }
thiserror = { workspace = true }
bytemuck = { workspace = true }

[dev-dependencies]
criterion = { workspace = true }

[[bench]]
name = "canvas_bench"
harness = false
